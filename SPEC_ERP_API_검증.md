# ERP API — 정본은 내보내기다. 검증을 통과해야 넘어간다

**2026-08-14 형님 지시**: *"API를 받아도 우선은 ERP 데이터가 우선이야 검증절차가
있어야돼, API 받고 나서 관리 메뉴 추가해"*

두 가지를 정한 것이다 — **정본 순서**와 **작업 순서**.

---

## 1. 정본 순서 — 무엇이 우선인가

| 층 | 무엇 | 자리 |
|---|---|---|
| **정본** | 사람이 ERP 화면에서 내보낸 엑셀 | `Z:\…\0. 원본 자료\1. ERP 내보내기` → `reports/ERP판매_프로젝트색인.json` |
| 후보 | API 로 받은 것 | `reports/ERP판매_API색인.json` (**정본을 덮지 않는다**) |
| 관문 | 둘의 대조 | `erp_api_parity.py` → `reports/ERP_API대조.md`·`.json` |

**API 가 열려도 자동으로 갈아타지 않는다.** 관문을 통과하고 사람이 승인해야 한다.

### 왜 — 이 수치는 돈이다
공급가액·진행상태가 정산·계산서·수금 판정의 근거다. API 응답이 기간·집계·상태
낱말에서 조금만 달라도 화면은 **오류 없이** 다른 숫자를 보여 준다.
빈 값은 눈에 띄지만 **틀린 값은 안 띈다**(`[165]`).

---

## 2. 계약 — API 쪽이 지킬 것

API 로 무엇을 어떻게 받든, 결과는 **정본과 똑같은 모양**으로 적는다:

```jsonc
{"src": [...], "count": N, "as_of": "2026-08-14T14:05:24",
 "index": {
   "UJ2600050": {"supply":433000, "vat":43300, "total":476300,
                 "state":"4.세금계산서발행대기", "date":"2026/01/07",
                 "po":"", "cust":"강서1MB(가양A)", "rows":1}
 }}
```

- 칸 이름은 **영문**이다(`state`·`supply`·`cust`·`po`). 한글로 물으면 모든 번호가
  '상태 없음'으로 나오는데 **오류는 안 난다**(`[165]` 와 같은 함정).
- 금액은 **원 단위 정수**. 반올림·천원 변환 금지.
- 같은 UJ 가 여러 행이면 금액은 합산하고 상태는 **가장 덜 진행된 것**을 남긴다
  (한 건이라도 미발행이면 그 프로젝트는 안 끝난 것이다 — 낙관 금지).
  정본이 그렇게 만든다: `erp_sales_index.build_one()`.

---

## 3. 관문 — `python erp_api_parity.py`

| 판정 | 뜻 |
|---|---|
| `미실시` | API 색인이 아직 없다. **정상이다** — 정본만 쓴다 |
| `열쇠확인` | 겹치는 프로젝트NO 가 절반 미만. **값 이야기가 아니라 조회조건 이야기다** |
| `미통과` | 차이가 있다. 전부 설명된 뒤에만 넘어간다 |
| `통과` | 비교 구간 안에서 차이가 없다 |
| `통과(승인)` | 차이를 사람이 설명하고 `--ack` 했다 |
| `못함` | 정본 색인을 못 읽었다. **'이상 없음'이 아니다** |

### 이 도구가 지키는 것
- **읽기 전용.** 색인을 고치지도, 큐에 넣지도, 엑셀을 열지도 않는다.
- **기간이 다른 것을 불일치라 부르지 않는다.** 두 색인의 날짜 범위가 겹치는
  구간 안에서만 비교한다. 안 그러면 3월치만 받아 온 날 1·2월 전체가 '정본에만'으로
  쏟아지고, **경보가 대부분이면 아무도 안 본다**(`[170]`).
  실측 시험: 정본 3건 · API 1건(3월) → `통과` · 정본에만 **0** · 기간밖제외 2.
- **겹침이 절반 미만이면 값을 세지 않는다.** 짝이 안 지어진 것을 전부 '값이 다르다'로
  내놓으면 사람이 **없는 오류를 찾아 나선다**(`[172]`).
- **승인은 그 차이에만 유효하다.** 지문에 불일치 목록이 들어가므로 새 차이가 생기면
  승인이 저절로 풀린다. 실측 시험으로 확인했다(승인 → 금액 바꿈 → `미통과` 복귀).
- **API 색인이 없으면 '이상 없음'이 아니라 '미실시'다**(`[169]`).

---

## 4. 작업 순서 — 형님이 정하신 것

```
① 인증키 신청  ──▶ ② IP 등록  ──▶ ③ 키 수령  ──▶ ④ 수집 붙이기
                                                    │
                                     ⑤ 관문 통과 ◀──┘
                                          │
                                          ▼
                                     ⑥ 관리 메뉴
```

**⑥ 은 ③ 뒤다.** 그전에 화면을 만들면 무엇이 오는지 모른 채 칸을 짐작하게 되고,
짐작으로 만든 화면은 고치는 것이 새로 만드는 것보다 비싸다.

### 지금 어디인가 (2026-08-14)
- ① 신청 문구 준비됨 — 형님이 화면에서 전송
- ② **`118.36.170.138` 미등록** ← **지금 막힌 것은 이것 하나다**
- ③ 실서비스 키·테스트 키는 **이미 있다**(만료 2027-07-23). 새 테스트키는 신청 중
- ④ **판매·세금계산서·수금 조회 API 는 존재하지 않는다고 삼중 확인됐다**
  (AGENTS.md 절대규칙 3). 이번 신청의 회신이 그것을 확정한다 —
  **회신 내용을 반드시 파일로 남긴다.** 없다고 답이 오면 이 문서의 ④~⑥ 은 접고
  화면 수집(Playwright) 쪽으로 간다.

---

## 5. 관리 메뉴 (⑥ — 아직 만들지 않았다)

**근거가 이미 있는 것만** 올린다. API 가 무엇을 주는지 모르는 채로 칸을 만들지 않는다.

| 칸 | 근거 파일 | 지금 값 |
|---|---|---|
| 인증키 상태 | `config/ecount_config.json` | 실서비스 · 만료까지 343일 |
| 테스트/실서비스 | 같음 (`IS_TEST`) | 실서비스(oapi) |
| 허용 IP | `config/erp_allowed_ips.json` + `erp_ip_guard` | **미등록** |
| 대조 관문 | `reports/ERP_API대조.json` | 미실시 |
| 정본 색인 | `reports/ERP판매_프로젝트색인.json` | 1,822건 · 원본 2개 |

- **읽기 전용으로 시작한다.** '지금 수집' 같은 단추는 ⑤ 통과 뒤에 붙인다.
- **인증키 값은 화면에 안 띄운다.** 마스킹된 길이·만료일까지만.
- ★ **기사(tech) 화면에 섞지 않는다** — 기사 링크는 카톡으로 돌아다닌다.

---

## 6. 아직 안 한 것 (분담판)

- **회귀 검증 항목** — 정본 경로 5곳이 API 색인으로 바뀌는 것을 막는 검사.
  `tests/synthetic_check.py` 가 **옆 세션 미커밋 변경 139줄을 담고 있어** 손대지
  않았다(같은 파일을 커밋하면 남의 미완성이 딸려 올라간다, `[104]`).
- ⚠ **정본 경로가 5곳에 문자열로 흩어져 있다**: `local_ai.py:83` ·
  `settlement_completion.py:125` · `webapp/app_server.py:5236·5353·5663` ·
  `po_reconcile.py:91(erp_idx)`. 갈아타는 날 한 곳만 고치면 **화면마다 다른 숫자**가
  나온다. 모으는 것이 ⑤ 전에 와야 한다.

---

## 이어받는 법

```
python erp_api_parity.py --print      # 지금 관문 상태
python ecount_client.py --check       # 인증까지 (조회 API 는 안 부른다)
```
