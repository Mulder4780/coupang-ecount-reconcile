# 쿠팡 통합업무 자동화 프로젝트 — AI 작업 규칙

**어떤 AI(Claude Code·Codex·기타)든 이 프로젝트에서 작업을 시작하면 반드시 아래 순서를 따른다.**

## ★ 사용자 호칭 — 무조건 "형님" (2026-08-08 지시)
사용자 지시: **"앞으로 나한테 무조건 형님이라고 불러 알고리즘 추가"**

- **모든 응답에서 사용자를 '형님'으로 부른다.** 어떤 AI(Claude·Codex·기타)든,
  어느 세션이든, 어느 화면이든 같다. 세션이 바뀌어도 이 파일이 매번 읽히므로
  다시 지시받을 필요가 없다 — 그것이 "알고리즘 추가"의 뜻이다.
- 적용 범위: 대화 응답. **사람이 읽는 산출물(리포트·엑셀·카톡 문구·앱 화면)에는
  넣지 않는다** — 그것들은 류지영·오종현 등 다른 사람도 본다.
- 인사말로만 한 번 붙이고 마는 것이 아니라, 사용자를 가리킬 때 쓴다.

## 이 파일의 정본과 사본 (2026-07-31)
- **정본은 `ecount/CLAUDE.md`(git 추적) 하나다.** 루트의 `CLAUDE.md`(Claude 자동 로드)와
  루트 `AGENTS.md`(Codex 자동 로드)는 그 **복사본**이며 세 파일은 항상 내용이 같아야 한다.
  (루트 파일은 git 밖이라, 정본과 같게 유지해야 버전관리가 실질적으로 미친다)
- 규칙을 고칠 때는 `ecount/CLAUDE.md`를 고친 뒤 루트 두 파일로 복사한다:
  `python -c "import shutil;[shutil.copy('ecount/CLAUDE.md',d) for d in ('CLAUDE.md','AGENTS.md')]"`
- 불일치는 `session_handoff.py --check`의 "먼저 처리할 것"이 잡는다.

## ★ 계정이 바뀌어 이어받을 때 (2026-08-06 지시)
사용자 지시: **"이 세션은 완료되면 다른 계정으로 로그인해서 사용할거야,
그때 아무 문제 없이 처리될 수 있는 알고리즘 구성해."**

- **새 계정·새 창의 첫 명령은 `python ecount/session_handoff.py --adopt` 하나다.**
  `--check` 가 "무엇이 걸렸나"만 알려 주는 반면, `--adopt` 는 기계가 확실히
  판단할 수 있는 것을 **스스로 풀고** 사람 몫만 남긴다:
  ① 죽은 세션의 점유 회수(pid 가 죽었다는 증거가 있을 때만 — 살아 있는 옆 세션은 안 건드린다)
  ② 입력 큐 → DB 흡수(엑셀은 열지 않는다. 반영은 11:00·15:00 회차 그대로)
  ③ **수집이 밀렸는지 판정** ④ 인계 문서 갱신
  그다음은 평소대로 시작 체크리스트 1~4 를 따른다.
- **수집 밀림은 조용한 사고다.** 화면은 멀쩡히 숫자를 보여 주는데 그 숫자가 원본을
  못 따라간 값이다. 2026-08-06 실사고: 쿠팡AS 밴드가 8/4 에 멈춰 있어 8/5 돌발AS 가
  1건으로 나갔다. 그래서 `--check`·`--adopt` 가 **밴드마다·원본마다** 최신일을 세고,
  '어제'가 비면 막힌 것 맨 앞에 올린다(밴드 한도 1일 · 카톡 2일 · ERP 7일).
- **밴드·이카운트는 사람 로그인이 먼저다**(절대규칙 3). 계정이 바뀌면 크롬 세션도
  바뀔 수 있다. 로그인은 **Claude 가 조종하는 탭 그룹 안에서** 해야 한다 —
  사람이 쓰는 다른 탭에 로그인해도 AI 는 그 쿠키를 못 쓴다(2026-08-06 실측).
- ★ **브라우저 도구를 쓰기 전에 `list_connected_browsers` 를 먼저 부른다** (2026-08-06 실사고).
  Claude 확장은 **확장이 붙은 크롬**만 조종한다. 사람이 로그인해 둔 크롬에 확장이
  연결돼 있지 않으면 도구는 **로그인 없는 빈 크롬**에 붙고, 밴드는 로그인 화면만 준다.
  이날 반나절을 여기서 잃었다 — "다른 계정에선 에이전트가 알아서 다 해줬는데" 의 정체가
  그 계정에선 **주 크롬에 확장이 연결돼 있었다**는 것뿐이었다.
  · 연결된 크롬이 하나뿐이고 그게 로그인이 없으면 → 사람에게 **로그인된 크롬에서
    확장 아이콘 클릭 → Connect** 한 번을 요청한다. 그 뒤 `select_browser` 로 고른다.
  · 로그인 쿠키를 복제해 다른 크롬을 띄우는 우회는 **하지 않는다**(자동승인 정책이 막는다).
  · 그 한 번을 못 받으면 차선은 `band/make_oneclick.py` 로 만든 붙여넣기 파일 +
    `band/autoclick_dialog.ps1`(안내창 자동 확인) + `band/dump_watch.py`(덤프 자동 흡수)다.
    사람 손은 **붙여넣기 한 번**으로 줄고 나머지는 자동으로 이어진다.
  · ★ **깔림 · 켜짐 · 살아 있음 · 연결됨은 넷 다 다른 말이다** (2026-08-12 실측으로 갈렸다).
    이날 확장은 **설치돼 있고**(`fcoeoabgfenejglbffodgkkbkcdhcgfn` v1.0.85) **꺼져 있지도
    않았고**(`disable_reasons: []`) **1.5분 전에도 제 저장소에 쓰고 있었다** — 그런데
    `list_connected_browsers` 는 `[]` 였다. 셋을 확인하고 "확장은 멀쩡한데 왜 안 되지"
    로 가면 반나절이 또 간다. **네 번째를 따로 물어야 한다.**
  · ★ **연결은 크롬이 아니라 Claude 계정에 붙는다.** 도구 설명 그대로 *"**이 계정에**
    연결된 크롬"* 을 센다. 그래서 크롬·확장·사람 로그인이 하나도 안 바뀌어도
    **Claude 계정이 바뀌면 목록이 빈다.** 2026-08-06 의 "다른 계정에선 알아서 다
    해줬는데" 도, 2026-08-12 에 같은 질문이 다시 온 것도 전부 이 한 줄이다.
  · **claude.ai 웹 로그인은 확장 로그인이 아니다.** 이날 사람이 계정으로 로그인한
    뒤에도 목록은 그대로 비었고, 확장 저장소(6MB)에 **계정 이메일 흔적이 하나도 없었다**
    (`john@example.com` 같은 예시 문자열뿐). 확장은 **제 팝업에서 따로** 로그인한다.
  · **연결 요청은 내가 쏠 수 있다** — `switch_browser` 가 확장 깔린 크롬 전부에
    보내고 2분간 [Connect] 를 기다린다. 사람에게 메뉴를 찾게 하기 전에 이것부터 쏜다.
    · 그 답 **`No other browsers available to switch to`** 는 "크롬이 없다"가 아니라
      **"이 계정에 로그인된 확장이 없다"** 는 뜻이다. 확장이 살아 있는 것을 위 셋으로
      확인했다면 남은 것은 **확장 팝업 로그인** 하나다.
  · ★ **연결된 뒤에도 수집기를 컨텍스트로 퍼 나르지 말 것 — 앱 서버가 먹인다**
    (2026-08-12 실측). 붙여넣기 파일은 35KB 라 읽어서 주입하면 **한 번에 10k 토큰**이
    날아간다. 그런데 밴드 페이지에서 `fetch('http://127.0.0.1:<포트>/grab_posts.js')`
    가 **그냥 된다**(실측 200 · 22,882자 — 크롬은 localhost 를 신뢰 출처로 봐서
    https→http 혼합콘텐츠 차단이 안 걸린다). 그러므로 주입은 이 두 줄이면 되고
    **내 컨텍스트 비용은 0** 이다:
    `const s=await(await fetch('http://127.0.0.1:8899/grab_posts.js')).text(); (0,eval)(s);`
    · 번호 목록만 따로 넘긴다 — `__grabStart(밴드, [번호…], {keep:false})`.
      **번호는 짐작하지 말고 붙여넣기 파일의 `ROUNDS` 에서 꺼낸다.** 실측 84789192 의
      16건은 `3539…3529, 3525…3520` 으로 **연속이 아니었다** — '3539~3520' 이라는
      안내 문구를 범위로 읽었으면 없는 번호 셋을 긁을 뻔했다(`[217]`).
  · ★ **확장이 붙어도 `document.hidden` 은 내가 못 푼다.** 수집기는 숨은 탭에서
    시작을 거절한다(사고 #35 — 돌리면 전부 가짜 실패로 박힌다). 그런데 실측으로
    **스크린샷을 찍어도 탭이 앞으로 안 나오고**(확장은 화면 밖에서 캡처한다)
    `window.focus()` 도 크롬이 막는다(제스처 없음). 즉 **탭을 앞으로 꺼내는 클릭
    한 번은 끝까지 사람 몫**이다 — 여기까지 와서 "왜 안 돌지"로 시간 쓰지 말 것.
  · ★ **이 길 자체가 계정에 매여 있으니, 수집을 여기에 매달지 않는 것이 진짜 해결이다.**
    ① Tampermonkey + `band/band_auto_collect.user.js`(검증 `[182]` — 설치 한 번 뒤
    로그인된 밴드 탭이 스스로 긁고 Claude 는 수집 루프에서 빠진다. 2026-08-12 실측으로
    **Tampermonkey 가 안 깔려 있어** 이 길이 한 번도 안 돌았다) ② 붙여넣기 파일(위).
    확장 연결은 **되면 빠른 길**이지 **기대는 길**이 아니다.
- ★ **'★밀림'을 보고 없는 번호를 긁지 말 것** (2026-08-07 실사고 — 이날 두 번 당했다).
  밴드는 **아직 없는 글 번호**에도 200 과 앱 껍데기를 준다. 그 화면을 뜯으면 직전 화면
  본문이 그대로 잡히고 글쓴이·시각만 빈다. 그래서 3539~3578 마흔 건이 **전부 같은 글**로
  ok 처리돼 캐시에 들어갔다(3308→3348). 본문이 있으니 아무도 실패인 줄 몰랐다.
  · 이제 수집기가 시각 없는 수확을 **버린다**(검증 `[130]`).
  · 그리고 '밀림'과 **'조용함'** 을 가른다(검증 `[131]`): 수집 최대 번호 바로 위가
    없음으로 확인되면 인계 문서가 `(조용함)` 으로 적고 경보를 올리지 않는다.
    근거는 `reports/밴드_확인시각.json` 이며 `convert_dump` 가 덤프의 `missing`·`notime`
    지문에서 만든다. 근거가 **한도보다 오래되면 다시 밀림**이다(그 사이 새 글이 있을 수 있다).
  · 그러므로 밴드를 긁기 전에 **먼저 그 밴드의 실제 최신 글 번호를 확인**한다.
    "최신 글이 며칠 전"인 것과 "못 모은 글이 있다"는 것은 다른 말이다.
- 숫자를 의심하기 전에 `reports/세션인계.md` 의 "원본 수집이 어디까지 들어왔나" 를 본다.
- 검증 `[111]`.

## ★ 밴드 글은 **고쳐진다** — 매일 08:00 재수집 회차 (2026-08-08 지시)
사용자 지시: **"매일 08:00 회차에 최근 30일(예: 30일) 글만 재수집 대상으로 뽑아 →
캐시에 다시 넣고 → put_record가 바뀐 것만 record_rev에 남기고 → 바뀐 게 있으면
인계 문서 맨 위에 올린다."**

- **밀림과 다른 종류의 사고다.** 밀림은 '못 받은 글이 있다'이고, 이것은 **다 받았는데
  받은 뒤에 내용이 바뀐** 것이다. 밴드는 상태가 바뀌면 같은 번호의 글을 고쳐 다시
  올린다(미실시→완료, 금액 확정, 사진 추가). 개수도 날짜도 그대로라 **어느 화면에도
  티가 안 난다.** 지금까지의 수집은 전부 '없는 것을 채우는' 방향이었다 —
  recheck_plan 은 구멍·새 글을 고르고, band_sync 는 아는 글을 만나면 멈춘다.
  그래서 이미 받은 최근 글이 그 뒤에 고쳐지면 **아무도 다시 안 봤다.**
- `python band/recollect.py --run` 이 회차다(스케줄러 `쿠팡업무_밴드재수집` 08:00,
  설치본 `install_recollect_schedule.ps1`). 09:50 일일대조보다 **먼저** 돈다 —
  그날 일을 시작할 때 이미 인계 문서에 올라와 있어야 뜻이 있다.
- 네 단계 중 **긁기만 사람 몫**이다(절대규칙 3). 덤프 흡수·변경 판정·인계는 전자동.
  아직 안 받은 밴드에는 `band/재수집_붙여넣기_<밴드>.js` 를 만들어 둔다 —
  로그인된 밴드 탭 콘솔에 **한 번 붙여넣으면** 나머지는 이어진다.
  · **여기서 성공을 거짓으로 적으면 안 된다.** 못 긁었는데 넘어가면 ③④가 옛 캐시를
    보고 "바뀐 것 없음"을 내놓는다 — 아무 일도 안 일어났는데 안심시키는 결과다.
- 바뀐 것은 `reports/밴드_재수집.json` → 인계 문서 **맨 위**(먼저 처리할 것보다 위).
  **사람이 `python band/recollect.py --ack` 로 내리기 전까지 남는다** — 다음 회차가
  조용하다고 지우면, 아침에 뜬 경보가 아무도 안 본 채 사라진다.
- 창은 30일이다(`--days`). 전량을 매일 다시 긁으면 5,000글×5초 = 7시간이라 회차가
  될 수 없고, 글이 고쳐지는 것은 대부분 그 달 안이다.
- 검증 `[152]`.

### 그날 같이 잡은 세 가지 (전부 '조용한' 종류였다)
- **밴드 날짜가 날짜가 아니었다** — 캐시의 `created_at` 은 **밀리초 정수**인데
  흡수기가 문자열로 잘라 썼다: `1766704935000` → `"1766704935"`. 7,782건이 그
  상태였고 **어떤 기간 질문에도 한 건도 안 걸렸다.** 비어 있으면 눈에 띄지만 틀린
  값은 안 띈다. 이제 `datalake.band_day()` 한 곳이 밀리초·초·ISO 를 다 흡수한다.
  교정은 `python datalake.py --repair-band-date`(여러 번 돌려도 안전).
- **없는 밴드가 생겼다** — 수집본 파일명 `84789192_260807.json` 의 뒤 숫자는
  **날짜(2026-08-07)**인데 `convert_dump` 가 '맨 뒤 숫자 덩어리'를 밴드번호로 읽어
  **두 밴드를 `260807` 유령 하나로 합쳤다**(5,453글). 글도 날짜도 그럴듯해 아무도
  이상하다 안 했고, 재수집 회차가 그 밴드에 붙여넣기 파일을 만들고 나서야 드러났다.
  이제 `band_from_name()` 이 자리가 아니라 **생김새**로 고른다: ① 이미 캐시에 있는
  번호 ② 없으면 가장 긴 덩어리(밴드는 8자리, 날짜 꼬리표는 6자리).
- **삭제·오염·유령이 업무 기록이 됐다** — 수집기·재수집·recheck_plan 은 셋 다
  걸렀는데 **흡수기만 안 걸렀다.** 캐시가 표시를 달아 두는데 DB 가 그 표시를 안 읽으니
  표시한 보람이 없었다. 이제 `ingest_band` 도 같은 네 가지를 거른다.

## ★ 코드를 고쳤는데 화면이 안 바뀌면 — 서버가 옛 코드다 (2026-08-08 실사고)
- **`앱서버실행.bat` 은 띄우기만 한다.** 이미 떠 있으면 새 서버는 포트를 못 잡고 조용히
  죽고 옛 서버가 계속 응답한다. 그날 반나절을 여기서 잃었다 — 어제 20:48 에 뜬 서버가
  그날치 변경(금액 기준·접수취소·갱신 개선·AS 기사 문)을 **하나도 반영 못 한 채**
  200 을 주고 있었다. 화면은 멀쩡히 숫자를 보여 주므로 **고친 사람만 몰랐다.**
- 고친 뒤에는 이 한 줄이다: **`python webapp/restart_server.py`** (또는 `앱서버재시작.bat`).
  지금 상태만 보려면 `--status`. 포트는 손으로 적지 말 것 — `app_server.py` 에서 읽는다
  (8765 라고 적어 뒀다가 실제 8899 라 "서버가 안 떴다"로 읽힌 적이 있다).
- 이제 **기계가 먼저 본다**: `session_handoff` 의 '먼저 처리할 것' 에 올라온다
  (워치독 30분마다 갱신). 판단은 커밋 시각이 아니라 **파일 mtime 대 프로세스 시작시각**이다
  — 받아만 놓고 안 띄운 경우(pull 직후)까지 잡아야 한다.
- 검증 `[156]`.

## ★ 사람이 바뀌어도 기록은 이어진다 — `people_alias.py` (2026-08-08 지시)
사용자 지시: **"김혜진 매니저 퇴사한지 1달이 넘었어. 카톡에 이렇게 올라와도
류지영으로 인식하는 알고리즘 추가. 번호는 같아"**
- 접수 안내 문구는 **양식으로 박혀** 있어 사람이 그만둬도 그대로 돈다. 실측: 밴드 글
  871건이 아직 그 이름을 담고 있었고 **35건은 `A/S 담당` 칸 자체**였다. 카톡은 더 심해서
  원본 34개에 3,490회 나오는데 **쿠팡담당자 칸에는 한 번도 없었다** — 칸만 고치면
  지시가 아무 데도 안 닿는다. 그래서 본문에서도 읽는다(`resolve_text`).
- **원문은 고치지 않는다.** 캐시·원본은 쓰인 그대로 두고 **읽을 때만** 옮긴다.
  원문을 고치면 "그때 정말 누구 앞으로 왔나"를 잃는다. `since` 로 **인계 전 글은 소급하지
  않는다**(실측 1,062건 중 인계 후 138건만 바뀌고 609건은 그대로).
- 근거는 **전화번호**다. 이름은 양식에 박혀 안 바뀌지만 번호는 실제 인계 대상이다.
- 새 인계가 생기면 `HANDOVERS` 에 한 줄 더한다 — 코드는 안 고친다.
- 검증 `[155]`.

## ★ 접수했다가 취소되는 건 (2026-08-08 지시)
사용자 지시: **"접수 했다가 접수 취소하는 경우도 많은데 이것도 잡아내는 알고리즘 추가해"**
- **조용한 사고다.** 취소된 건이 원장에 접수 그대로 남아 `AS 미실시`·`정기점검 대기`에
  계속 얹힌다. 숫자가 비어 보이지 않으니 아무도 이상하다 안 한다.
- `cancel_watch.py --sync` 가 밴드 캐시(읽기만)와 카톡 교차근거를 대조해 AppStore
  정본에 즉시 기록한다. **엑셀은 안 연다** — 검증된 자동 보관본만 뒤따라간다.
- ★ **취소와 청구자료는 지우지 말고 갈라 본다** (2026-08-11 UJ2600035 실사고).
  밴드 `90610953/4279`와 카톡은 `[접수취소] - 유선접화로 해결 완료`를 말했지만,
  AS에는 근거가 없고 연결 정산에는 PO·명세서·계산서·입금이 남아 미발행 경고가 났다.
  · 프로젝트NO가 정확히 같은 밴드·카톡을 우선 연결하고, 자동 재무 전파는 반드시
    **원천업무ID exact 관계**로만 한다. 캠프·날짜 유사매칭은 보고용이지 자동취소 근거가 아니다.
  · 열린 상태만 낙관잠금(CAS)으로 취소한다. 이미 완료일·완료상태가 있으면 자동으로
    뒤집지 않고 충돌로 남긴다. 이미 취소인 행도 사유·근거가 비면 멱등 보강한다.
  · 취소건은 미실시·미발행·입금대기·공급가액 KPI에서 제외한다. 단 이미 생긴 PO·명세서·
    계산서·입금은 삭제하거나 완료로 접지 않고 `취소건 청구자료 존재 — 교차입력 확인`으로 보존한다.
- ★ **어려운 것은 잡는 게 아니라 잘못 죽이지 않는 것이다.** 취소로 잘못 처리하면 그
  현장은 아무도 안 가는데 목록에서도 사라진다 — 미실시로 남는 것보다 나쁘다. 실측 오탐 둘:
  "바디부분 아크릴판은 캠프담당 취소요청함"(부품) · "택배발송 취소요청하심" 뒤에 밴드
  양식 `● A/S 완료 :` 줄이 붙어 'A/S 곁의 취소' 규칙이 **모든 글을 삼켰다**.
  그래서 근거는 **'접수'가 '취소'에 붙어 있는 것** 하나다.
- 순서도 뜻을 갖는다: **댓글 취소 > 완료 제목 > 본문 취소**(댓글이 글보다 나중이다).
- ★ **댓글 본문이 들어왔다** (2026-08-08 지시: "밴드 댓글도 같이 수집하는 알고리즘 실행").
  전까지는 반쪽이었다 — 취소는 대부분 **댓글**로 오는데 캐시에 `comment_count` 숫자만
  있고 본문이 없어, 판정 코드는 멀쩡히 돌면서 **아무것도 못 잡았다**(오류도 안 났다).
  이제 담는 쪽 둘이 같은 모양 `comments: [{author, created_at, content}]` 으로 담는다:
  화면 긁기 `band/grab_posts.js` → `band/convert_dump.py`, API `band/band_sync.py`.
  · **읽는 자리는 하나다** — `band_extract.comment_text`. 담는 쪽이 둘이라 읽는 쪽이
    갈리면 한쪽 경로 댓글은 영영 안 읽힌다. `cancel_watch.py` 는 한 줄도 안 고쳤다.
  · **시각 없는 댓글은 버린다**(본문 규칙 `[130]` 과 같다). 시각이 없으면 '댓글이
    글보다 나중'이라는 취소 판정의 순서 자체를 세울 수 없다.
  · **재병합은 댓글을 합친다**(덮지 않는다). 덤프는 매 실행 전부 재처리되므로 댓글이
    없던 시절의 옛 덤프가 나중에 이기면 애써 모은 댓글이 통째로 사라진다.
  · **반쯤 읽은 것을 다 읽은 것으로 세지 않는다** — 접힌 댓글은 끝내 안 펴질 수 있다.
    `comments_full`·`cancel_blind_count()` 가 '달린 수만큼 못 읽은 글'을 계속 보고한다.
  · **붙여넣기 파일이 낡으면 워치독(30분)이 스스로 다시 만든다** —
    `watchdog.heal_stale_pastefiles`. 수집 규칙을 고쳐도 사람 손에 가는 것은 디스크에
    있는 그 `band/*붙여넣기_*.js` 다. 이날 실측으로 네 개가 전부 수정 **이전** 것이었다
    — 그대로 붙여넣었으면 **댓글이 한 건도 안 들어오는데 수집은 성공으로 끝났다.**
    판정은 `grab_posts.js` mtime 대 파일 mtime 이고, 훑을 것이 없어 다시 안 써지는
    파일은 **지운다**(옛 JS 를 남겨 두는 것보다 없는 편이 낫다).
  · **긁는 것 자체는 여전히 'CSOS 리서치 및 자료 수집' 세션 몫이다.**
- 검증 `[155]` · `[162]` · `[208]`.

## ★ 금액이 안 맞아 보일 때는 먼저 **기준**을 의심한다 (2026-08-08 지시)
사용자 지시: **"여기서는 돌발as 정기정검만 집계해 / 나머지 다 진행해서 맞춰서 완료 처리로 변경해"**
- **ERP 비교는 돌발AS·정기점검만** 센다(`ERP_LEDGER_KINDS`). 관리대장이 원장에 담는
  업무가 이 둘이기 때문이다. 전체를 대 놓으면 없는 누락이 생긴다 — '원장 미반영
  930,539,994원' 중 894,118,514원이 ERP 에만 있는 업무였다(신규납품·기타·철거·계단).
  같은 종류끼리는 돌발AS +10.8M · 정기점검 −0.6M 로 거의 맞는다. 나머지는 'ERP 전용 매출'.
- **계산서 발행 판정은 근거 세 갈래**다: 원장 발행일 · 완료 DB · **ERP진행상태 6·7단계**.
  06시트 계산서 칸은 사람 손 입력이라 유상 716건 중 6건만 차 있어 발행율이 0.9% 로
  나왔다 — 미발행이 아니라 **빈 칸**이었다(ERP 기준 450건, 63.7%).
- **부가세 포함 명세서 금액은 ÷1.1 로 확정**한다(`supply_from_statement`). 단 **깨끗이
  떨어질 때만** — 되돌려 곱해 원본이 나오고 10원 단위일 것. 안 떨어지면 예전처럼 사람에게
  남긴다. 억지로 나누면 틀린 값이 화면에서 확정처럼 보인다.
- 검증 `[154]`.

## ★ 읽지 않는 열은 빈칸과 똑같이 보인다 (2026-08-08 실사고)
- `read_ledger` 열 목록에 06시트 **`청구상태`(AH)** 가 없어서 `원장_청구상태` 가
  **750행 전부 None** 이었다. 그 값으로 "완료 반영 대상 286건 — 전부 빈칸" 이라는
  결론을 낼 참이었는데, 실제로는 사람이 채워 둔 행이 **63개**였다(작업완료 59 ·
  거래명세서발행 4). `col_index` 는 없는 이름에 None 을 주고 조용히 넘어간다 —
  **오류가 나지 않는 종류의 잘못**이다. 비어 있는 값은 눈에 띄지만 **안 읽은 열은
  빈칸과 구별할 수 없다.** 원장 숫자가 이상하면 "그 열을 정말 읽고 있나"를 먼저 본다.
- 검증 `[165]`.

## ★ 사각지대를 재는 계기도 눈이 멀 수 있다 (2026-08-08 실사고)
- `cancel_blind_count` 가 `comment_count > 담긴 댓글 수` 로 셌는데, 캐시 10,312글 중
  `comment_count>0` 은 **6글**·댓글 본문은 **0글**이었다 — 밴드에 댓글이 없어서가 아니라
  **수집기가 그 숫자를 안 담아서**다. 계기는 "사각지대 0건"이라 말했고 아무도 안 봤다.
  **값이 비면 사람이 알아채지만, 계기가 0을 내면 의심하지 않는다.**
- 이제 `comments` 키가 **아예 없는 글**(한 번도 안 들여다본 글)도 사각지대로 센다.
  `comments: []`(보긴 봤고 없었다)와 가른다. 실측 **8,259건 / 8,561건**.
- **리포트가 스스로 못 믿겠다고 말한다** — 사각지대가 절반을 넘으면 `접수취소_확인.md`
  가 경고를 적는다. '취소로 읽힌 N건'은 **읽은 글 안에서만** 센 숫자다.
- 무엇이든 "0건"이 나오면 묻는다: **정말 없는 건가, 아니면 안 본 건가.**
- 검증 `[169]`.

## ★ 무작정 긁지 않는다 — 수집도 알고리즘이다 (2026-08-09 지시)
사용자 지시: **"무작정 자료 수집만 하지 말고 정확한 알고리즘을 만들어 수집하게 코딩해 AI 자동화"**

- **수집 목록을 '최근 것부터 N건'으로 만들지 말 것.** 그건 알고리즘이 아니라 순서다.
  그렇게 하면 몇 시간을 긁으면서도 **그중 무엇이 무엇을 바꾸는지 아무도 모른다.**
- **기준은 짐작하지 말고 읽는 쪽 코드에서 빌린다.** 그 자료를 소비하는 도구가
  "이럴 때만 쓴다"는 조건을 이미 갖고 있다. 그 조건 밖의 자료는 아무리 모아도
  **단 한 줄도 못 바꾼다.** 첫 적용(`band/comment_backfill.py`):
  `cancel_watch.build()` 는 취소를 찾아도 그 프로젝트에 **아직 안 끝난 원장 행**이
  있을 때만 대기열 행을 만든다(`open_ledger_rows`) — 그래서 그 집합이 곧 1순위다.
  · 실측: 댓글 미확인 7,475건 중 업무글 아님 3,070 · 프로젝트NO 없음 2,641 ·
    닫힘 1,684 · **열림 80**. **10시간이 7분**이 됐다.
- **급한 갈래는 날짜로 자르지 않는다.** 1순위는 반년 전 것이라도 뽑는다 —
  오래된 미실시가 그대로 얹혀 있는 것이 바로 그 사고의 본체다. 날짜 창은 2·3순위 몫이다.
- **모르면 1순위라고 우기지 않는다.** 근거(원장 등)를 못 읽으면 전부 '모름' 갈래에
  두고 날짜로만 고른다. 근거 없이 매긴 우선순위는 없는 것보다 나쁘다.
- **갈래마다 왜 그 갈래인지 한 줄로 말한다.** 사람이 목록을 보고 "이건 왜 긁나"를
  물을 수 있어야 한다.
- 검증 `[177]`.

## ★ 없는 번호는 40개가 아니라 **몇 개만** 찔러 본다 (2026-08-11 실사고)

- 16:10 회차의 붙여넣기 파일이 밴드 84789192 에 `3540~3579` **마흔 개**를 담았다.
  캐시 최대는 3539 이고 그 위는 아직 없는 번호다. 밴드는 없는 번호에도 200 과 앱
  껍데기를 주므로 수집기는 한 개당 **iframe 9초 + 본문 12초**를 꽉 채우고 시각이 없어
  버린다(`[130]`) — **약 14분에 수확 0**. 오류도 안 나고 '실패'로도 안 세인다.
- **근거는 이미 보고 있었는데 쓰는 법이 틀렸다.** `recheck_plan` 은 '없음 확인'
  근거(`reports/밴드_확인시각.json`)가 **한도(1일)보다 낡으면** 없는 것으로 치고 곧장
  `ahead`(40)를 통째로 쏟았다. 근거가 낡았다는 것은 "그 사이 새 글이 있을 수 있다"는
  뜻이지 **"마흔 개가 있다"는 뜻이 아니다.** 번호는 이어지므로 `hi+1` **하나**면
  존재 여부는 답이 나온다.
- 그래서 갈래를 셋으로 가른다(`recheck_plan.absent_line`):
  · 근거가 **살아 있다** → 그 구간은 **아예 안 넣는다**(조용함, `[131]`)
  · 근거가 **낡았다·없다** → **존재 확인용 `PROBE_AHEAD`(5)개만**. 있는 것이 확인되면
    `hi` 가 올라가 **다음 회차가 이어받는다** — 사다리는 저절로 오른다.
  · 근거 **아래**(실재가 확인된 구간) → 예전대로 끝까지. 있는 글을 긁는 것은 낭비가 아니다.
- ★ **근거가 추월될 수 있다.** 그 파일이 "3539 부터 없다"고 적어 둔 사이에 3539 가
  실제로 수확돼 캐시에 들어와 있었다(실측 84789192·90610953 둘 다). 낡은 것보다
  나쁜 것이 **틀린** 근거다 — 그대로 믿으면 **실재하는 글을 유령으로 표시**한다.
  그래서 캐시에 그 번호 이상의 **진짜 글**(작성시각 있는 수확)이 있으면 근거를 버린다.
- ★ **인계 문서에도 같은 구멍이 있었다**(2026-08-11 이어서). `session_handoff` 는
  근거의 **나이만** 보고 '(조용함)'을 적었다 — 신선한 채로 추월된 근거에는 **없는
  조용함을 확언**한다. 실측 90610953 은 '5438 부터 없다'는 근거를 가진 채 5447 을
  이미 수확해 뒀다. 수집 계획은 거르는데 인계 문서만 안 거르면, **같은 파일을 보면서
  한쪽은 긁으라 하고 다른 쪽은 조용하다고 한다.** 이제 판정은 `recheck_plan.judge_absent`
  한 곳이다 — 근거는 부르는 쪽이 읽어 넘기고(`band_quiet`) 판단만 빌린다. 파일까지
  거기서 읽으면 두 화면이 **서로 다른 한 장**을 놓고 답하게 된다.
  · 그리고 밀림일 때 **왜 아직 밀림인지**를 적는다(낡음/추월). '밀림'만 있고 이유가
    없으면 사람이 또 없는 번호를 긁으러 간다 — 그것이 이 사고의 시작이었다.
- ★ **거르기만 하면 사람 몫이 남는다 — 틀린 근거는 스스로 무효가 된다** (2026-08-11 지시
  "자동화 시켜"). 위 둘은 추월된 근거를 **읽는 쪽에서 걸렀을 뿐**이라 근거는 틀린 채로
  남았고, 되돌리는 길은 사람이 밴드 피드를 열어 `real_latest.py --latest` 를 적어 주는
  것뿐이었다. 그런데 이건 **지어낼 것이 없는 정정**이다: "N 부터 없다"는데 N 이상이 이미
  수확돼 있으면 판단이 아니라 **모순**이다. 이제 워치독(30분)이 `real_latest.heal()` 로
  그 근거를 **'모름'으로 되돌린다**(실측 84789192 3539 · 90610953 5438 둘 다 정정됨).
  · **`top+1` 을 지어내지 않는다** — 그 위가 없다는 근거는 아무 데도 없다. '모름'이면
    다음 회차가 다섯 건으로 싸게 물어보고 사다리는 저절로 오른다.
  · **낡기만 한 근거는 안 건드린다.** 낡음은 틀림이 아니라 다시 물어봐야 하는 것이다 —
    지우면 매 회차 근거가 사라져 탐색만 영원히 반복된다.
  · **캐시는 한 글자도 안 고친다.** 틀린 근거로 수확에 `absent` 를 찍으면 실재하는 글이
    유령이 된다(되돌릴 수 없는 쪽이다). 옛 값은 `이전없음확인` 으로 남긴다.
  · 정정과 판정이 **같은 `trusted_hi`** 를 본다. real_latest 는 `absent` 만 걸렀고
    recheck_plan 은 오염·삭제까지 거른다 — 눈이 갈리면 고쳐도 안 고쳐진다.
  · 근거 정정은 **붙여넣기 파일 만들기보다 먼저** 온다(목록에 담을 번호를 그 근거가 정한다).
- ★ **거르는 자리는 파일로 나가는 길목 하나다**(`make_oneclick.screen`). 번호를 정하는
  곳은 셋(`recheck_plan`·`comment_plan`·`recollect`)이라 각자 거르면 한 곳만 고쳐진다.
- ★ **낱말이 어긋나면 한 건도 안 걸린다.** `comment_plan` 은 `ghost`·`dirty` 로 물었는데
  캐시가 실제로 다는 표시는 `contaminated`·`absent` 다. 그래서 남아 있던 계획
  **84789192 102건 전부 · 90610953 522건 전부**가 없는 번호였다(그대로 긁었으면
  약 3시간 반에 수확 0). 이제 낱말은 `recheck_plan.DEAD_FLAGS` 한 곳에서 온다.
- **뺀 것은 숫자로 말한다** — 조용히 빼면 '0건'이 '다 봤다'로 읽힌다(`[169]`).
- 검증 `[217]` · `[223]`.

## ★ 손으로는 재현되지 않는 고장 — 무인 회차는 `pythonw` 에서 돈다 (2026-08-12 실사고)

- `쿠팡업무_밴드재수집` 이 **매일 08:00 exit 1** 로 죽고 있었다. 원인은 한 줄이었다 —
  `band/recheck_plan.py:18` 의 맨몸 `sys.stdout.reconfigure(...)`. 스케줄러는
  **`pythonw.exe`(창 없음)** 로 부르므로 `sys.stdout` 이 **None** 이고, import 중에
  `AttributeError` 가 터져 **부른 회차까지 통째로 죽는다**(`recollect.plan()` 경유).
- ★ **손으로 돌리면 멀쩡하다.** `python.exe` 에는 콘솔이 있어 `sys.stdout` 이 살아 있다.
  그래서 `--plan` 을 아무리 돌려도 재현이 안 됐고 며칠을 못 찾았다. **재현이 안 되는
  고장은 "없는 고장"이 아니라 "다른 환경에서만 나는 고장"이다** — 스케줄러가 어떻게
  부르는지(`Get-ScheduledTask ... Actions`)를 먼저 본다.
- 자국이 이것을 살렸다. `[228]` 이 만든 `reports/밴드_재수집_오류.json` 이 없었으면
  그날도 못 찾았다. **무인으로 도는 회차를 새로 만들면 자국 남기기를 같이 넣는다.**
- 막는 관용구는 둘 다 좋다: `try:` 로 감싸거나 `if hasattr(sys.stdout, "reconfigure")`
  (`hasattr(None, ...)` 는 False 다). **사람이 아니라 검증이 훑는다** — `[235]`.

### 같은 날 잡은 것 셋 (전부 '성공이라 적히면서 안 돌던' 것)
- **배터리 한 줄이 회차 셋을 세웠다.** `New-ScheduledTaskSettingsSet` 의 **기본값**이
  '배터리면 시작 안 함 + 배터리로 바뀌면 중단'인데 설치본이 그걸 안 껐다. 노트북을
  뽑자 `정오회차`·`AutomationPipeline`·`밴드재수집` 이 멈췄다 — 그런데 **실패가 아니라
  `Queued`** 라서 아무 자국도 안 남는다. 실패는 코드가 남지만 대기는 안 남는다.
  살아 있는 작업 셋을 풀고(백업 `reports/스케줄러_백업_배터리설정_*.xml`) 설치본
  여섯 개에 `-AllowStartIfOnBatteries -DontStopIfGoingOnBatteries` 를 넣었다.
  **새 회차를 만들면 이 두 깃발을 반드시 적는다** — 안 적으면 기본값이 도로 물려진다.
- **퇴역한 규칙이 `.bat` 에 남아 매일 아침 90분을 먹었다.** 아침 입력 보호시간은
  2026-08-11 에 퇴역했는데(`operation_window`) `워치독실행.bat` 은 08:00~09:30 을
  하드코딩한 채였다. 그동안 자가치유·회차감시·인계갱신이 전부 멈췄고 스케줄러에는
  `0x0`(성공)으로 적혔다. 이제 bat 이 `operation_window.is_input_window()` 를 부른다 —
  **판단은 한 곳이다. 사본이 둘이면 한쪽만 고쳐진다.**
- **유령 밴드가 스스로를 되살리고 있었다.** `band_from_name` 은 ①'이미 캐시에 있는
  번호'를 ②길이(7~10자리) 검사보다 **먼저** 봤다. 그래서 유령 캐시가 한 번 생기면
  그 번호가 '아는 번호'가 되어 ②를 통째로 건너뛴다. 실측 `202608082047`(12자리 시각
  도장, 글 0개)이 살아 있어 `dump_202608082047_84789192.json` 이 유령으로 갔고 그날도
  새로 써졌다. 이제 `known_bands()` 가 `plausible_band()` 를 통과한 것만 담는다 —
  **캐시에 있다는 것만으로는 근거가 안 된다.**

## ★ 회차 한 단계가 영원히 멈출 수 있다 (2026-08-08 실사고)
- **`subprocess.run(timeout=)` 을 쓰지 말 것.** CPython 은 시간이 넘으면 `kill()` 뒤
  **윈도우에서만 시간제한 없는** `communicate()` 를 한 번 더 부른다. 자식이 SMB(Z:)
  읽기처럼 끊기지 않는 대기에 걸리면 TerminateProcess 가 안 먹고 거기서 안 끝난다.
  실측: `timeout=1800` 을 걸어 둔 '원본 폴더 정리'(source_tidy)가 **13시간 30분**을
  매달렸다 — 부모 CPU 0.4초 · 자식 0.5초, 둘 다 그냥 서 있었다.
- **조용한 이유**: 멈춘 회차가 **락을 쥔 채**라 다음 회차는 "이미 실행 중"으로 건너뛰고,
  스케줄러는 '성공'이라 적는다. 그래서 09:50 이 하는 일(접수취소·객관완료·청구상태·
  대조)이 하루 종일 안 돌면서 **어느 화면에도 티가 안 난다.**
- 규칙 셋: ① `Popen` + `communicate(timeout=)` ② 죽일 때는 **나무째**
  (`taskkill /F /T` — 윈도우 `kill()` 은 손자를 안 죽인다) ③ **죽인 뒤 드레인에도
  제한을 건다.** 그래도 안 죽으면 pid 를 리포트에 남기고 **다음 단계로 간다** —
  단계 하나를 살리자고 회차 전체를 세우지 않는다.
- 검증 `[179]`.

## ★ 이카운트 회계 원장류는 **네 화면**이다 (2026-08-08 실사고)
- 분류 규칙이 한 줄이었다: "'적요' 와 차변/대변이 같은 표에 있으면 `ledger`".
  그런데 회계 원장류는 **넷 다 그 모양이다.** 실측 `pick("ledger")` 12개 중
  거래처별계정별원장은 **5개뿐**이고 계정별원장 5 · 분개장 1 · 현금출납장 1 이었다.
  게다가 계정별원장 한 파일은 한 시트에 **52개 계정이 층층이** 쌓여 있어(현금·보통예금·
  외상매입금·부가세예수금 …) 8,980행이 통째로 '쿠팡 매출 전표'로 읽혔다.
- 그래서 `erp_ledger_check` 가 전표 **5,157건**을 대조해 유형A('설치·작업 근거 확인
  필요 ★')를 **1,856건** 냈다. 대부분 남의 회사 거래라 **아무도 확인하러 갈 수 없는
  지시**였고, 그것이 매일 09:50 회차마다 새로 찍혔다. 파일도 있고 숫자도 나오니
  실패한 티가 안 났다. 가른 뒤 전표 **689건(고유 302) · 유형A 296건**.
- 가르는 근거는 **머리글**이다(제목줄은 잘리고 사람이 바꾼다) — `inbox_scan.ledger_kind`:
  · 거래처별계정별원장 E010809 = 적요+차변/대변, **거래처 열 없음**(거래처가 조회 조건)
  · 계정별원장 E010807 = 위 + **거래처명 열** · 분개장 = **전표번호+계정명**
  · 현금출납장 = **상대계정명·상대거래처명**
  분개장 판정이 '적요' 판정보다 **먼저** 와야 한다(분개장에도 적요·거래처가 있다).
- ★ **규칙을 고쳐도 캐시가 옛 답을 붙들면 헛일이다** — 같은 사고 **세 번째**다.
  `inbox_classify.json` 열쇠는 (크기, 수정시각)뿐이라 원본이 안 바뀌면 영원히 옛 갈래를
  준다. `source_index.rules_version()` 은 `classify_rows` 만 해싱해서 판별이 `ledger_kind`
  로 나가면 지문이 안 움직인다. 그래서 **`inbox_scan.RULES_VERSION` 을 손으로 올린다** —
  분류 규칙을 손대면 이 숫자를 올리고, 두 캐시가 다 그것을 본다.
- **갈래를 늘리면 `collect_sources.KNOWN` · `download_intake` 목록도 같이 넓힌다.**
  안 넓히면 새 이름을 단 순간 그 자료가 '모르는 종류'가 되어 **조용히 안 들어온다.**
- ★ **짝이 거의 안 지어지면 A·B 가 아니라 열쇠 이야기다**(`key_looks_wrong`).
  이 대조는 06시트 `거래명세서번호` = ERP `일자-No.` 를 전제하는데 실측은
  **302 대 65 중 겹침 6** 이었다 — 서로 다른 순번이다. 열쇠가 안 맞으면 짝이 안 지어진
  것이 전부 '현장 확인 필요'로 나온다. 이제 리포트가 스스로 그 말을 적는다.
- 검증 `[203]` · `[173]` · `[174]`.

## ★ 목록을 받을 때 딸려 온 값을 버리지 말 것 — 색인 2.5시간의 정체 (2026-08-11 실사고)

사용자 지시: **"원본 색인이 2시간 반 걸리는 이유 찾아"**

- 범인은 **한 줄**이었다. `scan()` 이 `os.walk` 로 **이름만** 받아 온 뒤 파일마다
  `os.stat(경로)` 를 다시 불렀다. 로컬 디스크에서는 공짜지만 **Z:(SMB)에서는 파일
  하나가 왕복 한 번**이다. 같은 폴더에서 잰 값:
  · `os.scandir` 항목의 `.stat()` = **0.04 ms/개** (목록에 크기·시각이 딸려 온다)
  · `os.stat(경로)` = **135~155 ms/개** → **3,000배**
  112,662개 × 0.145초 = **4.5시간**. 고친 뒤 실측 **24.8초 · 147,223개**.
- ★ **`os.walk` 도 속으로는 scandir 을 쓴다 — 그 결과를 버리고 이름만 넘겨줄 뿐이다.**
  그래서 코드를 아무리 봐도 잘못이 안 보인다. 느린 것이 **문법에 안 나타나는 자리**다.
  이제 `_walk_stat()` 이 `(폴더, 이름, stat)` 을 같이 준다.
- ★ **캐시가 있어도 하나도 안 줄었다.** 캐시 열쇠가 `경로|크기|수정시각` 이라
  **열쇠를 만들려면 먼저 stat 을 해야** 한다. 즉 파일이 하나도 안 바뀐 날도 4.5시간을
  똑같이 썼다. "두 번째부터는 빠르겠지"가 성립하지 않는 구조였다 —
  그래서 '느려서 못 끝냈다'가 아니라 **매일 못 끝냈다.**
- ★ **그래서 앱이 원본의 23%만 보고 있었다.** 직전 색인은 **33,514개**(2026-08-10 17:27)
  인데 실제는 **147,223개**다. 화면은 오류 없이 목록을 보여 주므로 **없는 파일이
  아니라 안 본 파일**이라는 것을 아무도 몰랐다([169]).
- 그리고 이것이 사고 사슬의 뿌리였다: 몇 시간짜리 일이 30분 워치독에 죽고 → 죽으면서
  락을 남기고 → 락이 하루치 회차를 막았다. **한 단계를 빠르게 만드는 것이 재시도
  규칙을 늘리는 것보다 낫다.**
- ★ **같은 병이 세 곳 더 있었다** — `source_tidy`(13시간 30분 매달렸던 그 단계) ·
  `source_organizer` · `collect_sources`. 전부 walk 로 이름만 받고 파일마다
  `os.stat`/`getmtime` 을 다시 불렀다. **워커를 베껴 쓰면 한 곳만 고쳐진다** —
  그래서 `source_index.walk_stat` 하나를 쓴다.
  · `[175]` 는 **어떻게 죽일까**를 고쳤고, 여기는 **애초에 왜 오래 걸렸나**다.
- ⚠ **거를 폴더 목록은 부르는 쪽이 정한다**(`skip_dirs`). 색인은 `_보관`·`_바로가기`
  를 안 담지만 **원본 정리는 바로 그 폴더를 손봐야 한다.** 공용 워커를 쓰면서 색인의
  목록을 말없이 물려주면, 정리 도구가 어느 날부터 그 폴더를 통째로 안 보면서
  **'완료'라고 적는다** — 빠르게 만들려다 일을 빼먹는 자리다.
- 훑는 자리에 `os.stat(` 이 다시 들어오면 검증이 막는다. 검증 `[198]`.

## ★ 답이 안 오면 Z: 를 탓하기 전에 코드를 본다 (2026-08-08 실사고)
- `erp_progress` 는 판매조회를 `glob(ERP_DIR/**, recursive=True)` 로 찾는데(Z: 재귀 탐색),
  그 glob 이 **캐시 검사보다 앞에** 있었다. `settle_status` 는 행마다 이 함수를 부른다 —
  750행이면 **Z: 를 750번 통째로 훑었다.**
- 증상이 "느리다"가 아니라 **"아무 답도 안 온다"** 였다. 같은 집계가 600초 제한에 **두 번**
  걸려 죽었고 세 번째는 45분을 넘겨도 안 끝났다. 그동안 "Z: 가 붐비나 보다"로 읽혔다.
  **느린 것과 멈춘 것은 겉으로 구별되지 않는다** — 두 번 죽으면 코드부터 본다.
- 30초 TTL 을 glob **앞에** 두자 같은 집계가 1분 안에 끝났다. 비싼 탐색은 언제나
  캐시 검사 **뒤에** 온다. '못 찾음'도 기억한다(찾을 게 없으면 glob 이 제일 오래 돈다).
- 검증 `[168]`.

## ★ 회차는 자국을 남기고 **반드시 끝난다** (2026-08-09 지시)
사용자 지시: **"32시간째 미완주 왜그런거야 해결해 이런 문제 생기면 코딩해서
다시는 안생기게 처리하는 알고리즘 구성해서 적용하고 보고해"**

- **화면이 '08-08 01:38 — 32시간째 미완주'만 말하고 이유를 못 댔다.** 진짜 문제는
  '느리다'가 아니라 **둘**이었다:
  · **어디서 멈췄는지 아무도 몰랐다** — 종합리포트는 `finish()` 가 **맨 끝에 한 번**
    쓴다. 그러니 완주하지 못한 회차는 **기록을 한 줄도 안 남긴다.**
  · **안 끝나는 회차가 다음 회차를 막았다** — 잠금을 쥔 채 몇 시간을 끌면 다음 회차는
    "이미 실행 중"으로 조용히 건너뛰고 스케줄러는 '성공'이라 적는다. 이틀 쌓이면 32시간.
- **고친 것 셋** (검증 `[174]`)
  ① `daily_run.note_progress()` 가 **단계마다** `reports/.daily_run.progress.json` 에
     자국을 남긴다 — 지금 단계·상태·머문 시간·끝낸 단계 수·회차 나이/예산.
     **죽어도 남는다. 그게 요점이다.** `finally` 에 `(회차 끝)` 표식까지 찍어
     '아직 도는 중'과 '죽었다'를 구별할 수 있게 했다.
  ② **회차 예산** `ROUND_BUDGET_MIN`(기본 150분, `COUPANG_ROUND_BUDGET_MIN` 으로 조절).
     넘으면 남은 단계를 **건너뛰고 완주시킨다** — 중단이 아니다.
     **완주해야 리포트가 써지고 잠금이 풀리고 다음 회차가 돈다.**
     반쯤이라도 완주한 회차가 영원히 안 끝나는 회차보다 낫다.
     건너뛴 것은 "다음 회차가 이어서 합니다"라고 **적어 남긴다** — 조용히 건너뛰면
     '돌았는데 왜 결과가 없나'가 된다(그게 지금까지의 증상이었다).
  ③ `session_handoff.daily_step_now()` 가 그 자국을 읽어 경보에 **단계 이름을 붙인다**.
     '몇 시간째'만 말하고 '어느 단계'를 못 대면 원인을 영영 못 찾는다.
- **판정은 언제나 둘로 한다**: ① 잠금 pid 생존 ② **자식/단계가 최근에 바뀌었나**.
  실측 2026-08-09 10:39 — pid 살아 있고 자식이 2분 전 시작 → **느린 것이지 멈춘 것이
  아니다.** 진행 중인 회차를 죽이면 그날 대조가 통째로 빈다.
- 검증 `[180]`.

## ★ 댓글을 다 찾아 담고, 카톡과 한 사건으로 묶는다 (2026-08-09 지시)
사용자 지시: **"밴드도 댓글도 다 찾아 저장하는 알고리즘 구성하고, 카톡도 댓글이랑
연관지어서 생각하고 반영하는 알고리즘 구현해"**

- ★ **고친 것과 채운 것은 다르다.** 2026-08-08 에 수집기가 댓글을 담도록 고쳤지만
  (검증 `[162]`) 그건 **그 뒤로 긁는 글**에만 해당한다. 실측 2026-08-09: 캐시 8,561글 중
  **8,258글이 댓글을 한 번도 안 봤고 본문이 담긴 글은 0건**이었다. 그래서 `cancel_watch`
  는 지금도 댓글 취소를 **하나도 못 잡는다** — 코드는 멀쩡히 돌고 오류도 안 난다.
  **알고리즘을 고쳐 놓고 "됐다"고 적으면 안 된다. 과거 자료를 채우는 회차가 따로 있어야 한다.**
- `python band/comment_plan.py --make` 가 그 회차다. 실측 **8,107건 · 33회차**.
  · 고르는 기준은 검증 `[169]` 그대로 — `comments` **키가 없는 것**만(안 봤다).
    `comments: []`(보고 없었다)를 섞으면 숫자가 영원히 안 줄고 같은 자리를 맴돈다.
  · **최근 글부터** 훑는다. 취소·변경은 최근 글에 붙고, 여러 밤이 걸리므로 도중에
    멈춰도 값어치가 남아야 한다.
  · 밴드번호는 **8자리**로 거른다 — `len>=7` 로 뒀더니 `202608082047`(날짜 도장)이
    들어와 헛 계획을 세웠다(`260807` 유령과 같은 종류).
  · 긁는 것은 여전히 **수집 세션** 몫이다. `band/댓글_붙여넣기_<밴드>.js` 를 만들어 둔다.
- ★ **긁어도 0건이던 진짜 이유는 선택자가 한 칸 아래를 가리킨 것이었다** (2026-08-09 실측).
  전엔 "댓글은 레이어를 클릭해야 DOM 에 생긴다"고 적었는데 — 그건 **피드(/post) 화면**
  얘기였다. **상세 페이지(/band/<no>/post/<no>)는 레이어 없이 처음부터 댓글을 그린다.**
  숨은 iframe(수집기와 똑같은 방식)으로 글 4369(댓글 6개)를 열어 직접 확인했다:
  본문·댓글 6개·시각까지 다 그려졌다. `document.hidden` 이어도 **iframe 안 상세페이지는
  그려진다**(top 문서의 rAF 정지와 별개다).
  · 막힌 자리는 **항목 선택자**였다. 각 댓글은 `div.cComment[data-viewname="DCommentLayoutView"]`
    **자체**인데 예전 `CMT_ITEM` 이 `.cComment li`(그 **안쪽** li)를 찾아 늘 0개였다.
    개수(`댓글 6`)는 멀쩡히 읽혀서, 250건을 실패 0 으로 긁고도 댓글이 한 건도 안 들어왔다.
  · 실측 셀렉터(각 cComment 안): 작성자 `.name` · 시각 `.time` · 본문 `._commentContent`.
    본문은 사진/스티커만 있는 댓글이면 비므로 **시각만** 가드로 둔다([130]).
  · `grab_posts.js` 한 곳만 고쳤다 — 붙여넣기 파일은 이 파일을 통째로 싣는다.
    검증 `[162]` 가 이제 '항목 선택자가 cComment **자체**를 집는지'까지 본다.
- ★ **같은 사건이 두 군데로 나뉘어 온다.** 캠프 담당자는 카톡에 쓰고 기사는 댓글에 단다.
  `cancel_watch`(밴드)와 `kakao_reconcile`(카톡)은 서로를 **모른 채** 돌았다 — 카톡에만
  온 취소는 밴드 화면에 안 뜨고 그 반대도 같다. 어느 쪽에도 오류는 안 난다.
- `python cross_signal.py` 가 묶는다(09:50 회차). 근거는 **셋이 겹칠 때만**:
  **캠프**(괄호 앞 이름) · **날짜 ±3일** · **같은 사건 종류**(취소/연기/재방문/금액변경).
  짝이 약하면 **엉뚱한 현장 둘을 한 사건으로 만든다** — 가지 말아야 할 곳에 사람을
  보내거나 가야 할 곳을 지운다. 못 묶는 것보다 나쁘다.
  · 실측: 밴드 115 · 카톡 58 → 짝지어짐 15 · 밴드에만 100 · **카톡에만 43**.
    마지막이 제일 값어치 있다 — **기사에게 안 전달됐을 수 있는 것**들이다.
  · **아무것도 고치지 않는다.** 원장 대조·큐는 `cancel_watch` 몫이다.
    판정을 두 곳에서 하면 언젠가 갈린다.
- ⚠ `datalake.band_day()` 는 **글이 아니라 `created_at` 값**을 받는다. 글을 통째로
  넘기면 조용히 `""` 를 돌려주고 **모든 글이 건너뛰어진다** — 오류 없이 "밴드 신호 0".
- 검증 `[179]`.

## ★ 앱이 스스로 답한다 — 클로드를 부르기 전 단계 (2026-08-09 지시)
사용자 지시: **"클로드 코드 또는 코덱스 없이 앱 자체적으로 처리할 수 있는 AI 알고리즘
기능 넣어서 클로드코드 크래딧 사용 최대한 아낄 수 있게 설계하고 알고리즘에 반영해"**

- **크레딧은 '작업'이 아니라 '되묻기'에서 새었다.** 지금까지의 자동화는 전부 **일을 하는**
  쪽이었다(수집·대조·큐·반영). 그런데 돈은 거기서 안 새다. 샜 자리는 이렇다 — 사람이 앱을 열어
  이상한 숫자를 보고, **앱이 이유를 못 말해서** 클로드에게 묻는다. 그러면 클로드는 매번
  리포트를 열고 ERP 를 대 보고 원장을 뒤져서 **이미 디스크에 있는 사실**을 다시 조립한다.
  실측된 예: "작업은 완료인데 왜 계산서 발행이 안된거지" 한 마디가 도구 호출 열몇 번이 됐고,
  답은 `ERP 4.세금계산서발행대기` 라는 **파일에 이미 적혀 있던 한 줄**이었다.
  그러니 아낀야 할 것은 계산이 아니라 **왕복**이다.
- `python local_ai.py "질문"` · 앱은 대시보드 **[앱에게 물어보기]** 카드(`/api/ask`).
  지금 답하는 것: 프로젝트NO·PO번호 조회 · 계산서 미발행 갈래 · 회차 상태 ·
  엑셀 반영 시각 · 수집 밀림 · 오기입 · 취소·교차신호 · 앱서버 옷코드 · 지금 할 일.
- **언어모델을 부르지 않는다 — 이 업무에서는 규칙이 더 세다.** 질문은 몇 가지 모양으로
  반복되고 답의 근거는 전부 구조화된 파일에 있다. 모델을 부르면 돈이 들고, 느리고,
  무엇보다 **지어낼 수 있다.** 규칙은 근거가 없으면 답을 못 하고, 못 하는 것이 미덕이다.
- **세 갈래로만 답한다**: ① **답함**(근거 파일명·나이를 같이 준다) ② **모름** ③ **낡음**.
  어느 가래든 **몇 시간 전 자료인지 밝힌다** — 낡은 근거로 자신 있게 말하는 것이
  이 프로젝트에서 제일 위험하다.
- ★ **못 답해도 빈손으로 돌려보내지 않는다** — 여기가 진짜 절약이다. 규칙에 없는
  질문이면 **클로드에게 그대로 붙여넣을 문구**를 만들어 준다. 그 문구에는 앱이 이미
  확인한 수치와 리포트 이름이 박혀 있어 클로드가 처음부터 뒤질 일이 없다.
  **못 답해도 왕복 한 번은 짧아진다.**
- ★ **자가점검은 '갈래까지' 본다.** 첫 판은 "터지지 않으면 통과"였다. 그래서 이 프로젝트에서
  제일 자주 온 질문("왜 계산서 발행이 안된거지")이 **모름으로 떨어졌는데도 '모두 통과'**
  라고 말했다 — 계기가 0 을 내면 아무도 의심하지 않는다(`[169]` 와 같은 모양).
  `PROBES` 에 **기대하는 갈래**를 적어 두고 거기로 안 가면 실패라고 말한다.
  `None` 은 **모름이어야 맞는** 질문이다 — 규칙이 아무 데나 걸리는 것도 고장이다.
- **읽기 전용이다.** 큐에도 안 넣고 엑셀은 열지도 않는다. 물어봤을 뿐인데 값이 바로잡히면
  아무도 안 묻는다. **비싼 탐색도 안 한다** — Z: 재귀 glob 은 부르지 않는다(`[168]`).
  질문 하나에 1초를 넘기면 사람은 그냥 클로드에게 묻는다.
- **스스로 자란다 — 모델 없이.** 못 답한 질문은 `reports/앱_자문기록.json` 에 쌓이고
  `python local_ai.py --stats` 가 **"이 모양이 N번 왔는데 아직 규칙이 없다"** 를 보여 준다.
  다음에 만들 규칙이 추측이 아니라 기록에서 나온다. 얼마나 아갔는지도 그 파일이 답한다.
- 근거 파일을 늘리거나 새 질문 갈래를 만들면 `SOURCES` · `INTENTS` · `PROBES` **셋 다** 손대는 것이다.
  `PROBES` 를 빼먹으면 그 규칙은 아무도 안 지키는 규칙이 된다.
- ⚠ 근거 파일의 **칸 이름을 짝짓하지 말 것.** ERP 색인은 `{src, count, index}` 이고 알장이
  `index` 아래에 있으며 칸 이름은 **영문**(`state`/`supply`/`cust`/`po`)이다. 한글로만 물으면
  **모든 번호가 '상태 없음'으로 나온다** — 오류는 안 난다(`[165]` 와 같은 함정).
- 검증 `[181]`.

## ★ 앱이 스스로 수집한다 — 수집 루프에서 클로드가 빠진다 (2026-08-09 지시)
같은 지시(위)의 나머지 반쪽이다. `[181]` 이 **되묻기**를 줄였다면 이것은 **수집**을 줄인다.

- **크레딧이 새던 또 한 자리**: 밴드는 조회 API 가 없어 **로그인된 브라우저 DOM** 안에서만
  글·댓글이 읽힌다. 그 DOM 에서 JS 를 돌릴 수 있는 것은 둘뿐이었다 — 클로드(브라우저
  도구)냐, 사람 브라우저 안의 스크립트냐. 지금까지는 **클로드가 매번 수집기를 주입**해
  크레딧을 썼다(이 대화가 바로 그 예다).
- **유저스크립트가 그 자리를 대신한다** — `band/band_auto_collect.user.js`. Tampermonkey
  에 **한 번 설치**하면 로그인된 밴드 탭이 스스로 긁는다. 클로드는 수집 루프에서 완전히 빠진다.
- **'AI 알고리즘'(무엇을 긁을지)은 회차가 미리 계산해 앱이 내려 준다** — 원장 읽기가
  비싸 웹 요청마다 다시 계산하지 않는다. 09:50 회차의 `comment_backfill.py --write` 가
  우선순위(열린 원장 행 = 1순위, `[177]`)를 골라 `reports/밴드_수집계획.json` 에 적고,
  앱이 `/api/collect_plan?band=<no>` 로 그대로 내려 준다. 유저스크립트는 **실행만** 한다 —
  무작정 최근순으로 긁지 않는다(`[177]`).
- **정본 수집기는 하나다** — 앱이 `/grab_posts.js` 로 내려 주는 그 파일. 규칙이 바뀌면
  앱만 고치면 되고 유저스크립트는 안 바꾼다(붙여넣기 파일과 같은 원칙, `[162]`).
- **탭이 보일 때만 긁는다** — 밴드 본문은 숨은 탭에서 안 그려지므로 `grab_posts.js` 가
  알아서 멈춘다. 이 창을 열어 두기만 하면 된다. 밴드당 3시간 간격으로 몰아 긁기를 막는다.
- ★ **확인된 0개와 못 읽음을 가른다** (2026-08-09 실측). 댓글 0 인 글은 '댓글 N' 표시가
  없어 예전엔 전부 '미확인'이 돼 **영원히 다시 뽑혀 백필이 수렴하지 못했다.** 이제 댓글
  입력창(`_commentInputRegion`)이 있는데 목록이 0 이면 **확인된 0개**로 닫는다(`[162]`).
- **흡수·대조는 그대로 회차 몫**: 유저스크립트가 내려받은 덤프를 09:50 `download_intake`
  가 Z: 로 옮기고 `convert_dump` 가 캐시로 합치고 `cancel_watch`·`cross_signal` 이 읽는다.
  즉 **설치 한 번 뒤로는 손이 안 간다.**
- 검증 `[182]`.

### ★ 그런데 그 스크립트가 **나흘 동안 한 번도 안 돌았다** — 되보고 (2026-08-13 지시)

사용자 지시: **"앞으로 크롬에서만 긁어오는 알고리즘 만들어서 적용해"**

- `[182]` 는 2026-08-09 에 만들어져 검증까지 붙어 있었는데 **2026-08-13 까지 한 번도
  안 돌았다**(Tampermonkey 미설치). 그런데 그 사실을 말해 주는 화면이 **한 곳도
  없었다.** 나흘 동안 지시문에는 '자동 수집이 있다'고 적힌 채 아무 일도 안 일어났다.
  **회차는 하나도 실패하지 않는다** — 긁는 것이 브라우저 안이라 스케줄러가 모르는
  축이다. `schedule_watch` 가 회차에 대해 하는 일을 여기서는 **브라우저**에 대해 한다.
- 길은 넷이고 **이제 다 이어졌다**: 유저스크립트 `POST /api/collect_report` →
  `app_server.collect_report_save` → `band/userscript_watch.py` → 워치독(30분) →
  인계 '먼저 처리할 것'. 사람이 볼 것은 `python band/userscript_watch.py --print`.
- ★ **받는 자리는 PIN 게이트 앞이다.** 보내는 쪽은 `band.us` 탭이라 이 서버의 인증
  쿠키를 못 싣는다(다른 출처다). 뒤에 두면 **모든 되보고가 401 로 버려지고** 감시자는
  '한 번도 안 옴'이라 말한다 — 스크립트는 멀쩡히 도는데도(`[169]`). 대신 문을 좁힌다:
  본문 20KB 제한 · 상태 한 줄만 · **밴드번호 모양 검사**.
- ★ **유령 밴드를 키로 받지 않는다.** `260807`(날짜 꼬리표)·`202608082047`(시각 도장)이
  한 번 들어가면 그 번호가 '아는 번호'가 되어 스스로를 되살린다(2026-08-12 실사고).
  7~10자리만 받고 아니면 **400 으로 거절**한다.
- ★ **못 적었으면 못 적었다고 말한다.** 저장이 실패했는데 200 을 주면 스크립트는 보고가
  도착한 줄 알고, 감시자는 빈 파일을 보고 '안 옴'이라 한다 — 둘 다 틀린 채로 조용하다.
- ★ **인계는 셋을 가른다**: 키가 **아예 없다**(안 물었다 — 아무 말 안 함) · `None`
  (물었는데 **실패** — '확인 못 함') · **빈 목록**(정상 — 아무 말 안 함).
  만들면서 그대로 함정에 빠졌다 — 키 없음을 실패로 읽어 **없는 경보**가 났고
  `t111` 이 잡았다. 조치 칸에는 **붙여넣어 도는 명령**만 넣는다(안내 문장을 거기
  적으면 명령처럼 보여 사람이 그대로 붙여넣는다).
- ⚠ **되보고 파일에 시험 행을 섞지 말 것.** 만들면서 살아 있는 서버로 시험 POST 를
  넣었더니 `reports/크롬수집_보고.json` 이 **6시간 동안 '정상'을 확언**하게 됐다 —
  이 기능이 막으려던 바로 그 모양이다. 지우고 다시 쟀다. 검증도 임시 경로로만 잰다.
- ⚠ 같은 날 잡은 것: `userscript_watch.py` 에 `sys.stdout` 보호가 없어 **인계 문서가
  알려 주는 그 명령**(`--print`)이 콘솔에서 죽었다(cp949 가 `—` 를 못 쓴다).
  무인 회차는 `sys.stdout` 이 None 이라 같은 자리에서 또 죽는다(`[235]`).
- **긁는 것 자체는 여전히 'CSOS 리서치 및 자료 수집' 세션 몫이다** — 여기는 보고 말하는 자리다.
- ★ **그 감시자가 '안옴'의 원인을 확언하고 있었다 — 2026-08-19 실측으로 반증됐다.**
  안내가 *"되보고가 0건이라는 것은 밴드 글 화면이 크롬에 한 번도 안 열렸다는 뜻이다"*
  였는데, 그날 로그인된 크롬으로 `www.band.us/band/84789192` 를 **실제로 열었다** —
  피드 카드 10장 · 본문 3,597자 · 그 페이지에서 `/api/ping` **200**(되보고 길도 살아 있다).
  그런데 되보고는 **그대로 0건**이었고, 그 탭의 `localStorage` 에 이 스크립트가 쓰는
  `coupangAutoCollect.beat.<밴드>` 열쇠가 **하나도 없었다** — 본문이 한 줄도 안 돈 것이다
  (붙여넣기 경로 자국 `__grabBeat`·`__grabDeath` 는 있으니 크롬 프로파일은 맞다).
  · **틀린 지목은 못 잡는 것보다 나쁘다**(`[172]`) — 그 안내를 따르면 사람은 이미 한 일을
    또 한다. 이제 후보 **둘**(안 열림 / 안 들어감)을 나란히 두고 **가르는 법**을 준다:
    그 탭 콘솔에서 `coupangAutoCollect` 열쇠가 비면 ②다.
  · **왜 안 들어갔는지는 아직 모른다 — 모른다고 적는다**(`[169]`). `@grant none` 이라
    페이지에서 `GM_info` 로 확인할 수 없고, 근거 없이 Tampermonkey 를 지목하면
    그것이 또 하나의 틀린 지목이다. 분담판에 남겼다.
- 검증 `[247]`.

## ★ 진짜 0 인 밴드가 백필을 무한루프에 가둔다 — `comments_full` 은 믿는다 (2026-08-11 실사고)
- **증상**: 90610953 열린 원장 1순위 95건을 두 번 재수집·흡수했는데도 1순위가 계속 95건.
  줄지 않았다. 화면도 오류도 멀쩡했다.
- **원인**: `comment_backfill.blind()` 의 distrust 로직([162] 사고 방지)은 밴드에
  '댓글 담긴 글이 0'이면 수집기가 깨진 것으로 의심해 `comments: []`(확인된 0)를
  '못 읽음'으로 되뽑는다. 그런데 이 밴드는 **댓글 담긴 글이 실제로 0**이다(캐시 5,255글
  중 댓글 0). 하필 1순위 95건이 전부 진짜 댓글 없는 글이라 아무리 긁어도 distrust 가
  안 풀린다 → 영원히 같은 95건. **실패가 성공처럼 보이는 자리**의 반대편 —
  성공(진짜 0)이 실패(못 읽음)처럼 보여 무한히 다시 긁게 만든다.
- **고친 것**: 수집기가 이미 다는 `comments_full`([182] '확인된 0개' — 입력창까지
  그려진 뒤 목록이 0)을 **distrust 여도 믿는다.** 되뽑는 것은 '비었고 & comments_full
  도 아닌' 글뿐. [162] 가드는 유지된다 — 깨진 수확은 `comment_count>0` 인데 목록이 0 이라
  `comments_full=False` 로 남아 계속 재선택된다. 경보 문구도 '확인된 0개'와 '미확인'을
  갈라 정직하게 말한다(안 그러면 고쳤는데 안 고친 걸로 읽힌다, `[169]`).
- **실측**: 90610953 확인된-0개 272 · 미확인 4,468 · 댓글 담긴 글 0. 1순위 **95→1**
  (남은 1건은 comments_full 미확정 = 정당한 재수집 대상). 미확인 4,468 은 로그인 밴드
  탭에서 `band/댓글채우기_붙여넣기_90610953.js` 를 돌려 확인된-0개로 바꿔야 완전히
  수렴한다(수집 세션 몫).
- **교훈**: "0건"을 볼 때 [169]는 "없는 건가, 안 본 건가"를 물으라 했다. 여기선
  한 겹 더다 — **'안 본 것'으로 의심하는 계기 자체가 진짜 0 을 안 본 것으로 오판**해
  루프를 만들 수 있다. 의심에도 근거(comments_full)가 있어야 한다.
- 검증 `[199]`.

## ★ 폰이 PC 없이 답한다 — 규칙은 한 곳, 답은 미리 만들어 싣는다 (2026-08-09 지시)
사용자 지시: **"대시보드에 내가 앱에 텍스트로 명령하면 클로드 코드처럼 조사하고 정리하고
답변주고 구현하는 기능을 앱에 탑재해서 클로드 코드 안 거치고 대시보드에서 처리할 수 있는
기능 코딩해 (PC가 꺼져있어도 휴대폰으로 들고다니면서 업무처리할 수 있게 고도화 작업 진행해)"**

- `[181]` 이 만든 답변기는 **PC 가 켜져 있을 때만** 돌았다(`/api/ask`). 폰 사본은
  GitHub Pages 의 잠긴 `data.enc` 라 파이썬이 없다. 그렇다고 **규칙을 JS 로 옮겨 적으면
  같은 판단이 두 곳에 생기고, 갈린 뒤에는 어느 쪽이 맞는지 아무도 모른다.**
- 그래서 **답을 미리 만들어 싣는다** — `local_ai.answer_pack()` 을 `cloud_publish.payload()`
  가 `d["ask"]` 로 담는다. 실측 압축 **51KB · 만드는 데 0.1초**(미리 만든 답 8 + 번호 2,014).
  폰은 사본이 준 **말투로 갈래만 고르고** 그 답을 읽는다. 판단은 여전히 파이썬 한 곳이다.
- ★ **말투는 JS 에서도 컴파일돼야 한다.** 파이썬에서만 되는 문법(`(?P<>`·`(?i)`·`\A`)을 쓰면
  폰에서 **그 갈래만 조용히 죽는다** — 오류도 안 난다(`[169]` 와 같은 모양). 검증이 막는다.
- ★ **번호 알아보는 말투도 사본이 준다**(`번호말투`). 폰이 제 손으로 `[A-Z]{2}\d{7}` 을
  적으면 그것도 갈릴 사본이다 — 번호 모양이 바뀌는 날 폰만 못 알아본다.
- ★ **만든 시각을 항상 같이 띄운다.** 꾸러미는 PC 가 꺼져 있는 동안 낡는다.
  **낡은 답을 시각 없이 자신 있게 보여 주는 것**이 여기서 제일 위험하다.
- ★ **'규칙에 없는 질문' 과 '근거가 없어 답이 안 실린 갈래' 를 가른다.** 실측으로 걸렸다 —
  `앱서버` 갈래는 규칙이 멀쩡히 있는데 인계 문서에 그 항목이 없어 답이 안 실렸다.
  뭉쳐서 "규칙에 없습니다"라고 하면 **사람이 이미 있는 규칙을 다시 만들러 간다.**
- 못 답하면 **지어내지 않고** 클로드에 붙여넣을 문구를 만든다(사본 만든 때가 박혀 있다).
- **쓰는 길은 넓히지 않았다.** 폰은 원장을 직접 못 쓴다 — 등록은 예전대로 `cloud_queue`
  예약이고 사람이 확인창에서 승인한 것만이다(`[170]`).
- **정직하게 적어 두는 한계**: "클로드처럼 **코드를 짜는**" 부분은 모델 없이 안 된다.
  이 설계가 대신하는 것은 **조사·정리·답변·예약**이며 실측된 질문의 대부분이 그것이었다.
  다음에 만들 규칙은 `python local_ai.py --stats` 의 '아직 규칙이 없는 갈래'가 알려 준다.
- 검증 `[184]`.

## ★ PC가 꺼져도 앱은 외부 연속운영으로 남는다 (2026-08-14 지시)
사용자 지시: **"내가 이 PC를 꺼도 앱이 구동되고 업무볼 수 있는 코딩 고도화 작업 진행해"**

- **꺼진 PC에서는 어떤 로컬 코드도 실행되지 않는다.** 그러므로 8899 서버·터널을 더 세게
  재시작하는 것으로 이 지시를 완료했다고 말하지 않는다. 고정 앱은
  `https://mulder4780.github.io/coupang-ecount-reconcile/`, 외부 보관 정본은 Sites+D1
  `csos_cloud_queue_site`다. 둘은 사무실 PC 전원과 다른 실패 영역이다.
- 읽는 순서는 **D1 최신 암호문 → GitHub Pages `data.enc` → 서비스워커의 기기 사본**이다.
  외부에는 PIN·원문·API 키를 평문으로 올리지 않고 `csos_crypto` 암호문만 둔다. D1 쓰기는
  작업 토큰이 있어야 하며 GET은 암호문만 반환한다. `/api/ping`에는 내용이 아니라 사본
  기준시각만 나온다.
- PC가 켜져 있는 동안 앱 서버가 **10분마다** `cloud_publish.py --cloud`로 최신 내용을
  확인한다. 같은 내용이면 D1 대형 암호문을 다시 쓰지 않는다. `--cloud` 회차는 추적 중인
  `docs/data.enc`도 건드리지 않는다. **3시간마다** `--push`로 GitHub Pages 비상 사본도
  갱신한다. 갑자기 전원을 내려도 최대 지연을 3시간에서 10분으로 줄인 이유다.
- PC OFF 입력은 원장을 직접 쓰지 않는다. 프로젝트 등록 예약은 외부 D1의 멱등 키로 영구
  보관하고, 통신까지 끊기면 먼저 기기 큐에 둔 뒤 복구 즉시 D1로 보낸다. PC가 돌아오면
  서버 연속운영 루프가 사람의 앱 실행을 기다리지 않고 `cloud_queue_sync.py`를 먼저 돌린다.
  **lease → 앱 DB 저장 → ack** 순서이며, 실패는 release해 다음 회차가 잇는다. ack 전에
  지우거나 Excel을 바로 열지 않는다. 워치독 30분 회차도 같은 복귀 경로의 2차 안전망이다.
- 화면은 언제나 **사본 기준시각과 출처(클라우드/비상 사본)**를 표시한다. PC OFF 중에는
  외부 원본 수집·ERP 실조회·Excel 생성이 진행되지 않으므로 이를 실시간이라고 부르지 않는다.
  조회·미리 만든 조사 답변·프로젝트 확인·등록 예약은 계속되며, 전체 실시간 기능은 PC 복귀 뒤
  고정 앱의 별도 버튼으로 연다.
- 검증 `[271]`.

## ★ 카톡 한 파일을 **끝까지** 반영한다 · 죽은 회차의 tmp 가 다음을 막지 않는다 (2026-08-09 지시)
사용자 지시: **"이거 반영하고 엑셀 및 앱에 반영해
(내가 반영하라고 명령하면 규칙 무시하고 반영하는 알고리즘 추가)"**

- 빠져 있던 것은 '반영하는 기능'이 아니라 **한 줄로 끝까지 가는 길**이었다.
  조각은 다 있었다 — 흡수는 `download_intake`, 추출은 `kakao_extract --new --queue`,
  엑셀 쓰기는 `ledger_db --apply`. 그런데 사람이 카톡 파일을 건네면 그 셋을
  **손으로 이어야** 했고, 이어지는 도중 어디서 끊겼는지는 아무 화면에도 안 나왔다.
- `python kakao_apply.py <파일...> --now` 하나다(새 파일 없이 돌리려면 경로를 비운다).
  네 단계가 순서대로 돌고 단계마다 `reports/카톡_반영회차.json` 에 자국을 남긴다.
- ★ **없는 파일을 조용히 건너뛰지 않는다.** 주신 경로가 실제로 없는데 나머지
  단계가 멀줦히 돌면 **'0건 반영 성공'** 으로 끝난다 — 숫자도 나오고 오류도
  안 나서 아무도 안 본다(`[169]` 와 같은 모양). 하나라도 없으면 **멈춘다**(exit 2).
  추출이 실패해도 엑셀을 안 열어다 — 실패한 채로 반영하면 **없는 것이 반영된 것처럼** 보인다.
- **원본은 옮기지 않고 복사한다.** 사람 바탕화면 파일을 지우면 "그때 정말 무엇을
  받았나"를 사람 쪽에서 잃는다. 같은 내용이면 이름이 달라도 다시 안 넣는다(해시).
- **즉시 반영은 사람이 명령했을 때만**(`--now`). 이것은 2026-08-07 지시의 연장이지
  규칙이 뒤집힌 것이 아니다. 무인 경로(`COUPANG_UNATTENDED=1`)는 거부하고,
  `daily_run`·`session_wrapup` 이 이 길을 즉시반영으로 부르지 않는지를 검증이 지킨다(`[93]`).
- ★ **모르는 깃발을 조용히 무시하지 않는다** (2026-08-19). `given` 이 `--` 로 시작하는
  것을 통째로 버려서, 실측으로 **`--help` 가 도움말 대신 회차를 통째로 돌았다.**
  `--nwo` 같은 오타는 더 나쁘다 — 엑셀 반영을 시킨 사람에게 큐까지만 하고 **"완료"**
  라 답한다(오류도 안 나고 숫자도 나온다 · `[169]`). 이제 `unknown_flags()` 가
  **아무것도 하기 전에** 멈춘다(exit 2).
  · `-` 하나짜리(`-now`)도 깃발로 본다 — 파일 이름으로 읽으면 *"파일을 다시
    내려받으십시오"* 라는 **엉뚱한 안내**가 나간다(`[172]`). 경로가 정말 `-` 로
    시작하면 `./` 를 붙인다.
  · 쓸 수 있는 깃발은 `KNOWN_FLAGS` **한 곳**이 정한다(`[162]`) — 안내 문구도 거기서 나온다.
  · 글자 검사로는 '정말 멈추는가'를 못 잰다 — **불러서** 잰다(`[295]`). 계기 자신도
    시험한다(`[272]`): 표를 넓히면 통과해야 한다. 안 그러면 그 검사는 표가 아니라
    어딘가에 박힌 낱말을 보고 있는 것이다.

### ★ 실패 하나가 **모든 다음 회차를 영원히** 막고 있었다 (같은 날 실사고)
- 11:00 회차가 쓰다 죽으며 `v571.tmp.xlsx` 를 남겼고, 그 뒤 11:00·15:00·사람 지시
  반영이 **전부** `FileExistsError: 임시 결과가 이미 존재` 로 죽었다. 그 줄은 리포트
  깊숙이 한 줄로만 남고 화면은 여전히 "반영 대기 N건"만 보여 준다 —
  **실패가 성공처럼 보이는 자리**다(`[171]` 과 같은 종류).
- 가르는 근거는 **나이**다(`ledger_writer.STALE_TMP_MIN`, 기본 30분):
  · 방금 생긴 tmp = **다른 writer 가 지금 쓰는 중** → 손대면 안 된다(그대로 멈춘다)
  · 오래된 tmp = 죽은 회차의 찌꺼기 → **옆으로 치우고 계속 간다**(`.stale-<날짜>.xlsx`)
- **지우지 않는다.** 그 안에 마지막 회차 결과가 들어 있을 수 있고, 깨진 zip 인지는
  사람이 열어 봐야 안다. 치우는 것까지가 기계 몫이고 **판단은 사람 몫**이다.
- 정본(vN+1)이 이미 나왔으면 그 tmp 는 남의 것이므로 안 건드린다.
- 못 치우면(누가 물고 있으면) 그것은 '쓰는 중'이므로 예외를 그대로 올린다.
- 검증 `[186]`.

## ★ 무상은 무상, 보험은 보험 — 빈칸은 **무상이 아니다** (2026-08-09 지시)
사용자 지시: **"무상이면 무상 보험이면 보험 표시 /
보험사에서 돈이 입금 되면 그것도 찾아 반영하는 알고리즘 구성"**

- 판정이 한 줄이었다: `비용구분 != '유상'` 이면 전부 **`무상/보험`**.
  그래서 **비어 있는 칸까지 무상이라고 불렀다.** 실측 750행:
  유상 716 · **무상 2** · **보험 0** · 미확정 4 · 빈칸 26 · `'0'` 2.
  회색 딱지 34건 중 진짜 무상은 2건이고 **32건은 아직 안 적은 칸**이었다.
  그중 **31건에 금액이 있다** — UJ2601280 울산2캐프 3,049,310원 ·
  UJ2601288 인천5MB 672,540원(명세서까지 나간 건이다).
- **무상이라 부르는 순간 청구 대상에서 조용히 빠진다.** 비어 있는 값은 눈에 띄지만
  **틀린 딱지는 안 띄다** — `[165]` 와 같은 종류의 사고다.
- 이제 세째로 말한다: **`무상`** · **`보험`** · **`비용구분 미입력`**.
  모르는 것을 아는 것처럼 부르지 않는다.
- ★ **딱지를 가르면 그 문자열을 손으로 적어 둔 곳들이 오류 없이 안 걸리게 된다.**
  그래서 '청구하지 않음'을 읽는 자리를 `ecount_reconcile.NON_BILLABLE` 한 곳으로 모았다.
  예전 값 `무상/보험` 은 남겨 둔다 — 리포트·DB 에 이미 적힌 것이 있다.
  **`미입력` 은 절대 여기 넣지 않는다** — 넣는 순간 32건이 다시 조용히 사라진다.
- **엑셀에 쓸 값은 없다.** 미입력 32건을 기계가 채우면 안 된다 —
  유상인지 무상인지는 사람만 안다. 화면이 **주황 딱지**로 물을 뿐이다.

### ★ 보험사 입금은 어느 화면에도 안 떴다 — `insurance_watch.py`
- 보험금은 **쿠팡이 아니라 보험사가** 낸다. 그런데 입금을 찾는 `receipt_fill` 은
  '26년도 쿠팡 입금내역'만 본다(실측 101건 중 81건이 쿠팡로지스틱스, 나머지는 물류사).
  그러니 보험사 돈이 들어와도 **받을 돈인데 안 받아도 티가 안 난다.**
- **양쪽에서 본다.** ① 원장 → 입금: `보험` 행에 입금일이 비었나
  ② 입금 → 원장: 거래처가 보험사 모양인 돈이 원장 어디에도 안 붙었나.
  ②가 더 값어치 있다 — 원장에 `보험` 이라고 안 적혔어도 보험사 돈이 들어왔다면
  그건 보험 건이다. **표시가 빠진 것을 돈이 알려 준다.**
- 보험사는 **회사 이름이 아니라 업종 낱말**로 고른다(보험·해상·화재·손해·공제).
  회사 이름을 박아 두면 목록에 없는 보험사 돈은 영영 안 보인다.
- 자동으로 채우는 문은 좁다(`receipt_fill` 과 같은 양방향 유일 일치).
  보험금은 자기부담금·감가로 청구액과 **다르게** 들어오는 일이 잦아 대부분 사람 몫으로 남는다.
  억지로 붙이면 엉뚜한 현장에 남의 돈이 꽂힌다 — 못 찾는 것보다 나쁘다.
- ★ **첫 판은 0건이다**(원장 보험 0 · 입금 101건 중 보험사 0). 그것을 **0이라고만 적지 않는다** —
  리포트가 '없는 것인가, 안 본 것인가'를 스스로 적고 **무엇을 보고 0이라 했는지**를
  밝힌다(`[169]`). 보험사 입금이 찍히는 은행·ERP 자료를 `7. 입금내역` 에 넣으면
  다음 회차부터 자동으로 본다.
- 09:50 회차 단계다(`insurance_watch.py --queue`). 결과는 `reports/보험입금_확인.md`.
- 검증 `[187]`.

## ★ 오타·오기입 탐지 — `typo_watch.py` (2026-08-09 지시)
사용자 지시: **"오타 오기입 이런거 잡아낼 수 있는 알고리즘 구성해서 적용해"**

- **손으로 적는 칸에는 반드시 오타가 생기고, 오타는 빈칸과 달리 눈에 안 띈다.**
  `PO372139` 를 `PO372936` 이라 적어도 화면은 멀쩡히 번호를 보여 준다.
- `python typo_watch.py` 하나다(09:50 일일대조 단계). **읽기 전용 — 아무것도 안 고치고
  큐에도 안 넣는다.** 무엇이 맞는지는 사람만 안다. 자동으로 고치면 "그때 정말 뭐라고
  적혀 있었나"를 잃는다. 결과는 `reports/오기입_확인.md`.
- **정답 목록이 따로 있는 것만 본다** — 지어내지 않는다: PO번호←쿠팡 PO목록 ·
  프로젝트NO←ERP 색인 · 캠프명←원장에서 3회 이상 쓰인 이름 · 금액←ERP 와의 자릿수 관계.
- ★ **어려운 것은 잡는 게 아니라 잘못 지목하지 않는 것이다.** 오타라고 잘못 부르면
  사람이 **멀쩡한 값을 고치러 간다** — 못 잡는 것보다 나쁘다. 첫판이 750행에서
  **163건**을 쏟았고 두 번 다 규칙이 틀린 것이었다:
  · 날짜 160건 — 전부 **명세서발행일이 완료일보다 1~3일 앞선 것**. 그건 오타가 아니라
    정상 업무다(명세서를 먼저 끊는다). 이제 **연도 한 자리 차이**나 **1년 이상**만 본다.
  · `제주3캠프` → `양주3캠프` 로 지목했는데 **제주에는 제주1·2·3캠프가 다 실재한다.**
    '드물게 쓰인 것'과 '잘못 쓰인 것'은 다르다. 이제 **괄호 안 지명이 같은데 앞 숫자만
    다른 것**만 본다(`송파1MB(감일동)` 1번 ↔ `송파5MB(감일동)` 4번).
  · **163 → 1건.** 남은 1건은 근거가 선다.
- 문 셋: **후보가 유일할 때만** 지목 · **길이가 같을 때만** 번호 오타로 봄 ·
  **절대 자동으로 고치지 않음.**
- 검증 `[172]`.

## ★ 캐시를 갈아끼울 때 — 읽는 쪽이 물고 있으면 실패한다 (2026-08-08 실사고)
- 흡수가 `os.replace(tmp, dst)` 에서 **`PermissionError [WinError 5]`** 로 죽었다.
  앱 서버가 마침 `band/cache/84789192.json`(5MB)을 읽는 중이었다. 리눅스와 달리
  **윈도우는 열려 있는 파일을 갈아끼우지 못한다.**
- ★ 남는 그림이 나쁘다: 새 글을 다 긁어 놓고도 캐시는 **어제 것**이고 `.tmp` 만 남는다.
  다음 회차는 그 옛 캐시를 보고 **"바뀐 것 없음"** 을 내놓는다 — 아무 일도 안 일어났는데
  안심시키는 결과다. **실패가 성공처럼 보이는 자리**다.
- 이제 `band/convert_dump.py` 의 `swap_in()` 한 곳을 거친다. 읽는 쪽은 곧 놓으므로
  **물러서며 여섯 번**(0.5s→1.0s→…) 다시 건다(실측 3번째에 성공).
- **끝내 안 되면 예외를 올린다.** `.tmp` 를 지우고 조용히 넘어가면 애써 만든 새 캐시까지
  잃는다. 실패는 실패라고 말하고, 그 `.tmp` 는 사람이 쓸 수 있게 남긴다.
- 큰 파일을 원자적으로 갈아끼우는 다른 자리를 만들면 **여기를 그대로 쓴다.**
- 검증 `[171]`.

## ★ 경보가 대부분이면 기준을 의심한다 (2026-08-08 실사고)
사용자 질문: **"작업은 완료인데 왜 계산서 발행이 안된거지"** 에서 시작해 둘이 나왔다.

- **화면과 대조기가 서로 다른 금액을 보고 있었다.** PO 대조 유형C(금액 불일치)가
  51건 중 **44건**이었다. 근거는 전부 `원장공급가액합: 0` — 06시트 공급가액이 사람 손
  입력이라 비어 있었을 뿐이다. ERP 로 대면 **원 단위까지 맞았다**
  (PO327948 쿠팡 7,551,500 = ERP 합 7,551,500). `app_server` 는 이미
  실제작업 → ERP → 명세서 사다리로 채워 보여 주고 있었는데 `po_reconcile` 만 안 썼다.
  · **경보가 44/51 이면 그 경보는 아무도 안 본다.** 조용한 사고의 반대편이지만
    결과는 같다 — 아무도 안 본다. 경보 비율이 절반을 넘으면 **기준부터 본다.**
  · 같은 값을 두 곳에서 계산하면 언젠가 갈린다. `supply_of()` 로 사다리를 한 벌로 맞췄다.
  · 고친 뒤 C 는 35건이 됐고 **이유를 말한다**: 원장 연결 누락 20 · 진짜 금액 차이 15.
    쿠팡=ERP 인데 원장만 모자라면 그건 금액 오류가 아니라 **연결이 빠진 것**이다.
- ★ **경보는 넓혀도 되지만 자동으로 쓰는 자리는 안 넓힌다.** 유형A~C 는 사람이 보는
  경보라 금액 출처를 넓혀도 잃는 것이 없다. 그러나 **유형D 는 06시트에 PO번호를
  써 넣는 길**이라 그대로 뒀다 — 짐작으로 채운 금액으로 짝을 지으면 틀린 PO번호가
  원장에 박히고, **그건 빈 칸보다 나쁘다.** 읽는 길과 쓰는 길의 기준은 달라야 한다.
- **'세금계산서 미발행' 한 덩어리에 가야 할 사람이 다른 것들이 섞여 있었다.**
  실측 293건: `ERP 3.오더처리` 188 · **`ERP 전표 없음` 62** · **`발행 대기(ERP 4단계)` 22**.
  · '발행 대기'는 PO 도 왔고 금액도 맞고 명세서도 나갔다 — **류지영이 ERP 에서
    넘기기만** 하면 된다. '전표 없음'은 아예 **등록부터**다.
  · 한 딱지로 묶어 두면 화면이 "누구에게 넘길지"를 말해 주지 못한다. 카드에
    `미발행사유` 딱지를 붙였다(`app_server` 가 내려보내고 정산 카드가 보여 준다).
- ★ **짐작 대신 원본이 말하게 한다 — 유형E** (2026-08-08 지시 "근거 찾아서 완료
  처리할 수 있는건 다 완료 처리해"). '원장 연결 누락 20건'은 누가 빠졌는지 **짐작할
  필요가 없었다**: ERP 판매전표가 **프로젝트NO 와 PO번호를 한 줄에 같이** 담고 있다.
  금액이 우연히 같아 고르는 D 와 달리 원본이 직접 말한 것이라 근거가 세다.
  · 그래도 쓰는 자리라 문을 넷 건다: ERP 의 PO 토큰이 **유일**할 것 · 그 번호가
    **쿠팡 목록에 실재**할 것(ERP 오타를 원장에 옮기지 않는다) · 원장 칸이 **빌 때만** ·
    큐에서도 `only_if_empty` 로 한 번 더(대조와 반영 사이에 사람이 채웠을 수 있다).
  · 실측 **27건**을 큐에 넣었다. 교차검증: PO370038 의 14건 합 5,405,100 =
    그 PO 의 ERP 전체합 5,405,100 — **정확히 일치**했다(나머지 PO 가 부분합인 것은
    이미 원장에 적혀 있던 행을 안 건드리기 때문이라 정상이다).
  · 같은 회차에 `billing_status --queue` 가 청구상태 **286건**을 채웠다(근거 ERP 7.수금완료).
  · 둘 다 **11:00·15:00 회차**로 들어간다 — 임의 시각에 원장을 열지 않는다.
- ★ **번호로 물을 수 있으면 금액으로 묻지 않는다** (같은 날 이어서). 유형A(미청구)는
  '금액이 같은 계산서가 ERP 에 있나'로만 판정했다. `PO336120`(쿠팡 38,000,000)이
  미청구로 나왔는데 ERP 를 **프로젝트로** 보면 `UJ2600211 · 6.세금계산서발행 ·
  37,577,911` — 이미 끊긴 것이었다. 422,089원 차이 때문에 금액 대조가 못 잡았다.
  이제 `erp_state_by_po()` 로 **번호를 물어본다**(A 6→5, 발행됨 47→48).
- ★ **가를 수 있는 것을 '또는'으로 묶지 않는다.** 유형B 판정이 "번호 오기입 **또는**
  목록 기간 밖"이라 사람이 **없는 오타를 찾아 나섰다.** 이제 정산 날짜와 쿠팡 PO목록
  기간을 대 보고 갈라 말한다. 단정할 수 없으면 **단정하지 않고 두 날짜를 보여 준다.**
- ★ **[168] 을 그대로 다시 밟았다.** ERP 색인 쓰는 곳이 넷으로 늘자 각자 `build()` 를
  불러 **Z: 를 네 번 훑었고** 560초 제한에 걸려 요약 한 줄도 못 찍고 죽었다.
  `erp_idx()` 한 곳으로 모았다. **비싼 탐색은 쓰는 곳이 늘어날 때 다시 본다.**
- 검증 `[170]`.

## ★ 청구상태 — ERP 수금확인으로 맞춘다 (2026-08-08 지시)
사용자 지시: **"ERP 기준으로 확정하고 객관적으로 입증되면 엑셀에 완료처리해"**
- `python billing_status.py --queue` 하나다(09:50 일일대조 단계로도 돈다).
  근거는 **`settle_status == '완료(ERP 수금확인)'`** — 그 프로젝트의 ERP 전표가 전부
  `7.수금완료` 라는 뜻이다(섞이면 `erp_progress` 가 '혼재'로 돌려줘 안 걸린다).
  **원장 빈 칸은 근거가 아니다** — 그 착각이 '세금계산서 미발행 190건'의 정체였다.
- **이 열은 예외적으로 엑셀에 쓴다.** DB 전환 원칙의 예외 ①(사람이 엑셀에서 직접 읽는
  값)이다 — 데이터유효성 안내문이 "[필수] 작업완료부터 입금완료까지 현재 단계를
  고릅니다" 인, 사람이 보고 고르는 칸이다. DB 에만 적으면 뜻이 없다.
- **낱말을 지어내지 않는다.** 사다리는 열에 실제로 쓰여 있는 값이 정한다:
  `작업완료 → 거래명세서발행 → 세금계산서발행 → 청구 → 입금완료`. 마지막이 '완료'가
  아니라 **'입금완료'** 인 이유다. 새 낱말을 넣으면 사람이 쓰던 정렬·필터가 어긋난다.
- **한 방향으로만 움직인다.** 빈칸은 채우고(`only_if_empty=True`), 낡은 아래 단계는
  올리고(`False`), **뜻을 모르는 낱말은 손대지 않는다.** 사람이 적은 값을 덮는 것이
  위험한 게 아니라 **무슨 뜻인지 모르는 값을 덮는 것**이 위험하다.
- **회차로 만든 이유**: 수금은 매일 새로 생긴다. 한 번 손으로 맞추면 그다음 날부터
  낡는데, 낡은 단계 딱지는 **비어 있지 않아서 아무 화면에도 티가 안 난다.**
- 검증 `[166]`.

## ★ 이 세션도 스스로 이어진다 — **풀린 것도 말한다** (2026-08-11 지시)

사용자 지시: **"이 세션도 완전 자동화시켜 / 이 세션 100% 자동화시켜"**

- **자동화가 빠져 있던 자리는 '일을 하는 쪽'이 아니라 '막힘이 풀렸다는 사실을 아는 쪽'
  이었다.** 이날 실측 둘 — 둘 다 기계가 근거를 **이미 갖고 있었다**:
  · 분담판 `[34]`(사고 #38 검증)는 옆 세션이 `code` 를 잡고 있어 세워 뒀다. 그 세션이
    점유를 놓은 뒤에도 **어느 화면도 "이제 된다"고 말하지 않아** 사람이 "하던 작업
    진행" 을 두 번 쳤다. 인계 문서는 **막힌 것만** 적고 있었다.
  · 자동 마무리는 옆 세션이 살아 있으면 푸시를 보류한다(`[104]` — 그것은 옳다).
    그런데 그 세션이 사라지면 **미는 사람이 없다.** 폰·웹은 **푸시된 것만** 본다.
- `python worksplit_auto.py --run` 이 그 회차다(워치독 30분 안의 단계 `resume_parked`).
  `--run` 없이 부르면 판정만 보여 주고 아무것도 안 한다.
- **새 판단을 만들지 않았다.** 살아 있는 세션은 `session_wrapup._other_live_sessions`
  + `live_transcripts`, 점유 생사는 `ai_claim._is_dead`, 주인은 `worksplit._owner_state`,
  비밀값 형태는 `handoff_review.secret_findings` 를 그대로 빌린다. 같은 판단을 두 곳에서
  하면 언젠가 갈리고, 갈린 뒤에는 어느 쪽이 맞는지 아무도 모른다.
- ★ **기계 회차에는 '내가 누군지'가 없다.** 그래서 둘을 조심한다:
  · 대화기록 폴더를 못 찾는다(`CLAUDE_CODE_SESSION_ID` 가 없다) → 세션 안에서 한 번
    확인한 경로를 `reports/.대화기록_폴더.txt` 에 남겨 두고 회차가 그것을 읽는다.
    폴더 이름 규칙을 코드에 적는 것은 **짐작**이다 — 규칙이 바뀌면 조용히 0건이 된다.
  · `worksplit._owner_state` 는 부르는 쪽을 모르면 남의 '진행'을 8시간 뒤 고아로 본다 →
    **지금 일하는 사람의 항목까지 고아로 읽는다**(실측 `[34]`). 그래서 주인 sid 가
    살아 있는 창 목록에 있으면 건드리지 않는다. 잘못 고아로 읽으면 같은 일을 AI 에게
    한 번 더 시켜 **같은 파일을 둘이 고친다**(사고 #36).
- ★ **아무도 없을 때만 AI 에게 넘긴다.** 사람과 대화 중인 창이 있으면 그 창이 할 일이다 —
  거기에 AI 를 하나 더 붙이면 충돌한다. 그때는 인계 문서에 올리는 것으로 끝낸다(공짜고,
  볼 사람이 있다). 표는 **항목당 하나**뿐이고 회차당 하나만 만든다 — 회차마다 새 표를
  만들면 크레딧이 새고 큐가 쓰레기가 된다. `COUPANG_AUTO_AI=0` 으로 끌 수 있다.
  넘기는 자원은 **`code` 뿐**이다(관리대장·밴드·게시는 사람 인증·비가역 승인, `[190]`).
- ★ **푸시는 넓히지 않는다.** 밀지 않는 경우: 옆 창이 살아 있다 · upstream 이 없다 ·
  리베이스·머지 중이다 · 미푸시 범위를 **못 읽었다** · 그 범위에 비밀값 형태가 있다.
  **'못 읽음'을 '깨끗함'으로 치지 않는다**(`[169]`). `--force` 는 없다 — 거절되면 적고 넘긴다.
- **여전히 사람 몫**(자동으로 꾸며내지 않는다): 밴드·이카운트 로그인 · 경보 확인(`--ack`) ·
  `사람대기` 항목 · 비가역 승인. `[190]` 의 경계 그대로다.
- ★ **'풀렸다'는 어느 창에서 풀렸는지까지 말한다** (2026-08-12). 자원 점유가 풀려도
  **차선**(`lanes`)이 다른 창에서는 `--take` 가 거부된다 — 실측으로 인계가 `[39][48][50]`
  을 '가져가라'로 올렸는데 수집 차선 창에서는 안 됐다. 한 낱말이 두 뜻이 되면 사람은
  **없는 문을 민다**(`[169]` 모양). 이제 `_lane_hint()` 가 차선표를 읽어 붙인다:
  `막힘이 풀렸다 — … (collect 차선 창에서)`.
  · **'가능'을 거짓으로 바꾸지 않는다** — 차선은 남을 막는 자물쇠가 아니라 제 창의
    약속이라(`lanes.can()` 은 부르는 쪽만 본다) 차선을 안 정한 창은 그대로 할 수 있다.
    거짓으로 바꾸면 할 수 있는 일까지 세워 둔다.
  · 차선 이름을 코드에 적지 않는다 — `lanes.LANES` 를 읽는다(표가 바뀌면 글도 따라간다).
- 검증 `[225]` · `[238]` · 사고 #38 의 거부 경로는 `[224]`.

## ★ "코딩했다"와 "돈다"는 다른 말이다 — `schedule_watch.py` (2026-08-12, 분담판 [35])

- **자동화의 마지막 구멍이 여기였다.** 코드를 만들고 설치본을 만들고 위 '자동으로 도는
  것' 목록에 한 줄을 적으면 자동이 된 것처럼 보인다. 그러나 **실제로 도는지를 아는 것은
  그 목록이 아니라 작업 스케줄러**다. 실측 2026-08-12 — 프로젝트 어디에도
  `LastTaskResult` 를 읽는 코드가 **한 줄도 없었고**(`grep -rl` 0건) 그래서 이것들이
  전부 **아무 화면에도 안 떴다**:
  · `쿠팡업무_일일자동대조`·`쿠팡업무_원본자료자동정리` — `0xC000013A`. 제한시간(PT3H)에
    걸려 **매일 강제 종료**. 회차 실측 292.3분이라 `ROUND_BUDGET_MIN`(150)도 못 지켰다
    (예산은 **단계 사이**에서만 보므로 한 단계가 길면 그냥 지나친다).
  · `쿠팡업무_정오회차` — 등록이 안 돼 **한 번도 안 돌았다**(그 사고는 이미 위에 적혀 있다).
  · `쿠팡업무_밴드재수집` — exit 1.
- `python schedule_watch.py` 하나다(워치독 30분 단계 `watch_schedules`). 결과는
  `reports/스케줄러_회차감시.md` → 인계 문서 **'먼저 처리할 것'**.
- **읽기 전용이다.** 회차를 다시 띄우지도, 제한시간을 고치지도, 등록하지도 않는다.
  옳은 조치는 회차마다 다르다 — 지금 도는 중이면 **띄우면 안 된다**(잠금에 막혀 조용히
  건너뛰거나 같은 자료를 두 번 긁는다). 여기는 보고 말하는 것까지다(`typo_watch` 와 같은 자리).
- ★ **가르는 것이 세는 것보다 어렵다**(`[170]`). 회차가 열둘이면 '0 이 아닌 결과'는 늘
  몇 개다. 통째로 올리면 경보가 대부분이 되고 아무도 안 본다:
  **성공·도는중**(정상 — 5분 회차가 지금 도는 것은 사고가 아니다) · **밀림**(앞 회차가
  아직 돌아 거부됨 — 한 번은 정상, **연속 3회부터** 경보) · **죽음**(강제 종료·비정상 —
  한 번이라도 경보) · **안 돎**(예정이 지났는데 기록이 없다. **등록 안 됨**도 여기다).
- ★ **연속은 관찰이 아니라 회차를 센다.** 워치독이 30분마다 보므로 관찰을 세면 하루 한 번
  실패하는 회차가 "48회 연속"이 되고 밀림 판정이 첫날 아침에 터진다. 같은 회차인지는
  **마지막 실행 시각**이 말해 준다.
- ★ **감시자가 실패한 것만 골라 못 볼 뻔했다.** 첫 실행이 12개 중 **9개**만 가져왔다 —
  PowerShell 이 결과를 `[int]` 로 받는데 `0xC000013A`(3221225786)·`0x800710E0`
  (2147946720)이 **Int32 를 넘겨** 그 작업이 통째로 빠진 것이다. 빠진 셋이 하필
  **지금 실패하고 있는 바로 그 회차들**이었고, 빠진 자리가 '등록 안 됨'으로 읽혀 멀쩡한
  작업에 거짓 경보까지 났다. `[long]` + 작업마다 try/catch — **조용히 빠지느니
  '확인못함'이라 적힌 행이 낫다**(`[169]`).
- ★ **못 본 것을 정상이라 하지 않는다.** 조회가 실패하면 '이상 없음'이 아니라
  **'확인 못 함'** 이다. 이 구별이 없으면 감시자 자신이 눈먼 채 "모두 정상"을 말한다.
- ★ **있어야 할 회차 목록은 `install_*.ps1` 이 등록하는 것**을 읽는다(`declared()`).
  손으로 적으면 사본이 둘이 되어, 설치본만 늘고 목록은 안 늘면 **새 회차가 등록 안 된 채
  조용히 빠진다** — 그것이 정오회차 사고의 모양이다.
  · **이름 관례에 기대지 않는다.** 첫 판은 `$TaskName = "…"` 라는 모양을 찾았는데
    `install_browser_chain_schedule.ps1` 은 `$name = 'CSOS_BrowserChain'` 이라
    **그 회차만 통째로 빠졌다**(사라져도 경보가 안 뜬다는 뜻이다). 이제 `-TaskName` 에
    **실제로 넘기는 값**을 읽는다 — 관례는 어긋나도 등록 인자는 어긋날 수 없다.
  · **이름을 못 읽은 설치본은 조용히 넘기지 않는다**(`[169]`) — 감시 밖이라는 사실을 적는다.
  · `일일자동대조`·`원본자료자동정리` 에 **설치본을 만들었다**(`install_daily_schedule.ps1` ·
    `install_source_tidy_schedule.ps1`). 둘은 7월부터 돌면서도 설치본이 없어 기계를 새로
    만들면 사라졌고, `declared()` 도 못 셌다. 설정은 **살아 있는 작업에서 뽑아 적었다** —
    지어내지 않았다(일일대조의 `StartWhenAvailable=False` 도 그대로 뒀다. 고치는 것은
    사람의 판단이고, 그날 안 돈 사실은 감시자가 '안 돎'으로 이미 말한다).
- **컴팩팅 배선도 같이 본다** — `autoCompactEnabled` · PreCompact→`session_wrapup` ·
  `context_guard` 훅. 빠져도 그 순간에는 아무 일도 안 일어나고, 세션이 가득 찬 날에야
  **인계 없이 끊긴다.** 회차가 조용히 죽는 것과 같은 모양이라 여기서 같이 본다.
- **인계 문서는 스케줄러를 다시 묻지 않는다**(`[168]`) — 회차가 써 둔 판정을 읽기만 한다.
  그래서 감시 단계는 `snapshot_handoff` **앞**에 있다(뒤에 두면 인계가 늘 30분 전 판정을 싣는다).
- ★ **죽었다는 것을 아는 다음은 왜인지 아는 것이다 — 단계마다 걸린 시간을 남긴다.**
  `[180]` 은 "어디서 멈췄나"까지만 남겼다. 그런데 회차 예산(`over_budget`)은 단계
  **사이**에서만 검사하므로 **한 단계가 길면 그냥 지나친다** — 그 단계가 누구인지
  모르면 예산을 조여도 소용이 없다. 이제 `note_progress` 가 `단계기록`·`느린단계`
  (오래 걸린 순 다섯)를 같이 쓰고, 인계가 `오래 걸린 단계: … N분` 으로 이름을 댄다.
  · `끝난단계`(문자열 목록)의 **모양은 안 바꾼다** — 읽는 쪽이 있다. 시간은 옆에 따로 쌓는다.
  · 5분 미만은 '오래 걸린 단계'라 적지 않는다 — 전부 적으면 아무도 안 읽는다.
- ★ **exit 1 은 그저 1 이다 — 왜인지는 회차가 스스로 남겨야 한다.** 이 프로젝트의
  회차들은 스케줄러에서 **`pythonw.exe`** 로 돈다. 창이 없으니 **트레이스백이 어디에도
  안 남는다.** 그래서 `쿠팡업무_밴드재수집` 이 매일 08:00 에 죽고 있었는데 그 사실조차
  이 감시자를 만들고서야 보였고, **왜인지는 그때도 알 길이 없었다**(`--plan` 은 멀쩡히
  돌아 재현도 안 된다). 이제 `band/recollect.py` 가 죽으면
  `reports/밴드_재수집_오류.json` 에 자취를 남기고, **성공하면 지운다**(옛 자국이 남으면
  이미 고쳐진 고장을 계속 보고한다). 리포트의 '회차가 남긴 자국' 칸이 `*_오류.json` 을
  **글로브로** 모은다 — 목록을 손으로 적지 않으므로 자국을 남기는 회차가 늘면 저절로 나온다.
  · **무인으로 도는 회차를 새로 만들면 이 자국 남기기를 같이 넣는다.**
- 남은 것은 분담판 **[38]** 이다: 다음 회차가 범인 단계를 대면 그 안에 시간 제한을
  두거나(권장) 제한시간을 실측에 맞춘다. **짐작으로 먼저 고치지 않는다.**
  · 그리고 `일일자동대조`·`원본자료자동정리` 는 **설치본이 없다** — 기계를 새로 만들면
    이 둘은 안 살아난다. `declared()` 가 못 세는 이유이기도 하다.
- 검증 `[228]`.

## ★ 회차는 **창 없이** 돈다 — 검은 창이 사람 화면을 가로챘다 (2026-08-13 지시)

사용자 지시: **"이런 팝업이 너무 많이 떠서 내가 업무를 못보겠어, 방법 없을까?"**

- 실측: 회차 12개 중 **6개**가 콘솔 창을 띄웠다 — `.bat`(워치독 **30분** · 일일대조 ·
  원본정리) · `cmd.exe`(실시간감시 · 대표보고) · `python.exe`(BrowserChain **15분**).
  합쳐 **하루 약 150번**. 그런데 그 창에는 **볼 것이 없다** — bat 들은 이미 출력을
  `reports\*.txt` 로 보내고 있었다. **순수한 방해**였고, 고장이 아니라서 아무 감시자도
  잡지 않았다(`schedule_watch` 는 '돌았나'만 본다. **'어떻게 도는가'는 아무도 안 봤다**).
- **`pythonw.exe` 는 창이 없고 `python.exe`·`.bat`·`cmd.exe` 는 창을 단다.** 그래서:
  · python 을 직접 부르는 회차 → **`pythonw.exe`**
  · bat 을 부르는 회차 → **`wscript.exe "run_hidden.vbs" "<그 bat>"`**
- ★ **종료코드를 반드시 그대로 돌려준다.** `run_hidden.vbs` 는 `WScript.Quit sh.Run(line, 0, True)`
  다 — `0`=창 숨김, `True`=기다렸다가 그 코드를 돌려줌. 삼키면 죽은 회차가 전부 성공으로
  적히고 `[228]` 의 눈이 멀어진다 — **창을 없애려다 눈을 없애는 셈**이다.
  실측으로 7→7 · 0→0 을 확인했다.
- ★ **'로그온 여부와 관계없이 실행'로 숨기지 않는다.** 그러면 세션 0 에서 돌아 매핑
  드라이브 **`Z:` 가 안 보이고 크롬 조종도 안 된다** — 창은 사라지지만 일이 죽는다.
- ★ **창을 숨기면 출력을 반드시 파일로 남긴다.** `realtime_monitor.bat` 은 리다이렉트가
  없어 콘솔로만 나갔다 — 숨기는 순간 **아무 데도 안 남는다**(`[169]`). 로그를 붙였다.
- ★ **살아 있는 작업만 고치면 되살아난다.** 설치본(`install_daily_schedule.ps1` ·
  `install_source_tidy_schedule.ps1` · `install_browser_chain_schedule.ps1`)도 같이
  고쳤다 — 사본이 둘이면 한쪽만 고쳐진다.
- ⚠ **`워치독` · `실시간문제감시` · `유수비_대표보고` 는 아직 설치본이 없다.** 기계를
  새로 만들면 이 셋은 창 뜨는 채로 되살아나는 것이 아니라 **아예 안 살아난다**
  (`[228]` 의 `declared()` 가 못 세는 이유이기도 하다). 분담판에 남겼다.
- ⚠ 실행기 이름을 **ASCII**(`run_hidden.vbs`)로 둔다 — 설치본은 PowerShell 5.1 이
  BOM 없는 UTF-8 을 CP949 로 읽어 한글 이름이 깨진 전례가 있다(그래서 `install_source_tidy`
  가 파일명을 `[char]0x…` 로 짜 맞춘다). VBS 자신은 **UTF-16LE** 로 저장한다.
- 검증 `[248]` — 설치본이 창 뜨는 실행기를 다시 등록하는지 · 종료코드를 돌려주는지.
  ★ 그 검증도 **못 읽은 액션을 조용히 넘기면 안 된다** — 첫 판이 PowerShell 줄바꿈
  (백틱) 형태를 못 읽어 **9개 중 6개를 말없이 건너뛰었다**(`[169]` 가 검사 자신에게
  붙은 자리다). 지금은 못 읽으면 **실패**라고 말한다.

### ★ 창을 띄우는 것은 회차만이 아니다 — **회차가 띄운 자식** (2026-08-13 같은 날 이어서)

사용자 보고: **"이것좀 안뜨게 해 내가 컴퓨터 작업을 못해, 이거 밴드 자료 긁으라고
할 때 꼭 이러더라고"**

- `[248]` 은 **스케줄러가 부르는 것**을 다 고쳤는데 창이 또 떴다. 남은 창은 회차가
  아니라 **회차가 띄운 자식**이었다 — `webapp/tunnel_run.py:265` 의 `cloudflared.exe`.
  실측 `WindowsTerminal pid=72752` 의 제목이 `C:\…\ecount\webapp\cloudflared.exe` 다.
- ★ **콘솔 없는 부모가 콘솔 앱을 깃발 없이 띄우면 윈도우가 새 콘솔을 할당한다.**
  즉 `pythonw`(창 없음)로 바꾼 것이 **자식의 창을 새로 만든 셈**이다 — 부모에게 콘솔이
  있었다면 자식은 그것을 물려받아 새 창을 안 만들었다. **창을 없앨 때는 그 프로세스가
  띄우는 자식까지 같이 본다.**
- **왜 밴드를 긁을 때인가**: 워치독(30분)·`phone_access` 가 터널을 되살리고,
  `tunnel_run` 의 `while True` 는 끊기면 **5초마다 다시 띄운다** — 창이 쌓인다.
- **그 창에는 볼 것이 하나도 없었다.** cloudflared 로그는 `stdout=PIPE` 로 부모가 빨아
  읽어 주소를 뽑는다. 그래서 창은 늘 **텅 빈 검은 화면**이고, 순수한 방해라 어느
  감시자도 안 잡았다(`schedule_watch` 는 '회차가 돌았나'만 본다).
- 고침은 한 줄 — `creationflags=CREATE_NO_WINDOW`. **잃는 로그는 없다**(PIPE 는 그대로).
  실측: cloudflared 살아 있고 **창 있는 콘솔 프로세스 0개**.
  · ★ 고친 뒤 **프로세스를 갈아야 한다**(`[156]` 과 같은 자리). 도는 `tunnel_run` 은
    옛 코드를 메모리에 물고 있어, cloudflared 만 죽이면 **옛 코드가 또 창을 단다.**
    `heal_tunnel` 은 '터널 정상'이면 안 갈아 준다 — `kill_stale_tunnel` + `start_hidden`.
- ⚠ **`DETACHED_PROCESS`(0x8) 를 '창 안 뜨는 깃발'로 쓰지 말 것.** 그것은 부모 콘솔을
  **안 물려받는다**는 뜻일 뿐이라, 자식이 콘솔 exe 면 **새 콘솔을 할당해 창이 뜬다.**
  창을 없애는 깃발은 `CREATE_NO_WINDOW` 다. 지금 0x8 을 쓰는 세 자리
  (`watchdog.start_hidden`·`ledger_db`·`phone_access`)는 `pythonw`/`python` 을 띄워
  실측으로 창이 없어 **안 건드렸다**(문제 없는 것을 고치지 않는다, `[172]`) —
  그러나 그 자리에 **콘솔 exe 를 넣는 날 창이 되살아난다.**
- ⚠ **`watchdog.kill_stale_tunnel()` 은 `CommandLine -like '*tunnel_run*'` 으로 죽인다** —
  그 문자열을 명령줄에 담은 **무관한 프로세스까지 죽인다.** 실측으로 이 고침을 확인하던
  내 PowerShell 이 통째로 죽었다(exit 255 · 출력 0). 사람이 그 파일을 편집기로 열어 두면
  그것도 죽는다. **이름으로 죽일 때는 그 이름이 남의 명령줄에도 있다고 생각한다.**

## ★ AI 를 부를 때 **모델·노력을 스스로 고른다** — `ai_tier.py` (2026-08-12 지시)

사용자 지시: **"루프는 하루에 한번 매일 12시에서 13시로 설정하고 자동으로 모델과
노력 강도 설정해서 진행하는 알고리즘 적용"**

- **전까지는 한 번도 고르지 않았다.** `agent_dispatch._agent_command` 가
  `claude -p <프롬프트>` 만 불러 **모든 인계가 기본 모델(제일 비싼 것)** 로 돌았다.
  자율복구 재시도 한 장이나 원인 모를 회차 고장이나 같은 값을 치렀다.
- **싸게 하는 것이 목적이 아니라 값에 맞게 하는 것이 목적이다.** 이 프로젝트에서
  잘못된 판단은 못 한 판단보다 나쁘다(`[172]`·`[196]`) — 그래서 **원인을 모르는 일은
  값을 안 아낀다.** 아끼는 것은 답이 이미 정해진 일뿐이다. 가르는 근거는 셋:
  · **조회**(`--check`·`--print`·`--status`… 읽기만) → `sonnet` / low
  · **재시도**(자원·인증이 풀려 같은 명령을 다시) → `sonnet` / medium
  · **원인·설계**(왜 실패했는지 모른다 · 구조를 정한다) → **`opus` / high**
  ★ 순서가 뜻을 갖는다 — `--check` 가 섞여 있어도 **세 번 죽었으면** 물어야 할 것은
  '조회 결과'가 아니라 '왜 죽나'다. 그리고 `--apply`·`--queue` 가 붙은 명령은
  **절대 조회로 읽지 않는다**(싸게 판단해서 원장을 건드리면 안 된다).
- ★ **`haiku` 는 판정 층에 안 넣는다.** 싸지만 이 지시문은 길고 판정이 미묘하다
  ("0건이 없는 건가 안 본 건가", "밀림인가 실패인가"). 값싼 오판은 아끼는 것보다 크다.
- ★ **없는 손잡이를 지어내지 않는다 — 그러나 있는 손잡이는 쓴다.** 2026-08-13 실측
  (`claude 2.1.222 --help`)으로 **`--effort <low|medium|high|xhigh|max>` 와
  `--model`(별칭 `fable`·`opus`·`sonnet`)이 둘 다 실재**한다. 그래서 이제 **모델도
  노력도 깃발**로 간다. 다만 `ai_tier.flags()` 가 **그 실행파일에게 `--help` 로 직접
  물어본 뒤에만** 붙인다(실측 0.4초 · 그 뒤 캐시 0초). 못 물어봤으면 안 붙이고
  프롬프트 문장이 그 몫을 한다 — 틀린 깃발 하나면 CLI 가 통째로 안 뜨고 인계가
  **조용히** 안 된다(`[169]`). `codex` 쪽은 확인된 것이 없어 안 붙인다.
- ★ **사람과 대화하는 이 세션에도 걸린다** — `ai_tier.py --hook` 이 `UserPromptSubmit`
  에 배선돼 사람 말 한 줄의 갈래를 판정해 알려 준다(`pick_for_prompt`). **갈래가
  바뀔 때만** 말한다 — 매 차례 같은 말을 붙이면 아무도 안 읽고 토큰만 먹는다.
  · **대화창 모델은 코드가 못 바꾼다.** `--model`·`--effort` 는 **새로 띄우는 세션**의
    손잡이다. 그러니 판정은 권고까지이고, 바꾸는 것은 사람이 앱에서 고른다 —
    "바꿨다"고 적지 않는다. 부하 에이전트를 띄울 때는 그 등급을 그대로 적용한다.
  · ⚠ **로그인 안 된 CLI 는 `exit 0` 을 준다**(실측): `claude -p` 가
    `Not logged in · Please run /login` 한 줄만 찍고 성공으로 끝난다. 그러면 티켓이
    `done` 으로 적히고 Codex 폴백도 안 걸린다 — **실패가 성공처럼 보이는 자리**다.
    이제 `agent_dispatch._looks_not_logged_in()` 이 **정형 문구 + 짧은 응답**일 때만
    실패로 가른다(본문에서 '로그인'을 말하는 정상 답을 죽이지 않는다). 검증 `[241]`.
- **고른 이유를 티켓에 적는다**(`ai_tier`). "왜 이건 비싼 모델로 돌았나"를 물을 수
  있어야 규칙이 자란다. 고르기가 실패해도 **인계는 예전 그대로 나간다.**

### 하루 한 번 12~13시는 세션 루프가 아니라 **회차**다
- 세션 `/loop` 은 깨우기 간격이 **최대 1시간**이고 창이 닫히면 같이 죽는다 —
  하루 한 번을 표현할 수 없다. *"파일과 작업 스케줄러에 넣은 것만 산다."*
- 그 자리는 이미 있다: **`쿠팡업무_정오회차`(12:00~13:00 · `noon_run.py`)**.
  창 안에서 하루 한 번, 남의 점유에는 양보하고, 마지막 기회에는 무거운 단계만 비켜
  두고 돈다(`[219]`). 무인 축은 워치독 30분 · 스케줄러 · `autopilot` 이 받친다.
- 그러므로 **코딩 세션 루프는 상시로 돌릴 필요가 없다.** 사람이 옆에 있을 때만 켜고
  (`/loop` 한 줄), 자리를 뜰 때는 끈다 — 빈 틱은 대화 전체를 매번 다시 실어 크레딧만 쓴다.
- 검증 `[230]`.

## ★ 김미영 업무센터 — 매출 실적은 **발행월** 기준이다 (2026-08-12 지시)

사용자 지시: **"김미영 업무센터 하나 추가해서 쿠팡 매출 실적(전체) 포함 관리할 수
있게 구성해서 만들어"**

- 주소는 `/staff/kim-miyeong`(고정). 로스터 정본은 `app_server.STAFF_CENTERS` 이고
  `index.html` 의 같은 이름 표는 **사본**이다 — 한쪽만 고치면 화면 제목과 서버 권한이
  갈린다(검증 `[234]` 가 둘을 대 본다). 새 센터의 PIN 은 **예측 불가능한 임의값**으로
  만들어지므로 관리자가 한 번 정해 줘야 열린다(그게 안전한 기본값이다).
- **이 센터만 세금계산서 발행월로 일한다.** 앱의 다른 화면은 완료월로 센다. 그래서
  화면 맨 위에 기준을 **항상** 적는다 — 안 적으면 맞는 달까지 "왜 안 맞지"가 되고
  사람이 멀쩡한 값을 고치러 간다(`[172]`). 정산 화면 숫자와 직접 빼면 안 된다.
- 근거는 관리대장 **25_ERP매출서류**(이카운트가 실제로 끊은 매출계산서)다. 화면은
  `/api/revenue` 하나를 읽고, 그 안에서 **Z: 를 훑지 않는다**(`[168]`) — 이미 만들어진
  캐시·리포트 파일만 읽는다.
- **부가세 포함액을 우리 공급가액과 대지 않는다.** 김미영 표 원본은 포함액이라
  `reports/김미영_매출실적_2026.json` 의 `*_공급가액`(÷1.1 로 확정, `[154]`)을 쓴다.
  원본을 그대로 대면 **모든 달이 10% 어긋나 보인다.**
- **차이의 원인을 화면이 단정하지 않는다.** 후보는 둘이고 둘 다 실재한다 —
  ① 아직 안 끊긴 건 ② 우리 자료가 아직 안 들어옴. 근거를 나란히 놓고 사람이 고른다.
  · 실측 2026-08-12: 14달 중 **8달이 원 단위까지 일치**, 다른 달 6개 · 차이 합
    **−24,928,920원**(우리가 적다). 그중 **7월 두 항목이 23,962,220원(96%)** 이다 —
    형님 전언 "7월분은 세금계산서 발행 안된 건도 있다"와 같은 방향이다.
- **미발행의 근거는 ERP 진행상태**(6·7 이 아님)다. 원장 계산서 칸은 사람 손 입력이라
  대부분 비어 있어, 그 빈 칸을 세면 '미발행 190건' 같은 **없는 숫자**가 나온다.
  실측 237건 153,917,190원(정기점검 125 · 돌발AS 112). 프로젝트NO 가 없거나 ERP 에
  아직 없는 65건은 **'모름'으로 따로** 센다 — 미발행이라고 단정하지 않는다.
- **못 읽으면 0 이 아니라 '못 셈'이라고 말한다**(`[169]`). 색인·표가 없을 때
  '차이 없음'·'미발행 0건'으로 보이면 아무도 안 본다.
- 권한은 **`settle` 뿐**이다. 서류 담당이 현장 기록(as·pm)을 고치게 열어 주면
  서류를 보고 현장 사실을 맞추게 되고, 그 순간 근거가 뒤집힌다.
- 검증 `[234]`.

## ★ 목록은 **묶음으로 접어** 두고 묶음을 누르면 아래서 위로 (2026-08-12 지시)

사용자 지시: **"정산 카테고리도 클릭하면 아래서 위로 올라는 구조로 알고리즘 변경
전체 메뉴 전부다 스크롤이 너무 길어"**

- **다 보여 주는 것과 볼 수 있는 것은 다른 말이다.** 정산 본문이 폰(375×812)에서
  **35,811px = 44.1 화면**이었다 — 750건이 카드로도 표로도 **전부** 깔렸다. 오류도
  빈칸도 없으니 화면은 멀쩡해 보이는데, 사람은 끝까지 못 내려간다.
  묶음으로 접은 뒤 **923px = 1.1 화면**.
- 본문은 묶음 요약만 두고 묶음을 누르면 **그 묶음만** 시트로 올라온다
  (정산·돌발AS·정기점검·확인필요 **네 카테고리 전부** — 한 `renderSettle` 이 넷을 그린다).
- ★ **시트를 새로 만들지 않는다.** 이미 있는 `openExecMetric → showSheet` 를 그대로 쓴다 —
  상세로 들어갔다 뒤로 나오는 층 쌓기(`_sheetStack`)·인쇄·이미지·엑셀이 거기 이미 있다.
  새로 만들면 사본이 둘 되어 한쪽만 고쳐진다(`[162]` 와 같은 모양). 실측으로
  묶음 시트 → 상세 → 뒤로 가 그대로 묶음 시트로 돌아온다.
- ★ **접는 것이지 숨기는 것이 아니다**(`[169]`). 묶음마다 건수를 적고 '전체 목록'을 맨 위에
  둔다. 묶음이 많으면 묶음 목록 자체가 다시 길어지므로(실측 캠프별 **110묶음 = 6,421px**)
  큰 것부터 12개만 두되 **'나머지 98묶음 펼치기 · 126건'** 이라고 **말하고** 접는다.
  조용히 자르면 그 캠프는 '없는 캠프'가 된다.
- ★ **목록·표는 접혀도 계속 만든다.** 엑셀 저장·캡처·인쇄가 `#slist`·`#sgrid` 를 그대로
  읽는다 — 안 만들면 접었을 때 **저장한 파일이 빈 채로 나온다**(오류는 안 난다).
  그래서 `renderGroups` 는 `renderSettle` 의 **맨 끝**에 온다.
- ★ **넓은 화면의 기본값은 건드리지 않았다.** 표는 이미 제 상자 안에서 스크롤한다
  (`.gridwrap{max-height:calc(100vh - 250px)}`) — 길어지는 것은 좁은 화면 쪽이다.
  기본값은 `max-width:899px` 가 정하고, **사람이 한 번 고르면 그 선택이 이긴다**(기억한다).
  문제가 없는 쪽의 도구를 말없이 빼앗지 않는다.
- ★ **손잡이를 툴바에 두지 않는다.** 처음엔 툴바에 넣었다가 폰에서 툴바가 **+49px**
  (한 줄) 늘었다 — 스크롤을 줄이겠다면서 화면을 더 밀어낸 것이다. 지금은 묶음 목록
  바로 위 한 줄(`.grouphead`)에 있고, **접었을 때도 그 줄이 남는다**(안 남으면 한 번
  펼친 사람은 영영 전체 목록이다).
- 검증 `[236]`.

### 카드는 **접어** 둔다 — 접기 도구는 한 벌이다 (같은 지시의 나머지 반쪽)
- `[236]` 이 **반복되는 행**을 묶었다면 여기는 **서로 다른 카드**다. 실측(같은 폰):
  **실행 7,514 → 2,999px**(9.3→3.7화면) · **업무센터 3,690 → 2,028px**(4.5→2.5화면).
  실행 화면의 절반(3,843px)이 '처리 방법·용어 설명' 한 장이었다 — 필요할 때 펴 보는
  것이지 늘 깔아 둘 것이 아니었다.
- 마크업에는 `data-fold` 만 붙이고 손잡이·판단은 **`foldSetup` 한 곳**이 만든다.
  화면마다 접기를 새로 만들면 사본이 셋이 된다(`[162]`).
- ★ **`hidden` 만으로는 안 접힌다.** `.ui-cards{display:grid}` 같은 클래스 규칙이
  브라우저 기본 `[hidden]{display:none}` 보다 세서 **내용이 그대로 보인다.** 그런데
  손잡이는 '▸ 펼치기'로 바뀌므로 **화면만 보면 고친 줄 안다** — 실측으로 업무센터가
  펼침 3,659px = 접힘 3,659px 이었다. `[data-fold][hidden]{display:none!important}`.
- ★ **사람 카드는 이름·역할·현황을 남기고 단추 묶음만 접는다**(413→189px). 거기까지
  접으면 '누가 있는지'를 잃는다. 접힌 줄은 `할 수 있는 일 3개 · 펼치기` 라고 말한다(`[169]`).
- ★ 화면을 **열 때마다** 다시 맞춘다 — 내용을 나중에 채우는 카드(설명·AS 기사 단추)가
  있어 부팅 때 한 번만 붙이면 그 카드는 영영 안 접힌다.
- ⚠ **대시보드 블록(`[data-dash-block]`)에는 아직 안 붙였다** — 끌어서 정렬하는 손잡이와
  같은 자리라 눌림이 엇갈릴 수 있고, 그쪽은 이미 [화면 구성] 팔레트로 켜고 끈다.
- 검증 `[237]`.

## ★ 살아 있는 것과 일하고 있는 것은 다르다 — 차선 자동 회수 (2026-08-12 지시)

사용자 지시: **"니가 알아서 해 이런 문제 발생되면 에이전트가 알아서 처리하게 코딩해"**

- **`lanes` 의 주인 판정이 `_dead` 하나였다** — pid 가 살아 있으면 영원히 주인이다.
  실측 2026-08-12: sid 59fb7614 가 `build` 를 **30.7시간** 쥔 채 pid 29000 은 멀쩡했고,
  그동안 코드 수정 다섯 건([39][48][50][52][57])이 통째로 멈췄다. 그중 [57] 은 매일
  아침 거짓 경보를 내는 결함이라 막혀 있는 동안 손해가 계속 쌓였다.
- ★ **Claude Desktop 은 창마다 `claude.exe` 를 띄우고 닫아도 남긴다** — 실측으로 한
  부모 밑에 **29개**가 떠 있었다. 그러므로 이 환경에서 **pid 생존은 세션 생존의 증거가
  아니다.** 20초를 재니 그 pid 의 CPU 는 0.41초 늘었는데, 그것은 일하는 창의 자국이
  아니라 열려만 있는 창의 숨소리였다(내 유휴치 0.48초와 같았다).
  · **누적 CPU 를 시간 건너뛰며 비교하지 말 것.** "1855→2204→2448 이니 일하는 중"은
    틀린 읽기다 — 누적값은 죽은 뒤에도 그대로 남는다. **증가율**을 재야 한다.
- 그래서 묻는 것을 바꿨다: "살아 있나"가 아니라 **"자국을 남기고 있나"**.
  `lanes._idle(rec)` 는 **넷 다** 맞을 때만 참 — ① 잡은 지 `LANE_IDLE_HOURS`(8시간)
  초과 ② **대화기록이 그 사이 안 자랐다** ③ 그 sid 자국이 붙은 커밋이 없다
  ④ `ai_claim` 배타 점유를 안 쥐고 있다. 무게는 ②가 진다.
- ★ **모르면 안 뺏는다.** 대화기록 폴더를 못 찾거나 점유판을 못 읽으면 '조용함'이
  아니라 **'모름'** 이다(`[169]`). 일하는 창의 차선을 빼앗으면 두 창이 같은 파일에서
  만난다(사고 #36) — **못 뺏는 것보다 나쁘다.**
- **조용히 뺏지 않는다**: `reports/차선_자동회수.json` 에 근거를 적고, **기록을 못
  남기면 회수를 되돌린다.** 미커밋 변경이 있으면 아예 회수하지 않는다(반쯤 고쳐 놓은
  것일 수 있다). 회차당 **하나만**. `COUPANG_LANE_AUTORECLAIM=0` 으로 끈다.
- **`--force` 는 여전히 없다.** 이것은 사람이 뺏는 문을 여는 것이 아니라, **아무도 안
  보고 있을 때 무한정 멈추는 것**을 막는 장치다. 회차는 `worksplit_auto.py --run`
  (워치독 30분)의 **맨 앞** — `parked()` 뒤에 두면 늘 한 박자 늦은 판정을 싣는다.

### ★ 같은 이름을 두 이름공간으로 쓰고 있었다 (그날 같이 잡음)
- `ai_claim.session_id()` 는 UUID 의 **sha1 앞 8자리**(`271e7096`)이고
  `session_wrapup.live_transcripts()` 는 **파일 이름 앞 8자리**(`4468f365`)다.
  **두 이름공간은 영원히 안 겹친다.** 대조하면 **한 건도 안 걸리면서 오류도 안 난다.**
- 실측으로 `worksplit_auto` 두 곳이 그 상태였다: `live_others()` 가 둘을 섞어 담아
  같은 창을 둘로 셌고, "내 항목은 안 건드린다" 문이 환경변수 UUID 앞토막으로 물어
  **한 번도 안 닫혔다** — [34] 를 막으려고 세운 바로 그 문이다.
- 규칙 하나(`ai_claim.sid_of`)와 옮기는 함수 하나(`session_wrapup.live_sids`)로 모았다.
  **개수만 셀 때는 `live_transcripts`, 대조할 때는 `live_sids`.**
- ★ **Codex Desktop의 세션 이름은 `CODEX_THREAD_ID`이고 기록 파일은
  `rollout-...-<thread UUID>.jsonl`이다**(2026-08-14 실측). `CODEX_SESSION_ID`만 찾거나
  파일 이름 전체를 해시하면 살아 있는 Codex 창을 `99시간 자국 없음`으로 읽어 차선을
  빼앗는다. `ai_claim.SID_ENV`와 `session_wrapup.live_stems`가 이 차이를 한곳에서 흡수한다.
- 검증 `[239]`.

## ★ 재수집 '바뀜'이 늘 100% 였다 — 지문에 수집시각이 들어 있었다 (2026-08-12 실사고)

- 밴드 140건을 다시 받았더니 `새 글 0 · **바뀐 글 140** · 그대로 18` 이 나왔다.
  받은 것의 **전부**가 바뀐 것이다. 원인은 `datalake.ingest_band` 가 페이로드에
  `수집시각`(captured_at)을 담고 **`hash_on` 을 안 준 것** — 그러면 페이로드 전체를
  해싱하므로 **다시 긁은 글은 예외 없이 '바뀜'** 이다.
- ★ 그래서 **08:00 재수집 회차가 존재 이유를 잃고 있었다.** 그 회차는 '받은 뒤에
  내용이 바뀐 글'을 찾으라고 만든 것인데, 전부가 바뀜이면 아무도 안 본다(`[170]` —
  경보가 대부분이면 그 경보는 죽는다). 숫자도 나오고 오류도 안 나서 티가 안 났다.
- `hash_on=["밴드ID","글번호","글쓴이","본문","사진수","댓글수"]` 한 줄. **수집시각은
  페이로드에 그대로 남긴다** — 언제 긁었나는 여전히 알아야 한다. 빼는 것은 지문에서다.
- 실측: 수집시각만 다르면 `same`, 본문이 바뀌면 `changed`.
- ⚠ **새 갈래를 `put_record` 로 담을 때는 `hash_on` 을 먼저 생각한다.** 페이로드에
  '그때그때 달라지는 값'(수집시각·조회시각·일련번호)이 있으면 반드시 빼야 한다.
  ERP 쪽은 이미 `hash_on=core_cols` 를 주고 있었다 — 밴드만 빠져 있었다.

## ★ '본 것'의 근거는 `comments_full` 하나다 (2026-08-12, 분담판 [39])

- `comment_backfill.blind()` 가 두 밴드 다 **"미확인 0건"** 을 내고 있었다. 그런데
  `comments: []` 인데 `comments_full` 이 없는 글이 **5,829건**이었다 — "봤고 없었다"가
  아니라 **목록이 다 그려진 것을 확인 못 한 채 0 으로 적힌 것**이다(`[182]` 의
  '확인된 0개' 조건 미충족). 계기가 0 을 내면 아무도 의심하지 않는다(`[169]`).
- 범인은 `distrust`(=`harvest_looks_broken`)에 매인 조건이었다. 그 함수는 **댓글 담긴
  글이 하나라도 있으면 거짓**인데 실측 두 밴드가 1건·7건이라 둘 다 거짓이었고,
  그래서 빈 목록이 전부 '본 것'으로 넘어갔다.
- 이제 근거는 **`comments_full` 하나**다: `댓글이 담겼거나 · 확인된 0개` 면 안 뽑는다.
  **`[199]` 는 그대로 지켜진다** — 문을 넓힌 것이 아니라 근거를 좁힌 것이라, 진짜 0 은
  수집기가 `comments_full` 을 달아 닫는다. 무한루프가 아니라 **한 번 훑으면 수렴**한다.
- 실측(고친 뒤): 84789192 **0건** · 90610953 **3건**(1순위 1). `[177]` 의 갈래가
  이미 대부분을 거르므로 실제 긁을 것은 몇 건뿐이다.
  · ⚠ 84789192 의 `0` 은 **아직 근거를 못 댔다** — 미확인 2,986건이 전부
    업무글 아님/프로젝트NO 없음으로 걸리는지 확인이 필요하다(`[169]`).
    `blind()` 안에서 갈래별 건수를 세어 내보내게 하는 편이 낫다 — **밖에서 흉내 내면
    안 된다**(`parse_post` 는 모듈 밖에 없어 `AttributeError` 가 나는데 그것을
    `except: pass` 로 삼키면 **모든 글이 '업무글 아님'으로 세어진다**. 실제로 그랬다).

### ⚠ 합성검증을 파이프에 태우지 말 것 (같은 날 실측)
- `python tests/synthetic_check.py 2>&1 | tail -20` 로 돌리면 **종료코드가 `tail` 의
  것(0)** 이 된다. 실제로는 `t177` 에서 죽어 `ALL GREEN` 이 안 찍혔는데 알림은
  "exit code 0" 이라 했다 — **빨간 검증이 초록으로 보이는 자리**다.
- 이렇게 돌린다: `python tests/synthetic_check.py > out.txt 2>&1; echo "exit=$?"`
  그리고 **`ALL GREEN` 글자를 눈으로 확인**한다. 종료코드만 믿지 않는다.
- 소스 **글자**를 확인하는 검사(`'...' in src`)는 고칠 코드를 얼린다. 실제로 `t199` 가
  `distrust and not comments_full` 이라는 글자를 지키고 있었는데 **그 글자가 곧 [39] 의
  고장**이었다. 글자 검사는 '있어야 할 것'이 아니라 **'되돌아가면 안 되는 것'** 에 쓴다.

## ★ 크롬 '앱 설치' — 조각이 다 멀쩡해도 **문이 없으면 못 쓴다** (2026-08-13 지시)

사용자 지시: **"크롬에서 앱 설치 안되는 문제 해결"**

- **부품은 하나도 안 고장 나 있었다.** 실측 127.0.0.1: 매니페스트 200
  (`application/manifest+json` · 아이콘 192·512 · `start_url`/`scope` 동일 출처) ·
  서비스워커 `activated`(fetch 핸들러 있음) · 보안 컨텍스트 ✓. 그런데 사람은 설치를
  못 했다. **PWA 조건을 다 갖추는 것과 사람이 설치에 도달하는 것은 다른 문제다.**
- 이유는 둘이었고 **둘 다 조용했다**:
  · **문이 담당자 화면 안에만 있었다.** `[앱 설치]` 단추는 `#v-ryu`(류지영 업무센터)
    안에만 있어 관리자로 열면 **보이지도 않았다.** 게다가 `showInstallCard()` 첫 줄이
    `const center=STAFF_CENTERS[staffSlug]; if(!center) return;` 이라, 보이는 자리에서
    눌러도 **오류도 토스트도 없이 아무 일이 안 일어났다.**
  · **터널 주소에서는 일부러 막아 놓고 아무 말도 안 했다.** 서버는 `trycloudflare` 호스트면
    매니페스트·SW 를 뺀다 — 임시 주소가 아이콘에 박히면 다음날 `ERR_FAILED` 만 뜨기
    때문이고(2026-07-28 실사고) **그 차단은 옳다.** 빠져 있던 것은 **왜 막혔는지 말해
    주는 일**이다. 서버 주석은 "index.html 이 배너로 알린다"고 적어 뒀는데
    **그 배너가 실재하지 않았다** — 코드가 약속만 하고 안 지킨 자리다.
- 이제 판단은 **`installState()` 한 곳**이다(실행 탭 상태줄·설치 카드·자동 안내가 모두
  이것을 읽는다). 갈래를 가른다: `installed` · `tunnel` · `insecure` · `no-manifest` ·
  `ready` · `ios` · `browser`. 뭉쳐서 "설치 불가"라고만 하면 사람이 엉뚱한 데를 고친다.
  · ★ **'아직 프롬프트가 안 왔다'를 '설치 불가'라고 하지 않는다**(`[169]`). 크롬은 조건을
    다 갖춰도 잠시 뒤에 주고 사파리·파이어폭스는 아예 안 준다 — 그때는 `browser` 갈래로
    **주소창 설치 아이콘·메뉴 경로**를 안내한다. 모르는 것을 아는 것처럼 말하면 사람이
    멀쩡한 주소를 버리고 딴 데를 헤맨다.
  · 막혔으면 **고정 주소로 가는 단추**를 준다. 그래서 주 단추는 마크업에 동작을 박지 않고
    `id="installGo"` 로 두고 상황마다 갈아 끼운다(설치하기 / 고정 주소로 열기 / 감춤).
- ★ **고칠 때 같은 함정을 한 번 더 밟았다.** 자동 안내를 `initStaffCenter()` 안에 넣었는데
  그 함수도 첫 줄이 `if(!staffSlug) return;` 이라 **관리자·터널 화면에서는 아예 안 돈다.**
  실측으로 터널에서 안내가 한 번도 안 떴다. **설치가 막히는 사람은 대부분 담당자가
  아닌 화면에 있다** — 안내는 담당자 전용 초기화 **밖**에 둔다.
- **고정 주소를 적는 자리는 하나다**(`FIXED_APP_ORIGIN`). 사본이 둘이면 이사한 날
  한쪽만 고쳐지고, 그 아이콘은 아무 말 없이 죽는다.
- 실측 결과: 고정 주소 `https://mulder.tailf14aae.ts.net` = 설치 가능(https·매니페스트·
  SW activated) · 터널 주소 = 안내와 함께 차단 유지 · 127.0.0.1 = 설치 가능.

### ★ 안드로이드에서 "설치됐다는데 앱 서랍에 없다" — **바로가기와 WebAPK 는 다른 것**
사용자 보고(같은 날): **"휴대폰을 껐다 켜고 크롬도 껐다 켜고 다시 해도 안돼
설치되었다는 문구는 떴는데 앱서랍 어디에도 안보여"**

- **둘은 겉으로 구별이 안 된다.** 크롬은 조건이 맞으면 **WebAPK** 를 구워 **앱 서랍**에
  넣고, 굽기에 실패하면 조용히 **홈 화면 바로가기**로 떨어진다. 그런데 안내 문구는
  **둘 다 "추가되었습니다"** 다. 그래서 사람은 설치가 된 줄 알고 서랍만 계속 뒤진다 —
  이 프로젝트가 반복해 당한 **'실패가 성공처럼 보이는 자리'** 의 안드로이드판이다.
  확인하는 법은 하나다: 크롬 주소창에 **`chrome://webapks`** — 목록이 비면 안 구워진 것이다.
- ★ **원인을 지목하지 않는다.** 실측으로 서버 쪽 조건은 **전부 맞았다**: Funnel 켜짐 ·
  공개 DNS 로 풀림(103.84.155.153) · `/`·매니페스트·아이콘·SW 전부 200 ·
  아이콘은 **진짜 192×192 · 512×512 PNG**. 근거 없이 "이 주소가 나쁘다"고 말하면
  사람이 **멀쩡한 것을 고치러 간다**(`[172]` 의 문). 그래서 화면은 **확인하는 법**과
  **확실히 되는 길**만 준다.
- 확실히 되는 길은 **고정 배포본(GitHub Pages)** 이다 — 평범한 공개 CDN 이라 WebAPK 가
  정상으로 구워진다. 실측으로 매니페스트·아이콘·SW 전부 정상이고 `start_url` 은
  `.../app.html`, `scope` 는 `.../` 다.
  · **그 사본은 읽기 전용 반쪽이 아니다** — 혼자서도 열리고(`data.enc`), PC 가 켜져
    있으면 카드가 **[실시간 PC 앱 열기]** 를 띄워 살아 있는 앱으로 넘겨준다.
    즉 **폰의 정문은 원래 이쪽**이고, 고정 주소는 그 안에서 여는 문이다.
  · ★ 그 창을 터널 주소로 **옮기지 않는다**(2026-07-28 실사고) — 크롬이 종료 시점
    주소를 복원해서 다음 부팅에 죽은 이름이 뜬다. 새 창으로 띄운다.
- 그래서 안드로이드에서 열면 설치 카드가 **[앱 서랍용으로 설치(고정 배포본)]** 을 같이
  띄운다(`installState().alt`). PC 에서는 안 나온다 — 없는 문제를 만들지 않는다.
- 검증 `[240]`.

## ★ 취소된 건이 매일 '미처리'로 서 있었다 — 캘린더 (2026-08-13 지시)

형님 통화(류지영·오종현): **"너 이거 완료된 것도 다 들어가 있다니까. 8월 3일 삼태리
오늘 하고 있고 구리 송파 이거 다 끝났어 … 완료가 됐는데 네가 지금 미처리로 남아 있는
거야. 이렇게 되면 일 안 하는 줄 알잖아." / "취소된 거는 접수 취소가 됐다는 말이잖아.
있으면 계속 접수가 돼 있는 상태가 되잖아. 접수해서 취소된 건 다 지워 버려."**

- **실측 2026-08-13**: 캘린더 `as_open`(돌발AS 미처리) **131건 중 41건(31%)이 진행상태
  `취소`** 였다. 판정이 `작업완료일이 비었나` **한 줄**이라 취소 낱말을 아예 안 봤다.
  · 취소된 현장은 **아무도 안 간다.** 그런데 화면은 매일 빨갛게 "밀렸다"고 말한다 —
    가야 할 곳처럼 보이고, **일한 것이 안 한 것으로 보인다.**
  · 그리고 경보의 3분의 1이 가짜면 **나머지 88건도 아무도 안 본다**(`[170]`).
- 같이 드러난 것: **진행상태가 이미 `작업완료`인데 날짜 칸만 빈 행 2건**도 미처리로
  서 있었다. '완료라는데 날짜가 없다'와 '아직 안 갔다'는 **다른 사실**이다.
- 고친 뒤 **131 → 88건**. 뺀 43건은 **회색(`etc`)으로 그대로 남는다.**

### 지키는 것
- ★ **판정은 한 곳이다** — `work_flow.is_cancelled(row, kind)` · `work_flow.says_done`.
  낱말 짝(`취소`·`철회`)은 이미 `complete_verified`·`ledger_db`(conflicts)·`backfill_rows`
  **세 곳에 흩어져** 있던 것을 모은 것이다. 캘린더와 프로젝트 이력이 같은 답을 해야 한다 —
  갈리면 같은 원장을 보면서 두 답이 나온다(`[162]`).
  · 어느 칸을 읽을지도 `KINDS` 가 정한다(AS=`진행상태` · PM=`점검상태`). 칸 이름을
    부르는 쪽에 적으면 시트가 바뀐 날 **오류 없이 한 건도 안 걸린다**(`[165]`).
- ★ **낱말이 정확히 같을 때만** 취소로 본다(공백만 무시). 넓히면 멀쩡한 현장이 목록에서
  사라지고 **아무도 안 가면서 목록에도 없다** — 미처리로 남는 것보다 나쁘다(`[172]`).
  실측 오탐의 모양이 이미 있다: `"택배발송 취소요청하심"` 같은 본문은 취소가 아니다.
- ★ **조용히 지우지 않는다**(`[169]`). "다 지워 버려"의 뜻은 **빨간 미처리에서 빼라**이지
  기록을 없애라가 아니다. 회색 `etc` 로 `돌발AS 접수취소 · <캠프>` 라고 적어 남긴다 —
  통째로 지우면 "그때 정말 접수가 있었나"를 잃는다.
  · **새 캘린더 갈래를 만들지 않았다** — `etc` 는 `CAL_KINDS` 와 `index.html` 의
    `CAL_FALLBACK_KINDS` 에 **이미 양쪽 다** 있다. 갈래를 늘리면 그 표 둘을 같이 고쳐야
    하고, 한쪽만 고치면 그 색만 화면에서 사라진다.
- **04시트 점검상태에는 아직 취소 낱말이 없다**(실측: 완료·미점검·예정·예정월).
  그래도 PM 쪽에 같은 문을 달아 뒀다 — **없다고 안 거는 것이 아니라**, 생기는 날
  저절로 걸리게 둔다.
- 검증 `[243]`.

### ★ 나머지 반쪽 — 밴드가 완료라 하면 미처리가 아니다 (같은 지시: "일단 다 해")

- **먼저 내 짐작이 틀린 것부터 적는다.** 위에 "`completion_evidence`·`cancel_watch`·
  `cross_signal` 이 이미 근거를 모으는데 캘린더가 안 읽는다"고 적었는데 **실측으로
  틀렸다**: `completion_evidence` 3,114건은 출처가 전부 `ledger-objective-proof` —
  **이미 원장에 완료일이 있는 행의 증명**이라 열린 행과 겹침이 **0** 이다(원천업무ID·
  프로젝트NO 어느 쪽으로 대도 0). 그 표는 원장의 **하류**지 상류가 아니다.
  **짐작한 근거를 확인 없이 문서에 적으면 다음 사람이 없는 길을 판다.**
- 진짜 원천은 `band_extract.load_records()` 하나다(밴드 글 + 카톡 내보내기를 같은
  양식으로 읽는다). 실측 **8,049건 · 작업완료 5,449 · 완료로 읽힌 프로젝트 1,557개**.
  `app_server._band_completion_index()` 가 그것을 색인한다 — **읽는 자리를 새로
  만들지 않았다**(`[162]`).
- ★ **닫는 문은 좁다**(`[172]`·`[208]`): 밴드가 완료라 해도 **그 프로젝트에 열린 원장
  행이 하나뿐일 때만** 완료로 내린다. 여럿이면 어느 건이 끝났는지 원본이 안 말해 준다 —
  짐작으로 닫으면 그 현장은 아무도 안 가는데 목록에도 없다. 실측 지금 **1건**
  (UJ2601321 부산1MB · 밴드 8/3 완료 게시).
- ★ **못 닫으면 왜 아직 미처리인지 갈라 말한다**(`_why_still_open`, `[169]`).
  실측 111건: **밴드에 접수 글은 있으나 완료 글이 없다 87 · 밴드·카톡에 이 프로젝트
  글이 없다 16 · 밴드 수집이 2026-08-11 까지만 왔다(아직 못 본 것일 수 있다) 8**.
  · 마지막 갈래가 이 절의 핵심이다 — **"완료 글이 없다"와 "아직 안 긁었다"는 다른
    말**인데 뭉치면 사람이 다녀온 현장을 다시 찾아간다.
  · **'못 읽음'을 '근거 없음'으로 치지 않는다** — 그 색인은 **디스크에 남기지 않는다**.
    남기면 다음 재시작이 빈 색인을 지문째 믿고 "완료 근거 0건"을 조용히 확언한다.
- ★ **비싼 파싱은 캐시 검사 뒤에**(`[168]`). 실측 첫 계산 **23.5초** — 웹 요청 하나가
  그만큼 서면 폰은 그냥 실패로 본다(`[197]`). 메모리 TTL(5분) + **디스크 캐시**
  (`reports/밴드_완료색인.json`, 지문 = 밴드 캐시 파일의 이름·크기·수정시각)로
  **서버를 다시 띄워도 다시 안 센다**(실측 23.5초 → 1초).
- 화면은 서버가 정한 갈래를 **그대로 싣는다**(`calWhyOf`) — 화면이 제 손으로 이유를
  지어내면 판정이 두 곳이 된다.
- **원장에는 한 글자도 안 쓴다.** 이것은 **읽는 길**이다 — 밴드 근거로 원장 완료일을
  채우는 것은 쓰는 길이라 기준이 달라야 한다(`[170]`). 지금은 화면이 사실을 보여 줄 뿐이다.
- ⚠ **남은 사람 몫**: 87건은 밴드에 완료 글이 실제로 안 올라온 것이다. 형님이 부른
  캠프들(야탑·목포·감전·김해·망미동·판토스·허브넷·대전·안동)이 여기 있다 — 기사가
  다녀왔는데 밴드에 완료를 안 적었거나 카톡으로만 알린 것이다. **수집을 채우는 것은
  'CSOS 리서치 및 자료 수집' 세션 몫**이고, 채워지면 이 길이 저절로 돈다.
- 검증 `[244]`.

## ★ 화면이 조용히 틀린 값을 보여 주는 것을 매일 먼저 묻는다 (2026-08-13 지시)

사용자 지시: **"위 같은 문제 잡아내는 기능 AI 추가해"**

- **그날 두 사고 다 사람이 전화로 지적하고 나서야 알았다.** 미처리의 31%가 이미
  취소된 건이었고(`[243]`), 다녀온 현장이 원장 완료일이 비어 미처리로 서 있었다
  (`[244]`). **둘 다 오류가 안 났다** — 화면은 멀쩡히 숫자를 보여 줬고 파일도 다
  있었다. 이 프로젝트가 반복해 당하는 바로 그 모양이다(`[169]`).
- `python truth_watch.py` 하나다(09:50 회차 단계 · `--print` 로 본문). 결과는
  `reports/화면_사실대조.md`·`.json` → 인계 문서 **'먼저 처리할 것'**.
- ★ **이 회차가 하는 일은 계산이 아니라 질문이다.** 매일 넷을 묻는다:
  ① 화면이 '아직 안 끝났다'는 건을 **원본도 그렇게 말하나**
  ② 한 갈래가 목록의 **절반을 넘지 않나**(넘으면 경보가 아니라 기준이 틀린 것, `[170]`)
  ③ 지금 "0건"인 것이 **없는 것인가 안 본 것인가**(`[169]` — 밴드 최신일을 같이 적는다)
  ④ 값이 전부 빈 열이 있나 — **안 읽은 열은 빈칸과 구별이 안 된다**(`[165]`)
  ⑤ 취소·완료가 미처리에 **다시 섞이나**(`[243]` 회귀 감시)
- ★ **판정을 새로 만들지 않는다**(`[162]`). 화면이 실제로 내보내는 그 함수
  (`app_server._calendar_work_events`)를 **그대로 불러** 결과를 센다. 여기서 다시
  판정하면 화면과 감시가 갈리고 — **감시자가 틀리면 아무도 그 사실을 모른다.**
- ★ **읽기 전용이다.** 안 고치고 큐에도 안 넣고 엑셀도 안 연다(`typo_watch` 와 같은
  자리). 무엇이 맞는지는 사람만 안다. 자동으로 고치면 "그때 화면이 정말 뭐라고
  했나"를 잃는다. 검증이 `enqueue(`·`--apply`·`openpyxl` 이 들어오는 것을 막는다.
- ★ **못 물어본 것을 '이상 없음'이라 하지 않는다.** 리포트가 '못 물어본 것' 칸을
  따로 두고 그것도 '먼저 처리할 것'에 올린다 — 이 구별이 없으면 **감시자 자신이
  눈먼 채 "모두 정상"을 말한다**(`[228]` 과 같은 자리).
- ★ **인계는 다시 세지 않고 회차가 써 둔 것을 읽는다**(`session_handoff.truth_gap`,
  `[168]`). 여기서 밴드를 다시 파싱하면 인계 한 장에 몇십 초가 든다.
- **첫 실행 실측**(2026-08-13): 먼저 볼 것 0 · 확인 1 · 못 물어봄 0.
  확인 1건은 *"미처리 사유가 한 갈래에 몰렸다 — 밴드에 접수 글은 있으나 완료 글이
  없다 96건(88%)"*. 이것이 정확히 `[170]` 이 말한 자리다 — **그 88%는 우리 잘못이
  아니라 기사들이 밴드에 완료를 안 적은 것**이고, 회차가 그것을 매일 숫자로 말한다.
- 무인 회차이므로 `sys.stdout` 이 None 일 수 있다 — `hasattr` 로 감쌌다(`[235]`).
- 검증 `[245]`.

### ★ 회차도 조수다 — 스테이션에 지금 도는 것을 다 올린다 (2026-08-13 지시)

사용자 지시: **"조수 스테이션에 지금 돌아가는 에이전트나 세션 모두 추가해서 구현해"**

- **스테이션은 이미 대화 세션을 실시간으로 보여 주고 있었다**(`ai_claim` 점유 + 대화기록).
  빠져 있던 것은 **무인 회차**다 — 이 프로젝트에서 실제로 일을 하는 것은 대부분 그쪽인데
  (워치독 30분 · 증분 파이프라인 5분 · 09:50 일일대조 · 08:00 밴드재수집 · 정오회차)
  **어느 화면에도 안 보였다.** 그래서 "지금 무엇이 돌고 있나"를 사람이 물어야 했다.
- 실측 2026-08-13: 자리 **4개 → 15개**(세션 2 · 로스터 2 · **회차 11**).
- ★ **판정을 새로 만들지 않는다**(`[162]`) — `schedule_watch` 회차가 써 둔 갈래
  (`도는중`·`성공`·`죽음`·`안돎`·`밀림`·`확인못함`)를 **읽기만** 한다. 여기서 스케줄러를
  다시 물으면 화면 요청마다 비싼 조회가 된다(`[168]`).
- ★ **못 읽은 것을 '도는 것이 없다'로 치지 않는다**(`[169]`) — 감시 파일이 없으면
  '상태 못 읽음' 자리를 하나 세워 **없는 것이 아니라 못 본 것**임을 적는다.
- **사람이 끈 회차(`꺼짐`)는 안 올린다** — 고장이 아니라 사람의 결정이다.
  올리면 자리만 차지하고 경보가 흐려진다(`[170]`).
- **빈 시각을 '방금'이라 적지 않는다**(`_ago_iso` → `기록 없음`). 한 번도 안 돈 회차가
  방금 돈 것처럼 보이는 것이 이 프로젝트가 반복해 당한 그 모양이다.
- 화면은 안 고쳤다 — `zones[].people[]` 을 그대로 그리므로 **서버만 채우면 된다**.

## ★ '못 찾음' 도 세 가지 뜻이다 — 원인을 지목하지 않는다 (2026-08-13 실사고)

형님이 보낸 화면 한 장: 확인 필요 목록에서 `JS-2606-634` 를 눌렀더니
**"찾지 못했습니다 (다른 월 데이터거나 아직 미등록)"**.

- **그 건은 멀쩡히 있었다.** 실측 `get_settlements()` 750행 안에
  `JS-2606-634 · UJ2601032 · 송파3Sub-FC · 완료일 2026-06-12 · 돌발AS`. 즉 화면이
  **없다고 말한 것이 있었다.** 오류도 안 났고 목록은 두 건을 잘 보여 줬다.
- **원인은 없음이 아니라 순서였다.** `/api/issues` 는 금방 오는데
  `/api/settlements` 는 ux 실측 **평균 54,656ms**(최근 7일 812회 · 최대 27분)다.
  그래서 **목록은 이미 그려졌고 `settleRows` 는 아직 빈** 창이 1분 가까이 열려 있다.
  그 창에서 누르면 못 찾는다 — **없어서가 아니라 아직 안 왔기 때문이다.**
- ★ **그런데 화면은 원인까지 확언했다.** `[169]`·`[251]` 은 '0' 을 두고 "없는 건가 안
  본 건가"를 물으라 했는데, 여기서는 한 겹 더 나가 **왜 없는지를 지어냈다**
  ("다른 월 데이터거나 아직 미등록"). 틀린 지목은 못 잡는 것보다 나쁘다(`[172]`) —
  사람이 **없는 문제**를 찾아 나선다. 실제로 그렇게 됐다.
- 이제 `openByPrj` 는 못 찾으면 `jumpNotFound` 로 가고 거기서 **세 갈래**로 갈라 말한다:
  ① **아직 안 왔다** → 없다고 말하지 않고 "받고 있습니다" 라 적은 뒤 받아 오고,
     받으면 **눌렀던 그 기록을 저절로 연다**(사람이 다시 누를 일이 없다)
  ② **다 받았는데 없다** → 그때만 없다고 말하고 **기준 시각을 같이** 준다
  ③ **못 받았다** → "없다는 뜻이 아닙니다" 라고 적고 다시 불러오는 길을 가리킨다
- **판정을 새로 만들지 않았다** — 자료 상태는 `dataZeroState` 한 곳에 묻는다(`[251]`).
  여기서 다시 세면 같은 화면이 두 가지 답을 갖는다.
- ★ **오는 중이면 다시 걸지 않는다.** 같은 요청을 두 번 보내면 55초짜리가 더 느려진다.
  멈춘 것(실패·한 번도 못 받음)만 다시 건다.
- ★ **기다리는 시간은 실측보다 길어야 뜻이 있다**(`[197]` 과 같은 이유) — `JUMP_WAIT_MS`
  120초 대 실측 평균 54.7초. 짧게 잡으면 기다려 놓고도 결국 같은 실패로 끝난다.
  **`/api/settlements` 가 빨라지면 이 값도 다시 본다.**
- **넘겨받은 프로젝트NO 를 버리지 않는다.** 예전 `openByPrj(rid||prj)` 는 ID 만 넘겨
  놓고 그 ID 를 프로젝트NO 자리에도 대 봤다 — **프로젝트로 찾을 기회가 아예 없었다.**
- ⚠ **남은 것은 55초 자체다** — 분담판 `[92]`(오종현 '0건'의 뒷단)가 "서버 계산인가
  터널 왕복인가"를 묻고 있다. 실측이 **절반을 답한다**: 프로세스 안에서
  `get_settlements()` 가 **콜드 13.03초 · 웜 5.43초**(750행)다. 즉 터널만의 문제가
  아니고 **웜 캐시가 없다** — `[191]` 이 `get_exec_report` 를 웜 0.000008초로 만든
  그 자리와 같은 모양이다. 짐작으로 캐시부터 늘리지 말고 `[92]` 에서 가른 뒤 고친다.
- 검증 `[257]` — 원인을 확언하는 문장이 되돌아오는 것 · 기다림이 실측보다 짧아지는 것 ·
  오는 중에 다시 거는 것 · 폴백이 프로젝트NO 를 버리는 것을 막고, node 가 있으면
  **여덟 가지를 실행으로** 확인한다(글자 검사만으로는 이 셋을 못 잡는다).

## 시작 체크리스트 (매 세션)
0. **`python ecount/session_handoff.py --check` ← 제일 먼저.**
   (계정·창이 바뀌었으면 `--check` 대신 `--adopt` 로 시작한다 — 위 항목)
   앞 세션이 컨텍스트가 차서 끊겼을 수 있다. 그때 남는 것들을 이 한 장이 잡아 준다:
   · 죽은 세션이 잡고 있는 점유(프로세스 생사로 판정 — 45분 기다릴 것 없다)
   · 반영 안 된 입력 큐 · 만들다 만 `.tmp.xlsx` · 미커밋/미푸시
   **"먼저 처리할 것" 이 비어 있을 때만 새 작업을 시작한다.**
   (워치독이 30분마다 `reports/세션인계.md` 를 갱신하므로 세션이 갑자기 죽어도 남는다)
0-1. `ecount/INCIDENTS.md` — 과거 사고(증상→원인→조치→검증번호). 비슷한 증상이면 여기부터.

1. `ecount/AGENTS.md` 전체 읽기 — 프로젝트 전모·절대규칙 7항·현재 상태·대기 항목
2. 관리대장 최신본(vN)의 `19_AI작업인수인계` 시트 하단 행들 읽기 — 사람·AI 공용 인수인계 원장
3. `python ecount/tests/synthetic_check.py` 실행 → **ALL GREEN 확인 후에만** 실데이터 작업
4. `python ecount/data_status.py --print` — 자료가 지금 얼마나 있나(밴드·서류·입금·원장 채움).
   같은 것을 다시 세지 말 것. 시스템 상태는 `coupang_workbench.py --status`.

## Terra → Sol 인수인계 검토 관문

Terra가 의미 있는 작업을 끝내면, 커밋·푸시 후 `python ecount/handoff_review.py --mark-terra`를 실행해 Sol 검토 범위를 표시한다.

Sol은 실데이터·코드·관리대장 쓰기 전에 반드시 다음을 실행한다.

1. `python ecount/session_handoff.py --check --for-sol`
2. `python ecount/handoff_review.py --review-sol`

검토는 기준 커밋부터 현재 HEAD까지의 공백 오류·비밀값 형태·변경 Python 문법·전체 합성검증을 확인한다. PASS 전에는 `ai_claim.py`가 Sol의 `ledger/code/band/publish` 점유를 차단한다. 새 커밋 또는 의미 있는 미커밋 변경이 생기면 다시 검토한다.

## ★ 상시 원칙 — 자동화로 만들어 두라 (2026-07-31 지시)
사용자 지시: **"세션이 95% 차면 모든 내용을 다음 세션으로 자동으로 넘겨 자동으로 시작하게 하고
모든 자동화가 가능하게 항상 생각해."**

- **대화에 남긴 것은 사라진다. 파일과 작업 스케줄러에 넣은 것만 산다.** 무엇을 하든
  "이걸 다음에 또 시켜야 하나?" 를 먼저 묻고, 그렇다면 **daily_run 단계 + 스케줄러**로 만든다.
  지시를 받아 한 번 처리하고 끝내지 말 것 — 그건 다음 세션에 그대로 다시 요구된다.
- **컨텍스트 95% 전에 반드시 인계를 끝낸다.** 100%에서 끊기면 아래 4단계를 못 한다.
  80%쯤부터 새 작업을 벌이지 말고 마무리로 전환한다.
- 지금 자동으로 도는 것(2026-08-01 확인): 워치독 30분 · **밴드재수집 08:00** · 원본자료자동정리 09:35 ·
  일일자동대조 09:50(다운로드 흡수·폰 원격 점검 포함) ·
  **원장일괄반영 11:00·15:00 (한 작업에 트리거 둘)** ·
  실시간문제감시 4시간 · **자동화후보검색 10:00**(auto_scout.py — GitHub/오픈소스에서
  반복 업무에 맞는 도구를 매일 찾아 reports/자동화_후보.md 에 ★신규 표시, 2026-08-04 지시)
  · **접수취소 확인** — 09:50 일일대조 안의 단계(`cancel_watch.py --queue`)
  · **화면 ↔ 원본 사실대조** — 09:50 단계(`truth_watch.py`). 화면이 오류 없이
    틀린 값을 보여 주는 것을 매일 먼저 묻는다. 읽기 전용, 결과는 인계 '먼저 처리할 것'
    (2026-08-13 지시)
  · **업무 단계 정의 확인** — 09:50 단계(`work_flow.py --check`). 관리대장 드롭다운·앱 흐름도가 바뀌면 인계 문서에 올린다(2026-08-10 지시)
  · **유수비 대표 대화 쿠팡 접수 추출** — 09:50 단계(`ceo_events.py --sync`). 프로젝트번호
    배정 전이라도 대표가 쿠팡방에 올린 캠프 접수는 대표보고·캡처에 별도 `대표 전달 접수`로
    싣는다. 사진·동영상과 비쿠팡 내부 지시는 제외하되, **쿠팡 AS 대표이사 업무지시서**는
    접수 건과 섞지 않고 별도 `대표 지시 이행현황`으로 싣는다. 보고기한·요구사항·제출내용을
    저장 캡처 상단에 표시하고 완료 근거가 없으면 `경과보고 확인 필요/결과 미확인`으로 남긴다.
    게시일이 지났다고 숨기지 않으며, 돌발·정기방에 같은 지시가 동시에 올라오면 동일 시각·본문은
    한 건으로 합친다. `쿠팡 건/쿠팡 무관/모름`을 모두 세어 사각지대를 숨기지 않고 원장·입력
    큐·카톡 원문에는 쓰지 않는다(2026-08-14 지시 · 검증 `[260]`·`[262]`).
  · **세워 둔 일 재개 · 보류된 푸시** — 워치독 30분 안의 단계(`worksplit_auto.py --run`).
    막힘이 풀린 분담판 항목을 인계 문서 '먼저 처리할 것' 에 올리고, 아무 창도 없으면
    AI 에게 표를 하나 넘긴다. 옆 세션이 사라진 뒤 남은 미푸시를 스스로 민다(2026-08-11 지시)
  · **정오 회차 12:00** (`쿠팡업무_정오회차` · `noon_run.py` · 설치본
    `install_noon_schedule.ps1`) — 하루 한 번, **12:00~13:00 창 안에서만**. 10분마다
    55분간 재시도하지만 그것은 '여섯 번 돈다'가 아니라 **양보한 날의 두 번째 기회**다
    (마커 `reports/.정오회차.json` 의 날짜 도장이 하루 한 번을 지킨다).
    단계는 인계 갱신 · 합성검증 · 정기점검 내용 조사 · 캠프명 표준 대조뿐 —
    **수집도 엑셀 쓰기도 커밋도 안 한다.** `ledger`·`publish` 점유와 도는 회차에는
    전부 양보하고 `--force` 로도 안 뺏는다. `code` 점유는 합성검증만 건너뛴다.
    ★ **양보를 완주로 적지 않는다** — 적으면 그날 회차가 영영 안 돈다. 검증 `[219]`.
    ★ 2026-08-12: 코드·검증·설치본은 8/11에 다 있었는데 **스케줄러에 등록이 안 돼
      한 번도 안 돌았다**(마커 파일 자체가 없었다). 그래서 같은 지시가 다시 왔다 —
      **"코딩했다"와 "자동이다"는 다른 말이고, 그 차이는 이 목록이 아니라
      `Get-ScheduledTask` 가 안다.** 새 회차를 만들면 등록까지 확인할 것.
  · **ERP 전화면 몰이도 같은 창이다** (`band/browser_chain.py` · 15분 tick 안의 단계).
    전에는 간격(4시간)만 보고 **하루 중 아무 때나** 크롬 포커스를 빼앗았다 —
    사고 #35 가 난 21:10 도 그 회차다. 이제 `COUPANG_ERP_WINDOW`(기본 `12:00-13:00`)
    안에서 **하루 한 번 성공한 시작**까지만 하고, `band`·`publish` 를 잡은 다른
    세션에는 양보한다(판단은 `ai_claim` 것을 그대로 빌린다 — 두 곳에서 하면 갈린다).
    창 안에서 어긋나면 남은 tick 이 다시 본다(최대 네 번) — 한 번 실패로 그날을 접으면
    사람이 손대야 하고 그건 "손 안 대게"가 아니다. **창을 놓친 날은 그 사실을 한 줄
    적는다**(조용히 넘어가면 '돌았는데 왜 결과가 없나'가 된다).
  · **앱이 먼저 답하기**(local_ai.py) — 회차가 아니라 **사람이 물을 때마다**
    도는 읽기 전용 답변기다(앱 [앱에게 물어보기] 카드 · `/api/ask`). 클로드를
    부르기 전 단계라 크레딧을 아낀다, 2026-08-09 지시.
  · **업무센터UX점검 3일마다 12:00**(ux_review.py — 실제 사용 기록에서 오류·느린 화면·
  동선 문제를 뽑아 reports/UX개선_제안.md 에 ★신규 표시, 2026-08-05 지시. 읽기 전용)
  · **세션 자동 마무리**(스케줄러가 아니라 **PreCompact 훅** — 컨텍스트가 차서 대화가
  요약되기 직전 `session_wrapup.py` 가 큐→DB·점유해제·커밋·푸시·인계기록을 자동 수행,
  2026-08-05 지시. 실측 5/5 단계 성공).
  · **세션 경계 — `SessionEnd`·`SessionStart` 훅**(2026-08-13 지시). `/clear` 와 종료도
    같은 `session_wrapup.py` 가 받고(전엔 compact 만 받아 **점유가 고아로 남았다**),
    새 대화가 열리면 `session_boundary.py --start` 가 상태를 되찾아 주입한다 —
    고아 점유 회수 · 맡은 일 · **최근에 바뀐 코드**(요약 속 낡은 사본 경고). 검증 `[241]`.
  · **모델·노력 판정 — `UserPromptSubmit` 훅**(`ai_tier.py --hook`). 사람 말의 갈래가
    **바뀔 때만** 등급을 알려 준다. 대화창 모델은 코드가 못 바꾼다(권고까지다).
  ★ 압축 시점은 `.claude/settings.json` 의 **`autoCompactWindow`**(토큰, 100k~1M)가 정한다.
    **2026-08-07 부터 이 키를 지우고 auto 다**(사용자 승인 — 모델에 맞는 창을 Claude Code
    가 고른다. Fable 5 전환 뒤 수동 420k 는 오히려 창을 좁히고 요약만 잦게 했다).
    더 일찍 요약되게 하려면 이 키를 다시 넣는다. `autoCompactEnabled: true` 가 전제다.
    ※ PreCompact 훅은 공식 설정 문서에 없는 이벤트다 — 지금은 동작하지만(실측),
      Claude Code 판올림 뒤에는 `python ecount/session_wrapup.py` 로 손수 돌려 확인할 것. PC 절전은 AC 에서 **해제됨**(2026-08-01, powercfg 는 관리자 불필요였다).
  · **정오 회차 12:00~13:00** — `쿠팡업무_정오회차`(설치본 `install_noon_schedule.ps1`,
    12:00 부터 10분마다 55분간 부름 · 실행은 `noon_run.py`). **하루 한 번**이고 나머지
    부름은 '다시 시도할 기회'다(2026-08-11 지시: "하루한번 12시에서 13시 사이 다른
    세션이랑 충돌 안나게"). 충돌은 **빼앗지 않고 양보**한다 — `ledger`·`publish` 점유에는
    전부 물러나고(`--force` 로도 안 뺏는다), `code` 점유면 **합성검증만**
    건너뛴다(남이 반쯤 고친 코드가 내는 빨강은 내 회차의 사실이 아니다).
    ★ **도는 회차에는 양보하되, 창의 마지막 기회(12:50)에는 무거운 단계만 비켜 두고 돈다**
    (2026-08-12 실측). 증분 파이프라인은 5분마다 불리는데 새 자료를 만나면 **12분을 넘겨**
    돈다(ERP 대조 한 단계가 417초) — 자료가 들어온 날은 그 락이 창 내내 걸려 **여섯 번이
    전부 양보**로 끝난다. 그런데 '양보'는 실패가 아니라서 **아무 경보도 안 뜬다**
    (`[169]` 모양 — 매일 양보만 하는 회차는 없는 회차와 같다). 양보의 뜻은 **'Z: 를 같이
    긁지 않는다'**이지 '아무것도 하지 않는다'가 아니다. 그래서 마지막 기회에는 Z: 를 훑는
    단계(`HEAVY_STEPS` — 정기점검 조사·캠프명 대조)만 건너뛰고 관문·인계는 돈다.
    이름은 `steps()` 와 **글자까지 같아야** 한다 — 어긋나면 안 건너뛰면서 건너뛴 줄 안다.
    ★ **양보를 완주로 적지 않는다** — 적는 순간 그날 회차가 영영 안 돈다. 창이 끝나도록
    막히면 리포트가 '오늘 건너뜀'을 맨 위에 적는다(조용히 넘어가면 고장과 구별이 안 된다).
    도는 것: 인계 갱신 · 합성검증 · 정기점검 내용 조사 · 캠프명 표준 대조(조사만) ·
    **컴팩팅 자동설정 점검**(autoCompactEnabled + PreCompact 훅을 읽어서 확인한다 —
    빠지면 세션이 가득 찬 순간 인계 없이 끊기는데 그전까지 아무 화면에도 안 뜬다).
    수집·엑셀쓰기·커밋은 하지 않는다. 검증 `[219]`.
  · **코딩 세션 자동 루프 — 하루 한 번, 12:00~13:00** (2026-08-11 "이 세션도 완전
    자동화시켜" → 2026-08-12 지시로 **주기가 바뀌었다**: "루프는 하루에 한번 매일 12시에서
    13시로 설정하고 자동으로 모델과 노력 강도 설정해서 진행하는 알고리즘 적용").
    ★ **그 하루 한 번을 지는 것은 대화 루프가 아니라 스케줄러 회차다** —
    `쿠팡업무_정오회차`(`noon_run.py`, 위 항목). 세션 `/loop` 는 창이 닫히면 같이 죽고,
    **깨우기 간격의 상한이 1시간**이라 '하루 한 번'을 애초에 표현하지 못한다(하루를
    기다리려면 스물네 번 깨워야 한다 — 그건 낭비고, 그 사이 창이 닫히면 그대로 끝난다).
    상시 원칙 그대로다: **파일과 작업 스케줄러에 넣은 것만 산다.**
    · 세션에서 루프를 켜도 된다 — 다만 **창(12~13시) 밖의 틱은 일을 벌이지 않는다**
      (확인만 하고 길게 잡아 물러난다). 창 밖에서 큰 작업을 시작하면 회차와 같은 파일을
      두고 부딪힌다. 켜는 줄: `/loop 코딩 세션 자동 틱: 12~13시 창에서만 대기 작업 처리`.
    · ★ **세션 루프도 '하루 한 번'을 표현할 수 있다 — 깨우기가 아니라 cron 으로**
      (2026-08-12 실측). 상한 1시간은 `ScheduleWakeup` 얘기고, `CronCreate`
      (`3 12 * * *`)는 하루 한 번 12:03 에 그대로 뜬다 — 스물네 번 깨울 필요가 없다.
      다만 **세션 안에만 살고 7일 뒤 만료**되므로 무인 축은 여전히 스케줄러 회차다.
      :00·:30 을 피해 :03 을 쓴다(같은 순간에 몰리지 않게).
    · **이번 틱을 얼마나 세게 할지도 근거로 정한다** — `python loop_policy.py --print`
      (검증 `[231]`). 이미 디스크에 있는 것만 본다: 인계의 '먼저 처리할 것' · 회차 경보.
      가르는 근거는 낱말이 아니라 **누가 할 일인가**다 — 세워 둔 일·회차 끊김은 코드를
      고쳐야 끝나므로 **무거움**, 큐·미푸시·점유는 **보통**, **수집 밀림은 가벼움**
      (수집 세션 몫이라 내 무게가 아니다. 그것만 있는 날까지 opus 를 쓰면 이 알고리즘이
      있으나 마나가 된다). ★ **값(모델·노력)은 여기서 안 정한다** — `ai_tier.TIERS`
      한 곳에서 온다. 첫 판에 표를 여기에도 적었다가 `haiku` 를 넣었는데 ai_tier 는
      **일부러 뺀** 것이었다. 사본을 두면 그 이유가 조용히 사라진다.
      ★ **근거를 못 읽으면 가벼움이 아니라 보통**이다(`[169]`) — 못 읽었다는 이유로
      노력을 낮추면 파일이 깨진 날 사고가 가장 싼 판단을 만난다.
    · **모델·노력 강도는 기계가 고른다** — `ai_tier.pick()` **한 곳**이 판단하고
      `agent_dispatch` 는 싣기만 한다(검증 `[230]`). 갈래: 조회 `sonnet`/low ·
      재시도 `sonnet`/medium · **원인·설계 `opus`/high**. `haiku` 는 안 쓴다 — 이 프로젝트는
      판정이 미묘해서("0건이 없는 건가 안 본 건가") 값싼 오판이 아끼는 것보다 비싸다.
      ★ **없는 깃발은 지어내지 않는다 — 확인한 깃발은 쓴다**: 2026-08-13 부터 모델
      (`--model`)과 **노력(`--effort`) 둘 다 깃발**로 간다(실측 `claude 2.1.222 --help`
      에 `--effort <low|medium|high|xhigh|max>` 실재). 붙이기 전에 `ai_tier.flags()` 가
      **그 실행파일에게 `--help` 로 물어본다** — 못 물어보면 안 붙이고 프롬프트 문장이
      대신한다. 없는 깃발을 붙이면 CLI 가 통째로 안 뜨고 인계가 **조용히** 안 된다
      (`[169]`). 분담판 `[41]`·`[59]` 는 이것으로 끝났다.
    틱 규칙: ① `session_handoff --check` 의 '먼저 처리할 것' → ② 합성검증 빨강 →
    ③ 미커밋(내 것만·경로 지정) → ④ reports ★신규 순으로 보고, **수집은 절대 안 한다**
    (수집 세션 몫). 남의 세션이 살아 잡은 것·남의 미푸시 커밋은 건드리지 않고 다음 틱으로
    넘긴다. ★ **컨텍스트가 차도 루프를 멈추지 않는다** (2026-08-11 "100% 자동화" 지시) —
    요약 직전 PreCompact 훅이 인계(큐→DB·점유해제·커밋)를 자동으로 하고, 자동 요약 뒤
    예약된 깨우기가 루프를 그대로 다시 태운다. 85% 부터는 틱을 가볍게만(새 큰 작업 금지,
    확인·커밋 위주) 돈다. 세션 창이 닫히면 루프도 닫힌다 — 그때는 결정론적 회차(스케줄러)
    와 autopilot(반복 실패 시 Claude CLI 자동 호출)이 무인 축을 그대로 받친다.
    새 세션에서 다시 켜려면 위 한 줄이면 된다.
  · **스케줄러 회차 감시** — 워치독 30분 안의 단계(`schedule_watch.py`). 위 회차들이
    **정말 돌았는지**를 작업 스케줄러에게 직접 묻는다. 죽음·안 돎·등록 안 됨을 인계
    문서 '먼저 처리할 것' 에 올린다(2026-08-12, 분담판 [35]).
  · **크롬 전용 수집 감시** — 워치독 30분 안의 단계(`band/userscript_watch.py`).
    스케줄러가 **모르는 축**이다: 긁는 것은 브라우저 안 유저스크립트라, 그것이 꺼져
    있으면 **어느 회차도 실패하지 않으면서 수집만 0건**이 된다. 실측 2026-08-13 —
    나흘 동안 한 번도 안 돌았는데 아무 화면에도 안 떴다. 안옴·끊김·가려짐·계획굳음·
    확인못함을 인계 '먼저 처리할 것' 에 올린다(2026-08-13 지시, 검증 `[247]`).
  · **올리면 알린다 — 업로드 알림** (2026-08-14 지시). 접수는 올린 그 자리에서
    (`app_server` 가 `notify.expect_upload`), **끝남·실패는 워치독 30분 안의 단계**
    `close_upload_notices`(→ `notify.sweep_uploads`)가 알린다. 판정을 새로 만들지
    않는다(`[162]`) — 누가 돌린 회차든 같은 자국(`automation_pipeline_state.json`)을
    읽는다. 카톡·김미영 입금·Share·PO첨부가 같은 모양이다. 설계는 `SPEC_업로드알림.md`.
    ★ **지금 도는 것은 앱 안 알림뿐이다** — `config/notify.json` 이 없으면 밀어 주는
    채널이 하나도 없으므로 형님이 앱을 열어야 본다. 켜기 전까지 "알림이 간다"고
    말하면 안 된다(`[169]`). 켜도 나가는 것은 **건수·상태뿐**이다
    (업무값은 `notify.external_text` 가 막는다). 검증 `[259]`.
  **여기 없는 것은 자동이 아니다.** 새 알고리즘을 만들면 이 목록에 넣었는지 확인한다.
  ★ 그리고 **넣었다고 도는 것도 아니다** — 등록까지 확인할 것. 그 차이는 이 목록이
    아니라 `schedule_watch.py` 가 안다.
- **자동화의 상한선**: 이카운트·밴드는 **로그인(사람 인증)만 사람이** 하고 나머지는 전자동이다.
  비밀번호 대리 입력은 하지 않는다. 조회 API 부재(절대규칙 3)·앱 심사 대기가 원인이며,
  로그인된 크롬 세션만 있으면 수집→분류→대조→큐→반영이 손 없이 돈다.

## 원본 자료는 한 곳 — 다운로드도 흡수한다 (2026-07-31 지시, 상시)
- 모든 원본 자료의 자리는 `0. 원본 자료`(Z:) 하나다 — 경로는 `source_dirs.py`가 정한다.
- `0. 원본 자료/100. 업로드용 자료`는 종류를 가리지 않는 **단일 투입함**이다.
  `upload_intake.py --apply`가 재귀 검색해 ERP·쿠팡 PO·카톡·밴드·정기점검·PO 문서·
  입금·업무일지 정본으로 내용 분류하며, 모르는 파일도 `9. 미분류`에 원본 보존한다.
  30분 워치독·09:35 원본정리·09:50 전체대조가 모두 이 투입함을 먼저 확인하고,
  새 원본이면 전체 대조와 DB 객관완료 판정까지 이어 간다. 분석기는 미분류/투입 중 파일을 읽지 않는다.
- 브라우저 다운로드(ERP Excel·밴드 덤프)·카톡 내보내기가 Downloads/바탕화면에 떨어지면
  `download_intake.py --apply`가 **내용 판별로** Z:에 이동한다(09:50 daily_run 단계).
  손으로 옮기지 말 것 — 옮겨지는 순간 Z:라서 모든 PC·세션이 같은 자료를 본다.
- 새 자료 종류를 만들면: source_dirs에 자리 → upload_intake/download_intake 판별 → 대조 도구 순으로 잇는다.

## 폰에서 이어받기 (2026-07-31)
- 준비 상태는 언제나 `python ecount/remote_ready.py` 한 줄로 본다. 절차는 `ecount/REMOTE_PHONE.md`.
- **웹 claude.ai/code** — 폰 브라우저, PC 꺼져도 됨. **푸시된 것만** 보이고 Z:·ERP·밴드는 안 된다
  (코드·문서·합성검증용). 그래서 자리를 뜨기 전에 **푸시**해 두는 것이 폰 작업의 전제다.
- **SSH → 이 PC** — 전부 되지만 PC 가 깨어 있어야 한다. 사설망(Tailscale)으로만 접속한다.
- 폰에서든 PC 에서든 **시작 체크리스트는 똑같이 따른다** — 화면이 좁다고 건너뛰지 않는다.

## ★ 컨텍스트가 차 가면 — 이제 자동이다 (2026-08-05 지시)
사용자 지시: **"세션이 컨텍스트 윈도우가 90% 도달시 자동으로 정리하고 /compact 명령 실행."**

- **자동 부분(사람도 AI도 아무것도 안 해도 된다)**
  · 컨텍스트가 차면 Claude Code 가 스스로 요약(compact)한다 — `.claude/settings.json`
    의 `autoCompactEnabled: true`. `/compact` 를 손으로 칠 필요가 없다.
  · **요약이 시작되기 직전** `PreCompact` 훅이 `ecount/session_wrapup.py` 를 돌린다.
    아래 4단계가 그 안에 그대로 들어 있다. 결과는 `reports/세션마무리_기록.json`.
  · 이 스크립트는 **무슨 일이 있어도 exit 0** 이다 — 인계를 남기려다 요약을 막지 않는다.
    엑셀도 열지 않는다(반영은 11:00·15:00 회차 그대로).
- **자동으로 도는 4단계** (직접 부를 때: `python ecount/session_wrapup.py --who claude`)
  1. 입력 큐를 DB 로 — `ledger_db.py --intake`
  2. 점유 해제 — `ai_claim.py --who <claude|codex> --free-all`
  3. 커밋(+푸시 시도). 스테이징에 비밀값 형태가 보이면 **커밋을 멈추고** 사람에게 넘긴다.
  4. 19시트 인수인계 예약 + `session_handoff.py --snapshot`
- ★ **다 차기 전에 알려 주는 눈 — `context_guard.py`** (2026-08-06 지시: "컨텍스트
  윈도우 다 차기전 컴팩팅 자동으로 하는 알고리즘 추가"). 빠져 있던 것은 "요약을
  실행하는 기능"이 아니라 **차 가고 있다는 사실을 제때 아는 것**이었다. 이제 사람이
  입력할 때마다(`UserPromptSubmit` 훅) 실제 사용량을 재고 단계가 올라간 순간에만 말한다:
  **70% 예고**(큰 작업 새로 벌이지 않기) · **85% 마무리**(하던 것만 끝내고 커밋·인계
  전환, 그 순간 `session_wrapup.py` 를 세션당 한 번 자동 실행) · **95% 즉시**(새 작업
  금지 + 사람에게 `/compact` 권유). 지금 몇 %인지는 `python ecount/context_guard.py --print`.
  기록은 `reports/컨텍스트_사용량.json`. 훅은 무슨 일이 있어도 exit 0 이라 사람 입력을
  막지 않는다. `autoCompactWindow` 오버라이드는 2026-08-07 에 **제거(auto)** 했다 —
  키가 없으면 context_guard 는 450,000 을 보수 한도로 삼아 그대로 경보한다. 검증 `[119]`.
- **그래도 AI 가 할 일**: 80% 쯤부터 **새 작업을 벌이지 않는다.** 자동 인계는 "무엇을 했나"를
  파일로 남길 뿐, 반쯤 하다 만 작업을 대신 끝내 주지는 않는다. 마무리로 전환할 것.
- 훅이 안 도는 것 같으면: `.claude/settings.json` 의 PreCompact 항목을 확인하고,
  세션을 새로 열거나 `/hooks` 를 한 번 연다(설정 감시가 그때 다시 읽는다).
- 검증 `[103]` 이 스크립트 4단계와 훅 배선을 지킨다.

## ★ 아무 때나 compact·clear 해도 이어진다 — **세션 경계** (2026-08-13 지시)

사용자 지시: **"아무때나 세션 컴팩팅이나 클리어 해도 문제 없이 돌아갈 수 있는
알고리즘 구현해"**

- **빠져 있던 것은 '요약하는 기능'이 아니라 요약 뒤에 남는 두 가지였다.**
  · **`/clear` 에는 인계가 없었다.** `PreCompact` 훅은 compact 에만 온다. `/clear` 는
    대화만 비우고 **프로세스는 안 죽는다** — 그래서 `ai_claim._is_dead` 는 pid 를 보고
    '살아 있다'고 답하고, 그 sid 는 사라졌으니 `--free-all`(내 세션 것만, `[104]`)도
    닿지 않는다. **아무도 못 푸는 점유**가 남고 그동안 관리대장 동시 쓰기 금지가
    조용히 무너진다. 화면에는 멀쩡히 '점유 중'이라고 뜬다.
  · **요약이 담은 파일 사본은 낡는다.** 실측 2026-08-13: 요약에 실려 온 `ai_tier.py`
    가 옛 것(`flags` 2인자)이라 **이미 3인자로 고쳐진 코드**를 "어긋나 있다"고 진단하고
    도구를 세 번 헛돌렸다. 요약은 **대화**를 줄이는 것이고 **파일을 다시 읽어 주지
    않는다.** 비어 있으면 알아채지만 **낡은 사본은 안 띈다**(`[165]` 와 같은 모양).
- **끝나는 쪽은 이미 있는 것이 받는다** — `SessionEnd`·`PreCompact` 둘 다
  `session_wrapup.py --who claude --quiet`. **마무리 사본을 새로 만들지 않았다**:
  같은 판단이 두 곳에 있으면 언젠가 갈리고, 갈린 뒤엔 어느 쪽이 맞는지 아무도 모른다.
- **시작하는 쪽이 새로 생겼다** — `SessionStart` → `python session_boundary.py --start`.
  한 장으로 주입한다: 고아 점유 회수 · 내 점유 · claude 가 맡은 일 · 먼저 처리할 것 ·
  이번 세션 권장 등급(`ai_tier`) · ★ **최근 45분 안에 바뀐 코드 목록**.
  마지막이 핵심이다 — "요약·기억 속 사본을 근거로 삼지 말고 Read 로 다시 읽어라"에
  **근거**를 붙여 준다. 사람이 보려면 인자 없이 `python session_boundary.py`.
- ★ **`SessionStart` 에 인계를 하나 더 걸지 않는다** (2026-08-13 실측 — 그 자리에
  `session_handoff.py --adopt` 가 **같이 걸려 있었다**. 같은 지시를 두 세션이 각자
  받아 한쪽은 이 파일을, 한쪽은 그 훅을 만들었다). 세 가지가 어긋난다:
  · **점유를 고치는 손이 둘이 된다** — `--adopt` 는 *pid 가 죽은* 점유를, 이 파일은
    *sid 만 사라진* 점유를 회수한다. 노리는 것은 안 겹치지만 **같은 순간 같은
    `ai_claims.json` 을 읽고 쓴다.** 한쪽 기록이 조용히 묻힐 수 있는 자리다.
  · **한 장이 두 장이 된다** — 시작마다 목록이 둘 실린다. 서로 다르게 말하면
    (한쪽은 내 점유를 '경고'로 적는다) 사람도 AI 도 어느 쪽을 볼지 모른다.
  · **`/clear` 가 비싸진다** — `--adopt` 는 큐 흡수·수집 밀림 판정까지 해서 밴드
    캐시를 훑는다(예산 300초). 클리어를 아무 때나 해도 되게 만들자는 것이 이
    지시인데, 클리어마다 그 값을 물리면 뜻이 뒤집힌다.
  · `--adopt` 는 **계정·창이 바뀔 때 사람이 치는 첫 명령**으로 그대로 둔다(위 항목).
    거는 자리가 아니라 **부르는 때**가 다른 것이다.
- ★ **고아 점유를 잘못 회수하면 살아 있는 옆 창을 빼앗는다** — 못 잡는 것보다 나쁘다.
  그래서 문이 넷이다: ① 그 sid 가 살아 있는 목록에 없다 ② **내 sid 는 그 목록에 있다**
  (목록을 믿을 수 있다는 증거 — 내가 없으면 **아무것도 안 한다**, `[169]`) ③ pid 는
  살아 있다(죽은 것은 기존 규칙 몫) ④ 잡힌 지 15분은 지났다(새 창은 대화기록이 아직
  없다). 판단은 `session_wrapup.live_sids`·`ai_claim._is_dead` 를 **빌린다**.
- ★ **훅 갈래를 문서에서 짐작하지 않는다.** `SessionStart` 의 `source`
  (startup·clear·compact·resume)와 `SessionEnd` 의 `reason` 은
  `reports/세션경계_기록.json` 에 쌓이는 **실측**이 말해 준다. 짐작으로 분기를 만들면
  그 분기가 영영 안 타면서 아무 오류도 안 낸다.
- ⚠ **칸 이름을 짝짓지 말 것** — 만들면서 그대로 걸렸다: `worksplit` 은 `owner`·`no` 가
  아니라 **`who`·`id`** 이고 `loop_policy.build()` 는 `무게`·`왜` 가 아니라
  **`갈래`·`값근거`** 다. 틀리게 물었더니 오류 없이 **빈 목록**이 나와 맡은 일이 있는데도
  "없음"으로 보였다(`[165]`). 검증이 이제 실제 칸 이름을 대 본다.
- ★ **배선이 있는 것과 그것이 도는 것은 다른 말이다 — 계기가 눈멀어 있었다**
  (2026-08-13 같은 날 이어서 실측). 위 문단은 "`SessionEnd` 의 `reason` 은
  `reports/세션경계_기록.json` 에 쌓이는 **실측**이 말해 준다"고 적어 뒀는데,
  **그 파일에는 SessionEnd 가 영영 안 쌓인다.** 쓰는 것은 `session_boundary.note` 뿐인데
  그 파일은 `SessionStart` 에만 걸려 있고, `SessionEnd` 에 걸린 `session_wrapup` 은
  `trigger` 만 물었다 — `SessionEnd` 는 `trigger` 가 아니라 **`reason`**(clear·logout…)
  을 준다. 그래서 계기는 `manual` 로 적혔고 그것은 **손으로 돌린 것과 같은 글자**다.
  · 그림이 나쁜 자리는 여기다: `/clear` 마무리가 **한 번도 안 돌았어도, 매번 돌았어도
    두 기록 파일이 똑같이 보인다.** 이 프로젝트가 반복해 당한 `[169]` 의 모양이며,
    하필 **그 사고를 막으려고 만든 기능** 자신에게 붙어 있었다.
  · 고친 곳 둘: `session_wrapup.reason_from_payload()`(계기를 훅마다 제 칸에서 읽는다 —
    `SessionEnd/clear` 처럼 갈래까지 적고, **모르면 `?` 로 적지 `manual` 로 뭉개지 않는다**)
    · `note()` 가 `source`·`reason`·**`trigger`** 를 다 본다(하나만 물으면 나머지 훅은
    갈래가 `?` 로 쌓여 실측이 실측 구실을 못 한다).
  · **마무리 사본은 여전히 안 만들었다** — 자국을 남기는 자리도 `note` 한 곳이다.
  · 검증이 이제 **자국까지** 본다(진짜 기록 파일은 안 건드리고 임시 경로로 잰다 —
    실측 증거에 합성 행을 섞으면 그 파일이 더는 실측이 아니다).
- 검증 `[241]`.

## 종료 체크리스트 (작업을 마칠 때)
1. 의미 있는 변경이면 `ecount/AGENTS.md`의 "현재 상태"·"대기 항목" 갱신
2. `python ecount/workbook_patch.py --b "제목" --c "상세"` 로 19시트 인수인계 행 추가(vN+1)
3. git commit + push (푸시 전 `git grep --cached`로 비밀키 스캔 — AGENTS.md 규칙 1)

## **동시 작업** 규칙 — 세션 단위다 (2026-07-28 · 2026-08-05 지시)
- 시작 전 `python ecount/ai_claim.py --who claude --take <ledger|band|code|publish> --why "이유"` 로 잡는다.
  이미 상대가 잡았으면 **배타 작업은 하지 않는다** — 조회·분석으로 돌리거나 다른 일을 한다.
- **관리대장 쓰기는 절대 동시에 하지 않는다.** 각자 vN+1을 만들면 한쪽이 통째로 묻힌다.
- 우선순위: ① 사용자와 지금 대화 중인 AI ② 원장 쓰기 > 밴드/게시 > 리포트 ③ 먼저 잡은 쪽.
- push 전 `git pull --rebase`. 상대 작업을 지우지 말고 합친다.
- 끝나면 `--free`, 세션 종료 시 `--free-all`.

### ★ 세션마다 맡은 일이 다르다 — 수집 세션 / 코딩 세션 (2026-08-07 지시)
사용자 지시: **"밴드랑 ERP 자료 수집은 'CSOS 리서치 및 자료 수집' 세션에서 항상
하고 있으니 이 세션에서는 하지말고 자료만 참고해서 코딩에 집중해."**

> ★ **2026-08-19 지시로 이 규칙의 판정 근거가 바뀌었다** — 아래 '수집 문' 항목이
> 우선한다. 여기의 세션 이름 규칙은 **역사**로 남긴다. 막으려던 사고 둘(#27·#29)은
> 그대로 막되, 막는 방법이 '누구인가'에서 **'지금 자원이 비었나'** 로 바뀌었다.

- **수집은 'CSOS 리서치 및 자료 수집' 세션이 맡는다**(→ 이제는 *주로* 맡는다).
  밴드 수집·ERP 내려받기가 거기서 항상 돌고 있다.
- 코딩 세션에서 **함부로 하지 않는 것**: `band/collect_*.py` · `band/convert_dump.py` ·
  ERP 내려받기 · `upload_intake --apply` 같은 수집·흡수 명령. 수집 단계가 섞인
  일괄 작업(`daily_run.py`)도 임의로 돌리지 않는다 — **문(`collect_gate.py`)을 거친다.**
- 대신 **이미 있는 것을 읽는다**: `data_status.py --print` · `reports/세션인계.md` ·
  `band/cache` · 최신 대조 CSV.
- **왜**: 같은 자료를 두 세션이 동시에 긁으면 밴드 캐시·Z: 원본이 서로 덮어써
  오염된다(사고 #27 — 유령 22건이 정기점검 1건을 23건으로 부풀렸다). 그리고 수집은
  Z: SMB I/O 를 독점해 앱과 코딩 작업까지 같이 느려진다(사고 #29 — 10시간).

### ★ 수집 문 — **이름이 아니라 자원**으로 판정한다 (2026-08-19 지시)

사용자 지시: **"CSOS 리서치 및 자료 수집 세션에서 하던 일을 지금 현재 이 세션에서도
작업할 수 있게 알고리즘 반영해"**

- **먼저 무엇이 막고 있었는지 재 봤다.** 실측 2026-08-19: 작업 차선 `collect`·`build`
  **둘 다 비어 있었고** `band` 점유도 **비어 있었다**(쥔 것은 `code`·`publish` 뿐).
  즉 **코드로 막는 것은 한 줄도 없었다.** 위 규칙은 **세션 이름**으로 써 있는데
  `ai_claim` 은 창 제목을 모르고 sid 만 안다 — 기계가 지킬 수 없는 규칙이었다.
- 그래서 **두 방향으로 샜다.** 옆 창이 마음대로 긁어도 아무도 못 막고, 이 창은
  **비어 있는 자원을 두고도 일을 안 했다.** 뒤쪽이 이날 지시의 이유다.
- `python collect_gate.py` 하나다(읽기 전용 판정) ·
  `python collect_gate.py --why "이유" --run <스크립트> [인자...]` (문을 통과하면 실행).
  종료코드 **0 완주 · 1 실패 · 3 양보/모름**(아무것도 안 했다).
- ★ **새 판단을 만들지 않았다**([162]) — 세 판정을 **빌려서 합치기만** 한다:
  `lanes.can("band")`(차선) · `ai_claim` 점유판 · `coordinate.running()`(도는 회차).
  여기서 다시 판정하면 같은 자원을 놓고 두 답이 나온다.
- ★ **갈래는 셋이고 '모름'을 '가능'으로 치지 않는다**([169]). 못 읽었는데 긁으면
  그때가 바로 캐시가 덮이는 순간이다 — **물러나는 값은 회차 한 번이고 부딪히는
  값은 되돌릴 수 없다.** 주인을 댈 수 있으면 '모름'으로 뭉개지 않는다(뭉개면
  사람이 할 일이 달라진다).
- ★ **양보는 주장이므로 자국을 남긴다**([293]) — `coordinate.record_yield` 에
  **주인 이름과 함께** 적는다. 주인 이름은 지어내지 않고 `coordinate.running()`·
  점유판이 준 그대로 쓴다. 매일 양보만 하는 문은 없는 문과 같으므로 굶주림 판정이 잡는다.
- ★ **`--force` 는 없다.** 이것은 남이 쥔 자원을 뺏는 문이 아니라 **비어 있는
  자원을 놀리지 않는** 문이다. 남의 점유는 여전히 `ai_claim` 규칙대로만 풀린다.
- ⚠ **`--run` 뒤는 전부 자식 명령**(REMAINDER)이라 `--why`·`--who` 는 **앞에** 적는다.
  뒤에 적으면 자식이 그 깃발을 받고 죽는다 — 만들면서 실측으로 그대로 당했다.
  가로채서 고쳐 주지는 않고(자식이 제 `--why` 를 가질 수 있다 · [172]) **말만 해 준다**.
- ⚠ **자식 stderr 를 버리지 않는다**([289]). 처음에 stdout 만 찍었더니 없는 깃발을
  받은 argparse 오류가 통째로 사라지고 화면에 **'종료코드 2' 다섯 글자만** 남았다 —
  실패를 말하기는 하는데 **왜인지는 영영 알 수 없는** 자리다.
- ⚠ `[175]` 그대로 `proc_guard.run_tree` 를 쓴다. 자식이 SMB(Z:) 대기에 걸리면
  `subprocess.run(timeout=)` 은 윈도우에서 안 끝난다.
- **여전히 사람 몫**은 그대로다: 밴드·이카운트 **로그인**(절대규칙 3) · 로그인된
  밴드 탭을 **보이는 창으로** 꺼내는 클릭 한 번([111]).
- 실측(문을 만든 날, 이 세션에서 통과): 밴드 84789192 보유 3,312건 · 새 글 후보 **0** ·
  구멍 **0** / 90610953 보유 5,276건 · 새 글 후보 **0** · 구멍 **0** · 남은 1건 [5442].
- 검증 `[313]` — 남의 점유·도는 회차를 정말 막는지, 못 읽으면 '모름'이라 하는지를
  **실행으로** 재고([295]), 막는 문을 빼면 그 검사가 통과해 버리는지까지 본다([272]).

### ★ 주인은 `claude` 가 아니라 **세션**이다 (2026-08-05 지시: "지금 현재 열려있는
    세션과 병렬 작업 가능한 구조로 정리")
- 같은 프로젝트 폴더에 **Claude 창을 여러 개** 띄워도 된다. 상대는 Codex 만이 아니다.
- 점유의 주인은 `CLAUDE_CODE_SESSION_ID` 에서 자동으로 오는 **세션 식별자(sid)** 다.
  넘길 것도 외울 것도 없다. `python ecount/ai_claim.py --who claude --whoami` 로 확인한다.
- 그래서 이제:
  · 다른 창의 Claude 가 잡은 배타 점유는 **`who` 가 같아도 못 빼앗는다**(예전엔 말없이 빼앗겼다).
  · `--free`/`--free-all` 은 **내 세션 것만** 놓는다. 남의 원장 점유를 열어 주지 않는다
    (PreCompact 자동 마무리가 특히 위험했다 — 그게 이 규칙이 생긴 이유다).
  · 주인 세션이 **죽었으면 45분 기다리지 않고 즉시** 넘겨받는다(`agent_pid` 생사로 판정).
  · 정말 남의 것을 풀어야 하면 `--force` — 사람이 판단해서 쓴다.
- **일을 시작하기 전에 `python ecount/ai_claim.py` 를 한 번 본다.** 다른 세션이 무엇을
  하고 있는지가 한 줄로 나온다(`← 내 것` 표시). 무엇을 병렬로 해도 되는지가 거기서 정해진다.
- 병렬로 **해도 되는 것**: 서로 다른 배타 자원(한쪽 `band`, 한쪽 `code`), 조회·분석·리포트.
  병렬로 **하면 안 되는 것**: 같은 배타 자원, 같은 파일 편집, 관리대장 쓰기.
- **할 일 분담은 `worksplit.py`** 가 따로 맡는다(층이 다르다: `ai_claim`=자원 잠금,
  `worksplit`=누가 어떤 일을 맡았나). 새 일을 시작하기 전에 `python ecount/worksplit.py`
  로 남은 일과 주인을 보고, 맡을 것을 `--take <번호> --who claude` 로 적는다.
- ★ **`git add -A` 를 쓰지 말 것 — 작업 폴더는 세션끼리 공유한다.** 2026-08-05 실사고:
  `add -A` 로 커밋했더니 옆 세션이 만들던 `worksplit.py`·`ux_review.py`·`index.html`
  까지 한 커밋에 딸려 들어가 원격에 올라갔다(다행히 완성본이라 깨지진 않았다).
  **고칠 파일 경로를 지정해 커밋한다.** `session_wrapup.py` 의 자동 커밋은 다른 세션이
  살아 있으면 **푸시를 보류**한다 — 커밋은 잃는 것이 없지만 푸시는 남의 일이 된다.
- 검증 `[104]`.

## ★ 앱 글꼴 — 한 곳에서 정하고 한 줄로 되돌린다 (2026-08-06 지시)
사용자 지시: **"폰트도 나눔고딕 말고 디자이너들과 사용자들이 선호하고 잘 보이는
폰트로 전체 변경해 / 나중에 내가 명령 내리면 다시 원래대로 할 수 있는 보호 장치도 마련해."**

- **되돌리라는 말을 들으면 이 한 줄이다**: `python webapp/font_switch.py --legacy`
  (다시 바꿀 때는 `--modern`, 지금 상태만 보려면 인자 없이). 네 파일을 한 번에 맞춘다 —
  `webapp/index.html` · `docs/app.html` · `docs/cal.html` · `mobile_snapshot.py`.
  **손으로 고치지 말 것.** 네 곳 중 한 곳을 빠뜨리면 그 화면만 다른 글꼴로 남고,
  화면을 열어 보기 전에는 아무도 모른다.
- 글꼴을 정하는 자리는 파일마다 `:root` 의 **`--font-ui` 한 줄**이다. 본문·캔버스 캡처·
  인쇄가 전부 이것을 본다. 캔버스는 CSS 변수를 못 읽으므로 `uiFont()` 가 읽어다 준다 —
  **그리는 곳에 글꼴 이름을 손으로 적지 말 것**(그러면 화면만 바뀌고 저장한 이미지는
  옛 글꼴로 남는다. 카톡으로 보내고 나서야 안다).
- `--font-ui-legacy` 는 **원래 값(나눔고딕) 그대로** 남겨 둔다. 지우면 "원래대로"가
  무슨 값인지 잃는다. 동봉 사본 `/fonts/nanumgothic.css` 도 그대로 둔다
  (@font-face 는 실제로 쓰일 때만 내려받으므로 지금은 한 바이트도 안 쓴다).
- 사람이 **제 기기에서만** 되돌리려면 앱 [실행] 탭의 '글꼴' 카드에서 고른다(브라우저에 기억).
- 새 글꼴은 기기가 제 UI 에 쓰는 것을 먼저 쓴다(내려받을 것 없음): 애플 SD 산돌고딕 ·
  Segoe UI Variable + 맑은 고딕 · Roboto + 본고딕. Pretendard 가 깔려 있으면 그것이 먼저다.
- 검증 `[126]` 이 구조와 **왕복 무손실**(기본→예전→기본 뒤 파일이 원본과 동일)을 지킨다.

### ★ 글자체를 사람이 고른다 — 표는 하나, CSS·단추는 거기서 만들어진다 (2026-08-13 지시)

사용자 지시: **"갤럭시 글자체 , 아이폰 글자체, 스타일등을 적용해서 사용자가 설정에서
변경할 수 있게 다양한 스타일 적용 코딩해"**

- **층이 하나 늘었다.** `[126]` 은 *모두의 기본 글꼴*을 지켰고, 여기는 그 위에
  **사람이 제 기기에서 고르는 층**을 얹는다. 그래서 갈릴 자리가 셋이 됐다 —
  ① `font_switch.FONT_PRESETS` 표 ② 네 파일의 `:root[data-font="…"]` CSS
  ③ 앱 [실행] 탭 '글꼴' 카드의 단추. **셋 중 하나만 늘면 오류가 안 난다**:
  CSS 만 늘면 고를 단추가 없고, 단추만 늘면 눌러도 아무 일이 안 일어난다(둘 다 조용하다).
- 그래서 **②③ 을 도구가 만든다.** 표를 고치고 `python webapp/font_switch.py --sync`
  한 줄이면 네 파일 CSS 와 `index.html` 단추가 같이 맞춰진다(표식
  `FONT-PRESETS:BEGIN`·`FONT-CARDS:BEGIN` 사이). `apply()` 도 늘 같이 맞춘다 —
  글꼴만 바꾸고 블록을 안 맞추면 그 화면에서만 프리셋이 안 먹는다.
- 지금 표: **기본 · 갤럭시 글자체 · 아이폰 글자체 · 윈도우 글자체 · 또렷하게 · 예전(나눔고딕)**.
  · 사람: 앱 [실행] 탭 '글꼴' 카드에서 고른다(그 기기에서만, 브라우저가 기억).
  · 모두: `python webapp/font_switch.py --preset galaxy` · 목록은 `--list`.
  · **되돌리기 한 줄은 그대로다** — `--legacy`(옛 이름 `--modern` 도 그대로 받는다).
- ★ **내려받는 웹폰트를 넣지 않는다.** 못 받으면 **조용히 다른 글꼴로** 그려진다 —
  화면을 열어 보기 전에는 아무도 모른다. 그래서 '갤럭시 글자체'는 삼성 기기에서만
  진짜로 바뀌고, 그 사실을 카드가 **`실제로:` 줄로 그대로 말한다**(`[169]`) —
  안 적으면 아이폰에서 고른 사람이 "안 바뀐다"고 여기고 멀쩡한 것을 고치러 간다.
- ★ **화면 JS 는 프리셋 이름을 모른다** — `setFontPreset(key)` 가 `data-font` 값
  하나만 받는다. 예외 둘은 이름이 아니라 구조다: `basic`(=:root 기본값이라 속성을
  지운다) · `legacy`(2026-08-06 부터 있던 되돌리기 스위치·옛 열쇠 이어받기).
- ★ **옛 열쇠를 이어받는다** — `csos_font_preset` 이 없으면 `csos_font_legacy` 를 읽는다.
  안 읽으면 예전에 나눔고딕으로 돌려 둔 사람이 **말없이 기본으로 돌아간다.**
- ★ **고른 것을 화면이 표시한다**(`.ui-card.on` + `지금: …` 줄). 무엇이 켜져 있는지
  안 보이면 사람이 눌러 놓고도 바뀌었는지 몰라 계속 다시 누른다.
- **크기는 안 건드렸다.** '또렷하게'는 획을 굵게 그리는 것(안티에일리어싱을 끈다)이지
  글씨가 커지는 것이 아니며, 카드가 그렇게 적어 둔다. 앱은 px 로 못 박은 자리가 많아
  본문 크기만 키우면 **화면이 깨진 채로 커진다** — 반쪽짜리를 넣지 않았다. 분담판 `[72]`.
- ⚠ **고르는 단추는 `webapp/index.html` 하나뿐이다** — 프리셋 CSS 는 네 파일에 다
  들어갔지만 폰 사본(`docs/app.html`)에는 카드가 없다(되돌리기 스위치도 원래
  그랬다). 폰에서 고르려면 고정 주소로 실시간 앱을 열면 된다 — **막힌 것은
  아니지만 반쪽이다.** 분담판 `[74]`. 사본은 **다른 출처**라 localStorage 가 따로
  놀므로, 옮길 때 "한쪽에서 고른 것이 다른 쪽에 안 간다"를 화면이 말해야 한다.
- 검증 `[246]` 이 표·CSS(4파일)·단추가 **정확히 같은 집합**인지, 웹폰트가 안 섞였는지,
  모르는 이름이 **실패로 끝나는지**(0건 성공 금지)를 지킨다.

### ⚠ `[192]` 는 옆 세션의 정상 작업에도 빨개진다 (2026-08-13 실측)
- 이날 합성검증이 **245개를 다 통과하고 맨 마지막 `[192]` 하나에서** 죽었다:
  *"합성검증 시작 뒤 [192]에 오기 전에 실제 계획·게시본이 이미 바뀌었다"*.
  범인은 내 변경이 아니라 **옆 수집 세션**이었다 — `reports/밴드_수집계획.json` ·
  `docs/collect/plan.json` · `docs/collect/grab_posts.js` 셋이 검증이 도는 도중
  같은 초(10:41:46)에 새로 써졌다.
- `[192]` 는 '검증이 실제 파일을 건드리지 않았나'를 **시작 시점 스냅샷과 대조**해
  본다. 그런데 이 프로젝트는 창을 여럿 띄우고 회차가 30분·5분마다 돌므로,
  **남이 정상적으로 쓴 것도 '검증이 오염시켰다'로 읽힌다.**
- 그러므로 `[192]` 가 이 문구로 죽으면 **먼저 그 세 파일의 mtime 을 본다.**
  검증이 도는 동안 찍혔고 내 변경 목록에 없으면 옆 세션 것이다 — 다시 돌린다.
- **그래도 초록이라고 적지 않는다.** 245개가 통과했다는 것과 `ALL GREEN` 은
  다른 말이다(`[169]`). 다시 돌려 `ALL GREEN` 글자를 눈으로 확인한 뒤에 넘어간다.

### ⚠ `[6] 데모 웹앱` 도 같은 자리다 — 기계가 바쁘면 45초를 못 지킨다 (2026-08-18 실측)
- 이날 검증이 295줄째 `[6]` 에서 죽었다: *"데모 웹앱이 45초 안에 포트 18899 를 잡지
  못했습니다(프로세스 종료코드 0)"*. **내 변경 때문이 아니었다** — `import app_server`
  정상, 손으로 띄우면 그대로 뜨고, `netstat` 에 18899 는 **TIME_WAIT 14개뿐 LISTEN 0**
  이었다(쥔 것이 없다). 같은 시각 `automation_pipeline` 이 33분째 Z: 를 물고 돌았다.
- **원인은 순서다** — 데모 서버가 **포트를 잡기 전에** Z:(SMB)에서 관리대장 v598 을
  열어 파싱한다. 그 한 단계가 느려지면 포트는 영영 안 열리고, 검사는 그것을 '안 뜬
  서버'라고 읽는다. 실패 문구가 스스로 후보로 적어 둔 *"기계가 몹시 바쁩니다"* 가 그것이다.
- 그러므로 `[6]` 이 이 문구로 죽으면 **내 변경을 의심하기 전에 셋을 본다**:
  ① `python -c "import app_server"` ② 손으로 `--demo --port 18899` ③ `netstat` 에 LISTEN.
  셋이 멀쩡하면 기다렸다 **다시 돌린다**(실측 재실행에서 `ALL GREEN`).
- **다시 돌려서 초록이 났다고 첫 실패를 지우지 않는다** — 같은 문구로 자주 죽으면
  그때는 기다릴 것이 아니라 **비싼 Z: 읽기를 bind 뒤로 옮기는 것**이 답이다(`[168]`).

## ★ 워크트리에서 일할 때 — 상태는 본체 하나를 본다 (2026-08-06)
- **워크트리의 첫 명령도 `python session_handoff.py --adopt` 하나다.** 거기 0번
  단계로 **본체 잇기**가 들어 있다(잇기 → 큐 흡수 순서다. 뒤집히면 빈 큐를 보고
  "0건"이라 답한다). 손으로 하려면 `python worktree_state.py --apply`,
  지금 상태만 보려면 `python worktree_state.py`.
- **왜 필요한가**: Claude Code 가 만드는 워크트리(`.claude/worktrees/<이름>`)는
  **추적 파일만** 체크아웃한다. 그런데 이 프로젝트의 상태는 거의 전부 git 밖이다
  (`reports/`·`updates/`·`config/*.json`·`db/*.db`). 그대로 두면 실측처럼 된다:
  · **점유가 갈린다** — 워크트리와 본체가 서로 다른 `ai_claims.json` 을 봐서 두 세션이
    동시에 `ledger` 를 잡아도 서로 안 보인다. **관리대장 동시 쓰기 금지가 조용히 무너진다.**
  · **큐가 갈린다** — 워크트리에서 `enqueue()` 한 입력은 11:00·15:00 반영이 영영 못 본다.
  · **분담판이 갈린다** — 워크트리에서 맡은 일이 본체 세션에 안 보여 둘이 같은 일을 한다.
  · **합성검증이 안 돈다** — `config/` 가 없어 t1 에서 죽는다. 즉 "ALL GREEN 확인 후
    실작업" 관문 자체를 통과할 수 없다.
  · **거짓 경보** — 루트 지시문 비교가 `.claude/worktrees/CLAUDE.md` 를 찾아 매번
    "정본과 다르다"를 맨 위에 올렸다(해시는 같았는데도).
- **잇는 방법은 대상마다 다르다.** 폴더가 '전부 무시 대상'인지가 갈림길이다:
  · `updates/`·`band/cache`·`band/ocr_cache` → **정션**(관리자 권한 불필요)
  · `config/*.json` 비밀설정 → **하드링크**(폴더에 추적되는 `*.example.json` 이 섞여 있다)
  · `reports/ai_claims.json`·`reports/worksplit.json`·`db/ledger_queue.db`
    → **링크 금지, 코드가 본체 경로를 집는다**
    (`os.replace` 는 하드링크를 끊고, SQLite 는 `-wal` 사이드카가 갈려 DB 가 깨진다)
  · `reports/` 통째로는 **잇지 않는다** — 추적 파일이 섞여 있어 정션하면 워크트리 git 이
    남의 체크아웃 파일을 물고 간다.
- **이미 따로 있는 것은 절대 덮지 않는다.** '따로있음'으로 남겨 사람이 판단한다.
- 본체에서 돌리면 **동작이 하나도 바뀌지 않는다**(`shared()` 가 제 폴더를 돌려준다).
- 미푸시 판정은 **내 브랜치의 upstream** 기준이다 — 브랜치를 푸시하고도 "미푸시"가
  남던 유령 경보를 없앴다. master 에 아직 안 들어간 커밋은 '미머지'로 따로 보여 준다.
- 워크트리 작업을 본체로 합친 뒤에는 루트 `CLAUDE.md`·`AGENTS.md` 사본을 다시 맞춘다.
- 검증 `[125]`.

## ★ 자율복구는 실패를 숨기는 것이 아니라 **다음 시도를 책임지는 것** (2026-08-09 지시)
사용자 지시: **"클로드 코드에서 작업했던 코딩 작업 모두 고도화해서 문제없이 전체
자동화가 가능하게 코딩해(AI 최대한 활용해서 내가 손댈 필요없이 자동화 100%가 목표)"**

- `autopilot.py`가 결정론적 업무 뒤의 예외를 맡는다. 순서는 **로컬 규칙 실행 → 안전한
  명령만 영속 대기 → 워치독 제한 재시도 → 코드·시간초과 3회 반복만 Claude Code 우선,
  Codex 폴백**이다. 모든 정상 업무를 AI에게 시키지 않는다 — 같은 계산을 비싸고
  비결정적으로 다시 하는 것은 자동화가 아니다.
- 2026-08-09 실사고: 회차 중 Z:가 끊겨 원인 하나가 `관리대장 없음` 18개 실패로
  번졌다. 이제 `daily_run`은 자원·인증·코드·시간초과를 가르고, **재실행 안전한 명령만**
  `reports/autopilot_queue.json`에 중복 없이 둔다. `watchdog`이 30분마다 최대 2개·10분
  예산으로 이어 한다. 성공 확인 전에는 `done`으로 지우지 않는다.
- `--apply`·`--queue`·`--send`·`--post`·`--upload`·`--delete`가 있는 명령은 자동
  재실행하지 않는다. 앞 실행이 반쯤 성공했을 수 있어서 두 번 하면 더 나쁘다.
- AI CLI도 `subprocess.run(timeout=)`을 쓰지 않는다. `proc_guard.run_tree()`가 Windows
  자식 나무를 끊고 드레인에도 제한을 둔다. AI 한 건이 워치독·회차를 영원히 잡지 못한다.
- 앱 `/api/autopilot`과 [실행] 화면이 대기 건수를 말한다. 상세 정본은
  `reports/자율자동화_상태.md`; 질문하지 않아도 무엇이 자동 재개 중인지 보인다.
- **100% 무인 처리와 100% 무권한 처리는 다르다.** 밴드·이카운트 최초 로그인/만료 복구,
  세금계산서 실발행·외부 메시지·취소 확정 같은 비가역 승인, 아직 오지 않은 은행·PO
  원천은 자동으로 꾸며내지 않는다. 인증·자료가 들어오면 그 뒤부터는 자동 재개한다.
- 검증 `[190]`.

## ★ 갱신 한 번은 계산도 한 번이다 (2026-08-10 실사고)
사용자 지시: **"갱신 시간이 너무 오래 걸려 코딩해서 빨리 되게 정리해"**

- 2026-08-09 실측으로 같은 초에 `/api/exec_report`가 **6회**, 어떤 회차에는 **16회**
  호출됐다. 원인은 API 7개가 각각 `() => loadSettle()`이라는 **서로 다른 함수 객체**를
  SWR `Set`에 넣어, 겉은 같은 일인데 중복 제거가 한 번도 되지 않은 것이었다.
  · 여러 API가 같은 화면을 다시 그리면 **하나의 함수 참조**를 공유한다.
  · 화면 집계 함수 자체도 **single-flight**로 감싼다. 보통 갱신은 진행 중인 Promise를
    같이 쓰고, 사람이 누른 강제 갱신만 보통 갱신 뒤에 한 번 더 실행한다.
- **캐시 검사는 Z:/Excel 탐색보다 먼저**다. `get_exec_report(None)`는 캐시가 있어도
  먼저 최신 원장 탐색·엑셀 파싱을 해 매 요청 2~3초를 썼다. 수정 뒤 실측은 콜드
  13.639초, 같은 프로세스 웜 조회 **0.000008초**다. stale이 있으면 즉시 돌려주고
  뒤에서 한 번만 강제 갱신한다.
- 최신 원장 mtime 확인도 요청마다 하지 않는다. 30초 캐시 + 이중확인 락으로 첫 화면의
  병렬 API들이 Z: 탐색을 **한 번만** 공유한다. '못 찾음'도 캐시한다.
- 앱 버튼의 결정론적 작업은 `proc_guard.run_tree()`로 제한시간 안에 반드시 끝낸다.
  정상 종료까지 매번 Claude/Codex에 다시 보내지 않는다. 반복 실패만 `autopilot`이
  안전 재시도·AI 인계한다.
- `확인 필요` 숫자는 **행동 건수·원시 신호·시스템 오류를 더하지 않는다.** 객관완료 DB는
  즉시 숨기고, 전표 매칭키가 깨졌으면 ERP A·B 수백 건 대신 시스템 원인 한 건을 낸다.
  세금계산서 발행일 공란도 실제 미발행이 아니다 — ERP 단계에 따라 `승인 필요 / 선행업무
  자동추적 / 연결자료 필요`로 갈라 이유를 화면에 적는다.
- UX 느린화면 통계는 `AVG(ms)`·횟수·`MAX(ms)`를 따로 저장한다. `MAX/COUNT`를 평균이라
  부르지 않는다.
- 검증 `[191]`.

## ★ 단계 낱말은 한 곳에서 온다 — 앱에서 접수를 등록한다 (2026-08-10 지시)

사용자 지시: **"돌발 AS 카테고리 및 정기점검 카테고리에 입력창을 만들어 여기서 입력하면
db로 저장되고 나중에 엑셀을 한번에 업데이트할 수 있게 류지영 업무센터와 같이 효율적으로
앱에서만 접수 관리 등 진행할 수 있게 알고리즘 구성해, 플로우차트 참고해서 진행할 수 있게
하고 나중에 플로우차트가 변경되면 기능도 따라서 연동되게 코딩 진행해"**

- **입력창**: 돌발AS·정기점검 화면 툴바의 `+ 신규 접수(점검) 등록`. 전에는 **류지영
  업무센터로 로그인했을 때만** 신규 등록 폼이 있었고, 관리자로 앱을 열면 **새 건을
  만들 길 자체가 없었다.** 이제 관리자도 등록한다(`/api/staff/new-job`).
- ★ **폼은 하나다.** 화면마다 새로 그리면 칸이 갈리고 한쪽만 고쳐진다(`[162]`).
  류지영 업무센터의 그 폼을 `layerOpen('newWorkForm')` 으로 끌어올려 쓴다.
- 저장은 2026-08-10 정본 규칙 그대로다 — 응답 전에 SQLite 커밋 + 감사로그 한 트랜잭션,
  멱등키, 낙관잠금. **Excel 직접 mutator 를 새로 만들지 않았다.** "나중에 엑셀을 한번에"는
  이미 있는 보관본 회차(11:00·15:00 + 사람 지시 즉시)가 그대로 한다.

### ★ '플로우차트 연동'은 낱말을 코드에 안 적는 것이다 — `work_flow.py`
- 흐름도가 바뀌어도 기능이 따라가려면 **화면·서버·검증이 단계 낱말을 스스로 적으면 안 된다.**
  적는 순간 사본이 셋이 되고, 흐름도만 바뀌고 기능은 그대로 남는다.
- 정의는 `python work_flow.py` 한 곳이 만든다. 근거는 셋이고 층이 다르다:
  · **관리대장 `10_코드관리` 드롭다운** — 낱말의 정본. 02시트 `진행상태`(Q)·04시트
    `점검상태`(G) 의 데이터유효성이 가리키는 그 열을 읽는다. **어느 열인지도 워크북에서
    읽는다** — 'G열'이라 적어 두면 사람이 출처를 옮긴 날 아무 화면에도 안 뜬다(`[165]`).
  · **앱 흐름도** `ledger_db.flow_steps()` — 낱말이 아니라 **일의 순서와 담당**이다.
  · **원장에 실제로 쓰인 값** — 목록 밖 낱말을 찾기 위해서만 본다. 지우지 않는다.
- ★ **잡는 것보다 잘못 붙이지 않는 것이 어렵다.** 흐름도 단계를 낱말에 붙이는 근거는
  '한쪽이 다른 쪽을 통째로 담고 있다' **하나**이고, **후보가 유일할 때만** 붙인다.
  잘못 붙이면 화면이 엉뚱한 담당자를 가리킨다 — 안 붙은 것보다 나쁘다(`[172]` 의 문).
  그래서 지금 연결은 갈래마다 1개뿐이다. 적다는 것이 곧 **흐름도 이름과 원장 낱말이
  서로 다르게 적혀 있다**는 사실이며, 그것을 숨기지 않고 그대로 보고한다.

### 그날 드러난 것 둘 (둘 다 조용한 종류였다)
- **앱이 목록에 없는 낱말을 만들어 넣고 있었다.** 신규 등록 기본값이 코드에 박힌
  `"접수"`(AS)·`"예정"`(PM) 이었는데 **`접수` 는 관리대장 드롭다운에 없다**(목록은
  `신규접수`). 원장 **85건**이 그 값이다. 엑셀에서 그 칸을 열면 드롭다운에 없는 값이라
  사람이 다시 골라야 하는데, 화면은 멀쩡히 '접수'라고 보여 주므로 **아무도 몰랐다.**
  정기점검도 같은 모양으로 `예정월` **74건**이 있다.
- **여덟 단계 중 다섯은 앱에서 고를 수조차 없었다.** 상태 선택지를 **지금 목록에 쓰여
  있는 값**만 모아 만들었기 때문이다 — 한 건도 안 쓰인 `기사배정·일정확정·방문중·
  재방문예정·보류` 는 후보에 안 올라오고, 안 올라오니 영영 안 쓰이고, 안 쓰이니 다시
  안 올라온다. 그래서 모든 건이 **접수 → 작업완료** 두 칸으로만 움직였다.
  스스로를 강화하는 고장이라 숫자만 보면 정상으로 보인다.

### 지키는 것
- **낱말을 지어내지 않는다**(`[166]`). 목록 밖 값도 **지우지 않고** '— 목록 밖'으로 적어
  고를 수 있게 둔다(이미 그렇게 적힌 행이 있다). 다만 새로 만들지는 못하게 서버가 막는다.
- **바뀌면 자국이 남는다.** 지문이 달라지면 `reports/업무흐름_변경.json` → 인계 문서
  위로 오르고, 사람이 `python work_flow.py --ack` 로 내릴 때까지 남는다. 흐름도가
  바뀌었는데 **아무 화면에도 티가 안 나는 것**이 제일 위험하다.
- **지문에 '쓰인 건수'를 넣지 않는다.** 매일 움직이므로 넣으면 매일 '바뀜'이 뜨고,
  그러면 아무도 안 본다(경보가 대부분이면 그 경보는 죽는다).
- **읽기 전용이다** — 큐에도 안 넣고 엑셀도 안 연다. 비싼 워크북 열기는 캐시 검사
  **뒤**에 온다(`[168]`, TTL 10분 · `reports/업무흐름_정의.json`).
- 09:50 회차 단계다(`work_flow.py --check`). 앱은 `/api/flow-stages` 로 받아 간다.
- 검증 `[196]`.

## ★ 잠깐 끊긴 것과 정말 실패한 것은 다르다 (2026-08-10 지시)

사용자 지시: **"갱신실패 없애고 갱신 잘 되게 해"** (폰 화면에 빨간 카드 일곱 장 ·
전부 `서버 요청이 실패했습니다 (HTTP 502)`).

- **502 는 앱 서버가 준 것이 아니다.** 폰은 클라우드플레어 터널
  (`cloudflared tunnel --url http://127.0.0.1:8899`)을 거쳐 이 PC 로 온다.
  터널이 원본을 못 찾으면 **터널이** 502 를 준다. 앱 서버 코드에는 502 가 한 줄도 없다.
- **원본이 사라지는 때는 정해져 있다 — 앱 서버를 다시 띄울 때다.** 그리고 그것은
  드문 일이 아니다: 코드를 고칠 때마다 그러라고 시켰고(`[156]`) 워치독도 스스로 고친다.
- ★ **실측 9.3초.** `restart_server.py` 가 이제 그 초를 매번 찍는다. 예전에는
  프로세스가 목록에 뜨자마자 '올라왔습니다'를 찍었는데, **프로세스가 있는 것과 답을
  주는 것은 다르다** — 그 사이 9초 동안 폰은 502 를 받는다. 고친 사람은 성공이라
  읽고 폰을 든 사람만 실패를 본다. **실패가 성공처럼 보이는 자리**였다.

### 고친 것
- **먼저 스스로 다시 걸어 본다.** 0 · 1.5 · 5.5 · 14.5 · 29.5초 — 마지막 시도가
  실측 9.3초보다 **뒤**에 있어야 한다. 짧게 잡으면 다시 걸어 놓고도 결국 실패로 끝나
  예전과 똑같아진다. 재시작이 느려지면 이 표도 다시 본다.
- **기다리는 동안 화면이 무슨 일인지 말한다** — `연결이 잠시 끊겼습니다 — 2/5번째
  다시 시도 중`. 그냥 '확인 중'만 있으면 멈춘 줄 알고 사람이 또 누른다.
- **뒤에서 받아 오다 끊긴 것도 곧장 빨갛게 칠하지 않는다.** 사람이 아무것도 안 눌렀는데
  빨간 카드가 뜨던 자리다. 포기했을 때만 위 칩과 카드가 실패라고 말한다.
- **원인이 하나면 손도 한 번이다** — `전부 다시 시도 (n)` 단추 하나. 서버가 잠깐
  끊기면 일곱 개가 **같이** 실패한다. 일곱 번 누르게 하는 것은 화면의 잘못이다.

### ⚠ 아무거나 다시 걸지 않는다
- 다시 건다: `502·503·504·408·425·500` · 네트워크 끊김 · 응답을 끝까지 못 읽음 ·
  JSON 이 아닌 답(터널이 HTML 오류 쪽지를 준다).
- **안 건다**: `401`(로그인 만료 — 백 번 걸어도 같다) · `403` · **`429`(잠금 —
  다시 걸수록 나빠진다)** · `400`대 나머지(내 요청이 틀린 것이다).
- 자동 재시도는 **읽기(GET)에만** 있다. 저장·등록은 한 번 보낸 것이 반쯤 성공했을 수
  있어 그대로 두고 사람에게 알린다(`autopilot` 의 `--apply·--queue` 규칙과 같은 이유).
- 검증 `[197]` 이 '기다리는 시간이 실측보다 길다'와 '다시 걸면 안 될 것을 안 건다'를 지킨다.

## ★ 서버 생존 감시는 업무 회차와 분리한다 — 죽어도 담당자 입력은 남는다 (2026-08-14 지시)

사용자 지시: **"서버가 왜 맨날 죽어 안죽게 해"**, **"서버가 죽더라도 업무볼 수 있는
구조로 알고리즘 반영해"**

- 30분 워치독은 서버만 보는 프로세스가 아니다. 원본 흡수·대조·리포트·스케줄 검사까지
  한 회차라 실측으로 몇 시간 `Running` 상태가 남았다. 그동안 서버가 멎으면 다음 트리거는
  `IgnoreNew`에 막혀 즉시 복구할 감시자가 없었다. **생존 감시는 무거운 업무 회차에
  매달지 않는다.**
- `webapp/server_guard.py`는 10초마다 계산 없는 `/api/ping`만 본다. 한 번 늦은 것을 죽음으로
  단정하지 않고 **3회 연속 실패**일 때만 운영 서버 프로세스를 정확히 골라 재기동한다.
  프로세스가 아예 없으면 즉시 띄우며, 기동 완료는 프로세스 목록이 아니라 실제 ping이
  돌아올 때까지 최대 75초 확인한다. 1분 냉각으로 재기동 폭주를 막고, demo·다른 포트 서버는
  건드리지 않는다. 결과는 `reports/server_guard.log`·`server_guard_status.json`에 남긴다.
- 우선 작업 스케줄러 `CSOS_AppServerGuard`가 로그인 직후 상시 실행되고, 감시자 자신이 죽어도
  5분 트리거와 3회 재시작 설정이 다시 세운다. 이 PC처럼 신규 작업 등록 권한이 막히면 설치기가
  관리자 권한을 요구하지 않는 **HKCU 로그인 자동실행**으로 스스로 전환한다. 그 경우에도
  `tunnel_run.py`가 90초마다 guard heartbeat를 확인하고, guard는 60초마다 tunnel을 확인해
  어느 한쪽이 죽으면 다른 쪽이 다시 세운다. 배터리에서도 멈추지 않으며 창은 띄우지 않는다.
  `앱서버실행.bat`도 같은 감시자를 함께 부르고, 단일 인스턴스 잠금이 중복을 막는다.
- 담당자 화면은 Service Worker의 마지막 정상 화면·SWR 조회 캐시·입력 초안을 그대로 써서
  서버 재기동 중에도 열린다. 쓰기는 IndexedDB outbox에 **본문·첨부·최초 멱등키**를 함께
  보관하고 연결되면 입력 순서대로 자동 전송한다. 인증키·PIN은 디스크에 보관하지 않는다.
- **400대 응답도 조용히 지우지 않는다.** 특히 `409`는 다른 사람이 먼저 고친 낙관잠금
  충돌이다. 자동 재전송을 멈추고 원문을 기기에 남겨 서버 값과 비교하게 한다. 못 보낸 것을
  성공으로 세거나, 거부됐다는 이유로 입력을 버리면 오프라인 보관이 아니다.
- 검증 `[263]`.

### 고정 주소는 origin과 다른 생존 대상이다 (2026-08-14 실사고)

- 형님이 `https://mulder.tailf14aae.ts.net/staff/ryu-jiyeong`가 죽었다고 확인했다. 그 순간
  로컬 `/api/ping` 감시자는 정상이어도 **Tailscale Funnel 앞단**은 따로 끊길 수 있었다.
  origin 하나만 지키고 "서버 정상"이라 적은 것이 사각지대였다.
- `server_guard.py`가 60초마다 `tailscale_serve.public_funnel_alive()`로 **공개 DNS ingress에
  직접 TLS/SNI ping**한다. 이 PC의 사설 100.x 경로가 아니라 담당자 폰이 쓰는 길을 본다.
  한 번 실패로 Funnel을 갈지 않고 3회 연속 실패 때만 같은 고정 주소를 재등록한다.
  공개 DNS 자체의 일시 장애를 Funnel 장애로 세지 않도록 Google 실패 시 Cloudflare DoH로
  한 번 더 확인한다.
- 재등록은 `tailscale_serve.ensure_public_funnel(repair=True)` 정본을 빌린다. 이름을 새로
  만들거나 임시 Cloudflare 주소로 바꾸지 않는다. 최대 1분 넘게 걸릴 수 있어 별도 worker에서
  실행하고, 그동안 10초 origin 감시는 계속 돈다. 5분 냉각과 단일 잠금으로 재등록 폭주를 막는다.
- 감시자는 `pythonw webapp/server_guard.py`로 직접 뜬다. 이 실행에서는 프로젝트 루트가
  import 경로에 자동으로 안 들어오므로 시작 즉시 루트를 넣는다. 테스트 import에서만 되는
  코드는 무인 복구가 아니다.
- 검증 `[264]`.

### 류지영·오종현 업무센터는 서버 관리 에이전트가 지킨다 (2026-08-14 지시)

사용자 지시: **"서버 관리 에이전트 두고 절대 서버 안떨어지게 관리해 / 류지영,
오종현 업무에 지장 주지 마"**

- `server_guard.py`가 범용 `/api/ping`과 별도로 `/staff/ryu-jiyeong`·
  `/staff/oh-jonghyeon`의 실제 HTML을 30초마다 읽는다. 프로세스가 200만 내면서 담당자
  경로가 깨진 상태를 정상으로 세지 않는다. 세 번 연속 실패한 뒤에만 재시작하고, 이 갈래는
  15분 냉각을 둬 코드 오류를 서버 재시작 폭주로 키우지 않는다.
- 생존선은 한 프로세스에 몰지 않는다. ① 서버 관리 에이전트 ② `tunnel_run.py`의 120초
  heartbeat 상호감시 ③ 30분 예약 워치독의 독립 `heal_server_guard` 세 겹이다.
- 설치 1순위는 로그인+5분 반복 예약 작업이다. 관리 PC에서 `Register-ScheduledTask`만
  거부되는 경우가 있어 `schtasks.exe` 방식으로 한 번 더 등록한다. 배터리에서도 시작·유지,
  실행시간 무제한, 1분 간격 3회 재시작을 다시 덮어쓴다. HKCU 로그인 시작도 함께 남기고
  singleton 포트로 중복 실행을 막는다. 예약 작업까지 막힐 때만 로그인 시작으로 내려간다.
- 서버 코드가 새로워도 담당자가 최근 10분 안에 쓰고 있으면 워치독은 즉시 갈지 않고 다음
  회차로 미룬다. 죽은 서버의 복구와 살아 있는 서버의 무중단 갱신을 같은 행동으로 취급하지 않는다.
- PC 전원·인터넷 자체가 끊긴 상태까지 소프트웨어가 살릴 수 있다고 확언하지 않는다. 대신
  화면 Service Worker와 무손실 outbox가 마지막 화면·입력을 보관하고, 서버 복귀 뒤 자동 전송한다.
- 검증 `[265]`.

## Claude Code ↔ Codex 교대 규칙
- 진실의 원천은 **파일**이다: AGENTS.md(요약) + 19시트(원장) + git 이력. 특정 AI의 대화 기록·메모리에 의존하는 정보를 남기지 말 것.
- 교대 시 추가 인수인계 절차는 없다 — 위 시작 체크리스트가 곧 인수인계다.
- Claude 크레딧 소진 → Codex가 이어받고, Codex 소진 → Claude가 이어받는다. 어느 쪽이든 동일.

## ★ 업무 정본 확정 — 앱 뒤 SQLite가 유일한 정본, Excel은 단방향 보관본 (2026-08-10 최신 확정)

- **이 절은 이 문서의 모든 과거 Excel·19시트·큐 반영 설명보다 우선한다.** 위아래 어디에
  `엑셀 반영`, `원장 반영`, `11:00·15:00 입력 반영`, `--apply`, `applyExcelNow`,
  `지금 바로 엑셀에 반영`, `19시트(원장)가 진실의 원천`이라고 적혀 있어도 그것은
  **전환 전 역사와 호환 이름**이다. 현재 업무값의 유일한 정본은 **앱 서버 뒤 SQLite**다.
  규칙 충돌 때 옛 문장을 실행 근거로 삼지 말고 이 절을 따른다.
- **앱 입력은 저장 버튼 응답 전에 SQLite에 즉시 커밋한다.** 브라우저 메모리·대기 큐·Excel
  저장 시각을 기다렸다가 확정하지 않는다. 한 트랜잭션 안에서 업무 행과 감사로그를 함께
  커밋하고, 성공 응답은 두 기록이 모두 확정된 뒤에만 보낸다.
  · 감사로그에는 최소 `actor/session`, `request_id` 또는 멱등키, 대상·행 식별자,
    변경 전/후, 근거·출처, 서버 시각을 남긴다. 감사로그 없는 업무 변경은 성공이 아니다.
  · 모든 쓰기 API는 **멱등키**를 받아 같은 요청의 재전송을 한 번만 반영하고 기존 결과를
    돌려준다. 네트워크 재시도·더블탭이 중복 행이나 이중 완료를 만들면 안 된다.
  · 모든 수정·삭제는 `version`/revision 같은 **낙관잠금**을 검사한다. 읽은 뒤 다른 변경이
    있었으면 마지막 저장자가 덮지 말고 충돌로 남겨 사용자가 두 값을 비교하게 한다.
- **Excel은 SQLite에서 밖으로만 만드는 읽기 전용 시점 보관본이다.** 11:00·15:00은 더 이상
  입력 큐를 Excel에 쓰는 시간이 아니라, 커밋된 DB 스냅샷을 Excel 보관본으로 생성·검증하는
  회차다. 보관본에는 생성 시각·DB revision/배치ID·행수·해시를 남겨 어떤 DB 시점인지 증명한다.
  실패해도 DB 업무는 이미 확정돼 있으며 다음 보관 회차에서 다시 만들면 된다.
- **호환 이름도 의미를 바꾼다.** 기존 `ledger_db --apply`, 11·15시 “원장일괄반영”,
  `applyExcelNow`와 앱의 “지금 바로 엑셀에 반영” 문맥은 구현·문구 모두 **보관본 생성**을
  뜻한다. `applyExcelNow`는 요청 시점의 보관본을 즉시 생성할 뿐 SQLite 업무값을 바꾸거나
  Excel 값을 정본으로 올리지 않는다. 새 UI는 “Excel 보관본 생성/지금 생성”으로 표시한다.
- **역수입 금지.** 보관본·기존 관리대장 Excel과 브라우저 `localStorage`의 값을 SQLite
  정본으로 되밀거나 자동 병합하지 않는다. `localStorage`는 화면 환경설정과 전송 전 임시
  초안만 둘 수 있고, 업무 확정은 반드시 멱등키를 가진 앱 API를 다시 거쳐야 한다.
  ERP 등 외부 근거 파일을 읽더라도 staging·근거 대조를 거쳐 감사 가능한 DB 명령으로
  확정하며, Excel 한 칸을 권위값으로 직접 복제하지 않는다.
- **신규 Excel 직접 mutator 금지.** openpyxl·COM·zip 패치·매크로 등으로 관리대장 셀을
  직접 고치는 새 도구·엔드포인트·버튼을 만들지 않는다. 기존 mutator는 과도기 역사로
  격리·폐기 대상으로만 다루고 기능을 넓히지 않는다. 보관본 생성기는 새 파일을 원자적으로
  만들고 검증할 뿐 기존 Excel이나 SQLite를 역수정하지 않는다.
- **컷오버 완료를 선언하기 전 관문은 네 가지 모두 필수다.** 정책 우선순위는 지금부터
  적용하지만, 기술 전환을 완료했다고 말하려면 다음 증거가 있어야 한다.
  ① **shadow parity**: 같은 입력을 구 시스템과 DB 읽기 모델로 계산해 식별자·상태·금액·
     핵심 화면 집계가 정해진 기간 동안 일치하며 차이는 전부 설명된다.
  ② **백업**: SQLite 일관 스냅샷(WAL 포함)과 마지막 Excel 원본/보관본, 스키마·해시를
     보존하고 실제 복원 시험을 통과한다.
  ③ **충돌 0**: 낙관잠금 충돌·중복 멱등키·미매칭·미이관 대기열이 0이고 제외는 사용자가
     승인한 근거가 있어야 한다.
  ④ **rollback 관문**: 검증된 백업으로 되돌리는 절차와 읽기 전용 전환을 리허설한다.
     rollback도 Excel/localStorage 역수입이 아니라 컷오버 전 DB 스냅샷 복원으로 한다.
  네 관문 중 하나라도 실패하면 legacy 쓰기를 다시 섞지 말고 shadow 상태에서 원인을 고친다.

## ★ 사람 입력 창구는 앱 하나다 — 엑셀 손입력 종료 (2026-08-11 지시)

사용자 지시: **"이제 엑셀에 사람이 입력안하고 앱에서만 입력할거야 관련 내용 알고리즘
반영해, 김미영 입금내역 엑셀은 내가 주기적으로 세션에 업로드 할거야"**

- **2026-08-10 정본 규칙이 남겨 뒀던 마지막 결정이 내려졌다.** "엑셀을 끊는 결정은
  사용자가 한다"고 적어 뒀던 그 결정이다. 이제 관리대장 엑셀에 손으로 적는 것은
  **입력이 아니다** — 입력은 앱(SQLite 정본) 하나다. 류지영·오종현·관리자 모두 같다.
- **과거 손입력 값은 계속 근거로 읽는다.** 역사를 다시 쓰지 않는다 — 원장에 이미
  적힌 비용구분·청구상태·금액은 그대로 유효한 기록이다. 끊는 것은 **새 손입력**이다.
- **손입력을 발견하면 조용히 무시하지 않고 알린다.** 역수입 금지(2026-08-10)라 그 값은
  정본에 안 들어간다 — 그런데 말없이 버리면 **그 사람의 입력이 소리 없이 사라진다**
  (조용한 사고의 새 모양). 그래서 감지가 규칙의 반쪽이다:
  · `realtime_monitor` 가 최신 관리대장의 SHA256 지문을 **직전 지문과 비교**한다 —
    기계 회차(보관본 생성) 없이 바뀐 최신본은 손입력이다. 잡히면 실시간 이슈로 올린다.
  · `ledger_db.human_editing()` 의 ~$ 잠금 감지는 유지하되 뜻이 바뀐다 — 예전엔
    '입력 중이니 기다린다'였고, 이제는 파일 충돌 방지(그대로) + **손입력 시도 기록**
    (`reports/엑셀_손입력_감지.json`)이다. 앱으로 다시 입력하라고 안내한다.
  · 감지는 알리는 것까지다 — **자동으로 DB 에 반영하지 않는다**(역수입 금지 그대로).
    무엇이 맞는지는 사람이 앱에서 다시 입력한 것만이 정본이다.
- **아침 입력 보호시간(08:00~09:30)은 퇴역했다** (`operation_window.py`). '류지영이
  아침에 관리대장을 입력한다'는 전제 하나가 daily_run·게시·반영 등 아홉 진입점을
  매일 90분 멈추고 있었다 — 그 전제가 사라졌다. 실제 파일 충돌 방지는 시각이 아니라
  **증거**(~$ 잠금 감지·연기)가 이미 하고 있고 그쪽은 그대로 둔다.
  되돌리려면 환경변수 `COUPANG_INPUT_WINDOW=08:00-09:30` 하나다 — 지우면 다시 퇴역.
- **화면·안내가 엑셀 입력을 권하면 안 된다.** "엑셀을 한 번 열어 주세요"(제목) ·
  "엑셀을 열었다 저장하면 사라집니다" 같은 문구는 전부 자동 재계산·보관본 회차가
  처리한다는 말로 바꿨다. 관리대장 열기 단추에는 **읽기 전용 보관본 — 여기서 고친
  값은 반영되지 않습니다**를 명시한다. 열람은 정당하다 — 고치는 것만 아니다.
- **김미영 입금내역은 사용자가 세션에 주기 업로드한다** (16.Share 대기에서 변경).
  받으면 `python receipt_apply.py <파일> [--now]` 한 줄이다(kakao_apply 와 같은 골격):
  ① 내용 판별(inbox_scan.classify == receipt — 아니면 exit 2, '0건 성공' 금지)
  ② `7. 입금내역` 날짜폴더로 복사(해시 중복 방지, 원본 보존)
  ③ `receipt_fill --queue` 대조 ④ `--now`(사람 명령)일 때만 즉시 보관본.
  · ★ **csv 는 xlsx 로 바꿔 넣는다** — `receipt_fill.load_deposits` 는 `*.xlsx` 만
    읽는다. csv·xls 를 그대로 두면 **저장은 되는데 영영 안 읽힌다**(읽지 않는 파일은
    빈칸과 같다, `[165]`). xls 는 변환기가 없으면 거부하고 이유를 말한다.
- **아직 엑셀에 남은 큰 전제들은 단계적으로 이관한다**(각각 shadow parity 뒤에만):
  청구상태 정본(billing_status → 앱 DB+화면) · ID 채번(엑셀 수식 → DB 시퀀스) ·
  19시트 인수인계(→ DB) · 드롭다운 낱말 정본(10_코드관리 시트 → 앱 DB).
  이관 전까지 기존 경로는 **확장 금지·유지만** 한다.
- 검증 `[212]`.

## ★ 자료 한 번 넣으면 끝까지 간다 — 증분 자동화 파이프라인 (2026-08-10 지시)

사용자 지시: **"카톡 데이터는 나 또는 류지영이 매일 다운 받아서 올리고, 다른 것은
전부 자동화. 사람 손 없이 Excel도 자동 입력"**

- `automation_pipeline.py --once`가 **5분마다** 새 자료 지문만 본다. 카톡·밴드·ERP 중
  바뀐 갈래만 `원본 흡수 → 대조 → 객관완료 → app_store → 검증된 Excel 보관본`으로
  끝까지 잇는다. 전체 `daily_run`을 자료 한 건마다 다시 돌리지 않는다.
- **카톡의 사람 몫은 내보내기 파일 업로드 한 번뿐**이다. 앱 자동화 카드의 `.txt` 등록
  또는 바탕화면/다운로드의 `KakaoTalk*.txt`를 감지하면 이후는 자동이다. 같은 내용은
  이름이 달라도 SHA256으로 한 번만 처리하며 원본은 지우지 않는다.
- **밴드·ERP는 로그인만 사람 경계**다. 밴드는 공식 토큰 또는 로그인된 탭의 유저스크립트가
  수집하고, ERP는 로그인 세션이 만든 내보내기를 감지한다. 인증이 낡으면 성공을 가장하지
  않고 앱에 `로그인 필요` 한 건만 띄운다. 비밀번호 입력·인증 우회는 하지 않는다.
- `canonical_sync.py`가 과도기 `ledger_queue.db`의 미반영 큐뿐 아니라 현장·정산·담당자
  객관완료 근거를 **앱 DB의 `completion_evidence`**로 멱등 보존한다. ID 정확 일치 또는
  프로젝트NO 유일 일치일 때만 업무 상태를 올린다. 취소·철회나 다중 일치는 자동 덮지 않는다.
- Excel은 `archive_worker.py`가 **로컬 새 파일**로만 만든다. 기존 파일·Z:/UNC는 쓰지 않고,
  템플릿 바이트 불변·DB snapshot/command-plan 해시·OOXML CRC를 모두 통과해야
  `last-good`가 된다. 그 뒤에만 해당 app DB outbox를 완료로 내린다.
- 실패는 단계·원인·재시도 상태로 `reports/automation_pipeline_state.json`에 남는다.
  PID 잠금, 자식나무 종료, 제한 드레인으로 한 단계가 다음 회차를 영구히 막지 못한다.
- 주 경로는 작업 스케줄러 `CSOS_AutomationPipeline`(5분), 워치독 맨 앞 단계가 안전망이다.
  설치·갱신은 `install_automation_schedule.ps1`; 앱의 `지금 처리`도 같은 결정론적 회차를 쓴다.
- 검증 `[195]`.

## 과도기 역사 — 엑셀에 저장하지 않고 DB로만 관리 (2026-07-31 지시)
> 아래 내용은 전환 과정의 배경 기록이다. 현재 실행 의미는 위 2026-08-10 정본 규칙이 우선한다.

사용자 지시: "최종 목적은 엑셀 파일에 저장 안 하고 DB로만 관리하는 거야."
- **새로 만드는 상태·판정·완료 기록은 엑셀 셀에 쓰지 말고 DB(ledger_db)에 넣는다.**
  첫 적용: 객관 입증 완료(`resolution` 표) — ERP가 입증한 완료 건은 06시트 발행일을
  백필하지 않고 DB가 근거·최초 입증 시각을 기억한다(검증 [95]).
- 엑셀 반영(11:00·15:00 두 회차)은 **사람이 여전히 엑셀로 일하는 동안의 과도기**다.
  이관 순서: 상태·완료 판정(끝) → 신규 입력 → 원장 자체. 각 단계는 사람(류지영·오종현)이
  앱으로 같은 일을 할 수 있게 된 뒤에만 넘어간다 — 엑셀을 끊는 결정은 사용자가 한다.
- 판단 기준: "이 값을 엑셀에 꼭 써야 하나?" 기본값은 **아니오**다. 예외는
  ① 사람이 엑셀에서 직접 읽어야 하는 값 ② 기존 수식이 참조하는 값 뿐이다.

## 과도기 역사 — 엑셀은 하루 두 번만 반영 (2026-07-30 지시)
> 아래의 “반영”·`--apply`·강제 즉시 반영은 이제 모두 **DB→Excel 보관본 생성**으로만 읽는다.

- 앱·Claude·Codex 어느 쪽이 무엇을 채우든 **곧바로 엑셀에 쓰지 않는다.** `ledger_db.enqueue()`
  또는 기존 도구의 `--queue` 로 넣으면 `ledger_db --intake` 가 SQLite(`db/ledger_queue.db`)로 흡수한다.
- **엑셀 반영은 11:00 과 15:00, 하루 딱 두 번.** `python ledger_db.py --apply` 가 시각을 보고
  판단하며, 시각이 아니면 아무것도 하지 않는다. 놓친 입력은 다음 **허용된** 11시/15시
  회차에 함께 처리하며 임의 시각에 뒤늦게 원장을 열지 않는다. 작업 스케줄러는
  `install_ledger_schedule.ps1`이 두 회차를 한 작업으로 관리한다.
  ★ 예전에 트리거 누락을 메우려고 만들었던 **`쿠팡업무_원장일괄반영_15시` 는
    2026-08-07 에 지웠다**(사용자 승인). 본 작업이 이미 11:00·15:00 트리거를 둘 다
    가지고 있어 중복이었고, 둘이 같이 떠서 한쪽이 잠금에 막혀 **매일 `결과: 1` 로
    실패**하고 있었다. 되돌릴 정의는 `reports/스케줄러_백업_원장일괄반영_15시.xml`.
    다시 만들지 말 것 — 같은 실패가 되살아난다.
- 긴급할 때만 `--apply --force`. 강제 여부가 `batch` 표에 남는다.
- ★ **사람이 명령하면 지금 넣는다** (2026-08-07 지시: "이런거 무시하고 내가 명령
  내리면 실시간으로 엑셀 반영하는 알고리즘 추가"). 규칙이 뒤집힌 것이 아니라
  **무엇을 막는 규칙인지가 분명해진 것**이다 — 막으려던 것은 *도구가 채울 때마다
  저절로* vN+1 이 쏟아지는 것이었지(v311→v327), 사람이 스스로 내린 명령이 아니다.
  · 앱 [엑셀 반영 예정] 카드의 **'지금 바로 엑셀에 반영'** 단추 하나다.
    확인창을 거치고 `ledger_db.py --intake --apply --force --now` 로 돈다.
  · **자동으로 도는 것은 여전히 두 회차뿐이다.** `daily_run`·`session_wrapup`
    같은 무인 경로가 이 길을 부르면 검증 `[93]` 이 막는다.
  · 검증 `[93]` — '자동 금지 · 사람 지시 허용'으로 좁혀 다시 세웠다.
- 앱은 "몇 건이 대기 중이고 몇 시에 들어가는지"를 대시보드에 항상 표시한다(사용자 오해 방지).
- **왜**: 채울 때마다 vN+1 을 만들어 하루에 버전이 수십 개씩 늘었고(v311→v327), 사람이 파일을
  열어 작업하는 중에도 새 버전이 생겨 정본이 흔들렸다.

## UX 기록 — 앱 개선은 추측이 아니라 기록으로 (2026-07-30 지시, 상시)
- 앱 사용 흔적(화면 이동·탭·검색 무결과·오류·느린 화면)은 `ledger_db.ux_add()` 로 쌓인다.
- **앱 UI/UX 관련 지시를 받으면 먼저 `python -c "import ledger_db,json;print(json.dumps(ledger_db.ux_summary(),ensure_ascii=False))"`
  로 실제 사용 기록을 보고 시작한다.** 어디서 막히는지 화면 기록이 말해 준다.
- 서버 `/api/ux` 로도 요약을 볼 수 있다.

## ★ 앱 자체 진단·대표 보고·ERP 공식 API (2026-08-14 지시)
- **진단 정본은 `python system_audit.py --print` 하나다.** 앱 서버가 15분마다 원장·Excel을
  열지 않고 상태 캐시만 읽어 `reports/시스템_업무진단.{json,md}`를 갱신한다. 앱 실행 화면,
  Claude Code, Codex, 세션 인계는 모두 그 결과를 읽고 별도 판정을 만들지 않는다.
- 무인 워치독은 `COUPANG_UNATTENDED=1`을 호출 경계에서 보장한다. 따라서 서버 코드가
  바뀌어도 `지금 내릴까요? (y)` 같은 입력 질문을 띄우지 않는다. 터널 정리는 이 프로젝트의
  `webapp/tunnel_run.py`와 8899 포트 자식만 대상으로 하며 다른 cloudflared를 죽이지 않는다.
- **대표 캘린더 캡처는 오전 유수비 통화 순서가 정본이다.** 대표용에서는 정기점검 예측을 빼고,
  왼쪽은 정기점검 `완료 → 확정 일정 → 잔여`, 오른쪽은 돌발AS
  `완료 → 미처리 → 지연 사유`로 고정한다. 정기점검은 분기 실제 진도·선택 월말 기대 진도·
  분기말까지 필요한 하루 처리량을 자동 계산한다. 돌발AS 3일 초과는 미래 방문일·명시적 부품
  수급·날짜가 붙은 캠프 협의가 있을 때만 `협의대기`, 그 밖은 `★ 즉시조치`다.
- **ERP API는 확인된 조회만 호출한다.** `python erp_api_collect.py`가 품목과 발주서를
  가져와 `inbox/api` 원본과 datalake에 멱등 보존한다. 이카운트 공개 안내상 판매/매입전표·
  세금계산서·수금은 조회 API가 아니므로 method를 추측하지 않고 기존 XLSX 도착확인 경로를 쓴다.
  브라우저가 Excel 단추를 누른 결과는 `다운로드요청`일 뿐이며, `download_intake.py`가
  실제 새 XLSX를 확인해야 최종 성공이다.
- API 테스트키는 `config/ecount_config.json`의 테스트 칸에만 보관하고 운영 모드를 임의로
  전환하지 않는다. 현재 IP가 허용 목록에 없으면 호출 전에 멈추고 앱 진단에 `IP 등록 필요`를
  남긴다. 키·세션ID·회사코드는 보고서·채팅·git에 출력하지 않는다.
- 검증 `[266]`·`[267]`.

## 토큰 절약 상시 규칙 (별도 지시 없이 항상 적용)
- 명령 출력은 항상 좁힌다: `| tail -N`·`| head -N`·`grep`으로 필요한 줄만. 경고·진행률(`UserWarning`, 다운로드 바)은 걸러낸다.
- 파일은 부분만 읽는다(Read offset/limit, Grep). 방금 편집한 파일을 재확인용으로 다시 읽지 않는다(Edit이 실패하면 알려준다).
- 검증은 한 번에: 여러 확인을 python -c 한 줄로 묶어 실행한다. 같은 사실을 두 번 확인하지 않는다.
- 에이전트·스크립트 출력은 요약형으로 만든다(상세는 reports/ 파일에, 콘솔에는 집계 한 줄).
- 무관한 작업으로 넘어갈 때는 `/clear`를 권한다(`/compact`는 그 자체로 큰 요청이라 더 비싸다).
- 이 절약 규칙 때문에 검증을 생략하지는 않는다 — 합성검증·비밀스캔은 그대로 수행한다.

## ★ DevTools 자동 수집은 전면 Chrome의 지정 3페이지에서만 (2026-08-14 지시)

사용자 지시: **"세 군데서만 DevTools 자동 수집 크롬에서만 동작하게 변경하고,
15분마다 실행을 매일 12시 한 번으로 변경하고 내가 명령 내릴 때는 실행"**

- 허용 대상은 딱 셋이다: `https://www.band.us/band/90610953/post` ·
  `https://www.band.us/band/84789192/post` · `https://loginab.ecount.com/ec5/view/erp`.
  이카운트 `ec_req_sid`는 로그인마다 달라지므로 **scheme+host+path를 정확히** 비교하고
  query만 무시한다. 밴드 글 상세(`/post/번호`)·다른 밴드·하위 도메인도 허용하지 않는다.
- `band/browser_guard.ps1`이 **현재 전면 창의 프로세스가 `chrome.exe`인지**, Chrome의 진짜
  omnibox(`OmniboxViewViews`) 주소가 위 셋 중 하나인지 읽기 전용으로 확인한다. 웹페이지의
  URL 모양 입력칸은 주소창으로 인정하지 않는다. 주소를 못 읽어도 실행하지 않는다.
- **웨일·Edge·다른 프로그램·다른 사이트는 한 글자도 입력하지 않는다.** 창 활성화,
  최소화 해제, 탭 순회, URL 자동 이동을 전부 금지한다. 사람이 다른 창이나 탭으로 옮기면
  다음 키 입력 직전에 관문을 다시 검사해 즉시 중단한다. `--manual`도 이 관문을 우회하지 못한다.
- 예약 작업 `CSOS_BrowserChain`은 **매일 12:00 한 번만** 부른다. 반복 트리거와
  `StartWhenAvailable`은 쓰지 않는다 — 정오를 놓친 작업이 업무 중 갑자기 살아나면 안 된다.
  지정 페이지가 전면이 아니면 안전하게 아무 입력 없이 끝내고 이유만 기록한다.
- 추가 실행은 형님의 명시적 명령이 있을 때만 다음 중 하나를 쓴다:
  `python band/browser_chain.py --manual band-90610953` ·
  `python band/browser_chain.py --manual band-84789192` ·
  `python band/browser_chain.py --manual erp`. Claude Code·Codex·앱은 스스로 수동 명령을
  추론해 실행하지 않는다.
- 검증 `[269]`.

## ★ 창을 세는 자리를 만든다 — 자식까지 (2026-08-14 지시)

사용자 지시: **"팝업창좀 안뜨게 해 백그라운드에서 작업해, 노트북을 못쓰겠어"**

- **같은 지시가 세 번째다**(`[248]` 회차 · cloudflared · `[270]` AI CLI). 그때마다
  **그 자리 하나만** 고쳤으니 다음 자리가 남아 있었다. 이번엔 자리를 **세었다**.
- 실측 2026-08-14: 예약 작업 **13개 전부**와 로그인 항목이 이미 `pythonw`·
  `run_hidden.vbs` 였다 — `[248]` 은 지켜지고 있었다. 그런데도 창이 떴다.
  범인은 **회차가 띄운 자식 25곳**이었다: `git` 11 · `powershell` 4 · `cmd` 3 ·
  `taskkill` 2 · `node`·`tasklist` 등. 30분 워치독이 `session_handoff`·`lanes` 로
  git 을 부르는 자리, 앱 재시작이 `powershell` 로 프로세스를 세는 자리가 거기다.
- ★ **부모를 `pythonw` 로 바꾼 것이 자식의 창을 새로 만든 셈이다.** 콘솔 없는 부모가
  콘솔 exe 를 깃발 없이 띄우면 윈도우가 **새 콘솔을 할당한다.** 부모에게 콘솔이
  있었다면 자식은 그것을 물려받아 새 창을 안 만들었다 — 즉 창을 없앨수록 자식 쪽이
  나빠진다. **창을 없앨 때는 그 프로세스가 띄우는 자식까지 같이 본다.**
- `python tools/window_audit.py` 가 그 자리를 센다(읽기 전용 · AST). 검증 `[272]` 가
  **0곳**을 지킨다. 새로 `subprocess` 로 콘솔 exe 를 띄우면 여기서 걸린다.
- ★ **잃는 로그가 없다는 것을 먼저 확인하고 껐다** — 25곳 전부 출력을 파이프로
  붙잡고 있었다(`capture_output`/`stdout`). 콘솔로 흘리는 호출이었다면 창을 없애는
  순간 **아무 데도 안 남는다**(`realtime_monitor.bat` 이 그랬다, `[248]`).
- ★ **계기 자신을 시험한다.** 0 을 내는 계기는 아무도 의심하지 않는다(`[169]`).
  `[272]` 는 일부러 나쁜 코드를 만들어 **잡히는지**, 깃발 단 코드에 **거짓 경보를
  안 내는지**를 같이 본다.
- ⚠ **`DETACHED_PROCESS`(0x8) 는 창 없는 깃발이 아니다**(2026-08-13 에 적은 그대로).
  부모 콘솔을 안 물려받는다는 뜻일 뿐이라 자식이 콘솔 exe 면 **새 콘솔이 뜬다.**
  감사기도 그것을 창 없는 깃발로 치지 않는다.
- ⚠ **고친 뒤에는 도는 프로세스를 갈아야 한다**(`[156]`). 워치독·감시자는 옛 코드를
  메모리에 물고 있어 다음 회차까지 계속 창을 단다.

## ★ Claude Code·Codex 자동 작업은 창 없는 백그라운드 CLI로만 (2026-08-14 지시)

사용자 지시: **"작업할 때 클로드코드나 코덱스 모드 팝업 좀 안 뜨게 백그라운드에서
띄워 작업하게 코딩해"**

- 프로젝트 자동 인계는 데스크톱 Claude/Codex 앱을 실행하지 않는다. **Claude Code CLI의
  print 모드(`claude -p`)** 또는 **Codex 비대화식 모드(`codex exec`)**만 쓴다. 같은 이름의
  WindowsApps·`Programs\Claude`·`Programs\Codex` GUI 실행파일은 사용 가능 에이전트로
  인정하지 않고, CLI가 없으면 대기열에 `사용 불가`로 남긴다.
- 모든 자식 실행은 `proc_guard.background_popen_kwargs()` 한 곳을 거친다. Windows에서는
  `CREATE_NO_WINDOW`와 `STARTUPINFO(SW_HIDE)`를 둘 다 적용하고 stdin은 `DEVNULL`로 닫는다.
  하나만 쓰면 포장 실행파일이 다른 쪽을 무시해 콘솔·로그인 질문 창이 다시 나타날 수 있다.
- 백그라운드 에이전트 환경은 `CI=1`·`TERM=dumb`·`NO_COLOR=1`을 명시한다. 인증키를 새로
  환경변수에 넣지 않으며 저장된 CLI 인증만 쓴다. 로그인·권한 질문이 필요하면 창을 띄워
  기다리지 않고 실패/대기로 기록한다.
- 앱 업무 실행기와 `agent_dispatch.dispatch_async`도 같은 관문을 쓴다. stdout/stderr는
  티켓의 `reports/agent_dispatch/*.log`와 상태 JSON으로 남기고, 사람 화면으로 터미널을
  올리거나 포커스를 가져오지 않는다.
- **사람이 직접 연 Claude/Codex 데스크톱 창은 건드리지 않는다.** 프로젝트가 새 GUI 창을
  자동으로 열지 않는 것이 이 규칙의 범위다.
- 검증 `[270]`.

## ★ 대표 캘린더 캡처는 A4 세 장 · 사유는 전부 보인다 (2026-08-14 지시)

사용자 지시: **"이거 3장으로 나오게 처리해서 사유 내용이 다 보이게 코딩 업데이트 해"**

- 대표 캘린더 저장본은 **정확히 세 쪽**이다: 1쪽 현재 월 달력·핵심 판정, 2쪽 현재 월
  정기점검 전 건, 3쪽 현재 월 돌발AS·기타 일정 전 건. 전월 현장별 목록은 싣지 않고
  완료·예정·미처리 숫자 요약만 싣는다. 서로 다른 업무를 한 쪽의 좁은 단에 섞지 않는다.
- 상세 사유에는 말줄임표를 쓰지 않는다. `wrapAll`이 실제 글자 폭을 재 줄바꿈하고,
  `detailLayout`이 제목·사유의 실제 줄 수로 블록 높이를 계산해 다음 단으로 넘긴다.
- **자리가 모자라도 뒤 행을 조용히 버리지 않는다.** 세 장에 전 건을 넣지 못하면 불완전한
  이미지를 저장하지 않고 범위를 좁히라는 안내를 낸다. `slice(0, 표시가능행)` 같은 개수
  자르기와 사유 `clip(...)`은 금지한다.
- 저장 버튼·완료 문구·쪽 표시는 모두 `A4 세 장`과 `1/3·2/3·3/3`으로 맞춘다.
- 검증 `[273]`.

## ★ 모바일 조직도는 마지막 줄까지 가운데 정렬 (2026-08-14 지시)

사용자 지시: **"모바일 화면 조직도 왼쪽 정렬 말고 중간 정렬도 깔끔하게 정렬해"**

- 모바일 조직도 카드 묶음은 줄마다 가운데 정렬한다. 첫 줄만 가운데 있고 마지막 한 명이
  왼쪽에 남으면 안 된다.
- `grid auto-fill`은 마지막 줄에도 빈 열을 남기므로 이 요구를 충족하지 못한다. 폰 구간은
  `flex-wrap`과 `justify-content:center`를 쓰고, 각 책상은 `flex-basis:var(--org-w)`로
  고정해 방·인원수에 따라 카드 폭이 달라지지 않게 한다.
- 카드 내부의 이름·상태는 읽는 방향대로 왼쪽 정렬을 유지한다. 바꾸는 것은 카드 묶음의
  배치뿐이며, 길게 눌러 자리 재정렬하는 기능과 데스크톱 grid는 건드리지 않는다.
- 검증 `[274]`.

## ★ 정기점검·돌발AS 카드는 수정 → 저장을 사람이 확정한다 (2026-08-14 지시)

사용자 지시: **"수정, 저장 버튼 추가 및 기능 추가, 변경이되거나 저장이되면 알림 팝업
띄우는 기능 추가"**

- 정기점검·돌발AS 카드 입력칸은 기본 읽기 상태다. `수정`을 눌러야 입력할 수 있고,
  `저장`을 누르기 전 변경값은 브라우저 초안일 뿐 서버·Excel에 쓰지 않는다.
- 값이 바뀌면 비차단 알림으로 **"변경됨 — 아직 저장 전"**을 즉시 알린다. 저장 성공 후에는
  **"저장 완료"**와 실제 반영된 항목명을 알린다. 저장 실패도 초안을 버리지 않고 이유를 알린다.
- 한 카드에서 여러 항목을 바꿔도 `/api/staff/entry` 한 요청·한 트랜잭션으로 저장한다.
  상태와 완료일을 함께 쓰는 완료 처리도 두 번 나눠 저장하지 않는다.
- 다른 기기의 변경으로 버전 충돌이 나면 자동 덮어쓰지 않는다. 초안을 남기고 재시도를 안내한다.
- 알림은 업무를 가로막는 확인창이 아니라 잠시 보이는 `toast`를 쓴다.
- 검증 `[275]`.

+## ★ 워크플로우는 마우스로 놓고 잇고 그릴 수 있다 (2026-08-14 지시)

사용자 지시: **"워크플로우를 마우스로 클릭해서 넣고 화살표 넣고 그리고 이러는 기능 추가해"**

- 기존 단계 목록 `flow_step`은 업무 절차의 정본이고, 자유 배치 도면 `flow_visual`은
  노드 위치·화살표·손그림의 정본이다. 둘을 섞지 않는다. 도면 선을 지우다 업무 단계가
  지워지는 사고를 막기 위해서다.
- 도면 도구는 선택·이동, 단계 넣기, 화살표, 그리기, 선택 삭제, 방금 취소, 현재 단계
  자동 배치, 도면 저장, 저장본 되돌리기를 제공한다. 마우스와 포인터 이벤트를 함께 써
  터치 화면에서도 같은 동작을 유지한다.
- 작업면 좌표는 1400×820으로 고정하고 좁은 화면에서는 작업면 자체를 스크롤한다.
  기기 폭에 따라 좌표를 다시 계산하면 PC와 모바일에서 서로 다른 배치가 된다.
- 저장은 `revision` 낙관 잠금을 쓴다. 다른 기기가 먼저 저장했으면 늦은 저장으로
  덮지 않고 새로 읽은 뒤 다시 수정하게 한다. 저장 직전 모습은 `flow_visual_audit`에
  남겨 도면만 되돌릴 수 있으며, 업무 단계 되돌리기와 섞지 않는다.
- 한 번도 도면을 저장하지 않은 차트만 현재 업무 단계로 자동 배치해 보여 준다.
  이미 저장된 빈 도면은 사용자의 선택이므로 다시 자동 생성하지 않는다.
- 검증 `[276]`.


+## ★ 조직도 업무 중 표시등은 깜빡인다 (2026-08-14 지시)

사용자 지시: **"업무중일 때 불이 깜빡깜빡하게 수정"**

- 조직도 상태가 `busy`인 자리와 범례의 주황 표시등에만 점멸 효과를 준다.
  카드·이름·상태 글자까지 움직이면 업무 화면이 산만해지므로 표시등만 움직인다.
- `live`의 파란 확산 효과와 `busy`의 주황 점멸은 서로 다른 키프레임을 쓴다.
  대기·정상과 오프라인 표시는 움직이지 않는다.
- 운영체제의 `prefers-reduced-motion: reduce`가 켜져 있으면 모든 상태 표시등
  애니메이션을 멈춰 접근성 설정을 존중한다.
- 검증 `[277]`.


+## ★ 캘린더 상세는 현재 월만, 전월은 요약만 표시한다 (2026-08-14 지시)

사용자 지시: **"현재 월만 표시되게하고 전월은 요약 내용만 나오게 정리"**

- 대표 캘린더 캡처의 2쪽 정기점검 상세와 3쪽 돌발AS·기타 상세에는 선택한 현재 월
  일정만 싣는다. 분기 전체 행을 2쪽에 넣어 전월 현장 목록이 다시 나타나면 안 된다.
- 전월은 현장명·프로젝트·사유 목록을 펼치지 않고 **전체·정기 완료·정기 예정·정기
  미처리·AS 완료·AS 미처리·기타** 건수만 2쪽 위 요약띠에 싣는다.
- 1쪽의 분기 진도율은 대표 판단용 **요약 수치**이므로 유지한다. 요약과 현장별 상세를
  가려야 하며, 분기 진도 계산을 없애서도 전월 상세를 다시 넣어서도 안 된다.
- 현재 월 상세 사유는 기존처럼 말줄임 없이 전부 줄바꿈하고, 공간이 모자라면 불완전한
  캡처를 저장하지 않는다.
- 검증 `[278]`.


## ★ 설정 메뉴에서 관리자 모드를 관리한다 (2026-08-14 지시)

사용자 지시: **"설정 메뉴 적절한 위치에 추가해서 관리자모드 관리할 수 있게 코딩해"**

- 설정은 업무 메뉴와 섞지 않는다. 데스크톱 사이드바 아래 고정 구역에서 **조직도 다음·
  쿠팡 캘린더 위**에 두며, 담당자 업무센터 주소에서는 메뉴 자체를 숨긴다.
- 관리자 모드는 `localStorage`나 화면 변수로 켜지 않는다. `/api/auth/session`이 돌려준
  서버 서명 역할이 `admin`일 때만 사용 중으로 표시하고, 실행·정책·전체 원장 API는 기존
  서버 관리자 관문을 그대로 통과해야 한다.
- 설정 화면은 현재 권한·관리 범위·기기 인증 만료를 보여 주고, 관리자 PIN 변경·권한 다시
  확인·시스템 진단·조직도 접속 현황으로 이어진다. PIN 원문은 화면이나 저장소에 표시하지 않는다.
- 관리자 모드 종료는 **현재 브라우저의 HttpOnly 세션 쿠키만** 만료한다. 역할 버전을
  올려 다른 관리자·담당자 기기까지 로그아웃시키지 않으며, 오프라인 로그인 이력은 같이
  지워 서버가 없는 상태에서 종료한 관리자 화면이 다시 열리지 않게 한다.
- 담당자 주소에서 설정 URL을 억지로 열어도 업무센터로 되돌린다. 클라이언트 화면 숨김은
  편의 장치일 뿐 권한 경계는 서버다.
- 검증 `[279]`.


## ★ 워크플로우 도면은 Ctrl+Z로 방금 작업을 취소한다 (2026-08-14 지시)

사용자 지시: **"Ctrl+z 키도 먹게 해줘"**

- 워크플로우 도면 화면에서 `Ctrl+Z`(Mac은 `Cmd+Z`)를 누르면 화면의 **방금 취소**와
  같은 이력에서 노드 추가·이동·화살표·손그림·삭제를 한 단계 되돌린다.
- 단계 이름·담당 입력칸, textarea, select, contenteditable에 커서가 있으면 가로채지
  않는다. 그 자리에서는 브라우저 기본 글자 실행취소가 우선이다.
- 워크플로우 밖, 대화창 위에서는 도면을 되돌리지 않는다. 단축키가 다른 업무 화면이나
  입력 내용을 건드리면 안 된다.
- 검증 `[280]`.


## ★ 완료된 정기점검은 예정에서 빼고 A4 폭을 채운다 (2026-08-14 지시)

사용자 지시: **"점검완료된 예정건에서 완료된 건은 빼, 그리고 좌우 여백 1cm, 1cm 만
남기고 꽉 채워서 표시하는 알고리즘 구현해"**

- 정기점검 예정은 류지영 스케줄 원본, 완료는 관리대장 원장에서 오므로 원천업무ID가
  달라도 같은 점검일 수 있다. 프로젝트NO가 같고 완료일이 예정일 이상이면 이미 치른
  회차이므로 `pm_plan`에서 내린다. 프로젝트NO가 양쪽 모두 없을 때만 정규화한 캠프명으로
  보조 대조한다.
- 완료일이 예정일보다 앞선 것은 지난 회차다. 그 뒤에 잡힌 다음 점검 예정까지 지우면
  현장 방문이 사라지므로 반드시 남긴다.
- 서버 응답에서 먼저 상호배타 처리하고, 재시작 전 옛 응답·오프라인 캐시를 쓰는 대표
  캡처 길목에서도 같은 조건으로 한 번 더 막는다.
- 캡처의 좌우 여백은 A4 폭 21cm의 각각 1cm로 계산한다. 상세 자료는 최대 단 수를
  고정하지 않고, 같은 글자 크기에서 내용이 들어가는 가장 적은 단 수를 골라 남은 19cm
  전체 폭을 쓴다. 적은 자료가 왼쪽에만 몰리고 오른쪽이 비면 안 된다.
- 검증 `[281]`.


## ★ 설정 메뉴 아이콘은 톱니바퀴다 (2026-08-14 지시)

사용자 지시: **"설정 아이콘은 톱니바퀴로 변경"**

- 설정 메뉴는 톱니바퀴 아이콘으로 표시한다. 열쇠는 로그인·PIN·권한을 뜻하므로 설정
  전체 메뉴에 쓰면 기능 범위를 잘못 전달한다.
- 반응형 메뉴는 같은 `settings-nav` 요소와 아이콘 정본을 공유한다. 화면 폭마다 별도
  아이콘을 두어 설정 메뉴가 표시되는 화면끼리 모양이 달라지게 하지 않는다.
- 검증 `[282]`.


## ★ 대표 캘린더 각 페이지 하단에 설명 문구를 넣지 않는다 (2026-08-14 지시)

사용자 지시: **"각 페이지 하단에 이런 문구 넣지마"**

- 대표 캘린더 이미지 1·2·3쪽 하단에는 `1/3 · …`, `2/3 · …`, `3/3 · …` 형식의
  페이지 번호·설명·제외 건수 문구를 넣지 않는다.
- 세 페이지 구분, 페이지별 제목, 캘린더·대표 판단·상세 근거 본문은 그대로 유지한다.
  하단 안내를 없앤다는 지시를 페이지 수나 상세 내용을 줄이라는 뜻으로 확대하지 않는다.
- 예측 제외와 완료 중복 제거는 데이터 알고리즘에서 계속 적용하되, 그 내부 처리 설명을
  이미지 하단에 다시 출력하지 않는다.
- 검증 `[283]`.


## ★ 경보는 '마지막 발생'만 보면 안 된다 — **고친 시각**을 같이 본다 (2026-08-16, 분담판 [108])

- 인계 '먼저 처리할 것' 17건 중 **넷**이 같은 모양이었다: 회귀 58건(저장 버전 없음) ·
  새 오류 26건(`fmtDateTime`) · 6건·4건(`flowVisual*`). **넷 다 마지막 발생이 고침
  커밋보다 앞**인데 `error_book` 이 고친 시각을 안 봐서 매일 P1 으로 올라왔다.
  경보가 대부분 가짜면 **나머지도 아무도 안 본다**(`[170]`).
- ★ **고치기 전에 시각대부터 어긋나 있었다.** 실측 ux 42,339행 중 **42,322행이
  UTC(`…Z`)** 다(`ux_add` 가 화면이 보낸 `ts` 를 그대로 넣는다). 보관본은
  `datetime.now()`(이 PC 시각)이라 **둘을 문자열로 그냥 비교**해 왔고, `_ago` 가
  '며칠 전'까지만 말했으므로 9시간이 어긋난 채로도 티가 안 났다.
  · 그런데 '고친 뒤에도 났나'를 물으려면 분 단위가 맞아야 한다. 안 맞추면 고침 직후
    **9시간이 통째로 눈먼 창**이 되어 **고친 뒤에 난 오류를 '고쳐졌다'고 말한다** —
    `[169]` 중에서도 제일 나쁜 쪽(안심시키는 거짓)이다.
  · 실측이 그 크기를 보여 준다: `fmtDateTime` 마지막 `11:41:10Z` = 이 PC 시각
    **20:41**, 고침 커밋 **20:51** — 실제 간격은 9시간이 아니라 **10분**이었다.
    `flowVisual*` 은 **2분**. 분담판 [108] 에 '9시간 뒤'라 적혀 있던 것이 이 착시다.
  · 이제 `error_book.to_local()` 한 곳이 모든 시각을 이 PC 시계로 모은다.
- **고친 때는 지어내지 않는다 — 저장소가 말하게 한다**(`fix_evidence`). 근거는 둘뿐이고
  둘 다 실재하는 글자다: ① 사전의 `막음 [NNN]` → **검증 `def tNNN` 이 들어온 커밋**
  ② 문구가 `X is not defined` → **그 이름의 정의가 들어온 커밋**(아직 정의가 없으면
  근거 없음 = 안 고쳐진 것이다). **근거가 없으면 경보는 그 자리에 그대로 남는다.**
  · ⚠ 검증 함수 이름은 `def t249(` 가 아니라 `def t249_entry_save_never_silent():` 다.
    괄호까지 붙여 찾으면 **한 건도 안 걸리면서 오류도 안 난다**(`[165]` 모양) —
    만들면서 그대로 걸렸고 58건이 '근거 없음'으로 남았다.
- ★ **'안 났다'와 '아무도 그 화면을 안 열었다'는 다른 말이다**(`[169]`). 그래서 고침
  뒤 화면을 연 횟수(ux `view`)를 같이 세고, **0 이면 경보에서 내리되 '못 본 것'에
  올린다** — 아무도 안 쓴 화면이 '고쳐진 화면'으로 둔갑하지 않게.
- 실측(고친 뒤): 회귀 **1→0** · 새 오류 **9→6** · 고친 뒤 재발없음 **4**
  (그 뒤 방문 734·33·179·179회) · 못 본 것 0. **읽기 전용은 그대로다.**
- ⚠ **같은 구멍이 `schedule_watch` 에도 있다** — 분담판 `[110]`. P0 로 올라온
  `CSOS_BrowserChain` exit 1 은 마지막 실행 **08-15 12:00** 인데 그 exit 1 을 없앤
  고침은 **08-16 02:04** 커밋이다(14시간 뒤). 다음 실행 12:00 이 답한다. 거기서는
  '고쳐졌다'가 아니라 **'고친 뒤 아직 안 돌았다'** 라고 적어야 한다.
- 검증 `[288]`.

## ★ 꺼진 회차는 **실패하지 않는다** — 그래서 아무도 모른다 (2026-08-16, 분담판 [110])

- `schedule_watch` 는 결과 코드로 갈래를 갈랐다(성공·죽음·밀림·안돎). 그런데 **사람이
  끈 회차**는 결과를 아예 안 남긴다 — 실패도 아니고 도는 중도 아니다. 실측 2026-08-16:
  **다섯 개가 8/15 부터 꺼져 있었고 어느 화면에도 안 떴다.** 그중 하나가
  `쿠팡업무_일일자동대조`(09:50)라, 접수취소·객관완료·청구상태·대조·오기입·사실대조가
  하루 넘게 안 돌면서 **아무 경보도 없었다.**
- 이제 `notices()` 가 **알림**으로 올린다(경보 아님). 되살리지 않는다 — 끄는 것은 사람의
  결정일 수 있다(2026-08-14 "팝업창 좀 안 뜨게 해" 다음 날 꺼졌다). 일부러 끈 것이면
  `reports/스케줄러_꺼둔회차.txt` 에 이름을 적으면 내려간다. **그 파일을 못 읽으면
  '이상 없음'이 아니라 '확인 못 함'** 이다(`[169]`).
- ★ **경보와 알림을 같은 통에 담지 않는다.** `system_audit` 은 `schedule_watch` 의
  경보가 **하나라도 있으면 무조건 P0** 로 올린다. 같이 담으면 '꺼져 있다'가 매일 P0 가
  되어 진짜 P0 를 덮는다(`[170]`).
- ★ **실패한 코드가 이미 안 도는 경우를 가른다** — `고침대기`. 판정은 **그 작업이 부르는
  파일의 mtime 대 마지막 실행 시각**이다(`task_scripts` 가 `.bat`·`.vbs` 를 한 겹
  따라가 `.py` 를 찾는다). 실측: P0 `CSOS_BrowserChain` exit 1 은 8/15 12:00 인데 그
  exit 1 을 없앤 커밋은 8/16 02:04 이었다 — **14시간 뒤**다. 그래도 '고쳐졌다'고 적지
  않는다. **다음 예정이 답한다**(그날 12:00 이 실제로 exit 0 이었다).
- `고침대기` 를 `DEAD` 에 넣지 말 것 — 넣는 순간 P0 로 되살아난다. 검증 `[288]`.
- ★ **"경보 0" 을 "모두 정상"이라 적지 않는다.** 리포트가 그렇게 적고 있었다 —
  꺼진 회차는 알림이라 경보에 안 들어오는데, 그 문장은 **다섯이 꺼진 날에도**
  "회차 13개가 모두 정상이거나 도는 중"이라고 확언했다. 콘솔도 `걸린 것 없음` 만
  찍었다. 이제 둘 다 알림 건수를 같이 말한다(`[169]`).

### 같은 회차를 **두 작업이** 부르면 한쪽은 늘 실패한다 (2026-08-16 실측)
- 꺼진 회차 다섯을 다시 켜고 돌려 보니 `CSOS_유수비_대표보고_자동준비` 가 **exit 1**
  로 끝났다. 액션을 대 보니 `쿠팡업무_일일자동대조` 와 **같은 `daily_run.bat`** 이다.
  한쪽이 잠금을 쥐면 다른 쪽은 늘 실패하고, **그 실패는 '회차가 고장 났다'로 읽힌다.**
- 2026-08-07 에 `쿠팡업무_원장일괄반영_15시` 를 지운 것이 정확히 이 모양이었다
  (매일 `결과: 1`). **그때는 사람이 손으로 찾아냈다** — 이제 감시자가 `겹침` 알림으로
  잡는다(열쇠는 실행기 exe **와 인자**다. exe 만 보면 `wscript.exe` 를 쓰는 회차가
  전부 겹친 것이 된다).
- **지우지 않는다** — 어느 쪽을 남길지는 사람의 판단이다. 대표보고 내용(`ceo_events.py
  --sync`)은 이미 09:50 daily_run 의 한 단계라, 별도 작업은 같은 회차를 한 번 더
  부르는 것뿐으로 보인다. 그래도 단정하지 않고 근거를 나란히 놓는다.

### 살아 있는 예약 작업의 실행기도 본다 (분담판 [107])
- **`[248]` 은 설치본을, `[272]` 는 소스를 본다.** 그런데 설치본을 고쳐도 **이미 등록된
  작업은 그대로**고, 살아 있는 작업만 고치면 기계를 새로 만들 때 되살아난다.
  **둘 다 봐야 한다** — 창이 뜨는 것은 지금 등록돼 있는 그 실행기다.
- `python tools/window_audit.py --live`. 낱말(`창뜸`·`조용`·`모름`)은
  `window_audit.exe_verdict` 한 곳이 정하고, **회차가 이미 물어서 써 둔 것**을 읽는다
  (여기서 스케줄러를 다시 묻지 않는다 · `[168]`). 실측 2026-08-16 **0곳**.
- **못 읽으면 '0곳'이 아니라 이유를 돌려준다** — 옛 기록·조회 실패를 '전부 조용'으로
  세면 계기 자신이 눈이 먼다. `pythonw`·`wscript` 만 조용이고 **`cscript` 는 콘솔이라
  창이 뜬다**. 모르는 실행기는 `모름` 으로 남긴다.
- 검증은 **기계 상태에 기대지 않는다** — 낱말 판정과 '못 읽음' 갈래만 잰다(`[272]` ⑥).

## ★ 실패는 **왜인지 말한다** — 그러나 비밀값은 한 글자도 안 나간다 (2026-08-16, 분담판 [109])

- `reports/erp_api_latest.json` 에 남는 것이 `po_list API가 오류 응답을 돌려줬습니다`
  한 줄뿐이었다. 봉투 안에는 코드도 문구도 들어 있었는데 **판정 코드가 그것을 버렸다.**
  화면(`system_audit` P1)은 실패를 띄우는데 **고칠 자리를 아무도 못 찾는** 자리였다.
- 열어 보니 원인이 짐작과 달랐다 — `EXP00001 · "데이터 입력에 오류가 있습니다"`.
  IP 도 인증도 method 도 아니고 **요청 본문**이다(method 는 살아 있다 — 404 가 아니라
  구조화된 오류를 준다). 그래서 갈래를 넷으로 둔다: IP 미등록 · 인증 만료 ·
  조회 API 아님 · **요청 본문 형식**.
- ★ **'왜'를 넓히는 것과 '무엇을 지우나'는 같이 온다.** 조회 URL 에는 `?SESSION_ID=…`
  가 붙어 있어서 **HTTP 오류 한 번이면 세션ID 가 리포트에 박히고 앱 화면에 뜬다**(P1
  상세 220자). 지우는 자리는 `ecount_client.mask_secrets` **한 곳**이고, 응답·URL 을
  담는 raise 는 전부 `self.safe()` 를 거친다.
  · **가리려던 것은 이름이 아니라 값이다** — 첫 판이 `GetPurchasesOrderList`(21자)를
    통째로 가려 **어느 method 가 죽었는지가 사라졌다.** 이제 긴 토큰 중 **숫자가 섞인
    것만** 지운다.
- ★ **잘못 지목하지 않는 것이 잡는 것보다 어렵다**(`[172]`). 첫 판이 맨몸 `"ip"` 를
  표시로 써서 `recipient`·`description`·`multiple` 이 전부 'IP 미등록'이 될 참이었다 —
  그 조치는 "IP 를 등록하세요"라, 사람이 **멀쩡한 설정을 고치러 간다.** 이제 표시는
  낱말 경계를 가진 정규식이고, **후보가 유일할 때만** 지목한다. 둘에 걸리면 그것은
  '둘 다'가 아니라 **'모른다'** 이며, 그때는 원문을 그대로 보여 준다.
- ★ **한 갈래가 죽어도 다른 갈래의 성공을 지우지 않는다.** 예전에는 `po_list` 가 죽으면
  그대로 올려서, **품목 7,329건이 멀쩡히 들어왔다는 사실이 통째로 사라지고** `ok:false`
  한 줄만 남았다. 이제 endpoint 마다 갈라 적고 `살아남은것` 을 남긴다.
- ★ **조치는 갈래마다 다르다.** 'IP 미등록'에 `--force` 를 권하면 사람이 같은 실패를 한 번
  더 부르고 원인은 그대로 남는다. `system_audit` 이 수집기가 고른 조치를 그대로 쓴다.
- ⚠ **짐작으로 바꿔 가며 반복 호출하지 않는다** — 트래픽 제한이 ERP 전체 차단으로 번진다
  (절대규칙). 지금 남은 사람 몫은 `config/ecount_config.json` 의 `endpoints.po_list`
  요청 칸 이름을 이카운트 API 문서와 맞추는 것 하나다.
- 검증 `[289]`.

## ★ 막혔다는 말은 **어느 쪽으로 막혔는지**까지 해야 한다 (2026-08-16)

- 앱 오류 기록에 남은 두 건이 같은 모양이었다.
- **`runTask · evidence_sync:관리자 전용 기능입니다`** — `_require_admin` 이 담당자든
  세션이 없는 관리자든 **같은 문구**를 줬다. 관리자 본인이 그 문구를 보면 권한 문제가
  아니라 **앱이 고장 났다고 읽고**, 고칠 길(다시 인증)이 화면 어디에도 없다.
  이제 `ADMIN_ONLY`(담당자 화면) 와 `ADMIN_AUTH_NEEDED`(인증 필요)로 갈라 말한다.
  · ⚠ **상태 코드는 403 그대로 둔다.** 401 은 이 앱에서 **PIN 게이트의 코드**라,
    화면이 401 을 보면 PIN 을 지우고 잠금 화면을 띄운다. 권한 거절에 401 을 쓰면
    문구를 고치려다 **담당자를 잠금 화면으로 튕긴다**(`[172]`).
  · ⚠ 그리고 **짐작한 근거를 코드 주석에 적지 말 것** — 만들면서 "`/api/system-audit`
    이 이 갈래를 탄다"고 적었다가 실측으로 아니었다(그 401 은 PIN 게이트가 준
    것이고 본문이 비어 있었다). 확인 없이 적은 근거는 다음 사람이 없는 길을 판다.
- **`window · (사유 없음)`** — 계기가 무엇이 잘못됐는지 못 적은 기록이다. 게다가
  **리소스 로드 실패(img·script·link)는 버블하지 않아** capture 없이는 전역 리스너에
  **아예 안 온다** — 아이콘·스크립트가 404 여도 어느 화면에도 안 뜬다(`[169]`).
  이제 `capture:true` 로 듣고 `resource` 갈래로 **무엇이 안 불러와졌는지**를 적는다.
  실측: 없는 이미지를 넣자 `img 로드 실패 · …/없는파일_….png` 로 기록됐다(전에는 0건).
- 검증 `[290]`.

## ★ 크레딧이 떨어져도 다른 계정이 이어받는다 — `takeover.py` (2026-08-17 지시)

사용자 지시: **"다른 계정 다른 세션에서도 진행할 수 있게 알고리즘 추가해서 관리해,
언제든지 이 세션의 크레딧이 떨어지면 다른 계정에서 작업할 수 있게 준비"**

- **빠져 있던 것은 '이어받는 기능'이 아니다** — 그건 이미 있다(`session_handoff --adopt` ·
  `session_boundary --start` · `ai_claim` · `worksplit`). 빠진 것은 **끊겼다는 사실을
  아는 것**이었다. 세션이 끝나는 길 셋(compact · `/clear` · 종료)은 전부 훅이 받아
  인계를 남기는데 **크레딧 소진에는 훅이 없다.** 창은 그대로 뜬 채 pid 도 살아 있고
  **대화기록만 그 자리에서 멈춘다.** 그러면 `ai_claim._is_dead` 는 '살아 있다'고
  답해 점유가 안 풀리고, 인계 문서는 그 창을 **일하는 창**으로 세어 다른 계정이
  '남의 일'로 알고 비켜선다. 아무 화면도 멈췄다고 말하지 않는다(`[169]`).
- `python takeover.py`(사람) · 회차는 워치독 30분의 `watch_takeover` 단계.
  카드는 `reports/이어받기.md`, 인계 '먼저 처리할 것'에도 올라간다.
  **다른 계정의 첫 명령은 여전히 한 줄이다** — `python ecount/session_handoff.py --adopt`.
- ★ **'조용함'만으로 부르면 경보가 전부가 된다.** 첫 판이 **25건**을 쏟았는데 대부분
  몇 주 전에 깨끗이 닫힌 창이었다 — **대화기록 파일은 영원히 남는다.** 이어받을 것이
  있다는 근거는 하나다: **뭔가를 남겼나**(자원을 쥐었거나 맡은 일이 진행 중).
  **25 → 2건**이 됐고, 그 둘이 [89]·[90] — 이틀 넘게 **주인 없이 서 있던** 항목이다.
- ★ **뺏지 않는다. 읽기 전용이다.** 회수는 기존 규칙 몫이다(pid 사망 → `ai_claim`,
  8시간 자국 없음 → `lanes` `[239]`). 살아 있는 옆 창의 점유를 빼앗으면 두 창이 같은
  파일에서 만난다(사고 #36) — **못 뺏는 것보다 나쁘다.**
- ★ **새 판단을 만들지 않았다**(`[162]`): 살아 있음은 `session_wrapup.stem_ages`
  (`live_stems` 가 이제 이것을 자른다), 이름공간은 `ai_claim.sid_of`, pid 생사는
  `ai_claim._is_dead`, 맡은 일은 `worksplit`.
- ★ **다른 계정이 볼 수 있는 상태인가도 같이 잰다.** 폰·웹·다른 PC 는 **밀린 것만**
  본다 — 미푸시는 '나중에 밀면 되는 것'이 아니라 **이어받기가 불가능한 상태**다.
  미커밋은 그 창 안에만 있으므로 알리되 **자동으로 커밋하지 않는다**(남이 반쯤
  고쳐 둔 것일 수 있다, `[104]`).
- ⚠ **인계 문서는 여기서 다시 재지 않는다** — `st` 가 준 것만 읽는다(`schedule_watch`
  와 같은 규칙). 살아 있는 기계를 직접 물었더니 **합성 상태로 부르는 검증이 통째로
  막혔다**(t111 실측: "밀리지 않았는데 막았다").
- 검증 `[291]`.

## ★ 문구 없는 500 · 그리고 **★신규는 고침증거를 못 받는다** (2026-08-17)

- 실측: `/api/flow · HTTP_ERROR:500` **15건인데 사유가 한 글자도 없었다.** 전역 가드
  (`handle_one_request`)가 `{"error": str(e)}` 만 실었는데 **`str(e)` 는 빌 수 있다** —
  `Exception()` 처럼 인자 없이 오른 예외가 그렇다. 그러면 화면 기록에 `HTTP_ERROR:500`
  **다섯 글자만** 남는다. 실패를 말하기는 하는데 **왜인지는 영영 알 수 없다** —
  `[109]`(ERP API 가 봉투를 버렸다)와 **같은 모양이 앱 서버에도 있었다.**
- 만드는 자리는 `app_server.error_reason()` **하나**다(가드는 부르기만 한다 · `[162]`).
  갈래 이름(`type(e).__name__`)과 **터진 자리**(마지막 프레임 `파일:줄`)는 절대 비지
  않으므로 그 둘을 먼저 세우고, 사람이 쓴 문구는 있으면 덧붙인다. **문구를 버리지도
  않는다** — 갈래만 남기면 그게 또 반쪽이다.
- ★ **`막음 [292]` 의 뜻은 "500 이 안 난다"가 아니라 "문구 없는 500 이 안 나간다"** 이다.
  그러므로 이 뒤에 500 이 또 나면 회귀가 맞고, 이번에는 **사유를 달고** 오므로 사람이
  고칠 자리를 찾을 수 있다. 막음의 주장은 **정확히 고친 것만큼**이어야 한다.

### ★ 여기가 `[288]` 의 빈 자리다 — 사전을 쓰는 일이 곧 증거를 세우는 일
- `[288]` 의 고침증거 근거 ①은 사전의 `막음 [NNN]` 을 찾는다. 그런데 **★신규는 정의상
  사전에 없다.** 그래서 **이미 고친 오류도 낱말을 달기 전까지는 영영 P1 로 남는다.**
  실측: `[290]` 이 8/16 에 고친 둘이 그 뒤로도 매일 '먼저 처리할 것'에 올라와 있었다.
  경보가 대부분 가짜면 나머지도 아무도 안 본다(`[170]`).
- 그러므로 **오류를 고쳤으면 사전에 낱말을 다는 것까지가 한 벌이다.** 리포트가 "규칙을
  `error_book.BOOK` 에 한 줄 더한다"고 적어 두는 것이 잔소리가 아니라 배선이다.
- 실측 ★신규 **6갈래 → 1갈래**(회귀 0 — 거짓 회귀도 안 났다).
- ★ **404 는 이 앱에서 '없는 주소'가 아니라 '화면과 서버 판이 어긋난 것'이다.** 실측
  `/api/staff/records` 404 4건이 전부 8/11 11:46~11:54 인데 그 길이 생긴 커밋은
  **같은 날 12:12** — **길이 생기기 18~26분 전**에 화면이 먼저 불렀다(`[156]` 과 같은
  자리). 짐작하지 말고 `git log -S "<그 길>"` 로 **저장소에 물어보면** 바로 나온다.
- ★ **낱말을 달 때 옆 갈래를 삼키지 않는다**(`[172]`): 500 규칙이 502·503 을 삼키면
  서버를 다시 띄울 때마다 '서버가 멈췄다'가 뜬다. **더 좁은 규칙이 먼저 온다** —
  관리자 인증 만료가 '권한 없음'으로 먼저 잡히면 **고칠 길(다시 인증)이 화면 어디에도
  없어진다**(분개장 판정이 '적요' 판정보다 먼저 오는 것과 같은 이유).
- ★ **`[197]` 의 재시도 소진 문구(`… 최신 자료를 불러오지 못했습니다`)에는 막음을
  안 붙인다.** 서버를 다시 띄울 때마다 나므로 붙이는 순간 매일 '회귀'가 되어 아무도
  안 본다. 이미 있던 '서버가 잠깐 끊김' 항목의 **세 번째 말투**로 넣었다 — 그 항목이
  제 주석에 "같은 오류가 두 말투로 온다"고 적어 둔 바로 그 자리다.
- ⚠ **아직 낱말을 못 단 하나는 그대로 ★신규로 남겨 뒀다**(`[169]` — 못 푼 것을 푼 것처럼
  적지 않는다): `window · (사유 없음)` 1건. 상세가 **비어 있어**(`지문` 이 `window ·`)
  조각 규칙으로 잡으려면 `window` 를 **통째로** 삼켜야 하는데, 그러면
  `X is not defined` 들이 근거 ②(정의가 들어온 커밋)를 못 타고 '아는 오류'로 내려가
  **고침 근거를 잃는다**(실측 `fmtDateTime`·`flowVisual*` 넷이 그 길로 내려온다).
  **낱말 하나를 얻자고 넷의 근거를 버리지 않는다** — `[290]` 이 원인은 이미 막았다.
- 검증 `[292]`.

## ★ 겹치면 조율한다 — **양보는 주장이고, 주장은 나중에 검사한다** (2026-08-17 지시)

사용자 지시: **"다른 앱의 작업과 겹치면 서로 조율해서 진행하는 알고리즘 구현해"**

- **겹침의 결과가 세 얼굴이었다.** 실측: `CSOS_유수비_대표보고_자동준비` 와
  `쿠팡업무_일일자동대조` 가 **같은 초(09:50:02)에 같은 명령**(`daily_run.bat`)으로 뜬다.
  그런데 겹쳤을 때 남는 자국이 회차마다 달랐다 — `daily_run` 은 한 줄 찍고 **exit 0 ·
  자국 없음**(스케줄러는 '성공'), `automation_pipeline` 은 부르는 쪽이 안 적으면 사라지고,
  `noon_run` 만 제대로 '양보'로 적었다(**그 회차 안에서만**). 그래서 밖에서 보면
  **겹쳐서 안 돈 것과 정말 다 한 것이 구별되지 않는다**(`[169]`).
- ★ **그 혼동에 내가 그대로 걸렸다.** `대표보고 exit 1` 을 '겹침'으로 읽고 "작업을
  지울까요"까지 갔는데, 로그를 열어 보니 원인은 **합성검증 실패**였다 —
  `sys.exit("문구")` 는 문자열을 줘도 **exit 1** 이다. 겹침에 이름이 없으면 사람도 AI 도
  엉뚱한 것을 고치러 간다(`[172]`).
- **여기서 새 정책을 만들지 않았다**(`[162]`). 무엇과 부딪히면 어떻게 할지는 **회차가
  안다** — `noon_run` 은 제 단계와 창을, `daily_run` 은 제 락을 안다. `coordinate.py` 가
  맡는 것은 **이름과 기록** 하나다: 양보를 성공으로도 실패로도 적지 않고 **제 이름**으로
  적고, 거듭되는지 세고, **주장이 지켜졌는지 검사한다.**

### 양보는 주장이다
- 양보는 "**내가 안 해도 저쪽이 그 일을 한다**"는 주장이다. 주인마저 못 끝내면 그 일은
  **아무도 안 한 것**이 되는데, 오늘은 아무 화면에도 안 뜬다 — 양보는 실패가 아니라서
  경보가 없고 주인의 죽음은 제 이름으로 따로 적히기 때문이다. `coordinate.audit()` 이
  "양보한 뒤 그 주인이 실제로 끝냈나"를 되묻는다. 못 끝냈으면 **헛양보**다.
- ★ **가장 위험한 방향은 없는 주인에게 양보하는 것**이다(`[172]`) — 일은 영영 안 되고
  기록만 '정상'으로 남는다. 그래서 `record_yield` 는 **주인 이름을 반드시 받고**,
  모르면 '검사할 수 없는 양보'로 따로 센다(조용히 정상으로 세지 않는다).
- ★ **'아직 하는 중'을 '끝내지 못했다'로 읽지 않는다 — 첫 실행이 곧바로 거짓 경보를 냈다.**
  `정오회차` 가 11:27 에 `일일대조` 에게 양보했는데 그 회차는 **그때도 돌고 있었다**
  (실측 292분). 이대로면 **양보할 때마다** 경보가 뜬다 — 만들자마자 `[170]` 을 밟은
  자리다. 그래서 문 둘: ① 주인이 지금 락을 쥐고 있으면 **판정 자체를 미룬다**
  ② 실측 회차 길이보다 넉넉히(8시간) 지난 뒤에야 묻는다. **감시자를 만들면 그 감시자가
  첫날 무엇을 말하는지 실제로 돌려 본다** — 코드가 맞아도 기준이 틀릴 수 있다.
- **매일 양보만 하는 작업은 없는 작업과 같다** — `noon_run` 이 제 안에서만 알던 것을
  모든 작업으로 넓혔다(`굶주림`: 연속 3회 양보 + 한 번도 안 돎 / 24시간 넘게 못 돎).
  **지켜진 양보는 조용하다** — 정상까지 경보하면 아무도 안 본다(`[170]`).
- ⚠ **이름이 어긋나면 한 건도 안 걸린다.** 실측으로 `noon_run` 은 `일일대조(09:50)`,
  `daily_run` 은 `일일대조` 로 적고 있었다 — 그대로 뒀으면 **모든 양보가 영영 '헛양보'**
  로 보였다(오류는 안 난다, `[165]` 모양). 이름은 **`coordinate.LOCKS` 한 곳**에서 온다 —
  **락 파일이 곧 그 일의 정체**다.
- ⚠ **양보만 조율 표에 넘긴다.** `noon_run` 의 '창밖'·'완주'까지 넘기면 창 밖의 부름
  다섯 번이 매일 '연속 양보'로 세어져 **굶주림 경보가 거짓**이 된다.
- **읽기 전용이다** — 회차를 다시 띄우지 않고(지금 도는 중일 수 있다) 스케줄러도 안 고친다.
  자국을 **못 적으면 못 적었다고 말한다**(다음 회차가 '확인 못 함'으로 올린다).
- 회차는 워치독 30분의 `watch_coordination`(인계보다 **먼저**) · 사람은
  `python coordinate.py --print` · 결과는 `reports/작업_조율.md`.
- ★ **양보만 넘기고 완주를 안 넘기면 연속이 영원히 안 풀린다** (2026-08-18 실측).
  바로 위 규칙("양보만 조율 표에 넘긴다")은 옳은데 — **완주를 넘기는 자리를 아무도
  안 만들었다.** 그래서 정오회차가 12:20 에 4단계를 **정상 완주하고도** 표는
  `마지막 돎: 없음` · `27회 연속 양보` · **`한 번도 돈 적이 없다`** 라고 확언했고,
  굶주림 경보 자체가 거짓이 됐다(`[170]` — 거짓 경보는 진짜 경보를 덮는다).
  한쪽만 적는 표는 **시간이 갈수록 더 확신에 차서 틀린다.**
  · 이제 `noon_run.mark_coord_run` 이 **그날 한 번** 남긴다(마커 `coord_run_date`).
  · **근거는 마커의 `done_date` 뿐**이라 안 돈 날에는 아무것도 안 적는다(지어내지 않는다).
  · **못 적으면 표식을 안 남긴다** — 다음 부름이 그대로 이어받는다(`[169]`).
  · 갈래를 안 가리고 물러날 때도 잇는다 — **창밖이든 완주든 '오늘 돌았다'는 같은 사실**이다.
  · 단계 몇 개가 실패해도 갈래는 **완주**다. 회차는 실제로 돌았다 — 그것을 '안 돌았다'로
    접으면 굶주림 경보가 다시 거짓이 된다.
  · 실측 연속양보 **27 → 0** · `마지막 돎: 2026-08-18 14:58 완주`.
- ⚠ **이 계약은 글자 검사로 못 잡는다.** ⑩('양보만 넘긴다')만 있으면 완주를 넘기는
  자리가 **통째로 비어도 통과한다** — 실제로 그랬다. `[293]`-⑪ 이 가짜 `coordinate`
  로 갈아 끼워 **실행해서** 잰다(실측 증거 파일은 한 글자도 안 건드린다 · `[247]`).
  계기 자신도 시험했다 — 완주 기록을 없애자 그 자리를 그대로 짚었다(`[272]`).
- 검증 `[293]`.

### ★ 그 감시자를 **검증이 눈멀게 하고 있었다** (2026-08-19 실사고)

- 조율 표가 매일 `주인을 모르는 양보 6건` 만 말하길래 그것을 보러 갔다가 더 나쁜 것이
  나왔다: `reports/작업_조율.json` 의 **일일대조 자국 40줄 중 38줄이 검증이 쓴 가짜**였다.
  `t194` 가 진짜 `daily_run.main()` 을 부르면서 `PROGRESS` 만 임시로 돌리고 **조율은
  안 돌려서**, 검증 한 번에 네 줄(`완주`·`실패`·`synthetic failure`·`중단`)이 실측
  증거에 박혔다. 화면은 `마지막 돎 10:39 실패` 라 말했는데 그때 돈 것은 daily_run 이
  아니라 **내 검증**이었다.
- ★ **그림이 나쁜 자리는 여기다.** `audit()` 은 "양보했는데 주인이 끝냈나"를 주인의
  **완주 줄**로 묻는다. 가짜 완주가 섞이면 **진짜 헛양보가 지워진다** — `[293]` 이
  잡으려고 만든 바로 그 사고를, 그것을 지키는 검증이 만들고 있었다. 오류도 안 나고
  표도 그럴듯해서 아무도 안 봤다(`[169]`).
- **못 돌린 이유는 기본인자였다.** `def _load(path=MARK)` 는 **def 시각에 묶인다** —
  `coordinate.MARK` 를 임시 경로로 바꿔도 진짜 파일에 그대로 쓴다. 이제 `path=None`
  으로 받아 **부를 때** `MARK` 를 읽는다. 되돌릴 수 없는 모듈은 격리할 수도 없다.
- ★ **계기 자신을 시험한다**(`[272]`). `t194` 가 되돌리기 **전에** 진짜 파일의 mtime
  이 그대로인지 재고, 임시 경로에는 자국이 **남았는지**도 잰다 — 안 재면 "안 썼다"와
  "기록이 죽었다"가 구별되지 않는다. 그리고 기본인자로 되돌아가면 걸리게 따로 잰다.
- **오염된 38줄은 걷어내되 조용히 지우지 않았다**(`[169]`) — 백업
  `reports/작업_조율.검증오염백업_20260819.json` · 지운 근거는 '같은 초에 여러 줄이고
  그 낱말이 전부 시험 낱말'뿐이다. 모르는 묶음은 **안 지우고 숫자로 말한다**(`[172]`).
- ⚠ **`[247]` 이 되보고 파일에서 배운 것과 같은 규칙인데 자리가 달랐다.** 그러므로
  **진짜 회차 함수(`main()`)를 검증에서 부를 때는 그 회차가 쓰는 파일을 전부 세어
  본다** — 하나만 돌리면 나머지는 조용히 실측에 박힌다.

## ★ 파서가 **0건**으로 읽은 원본은 '없는 것'이 아니다 (2026-08-14 실사고 · 2026-08-17)

형님: **"오늘 오전 유수비가 이야기한거 반영이 하나도 안되었어"**(8/14 16:37 캡처 첨부)

- **그 지시는 이미 반영돼 있었다** — 형님이 보신 이미지는 고치기 **전**에 만든 것이고,
  그 캡처를 만든 코드는 지금 저장소에 없다(`A4 한 장`·`종류별 일정`·`전체 99건을 모두
  실었습니다` 세 문구가 `git grep` 0건). 고칠 것이 없는 자리를 고치면 안 된다(`[172]`).
- **그런데 자동 경로는 한 글자도 안 들어갔다.** 그날 원본
  `KakaoTalk_Longtxt_20260814_1116_36_104.txt` 는 카톡 내보내기가 아니라 **통화 녹취**
  (STT)다 — 첫 줄이 `발화자 1 (00:00)` 이고 **`유수비` 문자열이 0회**, 시각은 경과
  시간이라 `kakao_extract.parse_export()` 가 **0건**을 돌려준다. `ceo_events` 도 0건.
  **오류는 한 줄도 안 난다** — 파일도 있고 흡수도 됐고 회차도 성공이다.
  즉 그날 대표 지시가 반영된 유일한 이유는 **그날 세션이 눈으로 읽고 손으로 옮겼기**
  때문이고, 기계로는 앞으로도 0건이다(`[169]` — 없는 것과 안 본 것이 구별되지 않는다).
- 이제 `ceo_events.extract()` 가 **파일마다 파싱 건수를 세고** 0건이면 `못읽은원본` 에
  갈래와 함께 담는다(`unreadable_kind` — 알아본 것만 이름을 주고 나머지는 `모름`).
  리포트 **맨 위**에 뜨고, `session_handoff.ceo_unreadable()` 이 그것을 읽어 인계
  **'먼저 처리할 것'** 에 올린다.
- ★ **세는 것은 대표 메시지가 아니라 파싱된 전체 메시지다.** 대표 메시지로 세면 대표가
  말한 적 없는 멀쩡한 방이 전부 경보가 되어 아무도 안 본다(`[170]`). 실측 경보 비율
  **원본 51개 중 1개** — 노이즈가 아니다. 이 비율이 커지면 기준부터 다시 본다.
- ★ **녹취 본문을 자동으로 '대표 지시'로 만들지 않는다**(`[172]`). 파일 어디에도 누가
  대표인지 적혀 있지 않고(`발화자 3` 이 지시하고 `발화자 2` 가 "예 대표님" 이라 답하니
  3번으로 **보일 뿐**이다) STT 오인식이 잦다(실측 `돌발`→`뻥뚱` · `정기점검`→`전기 점검` ·
  `류지영`→`류재영`). 잘못 뽑아 박으면 **없는 대표 지시가 캡처에 뜬다** — 못 잡는 것보다
  나쁘다. 그러므로 **"사람이 읽어야 함"까지만 말하고 멈춘다.**
- ★ **인계까지 잇는 것이 반쪽의 나머지다.** 만들기 전 실측으로 `ceo_events` 를 읽는
  코드가 프로젝트에 **한 줄도 없었다**(`session_handoff`·`system_audit` 둘 다 0건).
  리포트에만 적으면 **아무도 안 본다** — 그것이 이 사고의 모양 그대로다.
- ⚠ 키가 **없는 것**(옛 리포트 — 회차가 아직 안 돌았다)과 **빈 목록**(다 읽었다)은
  다른 말이다(`[247]`). 뭉치면 한쪽이 조용히 사라진다.
### 폰 사본은 대표 캡처를 **못 만든다** — 안 옮긴 것이 아니라 자료가 없다 (2026-08-17)

- 먼저 **내가 이 자리에 잘못 적었던 것부터 고친다**: "폰 고정 배포본의 캘린더는
  **옛 모양**"이라고 적어 뒀는데 실측으로 틀렸다. 옛 모양이 아니라 **대표 캡처가
  아예 없다** — `docs/cal.html` 은 캔버스가 한 줄도 없고(현장 기사용 공유 달력이다),
  `docs/app.html` 은 목록 시트 `이미지 저장`만 있고, `mobile_snapshot.py` 는 화면이
  아니라 **자료 수집기**라 애초에 상관이 없다. `A4 세 장` 이 안 걸렸다는 것만 보고
  '옛 버전이 거기 있다'로 읽은 것이다. **짐작한 근거를 문서에 적으면 다음 사람이
  없는 길을 판다**(이 절이 그 말로 시작하는데 같은 자리에서 또 걸렸다).
- **그리는 코드를 안 옮긴 것이 아니라 그릴 자료가 없다.** `cloud_publish.payload()`
  가 담는 일정 칸은 `날짜·시간·분류·캠프명·장소·제목·프로젝트NO·담당기사·예측`
  아홉뿐이고, 캡처가 쓰는 **지연 사유**(`_why_still_open`)와 **분기 진도**는 한 칸도
  안 실린다. 그러므로 450줄짜리 캔버스만 옮기면 **사유 칸이 빈 채로 멀쩡해 보이는
  3장**이 나온다 — 그것을 대표께 보내면 아무도 빈 것을 눈치채지 못한다.
  `[273]` 이 "자리가 모자라도 뒤 행을 조용히 버리지 않는다"로 막은 그 모양이며,
  **못 만드는 것보다 나쁘다**(`[172]`).
- 그래서 **말하고 보낸다** — `docs/app.html` 캘린더 탭이 *"대표 보고 A4 3장 캡처는
  실시간 PC 앱에서 만듭니다 — 이 사본에는 지연 사유·분기 진도가 실리지 않습니다"* 를
  적고, PC 가 닿으면 `실시간 PC 앱에서 만들기` 단추를 준다.
  · **판정은 `updateMode()` 한 곳**이다(`[162]`) — 캘린더가 제 손으로 `/api/ping` 을
    걸면 같은 질문에 두 답이 생기고, 달을 넘길 때마다 터널을 한 번 더 두드린다.
    그 함수가 확인한 주소만 `LIVE_URL` 에 담기고, 담기는 순간 캘린더를 다시 그린다
    (안 그리면 부팅 순서에 따라 **연결됐는데도 단추가 영영 안 나온다**).
  · 새 창으로 띄운다 — 이 창을 실시간 주소로 옮기지 않는다(`[240]`).
- ⚠ **`docs/cal.html` 에는 아무 말도 안 넣었다.** 그것은 단톡방에 뿌리는 **현장 기사용**
  달력이라 대표 캡처를 만들 사람이 아예 없다. 없는 문제를 만들지 않는다(`[172]`).
- ⚠ 같이 드러난 것: **카톡 원본이 3일 밀렸다**(최신 2026-08-14 11:16, 오늘 8/17).
  수집은 'CSOS 리서치 및 자료 수집' 세션 몫이므로 여기서 긁지 않는다.
- 검증 `[294]`.

## ★ 캠프 전화번호는 ERP 에 없다 — 원천은 **밴드 접수 글**이다 (2026-08-18 지시)

유수비 대표(카톡 07:33): **"쿠팡 전국정기점검 캠프와 담당자리스트 오늘중으로 주세요"**
형님: **"카테고리에 전국쿠팡캠프 추가해서 정기점검 캠프명과 캠프 담당자 이름 전화번호
목록 볼 수 있게 앱 업데이트 하고 유수비 보고용 화면 캡처 및 엑셀로 다운받을 수 있는 기능 추가"**

- **캠프 목록은 이미 있었다**(`reports/캠프마스터.json` 268개). 빠진 것은 **연락처**였고
  실측 **5개**뿐이었다. 그래서 `[165]`("안 읽은 열은 빈칸과 구별 안 된다")를 먼저
  의심했는데 **아니었다** — `customer_index` 는 `연락처` 열을 제대로 읽고 있고 다른
  거래처는 423개가 차 있다. **쿠팡 캠프만 ERP 에 전화를 안 적어 둔 것**이다
  (CU 356개 중 18개). ERP 를 다시 읽는 쪽으로 갔으면 하루를 잃었다.
  **'빈칸'을 보면 안 읽은 것인지 원본에 없는 것인지를 먼저 가른다.**
- 진짜 원천은 **밴드 접수 글 본문**이다(9,557건 중 **6,246건**에 번호가 있다):
  `● 캠프이름` · `● 캠프주소` · `● 현장책임`+`● 담당번호` · `● 안전관리`+`● 담당번호`.
  캠프마다 **두 사람**의 이름·전화·메일이 다 있다.
- ⚠ **`A/S 담당` 은 우리 기사다** — 캠프 담당자가 아니다. 섞으면 대표 보고에 우리
  기사 번호가 캠프 번호로 실린다.
- ⚠ **`담당번호` 가 한 글에 두 번 나온다.** 순서로 고르면 언젠가 어긋나므로
  **이름 줄 바로 다음 줄**의 번호만 그 사람 것으로 본다(`RE_SITE_MGR`·`RE_SAFE_MGR`).
- **뽑는 자리는 하나다** — `band_extract.parse_post`(`[162]`). `camp_contacts.py` 는
  그것을 모아 세기만 한다. 기존 반환 칸은 한 글자도 안 건드렸다(소비자가 여럿이다).
- **사람 칸은 각각 따로 최신을 고른다.** 글 하나를 통째로 이기게 하면 현장책임만 적힌
  글이 안전관리 칸을 빈 값으로 덮는다.
- **표시 이름은 최신이 아니라 가장 자주 쓰인 표기**다. 실측으로 캠프명 칸에 사람이
  메모를 적은 글이 있다(`서초1MB(양재동) = 서초1Sub-hub - 2RT 안전수칙은 …` 58자).
  ★ **자동으로 자르지 않는다** — 잘라 붙이면 엉뚱한 캠프에 번호가 들어간다(`[172]`).
  `이름확인필요` 로 표시해 맨 뒤로 보내고 **사람이 판단한다.**
- **표기만 다른 것은 합친다**(대소문자·공백·괄호·하이픈 — 실측 19묶음 761→737).
  ★ **숫자는 안 건드린다** — `송파1MB(감일동)` 과 `송파5MB(감일동)` 은 실재하는
  다른 캠프다(`[172]` 의 실측 오탐).
- ★ **'전화 모름'을 '담당자 없음'이라 적지 않는다**(`[169]`). 밴드에 접수 글이 없는
  캠프는 **아직 못 본 것**이다. 화면·이미지·엑셀 셋 다 그 문장을 그대로 적는다.
- **화면은 다시 계산하지 않는다** — 회차가 써 둔 `reports/캠프_담당자.json` 을
  `/api/camps` 가 읽어 내려줄 뿐이다(밴드 전체 파싱은 수십 초 · `[168]`).
- 엑셀은 **`exportRowsXlsx` 를 그대로 쓴다**(`[162]`). 이미지 캡처는 40개를 넘으면
  **몇 개를 못 실었는지 적고 엑셀로 받으라고 말한다** — 조용히 자르지 않는다(`[273]`).
- 회차는 09:50 daily_run 의 `캠프 담당자` 단계(`camp_contacts.py --write`).
  **캠프 마스터 뒤**에 온다(거래처코드를 그 결과에서 빌린다).
- 실측(2026-08-18): 캠프 **737개** · 정기점검 **402개** · 전화 있음 **377(93.8%)** · 모름 25.

### ★ 역할을 뭉치면 화면이 **그게 누구인지** 말하지 못한다 (2026-08-18 지시)

사용자 지시: **"담당자1, 담당자2 가 무엇을 의미하는지 모르겠어 정확히 설명도 포함해서"** ·
**"현장책임·안전관리·담당자 전부 표시되게 (이름, 전화번호, 이메일주소) 캡처 화면은
가로로 변경해서 한줄에 캠프 담당자 정보가 모두 들어가게"**

- **원본은 이미 역할을 갈라 적는데 화면에서 그 뜻이 사라졌다.** 자료에는
  `현장책임`·`안전관리`·`담당자` 셋이 담겨 있는데 표는 `담당자 ①·②` 라는 **순서**만
  보여 줬다. 빈칸도 오류도 아니라서 화면은 멀쩡해 보이고, **받아 본 사람만 모른다.**
  이제 셋을 각각 이름·전화·이메일로 내고 **각 역할이 무슨 뜻인지 화면이 적는다.**
  판정은 `campRoles()` 한 곳이고 표·엑셀·캡처가 그것을 본다([162]).
- ★ **대표 보고 이미지가 전화번호를 잘라 내보내고 있었다.** `slice(0,12)` 때문에
  `010-6445-1001` 이 **`010-6445-100`** 으로 나갔다 — 번호가 **있어 보이는데 걸리지
  않는다.** 비어 있으면 눈에 띄지만 한 자 빠진 번호는 안 띈다([165] 와 같은 종류).
  이제 안 들어가면 **그 칸만 글자를 줄이고, 줄인 개수를 이미지에 적는다**([273]).
- ★ **메일이 오는 자리는 둘이었는데 하나만 읽었다.** ① `● 담당번호 : 010-… / mail@…`
  (1,414줄) ② `● E- MAIL : mail@…` **별도 줄**(약 2,750줄). ②를 안 읽어 실측
  안전관리 메일이 207건뿐이었다(고친 뒤 **311**, 담당자 5→**336**).
  라벨 표기가 제각각이라(`E - MAIL`·`E-mail`·`이메일`·`이- 메일`) 공백·하이픈을 흘려 읽는다.
- ★ **메일 줄이 없으면 아래 사람 것을 끌어오지 않는다**([172]). 그리고 **현장책임
  메일이 거의 없는 것은 고장이 아니다** — 두 사람이 다 적힌 글 **1,926건 중 4건**만
  현장책임 쪽에 메일이 있다(양식이 그 칸을 안 적는다). 화면·이미지가 **그 이유를
  적는다** — 안 적으면 사람이 없는 자료를 찾아 나선다([169]).
- ⚠ **글자 검사로 구현을 못 박지 말 것.** `fit(` 이 있어야 한다고 적었다가 같은 일을
  다른 함수로 하는 **옆 세션 코드를 빨갛게** 만들었다(결과는 다 맞았는데도).
  칸 폭·겹침은 **글자로 못 잡는다** — `[295]` 가 node 로 화면 로직을 **실제로 실행**해
  전화 통째·이메일 통째·열 안 겹침·iOS 캔버스 한도를 결과로 잰다.
- 실측(고친 뒤): 정기점검 **402개 전부 전화 있음** · 이메일 **329개(81.8%)** ·
  라벨이 이름으로 샌 것 **0건**.

## ★ **반쪽 목록**은 조용히 나간다 — 그리고 400 이 사람을 탓한다 (2026-08-18, 분담판 [89]·[95])

- 증상은 저장이 안 되는 것이었다: `/api/staff/entry` **HTTP 400 63회**, 사유
  `화면 데이터 버전이 없습니다` **58회**. 그런데 그 58건이 **2026-08-13T02 한 시간에
  전부 몰려** 있었다(흩어져 있지 않다). 같은 시각 `tap saveInputs` 61회 —
  **한 사람이 한 시간 동안 61번 눌러 58번 실패**했고 서버는 그때 심하게 느렸다.
- ★ **[89] 의 진단은 틀렸다** — 그것부터 적는다. "관리대장에서 온 행은 앱 DB 에 없어
  버전이 안 붙는다"고 적혀 있었는데 실측은 반대다: 02시트 1,035행·04시트 702행·
  06시트 750행이 **전부** `_store_id` 와 `record_version>=1` 을 갖고 있다(ver0 = 0건).
  그 진단대로 `expected_version=0 을 create 로 받기` 를 만들었으면 **실재하지 않는
  문제를 위해 쓰는 길에 문을 뚫고** 중복 생성 위험만 남았다([172]).
- **진짜 자리는 두 폴백이었다** — `_overlay_app_store_settlements` ·
  `_overlay_app_store_works` 의 `except: return rows`. 앱 DB 를 못 읽으면 Excel 행을
  그대로 돌려준다. **그 폴백은 옳다**(화면을 통째로 닫지 않는다). 잘못은 **조용한** 것이다:
  · 돌아간 행에는 `_store_id`·`DB버전` 이 없어 저장이 400 으로 막히고,
  · 안내는 `목록을 새로고침해 다시 저장해 주세요` 인데 **새로고침해도 같은 반쪽이 온다.**
  · 자국이 없어 **그날 폴백이 돌았는지 나중에 확인할 길조차 없었다**([169]).
- ★ **판정 근거는 시간 창이 아니라 대조 하나다** — `_staff_store_row` 로 앱 DB 를 읽어
  **거기에는 버전이 있는데 화면이 못 싣고 왔으면** 그것이 곧 반쪽이다. 폴백 자국은
  '왜' 를 덧붙일 뿐이라 **자국이 없어도 이 갈래는 정확히 걸린다.**
- **갈래는 넷이고 조치가 다 다르다**: 앱 DB 를 지금 못 읽음(복구) · 행이 앱 DB 에 없음
  (`먼저 원본을 수집` — `[256]` 갈래 그대로) · **버전이 있는데 못 실림(반쪽)** ·
  그 밖(진짜 새로고침). **옆 갈래를 삼키지 않는 것**이 새 문구를 넣는 것보다 중요하다.
- ★ **덮기 실패와 파생 실패를 가른다**(`[172]`). `_overlay_app_store_works` 의 try 는
  덮기와 파생 계산을 같이 감싼다 — 뭉치면 파생 오류에도 "앱 DB 가 반쪽을 줬다"고 말해
  **사람이 멀쩡한 DB 를 고치러 간다.** 짐작하지 않고 **결과**로 가른다: `_store_id` 가
  하나라도 붙었으면 덮기는 된 것이다.
- 자국은 `reports/앱DB_폴백.json`(1분에 한 번만 쓴다) · `/api/app-store/status` 의
  `fallback`. **지금 DB 가 멀쩡해도 아까 반쪽이 나갔을 수 있다** — 그 사실은 여기
  말고는 어디에도 안 뜬다. **자국을 못 남겨도 화면은 열린다**(폴백의 뜻이 그것이다).
- ⚠ **검증은 실측 증거 파일에 한 글자도 안 쓴다** — 임시 경로로만 잰다(`[247]` 에서
  그대로 당했다: 시험 POST 하나가 리포트를 6시간 동안 '정상'이라 확언하게 만들었다).
- 남은 것은 **왜 그때 앱 DB 를 못 읽었나**다(분담판 `[92]` 의 느린 응답과 같은 시각대).
  이제 다음에 나면 `앱DB_폴백.json` 이 갈래와 예외를 그대로 적어 둔다.
- 검증 `[296]`.

## ★ 조직도가 바뀌면 붙잡되 **사람 상태로는 안 울린다** (2026-08-13 지시, 분담판 [111])

사용자 지시: **"조직도 변경사항 감지되면 자동으로 반영해"**

- ★ **먼저 있는 것부터 확인했다 — 새로 만들면 사본이 둘 된다**(`[162]`). 확인해 보니
  지시의 절반은 **이미 돌고 있었고 나머지 절반은 할 것이 없었다**:
  · **① 서버 갈기 — 이미 있다.** 조직도 코드는 `webapp/app_server.py`·`webapp/index.html`
    이고 둘 다 `restart_server.WATCHED` 에 있다. 그래서 조직도를 고치면
    `watchdog.heal_stale_server` 가 **스스로 갈아 준다**(`[156]`). 담당자가 최근 10분
    안에 앱을 만졌으면 `restart_server.guard()` 가 미룬다(`[265]`). **여기서 또 갈면
    폰이 502 를 두 번 받는다**(실측 4~9초).
  · **② 폰 사본 갱신 — 할 것이 없다.** `cloud_publish` 에 조직도가 **한 칸도 안 실리고**
    `docs/app.html` 에 조직도 마크업이 **0건**이다(실측). 없는 것을 갱신한다고 적으면
    그 문장 자체가 거짓이 된다(`[169]`) — 그래서 **안 만들었고, 안 만든 이유를 적는다.**
- **아무도 안 보던 자리는 따로 있었다** — 흐름 정의는 `ledger_db.flow_steps()`, 즉
  **DB 표(`flow_step`)** 에서 온다. 사람이 앱에서 흐름도를 고치면 **파일 mtime 이 한 톨도
  안 움직인다.** `heal_stale_server` 는 "서버 정상"이라 말하고 스케줄러도 조용하고,
  조직도 흐름 그림만 달라진다. **코드가 아닌 변경은 어느 눈에도 안 걸렸다.**
- `python org_watch.py --print` · 회차는 워치독 30분의 `watch_orgchart` 단계.
  자국은 `reports/조직도_반영.json`, 인계 '먼저 처리할 것'에도 올라간다.
  **새 스케줄 작업은 안 만들었다.**
- ★ **지문에 사람 상태를 넣지 않는다** — `[170]`(재수집 `hash_on` 사고)과 같은 모양이다.
  `state`·`msg`·`ago`·`badge` 와 **`ai` 구역**(세션·회차)은 회차마다 달라진다. 넣으면
  **30분마다 '바뀜'** 이 되어 아무도 안 본다. 지문은 **누가 있고 무슨 일을 하나**뿐이다.
- ★ **바뀐 것 자체는 경보가 아니다.** 바뀌고 잘 따라갔으면 그것은 정상이다 — 정상까지
  경보하면 진짜 경보가 묻힌다(`[170]`). 올리는 것은 **못 따라간 것**뿐이다.
- 근거는 `app_server.get_orgchart()` **하나**다(`[162]`). 로스터·흐름을 여기서 다시
  조립하면 화면과 감시자가 **서로 다른 조직도**를 놓고 답한다. 실측 0.26초라 비싼
  탐색도 아니다(`[168]`).
- **읽기 전용이다** — 서버를 안 갈고 큐에도 안 넣고 엑셀도 안 연다. 검증이
  `restart_server.main(`·`enqueue(`·`openpyxl` 이 들어오는 것을 막는다.
- ★ **못 읽으면 옛 지문을 지우지 않는다.** 지우면 다음 회차가 처음처럼 굴어 **없던
  '바뀜'을 만든다.** 그때는 '이상 없음'이 아니라 **'확인 못 함'** 이다(`[169]`).
- **실측 첫 실행(2026-08-18)**: 자리 8개(mgmt 4 · field 4) · 못 따라간 것 **2건** —
  ① 흐름도가 부르는데 조직도에 자리가 없는 사람 **유수비·김경원**
  ② 조직도 코드가 바뀌었는데 서버가 아직 옛 `index.html` 로 돎.
  ⚠ ① 은 **자동으로 자리를 만들지 않는다** — 누가 어느 구역에 앉는지는 사람만 안다
  (`[172]`). 로스터에 넣을지는 형님·류지영의 판단이다.
- 검증 `[297]`.

## ★ 만든 것과 **지켜지는 것**은 다르다 — 조직도 길게 눌러 자리 옮기기 (분담판 [76])

- `[76]` 은 2026-08-13 에 다른 창이 다 만들어 놓고 **'완료'로 적었다.** 그런데 그 창이
  남긴 쪽지가 *"Full synthetic suite could not start"* 였다 — **만들었지만 한 번도
  검증을 못 돌린 채 완료였다.** 완료 딱지는 붙어 있고 코드도 있으니 **아무도 다시 안
  본다.** 그래서 완료 항목이라도 쪽지에 '검증 못 돌림'이 있으면 **다시 만들지 말고
  그 검증부터 돌린다.**
- 돌려 보니 통과했다(구현은 멀쩡했다). 진짜 구멍은 **검사 쪽**이었다:
  · `[232]` 의 `[76]` 검사가 **전부 글자 검사**였다. `[295]` 가 이미 배운 자리다 —
    같은 일을 다르게 쓴 옆 세션 코드를 **빨갛게 만들고**, 정작 계약은 못 잰다.
  · 실제로 어긋나 있었다: 검사는 `touch-action:none` 이 있는지만 봤는데 **진짜 규칙은
    `pan-y`** 다(2026-08-15 "화면 아무데나 터치해서 스크롤"). 즉 누가 `none` 으로
    되돌려 **폰에서 조직도가 통째로 안 내려가게** 만들어도 검사는 초록이었다.
    이제 그 규칙 블록 안에 `touch-action:none` 이 오는 것을 막는다.
    ⚠ 좁힐 때 `"none" not in lp` 로 썼다가 같은 줄의 `user-select:none` 을 잡아
    **멀쩡한 코드에 거짓 경보**를 냈다([172] 의 문) — 표시는 낱말이 아니라 속성이다.
- **계약 둘은 글자로 못 잰다** — node 로 실제 실행해 결과로 잰다(`_T232_ORDER_HARNESS`,
  화면 코드는 `index.html` 에서 읽고 `localStorage` 만 목이다):
  ① **로스터에 새로 생긴 사람이 안 사라지고 맨 뒤에 붙는다**([169])
  ② **15초 갱신 뒤에도 옮긴 자리가 남는다** — 안 남으면 사람은 '안 된다'고 본다
  ③ API 응답·오프라인 스냅샷 **정본을 안 바꾼다** ④ 저장값이 깨져도 아무도 안 사라진다
  ⑤ 저장은 **그 구역만** 건드린다 ⑥ 구역을 모르는 host 로는 아무것도 저장하지 않는다
- ★ **계기 자신을 시험했다**([272]). 일부러 고장 둘을 주입해(저장 목록에 없는 사람을
  버리기 · 저장할 때 표를 통째로 덮기) **둘 다 잡히는 것**을 확인했다. 0 을 내는 계기는
  아무도 의심하지 않는다.
- **자리 순서는 `localStorage`(`csos_org_order_v1`) 에 둔다** — 사람 화면 환경설정이지
  업무값이 아니라 2026-08-10 정본 규칙의 허용 범위 안이다. 구역을 넘나드는 이동은
  공용 `dashDragEnable` 의 '같은 부모 안에서만' 규칙이 막는다 — **사람을 다른 팀으로
  옮기는 것은 로스터가 정한다.**
- 검증 `[232]` 확장.

## ★ 손가락을 붙잡는 화면은 **제 창 안에서만** 그런다 (2026-08-18 지시 [113])

분담판 항목: **"워크플로우 도면을 별도 창(버튼)으로 분리 — 스크롤하다 건드리면 틀어진다"**

- 도면 작업면은 `touch-action:none` 이다 — 그러지 않으면 끌기가 안 된다.
  그런데 그것을 **흐름 화면에 그대로 깔아 둔** 것이 고장이었다: 폰에서 그 위를 지나
  쓸어내리면 **페이지가 안 내려가고**, 손가락이 단계에 닿으면 그대로 끌려 배치가 틀어졌다.
  저장을 안 해도 화면은 달라 보이므로 사람은 **도면이 망가졌다고 읽는다** — 오류도 안 난다.
- 편집은 **`layerOpen('flowVisualEditor')` 별도 창**에서만 한다.
  ★ **시트를 새로 만들지 않았다**([236]) — `layerOpen` 은 요소를 **복사하지 않고
  옆겼다가 닫을 때 제자리로 돌려놓는다.** 그래서 id 가 그대로라 `flowVisualRender`
  는 한 줄도 안 고쳤다. 닫힌 상태는 `#flowVisualEditor:not(.in-layer){display:none}`
  한 줄이 정한다 — `hidden` 속성을 손으로 꾸면 **닫힐 때 되돌릴 자리가 없다.**
- ★ **닫혀 있을 때 보이는 것은 누르지도 끌리지도 않는 미리보기**다. 막는 것을 **두 겹**으로 둔다:
  ① 손잡이(`onpointerdown`)를 **아예 안 단다** ② `pointer-events:none` 으로 한 번 더 막는다.
  한 겹만 두면 언젠가 다른 손질에 새어 나간다. 그리고 단계를 `<button>` 이 아니라
  **`<div>`** 로 낽다 — 버튼은 눌리진 않아도 **탭 키로 잡힌다.**
- **`touch-action:none` 은 편집용 작업면 하나에만** 남는다. 미리보기는 `pan-y` 라
  본문 스크롤이 그대로 산다([232] 가 조직도에서 지키는 것과 같은 규칙이다).
- **그리는 함수는 하나**다([162]) — `flowVisualStageHtml(interactive)`. 편집기와 미리보기가
  같은 도면을 서로 다르게 그리면 사람은 어느 쪽이 진짜인지 알 수 없다.
  ⚠ 화살촉 `marker` 는 **id 를 갈라** 둔다 — 편집기가 `display:none` 일 때 그 안의
  marker 를 참조하면 **선은 남고 촉만 조용히 사라진다**(아무도 못 알아채다).
- **도면 전체를 폭에 맞춰 줄여 보여 준다 — 잘라 내지 않는다**([273]).
  그리고 **미저장 변경이 있으면 미리보기가 그 사실을 적는다** — 닫고 나면
  저장 안 한 줄을 알 길이 없어진다([169]).
- ⚠ **폰에서 편집기 안을 손가락으로 미는 것은 아직 안 된다** — 분담판 `[119]`.
  이것은 [113] 이전부터 있던 제약이고 형님이 말한 증상은 아니다 — 그것은
  페이지 스크롤 쪽이었고 고쳤다. 반쪽짜리를 넣고 끝냈다고 적지 않는다.
- 검증 `[302]` — 글자 검사로는 '미리보기가 정말 못 움직이는가'를 못 재다([295]).
  node 로 **실행해** 재고, 일부러 손잡이를 다시 달아 그 계기가 잡는지까지 본다([272]).
  ⚠ 만들면서 둘을 그대로 밟았다: 선택자를 `.flow-vpreview-stage{` 로 찾으니
  공용 규칙 `.flow-vstage,.flow-vpreview-stage{` 안에 먼저 걸려 **엉뜻한 덩어리를
  재고도 재었다고 말했고**, `touch-action:none` 을 세는 검사가 **제 설명 주석**에
  걸렸다([301]⑨ 에서 똑같이 당했다). 선택자는 **줄 머리부터** 찾고
  규칙을 세기 전에 **주석을 걷어낸다.**
- 실측(375×812 · demo): 미리보기 위에서 pointerdown+drag → 노드 안 움직임 · dirty false ·
  본문 끝(3,434px)까지 스크롤 · [도면 열기] 뒤 편집기 안에서는 노드가 실제로 끌림
  (42,38 → 82,63) · 닫으면 제자리 복귀 후 다시 무반응.

## ★ 두 고장이 서로를 가리고 있었다 — 판매 후보 오염 · 열쇠 (2026-08-19 지시)

사용자 지시: **"692건 파고들고 sale 분류 진행해"**

- 리포트가 750건 중 **`이카운트미등록` 692건(92%)** 을 냈다. 경보가 대부분이면 그
  경보는 아무도 안 본다(`[170]`) — 그런데 이건 한 겹 더 나쁘다. 그 숫자가 **대표
  보고의 '미청구·미발행 근거'로 쓰일 참**이었다.
- **미등록이 아니라 물어볼 수가 없었다.** `match_project` 의 열쇠는 둘뿐인데 둘 다
  안 돈다: ① 1순위 **프로젝트NO** — ERP 판매전표 본문에 그 번호가 안 적혀 있다
  (724행이 번호를 갖고도 23건만 걸렸다) ② 2순위 **금액** — 원장 06시트 공급가액이
  사람 손 입력이라 **727건(97%)이 빈 칸**이고 `금액허용오차`가 **0** 이라 빈 값으로는
  한 건도 못 맞춘다. `[203]` 이 `erp_ledger_check` 에 세운 그 판정
  (`key_looks_wrong`)이 여기엔 없었다.
- 같은 리포트가 **양측미발행 708** 이라 하는데 ERP 진행상태로는 **발행 450** 이다 —
  둘은 동시에 참일 수 없다. 그것이 이 숫자를 믿으면 안 된다는 증거였다.

### 판매 후보의 절반이 판매 자료가 아니었다
- `load_inbox` 마지막 줄이 `else: … else "sale"` 이었다 — **갈래를 못 알아본 파일이
  전부 판매 후보**가 된다. 모르는 것을 아는 것처럼 부르는 자리다(`[169]`).
  실측 127개 중 **62개(49%)**: 계정별원장 10 · 분개장 2 · 현금출납장 1 ·
  **unknown 36** · 세금계산서진행단계 9 · 견적 4. 거래처 마스터(`ESA001M`)와
  채권잔액까지 매출 후보로 들어와 있었다.
- ★ **`[203]` 을 절반만 반영한 자리였다.** 회계 원장류는 **네 화면**인데
  (거래처별계정별원장·계정별원장·분개장·현금출납장) 여기서는 `ledger` **하나만**
  걸렀다. 나머지 셋이 그대로 판매로 샜다.
- 이제 `SALE_NEVER` 한 표가 정한다. `sales`·`stmt`·`slips` 는 **안 건드렸다** —
  일부러 넣어 둔 매핑이고, 문제 없는 것을 고치지 않는다(`[172]`).
  `po` 도 그대로 뒀다(주석에 이유가 적혀 있고 머리글 조건이 한 번 더 거른다).
- **뺀 것은 숫자로 말한다**(`[169]`) — `_skipped` 를 세어 리포트 비고 **맨 앞**에
  적는다. 뒤에 붙이면 파일 목록 백 줄에 묻혀 아무도 못 본다.

### ★ 순서가 뜻을 갖는다 — 금액부터 고쳤으면 더 나빠졌다
- ★ **거짓 '일치'는 이미 나고 있었다** — 처음엔 "금액 열쇠가 안 돌아서 아직 안
  난다"고 적었는데 실측이 그것을 뒤집었다. 후보를 걷고 회차를 다시 돌리자 판정이
  **정확히 4건** 뒤집혔고 **전부 한 방향**(일치·금액불일치 → 미등록)이었다:
  `UJ2601146` 770,000 · `UJ2601246` 390,000 · `UJ2601066` 1,472,500 ·
  `UJ2600520`(원장 0 / EC 962,900). 두 회차 CSV 를 대 보니 **원장 공급가액이 바뀐
  행은 0** 이다 — 원장은 그대로고 **바뀐 것은 판매 후보 풀뿐**이라, 그 매치들은
  걷어낸 파일(남의 회사 전표)에 붙어 있던 것이다. 즉 **일치 23건 중 3건(13%)이
  가짜**였다.
- **그래서 순서가 이 순서다.** 지금 금액이 든 행은 **68건뿐**이라 가짜가 3건에
  그쳤다. `supply_of()` 를 먼저 붙여 빈 금액 **682건**을 채웠다면 그 13% 가 수백
  행에 적용된다 — 미등록으로 남는 것보다 나쁘다. 그 건은 아무도 안 챙기는데 화면
  에서는 끝난 것으로 보인다(`[172]`). **후보를 걷는 것이 먼저고 금액은 그다음이다.**
- ⚠ **다음 단계(`[124]`)에서 늘어난 '일치'를 그대로 믿지 말 것.** 금액만 같은 것은
  근거가 약하다(`[170]` 유형D 와 같은 자리) — 표본을 열어 그 ERP 전표가 정말 그
  프로젝트 것인지 확인한 뒤에 숫자를 말한다.
- 그래서 이번에 금액 사다리는 **손대지 않았다.** 대신 리포트가 스스로 말한다 —
  `key_warning()` 이 `erp_ledger_check.key_looks_wrong` 을 **빌려**(`[162]`) 짝이
  거의 안 지어지면 *"이 집계는 'ERP 미등록'을 뜻하지 않는다 — 열쇠가 안 맞는다"* 를
  근거(빈 금액 건수·후보 전표 수)와 함께 적고, **미청구·미발행 근거로 쓰지 말라**고
  못 박는다. 짝이 지어지면 **아무 말도 안 한다** — 정상까지 경보하면 아무도 안 본다.
- `ecount_reconcile` 은 큐에도 안 넣고 엑셀도 안 연다 — **읽는 길**이라 경보 기준을
  넓혀도 잃는 것이 없다(`[170]` 의 '읽는 길과 쓰는 길').
- ⚠ **남은 것**: 금액 사다리 붙이기(후보를 걷은 뒤 다시 재고 나서) · `slips`
  (회계거래조회/현황)를 판매 후보로 두는 것이 옳은지 · `config` 의 `master_xlsx` 가
  아직 **v20** 을 가리킨다(실제 대조는 최신 vN 을 찾아 쓰므로 지금 동작에는 영향이
  없지만, 그 값을 믿는 코드가 생기면 조용히 옛 원장을 읽는다).
### 금액 사다리를 붙였다 — 그런데 **좋아지는 쪽이 아니었다** (2026-08-19 실측)

- 오래 미뤄 둔 `[124]` 다. 붙이는 자리는 **다섯**이었다(처음에 "둘"이라 적은 것은 틀렸다):
  (1) `match_project()` 2순위 열쇠 (2) 판정 비교 (3) 불일치 문구 (4) 리포트 열·마크다운 표
  (5) ★ `key_warning()` 의 `빈금액`. 다섯째가 제일 나쁘다 — 그대로 두면 사다리가 금액을
  채운 뒤에도 *"빈 행이 682건이라 한 건도 못 맞춘다"* 를 **근거를 대며** 확언한다([169]).
  값은 `supply_effective()` **한 곳**에서 나오고 다섯이 그 하나를 본다([170]).
- ★ **붙였더니 '일치'가 20 → 139 로 뛰었다. 그리고 그것이 전부 가짜였다.**
  · **139건 전부** `금액만 · 근거 약함` — 프로젝트NO 로 붙은 것은 **0건**
  · **122건(88%)은 EC 전표번호조차 없었다** — 붙었다는데 가리킬 전표가 없다
  · **서로 다른 금액 23개가 139건을 만들었다** — `380,000` 하나에 51건, `240,000` 에 36건
  판매 후보가 **8,781행**이라 흔한 AS 단가는 반드시 부딪힌다. 그건 근거가 아니라 **충돌**이다.
- ★ **잘못 붙은 '일치'는 못 붙는 것보다 나쁘다**([172]). 그 행은 `이카운트미등록`
  목록에서도 **빠진다** — 아무도 안 챙기는데 화면에서는 끝난 것으로 보인다. 숫자만 보면
  692 → 584 로 좋아진 것처럼 읽히므로 **아무도 안 의심한다**([169]).
- 그래서 이 프로젝트가 다른 데서 쓰는 문을 그대로 달았다 — **후보가 유일할 때만**
  (`typo_watch`·`camp_code_match`·`receipt_fill` 과 같은 문). 둘이면 어느 쪽인지 원본이
  안 말해 주므로 **안 붙인다.** 실측 **139 → 32건**.

| 판정 | 붙이기 전 | 사다리만 | +유일 후보 문 |
|---|---:|---:|---:|
| 일치(금액만 · 근거 약함) | 20 | **139** | **32** |
| 이카운트미등록 | 696 | 584 | 686 |
| 해당없음(무상) | 34 | 27 | 32 |

- **정직하게 적어 두는 결론: 사다리는 이 문제를 못 푼다.** 금액은 678행이 채워졌지만
  (유효금액 빈칸 306 → **4**) 매칭은 20 → 32 로 **12건** 늘었을 뿐이다. 750건 중 686이
  여전히 미등록이고, 그 이유는 1순위 열쇠다 — **ERP 판매전표 본문에 프로젝트NO 가 안
  적혀 있다.** 사다리로는 안 풀린다. 다음에 손댈 곳은 금액이 아니라 **그 번호**다.
- ⚠ **막는 이유가 바뀌면 리포트 문구도 같이 고친다.** 열쇠 경고가 *"빈 값으로는 한 건도
  못 맞춘다"* 라고 적어 뒀는데, 사다리 뒤에는 빈 행이 **4건(1%)** 뿐이라 그 문장이
  **틀린 이유를 확언**하게 된다. 지금은 *"같은 금액 전표가 여럿이면 안 붙인다"* 라 적는다.
- ⚠ **세금계산서 층에는 안 붙였다**([172]). 판매 층은 `일치(금액만 · 근거 약함)` 딱지가
  먼저 들어가 근거의 세기를 말할 수 있는데, 그 층에는 그 딱지가 없어 약한 금액 매치가
  **그냥 '일치'** 로 적힌다. 딱지를 먼저 만든 뒤에 넓힌다.
- ⚠ 사다리는 ERP 색인(Z: 재귀 탐색)을 **한 번** 만든다([168] — po_reconcile 모듈 전역
  캐시). 부르는 곳은 회차 둘뿐이라(`daily_run` · `automation_pipeline` 1200초 한도)
  웹 요청은 안 느려진다. 순환 import 라 **함수 안에서 늦게** 부른다.
- 검증 `[323]` — 값이 한 곳에서 나오는지 · 0 은 금액 없음인지 · 못 읽은 것을 '없음'이라
  안 하는지 · 경고가 유효 금액을 세는지 · **같은 금액 전표가 둘이면 안 붙는지**를
  합성 행으로 **실행해서** 잰다([295]). 유일하면 붙는 것도 같이 잰다(문을 너무 좁히면
  그것도 고장이다).
- 검증 `[317]` — 표·순서를 구조로 보고, 임시 xlsx 두 장을 만들어 **실제로 돌려**
  모르는 갈래가 안 담기는지·뺀 것을 세는지·열쇠 경고가 뜨고 멀쩡하면 조용한지를
  결과로 잰다(`[295]`). 문을 빼면 잡히는지까지 본다(`[272]`).

## ★ 회차가 **어느 단계에서** 멈췄는지 못 대면 며칠이 그냥 간다 (2026-08-19 실사고)

- `쿠팡업무_원본자료자동정리` 가 09:35 에 떠서 **12:35 에 제한시간(PT3H)으로 강제
  종료**됐다(`[중단됨]` P0). 그런데 `reports/source_organizer.log` 는 **시각도 단계
  표시도 없이** 파일 이름만 쏟다가 **11:37 에 멎어** 있었다 — 즉 **58분을 아무 말 없이
  서 있었고**, 네 단계(`upload_intake`·`collect_sources`·`source_organizer`·
  `new_project_flow_sync`) 중 어느 것이었는지 **알 길이 없었다.** 스케줄러가 아는 것은
  '끝내기 요청을 받고 멈췄다' 한 줄뿐이다(`[228]` — exit 코드는 왜인지를 말해 주지 않는다).
- ★ **제한이 회차 전체에만 있었다.** 그래서 **한 단계가 세 시간을 다 먹고 나머지 셋은
  아예 안 돌았다** — 그런데 그 셋이 안 돌았다는 사실은 **어느 화면에도 안 뜬다**(`[169]`).
  이제 `source_tidy_run.py` 가 단계마다 `STEP_TIMEOUT_S`(기본 40분)를 건다.
  **4×40=160분 < PT3H** 라 회차는 반드시 끝나고, 끝나야 자국이 써진다(`[180]`).
  ⚠ 이 값을 올릴 때는 **단계 수 × 값 < 작업 제한시간**을 지킨다 — 안 지키면 마지막
  단계가 잘리면서 자국도 안 써져 지금 고친 그 모양으로 되돌아간다.
- ★ **짐작으로 제한시간부터 늘리지 않았다**(분담판 `[38]`). 먼저 **범인 단계를 대게**
  만드는 것이 순서다. `collect_sources.py` 만 `[198]` 의 `walk_stat` 을 안 쓰는 것이
  눈에 띄지만 **그것만으로 고치지 않는다** — 다음 회차가 이름을 댄다.
- ★ **한 단계가 죽어도 다음으로 간다**(`[175]`) · **죽었다는 사실은 적는다**(`[169]`).
  자국은 `reports/원본정리_오류.json`(`schedule_watch.traces()` 가 글로브로 모은다) ·
  진행은 `reports/.원본정리_진행.json`(**죽어도 남는다**) · 로그는 이제 **시각과 단계
  머리글**을 단다. **다 되면 자국을 지운다**(`[228]`).
- ★ **아는 것을 짐작으로 덮지 않는다 — 만들면서 검증이 그것을 잡았다.** 시간초과로
  끊긴 것을 자취 글자로 분류했더니 갈래가 **`code`**("코드가 깨졌다")로 나왔다.
  조치는 갈래마다 다르므로(`[289]`) 그 한 줄이 사람을 **멀쩡한 코드**로 보낸다(`[172]`).
  `timed_out` 이 이미 말해 주는 것을 분류기에 다시 묻지 않는다.
- ★ **`.bat` 은 종료코드를 그대로 돌려준다**(`endlocal & exit /b %ERRORLEVEL%` · `[248]`).
  삼키면 죽은 회차가 성공으로 적히고 감시자의 눈이 먼다.
- ⚠ 같은 자리에서 배운 것: `synthetic_check.py` 에는 **`io`·`shutil` 이 모듈 수준에
  없다** — `io.open(` 을 쓰면 진짜 실패가 `NameError` 에 가려진다(`t323` 에서도 그대로
  당했다). 그리고 패치 스크립트의 삼중따옴표 안에서는 **역슬래시를 아예 안 쓴다**
  (`chr(10)` 을 쓴다) — 이스케이프가 한 번 더 풀려 문법이 깨진다.
- 검증 `[324]` — 진짜 Z: 는 안 건드리고 `run_tree` 만 갈아 끼워 **실행으로** 잰다(`[295]`).
  계기 자신도 시험한다(`[272]`): 자국 남기기를 빼면 잡히는지.

## 핵심 금지사항 (상세는 ecount/AGENTS.md)
- 관리대장을 openpyxl로 열어 save() 금지 (차트·도형 파괴) — 수정은 zip 패치 도구만
- 비밀키(config/*.json)를 커밋·채팅·엑셀에 넣기 금지
- 이카운트 무차별 API 탐침 금지(트래픽 제한으로 ERP 차단 위험)
- 실데이터 작업 전 합성검증 생략 금지


## ★ '모름'은 **근거가 유일할 때만** 채운다 (2026-08-19 지시)

사용자 지시: **"모름 표시되어있는 부분 밴드 및 erp, 원본 데이터등 전수 조사해서
반영될 수 있도록 코딩해"**

- **어디에 답이 있는지부터 쟀다** — 짐작으로 채우지 않는다. 실측 2026-08-19:
  · **밴드·카톡 본문** — 이미 쓰는데 **최신 글이 사람 칸을 통째로 덮어** 25칸을
    잃고 있었다. `[311]` 이 역할별로는 갈라 놓고 **그 안의 이름·전화·메일은 안
    갈랐다** — 바로 위 주석이 경고한 함정이 **한 층 아래**에 그대로 있었다.
  · **밴드·카톡 사람 명부** — 안 쓰고 있었다. 같은 사람이 여러 캠프에 나오므로
    한 곳에서 알아낸 메일을 다른 캠프에서 쓸 수 있다(**143칸**).
  · **ERP 거래처 마스터**(`ESA001M`) — 안 쓰고 있었다. 캠프 249개에 담당자명 217 ·
    메일 198 이 있고 **밴드와 같은 사람**이다(권순환 → `sukwon52@coupangls.com` 일치).
  · **관리대장** — 캠프 연락처 칸이 **아예 없다**(`read_ledger` 열 목록에 하나도 없다).
    전수 조사는 여기까지다.
- ★ **어려운 것은 채우는 게 아니라 잘못 채우지 않는 것이다**(`[172]`). 메일을 틀리게
  채우면 사람이 **엉뚱한 사람에게 캠프 업무를 보낸다** — 모름으로 두는 것보다 나쁘다.
  문 다섯: **후보가 유일할 때만** · **덮지 않음**(빈 칸만) · **역할을 지어내지 않음**
  (ERP 거래처 담당자는 직책을 말하지 않으므로 `담당자`(직책 미상) 칸에만) ·
  **번호가 이름보다 셈**(`people_alias` 와 같은 근거) · **출처를 적음**.
- ⚠ **한 글자는 이름이 아니다.** 첫 판이 앞 토막만 잘라 명부 열쇠에 `김`·`이`·`M`·
  직함 `SM` 이 들어왔고 그것으로 3칸이 채워질 참이었다. 이제 **한글이 든 2글자 이상**
  토막을 고른다(`SM 로건`→`로건` · `권순환 Cain`→`권순환`).
- ⚠ **방금 채운 값을 다시 열쇠로 쓰지 않는다.** 첫 판은 이름→전화→메일 순서로
  채우며 그 사람 칸을 그대로 열쇠로 썼다. ERP 가 준 이름으로 전화를 찾고 **그
  전화로** 메일을 찾는 연쇄가 생기는데, 한 칸이 틀리면 나머지도 같이 틀리면서
  **똑같이 확신에 차 보인다.** 조회는 언제나 **원문 그대로의 사람 칸**에 한다.
- ★ **보류한 칸을 숫자로 말한다**(`[169]`). 조용히 안 채우면 '다 채웠다'로 읽힌다.
  실측 **237칸 채움 · 119칸 보류 · 덮은 칸 0** · 전화 있는 캠프 **674/734**.
  정기점검 400 기준 현장책임 메일 100%→**86%** · 담당자 메일 44%→**33%**.
- ★ **남은 모름은 고장이 아니다.** 현장책임 메일은 밴드 양식이 그 칸을 거의 안 적어
  (1,926건 중 4건) 344건이 그대로 모름이다 — 화면이 **그 이유를 적어야** 사람이
  없는 자료를 찾아 나서지 않는다.
- ⚠ **아직 반쪽이다** — 채운 값이 화면·엑셀에서 **원문과 똑같이 보인다.** 행마다
  `보완` 칸에 출처가 들어 있는데 어디에도 안 나온다. 분담판 **[123]**.
- 비싼 ERP 읽기는 `camp_code_match._erp_customers` 를 빌린다(**캐시 먼저** · `[168]`).
  **못 읽으면 '없다'가 아니라 안 채우고 그 사실을 적는다**(`[169]`).
- 검증 `[314]` — '얼마나 채웠나'가 아니라 **'안 채워야 할 것을 안 채웠나'** 를 재고,
  유일 후보 문을 빼면 그 검사가 통과해 버리는지까지 본다(`[272]`).

## ★ 한 장에 안 들어가면 **자르지 말고 장을 늘린다** (2026-08-18 지시)

사용자 지시: **"장수가 여러장 되도 좋으니 전국 캠프 다 표시해서 나오게 해"** ·
**"엑셀 파일도 전부 기입해서 클릭하면 바로 다운 받을 수 있게 정리해"**

- 캠프 캡처가 **402개 중 89개**만 내보내고 있었다. 한도는 iOS 캔버스 면적(약 1,678만
  픽셀)이었고, 못 실은 만큼은 `[273]` 대로 그림에 적어 뒀다 — **조용한 사고는
  아니었다.** 그러나 대표 보고에 쓰는 그림이 자료의 5분의 1이었다.
  **적어 두는 것과 다 보여 주는 것은 다른 말이다.**
- 이제 한도는 **한 장의 높이**일 뿐 목록의 길이가 아니다 — 넘치면 장을 늘린다
  (실측 402개 = 5장). 폭주 방지 상한 `MAXPAGES`(40)에 걸릴 때만 `[273]` 문구가 나온다.
- ★ **장마다 머리글과 각주를 다시 그린다.** 2장째만 카톡으로 받은 사람도 어느 칸이
  누구 전화인지 알아야 하고, `'모름'은 담당자가 없는 것이 아니다` 라는 뜻이 닿아야
  한다(`[169]`). 몇 장 중 몇 장째인지는 **머리에** 적는다(아래에 안 적는 것은 `[283]`).
- ★ **크롬은 '파일 여러 개 다운로드'를 한 번 묻는다.** 사람이 차단하면 첫 장만 남고
  나머지는 **오류 없이 사라진다** — 받은 사람은 그 한 장이 전부인 줄 안다. 그래서
  `saveOrOpenPages` 가 몇 장을 보내는지 **먼저** 말하고 장마다 저장을 알린다.
  iOS 는 둘째 창부터 팝업이 막히므로 **한 창에 전부** 담는다.
  한 장이면 기존 `saveOrOpen` 그대로 간다 — 사본을 만들지 않는다(`[162]`).
- ★ **한 장이라도 못 만들면 아무것도 안 내보낸다.** 반쪽을 말없이 보내면 받는 사람은
  그것이 전부인 줄 안다.
- **엑셀은 빈 칸을 남기지 않는다** — 화면이 쓰는 그 낱말 `모름` 을 그대로 적는다.
  엑셀에서 빈 칸은 '적을 것이 없다'인지 '아직 못 찾았다'인지 구별이 안 된다(`[169]`).
  낱말을 새로 만들지 않는 이유는 화면 설명과 뜻이 이어져야 하기 때문이다.
- 검증 `[295]` — 250개를 넣어 **장이 둘 이상 나오는지 · 그린 캠프가 250개인지 ·
  파일 수와 장 수가 같은지 · 이름에 `NofM` 이 붙는지 · 엑셀에 빈 칸이 없는지**를
  node 로 **실행해** 잰다. 글자 검사로는 '몇 개가 빠졌는지'를 못 잡는다.

## ★ 카톡에 적힌 근거는 캡처까지 간다 — 그러나 없는 근거는 안 만든다 (2026-08-18 지시)

사용자 지시: **"카톡에 메시지에 근거가 있으면 그것도 표시해 캡처화면에"**

- **자료는 이미 들어와 있었다.** `band_extract.load_records()` 는 처음부터 밴드와
  카톡을 **같은 양식**으로 읽는다. 그래서 카톡에만 온 보고도 색인에는 있었는데,
  화면이 할 수 있는 말은 **뭉친 한 줄**뿐이었다 — `밴드·카톡에 이 프로젝트 글이 없다`.
  즉 **카톡에 뭐라고 적혀 있는지는 어느 화면에도 안 떴다.** 오류도 빈칸도 아니라
  아무도 몰랐다(`[169]`).
- **판정 자리는 그대로 하나다**(`[162]`) — `_why_still_open` 이 만든 사유를 대표
  캡처가 **이미 그대로 싣는다**(`[r['요청내용'],r['실제조치'],r['미처리사유']]`).
  그러므로 캔버스는 **한 줄도 안 고쳤다.** 근거를 사유에 붙이는 것으로 끝난다.
- 실측 2026-08-18: 언급 1,844 · 완료 1,564 · **카톡 근거 1,023**(전부 본문 있음).
- ★ **'글이 아예 없다' 갈래에는 절대 안 붙인다.** 없는 카톡을 있다고 말하면 사람이
  **없는 원본을 찾아 나선다**(`[172]`). 검증이 그 갈래를 따로 잰다.
- ★ **뭉쳐 말하지 않는다** — 카톡에 글이 있으면 `밴드에는 완료 글이 없다(카톡에는
  글이 있다)` 로 갈라 말한다. 예전 문구는 카톡에 완료 보고가 와 있어도 그냥
  '완료 글이 없다'였다.
- ★ **말이 없는 카톡을 없던 일로 만들지 않는다** — 양식만 있는 글은
  `적힌 말 없이 양식만` 이라고 적는다. 조용히 빼면 '카톡을 안 봤다'로 읽힌다.
- ★ **양식 칸은 걷어낸다**(`_kakao_gist`). 카톡 접수 글은 대부분이 양식이라
  (프로젝트NO·캠프이름·담당번호·메일) 그대로 실으면 **정작 근거인 한 줄이 묻힌다**.
  캡처가 이미 제 자리에 그리는 칸을 사유에 또 싣지 않는다.
- ★ **길이는 취향이 아니라 캡처의 제약이다.** 대표 캘린더는 A4 **세 장 고정**이고
  다 못 실으면 `[273]` 이 **저장을 거부한다** — 사유를 길게 실을수록 보고서가
  아예 안 나오는 쪽으로 간다. `_KAKAO_GIST_MAX`(60)로 묶고 자른 만큼은
  `…(원문 N자)` 로 말한다(`[273]` — 조용히 자르지 않는다). 실측 사유 평균 137자.
- ★ **카톡 원본을 색인 지문에 넣었다.** 그 지문은 예전부터 **밴드 캐시만** 셌다 —
  카톡이 새로 들어와도 밴드가 그대로면 지문이 안 움직여 **새 근거가 영영 안 실린다**
  (`[169]`). 근거를 화면에 띄우기로 한 지금은 그 구멍이 곧 '없는 근거'가 된다.
- ★ **`_BAND_EV_VER`(색인 판)를 손으로 올린다.** 지문은 '원본이 바뀌었나'만 보므로
  규칙을 고쳐도 원본이 그대로면 옛 캐시가 영원히 이긴다 — `inbox_scan.RULES_VERSION`
  과 같은 자리이고 이 프로젝트가 네 번째 겪는 모양이다.
- ★ **카톡 원본 자리는 `band_extract.KAKAO_INBOX` 한 곳**이다. 만들면서 검증이
  실제로 잡았다 — `app_server` 가 **쓰는 쪽 두 곳**에서 경로를 따로 조립하고 있었다
  (업로드 저장·자동정리 대상). 지금은 같은 폴더지만, **쓰는 쪽과 읽는 쪽이 갈리는
  날 올린 파일이 영영 안 읽힌다**(오류는 안 난다).
- 원천은 `parse_post` 가 **옆에 더한** `본문`(800자 상한 · `본문잘림` 표시)이다.
  기존 칸은 한 글자도 안 건드렸다 — 이 반환값을 읽는 곳이 여럿이다.
- ⚠ 같이 잡은 것: `t298` 의 node 호출에 **창 없는 깃발(`[272]`)도 시간제한(`[175]`)도
  없었다.** 검증 자신이 콘솔 창을 띄우고 있었고, node 가 멈추면 **검증 전체가 영원히
  안 끝나는** 자리였다. 정본 관용구(`Popen` + `CREATE_NO_WINDOW` + `communicate(timeout=)`)로 맞췄다.
- 검증 `[299]`.

## ★ 화면이 **없는 함수**를 부르면 "자료를 못 받았다"고 말한다 (2026-08-18 실사고)

형님 캡처(10:55): 전국 쿠팡캠프 화면이 표·요약 둘 다 빈 채
**"목록을 불러오지 못했습니다 — 잠시 뒤 새로고침해 주세요."**

- **자료는 멀쩡했다** — `reports/캠프_담당자.json` 737개(정기점검 402). 서버 경로도
  살아 있었다(`/api/camps` 는 08:32 커밋, 서버는 10:22 기동). 그런데도 빈 화면이었다.
- **범인은 한 낱말이다.** 캠프 화면 코드가 `esc(` 를 **12곳**에서 부르는데 그 이름의
  전역 함수가 **없다** — 이 앱의 정본은 `esc2`(516곳)다. `esc` 는 mermaid·마크다운
  두 함수 안의 **지역 `const`** 로만 존재한다. 그래서 `renderCampList` 가 **첫 셀에서**
  죽었다.
- ★ **찾은 길은 코드가 아니라 기록이었다.** ux 에 `01:55:02Z · error ·
  esc is not defined` 가 그대로 찍혀 있었다(형님 캡처와 같은 분). **화면이 남긴
  오류 한 줄이 반나절을 아꼈다** — `[290]` 이 전역 오류 수집을 고쳐 둔 자리가 여기서
  값을 했다. 증상만 보고 서버·수집·Z: 를 뒤졌으면 하루가 갔다.
- ★ **그런데 화면은 원인을 확언했다.** `renderCamps` 가 **받는 것과 그리는 것을 한
  try** 로 묶어 둬서, 그리다 터진 것까지 "못 불러왔다"로 나갔다. 형님은 서버와 수집을
  의심하며 새로고침만 반복하셨다 — **틀린 지목은 못 잡는 것보다 나쁘다**(`[172]`).
  `[257]`(“못 찾음도 세 가지 뜻이다”)이 정확히 이 모양을 이미 적어 뒀는데, **그
  교훈이 이 화면에는 안 와 있었다.** 한 화면에서 배운 것을 다른 화면이 모른다.
- 이제 셋으로 갈라 말한다: **못 받았다**(자료가 없다는 뜻이 아님) · **받았는데 서버가
  못 만들었다**(그 사유 그대로) · **받았는데 못 그렸다**(실제 예외 문구 + 받아 둔
  캠프 수 + **"[엑셀 저장]·[이미지 저장]은 그대로 됩니다"**).
  · 마지막 한 줄이 실질이다 — 그날 형님은 **대표 보고**가 급하셨다. 표가 안 그려져도
    엑셀·캡처는 `CAMPS` 를 직접 읽으므로 살아 있는데, 화면이 그 사실을 말 안 하면
    **사람은 보고 자체를 포기한다.**
  · 그리기 실패를 `uxEvent('error')` 로도 남긴다 — 다음에는 기록이 먼저 말한다.
- **확인은 실행으로 했다**(글자 검사로는 '몇 개가 빠졌는지'를 못 잡는다, `[295]`):
  실제 `캠프_담당자.json` 을 node 에 먹여 `renderCampList` 를 돌려 **402행 · 전화
  402 · 이메일 329 · 모름 0 · `<table>` 생성**을 결과로 쟀다.
- ⚠ **금지는 `esc(` 라는 이름이 아니다.** 지역 `const esc` 를 가진 두 함수 안에서는
  정당하다 — 이름으로 막으면 멀쩡한 코드가 빨개진다(`[295]` 가 배운 자리). 검증은
  **전역에 esc 가 없다는 사실**과 **캠프 블록 안에 bare `esc(` 가 없다는 사실**을
  따로 잰다. 계기가 진짜 고장을 잡는다는 증거는 있다 — 같은 정규식이 고치기 전
  **12곳을 그대로 짚었다**(`[272]`).
- 검증 `[300]`.

## ★ 죽은 회차는 **왜인지** 남긴다 · 뒤에 된 일로는 울지 않는다 (2026-08-18 실사고)

인계 맨 위의 `[P0] 쿠팡업무_일일자동대조 — 0 이 아닌 값으로 끝났다 (코드 1)` 에서
시작해 **반대 방향의 구멍 둘**이 같이 나왔다.

### ① 관문이 막은 이유가 어느 화면에도 안 떴다
- 실측: 09:50 회차가 **7일 중 6일**(8/10·12·14·15·16·18) 같은 자리에서 죽었다.
  그런데 남는 말이 전부 같다 — 스케줄러 `exit 1` · `schedule_watch` "0 이 아닌 값으로
  끝났다" · 인계 문서도 그 말. **진짜 이유는 종합리포트 파일을 열어야** 나왔다:
  `FileNotFoundError: 관리대장을 찾을 수 없음: Z:/… (폴더에 v*.xlsx 없음)`.
  즉 **코드가 깨진 게 아니라 그 순간 Z:(SMB)를 못 읽은 것**이었다.
- ★ **여섯 번 반복되는 동안 아무도 못 고쳤다.** `[228]` 이 '어느 회차가 죽었나'를
  보이게 만들었는데, 그다음 질문인 **'왜'** 에 답할 자국이 이 단계에는 없었다.
- ★ **이 단계는 자율복구 대기열에 일부러 안 들어간다** — `autopilot.defer` 첫 줄이
  `if name == "합성검증": return None` 이다. 안전문을 '나중에'로 돌리고 업무를 계속하면
  안 되기 때문이고 **그 판단은 옳다.** 그러므로 남는 길은 **자국뿐**이었는데 그것이 없었다.
- 이제 `daily_run._leave_gate_trace` 가 `reports/일일대조_오류.json` 에 남긴다.
  `schedule_watch.traces()` 가 `*_오류.json` 을 **글로브로** 모으므로 목록을 손으로
  안 적어도 저절로 실린다. **통과하면 지운다**(`[228]` — 옛 자국은 고쳐진 고장을
  계속 보고한다).
- ★ **갈래를 새로 만들지 않았다**(`[162]`) — `autopilot.classify_failure` 가 이미
  가른다(그 함수의 자기 시험이 **바로 이 문자열**을 `resource` 로 판정한다).
  그리고 **조치는 갈래마다 다르다**(`[289]`) — 자원 실패에 "코드를 고치세요"라고
  적으면 사람이 **멀쩡한 코드를 뒤진다**.

### ② 그 뒤에 실제로 끝났는데 경보가 안 내려갔다
- 같은 날 실측: 그 회차가 **14:54 에 80단계를 완주했다**(`agent_status.json`
  `aborted:false`). 스케줄러는 **작업**의 마지막 결과만 아는데, 이 프로젝트에서 같은
  *일*은 여러 길로 불린다(예약 작업 · `automation_pipeline` · 워치독 · 사람).
  그런데도 경보는 **[P0] 로 그대로** 인계 맨 위에 올라왔다 — 경보가 대부분 가짜면
  나머지도 아무도 안 본다(`[170]`).
- 새 갈래 `뒤에됨`(`schedule_watch.ran_after`). **`[110]` 의 `고침대기` 보다 센
  근거**다 — 저쪽은 '코드가 바뀌었으니 아마 고쳐졌을 것'이라는 추측이고 여기는
  **그 일이 끝났다는 사실**이다. 그래서 판정 순서가 이쪽이 먼저다(`[203]`).
- ★ **일반화하지 않는다.** 이 자국은 daily_run 계열에만 있다(`RAN_TRACE`). 다른
  회차에 이 근거를 대면 '자국이 없다'가 곧 '안 됐다'로 읽혀 거짓이 된다.
- ★ **못 읽으면 그대로 실패로 둔다**(`[169]`) · **중단으로 끝난 자국**과
  **실패보다 앞선 완주**는 근거가 아니다 · `DEAD` 에 넣지 않는다(넣는 순간 P0 로
  되살아난다) · 그렇다고 조용히 빼지도 않고 **알림으로 남긴다**.

### ③ 판정이 그 회차에 대해 **눈이 멀어 있었다** (같이 잡음)
- `task_scripts` 가 `쿠팡업무_일일자동대조` 의 스크립트를 `run_hidden.vbs`·
  `daily_run.bat` 까지만 찾고 **`daily_run.py` 를 못 찾았다.** bat 이 제 폴더를
  `%~dp0` 로 적는데(`"%~dp0daily_run.py"`) 정규식이 `dp0daily_run.py` 를 잡아
  **디스크에 없는 이름**이 되고 조용히 버려진 것이다.
- 그래서 `[110]` 의 `고침대기` 도 이 회차에 대해 한 번도 안 걸렸다 — **오류도 안 나고
  목록도 그럴듯해서 아무도 몰랐다**(`[165]`). 이제 배치 변수를 먼저 벗긴다.

### 만들면서 그대로 밟은 것 둘 (둘 다 '문법은 통과하는' 종류였다)
- ⚠ **정규식에 백스페이스가 들어갔다.** heredoc 을 거치며 `\\b` 가 `\b`(0x08)로
  줄어 `re.match(r"…(Error|Exception)\x08.*:")` 가 됐다. `ast.parse` 는 통과하고
  **한 건도 안 걸리면서 오류도 안 난다**(`[165]` 모양). 그러므로 **정규식이 든 코드는
  실행해서 결과로 재야 한다** — 글자·문법 검사로는 못 잡는다(`[295]`).
- ⚠ 변수명 하나(`src`→`text`)가 안 따라왔는데 **문법 검사는 초록이었다.** 잡은 것은
  실행이다. `[304]` 가 이 둘을 실행으로 잰다.
- ⚠ **실측 증거 파일에는 한 글자도 안 쓴다**(`[247]`) — 임시 경로로만 잰다.

- 실측(고친 뒤): 경보 **1 → 0** · 알림 1건(`뒤에됨`) · 계기 자기시험 **6/6 잡음**.
- 검증 `[304]`.

### 자가치유가 **안 건드린 파일**을 '새로 만들었다'고 적고 있었다 (같은 날, 분담판 [56])

- `watchdog.heal_stale_pastefiles` 가 낡음을 `mk_mt`(만드는 쪽 셋 중 최신)로 고르면서
  **새로워졌는지는 `js_mt`(`grab_posts.js`) 로만** 봤다. 그래서 `recheck_plan.py`
  (08-18 10:28)가 `grab_posts.js`(08-12 00:53)보다 새로우면 **같은 파일이 낡았으면서
  동시에 새로웠다** — 30분마다 골라서, 아무것도 안 바뀌었는데, "새로 1개"라고 적었다.
- 실측: `수집_붙여넣기_84789192.js` 가 **08-16 00:14 그대로**인 채 13:58·14:34·14:58
  **세 회차 연속 성공**으로 보고됐고 워치독은 `result=0` 으로 끝났다.
- ★ 2026-08-11 에 **낡음 기준만** 셋으로 넓히고 성공 기준을 안 따라가게 둔 것이 원인이다
  — 같은 판단을 두 곳에서 하면 언젠가 갈린다(`[162]`). 이제 `_기준()` 한 곳에서 온다.
- ★ 그대로 두면 사람이 **옛 규칙으로 만든 목록**을 붙여넣어 없는 번호를 긁는다
  (`[217]`·`[223]` — 14분에 수확 0). 즉 거짓 성공이 곧 사람의 헛수고였다.
- ★ **지운 것을 만든 것과 같은 숫자에 담지 않는다**(`[169]`). 전에는 둘 다 `made` 라
  "새로 2개"로 읽혔는데 실제로는 "2개 지웠다"일 수 있었다. 고친 뒤 실측 문구:
  `붙여넣기 옛 JS 1개 → 지움 1개(만들 것이 없어 — 다음 회차가 새로 만든다)`.
- 검증 `[305]`.

## ★ 캠프명 ↔ ERP 거래처코드 대조 — 유니웍스 기초등록 (2026-08-18 노승용 매니저 요청)

업무협조 요청: **① ERP 거래처코드(CU001)와 각자 엑셀의 캠프명이 100% 일치하는 것을
우선 유니웍스에 등록 ② 불일치·없는 것 찾아 등록·수정** — `US직구 허브넷` 인데 실제
캠프명은 `인천7캠프(허브넷)` 인 것, `00016 시흥1캠프(오류동)` 처럼 쿠팡 거래처코드
형식이 아닌 것, ERP 거래처코드로 검색이 안 되는 캠프. (담당 전상희·유현민·김미영)

- `python camp_code_match.py` · 결과 `reports/캠프_거래처코드_대조.{md,json,xlsx}` ·
  09:50 회차 단계(`캠프 거래처코드 대조`, **캠프 담당자 뒤**).
- ★ **찾아 주기만 한다. 아무것도 안 고친다.** ERP·유니웍스·원장·큐 어디에도 안 쓴다 —
  자동으로 짝지으면 **틀린 거래처코드가 유니웍스에 박히고** 그건 빈 칸보다 나쁘다(`[172]`).
  요청 3번(유니웍스 캠프 관리 화면)은 노승용 매니저 몫이고, 여기는 **1·2번의 찾기**다.
- ★ **원천마다 캠프 수가 다르다** — ERP 거래처 마스터 268 · 밴드 접수 글 737 ·
  원장 색인 198. 그래서 "미확인 캠프"의 답이 **기준에 따라 달라진다.** 셋을 다 모으고
  행마다 출처를 적는다. 한 원천만 보면 다른 원천에만 있는 캠프가 '없는 캠프'가 된다(`[169]`).
- **판정을 새로 만들지 않았다**(`[162]`) — 캠프명 맞추기는 `camp_contacts._norm`
  (숫자는 안 건드린다 — `송파1MB`·`송파5MB` 는 다른 캠프), 짝짓기는 회차가 이미 만든
  `거래처코드_색인.json` 의 `linked`·`ambiguous`·`unmatched`.

### 갈래와 실측 (2026-08-18 · 캠프 732개)
| 갈래 | 뜻 | 건수 |
|---|---|---:|
| **A** 바로 등록 가능 | 코드 있고 ERP 거래처명 = 캠프명 | 240 |
| **B** 이름 다름 | — **이 표로는 못 잰다**(아래) | 0 |
| **C** 코드 형식 다름 | `00016 시흥1캠프(오류동)`·`00028 송파3Sub-FC` 등 | **5** |
| **D** 후보 여럿 | `광주2MB(하산)` → CU371/CU372 등 | 6 |
| **E** 이름으로 못 찾음 | ERP 마스터에도 **거래처 원본에도** 댈 후보가 없다 | 238 |
| **F** 코드만 비었음 | ERP·원장에도 있는데 코드 칸이 빔 | 14 |
| **G** 이름이 다른 것으로 보임 ★요청 2번 | 후보가 **정확히 하나** — 지목(확정 아님) | **142** |
| **H** 후보는 있으나 확정 못 함 | 여럿이거나 지역 표기가 어긋난다 — 사람이 고른다 | **87** |

- ★ **B 가 0 인 것은 '이름 다른 캠프가 없다'는 뜻이 아니다.** 근거인 `캠프마스터.json`
  이 **`캠프명 == ERP거래처명` 인 것만** 담아서, 이름이 다른 캠프는 애초에 그 표에 안
  들어온다 — **구조상 B 로 못 걸린다.** 그러면 0 이 '없음'으로 읽혀 요청 2번이 통째로
  사라진다(`[169]`). 그래서 리포트가 **'이 표가 못 재는 것'** 을 맨 위에 스스로 적고,
  그 캠프들이 **E 로 떨어진다**는 사실을 밝힌다. 그래서 ERP 거래처 원본
  (`ESA001M.xlsx` 전체 2,981건)을 따로 읽어 **G·H 로 답한다**(아래).
- ★ **E 를 'ERP 에 없는 캠프'라 적지 않는다.** 밴드에서만 보이는 것뿐이고,
  다른 이름으로 있을 수 있다 — 그것이 곧 요청 2번의 대상이다.
- ★ **파싱이 깨진 조각은 표에서 빼되 숫자로 말한다**(`[169]`). 실측 `ambiguous` 에
  `)` 한 글자짜리 키가 있었고 그 후보가 `(주)대한민국문화재단` 같은 **남의 회사**였다.
- 엑셀은 담당자가 나눠 쓰도록 `담당자`·`처리결과` 빈 열을 같이 낸다(**새 파일**만 만든다).

### 요청 2번 — **지목(G)은 좁게, 보여 주기(H)는 넓게** (분담판 [122])

- 근거는 **ERP 거래처 원본 `ESA001M.xlsx` 2,981건**이고 대는 방법은 셋이다:
  **괄호 안 지명 · 도로명 주소 · 부분 이름**. 실측 G 142건의 근거 분포는
  괄호 지명 64 · 이름 일부 56 · 주소 22 다(한 건이 둘 이상을 갖기도 한다).
- ★ **비싼 탐색은 캐시 검사 뒤에**(`[168]`). `customer_index.load_customers()` 는 Z: 를
  재귀로 훑는다 — 같은 회차의 색인 단계가 이미 캐시를 갱신하므로 여기서는 **캐시를 읽는다**
  (실측 캐시 적중 · 받은 때를 리포트가 같이 적는다). **못 읽으면 '후보 없음'이 아니라
  '못 잰 것'이라 적는다**(`[169]`) — 안 그러면 G·H 0건이 '이름 다른 캠프가 없다'로 읽힌다.
- ★ **어려운 것은 잡는 게 아니라 잘못 지목하지 않는 것이다**(`[172]`). 이 표는
  **유니웍스에 코드를 적는 사람이 읽는다** — 틀린 코드가 박히면 빈 칸보다 나쁘다.
  프로토타입에서 실제로 걸린 오탐 넷을 문 넷으로 막았다:
  · `용인1MB(구갈동)` → `용인2MB(구갈동)` — **번호가 다르면 다른 캠프**다
    (`송파1MB`·`송파5MB` 와 같은 자리). 종류말(`모바일`/`MB`)만 다른 것은 안 막는다.
  · `MC12(삼척)` → `삼척파랑지게차` — 괄호 안이 **도시 이름**이면 그 도시의 남의 회사가
    전부 걸린다. 그래서 후보 풀을 **캠프처럼 생긴 거래처**로 좁혔다(2,981 → 363).
  · `남양주1MB(창동)` → `SR-ICH(원**창동**)` — 지명이 **남의 이름 안에 박힌 것**.
    **앞 경계만** 본다 — 뒤까지 막으면 `송하` 가 `송하동` 을 못 만난다.
  · `제주2MB(성읍리)` → `제주2MB(사계리)` — 괄호 지명이 어긋난다.
  · `송파3Sub-FC [= 송파SF-SPA3(FRESH)]` 처럼 **사람이 대괄호로 메모**를 적어 둔 이름이
    있어 번호 지문이 `33` 이 됐다. 이름을 읽기 전에 `[…]` 를 걷어낸다.
- ★ **문을 좁히다 요청의 본보기를 죽일 뻔했다 — 그래서 가르는 자리를 옮겼다.**
  `US직구 허브넷` 은 번호가 없어 지역 추출이 헛돌고(종류말 `허브` 를 뗀 `직구넷` 이
  '지역'으로 잡힌다) 지역 판정에 통째로 거절됐다 — **만들면서 검증이 그것을 잡았다.**
  반대로 지역 판정을 풀면 `MC제천(봉양)` → `양주2캠프(봉양동)`(제천 ↔ 양주)이
  **지목**으로 올라온다. 그래서 거절(`_reject`)과 **약한 근거**(`_weak`)를 갈랐다:
  지역이 어긋나면 **후보는 살리되 G 에서 내려 H 로** 보낸다. 캠프명이 실제로 바뀐 것
  (`부산5MB(중동)` → `해운대 중동MB`)도 조용히 사라지지 않고 근거와 함께 사람 앞에 선다.
- ★ **요청의 본보기가 '지목'으로 나오지 않았다 — 그것을 그대로 적는다.** 밴드에는
  `인천7캠프(허브넷)` 이라는 이름이 없고 `인천7 SH` 가 있는데, 그것은 **H 후보 둘**
  (`CU341 US직구 판토스` / `CU355 US직구 허브넷`)로 나온다. 둘 다 그럴듯하므로
  **기계가 고르면 안 된다.** 대신 진짜 개명 건은 잡혔다 —
  `인천7MB(마곡동) < 강서1MB(마곡동)에서 이름 변경` → **CU073**(괄호 지명 + 이름 일부).
- **아무것도 안 고친다**(이 절 맨 위 그대로). 리포트·엑셀이 `추정코드`·`추정근거` 를
  따로 내보이고 "**지목일 뿐 확정이 아니다**"를 행마다 적는다.
- ⚠ **검사가 독스트링에 속았다** — '캐시 검사가 먼저인가'를 글자 위치로 재는데
  독스트링이 `load_customers` 를 먼저 말해서 **멀쩡한 코드에 거짓 경보**가 났다.
  규칙을 세기 전에 **설명을 걷어낸다**(`[302]`·`[301]`⑨ 와 같은 자리).
- ⚠ **틀린 것이 코드가 아니라 내 기대였던 자리도 있다** — `양주2캠프` 의 후보가 1개인
  것을 '문이 무너졌다'고 검사했는데, `양주2캠프` 는 `양주2MB(봉양동)` 안에 안 들어가므로
  **후보 하나가 정상**이고 그 자리가 곧 G 다. 검증이 빨개지면 **코드부터 의심하기 전에
  기대를 확인한다.**
- 검증 `[309]` — 오탐 넷을 **실행으로** 재고(`[295]`), `_reject` 를 꺼서 오탐이
  되살아나는지 **계기 자신을 시험한다**(`[272]`).

## ★ 보호자가 "아직 실패가 아니다"라 적은 것을 장애라 부르지 않는다 (2026-08-18 실사고)

인계 맨 위의 **`[P0] 앱 서버 보호자가 장애를 보고함 — 고정 Funnel 외부경로 지연 감지
— 오탐 방지 재확인`** 에서 시작했다.

- **그 경보는 이미 풀린 것이었다.** 상태 파일은 `state: healthy · funnel_ok: true ·
  consecutive_funnel_failures: 0` 였다. 로그를 보면 `15:45:11 지연 감지` 바로 다음 줄이
  `15:46:13 정상 복귀` 다 — 그리고 15:45:10 은 **내가 앱 서버를 다시 띄운 시각**이다.
- **원인은 판정이 둘이었던 것이다.** `server_guard` 는 **한 번 늦은 것을 죽음으로
  단정하지 않는다**(`FAIL_LIMIT=3` — 세 번 연속일 때만 조치). 그런데 `system_audit` 은
  `state != "healthy"` **한 줄**로 갈라서 **첫 blip 부터 P0** 를 올렸다. 보호자가 스스로
  `오탐 방지 재확인` 이라 적어 둔 바로 그 상태를 감사기가 **장애**라고 부른 것이다.
- **실측이 그 크기를 말한다**: 그날 보호자 로그의 `지연 감지` **37회** 대 실제 재시작·
  실패 **1회**(로그 누적 153회). **경보가 대부분 가짜면 진짜 경보가 묻힌다**(`[170]`).
  앱 서버를 고쳐 다시 띄우기만 해도(`[156]` 이 시킨 그대로) 인계 맨 위가 빨개졌다.
- ★ **판정을 새로 만들지 않았다**(`[162]`) — 한도를 **보호자에게서 읽어 온다**
  (`system_audit.guard_limits()`). 여기 3 이라 적어 두면 보호자가 한도를 바꾼 날
  두 화면이 서로 다른 답을 한다. 검증이 보호자 상수를 바꿔 보고 따라가는지 잰다.
- ★ **내리는 것은 `*-degraded` 셋이 한도 아래일 때뿐이다.** `cooldown`(서버가 죽은 채
  재시작 과열 방지 대기) · `funnel-repairing`(이미 세 번 실패해 재등록 중) ·
  `restart-failed` · `guard-error` 는 **진짜 장애**라 그대로 P0 다. 낱말이 비슷하다고
  같이 내리면 못 잡는 것보다 나쁘다(`[172]`).
- ★ **못 읽으면 조용히 넘기지 않는다**(`[169]`). 한도를 못 읽었거나 연속 횟수 칸이
  비었으면 예전대로 P0 를 올리되 **"재확인 중인지 실패인지 가르지 못했습니다"** 라고
  적는다 — 확인 못 한 것을 이상 없음으로 세지 않는다.
- ⚠ **낱말이 어긋나면 한 건도 안 걸리면서 오류도 안 난다**(`[165]`). 상태 이름
  (`degraded`·`staff-degraded`·`funnel-degraded`)과 연속 실패 칸 이름이 보호자에
  실재하는지를 검증이 **매번 대 본다** — 보호자가 이름을 바꾸면 이 완화가 통째로
  죽으면서 모든 blip 이 다시 P0 가 된다.
- 검증 `[307]` — 실행으로 잰다(`[295]`). 합성 딕셔너리만 쓰고 실측 증거 파일은 한
  글자도 안 건드린다(`[247]`). **계기 자신도 시험한다**(`[272]`) — 상태 표를 비우면
  blip 이 다시 P0 가 되는지 확인한다.

### ⚠ 파이썬으로 파일을 고칠 때 **줄끝을 바꾸지 말 것** (같은 날 실사고)

- `[302]`(도면 별도 창)의 **계기 자기시험**이 빨갛게 떴다 — *"미리보기에 손잡이를
  일부러 달았는데도 검사가 통과했다 — 이 검사는 아무것도 안 재고 있다"*. 그 검사는
  node 로 `index.html` 을 읽어 `return interactive\n      ? ...` 를 갈아 끼워 고장을
  주입하는데, **그 앵커가 안 맞았다.**
- **범인은 내 편집이었다.** `io.open(p, "w", encoding="utf-8")` 는 텍스트 모드라
  `\n` 을 **`os.linesep`(윈도우에서 `\r\n`)** 으로 바꾼다. 그래서 한 줄을 고치려고
  읽고 쓴 것이 **파일 전체 줄끝을 LF→CRLF 로 갈아 버렸다.** 실측: 14:50 까지 LF 였고
  15:45 내 커밋부터 CRLF 21,517줄 — 파일 **일곱 개 · 약 48,000줄**이 그렇게 됐다.
- ★ **파이썬 텍스트 모드는 CRLF 를 읽을 때 `\n` 으로 접어 준다**(universal newlines).
  그래서 **파이썬으로 확인하면 앵커가 멀쩡히 맞는다** — node·git·다른 도구만 안 맞는다.
  같은 파일을 놓고 두 도구가 다른 답을 하는 자리이고, **고친 사람만 모른다.**
- ★ **`.py`·`.md` 는 티가 안 났다** — `core.autocrlf=true` 가 커밋할 때 되돌려 준다.
  그런데 `index.html` 은 git 이 손을 안 대서 **CRLF 가 그대로 커밋됐다**(공백만 바뀐
  43,034줄 차이가 진짜 변경 위에 얹혔다). **한 파일만 보고 '괜찮다'고 하면 안 된다.**
- 그러므로 파일을 고칠 때는 **`newline=""` 을 준다**: `io.open(p, encoding="utf-8",
  newline="")` 로 읽고 같은 인자로 쓴다. 이미 어긋났으면 바이트로 되돌린다
  (`open(p,"rb")` → `b.replace(b"\r\n", b"\n")` → `open(p,"wb")`).
- ★ **정본과 사본 세 벌은 바이트가 같아야 한다**(`ecount/CLAUDE.md` · 루트 둘).
  줄끝만 달라도 다른 파일이다 — 되돌린 뒤 반드시 다시 맞춘다.

### ⚠ 글자 검사가 **고장을 지키고 있었다** — `t162` (같은 날)

- `[305]` 는 워치독 자가치유의 성공 판정을 `js_mt` → `_기준(p)` 로 고친 것이다. 그런데
  `t162` 가 `assert "os.path.getmtime(p) >= js_mt" in heal` 로 **그 `js_mt` 를 글자로
  못 박아** 두고 있었다 — 즉 **고친 순간 검증이 빨개진다.**
- 더 나쁜 것은 그 검사 **바로 위 주석**이 *"문자 그대로 못 박으면 기준을 세분화할 때마다
  검증이 헛되이 깨진다"* 라고 이미 경고해 두고, 다음 줄이 정확히 그것을 한 것이다.
- 지키는 정책은 그대로 뒀다(성공을 **mtime 으로** 확인하고 안 써졌으면 지운다).
  글자만 풀었고, '낡음과 성공이 같은 기준인가' 는 `[305]` 가 따로 지킨다.
- **글자 검사는 '있어야 할 것'이 아니라 '되돌아가면 안 되는 것'에 쓴다**(`[39]`).

### ⚠ 검증번호는 옆 세션과 겹칠 수 있다 (같은 날)

- `[306]` 을 붙였는데 **옆 세션이 같은 번호를 먼저 썼다**(`t306_dead_server_revival…`,
  분담판 `[115]`). 작업을 시작할 때 센 최대값은 305 였고 그 사이에 늘어난 것이다.
  함수 이름이 달라 파이썬은 아무 말도 안 하고 **화면에만 같은 번호가 둘** 찍힌다.
- 그래서 **번호는 붙일 때가 아니라 커밋 직전에 다시 센다.** 이번 것은 `[307]` 로 옮겼다.

## ★ 도면 창은 전체 화면으로 연다 (2026-08-18 지시)

사용자 지시: **"도면 열기를 누르면 전체 화면으로 나오게 코딩해"**

- 공용 시트는 넓은 화면에서 `width:min(620px,92vw)` 다. 그래서 `[113]` 이 도면을
  별도 창으로 옮긴 뒤에도 **1400×820 작업면이 620px 안에 갇혀** 좌우가 잘렸다.
  실측(전이를 끄고 잰 값): 도면 창 **x0 y0 1265×800**(화면 전체) · 보통 시트
  **x323 y64 620×672**(그대로).
- ★ **시트를 새로 만들지 않았다**(`[162]`) — `layerOpen(el, {full:true})` 갈래 하나이고,
  끄는 자리는 `setSheetContent` **한 곳**이다(내용이 바뀌면 저절로 보통 크기로 돌아온다).
  화면마다 전체 화면 코드를 따로 두면 사본이 여럿 된다.
- ★ **순서가 뜻을 갖는다** — `.sheet.full.open{transform:none}` 은 `@media(min-width:900px)`
  **뒤**에 와야 한다. 앞에 두면 데스크톱 규칙이 이겨 **창이 화면 밖에 선다.**
- ★ **넓히는 것은 도면 하나뿐이다**(`[172]`). 모든 시트를 전체 화면으로 만들면
  목록·확인창까지 화면을 덮는다. 검증이 `full:true` 가 **한 곳뿐**인지 센다.
- ⚠ **숨은 탭에서는 CSS 전이가 안 끝난다.** 브라우저 도구로 재면 `document.hidden`
  이라 시트가 `translateY(100%)` 에 멈춘 채로 잡힌다 — **고친 것이 안 고쳐진 것처럼
  보인다.** 그때는 `style.transition='none'` 으로 끄고 최종 상태를 재야 한다.
  실제로 이날 그 값을 보고 잠깐 제 변경을 의심했다. 이 착시를 모르면 멀쩡한 코드를
  고치러 간다(`[172]`).
- ⚠ **`.flow-vscroll` 은 전체 화면에서만 높이를 묶는다**(`calc(100dvh - 250px)`) —
  그래야 도구 줄이 화면에 남고 도면만 스크롤한다. 좁은 화면 기본값은 안 건드렸다.
- 작업면 좌표(1400×820)는 **그대로**다(`[276]`) — 화면 크기에 따라 좌표를 다시
  계산하면 PC 와 폰이 서로 다른 배치가 된다.
- 검증 `[308]`.

## ★ 안 보이는 단추는 **크기가 아니라 CSS 가 없는 것**일 수 있다 (2026-08-18 지시)

사용자 지시: **"캠프 추가, 수정, 저장 등 아이콘이 크게 보이게 해 (담당자가 찾아야되잖아
잘 안보여)"**

- **크기를 키우러 갔다가 원인이 다른 것을 만났다.** 실측: `.btn`·`.btn.xs`·`.chk` 의 CSS
  등장 횟수가 **0** 이었다 — 같은 날 캠프 편집 기능을 만들면서 **마크업만 넣고 모양을
  안 만들었다.** 그래서 다섯 자리(캠프 추가·수정·자동값으로·닫기·저장)가 전부
  **브라우저 기본 단추**로 그려졌고, 어두운 화면에서는 작은 회색 글자였다.
  **오류도 안 나고 코드에는 `class="btn"` 이 적혀 있어 읽는 사람은 모양이 있는 줄
  안다**(`[169]`) — 화면을 열어 본 사람만 안다.
- ★ **"안 보인다"는 말을 들으면 키우기 전에 "그 클래스에 규칙이 있나"를 본다.**
  없는 것을 키우면 아무 일도 안 일어난다.
- 실측(브라우저 · 넓은 화면 1200px): 캠프 추가 **93×44**(아이콘까지 119) · 표 안 수정
  **52×36**(아이콘까지 75) · 닫기·저장 **112×44** · 켬/끔 164×44 · 아이콘 19px·16px.
  좁은 화면에서는 닫기·저장이 한 줄을 나눠 갖는다(담당자 폰에서 손가락이 닿아야 한다).
- ★ **쓰는 자리가 캠프 화면 다섯 곳뿐이라** 여기서 정해도 다른 화면은 안 바뀐다
  (`[172]` — 문제 없는 쪽의 모양을 말없이 바꾸지 않는다). 표 안의 `수정` 만 36px 로
  낮추되 **테두리와 색으로 단추임을 말한다** — 줄마다 44px 면 표가 부푼다.
- ★ **눌림 표시로 배경색을 바꾸지 않는다.** `--brand-dk` 는 **어두운 테마에서 밝은
  파랑**(`#A8C8FF`)이라 흰 글자가 명암비 **1.7** 이 된다 — 명암비 검증이 잡았다.
  밝기만 올리면 글자·바탕 짝이 그대로라 어느 테마에서도 안 무너진다.
- ⚠ **이 고침에는 서버를 안 내려도 된다** — `app_server` 가 요청마다 `index.html` 을
  디스크에서 읽는다. 화면 새로고침이면 된다. 담당자가 쓰는 중이면 `restart_server` 가
  먼저 묻는데(`[265]`) **그 물음을 무시하고 내리지 않는다.**
- ⚠ **숨은 탭에서 잰 값은 좁은 화면 값이다** — 브라우저 도구로 재면 `innerWidth` 가
  **0** 으로 나와 `max-width` 미디어쿼리 갈래로만 잡힌다(`[308]` 의 전이 착시와 같은
  자리). 넓은 화면을 재려면 **폭을 정한 iframe 안**에서 잰다.
- 검증 `[310]` — 캠프 화면만 보지 않는다. **단추처럼 생긴 클래스 전부**를 훑어 규칙이
  없는 것을 잡고(실측 9개 중 누락 0), 계기 자신도 시험한다(`[272]`).
  ⚠ 글자로 자리를 찾을 때는 **그 글자가 설명에도 있다고 생각한다** — 첫 판이
  `캠프 추가` 를 찾다가 지시를 그대로 옮겨 적은 **CSS 주석**에 먼저 걸렸다(`[301]`⑨).


## ★ 캠프마다 **호기**를 보여 준다 — 원천은 밴드 본문이다 (2026-08-19 지시)

사용자 지시: **"각 캠프별로 호기가 있을 텐데 호기도 보이는 구조로 정리해"**

- ★ **원천을 짐작하지 않고 재 봤다**(`[177]`). 두 후보의 크기가 열 배 넘게 달랐다:
  · 정기점검 점검내용(`pm_content` DB) → 호기가 읽힌 캠프 **55개**
  · **밴드·카톡 접수 글 본문** → **593개**
  점검내용만 보고 만들었으면 대부분이 '모름'으로 나오고, 그 '모름'은 **자료가 없어서가
  아니라 안 본 것**이라 아무도 눈치 못 챈다(`[169]`). 실측 결과 **정기점검 캠프 400개
  중 399개(99.8%)** 에 호기가 붙었다(전체 734개 중 590개, 80.4%).
- ★ **호기를 새로 읽지 않는다**(`[162]`) — `pm_content.parse_units` 한 곳이 판정한다.
  거기는 `1,2호기`·`1~3호기`·`#4`·`3호` 를 다 받고 **1~99 만** 인정해 날짜·금액 조각을
  호기라고 우기지 않는다. 캠프 화면이 제 정규식을 쓰면 **같은 글을 놓고 정기점검 화면과
  다른 호기를 말한다.** 화면에서 글자를 만드는 자리도 `campUnits` **하나**다(표·엑셀·캡처).
- ★ **'관측된 호기'이지 '보유 대수'가 아니다**(`[169]`). 접수·점검 글이 한 번도 없던
  호기는 안 보이므로 `1,2,5` 처럼 **사이가 빈다**(실측 정기점검 캠프 14개). 그것을
  '3·4호기는 없다'로 읽으면 안 되므로 화면·엑셀이 **`(사이 빔)` 이라 적고 뜻을 설명한다.**
  못 읽은 캠프는 빈 칸이 아니라 **`모름`** 이다 — 엑셀에서 빈 칸은 '없다'인지 '못 찾았다'
  인지 구별이 안 된다.
- ★ **호기 하나 때문에 캠프 목록을 못 만들지 않는다** — `_units` 는 실패하면 빈 것으로
  두고 넘어간다. 거기서 예외를 올리면 연락처까지 같이 잃는다.
- 검증 `[311]` — 구조(판정·글자 자리)와 **실행**(node 로 `campUnits` 를 돌려 이어진
  호기·사이 빔·모름·설명 네 가지를 결과로 잰다, `[295]`).


## ★ 상태 카드는 **보여 주기만 하지 않는다** — 누르면 그 자리에서 돈다 (2026-08-19 지시)

사용자 지시: **"여기서 해당 카드 누르면 바로 실행되게 기능 추가, 엑셀 자동 보관본
카드도 누르면 엑셀 보관 바로 되게 업그레이드"**

- 전체 자동화 상태 카드 여섯 장(마지막 처리·카카오톡·Band·ERP·앱 DB·Excel 보관본)은
  **읽기만** 하는 자리였다. 무엇이 막혔는지는 보여 주는데 **거기서 할 수 있는 일이
  없어서**, 사람은 카드를 보고 아래 단추 줄로 내려가거나 아예 다른 화면으로 갔다.
- **무엇이 도는지 정하는 자리는 하나다**(`[162]`) — `automationAction(key)`. 카드
  그리기·클릭·검증이 전부 이것을 읽는다. 표를 둘로 두면 화면에 적힌 말과 실제로 도는
  명령이 갈리고, 그때는 **눌러 놓고도 무엇이 돌았는지 아무도 모른다.**
- ★ **엑셀 카드는 제 확인창을 만들지 않는다** — 이미 있는 `applyExcelNow`(→ `ledger_now`
  = `archive_worker.py --run`)를 그대로 부른다. 여기서 또 물으면 **두 번 묻는다.**
  그래서 `ask` 는 회차를 돌리는 넷에만 있다.
- ★ **앱이 못 하는 일을 할 수 있는 것처럼 적지 않는다**(`[172]`). 밴드를 새로 긁는
  것도 ERP 에 로그인하는 것도 앱은 못 한다 — 그 카드의 단추는 **'이미 받아 둔 자료를
  앱 DB 까지 잇는'** 증분 회차이고, 확인창이 그 경계를 그대로 말한다. 숨기고 돌리면
  "돌았는데 왜 그대로지"가 된다.
- ★ **막힌 것은 확인창이 먼저 말한다**(`[169]`). ERP 로그인 게이트가 서 있으면
  *"지금 막힌 것 — … (이 회차로는 안 풀립니다)"* 를 desc 에 싣는다.
- ★ **누를 수 있는 카드만 `<button>` 이다.** 못 하는 자리를 눌리게 두면 눌러도 아무
  일이 없는데 화면은 멀쩡해 보인다 — 이 프로젝트가 반복해 당한 모양이다.
  그리고 카드가 **무엇을 할지 스스로 적는다**(`누르면 Excel 보관본 지금 생성`).
- ★ **도는 중이면 다시 걸지 않는다**(`[257]`). 같은 회차를 두 번 띄우면 잠금에 막혀
  조용히 건너뛰거나 같은 자료를 두 번 긁는다. 그때 카드는 `disabled` 이고 '지금 처리
  중입니다'라고 적으며, 눌러도 토스트만 뜬다.
- **아래 단추 줄은 안 건드렸다**(`[172]`) — 문제 없는 쪽의 동작을 말없이 바꾸지 않는다.
- 검증 `[312]` — node 로 카드 화면을 **실제로 그려** 여섯 갈래가 무엇을 부르는지,
  도는 중에 안 도는지, 확인창이 몇 번 뜨는지를 결과로 잰다(`[295]`). 엑셀 갈래를
  없애 보고 **잡히는지**까지 확인한다(`[272]`).

## ★ 같은 주소는 모아서 순차적으로 · 호기는 캠프명 옆 (2026-08-19 지시)

사용자 지시: **"캠프명 옆에 호기를 추가하고 같은 캠프 같은 주소는 모아서 순차적으로
보여줄 수 있는 구조로 변경해"**

- 호기 열이 담당자 아홉 칸 **뒤에** 있어 캠프명과 한눈에 안 읽혔다. 이제 표·엑셀 모두
  `캠프명 · 호기 · (주소묶음) · …` 순이고, 대표 보고 이미지에도 호기 열이 들어간다.
- ★ **묶는 근거는 주소 하나다**(`[172]`). 캠프명이 닮았다고 묶으면 `송파1MB(감일동)`
  과 `송파5MB(감일동)` 처럼 **실재하는 다른 캠프**가 한 덩어리가 되고, 그 목록은
  사람을 엉뚱한 현장으로 보낸다 — 못 묶는 것보다 나쁘다.
- ★ **주소를 모르는 것끼리는 안 묶는다**(`[169]`). '모름'은 같은 곳이라는 뜻이 아니다.
  실측 2026-08-19 정기점검 400개: 주소 모름 **1개** · 같은 주소 묶음 **97개 245행**
  (61%) · 최대 묶음 11행 · **끊긴 묶음 0**.
- ★ **순서는 묶음 대표 캠프명으로 잡는다.** 묶음 키(주소)로 정렬하면 관계없는 캠프
  사이로 묶음이 튀어 사람이 목록에서 캠프를 못 찾는다. 대표 이름으로 자리를 잡으면
  목록 전체는 지금처럼 가나다 순으로 읽히면서 같은 주소끼리만 붙는다.
- ★ **거르고 나서 묶는다.** 화면에 안 나오는 줄까지 세면 '3곳 중 1'만 보이고 나머지
  둘은 어디에도 없다 — 그 숫자가 거짓말이 된다(`[169]`).
- **줄 색을 바꾸지 않고 왼쪽 선만** 준다(이미 있는 짝수 줄 얼룩과 겹치면 흐려진다).
  묶음이 이어지면 색을 번갈아 써 **어디서 다음 묶음이 시작되는지** 보이게 한다.
- ⚠ **이미지 열을 늘렸으니 폭 계산을 다시 했다.** 호기 96px 을 넣고 최근작업
  (104→76)에서 28px 을 되찾아 이메일은 164→139px 만 줄었다. 역할 머리글 자리도
  `cols[1+i*3]` → **`cols[ROLE0+i*3]`** 로 바꿨다 — 자리를 손으로 적어 두면 열을
  늘린 날 **머리글만 어긋나면서 오류는 안 난다.**
- ⚠ **JS 식별자를 키워드에 붙여 쓰지 말 것** — 한글도 식별자라 `await눌러` 는 **한
  낱말**로 읽힌다. 문법 오류가 아니라 ReferenceError 라 **실행해야만** 드러난다.
- ⚠ **직접 `eval` 안의 `let`·`const` 는 그 eval 안에만 산다.** 화면 조각을 둘로 나눠
  eval 하면 서로를 못 보고, 밖에서 `CAMPS` 를 넣어도 안 닿는다 — 검증이 **아무것도
  못 재면서 통과**할 뻔했다. 이어 붙여 한 번에 eval 하고 맨 앞 선언만 `var` 로 편다.
### 축소해야만 다 보이던 표 — 숫자에는 이름을, 칸에는 줄바꿈을 (2026-08-19 지시)

형님 물음·지시: **"정기점검 아래 82,79 숫자가 무엇을 의미하는지 설명 필요"** ·
**"이 화면을 축소를 해야만 이렇게 다 보이는데 반응형으로 확대해도 다 보이게 정리해"**

- **숫자에 이름이 없었다.** 열 이름이 `정기점검` 뿐이라 82·79 가 회차인지 대수인지
  남은 횟수인지 알 수 없었다. 실제 값은 **밴드·카톡에서 모은 정기점검 접수 글의
  누적 건수**다. 이제 열이 `정기점검 접수 / 밴드·카톡 누적 글 수` 라고 스스로 말하고,
  엑셀 열 이름도 `정기점검 접수 글수` 로 같이 바꿨다(화면과 엑셀이 다른 이름을 쓰면
  받아 본 사람이 서로 다른 값으로 읽는다).
- **표를 넓힌 범인은 세 가지였다** — 셋 다 재서 고쳤다:
  · **호기 한 칸이 40자**였다(`1,2,3,…,16호기`). 이어진 번호를 **범위로 줄인다**
    → `1~16호기`. 가장 긴 칸 **40 → 15자** · 73개 캠프가 줄었다. ★ **줄이는 것이지
    감추는 것이 아니다** — 사이가 비면 `1~3,5호기` 처럼 **그 자리에** 드러난다(`[169]`).
    둘짜리(`1,2호기`)는 그대로 둔다(제일 흔하다).
  · **딱지가 이름과 한 줄**이었다(`강병MC(범일로) 같은 주소 2곳 중 1`). 딱지를 **둘째
    줄**로 내리고 화면 글자는 `주소 1/2` 로 줄였다 — 문장(`같은 주소 2곳 중 1`)은
    tooltip 과 엑셀에 그대로 남는다.
  · **공용 규칙이 모든 칸을 `nowrap`** 으로 묶고 있었다(`.gridwrap table.grid td`).
    열이 아홉인 이 표에서는 그것이 곧 가로 스크롤이다. 캠프 표만 줄바꿈을 허용한다.
- ⚠ **전화번호는 예외로 안 끊는다**(`td a{white-space:nowrap}`). 한 번호가 두 줄로
  갈라지면 **잘린 것처럼 읽힌다** — 이 프로젝트가 번호로 크게 당한 자리다(`[295]`).
- ★ **열을 감추지 않았다**(`[169]`). 좁으면 접을 뿐, 안 보이는 열을 만들지 않는다 —
  안 보이는 열은 빈 칸과 구별되지 않는다.
- 검증은 **되돌아가면 안 되는 것**만 얼린다(`[39]`): 범위 축약 · 구멍이 드러남 ·
  줄바꿈 허용 · 전화번호 nowrap · 열 이름이 무엇인지 말함.

- 검증 `[315]` — 같은 주소가 **붙어 있는지** · 모름끼리 **안 묶이는지** · 호기가
  캠프명 옆인지(표·엑셀)를 node 로 실행해 잰다. '모름은 안 묶는다' 문을 열어 보고
  **잡히는지**까지 확인한다(`[272]`).


## ★ **지난 예정은 '예정'이 아니다** — 같은 현장이 예정·미처리에 같이 떴다 (2026-08-19 지시)

사용자 지시: **"대표 보고 캡처 화면에서 정기점검 예정에도 떴는데 정기점검 미처리에도
뜨는 현장 알고리즘 구성해서 방지해, 오늘 날짜가 8월 19일인데 정기점검 예정이 8월
11일이야 이런 건들은 정기점검 미처리로 보내"**

- 실측 그날 대표 캡처: `pm_plan` **2건이 전부 지난 날짜**였고 **둘 다 미처리와 겹쳤다.**
  `UJ2601431 부산4MB(망미동)` 이 **'정기점검 예정 1건'** 목록과 **'정기점검 미처리
  11건'** 목록에 **같이** 떠 있었다. 겹쳐 보이면 대표는 그 현장이 **두 건인 줄 안다.**
- **막던 규칙은 반쪽이었다.** `_drop_served_pm_plans` 는 *완료된* 예정만 내렸다
  (`[281]`) — **완료되지 않은 채 날짜만 지난** 예정은 아무 문이 없었다. 예정은 류지영
  스케줄 원본, 미처리는 관리대장 04시트에서 오므로 **두 원천이 각자 옳은 말을 하면서**
  화면에서 한 현장이 둘이 된다. 오류도 빈칸도 없어 아무도 안 본다.
- ★ **조용히 지우지 않는다**(`[169]`). 갈래가 둘이다:
  · 미처리에 **같은 현장이 이미 있으면** 예정만 내린다 — 그쪽이 경과일·미처리사유까지
    말하므로 더 낫다(실측 2건 다 이 갈래).
  · 미처리에 **짝이 없으면 `pm_overdue` 로 옮긴다.** 스케줄 원본에만 있는 예정을 그냥
    지우면 **그 현장은 아무도 안 가는데 목록에도 없다** — 겹쳐 보이는 것보다 나쁘다.
- ★ **옮긴 것은 근거를 그대로 적는다**(`[172]`) — `근거는 류지영 정기점검 스케줄 원본` ·
  `원장(04_정기점검)에 이 예정의 짝이 없다`. 원장이 근거라고 말하면 사람이 **원장에서
  없는 행을 찾아 나선다.**
- ★ **예측(`pm_pred`)은 안 건드린다.** 빗나간 예측은 미처리가 아니다 — 그것을 미처리라
  부르면 **없는 현장이 대표 보고에 뜬다.** 대표 캡처는 예측을 이미 뺀다(`[281]`).
- ★ **관문이 둘인 것은 그대로 둔다**(`[281]` 이 세운 구조) — 서버 `_drop_served_pm_plans`
  와 화면 `calReportRowsExclusive`. 화면 쪽은 **낡은 응답·오프라인 사본**이 대표 보고로
  들어오는 길이라 같은 판단을 한 번 더 한다. **그래서 둘 다 고쳤고 둘 다 잰다** — 한쪽만
  고치면 서버를 다시 띄우기 전까지 폰에서는 그대로 겹쳐 보인다.
- ★ **`today` 를 인자로 받는다.** 검증이 날짜를 **못 박아** 재게 하려는 것이다. 실제
  오늘로만 재면 검사에 적힌 날짜가 지나는 순간 **뜻이 날마다 달라진다** — `t281` 이
  실제로 그 상태였다(8월 날짜를 못 박아 두고 실제 오늘과 비교했다).
- 실측(고친 뒤): `pm_plan` **2 → 0** · 예정∩미처리 겹침 **2 → 0** · 아직 남은 지난 예정
  **0** · 미처리로 옮긴 것 **0**(오늘은 둘 다 짝이 있었다 — 갈래는 살아 있다).
- 검증 `[318]` — 서버와 화면 **둘 다 실행해서** 잰다(`[295]`). 짝 있는 것은 하나만 남는지 ·
  짝 없는 것은 **안 사라지는지** · 예측은 그대로인지 · 앞날 예정은 남는지.

### ⚠ 관문 자신이 회차를 죽이고 있었다 — 합성검증 600초 (같은 날 실사고)

- 09:50 일일대조가 `시간초과(600s)` 로 죽었다. 그런데 그 **600 은 `run()` 의 기본값**이지
  이 단계에 맞춰 정한 값이 아니었다. 실측(한가한 기계) **395.7초** — 여유가 **1.5배**뿐이고,
  09:50 은 하필 제일 바쁜 시각이다(09:35 원본정리 · 증분 파이프라인 5분마다 · 둘 다 Z: 를
  훑는다). 그래서 **아무 코드도 안 고쳤는데** 바쁜 아침마다 회차 전체가 **첫 줄에서** 죽고,
  스케줄러에는 `exit 1` 만 남는다.
- `GATE_TIMEOUT_S`(기본 1500초 · `COUPANG_GATE_TIMEOUT_S`) 로 뽑았다. **실측이 이 값의
  절반을 넘으면 늘릴 때가 아니라 검증을 나눌 때다** — 검증은 계속 는다(지금 `[318]`).
- ⚠ 회차 예산(`ROUND_BUDGET_MIN` 150분)보다 훨씬 작아야 한다. 관문 하나가 예산을 다
  먹으면 완주해도 남는 단계가 없다(`[180]`).
- 이 자국은 `[304]` 가 만든 `reports/일일대조_오류.json` 이 남겼다. **그것이 없었으면
  이번에도 `exit 1` 다섯 글자뿐이었다.**

### ⚠ 파일마다 줄끝이 다르다 — 읽고 쓸 때 그 파일의 것을 지킨다 (같은 날)

- `webapp/app_server.py` 는 **CRLF**(실측 10,072/10,108), `webapp/index.html` 은 **LF** 다.
  2026-08-18 에 배운 `newline=""` 만으로는 모자라다 — 그것은 *바꾸지 않는다*는 뜻일 뿐이고,
  **넣는 글**이 `\n` 이면 CRLF 파일 안에 LF 줄이 섞인다.
- 그러므로 고칠 때는 **바이트로 세어 그 파일의 줄끝을 정하고**(`raw.count(b"\r\n")`),
  universal newlines 로 읽어 `\n` 으로 다루고, **쓸 때 원래 줄끝으로 되돌린다.**
  안 그러면 커밋이 파일 전체 차이로 부풀어 **진짜 변경이 묻힌다.**
- ⚠ **한글이 자모로 분해돼 코드에 들어갈 수 있다**(편집기·도구 경유). 분해된 `캠프명` 은
  사전 키 `캠프명` 과 **안 맞으면서 오류도 안 난다** — 조용히 0건이 된다(`[165]` 모양).
  그래서 패치 스크립트는 자리표(`__CAMP__`)로 쓰고 **NFC 로 정규화해 넣은 뒤**,
  `U+1100~U+11FF`(자모)가 남아 있으면 **실패시킨다.**


## ★ 갈래를 더하는 것과 한도를 푸는 것은 다르다 — 리모컨 (2026-08-19 지시)

사용자 지시 셋이 같은 표·같은 API·같은 화면이라 **한 번에** 붙였다(따로 하면 두 번
고치고 두 번 검증한다): **"리모컨 카테고리 이것도 추가 해서 관리할 수 있도록 반영"** ·
**"이것도 기능 추가해"**(화면 오류 `불출 기록 실패: 수량은 1~3개여야 합니다`) ·
**"엑셀 안받고 정리할 수 있게 해"**.

- 막고 있던 것은 두 줄이었다 — `remote_request` 의 `1 <= qty <= REMOTE_HOLD_LIMIT` 와
  `_remote_holdings` 보유 한도. **둘 다 '받는 쪽이 사람'을 전제**한다. 그래서 류지영
  매니저가 *"리모컨 VER.4 부산공장으로 30개 이동할껀데 불출은 AS기사님들만 있는거같아요"*
  라고 막혔다 — 공장은 사람이 아니라 3개 한도가 뜻이 없다.
- ★ **한도를 넓히지 않았다**([172]). 그것은 부사장 승인 규칙이다 — 넓히면 규칙이
  조용히 사라지고 **아무 화면에도 안 뜬다.** `to_kind`(AS담당자 | 공장·지사) 갈래를
  하나 더했고 **AS담당자 길의 두 검사는 한 글자도 안 바뀌었다.** 검증이 먼저 재는 것도
  '되는 것'이 아니라 **'안 풀리는 것'** 이다.
- ★ **받는 지점 재고는 파생으로 센다 — 짝 행을 만들지 않는다**([162]). 보내는 지점은
  이미 `issued` 로 줄어드는데 받는 쪽이 안 늘면 그 재고가 영영 모자라 보인다. 그렇다고
  `remote_stock` 에 +N 행을 같이 만들면 **원본 한 줄을 지울 때 그 행이 안 따라와**
  30개가 허공에서 생긴다(되돌릴 수 없는 쪽이다). `_remote_branch_stock` 이 원본에서
  세므로 수정·삭제가 저절로 반영된다 — 검증이 '지우면 0으로 돌아오는지'를 잰다.
- ★ **모르는 곳으로는 안 보낸다**([169]). 받는 곳이 부산공장·시화공장·증평본사 중
  하나로 안 읽히면 거절한다 — 받아 두고 어느 재고에도 안 넣으면 **기록은 남는데 물건은
  사라진** 모양이 된다. 보내는 곳과 받는 곳이 같아도 거절한다.
- ★ **공장 이동분이 '기사 보유'로 세이면 유령 보유자와 거짓 한도초과가 생긴다**([165]).
  `_remote_holdings` 가 `to_kind` 를 같이 본다. 2026-08-11 의 이름 검사(지점 직납)는
  **그대로 뒀다** — 그것은 `to_kind` 가 없던 옛 기록을 받는다.

### 엑셀을 기다리지 않는다 — '왜'와 '어디에'
- 김미영 대리 요청(류지영 전달): *"사유랑(고장이면 고장 왜 교체하는지) 이동식이면
  이동식 4RT 몇호기에 들어갔는지도 다 정리해야한데요 엑셀로 만들어준다하셨는데"*.
  **엑셀은 선택이다** — 사람 입력 창구는 앱 하나다(2026-08-11 규칙). `remote_issue` 에
  `reason`·`fault_detail`·`equip_kind`·`equip_spec`(예: 4RT)·`unit_no` 를 더했다.
- ★ **낱말은 `ledger_db` 한 곳에서 온다**([162][166]) — `REMOTE_REASONS` ·
  `REMOTE_REASON_NEEDS_DETAIL` · `REMOTE_EQUIP_KINDS` · `REMOTE_TO_KINDS`. 화면 선택지도
  API 검사도 이 표를 읽으므로 **목록을 늘리면 화면이 따라온다.** 검증이 화면 코드에
  `신규설치`·`고정식` 같은 낱말이 박히는 것을 막는다.
  · 지금 목록은 [127] 에 적어 둔 후보 그대로다(신규설치·고장교체·분실·회수·
    공장·지사 이동·기타). **실제 업무 말과 다르면 그 한 줄만 고친다.**
- ★ **필수는 근거가 설 때만 건다**([172]): 고장교체·기타는 왜인지가 곧 그 사유이고,
  호기는 **이동식일 때만** 묻는다. 고정식에 호기를 강요하면 아무 값이나 들어온다.
- ★ **목록 밖 값은 지우지 않는다**([196]) — 고치기 폼이 그대로 고를 수 있게 두고
  `— 목록 밖` 이라고 말한다. 없애면 "그때 정말 뭐라고 적었나"를 잃는다.
- ⚠ **`to_kind` 는 고치기 폼에 안 넣었다.** 그 값이 바뀌면 보유·재고가 통째로 다른
  곳으로 옮겨 가는데 그 폼은 그 뜻을 사람에게 못 물어본다. 잘못 적었으면 **지우고
  다시 적는다**(지운 것은 원장에서 되돌릴 수 있다). 검증이 이 문을 지킨다.
- ★ **화면은 막지 않는다 — 보여 줄 뿐이다.** 무엇이 필수인지는 `remote_purpose`
  한 곳이 정한다. 화면에 같은 검사를 적으면 언젠가 갈리고, 갈린 뒤에는 어느 쪽이
  맞는지 아무도 모른다. 다만 **적어 둔 글이 있으면 칸을 접지 않는다** — 접으면 쓴 것이
  조용히 사라진 것처럼 보인다.
- 실측(임시 DB): 증평 100 → 67(기사 2+1 · 부산 이동 30) · 부산 0 → **30**(VER.4 30) ·
  유령 보유자 0 · 거짓 한도초과 0 · 이동 행을 지우면 부산 재고 **0 으로 복귀**.
- 검증 `[319]` — 서버와 화면을 **둘 다 실행해서** 잰다([295]). 계기 자신도 시험했다
  ([272]): 한도를 넓히면 · 받는 곳 판정을 죽이면 · 사유 필수를 풀면 **셋 다 잡힌다**.

### ⚠ 필수를 더하면 **옛 호출자**를 같이 고친다 (2026-08-19 실사고, 같은 날 이어서)

- 위 변경이 `reason` 을 필수로 만들면서 새 검증 `[319]` 은 붙였는데 **옛 검증
  `t98` 의 호출 여덟 자리를 안 고쳤다.** 그래서 그 세션이 끝난 뒤 **관문이 빨간 채로
  남았고**, 관문은 `daily_run` 의 **0단계**라 09:50 회차가 매일 통째로 죽는다.
  실패 문구는 `합성검증 실패 — 전체 중단` 한 줄뿐이라 **무엇이 깨졌는지 안 나온다.**
- ★ **음성 검사가 제일 위험했다.** 한도 초과·지점 제한·재고 초과를 재는 세 검사는
  `ValueError` 를 기다리는데, 사유가 없으면 **그 규칙을 재기도 전에** "사유를 골라
  주세요"로 죽는다 — 그러면 검사는 **초록인데 아무것도 안 잰다**(`[169]`). 그래서
  사유는 양성 호출뿐 아니라 **음성 호출에도 반드시 넣는다.**
- **앱 경로는 멀쩡했다**(`app_server` 9473 이 사유를 넘기고 화면에도 칸이 있다) —
  깨진 것은 옛 검증뿐이었다. 그러니 **고칠 자리를 넓히지 않았다**(`[172]`).
- 그러므로 인자를 필수로 바꾸면 **그 함수를 부르는 곳을 전부 세어 본다**
  (`grep -rn "함수이름("`). 기본값이 `""` 로 남아 있어도 **안에서 막으면 필수**다 —
  서명만 보면 안 깨진 것처럼 보인다.


## ★ 챗봇은 지어내지 않았다 — **낡은 글에 새 시각을 찍었다** (2026-08-19 지시)

사용자 지시: **"챗봇 헛소리 자꾸 하는데 고도화 작업 진행해"**

- 형님이 물은 것은 `미처리건은 어디서 확인해?` 였고 답은 이랬다:
  `확신: 높` · 본문 **`관리대장 v420 · 반영 대기 0건`** · 근거 **`세션인계.json(0.0시간 전)`**.
  실제 원장은 **v603** 이다. 그런데 **어느 값도 지어낸 것이 아니다** — 저 문장은
  `ecount/AGENTS.md` 의 '대기 항목'에 **실제로 적혀 있는** 글이고, 저 시각도 그
  파일의 **진짜** mtime 이다. 둘 다 참인데 **붙여 놓으니 거짓**이 됐다.
- 사슬은 셋이다: ① `AGENTS.md` 의 그 대목이 v420 시절에 멈춰 있다
  ② `session_handoff.next_tasks()` 가 **30분마다** 그 발췌를 `세션인계.json` 의
  `다음할일` 로 다시 퍼 담는다 — 그래서 **파일은 언제 봐도 새것**이다
  ③ `local_ai.a_todo` 가 그 덩어리를 본문으로 읊고 그 위에 **파일 나이**를 찍었다.
  **겉포장만 새것이다.**
- ★ **회귀 지점은 '출처를 뗀 것'이다.** `session_handoff` 는 같은 값을
  **`## 다음 할 일 (AGENTS.md 발췌)`** 라는 머리글과 함께 보여 준다 — 출처가 붙어
  있으니 사람이 "아 저건 문서에서 긁어온 글이구나" 하고 판단할 수 있다.
  챗봇이 그 머리글을 **떼고 시각을 붙인** 것이 사고다. 값을 옮길 때 **출처를 떼면
  옮긴 쪽에서 그 값은 사실이 된다.**
- ★ **제 답 안에서 숫자가 어긋나 있었다** — 머리글은 실시간(`반영 대기 484건`),
  본문은 덩어리(`반영 대기 0건`). 한 답에 두 숫자가 있으면 **둘 다 못 믿는다.**
  기준이 다른 값을 한 답에 담을 때는 어느 것이 어느 때 것인지 말해야 한다.
- **판정을 새로 만들지 않았다**(`[162]`) — 인계 문서의 '먼저 처리할 것'을 만드는
  그 `session_handoff.blockers()` 를 **그대로 빌린다**(실측 import 0.023초 ·
  판정 0.001초 — 넘겨받은 스냅샷만 읽고 Z: 를 안 훑는다, `[168]`). 여기서 다시
  세면 같은 물음에 두 답이 생기고, 갈린 뒤엔 어느 쪽이 맞는지 아무도 모른다.
  실측: 굳은 발췌 4줄 → **근거와 조치가 붙은 14건**.
- ★ **발췌를 조용히 빼지도 않았다**(`[169]`). 있다는 사실과 **"이 글이 언제
  것인지는 이 파일이 말해 주지 않는다"** 를 같이 적고 출처가 붙은 자리를 가리킨다.
  빼 버리면 다음 사람이 그런 글이 있다는 것조차 모른다.
- ★ **못 세는 것을 '0건'이라 하지 않는다**(`[169]`). `blockers()` 는 스냅샷을
  **대괄호로** 읽으므로(`st["임시파일"]` · `for c in st["점유"]`) 칸이 빠진 인계
  파일에서는 죽는다. 그때 답은 **'못 셌습니다'** 이고 확신은 '중'으로 내려간다 —
  '걸린 것 없음'으로 보이면 그것이 곧 다음 사고다.
- ★ **갈래는 한 글자도 안 건드렸다**(`[172]`). '미처리' 질문이 `지금할일` 로 가는
  것은 **설계**다(PROBES). 고칠 것은 갈래가 아니라 **낡은 내용에 새 시각을 찍는
  것**이었다 — 갈래를 손대면 멀쩡한 규칙이 깨진다.
- ⚠ **원인의 뿌리는 `ecount/AGENTS.md` 의 굳은 '대기 항목' 자체**다. 그것을 AI 가
  제 판단으로 새로 쓰면 챗봇은 이번엔 **짐작을 새 날짜로** 확신 있게 읊는다 —
  같은 고장이 더 그럴듯해질 뿐이다. 그 대목은 사람이 고친다.
- 검증 `[320]` — 합성 스냅샷을 먹여 **실행해서** 잰다(`[295]`): 낡은 발췌가 본문에
  안 돌아오나 · 살아 있는 '먼저 처리할 것'이 실리나 · 한 답 안에서 숫자가
  어긋나나 · 못 셌을 때 '0건'이라 하나 · 갈래가 그대로인가. **계기 자신도
  시험한다**(`[272]`) — 옛 동작(발췌 읊기)을 만들어 넣어 잡히는지 본다.


## ★ 미처리 사유는 **사람이 적는다** — 기계 추정은 지우지 않고 뒤에 남긴다 (2026-08-19 지시)

사용자 지시: **"대표 캡처화면에서 정기점검 미처리 건 클릭해서 바로바로 사유 입력할 수
있게 해줘, 류지영이 업무하기 편하게"**

- **그 줄에 이미 사유가 적혀 있었다 — 다만 사람이 쓴 것이 아니었다.**
  `_why_still_open` 이 지은 문장('밴드에 접수 글은 있으나 완료 글이 없다' 87건 ·
  '밴드·카톡에 이 프로젝트 글이 없다' · '밴드 수집이 …까지만 왔다')은
  **'왜 아직 미처리인가'가 아니라 '왜 우리가 모르는가'** 다. 진짜 사유(부품 대기·
  캠프 협의·기사 일정)는 류지영만 안다. 대표는 그 문장을 사유로 읽는다.
- ★ **덮지 않는다 — 잇는다**([172]). 사람 사유로 기계 추정을 지우면 *수집이 밀렸다*는
  신호가 같이 사라진다. 둘은 서로 다른 사실이다. 그래서 한 줄이 이렇게 된다:
  `담당자: 부품 입고 8/25 예정 · (기계 추정) 밴드에 접수 글은 있으나 완료 글이 없다`.
  **사람 사유가 먼저**다(`[169]` — 아는 사실을 뒤에 두면 못 본다).
- ★ **읽는 자리를 새로 만들지 않았다**([162]). 캡처·목록·`truth_watch` 가 이미
  `미처리사유` 한 칸을 읽는다 — 그 칸의 **내용**만 넓혔다. 대신 화면이 되쪼개지
  않도록 `사람사유`·`기계추정` 을 **서버가 갈라서** 같이 내려 준다. 화면에서 다시
  자르면 판정이 둘이 되고 언젠가 갈린다.

### 이름을 새로 지은 이유 — 남의 뜻이 있는 칸에 밀어 넣지 않는다
- 담을 칸은 **`미처리사유(담당자)`** 이고 정본은 앱 DB 다(2026-08-10 규칙).
  기존 `staff_entry` 길을 그대로 타므로 멱등키·낙관잠금·감사로그가 공짜로 붙는다.
- **`문제내용` 을 쓰지 않았다** — 실재하는 원장 열이고 `pm_content` 가 읽는다.
- **`미처리사유` 도 아니다** — 그 낱말은 이미 **둘**이 쓴다(`work_log_sync` 대조 기록 ·
  캘린더 이벤트의 기계 추정). 한 낱말이 두 뜻이면 언젠가 갈리고, 갈린 뒤에는 어느
  쪽이 맞는지 아무도 모른다.
- ⚠ **새 비원장 필드를 만들면 `archive_worker.DB_ONLY_ARCHIVE_FIELDS` 에 같이 올린다**
  (`archive_worker.py:107`). 02/04 시트에 대응 열이 없어 안 올리면
  `archive_worker.py` 의 문이 `cannot archive unknown field` 로 **ArchiveRenderError**
  를 올려 그 회차 보관본이 통째로 실패한다. 그런데 **저장 자체는 성공**하므로
  적은 사람은 모르고 11:00·15:00 회차만 조용히 죽는다 — 실패가 성공처럼 보이는 자리다.
  올리면 *원문 그대로 DB(snapshot.json·SQLite backup)에만 보존* + 경고 한 줄로 지나간다.
  그 표의 주석이 이미 같은 판단을 적어 뒀다(접수취소 갈래). 표는 사본이 없다(한 곳).

### 같이 드러난 것 — 대표 보고의 '사유 미입력' 계기가 **영영 0** 이었다
- 캡처 1쪽 돌발AS 칸이 `사유 미입력 N건 · 담당자 설명 필요` 를 적는데, 그 N 은
  `!미처리사유 && !지연근거` 로 셌다. **`미처리사유` 는 서버가 기계 추정으로 늘
  채운다** — 그러니 이 조건은 한 번도 참이 될 수 없었다. 대표 보고는 매번
  '사유 미입력 0건'을 확언했고 그 0 은 **"다 적혀 있다"** 로 읽혔다([169]).
- 이제 **담당자가 적었나**를 센다. 그것이 이 칸이 원래 물으려던 것이다.
  첫날에는 숫자가 크게 나온다 — 그것이 사실이고, 류지영이 적을수록 줄어든다.
- **값이 비면 사람이 알아채지만, 계기가 0 을 내면 의심하지 않는다.**

### 지키는 것
- ★ **빈 것을 '사유 없음'으로 확정하지 않는다**([169]). 아직 안 적은 것과
  "없다고 적었다"는 다른 사실이다. 화면은 `아직 안 적었습니다` 라고 적고,
  서버 `_human_delay_reason` 은 빈 문자열을 돌려줄 뿐 낱말을 만들지 않는다.
- ★ **길이는 취향이 아니라 캡처의 제약이다**([273]·[299]). 대표 캘린더는 A4 **세 장
  고정**이고 다 못 실으면 **저장을 거부한다** — 사유를 적을수록 보고서가 안 나오는
  쪽으로 간다. 합친 한 줄만 `_HUMAN_REASON_MAX`(80)로 묶고 자른 만큼을
  `…(원문 N자)` 로 **말한다**.
  · ⚠ **고치기용 원문은 안 자른다**(`cap=0`). 자른 값을 폼에 넣으면 저장하는 순간
    원문이 잘린다 — 아무 오류도 안 나는 손실이다.
- ★ **버전이 없으면 누르기 전에 말한다**([296]). 저장은 `record_version` 낙관잠금을
  쓰는데, 앱 DB 를 못 읽어 **반쪽으로 내려온 목록**에는 그 값이 없다. 그대로 누르면
  400 `화면 데이터 버전이 없습니다` 가 나고 안내는 '새로고침'인데 **새로고침해도 같은
  반쪽이 온다.** 그래서 단추를 누른 순간 갈라 말한다.
- ★ **빈 값으로 저장하지 않는다**(집 안의 다른 입력과 같은 규칙) — 두 글자 미만이면
  돌려보낸다. 빈 값을 받으면 그것이 곧 "없다고 적었다"가 된다.
- ★ **저장 뒤 합친 한 줄을 화면이 만들지 않는다** — 저장하고 `loadCalendar(true)` 로
  다시 받는다. 화면이 만들면 자르는 규칙이 두 곳이 되고 언젠가 갈린다([162]).
  다시 받기가 실패해도 **저장은 됐다고 말한다**([169] — 된 것을 실패로 보이게 하지 않는다).
- 단추는 **미처리 갈래(`as_open`·`pm_overdue`)에 원장 연결이 있을 때만** 뜬다.
  없는 건에 붙이면 눌러도 저장할 열쇠가 없다.
- 검증 `[321]` — 서버와 화면을 **둘 다 실행해서** 잰다([295]). 계기 자신도 시험한다
  ([272]): 표에서 이름을 빼면 보관본 문이 막히는지, 옛 조건이면 정말 0 이 나오는지.

### 한 사건이 **두 목소리로** 울면 사람은 이미 고쳐진 것을 고치러 간다 (2026-08-19)

- 실측: 09:50 회차가 한 번 실패한 것이 인계 '먼저 처리할 것'에 **세 줄**로 떴다.
  그런데 그중 `schedule_watch` 만 *"그 뒤 코드가 바뀌었다(daily_run.py 10:17)"* 를
  알고 있었고(`[110]` 의 `고침대기`), `system_audit` 은 그것을 모른 채 **P0** 를
  올렸다. 같은 파일을 보는 두 화면이 서로 다르게 말하면 사람은 **없는 고장**을
  찾아 나선다([172]). 그리고 경보가 대부분 가짜면 진짜 경보가 묻힌다([170]).
- `system_audit._schedule_verdict()` 가 회차가 **써 둔 판정을 읽기만** 한다([162]) —
  여기서 스케줄러를 다시 묻지 않는다([168]). 갈래가 `고침대기`·`뒤에됨` 이면
  **P2**(지켜볼 것)로 내리고, 실패 사실과 그 원인은 **그대로 싣는다**([169]).
- ★ **'고쳐졌다'고 말하지 않는다** — 말할 수 있는 것은 '그 뒤 코드가 바뀌었다'
  까지다. 무관한 이유로 파일을 건드렸을 수도 있다. **다음 회차가 답한다**([110]).
- ★ **못 읽으면 예전대로 P0 다**([169]) — 근거 파일이 없거나 모양이 깨졌거나 남의
  회차 이야기면 완화하지 않는다. '확인 못 함'을 '괜찮음'으로 치면 감사기 자신이
  눈먼 채 조용해진다.
- 검증 `[322]` — 합성 리포트를 임시 폴더에 놓고 `build()` 를 **실행해서** 잰다([295]).
  계기 자신도 시험한다([272]): 근거 없음·다른 갈래·남의 회차·깨진 모양 넷에서
  **P0 가 그대로 뜨는지**.


### 경보를 **옮겨 적는 자리**가 사유를 먹고 있었다 (2026-08-19, `[322]` 이어서)

`[322]` 로 P0 하나를 내리고 목록을 다시 보니 15줄 중 셋이 같은 병이었다.

- **사전을 문자열처럼 찍었다.** `schedule_watch` 의 `경보` 는
  `{갈래·작업·무엇·어떻게}` **사전**인데 `system_audit` 이 `str(x)[:150]` 로 담아
  인계 문서에 `{'갈래': '중단됨', '작업': …}` 가 그대로 나왔다.
  · ★ **보기 나쁜 것보다 나쁜 것**: `[:150]` 이 **사전 껍데기까지 센다.** 실측으로
    300자짜리 사유가 옛 방식에서는 **119자**만 남고 새 방식에서는 **145자**다 —
    26자가 `'갈래':`·`'작업':` 같은 껍데기에 먹혔다. **겉은 경보인데 왜인지는 못 읽는
    자리**다(`[169]`). 만드는 쪽이 모양을 바꿔도 여기는 **오류가 안 난다**(`[165]`).
  · 그래서 `_alert_text()` 가 **사전이면 `무엇`을 꺼내고 갈래를 앞에 붙인다**.
    문자열도 그대로 받는다 — 모양을 하나로 못 박으면 다음에 바뀔 때 또 조용해진다.
    빈 사전에도 **빈 줄을 내지 않는다**(조용히 사라지느니 `{}` 라고 적는다).
- **같은 회차 경보가 언제나 두 줄이었다.** `system_audit` 이 회차 경보를 담는 것은
  옳다 — 앱 실행 화면에서는 그것만 읽는다. 그런데 인계 문서는 그 **원본**
  (`schedule_watch`)도 같이 읽는다. 그래서 `쿠팡업무_원본자료자동정리` 가
  `[P0] 자동 회차 실패 경보…` 와 `회차 [중단됨] …` **둘로** 떴다.
  · 남기는 것은 **원본 줄**이다 — 갈래(`[중단됨]`)까지 말해 조치가 갈린다(`[289]`).
  · ★ **원본이 실제로 실렸을 때만 뺀다**(`[169]`). 스케줄러 감시를 못 읽은 날 빼면
    그 경보가 **통째로 사라진다** — '다른 데 있겠지'는 근거가 아니다.
- **오늘에 대해 틀린 말을 확언하고 있었다.** 일일대조 경보가
  *"스케줄러는 '성공'으로 보고한다"* 라고 적는데, 그날 스케줄러는 `코드 1`(실패)이었고
  **같은 문서 두 줄 위**가 그렇게 말하고 있었다. 그 괄호는 '이 검사가 왜 있나'라는
  일반론이지 오늘에 대한 주장이 아니다 — 그러니 주장이 아닌 말로 적는다
  (*"스케줄러가 '성공'이라 적어도 완주하지 않았을 수 있다"*). 한 문서가 어긋나게
  말하면 사람은 **없는 것을 찾아 나선다**(`[172]`).
- 검증 `[325]` — **실행해서** 잰다(`[295]`). 계기 자신도 시험한다(`[272]`):
  원본을 **못 읽은** 상태에서 그 경보가 그대로 실리는지.
