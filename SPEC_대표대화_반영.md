# 유수비 대표 대화를 캡처에 반영한다 — 단 쿠팡 건만

**2026-08-14 형님 지시**: *"유수비 대화 내용 참고해서 쿠팡 관련 사항만 캡처할 때
꼭 반영해야돼 이거 반영해 / 하다가 크래딧 떨어지면 다른 계정이나 코덱스에서 진행할
수 있게 안전장치 걸어놔"*

분담판 **[96]**. 이 문서가 그 안전장치다 — **다른 계정·Codex 는 이 문서 하나만 읽고
이어받으면 된다.** 대화 기록에 의존하는 정보는 여기 안 적는다.

---

## ★ 재발이다 — 같은 지적이 두 번째

`AGENTS.md` 1051행: 2026-08-04 사용자 지적 *"유수비가 이야기한 사항 반영 하나도
안되어있음"*. 그때 고친 것은 `_augment_exec_daily`(app_server)로 **대표보고 카드
3장에 얹은 것까지**다. 원천을 잇는 일은 안 했다.

---

## 실측 (2026-08-14, 전부 이 세션에서 직접 확인)

### 1. 유수비 = 대표. 카톡 발신자 이름은 `[유수비 대표 유니버셜리프트]`

`ledger_db.py:267` 에 `("유수비·김경원 폰", …, "접수 경로")` 로 이미 등록돼 있다.

### 2. 들어온 카톡은 **전부 쿠팡 방**이다 — 비쿠팡 방은 0개

`source_dirs.KAKAO_DIR` 아래 `.txt` **54개**를 첫 줄(방 이름)로 집계:

| 방 이름 | 파일 수 | 유수비 등장 |
|---|---:|---:|
| `★UNI★ 쿠팡돌발점검 님과 카카오톡 대화` | 26 | 24 |
| `★UNI★ 쿠팡정기점검 님과 카카오톡 대화` | 26 | 15 |
| `★UNI★ 쿠팡돌발점검 25 카카오톡 대화` | 1 | 1 |
| `★UNI★ 쿠팡정기점검 25 카카오톡 대화` | 1 | 1 |

> ⚠ **그러므로 "쿠팡 관련만"은 방 이야기가 아니다.** 첫 해석("대화방에 비쿠팡이
> 섞여 있으니 방으로 걸러라")은 실측과 안 맞았다. 아래 3번이 진짜 대상이다.

### 3. 대표는 그 쿠팡 방에 **두 종류**를 올린다

최근 파일 `2026/08/2026-08-14/KakaoTalk_20260814_0810_14_780_group.txt`
(8,242줄) 안의 유수비 발언 **7줄** 전량:

```
[유수비 대표 유니버셜리프트] [오전 7:45] 제목 ● 캠프이름 : GWJ1      ← 쿠팡 접수 ○
[유수비 대표 유니버셜리프트] [오전 7:45] 동영상                        ← 첨부
[유수비 대표 유니버셜리프트] [오전 7:46] 사진                          ← 첨부
[유수비 대표 유니버셜리프트] [오후 3:54] (장식) 대표이사 업무지시서    ← 쿠팡 무관 ✗
[유수비 대표 유니버셜리프트] [오후 3:55] 사진 ×3                       ← 첨부
```

**이것이 형님 지시의 정확한 대상이다** — 접수 양식은 반영하고 `대표이사 업무지시서`
같은 회사 내부 공지는 캡처에 넣지 않는다.

> ⚠ **날짜를 파일명으로 읽지 말 것.** 위 GWJ1 글은 8/14 가 아니라 **2026-07-28**
> 것이다(파일만 8/14 에 내보내졌다). 카톡 내보내기는 전체 대화를 담으므로
> `--------------- 2026년 7월 28일 화요일 ---------------` 구분선을 봐야 한다.
> 이 세션에서 그대로 한 번 헛읽었다.

### 4. 왜 안 들어가나 — 원인은 한 줄이다

`kakao_extract.extract()` **216행**:

```python
if "프로젝트NO" not in text or not RE_CODE.search(text):
    continue
```

대표가 올리는 접수 양식에는 **프로젝트NO 가 아직 없다**(배정 전이다). 그래서
카톡 파서가 통째로 건너뛴다 — 오류도 안 나고 숫자도 안 준다(`[169]` 모양).

### 5. 그래서 지금 유일한 길은 **손 입력**이고, 7/28 이후 끊겼다

`reports/manual_daily_events.json` = **events 1건**(2026-07-28 GWJ1 M_순천1).
읽는 곳은 `daily_brief.py:41·63` 하나. 그 뒤 17일간 한 건도 안 들어왔다.

### 6. 이미 있는 재료 (새로 만들 필요 없는 것)

- `kakao_extract` 는 **`발신자`(msg["sender"])·방 이름(`room_of`)을 이미 담는다**
  — 거를 재료가 이미 있다. `extract()` 가 프로젝트NO 로 먼저 버릴 뿐이다.
- `daily_brief.rows()` 는 **프로젝트NO 없는 행을 이미 살린다**(`meaningful_orphan`,
  daily_brief.py:95~99). 즉 받는 쪽은 준비돼 있다 — **주는 쪽만 없다.**
- `_augment_exec_daily`(app_server)가 대표보고 카드·**캡처 PNG·폰 data.enc** 를
  공통으로 먹인다. 여기에 실으면 세 곳이 같이 따라온다.

---

## 설계 (아직 구현 안 됨 — 다음 사람이 여기서 시작한다)

`ceo_events.py`(가칭) 하나. `kakao_extract` 의 파서(`kr.parse_export`)를 **그대로
빌린다** — 카톡 파싱을 새로 짜면 사본이 둘 된다(`[162]`).

### 뽑는 문 (전부 통과할 때만 반영)

1. **발신자**가 유수비 대표일 것 — 이름 문자열은 코드에 박지 말고 표 한 곳에 둔다
   (사람은 바뀌고 표기는 흔들린다. `people_alias` 와 같은 자리)
2. **쿠팡 방**일 것 — 지금은 전부 쿠팡이라 무해하지만, 비쿠팡 방이 들어오는 날
   자동으로 걸린다. `kakao_extract.room_of` 를 그대로 쓴다
3. **쿠팡 업무 신호**가 있을 것 — `● 캠프이름 :` 접수 양식 또는 실재 캠프명/`UJ`번호.
   **지어내지 않는다**: 근거가 없으면 '모름' 갈래로 두고 반영하지 않는다(`[169]`)
4. **제외**: `대표이사 업무지시서` 같은 회사 내부 공지 · `사진`/`동영상` 단독 줄

### 지키는 것

- **원문은 한 글자도 안 고친다** — 읽을 때만 거른다(`[155]` 와 같은 원칙)
- **뺀 건수를 숫자로 말한다** — 조용히 빼면 '0건'이 '다 봤다'로 읽힌다(`[169]`).
  리포트에 `쿠팡 건 N · 쿠팡 무관 M · 모름 K` 를 적는다
- **판정은 한 곳** — 캘린더 캡처·대표보고 캡처·폰 사본이 같은 함수를 읽는다(`[162]`)
- **읽기 전용** — 원장에 안 쓰고 큐에도 안 넣는다. 대표 글은 프로젝트NO 가 없어
  원장 행과 짝지을 근거가 약하다. 짐작으로 붙이면 엉뚱한 현장에 꽂힌다(`[172]`)
- `manual_daily_events.json` 은 **그대로 둔다** — 자동이 못 잡는 것을 사람이
  보태는 자리로 남긴다. 지우면 7/28 기록을 잃는다

### 남은 일 (순서대로)

1. `ceo_events.py` 구현 + 대표 이름 표
2. `daily_brief` 에 잇기(`events` 갈래에 합류 — 받는 쪽은 이미 준비됨)
3. 합성검증 신규 — ① 쿠팡 무관 글이 안 실리는지 ② 뺀 건수를 적는지
   ③ 방이 비쿠팡이면 안 실리는지 ④ 날짜를 파일명이 아니라 구분선에서 읽는지
4. `ecount/CLAUDE.md` 정본에 규칙 한 절 + 루트 `CLAUDE.md`·`AGENTS.md` 사본 동기화
5. 09:50 회차 단계로 넣고 '자동으로 도는 것' 목록에 적기 (**등록까지 확인** — `[228]`)

---

## 이어받는 법 (다른 계정 · Codex)

```
python ecount/session_handoff.py --adopt
python ecount/worksplit.py            # [96] 을 본다
```

그다음 이 문서의 '남은 일' 1번부터. 실측은 다시 하지 않아도 된다 — 위 숫자는
2026-08-14 에 직접 잰 값이다. 다만 **카톡 파일이 늘었으면 3번 표는 다시 센다.**
