# 올리면 스스로 돌고, 올렸다고 알린다 — 설계 (2026-08-14 지시)

사용자 지시: **"류지영이 카톡 텍스트 파일 앱에 업로드 하면 바로 정리 시작하고 나에게
올렸다고 알려주는 구조 코딩해, 다른 비슷한 기능이 필요하다 판단되면 그것도 반영하고"**

> 이 문서는 **아직 구현 전**이다. 2026-08-14 세션이 컨텍스트 91% 라 실측만 하고 남겼다.
> 반쯤 만들어 두면 이 프로젝트가 제일 많이 당한 '실패가 성공처럼 보이는 자리'가 된다.

## 실측 — 절반은 이미 있다 (2026-08-14)

`webapp/app_server.py` `POST /api/automation/kakao-upload` (8543행):

- 권한: 관리자 또는 **류지영 업무센터**. 그 밖은 거절.
- 30MB 제한 · multipart `kakao_file`|`file`
- `automation_pipeline.submit_kakao_file(파일명, 바이트)` 로 저장
- **그 자리에서 `start_task("automation")`** — 파이프라인이 바쁘면
  `defer_task_until_free("automation")` 로 예약
- 응답 `{ok, saved, auto_started, auto_queued, msg}`

→ **"올리면 바로 정리 시작" 은 이미 돈다.** 5분 회차를 기다리지 않는다.

## 빠진 것 — 알리는 쪽

`grep` 실측: 코드 전체에 형님께 알리는 길이 **0건**이다
(`kapi.kakao`·`memo/v2`·텔레그램·push 어느 것도 없음). 분담판 `[75]` 와 같은 결론.

그래서 지금은 이렇게 된다 — **류지영이 올려도 형님은 모른다.** 올라왔는지 물어보거나
앱을 열어 봐야 안다. 그리고 더 나쁜 쪽: **처리가 실패해도 아무도 모른다.**

## 만들 것

### ① 알림 한 곳 — `notify.py` (새 파일)

```
notify.push(갈래, 제목, 본문, 근거=None) -> {보냄:[...], 실패:[...]}
```

- **판단을 두 곳에 두지 않는다**([162]). 알림을 만드는 자리는 이 함수 하나다.
- 채널은 **켜진 것만** 쓴다. 지금 확실히 되는 것은 **앱 안 알림**이다 —
  `/api/notifications` 가 이미 있다(ux 기록에 실사용 흔적 있음). 그것부터 채운다.
- 카톡 '나와의 채팅'(PlayMCP MemoChat)은 **세션에 매인 MCP** 라 무인 회차에서 못 부른다.
  분담판 `[75]` 의 문 셋을 그대로 지킨다: 사람이 켤 때만 · 건수·상태만 보냄
  (캠프명·금액·프로젝트NO 같은 업무값은 제3자 중계로 안 내보냄) · **실패를 성공으로
  안 적음**(응답 확인).

### ② 올린 순간과 **끝난 순간**을 둘 다 알린다

★ **"올렸다"만 알리면 반쪽이다.** 올렸다는 말만 가고 처리가 죽으면, 형님은 된 줄 알고
넘어간다 — 이 프로젝트가 반복해 당한 그 모양이다([169]).

| 때 | 누구에게 | 내용 |
|---|---|---|
| 업로드 접수 | 형님 | `류지영 님이 카톡 원본을 올렸습니다 — <파일명> · 정리 시작` |
| 정리 끝 | 형님 + **올린 사람** | `카톡 정리 끝 — 새 대화 N건 · 반영 M건` |
| 정리 실패 | 형님 + **올린 사람** | `카톡 정리 실패 — <단계> · <이유>` |

- 끝났는지는 `reports/카톡_반영회차.json` 이 이미 단계별 자국을 남긴다 — **다시 세지
  않고 그것을 읽는다**([168]).
- **올린 사람에게도 알린다.** 류지영이 올려 놓고 됐는지 모르면 또 올린다(중복 원본).

### ③ 같은 모양이 필요한 다른 업로드 (지시의 "다른 비슷한 기능")

전부 **올리면 조용한** 자리다. 같은 `notify.push` 를 쓴다:

- **김미영 입금내역 엑셀** — `receipt_apply.py`. 형님이 주기적으로 올린다(2026-08-11 지시).
  올린 뒤 대조 결과(짝지어진 건수)를 알려야 뜻이 있다.
- **오종현 Share 폴더 파일** — 지금은 투입함에 들어가고 끝. 분담판 [90] 의 그 파일도
  이렇게 조용히 들어갔다.
- **PO 첨부 · 개선요청 첨부** — 담당자가 올려도 형님은 모른다.
- **업로드 실패 자체** — 30MB 초과·형식 오류로 거절된 것도 알려야 한다.
  거절은 화면에만 뜨고 사라진다.

### ④ 지키는 것

- **알림이 대부분이면 아무도 안 본다**([170]). 하루 몇 건 수준으로 묶는다 —
  같은 갈래가 5분 안에 여러 번이면 **한 줄로 합친다.**
- **못 보냈으면 못 보냈다고 적는다**([169]) — 보낸 척하면 감시자 자신이 눈먼다.
  `reports/알림_기록.json` 에 보냄·실패를 남긴다.
- **업무값을 밖으로 안 내보낸다** — 외부 채널에는 건수·상태만.
- **알림 실패가 업로드를 죽이지 않는다** — `notify.push` 는 예외를 삼키고 기록만 남긴다.
  알리려다 원본 접수를 막으면 본말전도다.

### ⑤ 검증 (다음 번호)

- 업로드 핸들러가 `notify.push` 를 **접수 때와 끝났을 때 둘 다** 부르는지
- 실패 경로에서도 부르는지(성공만 알리면 조용한 사고가 그대로 남는다)
- `notify.push` 가 채널 실패를 **성공으로 안 적는지**
- 외부 채널 본문에 프로젝트NO·금액·캠프명이 **안 실리는지**
- 알림 실패가 예외로 새어 업로드를 500 으로 만들지 않는지

## 먼저 확인할 것 (구현 전)

1. `/api/notifications` 가 **읽기 전용인지 쓰기도 되는지** — 쓰는 길이 없으면 그것부터.
2. 폰에서 그 알림이 실제로 보이는지(고정 주소 `mulder.tailf14aae.ts.net`).
3. `automation_pipeline` 이 끝났음을 알리는 자국의 정확한 칸 이름 —
   **짝짓지 말 것**([165]). 틀린 칸을 물으면 오류 없이 빈 값이 온다.
