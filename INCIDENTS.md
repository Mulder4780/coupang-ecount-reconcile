# 사고 기록 (Claude · Codex 공용)

> 같은 실수를 두 번 하지 않기 위한 기록. **증상 → 원인 → 조치 → 재발방지**만 적는다.
> AGENTS.md 는 "지금 어떤 상태인가", 이 파일은 "무엇을 어떻게 틀렸나"를 담는다.
> 새 사고가 나면 맨 위에 추가하고, 재발방지에는 **검증 번호**를 반드시 남긴다.

---

## 2026-08-20

### 41. Z폴더 스캔이 세 번 실패했다 — 전체 파일을 훑고 CP949 진행문구 한 글자에서 죽었다
- **증상** 자율복구 `Z폴더 원장 누락·금액 공백 스캔`이 rc=1로 세 번 반복돼
  `autopilot-a1a5961eeedd8d5bbb0a` 후속검토로 넘어왔다. 09:50 회차는 이 단계에 321초를
  쓴 뒤 `deferred`였고, 17:06 워치독의 마지막 예외는 `zscan.py:326`이었다. 기존
  `reports/Z폴더_스캔.md`는 8/18 v600 기준에서 갱신되지 않았다.
- **원인** 스캔 뒤 첫 `print()`의 긴 대시 `—`(U+2014)를 CP949 표준출력에 쓰면서
  `UnicodeEncodeError`가 났다. `proc_guard.run_tree()`는 UTF-8로 자식 출력을 읽지만,
  `zscan.py`는 다른 자동화 스크립트들과 달리 `sys.stdout.reconfigure()`를 하지 않았다.
  그래서 실행 환경이 UTF-8이면 통과하고 별도 워치독이 CP949면 같은 데이터에서 실패했다.
  Z: 단절·원장 누락·금액 불일치가 종료 원인이라는 근거는 없었다.
- **조치** `zscan.py` 시작 시 표준출력을 UTF-8·`errors="replace"`로 재구성한다. 스캔·
  원장 읽기·금액 비교·리포트 쓰기 로직은 바꾸지 않았다. 실패한 업무 명령은 재실행하지
  않았고, 다음 정규 워치독이 rc=0을 확인하기 전까지 큐를 `retry`로 유지한다.
- **재발방지** 검증 `[360]`은 엄격한 CP949 `TextIOWrapper`를 만든 뒤 실제 실패 문구를
  써서 UTF-8로 온전히 나오는지 확인한다. 이 샌드박스에는 Python과 Z: 매핑이 없어 전체
  합성검증·Sol 관문·실데이터 확인은 실행하지 못했다.

## 2026-08-15

### 40. 원본 바로가기 정리가 세 번 timeout — 10분 워치독 안에서 100분 뒤 자진복귀하려 했다
- **증상** `원본 폴더 정리·바로가기`가 자율복구에서 세 번 반복 실패했다. 큐 마지막 값은
  `last_returncode=124`·`last_error=returncode=-9`이고, 12:42 일일회차의 같은 단계도
  정확히 **1,802초**를 쓴 뒤 `deferred`였다. AI 티켓은 별도 업무 로그를 가리켰지만 실제
  로그 파일은 없었다 — 어느 파일에서 멈췄는지 볼 수 없었다.
- **원인** `source_tidy.BUDGET_MIN` 기본값이 **100분**인데 daily_run 단계 제한은 30분,
  `autopilot.heal()`의 전체 워치독 예산은 **10분**이다. 그러므로 자체 “남은 몫은 다음
  회차” 분기에 닿기 전에 부모가 프로세스 나무를 끊었다. 색인도 11,124건에서 156,294건으로
  커졌고, 링크마다 `os.path.exists(dst)`와 `os.makedirs(..., exist_ok=True)`를 Z:(SMB)에
  되물어 재시작할 때 같은 앞부분을 다시 훑는 구조였다.
- **조치** 자체 예산을 7분으로 낮추고 미완료는 증분 코드 75로 돌려준다. 전체 링크 계획을
  메모리에서 먼저 완성한 뒤 `_바로가기`를 공용 `walk_stat`으로 한 번만 훑어 기존 경로와
  폴더를 집합으로 재사용한다. 다음 회차는 이미 만든 링크를 SMB 재조회 없이 건너뛴다.
  250개마다 단계 로그를 flush하고, 링크 작성이나 묵은 링크 확인 중 예산이 끝나면 삭제를
  넓히지 않은 채 75로 이월한다. `schedule_watch.RESULT[75]`는 `이어감`이라 전용 스케줄러도
  정상 증분을 실패로 경보하지 않는다.
- **재발방지** 검증 `[287]` — 7분 자체 예산과 10분 워치독 사이 2분 여유, 75의
  waiting 계약, 기존 링크별 `exists` 금지, 전체 `want` 선완성, 진행 로그를 확인한다.
  이 환경에는 Python 실행 파일이 없어 합성검증을 실제 실행하지 못했고 `git diff --check`만
  통과했다. 성공 확인 전 큐를 닫지 않는 규칙에 따라 해당 항목은 `retry` 그대로 두었다.

## 2026-08-11

### 38. 자동 커밋이 하루 종일 한 건도 안 됐다 — 관문이 **남의 옛 줄**에 잠기고, 멈추면서 스테이징을 남겼다
- **증상** 화면은 `세션 자동 마무리: 4/5 단계 완료` 만 말했다. 빠진 한 단계가 **③ 커밋**이고,
  오늘 회차 **14번이 전부** `비밀값 형태가 스테이징에 있어 커밋을 멈췄다` 로 끝났다(15:51~21:02).
  게다가 멈출 때마다 `git add -A` 로 담은 것이 공유 인덱스에 남았다 — 실측 21:02 이후
  옆 세션이 1분 전 저장한 파일 **10개**가 스테이징에 앉아 있었다. **기계가 사고 #36 의 그
  상태를 스스로 만들고 있었다.** 4/5 는 실패처럼 안 보이고 파일은 디스크에 그대로라 티가 안 난다.
- **원인** 셋이 겹쳤다.
  · 스캔이 `git grep --cached` 로 **인덱스 전체**를 훑었다. 걸린 단 한 줄은 **어제(08-10 10:57)
    커밋된** `canonical_sync.py:349` 의 `completion_token = "canonical-…" + sha256_json(...)`
    — 비밀값이 아니라 멱등키를 만드는 계산식이다. 커밋을 거부해도 이미 커밋된 줄은 사라지지
    않으므로 **영구히 잠긴 관문**이었다. 거부가 고칠 수 없는 것을 이유로 거부하고 있었다.
  · 메모가 **어디가 걸렸는지 말하지 않았다**(`사람이 확인할 것`). 확인할 자리를 모르니 아무도
    확인하지 못하고 같은 경보가 14번 쌓였다.
  · 멈추는 자리가 `add -A` **뒤**라 스테이징을 남긴 채 돌아섰다. 다음 사람의 `git commit -m`
    한 번이 그것을 통째로 커밋한다 — #36 이 그렇게 났다.
- **조치** `session_wrapup.step_commit` 넷.
  ① 스캔 범위를 **이번 커밋이 담는 경로**로 좁혔다(`--diff-filter=ACMRT`, 120개씩 끊어 명령줄
     한도를 피한다). 새 파일은 전체가 담기므로 그대로 걸린다 — 절대규칙 1 은 좁아지지 않았다.
  ② 걸리면 메모에 **`파일:줄`** 을 적는다. ③ 멈추거나 커밋이 깨지면 인덱스를 **담기 전 상태로
     되돌린다**. 단 원래 남의 스테이징이 있었으면 **손대지 않고 그렇다고 말한다**(남이 적어 둔 뜻이다).
  ④ 옆 세션 감지에 근거를 더했다(`_live_sibling_sessions`) — `~/.claude/projects/<이 프로젝트>/<sid>.jsonl`
     의 mtime. 점유판은 단발 pid 로 판정해 눈이 멀고(#36, 이날 세 번 실측: 21:07 에도 "잡혀 있는
     작업 없음"인데 옆 창 셋이 1·15·52초 전에 자랐다), 미커밋 mtime 으로 세면 **내가 방금 고친
     것까지** 옆 세션이 되어 늘 보류가 된다. 대화기록은 **파일 이름이 세션 식별자라 내 것을
     이름으로 뺄 수 있다.**
- **고치다 만든 실수도 적는다** 스테이징 목록을 `git()` 으로 읽었는데 `run()` 은 **마지막 한 줄만**
  준다(기록용 요약이라 그렇다). 그래서 목록이 `theirs.py` 하나로 줄고 비밀값이 담긴 `leak.py` 가
  빠져 **스캔이 그 파일을 안 보고 통과**했다 — 막으려던 것을 새게 만드는 방향의 실수다.
  세 갈래 시험의 ③이 잡았다. 목록은 이제 `git_lines()` 로 읽는다.
  · **사고를 적는 행위가 사고 모양을 만들었다** — 이 항목과 코드 주석에 걸린 줄을 그대로
    옮겨 적었더니 스캔이 그것까지 잡았다(내 커밋 5건 전부 내 설명·픽스처였다). 지문에
    걸리는 예시는 **줄여 적고**(`"canonical-…"`), 시험 픽스처는 **조립해서** 쓴다 —
    디스크에 써질 때만 그 모양이 되게. 안 그러면 다른 스캐너(Terra→Sol 검토)가 영구히 걸린다.
- **재발방지** 세 갈래를 시험으로 만들어 통과시켰다: 남의 옛 줄에 **잠기지 않는다** · 새 비밀값은
  **자리를 적고 막는다** · 남의 스테이징은 **빼지 않고 그렇다고 말한다**. 몸통은
  `tests/_pending_wrapup_commit_check.py` 에 두었다. **검증 번호는 아직 없다** —
  `tests/synthetic_check.py` 를 옆 세션이 지금 편집 중이라(21:12 저장) 같은 파일 동시 편집
  금지에 걸린다. 분담판 **`[33]`** 으로 남겼고 그 세션이 커밋하면 번호를 달아 옮긴다.
  기존 계약은 그대로다 — `[103]`·`[158]` 통과 확인.
  · ★ **`[221]` 이 생겼지만 이 세 갈래는 아직 덮이지 않았다**(2026-08-11 22:45 실측).
    그 시험은 `[33]` 을 잡은 옆 세션이 넣은 것이고 **보는 것이 다르다** — 옆 세션 표기·
    푸시 보류·문서 절차다. 비밀값 스캔을 **'없음'으로 가짜 처리**하므로 **거부 경로를
    한 번도 지나가지 않고**, 되돌림은 `_rollback()` 이 소스에 두 번 나오는지로만 본다.
    **소스 문자열 검사로는 못 잡는 종류가 바로 이 사고였다** — `run()` 한 줄 읽기 누락은
    실제 저장소에서 돌려 봐야 드러났다. 그러므로 **`[221]` 을 보고
    `tests/_pending_wrapup_commit_check.py` 를 지우면 안 된다.** 분담판 **`[34]`** 로 올렸다.
  · ✅ **`[224]` 로 옮겼다**(같은 날 22:58 — 옆 세션이 `code` 점유를 놓은 뒤). 세 갈래를
    그대로 옮겨 **실제 임시 저장소를 만들어 돌린다.** 임시 파일은 지웠고 분담판 `[34]` 는
    완료다. ★ 이 '옮길 때가 됐다'를 **사람이 알려 줘야 했던 것**이 그날의 또 한 가지
    사고이며(사람이 "하던 작업 진행" 을 두 번 쳤다), 그것은 `[225]` 로 막았다 —
    막힘이 풀리면 회차가 스스로 인계 문서에 올린다.
  · 이날 회차 실측: 고친 뒤 첫 PreCompact 가 **5/5** 로 끝났다(③ "커밋 완료 · 다른 세션
    4개가 작업 중이라 푸시는 보류"). 잠긴 관문이 풀린 것을 회차 기록이 스스로 말한다.

### 37. 7분 자진복귀를 성공으로 닫으면 잔량 수만 건이 영원히 남는다
- **증상** #35의 timeout을 없애려고 `collect_all`이 예산 이월을 반환코드 0으로 돌려주게
  했다. 그러면 첫 7분 배치가 정상 저장돼도 사진·글 잔량이 있는 채 `autopilot` 항목이
  `done`이 된다. 작업은 실패하지 않았지만 **완료도 아니었다**.
- **원인** 자율복구 계약에 성공(0)과 실패(비0) 둘만 있었고, 멱등 대량 작업의
  “한 회차 진척·아직 남음” 상태가 없었다. 1을 쓰면 정상 진척을 코드 실패로 세어 3회마다
  AI를 다시 부르고, 0을 쓰면 성공 확인 전에 큐를 지우는 양자택일이었다.
- **조치** `collect_all`은 현재 캐시 세트의 PDF·TXT·사진 잔량 또는 예산 이월이 있으면
  전용 코드 75를 반환한다. `autopilot.heal`은 이를 `waiting`으로 두고 30분 뒤 이어 가며,
  실패 연속 횟수는 0으로 재설정하고 처리된 AI 티켓은 비운다. 09:50 `daily_run`도 75를
  실패 재시도나 성공 `resolve`로 보내지 않는다. 잔량 0·전 단계 완주만 0이다.
- **재발방지** 검증 `[217]` — 75 회차가 `done`·`retry`가 아닌 `waiting`이고, 실패횟수 0,
  연속회차 증가, 이전 AI 티켓 해제를 모두 확인한다. `[216]`의 현재 세트 잔량 판정과 한 쌍이다.

### 36. 죽은 세션의 스테이징이 남의 커밋에 딸려 들어갔다 — `git commit` 은 인덱스 전체를 커밋한다
- **증상** `dac781f` 는 통계 카드(`webapp/index.html`) 하나만 커밋하려던 것인데 **17개
  파일**이 올라갔다. 딸려 온 16개는 직전에 죽은 codex 세션이 `git add` 만 해 두고 커밋
  못 한 "P1 통합 마감"(cancel_watch·band_extract·automation_pipeline·daily_run·
  pid_alive·session_handoff·tests/synthetic_check …)이었다. 그 커밋 뒤 합성검증이
  `t151` 에서 깨졌다 — `assert s["밴드글_보관"] == 1 and s["밴드글_남음"] == 1` 에서
  실측 **보관 0 · 남음 2**.
- **원인** 두 겹이다.
  · **`git commit -m` 은 내가 고친 파일이 아니라 인덱스 전체를 커밋한다.** 작업 폴더는
    세션끼리 공유하므로([104]) 옆 세션의 `git add` 가 **같은 인덱스**에 앉아 있다.
    죽은 세션의 스테이징에는 주인 표시가 없고, 보이는 자리는 `git status` 하나다.
    `git add -A` 를 안 써도 걸린다 — 이미 add 돼 있었기 때문이다.
  · **깨진 것은 코드가 아니라 전이 상태였다.** 그 t151 실패는 남의 버그가 아니라
    같은 자리를 고치는 **살아 있는** codex 세션의 3분짜리 중간 상태였다. mtime 이
    말해 준다: `collect_all.py` 16:00:06(새 `survey()` — 캐시가 가리키는 세트 경로만
    맞댐) → `tests/synthetic_check.py` 16:03:04(t151 을 그 규칙으로 고침). 그 사이에
    돌린 검증은 **새 코드 + 옛 테스트**라 옛 픽스처(밴드 루트에 평평한 `a.pdf`)가
    새 판정에 안 걸려 보관 0 이 나왔다. 재현 확인: 옛 픽스처 + 새 survey → 0·2,
    옛 survey + 새 픽스처 → 1·1, 새+새 → ALL GREEN.
- **조치** 이번에는 아무것도 커밋하지 않았다. 16:07 시점에 **또** 여섯 개가 스테이징에
  있었고(collect_all·band/archive_posts·tests/synthetic_check·autopilot …) 주인은
  16:03 에 `code`·`git` 을 잡은 살아 있는 codex(sid 8afc88c9)였다. 그대로 커밋하면
  같은 사고를 그 자리에서 되풀이한다. t151 은 그 세션이 이미 고쳐 놓았고 전체 검증은
  ALL GREEN 이므로 **고칠 것이 없었다.**
- **재발방지**
  · **커밋 전에 `git status --porcelain` 을 본다.** 내가 안 고친 경로가 있으면
    **`git commit -- <경로> …`** 로 경로를 명시한다 — pathspec 을 주면 인덱스의 다른
    경로는 스테이징 그대로 남는다(남의 작업을 빼앗지도, 지우지도 않는다).
  · **점유판이 비어 보여도 옆 세션이 없다는 뜻이 아니다.** `python ai_claim.py` 는
    "잡혀 있는 작업 없음"이라 했다 — 점유를 적은 pid(7204·43224)가 이미 끝난 단발
    프로세스여서 죽었다고 판정된 것이다. 세션은 멀쩡히 파일을 쓰고 있었다.
    **실질적인 옆 세션 감지기는 `git status` 와 mtime 이다.**
    같은 날 **20:49 에 한 번 더 실측했다** — 점유판은 여전히 "잡혀 있는 작업 없음"인데
    `watchdog.py`·`tests/synthetic_check.py` 가 **9초 전**에 저장돼 있었다(붙여넣기
    파일 낡음 기준 수정 + 새 검증 75줄). 한 번 어긋난 것이 아니라 **계속 그렇다** —
    점유판은 '무엇을 하겠다고 적었나'이고 mtime 은 '지금 무엇이 벌어지나'다.
  · **검증이 빨갛게 뜨면 먼저 "이게 내 코드인가"를 묻는다.** 같은 자리를 고치는
    옆 세션의 중간 상태일 수 있다. 그때 테스트를 '고치면' 남의 완성을 되돌린다.
  · [104] 의 짝이다 — 그쪽은 `add` 를, 이쪽은 `commit` 을 막는다. `session_wrapup`
    의 자동 커밋은 여전히 `add -A` 로 쓸어 담고 **푸시만** 보류한다(고의). 커밋 경로
    명시를 기계로 막는 자리는 아직 없다 — 검증 추가는 `tests/synthetic_check.py` 를
    그 세션이 편집 중이라 **분담판 `[33]`** 으로 남겼다(같은 파일 동시 편집 금지).
    20:49 에도 여전히 편집 중이라 착수하지 못했다. 그 세션이 커밋을 끝낸 뒤 시작한다.

### 35. 미수집 보관이 세 번 timeout 났고 합성 2건이 운영 보고서가 됐다
- **증상** 자율복구 `미수집 원본·사진·텍스트 보관`이 600초 회차에서 세 번 잘려 AI로
  넘어왔다. 상태에는 사진 21,060장 잔량이 남았는데 `미수집_수집보고서.md`는 글 2건·
  사진 2장이라고 했다. AI 티켓은 실제 timeout을 `로컬 업무 스크립트 종료 코드: 1`로
  보여 코드 오류처럼 읽혔다.
- **원인** 네 가지가 겹쳤다. ① `collect_all` 내부 자식 제한(최대 3,600초)이 바깥
  워치독 예산(600초)보다 길어 보고서를 쓰기 전에 나무째 잘렸다. ② 합성검증 [151]이
  `collect_all.REPORT`를 임시 경로로 바꾸지 않아 실 보고서를 합성 모수로 덮었다.
  ③ 현황은 `BAND_DIR` 전체 확장자 개수라 예전 개정본·문서사진까지 합쳐 PDF 8,692가
  현재 글 7,790보다 많았다. ④ 보관기는 PDF+TXT만 있으면 글을 건너뛰어 같은 글의 빠진
  사진을 다시 받지 않았다.
- **조치** 현재 캐시가 가리키는 세트 경로만 보관 폴더 한 번 순회로 맞댄다. 합성 모드
  보고서는 임시 폴더로 격리했다. `collect_all`은 `proc_guard.run_tree()`·420초 자체
  예산·45초 마감 여유로 보고서를 쓰고 돌아오며 예산 이월과 실제 실패를 가른다.
  보관기는 PDF·TXT가 있어도 누락 사진을 재시도하고 사진·텍스트·PDF를 임시파일 뒤
  원자 교체한다. timeout AI 인계는 rc=124를 쓴다. 기존 큐는 다음 정규 회차 rc=0 확인
  전까지 수동 완료 처리하지 않았다.
- **재발방지** 검증 [216] — 합성 캐시 1글·사진 2장에 현재 세트 사진 1장만 둬 잔량 1을
  정확히 세고, PDF·TXT가 있는 상태에서 두 번째 사진만 복구하는지 본다. `collect_all`의
  420초 상한·`run_tree` 및 autopilot timeout=124 인계도 함께 고정한다.

### 39. 잠금 두 곳이 아직 `os.kill(pid, 0)` 이었다 — 규칙 밖에 남은 사본
- **증상** 아직 터지지 않았다. [210]·[211] 을 마치고 pid 생존을 판정하는 자리를 전수로
  훑다가 찾았다(`ledger_db._pid_alive` · `ledger_writer._pid_alive`).
- **원인** 둘이다. ① **신원을 안 본다** — 재사용된 pid 면 보관본·큐 잠금이 영원히
  '주인이 살아 있다'가 되어 스스로 안 풀리고, 회차가 매번 "이미 실행 중"으로 조용히
  건너뛴다([186] 이 tmp 로 겪은 것과 같은 모양). ② **`os.kill` 은 확인이 아니라 신호다**
  — 윈도우 파이썬에서 `CTRL_*` 아닌 신호는 문서상 `TerminateProcess` 로 내려가고,
  POSIX 에선 남의 프로세스에 PermissionError → '죽었다'로 읽혀 **산 주인의 잠금을 빼앗는다**.
  (사용자 판단으로 실측 실험은 하지 않았다 — 근거는 CPython 문서와 프로젝트 규칙 [121].)
- **조치** 둘 다 `pid_alive.owner_alive()` 로 넘겼다(`is not False` — 모르면 살아 있다).
  지문은 **자리가 아니라 이름표**(`pid_alive.stamp()` → `fp=<생성시각>`)로 적는다 —
  `ledger_db` 안에서 형식이 다른 두 잠금(`{pid} {iso}` · `{pid} {iso} {ns}`)이 **한 판정
  함수**를 써서, 자리로 읽으면 `monotonic_ns` 를 생성시각으로 오해해 산 잠금을 빼앗는다.
  연기 감시자(`watcher_pid`)에도 `watcher_started_at` 을 붙였다 — 번호만 물려받은 남을
  '감시중'이라 하면 연기된 회차를 아무도 안 잇는다.
- **재발방지** 검증 [227] — 두 파일에 `os.kill(int(` 가 다시 들어오는 것을 막고,
  이름표 지문이 자리와 무관하게 읽히는지·재사용 회수와 산 주인 보호가 실제로 되는지 본다.

### 34. 진행 자국(progress) 판정에는 [210] 신원 검사가 빠져 있었다
- **증상** #33 과 같은 재사용(pid 37128 → `quick_share_server.exe` 11:15:09 시작)에서
  잠금은 회수됐는데, 인계 경보의 단계 힌트(`지금 단계: X · N분째`)는 여전히 죽은
  회차의 자국을 "돌고 있다"처럼 보여 줬다 — '기다려라'로 읽히는 정반대 지시다.
- **원인** `session_handoff.daily_step_now()` 가 `.daily_run.progress.json` 을 그냥
  읽어 단계·경과만 돌려줬다 — 자국을 쓴 pid 가 아직 그 프로세스인지 안 봤다.
  그리고 생성시각(born_before)만으로는 **기록 시각보다 먼저 떠 있던 상주 서비스**가
  pid 를 물려받은 경우를 못 가른다.
- **조치** `_progress_owner_alive()` — 자국의 '회차시작'·'시각'보다 뒤에 태어난
  프로세스는 주인이 아니고([210]과 동일), 이름이 읽히는데 python 이 아니면 남이다
  (`pid_alive.image_name()` 신설). `_daily_run_inflight`(잠금 나이 판정)에도 이름 문을
  걸었다. 죽었으면 `_step_hint` 가 "★회차 프로세스가 죽었다(끝나기를 기다리지 말 것)"
  로 반대로 말한다. '(회차 끝)' 자국은 판정하지 않는다(완주한 회차의 pid 는 원래
  죽는다). progress pid 를 읽는 다른 자리는 없음을 확인했다(워치독은 안 읽고,
  local_ai.a_round 는 단계 이름만 읽는다).
- **재발방지** 검증 [211] — 산 pid 로 재사용을 재현해 '죽음' 판정·경보 문구까지 보고,
  이름 계기(image_name)가 조용히 눈머는 것([169]꼴)도 잡는다.

### 33. 죽은 회차의 pid 를 딴 프로그램이 물려받아 '5시간째 돌고 있다'가 됐다
- **증상** 09:50 일일대조가 '거래처코드 색인' 단계에서 죽었는데, 인계 문서·경보가
  다섯 시간 동안 "지금 돌고 있다 — 새로 띄우지 말고 기다려라"(정반대 지시)를 냈다.
  잠금도 스스로 안 풀려 다음 회차가 전부 조용히 건너뛰었다.
- **원인** 둘이 겹쳤다. ① 윈도우가 죽은 회차의 pid(37128)를 `quick_share_server` 에
  **재사용**했는데 `pid_alive.alive()` 는 '그 번호가 있나'만 봐서 True 를 줬다 —
  번호가 같다고 같은 프로세스가 아니다. ② 그 단계가 죽도록 오래 걸린 이유는
  `customer_index.load_customers()` 의 Z: 재귀 glob + 파일마다 `getmtime`
  ([198]과 같은 병) + 매번 SMB 워크북 80개 열기였다.
- **조치** `pid_alive.started_at()`(프로세스 생성시각) + `alive(pid, born_before=)` —
  잠금·점유가 쓰인 시각보다 **뒤에 태어난** 프로세스는 주인이 아니다(증명이지 짐작이
  아니다). `daily_run` 잠금 회수·`session_handoff` 진행판정·점유판정이 시각을 넘긴다.
  `customer_index` 는 `walk_stat` 한 번 훑기(실측 101개 0.1초) + 후보 지문 캐시로
  안 바뀐 날은 워크북을 다시 안 연다.
- **재발방지** 검증 [210] — 산 pid 로 재사용을 재현해 잠금이 실제로 회수되는지,
  캐시가 워크북 재열기를 막는지까지 본다.

## 2026-08-09

### 32. Z: 단절 하나가 18개 실패로 번졌고, 복구는 다음 날까지 미뤄졌다
- **증상** 21:08 종합리포트에서 관리대장·세금계산서 감시·밴드 대조·폰 사본·대표
  브리핑 등 18개 단계가 한꺼번에 실패했다. 작업 스케줄러의 일일대조·원장반영 결과도
  `267014`·`1`이었지만 다음 실행 전까지 이어 하는 주체가 없었다.
- **원인** 회차 도중 Z:가 끊겼는데 각 단계가 같은 `FileNotFoundError: 관리대장을 찾을 수
  없음`을 독립 코드 결함으로 기록했다. 재시도는 현재 함수 안 한 번뿐이고 회차가 끝나면
  실패의 명령·다음 시도·성공 확인이 남지 않았다. 즉 **원인 하나를 18번 세면서도
  복구 책임은 한 번도 저장하지 않았다.**
- **조치** `autopilot.py` 영속 대기열을 추가했다. `daily_run`이 자원·인증·코드·시간초과를
  분류하고, 재실행 안전한 명령만 명령 지문으로 중복 제거해 저장한다. 워치독이 30분마다
  최대 2개·10분 예산으로 실행하고 반환코드 0을 확인한 뒤 닫는다. 코드·시간초과가 3회
  반복될 때만 `agent_dispatch`로 Claude Code→Codex 검토를 자동 요청한다.
- **안전 경계** `--apply`·`--queue`·전송·게시·삭제는 반쯤 성공했을 가능성이 있어 자동
  재실행하지 않는다. 로그인·실발행·외부 메시지는 권한 없이 우회하지 않는다.
- **곁가지** `agent_dispatch`도 Windows에서 멈출 수 있던 `subprocess.run(timeout=)`을
  공용 `proc_guard.run_tree()`로 바꿨다. `font_switch.py`는 CRLF를 LF로 바꿔 왕복
  무결성 검증을 깨던 읽기 모드를 고쳤다.
- **재발방지** 검증 `[190]`(중복 없는 대기·쓰기 재실행 금지·워치독 배선·AI 제한 실행)
  및 기존 `[126]`(글꼴 왕복 바이트 보존).

---

## 2026-08-08

### 30. 회차 한 단계가 13시간 30분을 서 있었고, 그동안 하루치 대조가 통째로 안 돌았다
- **증상** `session_handoff` 에 "일일자동대조 22시간째 완주하지 않았다 / 지금 13시간째
  돌고 있다". 스케줄러는 매일 '성공'으로 보고하고 있었다.
- **원인** `daily_run._run_once` 가 `subprocess.run(timeout=1800)` 을 썼다. CPython 은
  시간이 넘으면 `kill()` 을 부른 뒤 **윈도우에서만 시간제한 없는** `communicate()` 를
  한 번 더 부른다. 자식(`source_tidy.py`)이 SMB(Z:) 대기에 걸려 TerminateProcess 가
  안 먹자 **그 두 번째 드레인이 끝나지 않았다.** 부모 CPU 0.4초 · 자식 0.5초 —
  13시간 반 동안 둘 다 그냥 서 있었다.
- **이게 왜 무서운가** 멈춘 회차가 **락을 쥔 채**다. 다음 회차는 "이미 실행 중"으로
  조용히 건너뛰고 스케줄러는 '성공'이라 적는다. 09:50 이 하는 일(접수취소 확인·객관
  완료·청구상태·전 대조)이 하루 종일 안 도는데 **어느 화면에도 티가 안 난다.**
  '느린 것'과 '멈춘 것'이 겉으로 같다는 8/8 낮의 교훈이 회차 층에서 되풀이됐다.
- **조치** `Popen` + 제한 있는 `communicate` + `taskkill /F /T`(윈도우 `kill()` 은
  손자를 안 죽인다) + **죽인 뒤 드레인에도 제한**. 그래도 안 죽으면 pid 를 리포트에
  남기고 다음 단계로 간다. 멈춰 있던 나무(79936→103020)는 끊었다 — source_tidy 는
  README·하드링크만 쓰고 원본을 지우거나 옮기지 않으므로 끊어도 잃는 것이 없다.
- **재발방지** 검증 `[175]` 가 `_run_once` 본문에 `subprocess.run(` 이 없을 것,
  `communicate(timeout=` 가 **두 번** 나올 것, 나무째 죽일 것을 지킨다. 그리고 실제로
  1초 제한을 걸고 30초짜리 자식을 돌려 **예외 없이 결과가 돌아오는지** 확인한다.

### 29. 원장 한 통에 네 화면이 들어 있어, 남의 회사 전표로 현장 확인을 지시했다
- **증상** `ERP원장대조` 가 매일 "A. ERP에만 있는 전표 (설치·작업 근거 확인 필요 ★)
  **1,856건**" 을 냈다. 파일도 있고 숫자도 나오니 아무도 실패로 안 읽었다.
- **원인** 분류 규칙이 한 줄이었다 — "'적요' 와 차변/대변이 같은 표에 있으면 `ledger`".
  이카운트 회계 원장류는 **넷 다 그 모양이다.** `pick("ledger")` 12개의 정체는
  거래처별계정별원장 5 · 계정별원장 5 · 분개장 1 · 현금출납장 1 이었다. 게다가
  계정별원장 한 파일은 한 시트에 **52개 계정이 층층이** 쌓여 있는데(현금·보통예금·
  외상매입금·부가세예수금 …) 파서는 **맨 첫 머리글만** 찾고 나머지 8,980행을 그
  계정의 것으로 읽었다. 그래서 남양유업·모벤티스·유한양행 전표가 '쿠팡 매출'이 됐다.
- **이게 왜 무서운가** 그 1,856건은 **아무도 확인하러 갈 수 없는 지시**였고 매일
  09:50 마다 새로 찍혔다. 경보가 대부분이면 아무도 안 보게 된다(8/8 [170] 과 같은 갈래).
- **조치** `inbox_scan.ledger_kind()` 가 **머리글**로 넷을 가른다(제목줄은 잘리고 사람이
  바꾸므로 차선책일 뿐). 분개장 판정이 '적요' 판정보다 **먼저** 와야 한다 — 분개장에도
  적요·거래처가 있다. 전표 5,157 → **689건(고유 302)**, 유형A 1,856 → **296건**.
- **곁가지 셋**
  · 규칙을 고쳐도 `inbox_classify.json` 이 옛 답을 붙들고 있었다(열쇠가 크기·시각뿐).
    `source_index.rules_version()` 도 `classify_rows` 만 해싱해 `ledger_kind` 를 놓쳤다.
    **같은 사고 세 번째**다 — 이제 `inbox_scan.RULES_VERSION` 하나가 두 캐시를 다 민다.
  · 갈래를 늘리면 `collect_sources.KNOWN`·`download_intake` 도 넓혀야 한다. 안 넓혔으면
    그날부터 그 자료가 '모르는 종류'가 되어 **조용히 안 들어왔다.**
  · 가른 뒤에도 '정상 0건'은 그대로였다(302 중 6). 06시트 `거래명세서번호` 와 회계
    `일자-No.` 는 **다른 순번**이다. 열쇠가 안 맞으면 짝이 안 지어진 것이 전부
    '현장 확인 필요'로 나온다 — `key_looks_wrong()` 이 리포트 맨 위에 그 말을 적는다.
- **재발방지** 검증 `[203]`(네 화면 가름·순서·slips 안 샘) · `[173]`(규칙 고치면 캐시도
  다시 판별) · `[174]`(짝이 거의 없으면 열쇠를 의심).

---

## 2026-08-07

### 25. 보고 시트가 자기를 원장으로 읽어 제가 올린 문제를 지웠다
- **증상** 합성검증 `[8]` 의 **멱등**이 깨졌다. 같은 내용으로 두 번 돌렸는데 두 번째가
  새 버전을 만들었고 건수가 **2208 → 1985** 로 줄었다(223건 실종).
- **원인** `findings_export.doc_unregistered()` 는 관리대장 **모든 시트**의 4행에서
  '프로젝트NO' 열을 찾아 "이미 등록된 프로젝트" 집합을 만든다. 23시트를 8열 → 12열로
  넓히며 프로젝트NO 를 **독립된 열로** 뺐더니(예전엔 '내용·근거' 문장 속에 묻혀 있어
  머리글로 잡히지 않았다), 다음 실행 때 **23시트가 스스로를 원장으로 읽혀** 자기가
  올린 미등록 문서를 "이미 등록됨"으로 지웠다. **보고가 자기를 먹었다.**
- **이게 왜 무서운가** 멱등 검사가 없었으면 아무도 몰랐다. 화면에는 "미등록 문서 없음"
  이라는 **멀쩡해 보이는 거짓말**이 떴을 것이다. 실물 관리대장(v541)에서도 이미
  1건이 가려져 있었다(고치기 전 0건 / 고친 뒤 1건 — 실측). 합성에서 223건이나 났던 건
  합성 원장이 작아 OCR 문서 대부분이 '미등록'으로 보였기 때문이고, **크기만 다를 뿐
  같은 사고**다. 수집 밀림과 같은 갈래의 조용한 사고다.
- **조치** 보고 시트 1행에 표식(`AGENT_SHEET_MARK = "에이전트 자동 갱신"`)을 두고,
  원장을 훑는 쪽이 `findings_export.is_agent_sheet()` 로 건너뛴다.
  `fix_freeze.py` 도 같은 이유로 건너뛴다(틀고정을 고쳐 쓰면 `findings_sheet` 와
  서로 되돌리며 버전만 끝없이 는다).
- **재발방지** 시트 이름 목록으로 막지 **않았다** — 목록은 다음 보고 시트를 만들 때
  잊는다. 표식은 `build_generic_sheet` 가 자동으로 넣으므로 잊을 수가 없다.
  검증 `[8]` 이 표식·거르기·멱등을 함께 지킨다.
- **교훈 두 가지**
  ① **관리대장에 무언가를 쓰면 그것을 읽는 쪽을 반드시 같이 본다.** 쓰기만 확인하면
     되먹임을 놓친다. "이 값을 누가 읽나?" 가 시트를 늘릴 때의 첫 질문이다.
  ② **멱등 검사는 있어야 한다.** 이 사고를 잡아낸 건 기능 검사가 아니라 "두 번 돌리면
     같아야 한다" 였다. 값이 조금씩 어긋나는 사고는 그것 말고 잡을 방법이 거의 없다.

### 23. 없는 글 40건을 "수집 성공"으로 받아 캐시에 넣었다 — 전부 같은 글이었다
- **증상** `--adopt` 가 "밴드 84789192 수집 2일 밀림"이라 해서 `recheck_plan` 이 준
  3539~3578 마흔 건을 걸었다. 결과는 **ok 40 · fail 0**, 덤프도 40건. 캐시는
  3308→3348 로 늘었다. 그런데 40건이 **전부 같은 본문**(PO375207)이었고
  글쓴이·시각이 **전부 비어 있었다**. `--check` 는 여전히 "최신 2026-08-05, 밀림"
  이라고 말했다 — 그 40건에 날짜가 없어 최신일이 안 올라갔기 때문이다.
- **원인** 두 가지가 겹쳤다.
  ① **밴드의 실제 최신 글은 3538 번이다.** 3539 부터는 아직 존재하지 않는다.
     밴드는 없는 글 번호에도 **HTTP 200 + 앱 껍데기**를 준다(리다이렉트도 없다).
     그 화면에는 직전 화면(피드 맨 위 글)의 본문이 남아 있어 `.postText` 가 잡힌다.
  ② `grabOne` 이 시각(`.time`)을 3초까지만 기다린 뒤 **없어도 `status:'ok'`** 로
     넘겼다. 본문이 있으니 성공으로 보였다.
  → 즉 "수집이 밀렸다"는 진단부터 틀렸다. **밴드에 8/5 이후 새 글이 없었을 뿐**이다.
- **조치**
  · `grab_posts.js` — 시각이 끝내 안 붙으면 `status:'fail'`(수확 폐기). `'missing'`
    이 아니라 `'fail'` 인 이유: 3539 는 **내일이면 진짜로 생긴다.** 묘비를 세우면
    recheck_plan 이 그 번호를 영영 다시 안 뽑는다(사고 19 의 교훈과 반대 방향).
  · 오염 40건을 캐시에서 제거해 3308(1~3538)로 되돌리고, 덤프는
    `reports/오염덤프_격리/` 로 옮겼다 — 그대로 두면 `convert_dump` 가 매 회차 다시 병합한다.
- **재발방지** 검증 `[130]`.
- **교훈** 사고 19·22 는 "안 되면 멈춘다"를 배웠다. 이번은 그 반대다 —
  **되는 것처럼 보이는 실패**가 더 위험하다. 수집기가 성공을 선언하려면
  "본문이 있다"로는 부족하고 **그 글이 그 글인지**를 말할 수 있어야 한다.
  시각이 그 최소 신원이다. 신원 없는 수확은 세지 말고 버린다.
- **남은 것** `--adopt`/`--check` 의 '밀림' 판정은 아직 **"밴드가 조용한 것"과
  "우리가 못 모은 것"을 구분하지 못한다.** 오늘 반나절이 여기서 나갔다.
  최신 글 번호를 함께 보고 "받을 것이 남았는가"로 판정해야 한다.

### 24. 민감한 통화 메모가 공유 폴더와 앱 목록에 그대로 떠 있었다
- **증상** 앱 '원본 자료' 화면에 `통화_20260805_김준형.md` 가 카드로 보였다.
  통화 메모에는 사람 이름·평가·거래 조건이 섞인다.
- **원인** `call_notes.py` 가 메모를 `0. 원본 자료/10. 통화·회의 기록/` 으로
  **복사**했는데 그곳은 Z: **공유 폴더**이고, `source_index` 가 그걸 그대로 색인했다.
  "보관"과 "공개"를 같은 동작이 하고 있었다.
- **조치** 본문은 DB(`ledger_db.call_note`)에만 넣는다. 색인은 `is_private()` 로
  통화 메모를 **아예 담지 않는다**(색인이 곧 `/api/source-file` 화이트리스트라
  목록에도 안 뜨고 내려받이도 막힌다). 기존 2건은 `--migrate` 로 옮기고 원본 삭제.
- **재발방지** 검증 `[129]`.
- **교훈** 민감 자료는 **숨기는 것이 아니라 두지 않는 것**이 조치다.
  파일이 있으면 언젠가 어떤 화면이 그것을 찾아낸다.

---

## 2026-08-06

### 21. 밴드 글 7,528건이 "수집 대상 밖"에 있었다 — 구멍인 줄도 몰랐다
- **증상** "밴드 전량 재확인 완주(잔여 0)"라고 다섯 번 보고했는데, 실제로는 캐시가
  90610953 은 4196~5424, 84789192 는 3334~3538 뿐이었다. 1번부터 글이 실재하고
  **2023-03-28** 부터 쌓여 있다 — 합쳐서 **7,528건**이 한 번도 수집되지 않았다.
- **원인** `recheck_plan.plan()` 이 구멍을 `range(lo, hi+1)` 로 계산했다. `lo` 는
  **보유한 것 중 가장 작은 번호**다. 그래서 아래쪽은 정의상 구멍이 될 수 없었고,
  "구멍 0"이라는 출력이 매번 안심시켰다. 없는 것을 없다고 말해 주지 않는 지표였다.
- **조치** `--floor` 추가(1 이면 밴드 개설 이후 전량이 대상). 진행률은
  `reports/밴드_전량수집_진행.json` 에 남겨 세션이 끊겨도 이어받는다.
- **교훈** "잔여 0"은 **무엇을 분모로 잡았는지**를 함께 말하지 않으면 거짓말이 된다.
  범위를 스스로 정하는 지표는 그 범위를 늘 함께 출력해야 한다.

### 22. 배치를 500건으로 올렸다가 탭이 얼어 수집분을 잃을 뻔했다
- **증상** 250건 배치는 다섯 회차를 멀쩡히 돌았는데 500건으로 올리자 CDP 호출이
  45초 타임아웃(`renderer may be frozen`). 그 시점 수집분 400여 건은 탭 메모리에만
  있어 새로고침하면 통째로 사라진다.
- **원인** iframe 을 500번 만들고 지우는 동안 렌더러가 버티지 못했다.
- **조치** `grab_posts.js` 에 `BATCH_MAX = 250` 을 두고 넘으면 시작 자체를 거부한다.
- **교훈** 브라우저 안에서 도는 수집은 **중간 산출물이 휘발**이다. 배치를 키우는 것은
  속도가 아니라 위험을 키우는 쪽이다 — 자주 저장하는 편이 빠르다.

---

## 2026-08-05

### 19. 밴드 수집기가 15분에 0건 — "느린 것"이 아니라 크롬이 재운 것이었다
- **증상** `__grabStart(90610953, 250건)` 을 걸고 15분(878초)을 기다렸는데 ok·missing·fail
  전부 0. 죽지도 않고 `running:true` 인 채 첫 글에서 멈춘 것처럼 보였다.
- **처음 본 원인(틀림)** "밴드 상세는 원래 글당 5초+ 걸려 느리다"고 넘겼다. 앞 세션 메모에도
  그렇게 적혀 있어 두 세션이 같은 오해를 물려받았다.
- **진짜 원인** **탭이 뒤에 있으면 크롬이 `setTimeout` 을 늦춘다.** 숨은 탭은 1초 간격,
  5분 넘게 숨어 있으면 intensive throttling 으로 **1분 간격**이 된다. 수집기는 본문이
  그려질 때까지 `setTimeout(400ms)` 를 30번 도는 구조여서, 12초짜리 대기가 **30분**이 됐다.
  자동화는 탭을 앞에 두지 않으므로 이 조건은 **항상** 성립한다 — 어쩌다 느린 게 아니었다.
- **조치** `band/grab_posts.js` 에서 **페이지 타이머를 전부 걷어냈다.**
  · 지연 → **Web Worker 타이머**(워커는 throttling 대상이 아니다)
  · 본문 등장 대기 → **MutationObserver**(이벤트는 늦춰지지 않는다)
  · 잘못 건 배치를 새로고침 없이 끊도록 `__grabStop()` 추가(새로고침하면 모은 것이 날아간다).
  같은 조건(숨은 탭)에서 **7분에 55건** — 0건에서 글당 7.4초로 돌아왔다.
- **교훈** 브라우저 안에서 도는 자동화는 **탭이 숨어 있다는 전제로** 짠다. `setTimeout` 은
  숨은 탭에서 약속한 시각을 지키지 않는다. "느리다"는 진단은 `sec` 대비 처리 건수가
  0일 때 성립하지 않는다 — 0건은 느린 게 아니라 **멈춘 것**이다.
- **재발방지** 검증 `[101]` · 파일 머리말에 "여기에 `setTimeout` 을 다시 넣지 말 것" 명시.

---

## 2026-07-31

### 0. 사람이 입력 중인 관리대장을 자동화 두 개가 밟았다 — 입력 유실·엑셀 강제종료 반복
- **증상** 류지영 매니저(다른 PC): "자꾸 엑셀 멈춰서 강제종료… 하나 입력하고 저장하는데도
  다 날라가여… 새거에 제가 입력한게 반영이 안되어있어여." Excel 문서복구 사본 다수.
- **원인** ① **autoprune 이 열린 파일을 옮겼다** — v336 생성 뒤 매니저가 v331 을 저장할
  때마다 "구버전"으로 판정해 OLD 로 이동(`__from_작업폴더` 8사본). 열린 파일이 밑에서
  사라지니 엑셀이 저장 충돌·응답없음·강제종료. ② **15:05 반영이 잠금을 무시** — `~$` 잠금이
  있었지만 "이 PC 에 EXCEL 프로세스 없음"으로 잔재 오판. 로컬 프로세스로는 **다른 PC 의
  편집이 안 보인다.** 매니저의 15:43 저장은 구판(v331)에 갇혀 고아가 됐다.
- **조치** 셀 대조로 순유실 확정: 14:59까지의 입력은 v336 에 있었음(반영 밑판).
  `UJ2600094` 진행상태='작업완료' 사고복구 큐 적재, `UJ2601070` 은 방향 불명 → 사람 확인.
  저장 전 크래시 유실분은 파일에 없음 — 매니저 PC 문서복구 사본을 받으면 대조 가능.
- **재발방지** [94] ① `ledger_versions._in_use()`: 잠금 있거나 30분 내 저장된 파일은
  옮기지 않는다(판단 불가여도 안 옮긴다) ② `ledger_db.human_editing()` + 쓰기 전 관문:
  잠금(24h 이내)=사람. 유예 내 대기 후 회차 건너뜀(큐 보존), `--force` 로도 못 뚫는다.
  ③ 교훈: **네트워크 공유에서 로컬 프로세스 검사는 무효** — 잠금파일이 진실이다.

---

## 2026-07-28

### -5. 코덱스 예약 작업이 통째로 죽었다 — 대화 스레드에 묶여 있었다
- **증상** 코덱스 화면에 `작업 생성 중 오류: thread not found: 019fa61d-…` +
  `Error submitting message`. 그 대화창에서는 무엇을 쳐도 전송 실패.
- **원인** 우리 코드 문제가 **아니다.** '쿠팡 실시간 문제 협업감시(4시간마다)' 예약 작업이
  **특정 대화 스레드 ID에 묶여** 있었는데 그 스레드가 서버에서 사라졌다.
  작업은 4시간마다 없는 스레드에 글을 쓰려다 실패하고, 사람도 그 창에서는 말을 못 건다.
- **확인한 것** 우리 쪽은 멀쩡했다 — 점유 잔여 0, 떠도는 감시 프로세스 0,
  윈도우 예약 작업 3종(실시간문제감시·워치독·일일자동대조) 전부 결과 0(정상).
- **조치(사람)** ① 그 대화창은 버린다(재시도해도 계속 실패) ② ChatGPT 예약 작업 목록에서
  그 작업을 지운다 ③ 새 대화에서 다시 만든다.
- **교훈** **자동화를 대화 스레드에 묶지 않는다.** 대화가 사라지면 자동화도 같이 죽는다.
  우리 감시는 윈도우 예약 작업 + 파일(AGENTS.md·19시트·git)에 묶여 있어 대화가
  사라져도 살아남는다 — 이 구조를 유지한다.

### -4. 자동 정리가 **남의 파일을 옮겼다** (합성검증이 잡음)
- **증상** 구 버전을 OLD 로 자동으로 접게 만들자마자 합성검증이
  `관리대장을 찾을 수 없음: 합성대장F_v1.xlsx` 로 깨졌다.
- **원인** `autoprune` 이 기존 `plan()` 을 그대로 썼는데 그게 `*_v*.xlsx` 를 **통째로** 잡는다.
  그래서 시험용 임시 폴더의 `합성대장F_v1.xlsx` 까지 OLD 로 옮겨 버렸다.
- **조치** 자동으로 도는 정리는 `^쿠팡_통합업무_일일보고_관리대장_v\d+\.xlsx$` 에**만** 손댄다.
  최신본·'보관' 표시본·남의 파일은 건드리지 않고, **지우지 않고 옮기기만** 한다.
- **재발방지** 검증 `[41]` 이 임시 폴더를 만들어 남의 파일이 안 옮겨지는지 직접 확인한다.
- **교훈** 사람이 부르는 명령(`--prune`)과 **저절로 도는 것**은 기준이 달라야 한다.
  저절로 도는 쪽은 아무도 안 보고 있으므로 **대상을 최대한 좁힌다.**

### -3. 셀 하나를 고치다 **옆 칸을 통째로 지울 뻔했다**
- **증상** 03_현장작업실적 33행 비고를 채웠더니 검증이 `AK33 이 바뀌었다`며 멈췄다.
- **원인** `workbook_patch.replace_inline_cell` 의 패턴이 `<c r="AJ33"[^>]*(?:/>|>.*?</c>)`.
  **빈 셀은 자기닫힘**(`<c r="AJ33" s="5"/>`)인데 `[^>]*` 가 `/` 까지 삼킨 뒤
  `>.*?</c>` 가 **다음 셀의 `</c>` 까지** 매치했다. 즉 옆 칸이 통째로 사라진다.
  내용이 있는 셀에서는 안 터져서 그동안 드러나지 않았다(A2 교체 등은 무사).
- **조치** 속성을 `이름="값"` 형태로만 받게 좁혀 `/` 를 못 삼키게 했다.
- **재발방지** 검증 `[41]` 이 빈 셀 교체 후 옆 칸 생존과 셀 개수를 확인한다.
- **교훈** '지운 것 말고는 한 칸도 안 바뀌었나'를 **전수 대조**하지 않았으면 그대로 저장됐다.
  검증이 값을 하는 순간이 이런 때다.

### -2. 담당기사 칸에 작업 메모가 들어가 대표보고에 그대로 나갔다 (10건)
- **증상** 대표보고 '오늘 우선 조치 TOP 5' 에 `담당: 000 (캠프상태확인 및 스케쥴 세팅)`.
- **원인** `band_extract.normalize_tech` 가 밴드 글에서 뽑은 값의 **첫 조각을 그대로 통과**시켰다.
  걸러 주는 `clean_tech` 는 이미 있었지만 **리포트에서만** 쓰이고 원장에 쓸 때는 안 거쳤다.
  (비고가 전부 '밴드 자동등록' 이었다 — 사람 오타가 아니라 우리 추출기가 넣은 값이다.)
- **조치** ① `normalize_tech` 재작성 — 괄호 설명 제거, 낱말이 **넷 이상이면 문장으로 보고 버린다**,
  붙여 쓴 직책 분리(`김승기기장`→`김승기`), 업체 목록 제외(`하이테크`·`대신택배`),
  자리표시자(`000`·`자)`·`미배정`) 제외. ② 기존 10건은 `fix_tech_names.py` 로 정정.
- **정보를 버리지 않는다** 담당기사에서 뺀 값은 **비고에 `담당기사 원본: …` 로 남긴다.**
  나중에 "왜 이름이 바뀌었지" 를 되짚을 수 있어야 한다.
- **함정** 조각을 하나씩 보면 '스케쥴'·'체크' 같은 낱말도 `[가-힣]{2,4}` 라 **이름처럼 통과한다.**
  칸 전체를 먼저 판단해야 한다. 처음 구현에서 이걸 놓쳐 검증에서 잡혔다.
- **함정 2** 02시트에는 공유수식이 5,321칸 있다(A·F·M·AK·AL·AN·AP·AR). '시트에 있으면 중단'
  으로 막으면 아무것도 못 고친다 — **건드릴 칸이 그 그룹에 속하는지**만 봐야 한다.
- **재발방지** 검증 `[41]` — normalize_tech 10가지 입력 + 원장 담당기사 전수 검사.

### -1. 대시보드 '당일 실적' 표가 통째로 0이었다 — 채우기 내림 한 번 때문에
- **증상** 화면 오른쪽에 `26` 이 56칸, 빈 구역마다 `0`. 지저분한 정도로 보였다.
- **진짜 문제** `H46` 은 '조회일 직접입력(선택)' — **사람이 비워 두는 칸**이다. 여기에
  엉뚱한 수식이 흘러들어 `0` 이 됐다. 적용일 `K46 = IF(H46="",집계기준일,H46)` 은
  빈칸이 아니라 0 을 받아 **적용일 = 1900-01-00**. 당일 실적 표는 전부
  `COUNTIFS(…,$K$46)` 이라 한 건도 안 잡혔다. 좌상단 '신규 접수 5' 와 당일 '0' 이
  어긋나 보인 이유가 이것이다. **미관 문제로 넘겼으면 표가 죽은 채로 남았다.**
- **원인** K5 를 아래로 끌어 채운 흔적. 같은 사고가 B·E·H 열에도 나 있었다(합계 150칸).
- **조치** `dashboard_clean.py` — 살릴 칸을 명시하고 나머지를 **스타일만 남기고 비운다**.
  공유·배열 수식이 없음을 먼저 확인하고(있으면 중단), 차트 5개·다른 zip 파트가
  안 바뀐 것까지 확인한다. `fullCalcOnLoad` 를 켜 파일을 열면 곧바로 재계산된다.
- **재발방지** 검증 `[41]` 이 잔해 0칸과 `H46` 이 비었는지 본다.
- **교훈** '보기 싫다'로 들어온 요청 뒤에 **죽어 있는 계산**이 숨어 있을 수 있다.
  화면을 고치기 전에 그 칸이 무엇을 먹여 살리는지 먼저 본다.

### 0. '금일'이라 적어 대표 보고에서 날짜가 헷갈렸다
- **증상** 타일에 "점검 예정 (금일)"이라 적혀 있는데, 그 숫자는 **집계기준일(어제)** 것이다.
  읽는 사람은 '금일'을 오늘로 받아들인다. 보고 자리에서 어긋난다.
- **원인** 라벨을 엑셀 대표보고 시트에서 그대로 가져다 썼다. 엑셀에서는 그 시트 자체가
  하루치라 '금일'이 통했지만, 앱은 여러 날을 오가므로 통하지 않는다.
- **조치** ① 화면·이미지에서 `(금일)` → **실제 날짜**로 바꿔 찍는다(`dateLabel`).
  ② 절 맨 위에 "2026-07-27(월) 하루치입니다" 띠를 놓는다.
  ③ 브리핑 문장의 **모든 항목을 '날짜 · 프로젝트NO · 캠프' 로 시작**시키고,
  완료건에는 `(접수 07-23 · 4일 만)` 을 붙여 언제 들어온 건인지 되묻지 않게 했다.
- **교훈** '금일·당일·오늘'처럼 **읽는 시점에 따라 달라지는 말**은 보고서에 쓰지 않는다.

### 1. 폰·PC 앱이 동시에 죽었다 — 설치 아이콘에 임시 주소가 박혔다
- **증상** 설치된 앱이 `hint-links-coding-away.trycloudflare.com` 로 `ERR_FAILED`. PC·폰 둘 다.
- **원인** 터널 주소로 접속한 상태에서 [설치]를 하면 매니페스트 `start_url:"/"` 때문에
  **그 임시 호스트가 아이콘에 영구히 박힌다.** 터널은 재시작마다 주소가 바뀌므로 즉시 죽는다.
  고정 주소를 만들어 둔 의미가 통째로 없어졌다.
- **조치** 앱 서버가 `Host` 를 보고 **trycloudflare 요청에는 매니페스트·서비스워커를 빼서** 내보낸다.
  설치는 고정 주소(GitHub Pages)에서만 되고 거기 `start_url` 은 바뀌지 않는다.
- **재발방지** 검증 `[26]`
- **사용자 조치 필요** 죽은 아이콘 삭제 후 고정 주소에서 재설치.

### 2. 며칠간 폰 접속 불가 — 사내 DNS가 터널 도메인을 막고 있었다
- **증상** 폰에서 계속 접속 실패. PC에서 터널을 찔러 보면 늘 `getaddrinfo failed`.
- **원인** 사내 DNS가 `*.trycloudflare.com` 을 안 풀어 준다(공개 DNS로는 정상).
  그래서 `publish_endpoint` 는 "죽은 주소"라며 게시를 취소하고, `watch()` 는 "터널이 죽었다"며
  4분마다 주소를 새로 만들었다. **정작 폰(LTE)에서는 살아 있는 주소였다.**
- **조치** `net_probe.py` — 공개 DNS에 HTTPS(DoH)로 물어 IP를 얻고 SNI·Host는 원래 이름으로 보낸다.
  publish_endpoint·tunnel_run 이 이 판정을 쓴다. **두 곳에서 직접 urlopen 하지 말 것.**
- **재발방지** 검증 `[30]` · 진단 명령 `python net_probe.py`

### 3. 터널은 살아 있는데 폰에 HTTP 530만 왔다
- **원인** 터널 대상이 `localhost` 였고 윈도우에서 IPv6(`::1`)로 먼저 풀리는데 앱은 IPv4로만 듣는다.
- **조치** 대상을 `127.0.0.1` 로 고정. **재발방지** 검증 `[30]`

### 4. 게시가 조용히 실패해 고정 주소가 죽은 주소를 가리켰다
- **원인** `publish()` 가 결과를 안 보고 무조건 "갱신 완료"를 찍었다.
- **조치** `endpoint.json` 을 실제로 확인하고 3회 재시도. `watch` 가 90초마다
  '고정 주소 == 현재 터널'을 검사해 어긋나면 다시 게시(자가치유).

### 5. 24_밴드업무추출이 4,223행 → 506행으로 잘렸다
- **원인** `band/ingest.py` 가 `band_extract --month … --sheet` 를 **월별 루프**로 불렀는데,
  `--sheet` 는 시트를 통째로 덮어쓴다. 마지막 달만 남고 2025-12~2026-05가 사라졌다.
- **조치** `--month` 없이 **한 번만** 호출. 4,250행으로 복구.
- **재발방지** 검증 `[32]` (월별 호출 차단 + 행수 급감 감지)

### 6. 밴드 캐시가 본문을 2,000자에서 잘라 프로젝트NO 18개가 사라졌다
- **증상** Downloads 덤프를 지워도 되는지 확인하다 발견.
- **원인** `convert_dump`·`band_sync` 가 `content[:2000]`. 목록형 글은 그보다 길다
  ("미실시 및 AS 진행건 공유" 4,288자 → 코드 36개 중 18개 소실).
- **조치** 한도 20,000자로 올리고 전 덤프 재반영. 전수 대조 결과 손실 0.

### 7. 없는 잘못으로 기사 28명을 추궁할 뻔했다
- **증상** 엑셀 저장으로 옛 행 상태가 계산되며 2025-12-08·09 작업 28건이 '밴드 게시 미확인'.
- **원인** 쿠팡AS 밴드 캐시는 **2025-12-16부터**다. 기사가 안 올린 게 아니라 우리가 그 이전을 못 긁었다.
- **조치** '수집범위밖'으로 따로 구분. **밴드별로** 시작일을 보고 가장 늦은 것을 기준으로 삼는다
  (합쳐서 최소값을 쓰면 매출처 밴드가 쿠팡AS 밴드의 빈 구간을 덮는다).
- **재발방지** 검증 `[37]`

### 8. 확인하지 않은 것을 '확인 완료'로 찍을 뻔했다 (37건)
- **원인** `confirm_fill.band_map()` 이 프로젝트당 **먼저 나온 기록 하나만** 집었다.
  한 건에 글이 여러 개(접수 안내 → 작업완료) 올라오는데 접수 글이 잡히면 완료 글을 놓친다
  (전체 '첫 기록이 완료' 382 vs '완료 기록 존재' 859).
- **조치** 완료 글 우선 + 정보량 순 선택. 수정 후 대상 55건 **전부** 근거가 '작업완료'와 일치.
- **함께** `only_if_empty` 가 False 여서 기존 확인 기록을 덮을 수 있었다 → True.
- **재발방지** 검증 `[35]`

### 9. 행을 늘려도 ID가 안 매겨졌다
- **원인** `expand_rows.ID_FORMULA` 에 02·04만 있고 03·05·06이 빠져 있었다.
  03_현장작업실적을 120행 늘려 놓고도 '여유 0'인 채였다.
- **조치** 5개 시트 전부 등록. 날짜열이 시트마다 다르다(02·04=D, 03·05=E, 06=F).
- **재발방지** 검증 `[28]` 이 실제 시트 수식과 대조.

### 10. 06시트 채번 수식이 02시트 것을 복사한 채였다
- **원인** `"AS-"` 접두어 + `$D`(프로젝트NO)를 날짜로 읽고 있었다. 빈 행이라 아무도 못 봤지만
  **다음 정산 1건이면** 정산ID가 접수ID와 이름이 겹칠 뻔했다.
- **조치** `"JS-"` + `$F`(작업완료일)로 정정(v172). 도구 `fix_id_formula.py`.

### 11. ERP 내보내기 3개가 빈 파일이었다
- **증상** 13KB·20KB라 정상처럼 보였는데 **0행**(회사명 한 줄만).
- **조치** `inbox_scan` 이 행 수를 세어 "★ 비어 있음"을 표시. 앱 상태에도 노출.

### 12. PO PDF 파서가 세 번 연속 오판했다
1. ToUnicode CMap을 **전부 합쳐** 'Coupang'이 'Coupapg'가 되고 날짜가 깨졌다 → **폰트별**로 갈랐다.
2. 'PO번호 뒤 숫자'를 금액으로 잡아 3,445,994,200,000원이 나왔다
   (원문이 `오더 번호금액PO3445994,200,000 KRW` 로 붙어 있다).
3. 그래서 콤마 단위로만 끊었더니 앞자리가 잘려 4,200,000 → 200,000. **목록과 다르다며 4건을
   잘못 신고할 뻔했고, 원문을 열어 보니 목록이 맞았다.**
- **조치** 서식이 두 가지(라벨형/표형)임을 인정하고 둘 다 처리 + 떼어 낸 PO번호가 파일과 같은지 확인.
- **교훈** 대조 결과가 '상대가 틀렸다'로 나오면 **원문을 먼저 열어 본다.**

### 13. 견적서를 PO 금액으로 잡아 불일치 46건
- **원인** 새 폴더는 PO별 하위 폴더에 **견적서 첨부**가 들어 있고, 첨부는 상위 폴더명에서 PO번호를
  가져오므로 통지문과 같은 번호가 된다. 견적서는 PO를 캠프별로 쪼갠 일부라 금액이 훨씬 작다.
- **조치** 통지문만 PO 금액으로 쓰고 견적서는 `quotes_by_po` 로 따로.
- **남은 2건**은 정확히 ×1.1 — 원본이 **부가세 포함**을 적은 것이라 '부가세 표기차'로 구분.
  (하필 미청구 7건 중 2건이라 그대로 뒀으면 멀쩡한 건을 조사할 뻔했다)

### 14. 자료가 있는데 '없음' 4건이 거짓 표시
- **원인** '내가 확인할 사항'이 `/api/status` 도착 **전에** 그려져 srcStats 가 빈 채로 판정.
- **조치** 상태 수신 후 다시 그린다. 또 '파일 없음'과 '넣었는데 비어 있음'을 구분.

### 15. 주석에 Windows 경로를 넣다 NUL 바이트가 들어가 모듈이 죽었다
- **증상** `inbox_scan.py` → `source code string cannot contain null bytes`.
- **원인** heredoc 안에서 `\0`(0. 원본 자료)이 **NUL 문자**로 해석됐다.
- **교훈** 주석·docstring에 Windows 경로를 그대로 쓰지 말 것. 폴더 이름만 적는다.

### 16. 앱 서버가 두 개 떠서 옛 코드가 응답했다 — **진짜 원인을 2026-07-28에 찾았다**
- **증상** 코드를 고쳤는데 화면·API가 안 바뀐다. 몇 번이나 반복됐다.
- **처음 본 원인(불완전)** Git Bash `pkill` 이 윈도우 python 프로세스를 못 죽인다.
- **진짜 원인** `HTTPServer` 는 `allow_reuse_address = 1` 이 기본이다. 윈도우에서 이 옵션은
  **남이 이미 듣고 있는 포트에도 바인드를 성공시킨다.** 그래서 새 서버가 "시작됨"을 찍고
  정상처럼 보이는데 요청은 전부 옛 프로세스가 받아 갔다. 죽었어야 할 것이 조용히 살아 있었다.
- **조치** `_Server.allow_reuse_address = False`. 두 번째 서버는 이제 **에러로 죽고**,
  "옛 서버가 응답 중이니 먼저 정리하라"는 안내와 정리 명령을 찍는다.
- **정리 명령** (PowerShell — `pkill` 대신 이걸 쓴다)
  ```
  Get-CimInstance Win32_Process -Filter "Name='python.exe'" | ? { $_.CommandLine -like '*app_server.py*' } | % { Stop-Process -Id $_.ProcessId -Force }
  ```
- **교훈** '조용히 성공'은 실패보다 나쁘다. 같은 증상이 세 번 나오면 대증요법을 멈추고
  **왜 실패해야 할 것이 성공했는지**를 본다.

### 17. '미처리 84건'은 사실이 아니었다
- **원인** 완료일이 비어 있다고 다 미처리가 아니다 — 84건 중 59건이 30일 이전 접수로
  사실상 **완료일 미기입**이다.
- **조치** 30일 기준으로 갈라 "미처리 25건(최근 30일)"과 "완료일 미기입 59건"을 따로 적는다.
- **재발방지** 검증 `[38]`

### 18. 실 PIN이 소스에 리터럴로 들어갔다
- **원인** 자체검증 코드에 실 PIN 네 자리를 그대로 적었다. 공개 저장소라 그대로 새면
  올려 둔 사본의 잠금이 통째로 무의미해진다.
- **조치** 시험용 `"0000"` 으로 교체. **재발방지** 검증 `[29]` 가 `git grep --cached` 로 막는다.
- **덧붙임(2026-07-28)** 이 사고를 **기록하면서 또 냈다.** 이 문단에 PIN을 예시로 적었다가
  검증 `[29]` 에 걸렸다. 사고 기록·주석·커밋 메시지도 커밋되는 파일이다 —
  **비밀값은 어떤 문맥에서도 적지 않는다.** 검증이 잡아 줘서 커밋 전에 지웠다.

### 19. 8/5 돌발AS가 1건으로 보고됐다 — 적용기가 수식 키를 못 찾아 조용히 버렸다
- **증상** 대표 보고에 8/5 돌발AS 1건·정기점검 완료 1건. 실제로는 각각 2건·3건이었다.
- **원인** 두 겹이었다.
  ① 쿠팡AS 밴드 수집이 **8/4에 멈춰** 있었다. 화면은 멀쩡히 숫자를 보여 주는데
     그 숫자가 원본을 못 따라간 값이었다 — **조용한 사고**의 전형이다.
  ② 원장 적용기가 조회 키를 **수식 통(`data_only=False`)** 에서 읽었다. 점검ID·접수ID는
     `=IF($B5="","","PM-"&…)` 수식이라 키 사전에 `'=IF(...)'` 가 들어갔고, 실제로 있는
     행을 "행 없음"으로 버렸다. 실패는 로그 한 줄로만 남아 아무도 못 봤다.
- **조치** ① 값 통(`data_only=True`)을 따로 열어 **키만 거기서** 읽는다.
  ② `--check`·`--adopt` 가 **밴드마다·원본마다** 최신일을 세어 '어제'가 비면 맨 앞에 올린다.
- **재발방지** 검증 `[110]`(수식 키) · `[111]`(수집 밀림)
- **교훈** 숫자가 이상하면 계산식보다 **원본이 어디까지 들어왔는지**를 먼저 본다.
  그리고 '한 건 건너뜀'을 로그로만 남기지 말 것 — 세어서 올리지 않으면 없는 일이 된다.

### 20. 브라우저 도구가 반나절 내내 '로그인 없는 크롬'에 붙어 있었다
- **증상** 사람은 밴드·ERP에 로그인해 뒀는데 AI가 여는 탭마다 로그인 화면이었다.
  "다른 계정에선 에이전트가 알아서 다 해줬는데"라는 말이 계속 나왔다.
- **원인** Claude 확장은 **확장이 연결된 크롬**만 조종한다. 사람이 쓰는 주 크롬에 연결이
  없어 도구가 임시 프로필 크롬(창도 안 보인다)에 붙어 있었다. 프로필·탭그룹 문제로
  오진해 시간을 더 썼다. 다른 계정과의 유일한 차이는 **주 크롬의 확장이 그 계정으로
  로그인돼 있었다**는 것뿐이었다.
- **조치** 브라우저 도구를 쓰기 전에 `list_connected_browsers` 를 **먼저** 부른다.
  하나뿐이고 로그인이 없으면 사람에게 확장 로그인·Connect 한 번을 요청한다.
- **하지 않은 것** 로그인 쿠키를 복제해 크롬을 띄우는 것, 키보드 입력을 주입하는 것 —
  둘 다 자동승인 정책이 막았고, 만들던 스크립트는 지웠다. 정책이 막으면 우회하지 않는다.
- **대신 한 것** 사람 손을 **밴드당 붙여넣기 한 번**으로 줄였다(예전엔 250건마다 한 번).
  회차 사이에 저장하고 탭 메모리를 비우면 한 파일이 끝까지 돈다 — 검증 `[112]`.
- **교훈** "왜 저 계정에선 되는데"는 능력 차이가 아니라 **연결 대상 차이**일 때가 많다.
  도구가 무엇에 붙어 있는지를 먼저 확인한다.

### 21. 캠프명 매칭 실패의 절반은 붙여넣기 흔적이었다
- **증상** 캠프 ↔ 거래처코드 미매칭 18건. 사람 입력 실수로 보였다.
- **원인** 뜯어 보니 `남김해Sub-Hub&amp;Sub-FC`(웹 복사로 HTML 엔티티가 그대로),
  `중구1캠프 <-서초1MB(양재동C)`(메모가 이름 칸에 섞임), `김포1Sub-FC ?(…))`,
  값 대신 들어간 `0`·`...`. 김미영 대리가 "복사 붙여넣기 오류로 매칭이 안 된다"고
  말한 그것이었다.
- **조치** 이름을 고치는 게 아니라 **비교할 때** 걷어 낸다(`clean()`·`bare()`).
  18 → 14건, 코드 확정 200 → 202건. 값이 아닌 표시 6건은 '못 잇는 자료'가 아니라
  **원장을 고칠 일**로 갈랐다 — 섞여 있으면 진짜 미등록이 몇 개인지 알 수 없다.
  같은 규칙을 `stmt_subset.norm_cust` 도 쓰게 했다(규칙이 두 벌이면 반드시 갈린다).
- **재발방지** 검증 `[113]`
- **교훈** 매칭이 안 되면 사람을 탓하기 전에 **값에 뭐가 묻어 들어왔는지** 본다.

### 22. 밴드 503건이 "없는 글"이 된 이유 — 창이 뒤에 있었다
- **증상** 밤새 남은 503건을 다시 걸었는데 20분 동안 성공 9 · 실패 61. 같은 시각
  ERP 화면은 `검색(F8)` 을 눌러도 네트워크 요청이 **0건** 나가고, 화면 캡처는
  5초 시간초과로 계속 실패했다. 서로 다른 두 사고로 보였다.
- **원인** 하나였다. 두 탭 모두 `document.hidden === true` — **크롬 창이 뒤에 있었다.**
  · 밴드는 SPA 라 본문을 `requestAnimationFrame` 으로 그린다. rAF 는 숨은 탭에서
    **한 번도 안 불린다.** 타이머와 달리 Worker 로도 못 우회한다(grab_posts.js 가
    Worker 타이머를 쓰는 건 *스케줄*을 살리려는 것이지 *렌더*를 살리는 게 아니다).
    그래서 iframe 은 영원히 "로딩 중입니다" 이고, 본문 없음 판정이 그것을
    **지운 글로 오해**했다.
  · ERP 격자도 같은 이유로 높이가 0 이었다.
- **어떻게 알았나** 캐시에 **확실히 들어 있는** 500번 글을 같은 방법으로 열어 봤다.
  그것도 "로딩 중입니다" 였다. 지운 글 문제가 아니라는 게 그 한 번으로 갈렸다.
- **조치** `__grabStart` 는 숨은 탭에서 **시작을 거절**한다. 돌던 중에 창이 뒤로
  넘어가면 실패로 적지 않고 **멈춰 기다린다**(`__grabStatus().paused`).
- **하지 않은 것** 창을 강제로 앞으로 끌어내는 것. 사람이 쓰던 창이다.
- **교훈** "밴드가 막았나"를 의심하기 전에 **살아 있는 걸 하나 열어 본다.**
  그리고 조용히 실패하느니 **시작을 거절**하는 편이 낫다 — 가짜 묘비가 캐시에
  쌓이면 `recheck_plan` 이 그 번호를 영영 다시 안 뽑는다.

### 23. ERP 검색이 안 눌린 진짜 이유 — 눈에 안 보이는 모달
- **증상** 계정별원장에서 `검색(F8)` 을 눌러도 아무 일이 없었다. `fetch`·`XHR` 을
  가로채 놓고 눌러도 **네트워크 요청 0건**. 합성 이벤트(mousedown→mouseup→click)도
  통하지 않고, 진짜 클릭(computer)도 통하지 않았다. 반나절을 "프레임워크가
  사람이 누른 이벤트만 받는다"고 오진했다.
- **원인** 두 겹이었다.
  ① 크롬 창이 뒤에 있어 `document.hidden === true` — 격자 높이가 0 이었다(사고 #22와 같은 뿌리).
  ② 창을 앞으로 꺼낸 뒤에도 안 됐다. `document.elementFromPoint(버튼중심)` 이
     **버튼이 아닌 DIV** 를 돌려줬다 — `.ui-widget-overlay.ui-front`,
     `position:fixed`, `z-index:2001`, **`opacity:0`**, 1440×675 전면.
     그 위(z 2002)에 jQuery UI 대화창이 떠 있었다:
     *"계정 검색조건을 지정하지 않고 검색 시 검색시간이 오래 걸릴 수 있습니다.
     조회 대상계정을 재지정하겠습니까? [확인] [취소]"*
     **검색은 진작에 걸렸고 ERP 는 답을 기다리고 있었을 뿐이다.**
     오버레이가 투명해서 화면만 봐서는 아무 일도 없는 것처럼 보였다.
- **조치** 취소(=그대로 검색)를 누르니 그 자리에서 5,170행·154페이지가 떴다.
  기간 2025/01~2026/08 로 다시 걸어 **8,980행**을 Excel 로 받았다.
- **다음에 같은 증상이면 30초 안에 가릴 수 있다**
  · `document.hidden` 을 먼저 본다.
  · `document.elementFromPoint(버튼 중심 좌표)` 가 그 버튼을 돌려주는지 본다.
    아니면 **무언가 덮여 있는 것**이고, `.ui-widget-overlay`·`[role=dialog]` 를 찾는다.
  · 화면 캡처가 5초 시간초과로 실패해도 javascript_tool 은 멀쩡히 돈다 —
    캡처가 안 된다고 손을 놓지 말 것. `getBoundingClientRect()` 로 좌표를 얻어
    진짜 클릭을 하면 된다. **다만 창 크기가 바뀌면 좌표도 바뀐다**(1920→1440에서
    한 번 헛클릭했다). 클릭 직전에 다시 재는 습관을 들일 것.
- **교훈** "클릭이 안 먹는다"는 대개 이벤트 문제가 아니라 **가려짐** 문제다.
  프레임워크를 의심하기 전에 그 좌표에 실제로 뭐가 있는지 물어본다.

### 24. 밴드 캐시가 "깨졌다"고 두 번 오해한 이유 — 원자적으로 안 쓰고 있었다
- **증상** 합성검증이 두 번 연속 죽었다. 한 번은 관리대장을 못 찾는다더니
  다음엔 `JSONDecodeError: Expecting ',' delimiter: line 1 column 2581023`.
  다시 돌리니 이번엔 `column 9738085`. **죽은 자리가 매번 달랐다.**
- **원인** `band/convert_dump.py` 가 캐시 정본에 **바로 쓰고** 있었다:
  `json.dump(out, open(dst,"w",...))`. `"w"` 는 파일을 **먼저 비운다.**
  18MB 를 흘려 넣는 몇 초 동안 읽는 쪽은 반쪽짜리 JSON 을 본다. 그래서
  읽는 순간마다 끊긴 자리가 달랐던 것이다. 파일은 멀쩡했다 — 쓰는 중이었을 뿐이다.
- **왜 위험한가** 읽는 쪽이 잠깐 실패하는 건 다시 돌리면 그만이다. 그런데
  **쓰다가 프로세스가 죽으면 정말로 깨진다.** 그때는 8,500 글을 다시 긁어야 한다 —
  밤샘 한 번 분량이다. 같은 파일 아래쪽에서 raw 덤프는 `os.replace` 로 옮기고
  있었다. 패턴을 알고 있었는데 정작 정본에는 안 쓴 것이다.
- **조치** 임시파일에 다 쓴 뒤 `os.replace(tmp, dst)`. 원자적이라 읽는 쪽은
  옛 파일이나 새 파일만 보고 그 중간은 못 본다. 검증 `[112]` 가 지킨다
  (`os.replace(tmp,dst)` 가 있는지 · `open(dst,"w")` 가 되살아나지 않았는지).
- **교훈** "파일이 깨졌다"는 진단을 내리기 전에 **누가 지금 쓰고 있는지** 본다.
  실패 위치가 실행할 때마다 다르면 손상이 아니라 **경합**이다.
  그리고 여러 프로세스가 보는 파일은 예외 없이 임시파일 + os.replace 로 쓴다.

### 25. '★밀림' 경보가 사고를 불렀다 — 밴드는 없는 글 번호에도 200 을 준다
- **증상** 인계 문서가 `밴드 84789192 ★밀림(2일)` 을 맨 위에 올렸다. 지시대로
  `recheck_plan` 이 뽑아 준 3539~3578 을 긁었더니 `ok 40 · fail 0` 으로 깨끗이 끝났다.
  그런데 캐시에 들어간 40건이 **전부 같은 글**이었다(PO375207 · 글쓴이 없음 · 시각 없음).
  캐시는 3308→3348 로 늘었고, 늘었으니 `recheck_plan` 은 그 번호를 **다시 뽑지 않는다.**
- **원인 둘**
  ① 밴드는 **아직 없는 글 번호**에도 200 과 앱 껍데기를 준다. 그 자리에는 직전 화면
     (피드 맨 위 글) 본문이 그대로 남아 있어서, 수집기가 그것을 뜯어 `ok` 로 처리했다.
     본문이 있으니 실패로 보이지 않았다 — **제일 나쁜 종류의 실패다.**
  ② 더 근본은 경보 쪽이다. 신선도 판정이 **'날짜 있는 최신 글'만** 봤다. 밴드에 새 글이
     없으면 최신 글은 당연히 며칠 전이고, 그게 그대로 '밀림' 이 됐다. 즉 **긁을 것이
     없는 밴드에 대고 긁으라고 시킨 것이다.** 3538 번이 실제 마지막 글이었다.
- **조치**
  · 수집기: 시각이 끝내 안 붙으면 **수확을 버린다**(`ok` 아님). `missing` 이 아니라
    `fail` 로 돌린다 — 3539 는 내일이면 진짜로 생기므로 묘비를 세우면 영영 못 모은다.
    대신 그때 잡힌 본문 **지문**을 남긴다. 검증 `[130]`.
  · 판정: '밀림' 과 **'조용함'** 을 가른다. 수집 최대 번호 바로 위가 없음으로 확인되면
    `(조용함)` 으로 적고 경보를 올리지 않는다. 근거는 `reports/밴드_확인시각.json`,
    만드는 곳은 `convert_dump._record_probe`. 근거가 한도보다 **오래되면 다시 밀림**이다.
    검증 `[131]`.
  · 증거를 좁게 잡았다: `missing`(밴드가 '없다'고 명시) 또는 **지문이 같은 notime 2건 이상**.
    지문 하나만으로는 '화면이 늦게 그려진 것'과 구분되지 않아 증거로 치지 않는다.
  · 오염된 40건은 캐시에서 제거(3308/1~3538 로 복원), 덤프는 `reports/오염덤프_격리/` 로
    옮겼다 — `convert_dump` 는 매 회차 모든 덤프를 다시 병합하므로 그냥 두면 되살아난다.
- **교훈** 경보를 고칠 때 **"이 경보가 시키는 대로 하면 무슨 일이 벌어지나"** 를 먼저 묻는다.
  "최신 글이 며칠 전"과 "못 모은 글이 있다"는 **다른 말이다.** 그 둘을 같은 것으로 놓으면
  조용한 원본에 대고 없는 번호를 긁게 되고, 그 결과가 도로 원본 행세를 한다.
  그리고 수집기는 `ok` 를 너그럽게 주면 안 된다 — 빈손으로 돌아오는 편이 낫다.

### 25-후속. 오염 621건의 최종 판정 — 삭제된 글이었다 (같은 날 오후)
- **판정** 표본 3건(84789192 #2694 · 90610953 #2902 · #5464)을 열어 보니 셋 다
  `/post/<번호>` 가 **피드로 리다이렉트**됐다. 밴드가 그 번호를 못 보여 준다는 뜻이고,
  #2694 는 메인 탭에서 "삭제되었거나 찾을 수 없습니다" 안내까지 확인했다.
  → **수집 경로 재설계는 불필요하다.** 지금 경로는 실재하는 글에는 잘 동작한다
  (날짜 있는 7,741건이 그 증거다). 621건은 되살릴 내용 자체가 없다.
- **추가로 막은 구멍 둘**
  ① 리다이렉트된 피드가 끝까지 그려지면 본문·시각이 다 있어서([130]의 시각 가드를 뚫고)
     **날짜 달린 가짜**가 들어올 수 있었다(실측: 피드의 "6시간 전"이 잡혔다).
     → grabOne 이 **내가 연 주소에 내가 있는지**(pathname) 확인한다. 검증 [135]⑥.
  ② 오염 표시가 **한 회차 만에 0건으로 되돌아갔다** — Z: 의 옛 덤프(가짜 본문 포함)를
     convert_dump 가 매 실행 전부 재병합하기 때문. → 작성일 없는 재병합은 오염 표시를
     못 덮는다(작성일을 가진 기록 = 진짜 재수집만 뚫는다). 검증 [135]⑦.
- **교훈** 원본 폴더의 덤프는 지워지지 않고 **매 회차 다시 살아난다.** 캐시를 고칠 때는
  "이 상태가 다음 재병합을 살아남는가"까지가 조치다. 그리고 리다이렉트 계열 실패는
  내용 검사(본문·시각)로는 못 막는다 — **주소 대조**가 확증이다.

### 26. 죽은 스케줄러 점유를 --adopt 가 못 넘겨받는 교착 (2026-08-07)
- **증상** `session_handoff --check` 는 "ledger 점유가 죽은 세션의 잔재(주인 52732 없음)"
  라며 처방으로 `--adopt` 를 권하는데, 정작 `--adopt` 는 같은 점유를 "살아 있어 그대로
  둔다"고 답한다. 처방이 처방을 물고 도는 교착 — 사람이 봐도 풀 방법이 없어 보인다.
- **원인** 점유 기록에 pid 가 둘 있다: `agent_pid`(Claude 세션 pid)와 `pid`(프로세스
  자신). 스케줄러(ledger_writer)는 에이전트가 아니라 `agent_pid=0` 으로 잡는다.
  `session_handoff` 의 판정은 `agent_pid or pid` 폴백을 쓰는데 `ai_claim._is_dead` 는
  `agent_pid` 만 보고 0 이면 "증거 없음 → 살아 있음"으로 물러났다. 15:00 반영을 마친
  ledger_writer 가 점유를 못 풀고 죽자 이 차이가 그대로 드러났다.
- **조치** `_is_dead` 도 같은 폴백(`agent_pid or pid`)을 쓴다. pid 마저 없으면 여전히
  보수적으로 "살아 있다"로 둔다(증거 없이 남의 점유를 뺏지 않는다는 원칙 유지).
- **교훈** 같은 질문("이 세션이 죽었나")에 대한 판정 코드가 두 곳이면 반드시 갈린다.
  판정을 못 합치면 최소한 **읽는 필드와 폴백 순서**는 한 줄로 통일할 것. 검증 [104].

### 27. 관리대장 버전 폭증 — 인수인계 예약을 건마다 따로 반영했다 (2026-08-07)
- **증상** 25분 새 v542→v554(12개), 그 전에도 v500→v524. 11:00·15:00 "하루 두 번"
  규칙을 만들었는데도 한 회차가 지나가면 버전이 열 개씩 늘어 있었다.
- **원인** 회차 규칙은 **호출 시각**만 묶었지 **호출 횟수**는 안 묶었다.
  `ledger_db` 의 회차 마무리가 19시트 인수인계 예약을 `for item in pending_handoffs()`
  로 돌며 건마다 `workbook_patch.py --b --c` 를 따로 불렀고, workbook_patch 는
  부를 때마다 vN+1 을 만든다. 예약 12건 = 새 버전 12개. 실측: 15:00 회차가
  15:18~15:26 에 v543~v554 를 만드는 것을 그대로 지켜봤다.
- **조치** workbook_patch 에 여러 행을 **한 버전에** 넣는 길을 만들었다
  (`patch(..., extra_rows=…)` · CLI `--batch rows.json`). 회차 마무리는 예약 전체를
  JSON 하나(`reports/handoff_batch.json`)로 묶어 **한 번만** 부르고, 성공 시
  일괄 applied 처리한다. 이제 예약이 몇 건이든 회차당 버전은 하나다.
- **교훈** "하루 두 번만 연다"는 규칙은 **여는 횟수**까지 세어야 완성이다.
  회차 안에서 같은 도구를 반복 호출하면 규칙은 지켜졌는데 결과는 폭증한다.
  vN+1 을 만드는 도구는 루프 안에 두지 말 것. 검증 [5](다중 행)·구조 단언(--batch).

### 27. 오염 표시가 하루 만에 다시 뚫렸다 — 한 번 치우는 조치의 한계 (2026-08-07 오후)
- **증상** 아침에 오염(삭제된 글) 621건을 표시하고 재병합 생존 가드까지 넣었는데,
  15:32 회차 뒤 밴드업무추출에서 정기점검 **UJ2601407 이 1건 → 23건**으로 부풀었다
  (`밴드업무추출_전체_20260807_1502.csv` 7,852행 → `_1635.csv` 7,921행, 삭제 0).
  화면·리포트는 정상으로 보였다. 숫자만 틀렸다.
- **원인** 아침 가드(`convert_dump` 226행)는 **이미 표시된 것**을 지킬 뿐이다.
  새 덤프가 **표시된 적 없는** 유령 22건(90610953 #5438~5464)을 들여오자 그대로 통과했다.
  표시는 사람이 `clean_contaminated --apply` 를 기억해서 돌려야만 붙는 1회성 조치였다.
- **조치** 판정을 병합 경로 **안으로** 옮겼다 — `convert_dump` 가 캐시를 쓰기 전에
  `clean_contaminated.find(merged)` 로 새 오염을 매번 표시한다. 옆 폴더 import 가 조용히
  실패해 보호가 통째로 꺼지지 않도록 제 폴더를 `sys.path` 에 넣는다. 실측: 90610953
  표시 501 → 523건, 재병합 뒤 재검출 0. 검증 `[137]`.
- **교훈** **"한 번 치웠다"는 조치가 아니다.** 원본이 계속 흘러 들어오는 곳에서는
  판정이 그 흐름 **안에** 있어야 한다. 밖에 두면 사람이 매번 알아채야 하고, 결국 못 알아챈다.
  같은 사고를 하루에 두 번 당한 이유가 이것이다(#25 · #25-후속 · #27).

### 27-후속. 날짜가 붙어 들어온 사본 16건, 그리고 진짜 글 98건을 지울 뻔한 순간 (같은 날 저녁)
- **증상** 리다이렉트된 피드가 **끝까지** 그려지면 본문도 시각도 다 있다. 그래서 오염
  판정①(작성일 없음)을 그대로 빠져나갔다. 실측 90610953 #5420~5424 다섯 건이
  같은 작성일(1785800580000)·같은 2,509자 본문으로 캐시에 앉아 있었다.
- **하마터면** 처음엔 "본문이 피드 머리글(`2026년 8월 4일 … 게시글 … 3시간 전`)로
  시작하면 가짜"로 잡으려 했다. **적용 전에 세어 보니 117건이 걸렸고 그중 98건은
  본문이 전부 달랐다.** 상세 화면 수집분에도 같은 머리글이 남아 있어서다 — 진짜 글
  98건을 지울 뻔했다. 머리글은 '어디서 긁혔나'를 말할 뿐 '가짜'를 말하지 않는다.
- **조치** 가짜의 정의는 그대로 둔다: **서로 다른 번호가 같은 글을 들고 있다.**
  ②로 '작성일·본문이 둘 다 같은 묶음'만 잡고, 그 묶음에서도 **맨 앞 번호는 남긴다**
  (①과 달리 원본이 섞여 있을 수 있고, 다 지우면 되돌릴 수 없다). 결과 16건 표시,
  #5420 은 본문을 유지. 진짜 반복 글은 올린 시각이 달라 걸리지 않는다. 검증 `[137]`④~⑥.
- **교훈** 판정을 넓힐 때는 **적용 전에 몇 건이 걸리는지 세어 본다.** 이번엔 '걸린 건수'가
  아니라 **'본문 종류'** 가 오탐을 드러냈다 — 가짜라면 종류가 적어야 하는데 98건이
  98종이었다. 숫자 하나가 데이터 손실을 막았다.

### 28. 관리대장 새 버전을 검증 전에 정본 이름으로 쓰고 있었다 (2026-08-07, 발생 전 차단)
- **위험** `findings_sheet.upsert()` 와 `ledger_writer` 가 vN+1 을 **정본 이름으로 먼저 만들고**
  그 뒤에 무결성을 검사했다. 원장은 Z: SMB 위의 수십 MB 짜리다. 쓰는 도중 프로세스가
  죽으면 **이름은 멀쩡한 `_v{N+1}.xlsx`, 내용은 잘린 zip** 이 남는다. `resolve_master()` 는
  v번호 최대를 정본으로 집으므로 앱·모든 대조 도구·다음 회차가 그 깨진 파일을 읽는다.
  `~$` 는 걸러도 이런 반쪽 파일은 못 거른다.
- **가정이 아니다** 같은 날 스케줄러가 원본정리 프로세스를 3시간 한도로 **강제 종료**했고
  (0x41306), Z: 는 이전에 `OSError WinError 1231`(네트워크 접근 불가)을 낸 적이 있다.
- **조치** 두 파일 모두 `.tmp.xlsx` → 검증(zip 무결성·재독) → `os.replace` 순서로 바꿨다.
  검증 실패 시 tmp 를 지운다(남기면 '임시 결과가 이미 존재'로 다음 회차가 막힌다).
  바로 옆 `workbook_patch.py` 가 처음부터 이 방식이었다 — 두 파일만 규약 밖에 있었다.
  검증 `[139]`.
- **교훈** **검증은 발행 전에 해야 검증이다.** 정본 이름을 먼저 주고 나중에 검사하면,
  그 사이에 남이 집어 가고 실패해도 되돌릴 수 없다. 같은 저장소 안에 이미 옳은 패턴이
  있는데 두 곳만 빠져 있었다 — 새 코드를 쓸 때 **옆 파일이 어떻게 하는지** 먼저 볼 것.

## #29 — 작업 스케줄러의 시간 제한이 **손자 프로세스를 못 죽인다** (2026-08-07)

**증상.** 앱 히어로 배지가 `에이전트 08-06 21:01 실행` 에 멈춰 있었다. 초록 점이
달려 있어 정상으로 보였다. 사용자 지적: "에이전트 실행 시간이 맞지 않아 지금
날짜로 반영이 안되었어."

**원인.** 09:35 `원본자료자동정리`(source_organizer.py --apply)가 **10시간 6분**
돌았다. 작업 스케줄러는 제 설정(PT3H)대로 12:35 에 종료를 시도했고 결과도
`0x41306`(SCHED_E_TASK_TERMINATED)으로 남았다 — **그런데 python 프로세스는
20:15 까지 살아 있었다.** 스케줄러가 죽이는 것은 제가 띄운 껍데기지 그 아래
손자 프로세스가 아니다. 그 10시간 동안 Z:(SMB)를 계속 두드려 09:50 일일대조가
매일 조용히 건너뛰었고, 스케줄러에는 `결과: 0`(성공)으로 적혔다.

측정: 커널 시간이 사용자 시간의 **42배**(1,076초 중 대부분) — 계산이 아니라
네트워크 I/O 에서 갈렸다. 로그는 시작 시각인 10:13 이후 **한 줄도 늘지 않았다**.
`--budget-min 1` 실측으로 Z: 스캔 속도가 **분당 4,000개**임이 확인됐다.

**조치.**
- `source_organizer.py` 에 **스스로 끊는 상한**(기본 120분, `--budget-min`).
  넘기면 exit 4 로 소리 내어 중단한다. 반쪽 정리는 '멀쩡해 보이는 거짓말'이다.
  훑기는 200개마다, 이동은 **이력을 확정한 직후에만** 시계를 본다 — 그 지점에서
  멈추면 옮긴 것이 전부 이력에 남아 되돌릴 수 있다(파일 하나가 뜨지 않는다).
- 앱이 밀림을 말한다: `agent_stale`·`agent_aborted`·`agent_age_h`. 밀리면 배지가
  빨갛게 '중단됨'·'N시간째 미완주'라 적고, 눌러서 바로 다시 실행한다.
- 작업 스케줄러: `업무센터UX점검` 은 배터리 조건 때문에 **한 번도 실행된 적이
  없었다**(`0x41303`) — 해제. `일일자동대조` PT72H→PT3H + 배터리 조건 해제
  (사흘을 걸치면 다음 회차가 조용히 밀린다).

**교훈.** 밖에서 거는 제한은 믿을 수 없다. 오래 도는 도구는 **제 시계를 제가**
들고 있어야 한다. 그리고 스케줄러의 `결과: 0` 은 "일을 했다"는 뜻이 아니다.

**검증** `[140]` · 사용자 지적으로 발견.

## #30 — '194분 전 자료 · 갱신 중' — 화면이 제 나이를 모른 채 말했다 (2026-08-07)

**증상.** 상단 칩이 계속 떠 있고, 볼 때마다 숫자가 달랐다("계속 194분전 갱신중
… 분이 이상해 / 계속 내용이 달라 / 팝업이 너무 자주 뜨고").

**원인 셋이 한 곳에 있었다.**
1. SWR 대상이 11곳인데 칩은 **하나**라 마지막에 쓴 경로의 나이가 찍혔다(3분↔194분).
2. 한 경로가 끝나면 `swrChip(0)` 이 칩을 지웠다 — 나머지 열 곳이 아직 옛 값인데도
   최신처럼 보였고, 다음 경로가 다시 띄워 깜빡였다.
3. **갱신 실패를 `.catch(()=>{})` 로 삼켰다.** 서버가 느려 갱신이 실패하면 옛 값은
   그대로 늙는데 칩은 영원히 '갱신 중'이라 했다 — 그 194분의 정체다.

곁가지: 대표 보고 `meta['데이터최종갱신일']` 이 `datetime.now()` 였다. 캐시가 최대
10분 낡을 수 있는데 '방금'이라 말했다. 캡처 payload 의 `updatedAt` 도 근거가 없으면
`new Date()` 로 떨어져 **묵은 숫자에 지금 시각을 찍었다**.

**조치.** 나이는 '아직 안 들어온 것 중 가장 오래된 것' 하나로 정한다 · 대기 목록이
빌 때만 지운다 · 1.2초 안에 끝나는 갱신은 아예 안 띄운다 · 실패는 빨간 칩으로
말한다 · 60분이 넘으면 '3시간 14분 전'으로 읽는다 · 같은 주소는 한 번만 나간다.
시각은 **원장이 저장된 시각**을 쓰고 모르면 빈 값이며, 화면과 캡처가 같은 `_asOf`
하나를 본다.

**교훈.** 느린 것보다 나쁜 것은 **얼마나 낡았는지 모르면서 말하는 것**이다.

**검증** `[140]`.

## #31 — 크롬 창이 '보임'인데 hidden 이었다 — 덮여 있음 (2026-08-07, 반나절)

**증상.** 밴드 수집기가 `__grabStart` 에서 "탭이 뒤에 있다"로 시작을 거절했다.
그런데 화면상 크롬 창은 **최소화도 아니고 그 탭이 활성 탭**이었다. Win32
`IsWindowVisible` 도 참, `IsIconic` 은 거짓 — 운영체제가 보기엔 멀쩡한 창이었다.

**헛짚은 것 둘.** ① 여분 탭이 활성 상태를 뺏는 줄 알고 탭 하나를 닫았다 — 무관했다.
② `window_show.py --apply`(`SetWindowPos(HWND_TOP)`) 를 걸었다 — 이것도 안 풀렸다.

**원인.** **native window occlusion** — 다른 창이 크롬을 **완전히 덮고 있으면**
크롬은 그 창을 가려진 것으로 보고 렌더링을 멈추며 `document.hidden` 을 참으로 만든다.
Win32 의 '보임'과 크롬의 '보임'이 다른 말이었다. `HWND_TOP` 은 '그 순간 맨 위'일 뿐
'항상 위'가 아니라서, 올려 놓아도 덮고 있던 창이 곧 다시 덮었다.

**조치.** `HWND_TOPMOST`(-1) 로 잠깐 고정하면 아무도 못 덮는다. 끝나면
`HWND_NOTOPMOST`(-2) 로 되돌린다. `band/window_show.py --topmost` / `--untopmost`,
파이썬에서는 `with pinned():`.

**진짜 위험은 푸는 쪽이 아니라 되돌리는 쪽이다.** 수집기는 중간에 자주 죽는데
(로그인 화면·없는 번호·네트워크), 그때 항상위가 남으면 사용자는 원인을 모른 채
"창이 안 내려간다"를 겪는다 — 수집 실패와 아무 상관없어 보여서 원인 찾기가 또 오래
걸린다. 그래서 복귀를 `finally` 에 두고, 검증이 **예외 경로**를 본다.

**교훈.** 같은 증상(`document.hidden`)에 원인이 셋이다 — 최소화(#22) · 창이 뒤(#22) ·
**덮여 있음**(여기). 앞의 둘이 아닌데 hidden 이면 세 번째다. 그리고 시스템이
'보인다'고 말하는 것과 응용프로그램이 '보인다'고 판단하는 것은 다른 말이다.

**검증** `[144]`.


---

## #34 — 주입은 "성공"이라 말하는데 콘솔에 들어간 적이 없었다 (2026-08-11 밤)

**증상.** 밴드 댓글 수집을 여섯 번 주입했고 스크립트는 매번 `INJECTED` 를 찍었다.
그런데 덤프는 한 번도 안 떨어졌고 캐시의 `댓글 본문 있는 글` 은 계속 0 이었다.
오류도 없고 실패로도 안 세어졌다 — **실패가 성공처럼 보이는 자리**의 전형이다.

**원인 셋 (전부 '가정을 증명 안 한 것').**
- `Ctrl+Shift+J` 는 **여는 키가 아니라 토글**이다. 개발자도구가 이미 열려 있으면
  **닫는다.** 그 상태로 26KB 수집기를 붙여넣으면 **페이지 본문에** 들어간다.
  아무 일도 안 일어나고 아무 말도 안 나온다.
- **포커스는 아무것도 증명하지 않는다.** 크롬을 앞으로 끌어와도 활성 탭이
  앱 대시보드("Mulder Control Hub")면 수집기는 **엉뚱한 오리진**에서 돈다.
  실제로 '검증된' 주입 하나가 거기 떨어졌다.
- **숨은 탭은 '가려진 창'만이 아니다.** 창이 훤히 보여도 **같은 창의 다른 탭이
  활성**이면 그 탭은 hidden 이고, 밴드는 rAF 로 본문을 그리므로 수집기가 스스로
  멈춘다(#22 `[144]` 의 다음 겹). 주입은 성공했는데 25분간 조용했던 이유다.

**조치 — 가정을 증명으로 바꾼다 (`band/collect_step.ps1`).**
1. **언제나 새 탭**을 연다. 새 탭은 정의상 개발자도구가 닫혀 있고(토글이 결정론적이
   된다) 정의상 활성 탭이다. 두 가정이 한 번에 사라진다.
2. **핑으로 콘솔 생존을 증명한다** — `band/console_ping.js` 가 마커 파일을 내려받는다.
   작은 핑은 페이지에 잘못 떨어져도 안전하지만 26KB 붙여넣기는 아니다.
3. **핑 파일 이름에 `location.hostname` 을 박는다.** 그래서 "콘솔이 살아 있나"가 아니라
   **"어느 페이지의 콘솔인가"** 를 읽는다. `band.us` 가 아니면 **수집기를 안 붙인다**(exit 3).
4. 탭이 뒤로 밀리면 `band/focus_collect_tab.ps1`(`Ctrl+9` = 마지막 탭)로 되살리고,
   `band/keep_chrome_visible.ps1` 이 `SWP_NOACTIVATE` 로 **작게+항상위** 고정한다 —
   가려지지 않으면서 **사람의 키보드 포커스는 안 뺏는다**.

**곁다리로 걸린 것 — PowerShell 5.1 은 BOM 없는 UTF-8 을 ANSI(CP949)로 읽는다.**
무인 수집기가 **로그 한 줄도 못 남기고** 죽었는데, 원인은 스크립트 안의 한글 경로가
깨진 것이었다. 파일은 `New-Object System.Text.UTF8Encoding $true`(BOM 포함)로 저장한다.
**한글이 든 .ps1 은 BOM 없이 저장하지 말 것.**

**교훈.** `[169]` 는 "0건이면 없는 건가 안 본 건가"를 물으라 했다. 여기는 한 겹 더다 —
**도구가 '했다'고 말하는 것 자체가 증거가 아니다.** 키보드 자동화는 눈이 없다.
증거를 만들지 않는 자동화는 성공을 자칭한다.

**검증** — 이 사고의 방지물은 스크립트 자체다(`collect_step.ps1` 의 exit 2/3 경로).


---

## #35 — 수집기가 스스로를 죽였다 · 몰이가 실패를 '완료'라 적었다 (2026-08-11 밤, 실측)

오늘 밤 주입은 **핑으로 증명된 진짜 주입**이었다(#34 로 고친 뒤였다). 그런데도 수확은 0 이었다.
이번에는 원인을 짐작하지 않고 **살아 있는 페이지에 물어봤다** — 그게 이 항목의 요점이다.

### (가) 밴드 댓글 백필 — 수확 0건, 스크립트가 죽어 있었다
- 20:49:05 `collect_step.ps1` 이 `www.band.us` 에 239건 계획을 주입(핑으로 증명).
- 21:21 `band/band_dump_state.js` 로 상태를 물으니 **`NO __GRAB`** — 전역이 없다.
  즉 **주입한 스크립트가 통째로 사라졌다.** 창 제목도 `게시글 : …` 로 바뀌어 있었다.
- **최상위 문서가 게시글로 이동하면 주입 스크립트는 그 자리에서 죽는다.** 수집기는
  숨은 iframe 으로 글을 여는 설계인데, 어떤 경로로든 top 이 navigate 되면 끝이다.
  죽은 뒤에도 **캐시는 멀쩡하고 오류도 없다** — 그래서 여러 세션이 "댓글 0건"을
  '아직 안 긁어서'로 읽었다. 실제로는 **긁다가 죽은 것**이었다.
- 실측 결과: `90610953` 글 5,255 · `comments` 키 4,733 · **댓글 본문 있는 글 0**.
  덤프가 하나 흡수됐지만 그것은 옛 덤프 재병합(5255→5255)이라 **새 수확이 아니다.**
- ~~**다음 세션이 할 일**~~ → **처리됨 (2026-08-12 01시, `band/liveness.py`)**. 아래 '조치'.

#### 조치 — 짐작하지 않고 **재현해서** 고쳤다 (2026-08-12)

**(1) 범인은 우리 코드가 아니었다.** 밴드 payload 전수를 훑어 `location` 대입이
**한 줄도 없음**을 확인했다(`grab_posts.js`·붙여넣기 파일·`band_auto_collect.user.js` —
유저스크립트는 `location.pathname` 을 **읽기만** 한다). 주소창을 치는 `.ps1` 은 언제나
`^t` 로 **새 탭**을 열고 거기에 친다. 그런데 top 이 도달한 곳은 **iframe 이 열던 그 글
주소**였다(제목 `게시글 : …`). 남은 설명은 하나다 — **액자 안의 밴드 페이지가 스스로
액자를 깨고 나왔다**(frame-busting). 그리고 우리 iframe 에는 `sandbox` 가 없었다.

**(2) 로컬에서 재현했다** (band.us 로그인 없이 · 사람 인증 필요 없음). 프레임 깨기를
하는 자식 페이지를 만들어 같은 iframe 을 **두 번** 실었다:
  · `sandbox="allow-same-origin allow-scripts"` → 최상위 문서 **살아남음** ·
    자식의 깨기 시도가 **SecurityError 로 막힘** · **그런데도 `contentDocument` 는 읽혔다**
    (본문 텍스트를 그대로 뜯었다 — 이것이 제일 중요한 확인이다. 못 읽으면 수집기가 죽는다).
    크롬이 직접 말했다: *"The frame attempting navigation of the top-level window is
    sandboxed, but the flag of 'allow-top-navigation' … is not set."*
  · `sandbox` 없음 → **최상위 문서가 실제로 넘어갔다.** #35 그대로다.
  즉 원인·해법이 둘 다 **측정으로** 확정됐다. (`allow-scripts`+`allow-same-origin` 조합
  경고는 예상된 것이다 — 밴드는 남의 코드가 아니고, 그래도 넘어가면 아래 (3)이 잡는다.)

**(3) 죽어도 자국이 남는다.** 넘어간 뒤에는 전역이 없으니 전역에 적어 둘 수 없다.
`grab_posts.js` 가 **localStorage** 에 심장 소리(`__grabBeat`)와 죽은 자리(`__grabDeath`,
`beforeunload`·`pagehide`)를 남긴다 — **출처가 그대로 band.us 라 살아남는다.**
그래서 처음으로 **'한 번도 안 함' 과 '긁다가 죽음'** 이 갈린다([169] 의 요구).
`band_dump_state.js` 가 그것을 읽어 `NEVER_STARTED`/`DIED_AFTER_START` 로 답한다
(`NO __GRAB` 문자열은 **계약이라 그대로 뒀다** — browser_chain·inject_and_verify 가 찾는다).

**(4) 중간 저장.** 예전엔 `__grabSave()` 를 **맨 끝에 한 번** 불렀다 — 90분 배치가 3분째에
죽으면 뜯어 놓은 것까지 통째로 사라지고, 남는 그림이 '덤프 없음'이라 **한 번도 안 긁은
것과 구별되지 않았다.** 이제 50건마다 떨어뜨린다(죽음의 값이 배치가 아니라 50건이 된다).
이름은 `dump_<시각>s2_<밴드>.json` — `band_from_name` 을 **실제로 불러** 유령 밴드가
안 생기는지 확인했다(2026-08-08 두 실사고와 같은 모양을 피한다).

**(5) 성공의 정의를 좁혔다** — `band/liveness.py`. 주입은 '붙여넣었다'만 증명하고(#34),
생존은 '살아 있다'만 증명한다([162] — 살아서 0건을 담을 수 있다). 그래서 **둘 다** 없으면
성공이라 적지 않는다: **생존확인 1회 이상 + 이 회차 덤프의 쓸 수 있는 댓글 증가**
(시각 있는 댓글만 — 흡수기와 같은 잣대, [130]). 0 은 셋으로 갈라 말한다 —
`확인된0개`(진짜 없음, [199]) · `미확인`(안 봄) · `덤프없음`(저장까지 못 감).

**(6) 거짓 죽음도 막았다** (#36 의 반대편). 탐침은 **활성 탭**에 붙으므로 사람이 다른
band.us 탭을 보고 있으면 전역이 없어 `NO __GRAB` 이 온다 — 죽음과 똑같이 생겼다.
멀쩡한 수집을 죽었다고 하면 **사람이 다시 돌려 중복 수확**한다. localStorage 는 탭이
달라도 같으므로 **심장 소리가 싱싱하고 아직 running 이면 '다른 탭에서 살아 있음'** 이다.

**실측으로 확인된 오래된 딱지**: `reports/브라우저_체인.json` 의 00:23:44 기록은
`밴드 댓글 84789192 · 시작됨 · 살아 있다 · running:false ok:2` 인데, 같은 시각 캐시
`90610953` 은 글 5,255 · comments 키 4,733 · **댓글 본문 0건** · 확인된0개 1,932 다.
살아 있었고 끝났는데 **쓸 수 있는 댓글은 하나도 안 늘었다** — 예전 딱지로는 그것이
'시작됨'으로만 남는다. 새 판정이라면 `살아있음_수확없음` 이라고 적힌다.

**재발방지** 검증 **`[229]`** (생존확인 없으면 수확이 있어도 성공 아님 · 0을 셋으로 가름 ·
싱싱한 심장은 죽음 아님 · sandbox 값에 `allow-top-navigation` 없음 · 덤프 이름이 유령을
안 만듦 · `subprocess.run(timeout=)` 금지[175]).
· 몸통은 8/12 00:58 에 이미 있었지만 옆 세션이 `synthetic_check.py` 를 편집 중이라
  `tests/_pending_band_liveness_check.py` 에 따로 두고 분담판 **`[36]`** 으로 올려 뒀다.
  **관문 밖에 있는 검사는 아무도 안 돌린다** — "ALL GREEN 확인 후 실작업" 은
  `synthetic_check.py` 만 본다. 만들어 뒀지만 안 도는 자리이며, 같은 새벽에 `[228]` 이
  스케줄러에서 잡아낸 구멍과 **모양이 같다.** 02:2x 에 옮기고 번호를 달았으며,
  **사본은 지웠다**(두 벌이 되면 한쪽만 고쳐지고 그쪽은 틀린 줄도 모른다 — `[229]` 가 막는다).
· **`[37]` 도 같은 틱에 끝냈다** — `browser_chain.settle_band()`. 전에는 붙여넣은
  **직후 한 번만** 묻고 '시작됨'이라 적은 뒤 떠났는데, 250건 수집은 20분을 넘게 돈다.
  3분째에 죽어도 그 회차는 이미 '시작됨'을 적어 뒀고 다음 tick 은 `too_soon`(90분)에
  걸려 건너뛴다 — **성공도 실패도 아닌 채로 90분**이고 화면에는 '시작됨'만 남는다.
  이제 tick 마다(15분) 한 번씩 물어 `liveness.verdict` 로 마무리한다.
  · 판정은 `liveness` 한 곳이 한다 — 이쪽 몫은 **'아직 이른가'** 뿐이다(`WATCH_MIN` 30분
    안의 무수확은 `진행중`. 멀쩡히 도는 수집을 실패라 부르면 사람이 다시 돌리러 간다 — #36).
  · **막지 않는다** — `liveness.watch`(30분)를 tick 안에서 돌면 잠금을 쥔 채 다음 tick 을
    굶는다([175]). 한 tick 에 한 번만 묻는다.
  · ★ **묻는 행위가 재시도를 영원히 미룰 뻔했다.** `note()` 가 '마지막' 시각을 지금으로
    갱신하는데 `too_soon` 이 그 시각을 본다 — 마무리를 물을 때마다 90분이 다시 시작된다.
    시계의 기준은 **붙여넣은 때**로 되돌린다.
· ★ 첫 판 검사가 `"allow-top-navigation" in js` 였다가 **그 플래그를 왜 안 넣는지
  설명한 주석**에 걸려 빨강이 났다 — #38 의 '사고를 적는 행위가 사고 모양을 만들었다'
  와 같은 자리다. 검사는 **실리는 속성값**을 뜯어 보게 고쳤다(설명은 남긴다).
· ★ **긁는 것은 여전히 'CSOS 리서치 및 자료 수집' 세션 몫이다.** 이 세션(코딩)은
  주입을 한 번도 하지 않았다 — 판정 논리는 `--selftest` 와 로컬 재현으로만 검사했다.

### (나) ERP 전화면 몰이 — 9화면 중 1개만 받고 `완료: true`
- 21:10:08 `loginab.ecount.com` 에 주입(핑으로 증명). 21:17 상태 조회 결과
  `done9-left0-fin1` — 겉으로는 완주.
- 그런데 화면별 결과를 꺼내 보니: `ledger`(계정별원장) **받음 18행**,
  나머지 **여덟 개 전부 실패** · 이유가 전부 같다 — **"사이트맵이 안 열렸다(빈 채로 읽음)"**.
- **첫 화면만 성공하고 그 뒤가 전부 같은 이유로 실패**하는 모양은 하나를 가리킨다:
  몰이가 화면을 연 뒤 **홈으로 돌아가지 않아** 사이트맵을 다시 못 연다.
  조회가 끝난 시점의 창 제목이 `판매조회` 인 것이 그 증거다.
- **고칠 곳**: `band/ERP_전화면몰이_붙여넣기.js` — 화면과 화면 사이에 **홈으로 복귀**하고,
  사이트맵이 실제로 열렸는지(링크수>0) 확인한 뒤에 다음으로 간다.
- ★ **그리고 요약 숫자가 거짓말을 했다.** `완료: true` · `끝난것 9` 는 '아홉 개를 받았다'로
  읽힌다. 실제로는 '아홉 개를 시도했다'였다. **시도를 수확으로 세지 말 것** —
  `끝난것` 은 `결과=='받음'` 만 세고, 실패가 있으면 완료라고 적지 않는다.

**교훈.** `[169]` 가 "0건이면 없는 건가 안 본 건가"를 묻게 했고, #34 는 "도구가 했다고
말하는 것은 증거가 아니다"였다. 여기는 그 둘의 합이다 — **살아 있는지 물어보면 답한다.**
페이지에 상태를 묻는 작은 스크립트 하나가 반나절짜리 오해를 1분에 끝냈다.


---

## #36 — 돌아오지 않은 몰이 · 성공을 실패로 읽은 종료코드 (2026-08-11 밤, 실측)

#35 가 "8화면이 왜 실패했나"를 **추정**으로 남겼다면, 여기는 그 추정이 **확정**된 자리다.

### (가) 근거는 탭 제목 하나였다 — 몰이는 홈으로 안 돌아간다
- 22:0x 크롬 탭을 훑으니 ERP 탭이 **`판매조회`** 화면에 그대로 서 있었다. 몰이가
  마지막으로 연 화면이다. 즉 화면을 연 뒤 **아무 데도 안 돌아갔다.**
- 화면 하나를 열면 그 화면이 셸을 차지해 **'사이트맵' 손잡이가 사라진다.** 그래서
  두 번째 화면부터는 `.wrapper-sitemap` 이 아예 없고 `링크수 0` 이 된다.
  **첫 화면만 성공하고 나머지가 전부 같은 이유로 실패하는 모양은 언제나
  '돌아오지 않았다'를 가리킨다** — 화면마다 다른 이유로 실패하면 화면 문제이고,
  전부 같은 이유면 그 사이의 공통 단계가 범인이다.
- 고친 것: `goHome()` 으로 화면 사이에 돌아가고, **사이트맵이 정말 열렸는지 링크수로
  확인**한 뒤 안 열렸으면 홈 복귀 후 한 번 더 시도한다. 실패하면 그 화면의 손잡이
  목록(`진단`)을 남긴다 — '안 열렸다'만 적으면 다음 회차도 똑같이 실패한다.
- ★ **`location` 은 건드리지 않는다.** 최상위 문서가 navigate 되면 주입 스크립트가
  그 자리에서 죽는다(#35 의 밴드 수집기가 정확히 그랬다). 그래서 홈 복귀도 **클릭만**이다.
- ★ 그리고 `완료: true` 를 무조건 적던 것을 고쳤다. 받음·실패·건너뜀을 따로 세고
  **실패가 0 일 때만** 완료다. 예전에는 아홉 개 '시도'가 아홉 개 '수확'으로 읽혔다.

### (나) 이번엔 반대였다 — 성공이 실패처럼 보였다
- `inject_and_verify.ps1` 이 주입 직후 `$LASTEXITCODE -ne 0` 으로 실패를 판정했다.
  그런데 `inject_here.ps1` 은 `Write-Output` 으로 끝난다 — **PowerShell 은 그때
  `$LASTEXITCODE` 를 건드리지 않는다.** 새 셸에서는 그 값이 비어 있고, 빈 값은
  `-ne 0` 에서 참이다. 그래서 **페이로드가 이미 콘솔에 들어간 1초 뒤에**
  `ABORT: injection failed` 를 찍고 검증을 접었다(22:08:20 주입 → 22:08:21 중단).
- 이 프로젝트가 계속 경계해 온 것은 '실패가 성공처럼 보이는 자리'였다. 여기는 그
  **반대편**이다. 결과는 더 나쁠 수 있다 — 실제로는 잘 돌고 있는 것을 실패로 읽으면
  사람이 **멀쩡한 것을 다시 돌리러 간다**(중복 수확·중복 다운로드).
- 고친 것: 주입기는 끝에 `exit 0` 으로 **성공을 소리 내어 말하고**, 검증기는
  종료코드만 믿지 않고 `INJECTED on` 표식으로 판단한다.
- ★ 그리고 **죽음의 신호는 페이로드마다 이름이 다르다** — 밴드는 `NO __GRAB`,
  ERP 는 `NOSTATE`. 하나만 보던 검증기는 시작도 안 한 ERP 스크립트를 ALIVE 라
  말했을 것이다. **죽음을 못 알아보는 계기는 계기가 아니다**(`[169]` 의 모양).

**교훈.** #34 는 "도구가 했다고 말하는 것은 증거가 아니다", #35 는 "살아 있는지
물어보면 답한다"였다. 여기는 셋째다 — **판정의 근거 자체가 틀릴 수 있다.**
종료코드든 상태 전역이든, 그 근거가 이 경로에서 정말 채워지는 값인지 먼저 확인한다.


---

## #37 — 붙여넣기 파일이 250건에서 잘려 **사람을 일곱 번 부르고 있었다** (2026-08-11 밤, 실측)

사용자 지시: **"전부 다 긁어와"**

### 무엇이 잘못됐나
- 밴드 90610953 의 댓글 백필 대상은 **1,663건**인데 `comment_backfill.write_paste` 가
  디스크에 낸 파일에는 **250건**만 들어 있었다. 나머지 1,413건은 사람이 명령을 여섯 번
  더 내려야 나온다 — 그리고 **아무도 여섯 번 더 안 한다.**
- **조용한 종류다.** 그 250건은 멀쩡히 긁히고 덤프도 나오고 캐시에도 들어간다.
  실패한 티가 한 군데도 안 난다. 그래서 이 백필은 며칠째 "돌렸는데 왜 안 줄지"였다.
- 250(`BATCH_MAX`)은 **한 회차** 한도다 — 그 위로는 탭 렌더러가 언다(실측). 그런데
  `make_oneclick.build()` 는 **이미** 250씩 끊어 회차 사이에 `__grabSave()` +
  `{keep:false}` 로 탭 메모리를 비운다. 즉 한 파일에 1,663건을 담을 수 있었다.
  못 담은 이유는 딱 하나 — **붙여넣기 파일을 만드는 자리가 둘이었다**([162] 가
  수집기에서 겪은 일이 한 겹 위에서 그대로 반복됐다).
- 고친 것: `write_paste` 가 `make_oneclick.build(nos=...)` 를 부른다. 제 구동부
  (HEAD/TAIL)는 **지웠다** — 남겨 두면 다음 사람이 그것을 고치고 파일은 안 바뀐다.
  덤으로 [217] 의 죽은 번호 거르기(`screen`)가 이 갈래에도 걸린다(예전엔 안 걸렀다).
- `--limit` 의 뜻도 갈랐다: **앱 계획(유저스크립트)이 한 번에 받아 갈 몫**이지
  사람이 붙여넣을 파일의 한도가 아니다.

### 시작조차 못 하던 자리 — `band/inject_find_tab.ps1`
- `inject_here.ps1` 은 **활성 탭**에 붙여넣고, ping 이 다른 출처라고 하면 거절한다
  (옳다). 그런데 ERP 몰이가 크롬을 ecount 에 세워 두면 밴드 주입은 **영영 시작을 못 한다.**
- 탭을 **제목으로 고르지 않는다.** `MainWindowTitle` 은 몇 초 늦고, 실측으로 탭 4개를
  2개로 셌다. 심판은 ping 의 hostname 이다 — 이미 그 일을 하고 있던 것을 그대로 쓴다.
  탭당 ping 한 번이 값인데, 틀릴 수 없는 값이다. 실측 **2번째 탭**에서 성공.
- 넘어가는 조건은 **'다른 출처'일 때뿐**이다. 크롬이 없다·콘솔이 안 열린다 같은 실패로
  탭을 넘기면 진짜 원인이 여덟 번 가려진 채 사라진다.

### '전부'가 무엇인지도 세 번 바뀌었다 — 세는 법이 곧 답이다
- 원시 캐시로는 **7,650건**(빈칸인데 `comments_full` 아님)
- `comment_plan` 은 **0건**(댓글 키가 아예 없는 글만 세는데 그것들이 전부 죽은 번호였다)
- `comment_backfill` 의 알고리즘([177])으로는 **1,663건** — 업무글이고 프로젝트NO 가
  있는 것만. 나머지는 댓글을 다 읽어도 **바꿀 것이 없다.**
- 같은 캐시를 놓고 세 도구가 0·1,663·7,650 을 말한다. **"전부"라는 말에는 기준이
  없다** — 무엇을 세는지 먼저 말하지 않으면 어느 숫자도 틀리지 않고 어느 숫자도 답이 아니다.

### 실측 (2026-08-11 밤)
- 파일 **1,663건 · 7회차 · 28.9KB** 한 장.
- 22:40:01 주입 → 100초 뒤 계기: `running:true · paused:false · ok:52 · failed:1 ·
  sec:155 · last:3298` → **3.0초/건**, 전체 약 83분.
- ERP 몰이는 **받음 5 · 실패 3 · 건너뜀 1**. 실패 셋은 전부 `홈복귀 false` —
  화면 하나를 연 뒤 거기 갇힌 채 다음 화면을 찾았다. 진단은
  `reports/ERP_전화면몰이_상태.md` 에 옮겨 뒀다(Downloads 는 파이프라인이 흡수해 사라진다).

**교훈.** #35 는 "살아 있는지 물어보라", #36 은 "판정의 근거 자체가 틀릴 수 있다"였다.
여기는 넷째다 — **사람 손을 한 번으로 줄였다고 적어 놓고 실제로는 일곱 번을 요구할 수
있다.** 손이 몇 번 가는지는 코드가 아니라 **디스크에 있는 그 파일**이 정한다.

## #38 — 댓글을 다 읽어 놓고 흡수기가 **전부 버렸다** (2026-08-12 새벽, 실측)

**증상.** 2026-08-09 에 선택자를 고쳐([162]) 수집기는 댓글을 담기 시작했다. 그런데
캐시의 '댓글 담긴 글'은 그 뒤로도 **계속 0** 이었다. 오류는 한 줄도 안 났고,
`cancel_watch` 는 멀쩡히 돌면서 댓글 취소를 한 건도 못 잡았다. 8/8·8/9·8/11 세 번에
걸쳐 "이 밴드는 정말 댓글이 없나 보다"로 읽혔다.

**원인.** 버린 것은 수집기가 아니라 **흡수기**였다. 글 4369 를 긁으니 덤프에는
작성자·시각·본문까지 댓글 6건이 멀쩡히 들어 있었다(밴드가 적어 둔 `comment_count 6`
과 정확히 일치). `convert_dump.parse_dt` 의 `ABS` 정규식이 **연·월·일 + 시:분을 전부**
요구하는데 밴드는 나이에 따라 줄여 적는다:

| 밴드가 적는 모양 | 예 | 예전 결과 |
|---|---|---|
| 오늘 | `오후 3:57` | None |
| 올해 | `3월 31일 오전 8:14` | None |
| 지난해부터 | `2026년 1월 26일` | None |

실측한 다섯 개가 전부 None 이었다. 시각이 없으면 `[130]` 규칙이 버리므로 댓글은
**100% 사라졌다.** 그리고 글은 `comments_full=True` 로 닫혀 '확인된 0개'가 되므로
`comment_backfill` 이 **다시 뽑지도 않았다**(`[199]`) — 스스로 닫히는 고장이다.

**왜 아무도 몰랐나.** 세 겹으로 조용했다. ① 흡수기는 버리면서 아무 말도 안 한다
② 캐시는 `comments: []` 라 '보고 없었다'와 구별이 안 된다 ③ 내가 만든 계기
`dump_comment_count` 까지 `created_at` 만 보다가 진짜 댓글 6건을 '껍데기'로 셌다.
**담는 쪽이 둘이면 세는 쪽도 둘을 다 알아야 한다** — API 덤프는 `created_at`(ms),
화면 긁기는 `timeText`(사람이 읽는 글자).

**조치.** `MD_TIME`(연도 없음 — 수확 시각보다 뒤면 작년으로 되돌린다) ·
`HM`(오늘·어제) · `ABS_DAY`(날짜만 → **하루의 끝**에 놓는다. 아는 것은 날짜뿐이지만
이 값이 쓰이는 자리는 '댓글이 글보다 나중인가' 하나이고, 하루의 끝은 그 순서를
절대 깨지 않는 유일한 지점이다). `dump_comment_count` 도 같은 잣대로 고쳤다.

**실측.** 합성검증 ALL GREEN → 재병합 후 캐시 **댓글 담긴 글 0 → 8 · 댓글 13건**
(84789192 1 · 90610953 12). 글 4369 = 6건.

**교훈.** `[169]` 는 "0건이면 없는 건가 안 본 건가"를 물으라 했다. 여기엔 세 번째가
있었다 — **보고 담았는데 받는 쪽이 버린 것.** 그리고 그것을 재는 계기마저 같은
낱말을 몰라 0 이라 말하면, 세 화면이 사이좋게 같은 거짓말을 한다.

### 같은 밤 같이 잡은 것 — 주입기가 브라우저·창·IME 를 짐작하고 있었다
- **브라우저**: `Get-Process chrome` 뿐이었다. 밴드·ERP 는 **웨일**에도 열려 있었고,
  유일한 크롬 창은 무관한 앱이었다. "콘솔이 안 열린다"는 참말을 **틀린 창에 대해**
  했다. 이제 창 있는 크로미움을 다 훑고 판정은 핑의 hostname 이 한다.
- **창**: 최소화된 크롬은 `MainWindowTitle` 이 빈 문자열이라 조건에서 빠졌다.
  01:22 주입은 **성공했는데** 그 뒤 창이 내려가 탭이 `document.hidden` 이 됐고
  수집기는 스스로 멈췄다(옳다). 13분 동안 덤프 0건 · 실패 표시 0줄 —
  **성공한 주입이 조용히 아무것도 안 하는 자리.** 이제 `MainWindowHandle` 로 고른다.
- **IME**: `SendKeys` 는 IME 를 통과한다. `https://www.band.us/...` 가
  `ㅗㅅ센://ㅈㅈㅈ.ㅠ뭉.ㅕㄴ/...` 로 들어가 네이버 검색이 됐고, 콘솔에 포커스가 없을
  때는 그 자모가 **주소창으로 가 사람 탭을 옮겼다**(2회). 글자는 클립보드나
  `SendInput` 으로 넣는다.
- **포커스**: 배경 프로세스의 `SetForegroundWindow` 를 윈도우가 거절한다 —
  00:23 성공 / 01:10 3회 실패의 정체. `AttachThreadInput` 을 붙여 해결.
- 검증 `[162]` · `[199]`.

---

## #39 — 원본은 있는데 최신 40개 밖이라 “판매조회 없음”이 됐다 (2026-08-15, 실측)

**증상.** 자율복구의 `명세서 ↔ 프로젝트 색인`이 세 번 연속 rc=1로 끝났고 마지막 문장은
`판매조회 원본을 찾지 못함 — 중단`이었다. 산출물 `명세서_프로젝트_매칭.*`도 8/13에
멈췄다.

**원인.** 원본이 없던 것이 아니다. 같은 회차에서 먼저 돈 `erp_sales_index.py`는 11:31에
유효 판매조회 두 파일로 1,822개 프로젝트를 만들었다. ERP 폴더 엑셀 120개를 수정시각으로
정렬하면 그 파일은 47·51위다. `stmt_link.py`만 `cands[:40]`으로 잘랐다. 40개 안에도
sales로 분류된 파일 세 개가 있었지만 유효 UJ 행이 없어, “찾았지만 쓸 행이 없음”이
“원본 없음”으로 합쳐졌다.

**조치.** `erp_sales_index.sales_candidate_paths()`를 공용 문으로 만들었다. 최신 60개는
새 파일 발견을 위해 그대로 보고, 분류 캐시에서 sales로 입증된 파일은 순위와 관계없이
후보에 더한다. 같은 경로는 한 번만 읽고, 캐시는 채택 근거로 믿지 않는다 — 두 소비자가
종전 머리글 검사를 다시 통과시켜야만 판매조회로 쓴다. `stmt_link`의 별도 40개 절단은
제거했다.

**검증.** `[286]`은 40개 밖 74번째 입증 파일이 남는지, 최근/캐시가 겹쳐도 한 번만
읽는지, 두 색인이 같은 선택기를 쓰는지 고정한다. 실캐시 모의 적용 결과 원인 파일
47·51위가 모두 선택됐다. 원 업무 명령은 후속검토에서 재실행하지 않았고, 성공 rc=0을
정규 워치독이 확인하기 전까지 큐를 수동으로 닫지 않았다.

**교훈.** 성능 상한은 **파일을 읽는 개수**에 걸어야지 **증거가 될 수 있는 원본의
자격**을 잘라서는 안 된다. “최신 N개”는 시간이 지나면 반드시 다른 종류 파일에 밀린다.
상한을 둘 때는 이미 입증된 원본을 상한 밖에서도 보존하고, 생산자·소비자가 같은 후보
선택 규칙을 써야 한다.

## #40 — 가드를 우회해 **가짜 묘비**를 만들었다 (2026-08-19, 실측)

**증상**  밴드 84789192 재수집 13건이 `ok 7 · missing 1 · fail 5` 로 끝났다. 첫
배치 3건은 `ok 3 · fail 0` 이었다.

**무엇을 했나**  크롬 확장이 이 계정에 연결돼 있어 로그인된 밴드 탭 안에서 직접
수집기를 돌릴 수 있었다. 탭이 화면 뒤라 `grab_posts.js` 가 시작을 거절하기에,
`document.hidden` 을 `Object.defineProperty` 로 속여 우회했다. 근거는 숨은
iframe 으로 글 3542 를 열어 **본문·작성시각까지 받은 실측**이었다.

**원인**  그 실측은 맞았지만 **재는 범위가 좁았다**. 막는 것은 `requestAnimationFrame`
이 아니라 **크롬 백그라운드 타이머 스로틀링**이다 — 단계별 자국으로 재 보니
`iframe.onload` 뒤의 `setTimeout(8000)` 이 **3분 넘게** 안 불렸다(intensive
throttling 은 백그라운드 5분 뒤부터 걸린다). 그래서 첫 몇 건은 되고 그 뒤가
무너진다. 무음 `AudioContext` 로도 안 풀렸다(3초 타이머가 18초째 미발화).

**나쁜 결과**  글 3532 는 본문 202자·작성시각이 남아 있는 **실재 글**인데,
껍데기(`.postMain`)만 그려지고 `.postText` 가 제때 안 와서 수집기가 `missing`
으로 판정 → 캐시에 `deleted=True` 가 박혔다. 그러면 `recheck_plan`·`recollect` 가
그 번호를 **영영 다시 안 뽑는다**. (`fail` 5건은 묘비가 아니라 안전했다 —
위험한 것은 `missing` 하나다.)

**조치**  ① 문제 덤프 둘을 **지우지 않고** `*.가짜묘비_격리_20260819.json.bak` 으로
옮겨 `dump_files()` 가 다시 안 읽게 했다 ② 캐시의 `deleted` 를 지우고
`deleted_revoked` 에 이유를 남겼다([169]) ③ 실측 16→15→**16** 복구.

**교훈**  가드가 *"돌리면 전부 실패로 기록된다"* 고 적어 둔 것은 경고가 아니라
**실측**이었다. 실측으로 반증했다고 생각되면 **그 실측이 재는 범위**를 먼저
의심한다 — 한 건이 되는 것과 열세 건이 되는 것은 다른 말이고, 스로틀링은
**5분 뒤에** 온다. 백그라운드 탭 수집은 하지 않는다.

**검증**  `[330]`(크롬 개발자 모드 관문) · 지시문 `[111]` 항목에 같이 적었다.

---

## 2026-08-28 · 작성시각이 보인다고 살아 있는 글이 아니다 — 묘비 오판

**증상**  캐시에 '지워짐'(deleted/absent) 딱지가 붙은 글이 379개(84789192 130 ·
90610953 396 중 최근구간 4)였다. 표본을 상세 화면으로 열어 보니 **작성시각과
글쓴이가 그대로 보였다.** 그래서 "실재하는 글인데 잘못 박혔다"고 읽고 5개를
되돌렸다(3332 · 5171 · 5195 · 5248 · 4257).

**원인**  판단이 틀렸다. **밴드는 글을 지워도 작성자·작성시각 껍데기를 남긴다.**
그것을 '살아 있다'는 증거로 읽은 것이 잘못이고, 그 증거로는 세 갈래가 하나도
안 갈린다.

**대조군으로 갈린 것**(2026-08-28 실측 · 살아 있는 글 3548·5501 과 나란히 잼)

| 갈래 | `.postText` | 문서 제목 | 본문 길이 |
|---|---|---|---|
| **살아 있는 글** | 있다 | **글 첫 줄** | 478 · 942 |
| **지워진 글** | 없다(80초 기다려도) | 밴드 이름만 | -1 |
| **아직 안 쓴 번호** | **있다**(직전 화면 잔상) | `게시글 : 밴드이름` | 여럿이 **똑같이** 185 |

곧 `.postText` 가 있다고 살아 있는 것도 아니다 — 없는 번호는 **직전 화면 본문이
그대로 잡힌다**(지시문 [217] 이 적어 둔 그 함정). 셋을 가르는 것은
**`.postText` 와 문서 제목을 같이 보는 것**이다.

**나쁜 결과**  없었다. 되돌린 5개는 캐시에 본문이 남아 있어 잃은 것이 없고,
덤프는 흡수 전에 격리했다(`dump_*.가짜묘비_격리_20260828.json.bak`).
다만 **되돌린 근거가 틀린 채로 자국에 남을 뻔했다** — 다음 사람이 그 자국을
보고 또 되돌리면 같은 일이 반복된다.

**조치**  ① 되돌린 5개의 `deleted_revoked` 에 `정정` 칸을 더해 *"이 근거는
틀렸다 · 다시 되돌리지 말 것"* 과 대조군 실측을 적었다([169] — 값은 안 지운다)
② 다음 회차가 다시 `missing` 으로 판정해 묘비를 되박게 둔다(사람이 손으로 박지
않는다) ③ 최대 번호보다 **위**에 있는 묘비는 0개임을 확인했다 — 밴드는 번호를
순차로 주므로 옛 번호에 나중에 글이 생기지 않는다. **지금은 안전하다.**

**교훈**  ★ **대조군 없이 '실재한다'고 말하지 않는다.** 살아 있는 글 하나를 같은
방법으로 재 보면 5분이면 갈린다 — 그것을 안 해서 하루를 잘못된 방향으로 썼다
([339] 가 열 존재 확인에서 배운 것과 같은 자리, 대상만 다르다).
★ 그리고 **2026-08-27 에 3545~3547 을 되돌린 것은 옳았다** — 그것은 *지워진 글*이
아니라 *아직 안 쓴 번호*였다가 그날 오후에 진짜 글이 생긴 경우다(PO 3건 ·
계산서 요청 1건을 실제로 받았다). 두 갈래를 뭉치면 둘 다 틀린다.

**검증**  없다 — 이 판정은 브라우저 화면을 봐야 갈리므로 관문에서 못 잰다.
대신 판정표를 여기 남긴다.

---

## 2026-08-29 — 명세서 PDF를 다 보지 않고 0으로 닫을 수 있었다

**증상.** 자율복구 키 `7e4b6250fd17ec25270f`(`stmt_archive.py --limit 150`)는
20회 이어간 뒤 `rc=0`으로 닫혔지만, 같은 이름의 일일회차 키
`a2e85f9f03eb344ccd0e`(`--limit 40`)는 9회째 `rc=124/-9`였다. 현재 AI 요청의 로컬
명령도 0이었으나 stdout 로그 파일은 남지 않아 `새로/건너뜀/실패` 숫자는 확인할 수 없었다.

**원인.** 세 가지가 한 성공 판정에 겹쳤다.

1. 기존 PDF를 원본 문서마다 `os.path.exists(dst)`로 확인해, 새로 만들 것이 없어도 Z:
   stat 왕복을 원본 수만큼 반복했다. 1,800초 키는 끝나도 300초 키는 같은 검사를 끝내지
   못할 수 있다.
2. `attempted >= --limit`에서 루프를 빠져나온 뒤 그대로 0을 반환했다. 상한 뒤 문서를
   아직 보지 않았으므로 이 0은 잔량 0의 증거가 아니었다.
3. `render_atomic()`이 False를 돌려 `fail`이 늘어도 마지막 반환값은 0이었다.

**조치.** 기존 PDF를 디렉터리 열람 한 번으로 집합화해 문서별 stat을 없앴고, 열람 오류는
무시하지 않는다. 시간예산 또는 건수상한 중단은 75, 렌더 실패는 1, 전체 문서를 확인하고
실패가 없을 때만 0으로 돌린다. 상한 75 뒤 다음 회차가 기존 파일을 모두 확인하면 그때 0이다.

**검증.** 합성검증 `[427]`을 확장해 기존 PDF별 `exists` 금지·건수상한 75·렌더 실패 1·
실제 완주 0을 실행으로 고정했다. 이 샌드박스에는 지정 Python과 Z:가 없어 검사는 실행하지
못했고 `git diff --check`만 통과했다. 업무 명령은 후속검토에서 재실행하지 않았으며, 활성
중복 큐도 성공 확인 전 수동으로 닫지 않았다. 상세:
`reports/autopilot_7e4b6250fd17ec25270f_후속검토_20260829_0311.md`.

## 2026-08-29 — 세금계산서 PDF도 다 보지 않고 0으로 닫을 수 있었다

**증상.** 자율복구 키 `356a36d720a2f67ea54d`(`tax_archive.py --limit 40`)는
300초 제한에서 6회째 `rc=124/-9`였다. 후속검토를 부른 로컬 실행은 0이었지만 stdout
로그가 없고 `세금계산서_상세.json`도 실행 전 시각인 8/28 22:04에 머물러 있었다.
따라서 `새로/건너뜀/실패` 숫자와 Z: 실물을 확인할 수 없는 0이었다.

**원인.** 바로 앞 명세서 PDF 사고와 같은 세 결함이 `tax_archive.py`에도 남아 있었다.

1. 마지막 상세 산출물의 280건마다 기존 PDF를 `os.path.exists(dst)`로 다시 물었다.
   `collect()` 자체도 오래 걸리는 단계라, 문서별 Z: stat이 더해지면 새 작업이 없어도
   300초 바깥 제한에 끊길 수 있었다.
2. 40건을 시도해 `--limit`에 닿으면 뒤 계산서를 보지 않고 루프를 끝낸 뒤 0을 반환했다.
3. `render_atomic()` 실패를 세어 놓고도 반환값은 0이었다.

**조치.** 기존 PDF를 디렉터리 열람 한 번으로 집합화해 계산서별 stat을 없앴다. 상한 뒤
잔량을 보지 못한 회차는 75, 렌더 실패는 1, 280건 전부를 확인하고 실패가 없을 때만 0이다.
디렉터리 스캔 오류는 일부 누락을 새 작업으로 오인하지 않도록 예외로 올린다.

**검증.** 합성검증 `[427]`에 세금계산서 합성 행을 추가해 기존 PDF별 `exists` 금지,
건수상한 75, 렌더 실패 1, 실제 완주 0을 고정했다. 업무 명령은 다시 실행하지 않았고,
다음 정상 회차가 새 계약으로 성공을 확인하기 전 큐는 수동으로 닫지 않았다. 이 샌드박스는
지정 Python과 Z:에 접근하지 못해 `git diff --check`만 통과했다. 상세:
`reports/autopilot_356a36d720a2f67ea54d_후속검토_20260829_0413.md`.

**교훈.** 같은 이름의 보관기라고 같은 계약을 자동으로 공유하지 않는다. 하나를 고쳤으면
형제 스크립트도 **0·75·1의 의미와 네트워크 왕복 위치**를 함께 검사해야 한다.

## 2026-08-29 — 카톡 새 원본을 못 찾은 반쪽 목록이 정상 캐시가 됐다

**증상.** 2026-08-27 카톡 원본 둘을 흡수기가 Z: 정본에 넣고 `새로 넣음 2`로
끝냈지만, `kakao_source_paths()`는 로컬 `kakao/inbox`의 옛 사본 18개만 돌렸다.
대표보고는 `카톡 최신 2026-07-31 · 밀림`, 추출기는 `등록 대상 0건`이라 했고 오류는
없었다. 잘못된 `.카톡_원본선택.json`은 그 회차에 새로 만들어져 캐시 오염이 원인이
아니라 **덜 읽은 계산 결과가 캐시된 것**이었다.

**원인.** 쓰는 쪽 정본은 `<카톡폴더>/2026/<원래이름>`인데 빠른 읽기는 그 자리를
공유하지 않았다. 이후 `canon_dir(create=False)`를 빌리고 자국 이름 누락 시 복구 재귀
탐색을 여는 고침이 자동 마무리 커밋에 들어갔지만, 복구 뒤에도 자국 파일을 못 찾았을 때
선택 캐시를 저장하는 문은 그대로였다. Z: 단절이나 부분 탐색이 다시 생기면 옛 두 방이
그럴듯한 결과를 채워 같은 사고를 반복할 수 있었다.

**조치.** 반영회차 `받은파일`을 빠른 길과 복구 탐색 양쪽에서 실제로 찾았는지 세고,
하나라도 없으면 선택 결과는 돌려주되 캐시하지 않는다. 누락 파일과 캐시 여부는
`카톡_원본선택_상태.json`에 원자 저장한다. 대표보고 수집 기준은 이 자국을 읽어
`사람 미보고`가 아니라 `원본 폴더를 다 못 읽었을 수 있음`을 함께 말한다. 카톡 하나가
실패했다고 확인된 쿠팡AS 밴드의 읽음 판정까지 끄지는 않는다.

**검증.** 합성검증 `[490]`은 옛 두 업무방만 있는 누락 대조군에서 캐시가 생기지 않는지,
누락 자국과 대표보고 원인이 남는지, 자국 파일 둘이 실제 정본에 있으면 정상 캐시되는지,
안전문을 강제로 끄면 계기가 실제로 회귀를 잡는지를 실행으로 고정한다. 후속검토
샌드박스에서는 지정 Python 실행 파일 접근이 거부되어 실행하지 못했고 `git diff --check`
만 통과했다. 카톡 업무 명령은 다시 실행하지 않았다. 상세:
`reports/worksplit_277_후속검토_20260829_1815.md`.

---
# CLAUDE.md 에서 옮겨 온 사고 기록 (2026-09-10)
형님 지시: **"옛 사고 기록을 INCIDENTS.md 로 옮겨"**.
지시문이 1.13MB 라 세션마다 컨텍스트의 큰 몫을 먹어서 옮겼다.
**내용은 한 글자도 안 바뀌었다** — 잘라 붙인 것이다.
CLAUDE.md 의 그 자리에는 제목 한 줄과 이 파일로 오는 길이 남아 있다.

## ★ 손으로는 재현되지 않는 고장 — 무인 회차는 `pythonw` 에서 돈다 (2026-08-12 실사고)

- `쿠팡업무_밴드재수집` 이 **매일 08:00 exit 1** 로 죽고 있었다. 원인은 한 줄이었다 —
  `band/recheck_plan.py:18` 의 맨몸 `sys.stdout.reconfigure(...)`. 스케줄러는
  **`pythonw.exe`(창 없음)** 로 부르므로 `sys.stdout` 이 **None** 이고, import 중에
  `AttributeError` 가 터져 **부른 회차까지 통째로 죽는다**(`recollect.plan()` 경유).
- ★ **손으로 돌리면 멀쩡하다.** `python.exe` 에는 콘솔이 있어 `sys.stdout` 이 살아 있다.
  그래서 `--plan` 을 아무리 돌려도 재현이 안 됐고 며칠을 못 찾았다. **재현이 안 되는
  고장은 "없는 고장"이 아니라 "다른 환경에서만 나는 고장"이다** — 스케줄러가 어떻게
  부르는지(`Get-ScheduledTask ... Actions`)를 먼저 본다.
- 자국이 이것을 살렸다. `[228]` 이 만든 `reports/밴드_재수집_오류.json` 이 없었으면
  그날도 못 찾았다. **무인으로 도는 회차를 새로 만들면 자국 남기기를 같이 넣는다.**
- 막는 관용구는 둘 다 좋다: `try:` 로 감싸거나 `if hasattr(sys.stdout, "reconfigure")`
  (`hasattr(None, ...)` 는 False 다). **사람이 아니라 검증이 훑는다** — `[235]`.

### 같은 날 잡은 것 셋 (전부 '성공이라 적히면서 안 돌던' 것)
- **배터리 한 줄이 회차 셋을 세웠다.** `New-ScheduledTaskSettingsSet` 의 **기본값**이
  '배터리면 시작 안 함 + 배터리로 바뀌면 중단'인데 설치본이 그걸 안 껐다. 노트북을
  뽑자 `정오회차`·`AutomationPipeline`·`밴드재수집` 이 멈췄다 — 그런데 **실패가 아니라
  `Queued`** 라서 아무 자국도 안 남는다. 실패는 코드가 남지만 대기는 안 남는다.
  살아 있는 작업 셋을 풀고(백업 `reports/스케줄러_백업_배터리설정_*.xml`) 설치본
  여섯 개에 `-AllowStartIfOnBatteries -DontStopIfGoingOnBatteries` 를 넣었다.
  **새 회차를 만들면 이 두 깃발을 반드시 적는다** — 안 적으면 기본값이 도로 물려진다.
- **퇴역한 규칙이 `.bat` 에 남아 매일 아침 90분을 먹었다.** 아침 입력 보호시간은
  2026-08-11 에 퇴역했는데(`operation_window`) `워치독실행.bat` 은 08:00~09:30 을
  하드코딩한 채였다. 그동안 자가치유·회차감시·인계갱신이 전부 멈췄고 스케줄러에는
  `0x0`(성공)으로 적혔다. 이제 bat 이 `operation_window.is_input_window()` 를 부른다 —
  **판단은 한 곳이다. 사본이 둘이면 한쪽만 고쳐진다.**
- **유령 밴드가 스스로를 되살리고 있었다.** `band_from_name` 은 ①'이미 캐시에 있는
  번호'를 ②길이(7~10자리) 검사보다 **먼저** 봤다. 그래서 유령 캐시가 한 번 생기면
  그 번호가 '아는 번호'가 되어 ②를 통째로 건너뛴다. 실측 `202608082047`(12자리 시각
  도장, 글 0개)이 살아 있어 `dump_202608082047_84789192.json` 이 유령으로 갔고 그날도
  새로 써졌다. 이제 `known_bands()` 가 `plausible_band()` 를 통과한 것만 담는다 —
  **캐시에 있다는 것만으로는 근거가 안 된다.**

## ★ 회차 한 단계가 영원히 멈출 수 있다 (2026-08-08 실사고)
- **`subprocess.run(timeout=)` 을 쓰지 말 것.** CPython 은 시간이 넘으면 `kill()` 뒤
  **윈도우에서만 시간제한 없는** `communicate()` 를 한 번 더 부른다. 자식이 SMB(Z:)
  읽기처럼 끊기지 않는 대기에 걸리면 TerminateProcess 가 안 먹고 거기서 안 끝난다.
  실측: `timeout=1800` 을 걸어 둔 '원본 폴더 정리'(source_tidy)가 **13시간 30분**을
  매달렸다 — 부모 CPU 0.4초 · 자식 0.5초, 둘 다 그냥 서 있었다.
- **조용한 이유**: 멈춘 회차가 **락을 쥔 채**라 다음 회차는 "이미 실행 중"으로 건너뛰고,
  스케줄러는 '성공'이라 적는다. 그래서 09:50 이 하는 일(접수취소·객관완료·청구상태·
  대조)이 하루 종일 안 돌면서 **어느 화면에도 티가 안 난다.**
- 규칙 셋: ① `Popen` + `communicate(timeout=)` ② 죽일 때는 **나무째**
  (`taskkill /F /T` — 윈도우 `kill()` 은 손자를 안 죽인다) ③ **죽인 뒤 드레인에도
  제한을 건다.** 그래도 안 죽으면 pid 를 리포트에 남기고 **다음 단계로 간다** —
  단계 하나를 살리자고 회차 전체를 세우지 않는다.
- 검증 `[179]`.

## ★ 재수집 '바뀜'이 늘 100% 였다 — 지문에 수집시각이 들어 있었다 (2026-08-12 실사고)

- 밴드 140건을 다시 받았더니 `새 글 0 · **바뀐 글 140** · 그대로 18` 이 나왔다.
  받은 것의 **전부**가 바뀐 것이다. 원인은 `datalake.ingest_band` 가 페이로드에
  `수집시각`(captured_at)을 담고 **`hash_on` 을 안 준 것** — 그러면 페이로드 전체를
  해싱하므로 **다시 긁은 글은 예외 없이 '바뀜'** 이다.
- ★ 그래서 **08:00 재수집 회차가 존재 이유를 잃고 있었다.** 그 회차는 '받은 뒤에
  내용이 바뀐 글'을 찾으라고 만든 것인데, 전부가 바뀜이면 아무도 안 본다(`[170]` —
  경보가 대부분이면 그 경보는 죽는다). 숫자도 나오고 오류도 안 나서 티가 안 났다.
- `hash_on=["밴드ID","글번호","글쓴이","본문","사진수","댓글수"]` 한 줄. **수집시각은
  페이로드에 그대로 남긴다** — 언제 긁었나는 여전히 알아야 한다. 빼는 것은 지문에서다.
- 실측: 수집시각만 다르면 `same`, 본문이 바뀌면 `changed`.
- ⚠ **새 갈래를 `put_record` 로 담을 때는 `hash_on` 을 먼저 생각한다.** 페이로드에
  '그때그때 달라지는 값'(수집시각·조회시각·일련번호)이 있으면 반드시 빼야 한다.
  ERP 쪽은 이미 `hash_on=core_cols` 를 주고 있었다 — 밴드만 빠져 있었다.

## ★ 파서가 **0건**으로 읽은 원본은 '없는 것'이 아니다 (2026-08-14 실사고 · 2026-08-17)

형님: **"오늘 오전 유수비가 이야기한거 반영이 하나도 안되었어"**(8/14 16:37 캡처 첨부)

- **그 지시는 이미 반영돼 있었다** — 형님이 보신 이미지는 고치기 **전**에 만든 것이고,
  그 캡처를 만든 코드는 지금 저장소에 없다(`A4 한 장`·`종류별 일정`·`전체 99건을 모두
  실었습니다` 세 문구가 `git grep` 0건). 고칠 것이 없는 자리를 고치면 안 된다(`[172]`).
- **그런데 자동 경로는 한 글자도 안 들어갔다.** 그날 원본
  `KakaoTalk_Longtxt_20260814_1116_36_104.txt` 는 카톡 내보내기가 아니라 **통화 녹취**
  (STT)다 — 첫 줄이 `발화자 1 (00:00)` 이고 **`유수비` 문자열이 0회**, 시각은 경과
  시간이라 `kakao_extract.parse_export()` 가 **0건**을 돌려준다. `ceo_events` 도 0건.
  **오류는 한 줄도 안 난다** — 파일도 있고 흡수도 됐고 회차도 성공이다.
  즉 그날 대표 지시가 반영된 유일한 이유는 **그날 세션이 눈으로 읽고 손으로 옮겼기**
  때문이고, 기계로는 앞으로도 0건이다(`[169]` — 없는 것과 안 본 것이 구별되지 않는다).
- 이제 `ceo_events.extract()` 가 **파일마다 파싱 건수를 세고** 0건이면 `못읽은원본` 에
  갈래와 함께 담는다(`unreadable_kind` — 알아본 것만 이름을 주고 나머지는 `모름`).
  리포트 **맨 위**에 뜨고, `session_handoff.ceo_unreadable()` 이 그것을 읽어 인계
  **'먼저 처리할 것'** 에 올린다.
- ★ **세는 것은 대표 메시지가 아니라 파싱된 전체 메시지다.** 대표 메시지로 세면 대표가
  말한 적 없는 멀쩡한 방이 전부 경보가 되어 아무도 안 본다(`[170]`). 실측 경보 비율
  **원본 51개 중 1개** — 노이즈가 아니다. 이 비율이 커지면 기준부터 다시 본다.
- ★ **녹취 본문을 자동으로 '대표 지시'로 만들지 않는다**(`[172]`). 파일 어디에도 누가
  대표인지 적혀 있지 않고(`발화자 3` 이 지시하고 `발화자 2` 가 "예 대표님" 이라 답하니
  3번으로 **보일 뿐**이다) STT 오인식이 잦다(실측 `돌발`→`뻥뚱` · `정기점검`→`전기 점검` ·
  `류지영`→`류재영`). 잘못 뽑아 박으면 **없는 대표 지시가 캡처에 뜬다** — 못 잡는 것보다
  나쁘다. 그러므로 **"사람이 읽어야 함"까지만 말하고 멈춘다.**
- ★ **인계까지 잇는 것이 반쪽의 나머지다.** 만들기 전 실측으로 `ceo_events` 를 읽는
  코드가 프로젝트에 **한 줄도 없었다**(`session_handoff`·`system_audit` 둘 다 0건).
  리포트에만 적으면 **아무도 안 본다** — 그것이 이 사고의 모양 그대로다.
- ⚠ 키가 **없는 것**(옛 리포트 — 회차가 아직 안 돌았다)과 **빈 목록**(다 읽었다)은
  다른 말이다(`[247]`). 뭉치면 한쪽이 조용히 사라진다.
### 폰 사본은 대표 캡처를 **못 만든다** — 안 옮긴 것이 아니라 자료가 없다 (2026-08-17)

- 먼저 **내가 이 자리에 잘못 적었던 것부터 고친다**: "폰 고정 배포본의 캘린더는
  **옛 모양**"이라고 적어 뒀는데 실측으로 틀렸다. 옛 모양이 아니라 **대표 캡처가
  아예 없다** — `docs/cal.html` 은 캔버스가 한 줄도 없고(현장 기사용 공유 달력이다),
  `docs/app.html` 은 목록 시트 `이미지 저장`만 있고, `mobile_snapshot.py` 는 화면이
  아니라 **자료 수집기**라 애초에 상관이 없다. `A4 세 장` 이 안 걸렸다는 것만 보고
  '옛 버전이 거기 있다'로 읽은 것이다. **짐작한 근거를 문서에 적으면 다음 사람이
  없는 길을 판다**(이 절이 그 말로 시작하는데 같은 자리에서 또 걸렸다).
- **그리는 코드를 안 옮긴 것이 아니라 그릴 자료가 없다.** `cloud_publish.payload()`
  가 담는 일정 칸은 `날짜·시간·분류·캠프명·장소·제목·프로젝트NO·담당기사·예측`
  아홉뿐이고, 캡처가 쓰는 **지연 사유**(`_why_still_open`)와 **분기 진도**는 한 칸도
  안 실린다. 그러므로 450줄짜리 캔버스만 옮기면 **사유 칸이 빈 채로 멀쩡해 보이는
  3장**이 나온다 — 그것을 대표께 보내면 아무도 빈 것을 눈치채지 못한다.
  `[273]` 이 "자리가 모자라도 뒤 행을 조용히 버리지 않는다"로 막은 그 모양이며,
  **못 만드는 것보다 나쁘다**(`[172]`).
- 그래서 **말하고 보낸다** — `docs/app.html` 캘린더 탭이 *"대표 보고 A4 3장 캡처는
  실시간 PC 앱에서 만듭니다 — 이 사본에는 지연 사유·분기 진도가 실리지 않습니다"* 를
  적고, PC 가 닿으면 `실시간 PC 앱에서 만들기` 단추를 준다.
  · **판정은 `updateMode()` 한 곳**이다(`[162]`) — 캘린더가 제 손으로 `/api/ping` 을
    걸면 같은 질문에 두 답이 생기고, 달을 넘길 때마다 터널을 한 번 더 두드린다.
    그 함수가 확인한 주소만 `LIVE_URL` 에 담기고, 담기는 순간 캘린더를 다시 그린다
    (안 그리면 부팅 순서에 따라 **연결됐는데도 단추가 영영 안 나온다**).
  · 새 창으로 띄운다 — 이 창을 실시간 주소로 옮기지 않는다(`[240]`).
- ⚠ **`docs/cal.html` 에는 아무 말도 안 넣었다.** 그것은 단톡방에 뿌리는 **현장 기사용**
  달력이라 대표 캡처를 만들 사람이 아예 없다. 없는 문제를 만들지 않는다(`[172]`).
- ⚠ 같이 드러난 것: **카톡 원본이 3일 밀렸다**(최신 2026-08-14 11:16, 오늘 8/17).
  수집은 'CSOS 리서치 및 자료 수집' 세션 몫이므로 여기서 긁지 않는다.
- 검증 `[294]`.

## ★ 계기가 **옛 값에 새 시각을 찍고 있었다** — 컨텍스트 감시 (2026-08-19 실사고 · [152])

- 증상: `/compact` 를 돌린 **직후**에 훅이 `170.7% (768,215 / 450,000) · 단계 즉시` 를
  확언했다. 그대로 믿으면 이 세션은 **영영 "새 작업을 시작하지 말 것"** 에 갇힌다.
- ★ **계기가 지어낸 것이 아니다 — 한 박자 늦은 값을 새 값인 척 말한 것이다.**
  `UserPromptSubmit` 은 compact 뒤 **첫 사람 입력** 때 오는데, 그 순간 기록에는 아직
  compact 이후 assistant 응답이 **하나도 없다.** 그래서 `_used_tokens` 가 집는
  '마지막 usage' 는 **경계 이전** 값이다. `[320]`(챗봇이 낡은 발췌에 새 mtime 을 찍은
  사고)과 **같은 모양**이며, 자리만 달랐다.
- **자국은 실재한다 — 짐작할 것이 없었다.** 기록에 `{"type":"system",
  "subtype":"compact_boundary"}` 가 그대로 찍힌다. 실측: 경계 **앞 768,215** ·
  경계 **뒤 447,569**(두 번의 compact 모두 뒤가 44.4만~44.8만).
- ★ **경계 뒤에 응답이 없으면 `0` 이 아니라 `None`(모름)이다.** 0 을 주면 비율이 0 이
  되어 **'여유'** 로 읽힌다 — 반대 방향의 거짓이다(`[169]`). 모름이면 **경보를 아예
  안 낸다**: 옛 값을 확언하지도, 없는 여유를 만들지도 않는다.

### 한도는 설정이 아니라 **관측**이 이긴다
- 실측한 **바닥이 44.5만**이다(지시문 두 벌만으로 시스템 프롬프트가 그만큼이다).
  그런데 한도가 `DEFAULT_LIMIT = 450,000` 이라 **요약 직후에도 99%** — 즉 `즉시`
  경보가 **상시로 켜져 뜻을 잃는다**(`[170]` — 경보가 대부분이면 아무도 안 본다).
  경보의 반대편이 아니라 같은 결과다: **아무도 안 본다.**
- 그리고 이 세션은 **768,215 까지 담기고도 안 터졌다.** 그러므로 450,000 을 한도라
  부르는 것은 **틀린 말**이다. 이제 `관측최대 > 설정한도` 면 관측이 이긴다.
  · ★ **짐작으로 올리지 않는다** — "실제로 담겼다"는 증거만큼만 올린다. 그 위는
    여전히 모르고, 관측치는 자동 요약이 걸리는 지점에서 저절로 멈춘다.
  · ★ **바꿨으면 그 사실을 말한다**(`[169]`) — 경보 문구가 설정값과 관측값을 같이 적는다.
  · **설정이 관측보다 크면 설정이 그대로 이긴다** — 낮추지 않는다.
- ★ **단계 표(70/85/95)는 한 글자도 안 건드렸다** — `[119]` 의 뜻은 그대로다.
  고친 것은 **분모**이지 기준이 아니다. 검증이 그 표를 못 박아 지킨다.

### 같이 드러난 것 — 폴백이 **옆 세션 기록**을 집고 있었다
- `_transcript()` 의 마지막 줄이 `max(files, key=mtime)` 라, 훅이 `session_id` 를 안
  주면(PostToolUse 에서 실제로 비어 온다) **가장 최근에 쓴 아무 파일**을 집는다.
  실측으로 상태 파일에 **남의 세션 id**(`dad51d70…`)가 박혀 있었다 — 옆 창이 더
  활발하면 **내 크기 대신 남의 크기로 경보가 뜬다.** 오류는 안 난다.
- 이제 `CLAUDE_CODE_SESSION_ID` 환경변수도 본다(실측으로 그 값은 살아 있다).
  폴백 자체는 남긴다 — 없애면 그때 0% 가 되어 더 나쁘다.
- 실측(고친 뒤): 집은 파일 **내 세션** · `62.3% (478,376 / 768,215) · 여유`.

### ⚠ 계기 자기시험은 **진짜 사고 모양**으로 재야 한다 (`[272]`)
- 첫 판이 고장(경계 리셋 제거)을 주입하고 **경계 뒤에 값이 있는** 기록으로 쟀다 —
  그러면 리셋이 있든 없든 마지막 값이 같아 **늘 통과하며 아무것도 안 잰다.**
  실제 사고는 **경계 뒤가 비어 있는** 순간이므로 그 모양으로 재야 잡힌다.
- ⚠ 모듈을 통째로 `exec` 할 때는 `__file__`·`__name__` 을 준다. 안 주면 `ROOT` 계산이
  `NameError` 로 죽고, **그 죽음이 '고장을 잡았다'로 오인된다.**
- ⚠ 반환 계약을 바꾸면 **그것을 재던 옛 검사**를 같이 고친다(`[328]` 과 같은 자리) —
  `t119` 가 `_used_tokens(없는파일) == 0` 과 `limit == 450000` 을 못 박고 있었는데,
  **그 두 값이 곧 이번에 고친 고장**이었다. 글자·값을 못 박는 검사는 '되돌아가면 안
  되는 것'에만 쓴다(`[39]`).
- ⚠ **검사가 완료 표시를 안 하면 '통과' 와 '아예 안 돎' 이 구별되지 않는다.** 첫 판이
  `✅` 를 안 찍어 목록에 아예 안 나왔다 — 호출이 빠져도 화면이 똑같다(`[169]` 가
  검사 자신에게 붙은 자리 · `[298]`·`[328]` 과 같은 모양). **새 검사를 만들면 마지막에
  제 번호를 찍는다.** 실측: ✅ 258 → **259**.
- 검증 `[329]` — 실행으로 잰다(`[295]`). 실측 증거
  `reports/컨텍스트_사용량.json` 에는 한 글자도 안 쓴다(`[247]` · 임시 경로로만).

## ★ 자국이 남는 것과 그 회차가 **도는 것**은 다른 말이다 (2026-08-20 실사고)

- 인계 맨 아래가 이렇게 적혀 있었다: *"일일자동대조 — 마지막 회차가 **중단**으로
  끝났다 · **지금 단계: (회차 끝)(실패) 464분째**"*. **한 문장이 「끝났다」와
  「464분째」를 같이 말한다.** 조치는 *"먼저 tasklist 로 앞 회차가 도는지 확인"* 이라,
  사람은 **7.7시간 전에 끝난 회차**를 기다리거나 없는 것을 찾아 나선다(`[172]`·`[325]`).
- 원인은 **갈래 하나가 샌 것**이다. `daily_step_now` 는 `(회차 끝)` 자국에
  `살아있음=None` 을 준다 — 완주한 회차는 pid 가 죽는 것이 정상이라 **판정하지
  않는다**(`[211]`). 그런데 `_step_hint` 가 `is False` 만 걸러 **None 이 「도는 중」
  쪽으로 떨어졌다.** 바로 그 자리의 주석이 막으려던 모양 그대로다.
- ★ **`(회차 끝)` 은 단계 이름이 아니라 표식이다** — 되풀이하지 않는다. 지금은
  `마지막 자국 (실패) 471분 전 — **그 회차는 이미 끝났다**(지금 도는 중이 아니다)`.
- ★ **죽은 회차 갈래·도는 중 갈래는 한 글자도 안 건드렸다**(`[172]`) — 좁히는 것도
  고장이다. 검증이 셋을 각각 잰다.
- 검증 `[338]` — 갈래 넷을 **실행으로** 재고(`[295]`), 갈래를 빼면 옛 문구가
  돌아오는지 계기 자신을 시험한다(`[272]`). 실측 증거 파일은 임시 경로로만(`[247]`).

### 그 한 줄이 **서로 다른 두 회차**를 말하고 있었다 (2026-08-28 실사고)

- 인계가 이렇게 적었다: *"일일자동대조 — **마지막 회차가 중단으로 끝났다** ·
  지금 단계: **Z폴더 원장 누락·금액 공백 스캔**(시작) **22분째**"*.
  위 `[338]` 이 `(회차 끝)` 표식에서 고친 그 모양인데 **자리가 달랐다** —
  여기서는 표식이 아니라 **정말 두 회차**가 한 문장에 섞였다.
- ★ **재고 나서 갈랐다**(`[67]`). `중단` 은 회차가 **끝날 때** 쓰는
  `reports/agent_status.json` 에서 온다(= **앞** 회차). 단계 줄은
  `_step_hint()` 가 읽는 `.daily_run.progress.json`, 곧 **지금 도는** 회차다.
  실측으로 그때 pid 63700 이 살아 60단계를 끝내고 74/150분을 돌고 있었다.
- ★ **조치는 맞았다**(*"새로 띄우지 말고 끝나기를 기다린다"*) — 그래서 `[338]` 보다
  값이 작다. 잘못은 **읽는 사람이 "끝났는데 왜 22분째지"로 읽는 것**이고, 한 문서가
  스스로 어긋나면 사람은 없는 것을 찾아 나선다(`[172]`·`[325]`).
- 고침은 **두 조각이 만나는 자리 한 곳**이다(`blockers()`):
  `**앞** 회차가 **중단**으로 끝났다 — **지금 도는 것은 그다음 회차다**`.
- ★ **도는 회차가 있을 때만** 갈라 말한다(`[172]` — 좁히는 것도 고장이다).
  안 도는 갈래는 예전 그대로 `마지막 회차가 …` 이고, `완주없음`·`N시간째` 갈래는
  **한 글자도 안 건드렸다** — *"한 번도 완주하지 않았다"* 와 *"지금 22분째"* 는
  서로 어긋나지 않는다.
- ⚠ **`_step_hint()` 에 인자를 늘리지 않았다** — 옛 검사가 `def _step_hint():`
  **서명을 글자로 얼려** 두었다(`[219]`). 얼린 글자를 깨는 것보다 **두 조각이
  만나는 자리에서 고치는 것**이 싸다.
- 검증 `[338]` 확장 — 세 갈래와 조치 갈림을 **실행으로** 잰다(`[295]`).
  합성 dict 로는 `blockers()` 를 못 부르므로(`[320]`·`[424]`) **실측 스냅샷을
  읽기만** 하고 `일일대조` 칸 하나만 갈아 끼운다(`[247]`). 계기 자기시험 1/1.
- ⚠ 만들면서 **다시 밟은 것 넷**: heredoc 이 역슬래시를 먹어 문법이 깨졌다(`[471]`) ·
  통글자 `replace` 가 **남의 줄을 같이 지웠다**(`[457]` — `hsrc = …` 가 사라졌다) ·
  같은 줄이 `t478` 에도 있어 **앞엣것**을 집었다(`[309]`) · 검사 안에서
  `_io`·`_os`·`_json` 이 그 함수에 없었다(`[406]`). **앵커는 손으로 짓지 말고
  소스에서 떠 오고, 갈아끼운 뒤에는 `git diff` 로 무엇이 같이 지워졌는지 센다.**

## ★ 긁은 것을 잃지 않는다 — 중간 저장 상한은 **시간**으로 정한다 (2026-08-22 실사고)

- 밴드 수집이 66건 중 **13건까지 갔는데 `saved: 0`** 이었다.  사람이 탭을 옮기자
  `pagehide` 로 죽었고 `saveEvery`(50)를 못 채워 **긁은 것이 통째로 사라졌다.**
  그날 하루 덤프가 **0개**였고 캐시 수확도 0이었다.
- ★ **묘비가 그 숫자를 그대로 남겼다.** 그 탭 콘솔의 `__grabStatus().prevDeath` 가
  `{band, ok:13, total:66, saved:0, why:"pagehide"}` 였다 — 사고 #35 때 넣어 둔
  자국이 이날 원인을 한 번에 갈랐다.  **무인으로 도는 것에 자국을 남기는 값이
  이런 자리에서 돌아온다**(`[228]`).
- **상한을 건수가 아니라 시간으로 읽으면 뜻이 분명하다.** 한 건이 최대 21초
  (iframe 9초 + 본문 12초)이므로 **50 은 최악 17분치**이고 **10 은 3.5분치**다.
  `saveEvery` 기본값을 **50 → 10** 으로 낮췄다.  파일이 7~12개로 나뉘는 것은 값이
  아니다 — `convert_dump` 가 재병합한다(그 자리 주석이 이미 그렇게 적어 뒀다).
- ⚠ **고치면 붙여넣기 파일도 같이 맞춘다**(`[162]`·`[208]`).  그 파일들은
  `grab_posts.js` 를 **통째로 싣기 때문에** 옛 값을 그대로 담고 있다 — 실측으로
  네 개가 전부 `50` 이었다.  `watchdog.heal_stale_pastefiles(False)` 한 줄이면 된다
  (30분 회차를 기다릴 것 없다).  **앱 서버가 내려 주는 `/grab_posts.js` 는 디스크를
  그때그때 읽으므로 저절로 새것**이다 — 두 길이 다르다는 것을 알고 있어야 한다.

### 확장이 붙어도 `document.hidden` 은 못 푼다 — 이날 세 번 재서 확인했다

- 확장이 이 계정에 연결된 상태(`list_connected_browsers` 1대)에서도 **탭을 앞으로
  꺼내지 못했다.**  잰 것 셋: ① `window.focus()` — 막힘 ② `open_application`
  ("Google Chrome") — 배경 모드라 앞으로 안 나옴 ③ **MCP 탭 그룹으로 만든 탭은
  언제나 배경**이다(`tabs_create_mcp`·`navigate` 둘 다 `hidden: true`).
- 그러므로 `[111]` 의 그 한 줄은 지금도 그대로다: **탭을 앞으로 꺼내는 클릭 한 번은
  끝까지 사람 몫이다.**  여기까지 와서 "왜 안 돌지"로 시간 쓰지 말 것.
- ★ **가드를 우회하지 않는다**(2026-08-19 가짜 묘비 — 되돌릴 수 없는 쪽이다).

### ⚠ DevTools 가 페이지를 덮으면 그 탭은 `hidden` 이다

- 이날 크롬이 화면 앞에 있고 밴드 탭이 활성인데도 `document.hidden` 이 **true** 였다.
  스크린샷으로 보니 **DevTools 가 별도 창으로 떠 페이지를 거의 다 덮고** 있었다.
  Windows 크롬은 완전히 덮인 창을 `hidden` 으로 친다(Native Window Occlusion).
- 그래서 **콘솔에 붙여넣는 그 순간이 가장 위험하다** — 사람은 창을 앞에 뒀다고
  여기는데 수집기는 시작을 거절하거나(`__grabStart`) 회차 도중 멈춘다.
- 안내할 때는 **"F12 를 화면 아래쪽에 붙은 상태(도킹)로 열라"** 까지 적는다.
  별도 창으로 띄우면 그 자체가 원인이 된다.
- ⚠ **확언하지 않는다**(`[172]`) — 이날 되보고가 끊긴 원인이 이것 하나라고 못 박을
  근거는 없다.  가르는 법만 준다: 그 탭 콘솔에서 `__grabStatus()` 를 부르면
  `paused`·`ok`·`running` 이 나온다.

## ★ 보호자가 "아직 실패가 아니다"라 적은 것을 장애라 부르지 않는다 (2026-08-18 실사고)

인계 맨 위의 **`[P0] 앱 서버 보호자가 장애를 보고함 — 고정 Funnel 외부경로 지연 감지
— 오탐 방지 재확인`** 에서 시작했다.

- **그 경보는 이미 풀린 것이었다.** 상태 파일은 `state: healthy · funnel_ok: true ·
  consecutive_funnel_failures: 0` 였다. 로그를 보면 `15:45:11 지연 감지` 바로 다음 줄이
  `15:46:13 정상 복귀` 다 — 그리고 15:45:10 은 **내가 앱 서버를 다시 띄운 시각**이다.
- **원인은 판정이 둘이었던 것이다.** `server_guard` 는 **한 번 늦은 것을 죽음으로
  단정하지 않는다**(`FAIL_LIMIT=3` — 세 번 연속일 때만 조치). 그런데 `system_audit` 은
  `state != "healthy"` **한 줄**로 갈라서 **첫 blip 부터 P0** 를 올렸다. 보호자가 스스로
  `오탐 방지 재확인` 이라 적어 둔 바로 그 상태를 감사기가 **장애**라고 부른 것이다.
- **실측이 그 크기를 말한다**: 그날 보호자 로그의 `지연 감지` **37회** 대 실제 재시작·
  실패 **1회**(로그 누적 153회). **경보가 대부분 가짜면 진짜 경보가 묻힌다**(`[170]`).
  앱 서버를 고쳐 다시 띄우기만 해도(`[156]` 이 시킨 그대로) 인계 맨 위가 빨개졌다.
- ★ **판정을 새로 만들지 않았다**(`[162]`) — 한도를 **보호자에게서 읽어 온다**
  (`system_audit.guard_limits()`). 여기 3 이라 적어 두면 보호자가 한도를 바꾼 날
  두 화면이 서로 다른 답을 한다. 검증이 보호자 상수를 바꿔 보고 따라가는지 잰다.
- ★ **내리는 것은 `*-degraded` 셋이 한도 아래일 때뿐이다.** `cooldown`(서버가 죽은 채
  재시작 과열 방지 대기) · `funnel-repairing`(이미 세 번 실패해 재등록 중) ·
  `restart-failed` · `guard-error` 는 **진짜 장애**라 그대로 P0 다. 낱말이 비슷하다고
  같이 내리면 못 잡는 것보다 나쁘다(`[172]`).
- ★ **못 읽으면 조용히 넘기지 않는다**(`[169]`). 한도를 못 읽었거나 연속 횟수 칸이
  비었으면 예전대로 P0 를 올리되 **"재확인 중인지 실패인지 가르지 못했습니다"** 라고
  적는다 — 확인 못 한 것을 이상 없음으로 세지 않는다.
- ⚠ **낱말이 어긋나면 한 건도 안 걸리면서 오류도 안 난다**(`[165]`). 상태 이름
  (`degraded`·`staff-degraded`·`funnel-degraded`)과 연속 실패 칸 이름이 보호자에
  실재하는지를 검증이 **매번 대 본다** — 보호자가 이름을 바꾸면 이 완화가 통째로
  죽으면서 모든 blip 이 다시 P0 가 된다.
- 검증 `[307]` — 실행으로 잰다(`[295]`). 합성 딕셔너리만 쓰고 실측 증거 파일은 한
  글자도 안 건드린다(`[247]`). **계기 자신도 시험한다**(`[272]`) — 상태 표를 비우면
  blip 이 다시 P0 가 되는지 확인한다.

### ⚠ 파이썬으로 파일을 고칠 때 **줄끝을 바꾸지 말 것** (같은 날 실사고)

- `[302]`(도면 별도 창)의 **계기 자기시험**이 빨갛게 떴다 — *"미리보기에 손잡이를
  일부러 달았는데도 검사가 통과했다 — 이 검사는 아무것도 안 재고 있다"*. 그 검사는
  node 로 `index.html` 을 읽어 `return interactive\n      ? ...` 를 갈아 끼워 고장을
  주입하는데, **그 앵커가 안 맞았다.**
- **범인은 내 편집이었다.** `io.open(p, "w", encoding="utf-8")` 는 텍스트 모드라
  `\n` 을 **`os.linesep`(윈도우에서 `\r\n`)** 으로 바꾼다. 그래서 한 줄을 고치려고
  읽고 쓴 것이 **파일 전체 줄끝을 LF→CRLF 로 갈아 버렸다.** 실측: 14:50 까지 LF 였고
  15:45 내 커밋부터 CRLF 21,517줄 — 파일 **일곱 개 · 약 48,000줄**이 그렇게 됐다.
- ★ **파이썬 텍스트 모드는 CRLF 를 읽을 때 `\n` 으로 접어 준다**(universal newlines).
  그래서 **파이썬으로 확인하면 앵커가 멀쩡히 맞는다** — node·git·다른 도구만 안 맞는다.
  같은 파일을 놓고 두 도구가 다른 답을 하는 자리이고, **고친 사람만 모른다.**
- ★ **`.py`·`.md` 는 티가 안 났다** — `core.autocrlf=true` 가 커밋할 때 되돌려 준다.
  그런데 `index.html` 은 git 이 손을 안 대서 **CRLF 가 그대로 커밋됐다**(공백만 바뀐
  43,034줄 차이가 진짜 변경 위에 얹혔다). **한 파일만 보고 '괜찮다'고 하면 안 된다.**
- 그러므로 파일을 고칠 때는 **`newline=""` 을 준다**: `io.open(p, encoding="utf-8",
  newline="")` 로 읽고 같은 인자로 쓴다. 이미 어긋났으면 바이트로 되돌린다
  (`open(p,"rb")` → `b.replace(b"\r\n", b"\n")` → `open(p,"wb")`).
- ★ **정본과 사본 세 벌은 바이트가 같아야 한다**(`ecount/CLAUDE.md` · 루트 둘).
  줄끝만 달라도 다른 파일이다 — 되돌린 뒤 반드시 다시 맞춘다.

### ⚠ 글자 검사가 **고장을 지키고 있었다** — `t162` (같은 날)

- `[305]` 는 워치독 자가치유의 성공 판정을 `js_mt` → `_기준(p)` 로 고친 것이다. 그런데
  `t162` 가 `assert "os.path.getmtime(p) >= js_mt" in heal` 로 **그 `js_mt` 를 글자로
  못 박아** 두고 있었다 — 즉 **고친 순간 검증이 빨개진다.**
- 더 나쁜 것은 그 검사 **바로 위 주석**이 *"문자 그대로 못 박으면 기준을 세분화할 때마다
  검증이 헛되이 깨진다"* 라고 이미 경고해 두고, 다음 줄이 정확히 그것을 한 것이다.
- 지키는 정책은 그대로 뒀다(성공을 **mtime 으로** 확인하고 안 써졌으면 지운다).
  글자만 풀었고, '낡음과 성공이 같은 기준인가' 는 `[305]` 가 따로 지킨다.
- **글자 검사는 '있어야 할 것'이 아니라 '되돌아가면 안 되는 것'에 쓴다**(`[39]`).

### ⚠ 검증번호는 옆 세션과 겹칠 수 있다 (같은 날)

- `[306]` 을 붙였는데 **옆 세션이 같은 번호를 먼저 썼다**(`t306_dead_server_revival…`,
  분담판 `[115]`). 작업을 시작할 때 센 최대값은 305 였고 그 사이에 늘어난 것이다.
  함수 이름이 달라 파이썬은 아무 말도 안 하고 **화면에만 같은 번호가 둘** 찍힌다.
- 그래서 **번호는 붙일 때가 아니라 커밋 직전에 다시 센다.** 이번 것은 `[307]` 로 옮겼다.

## ★ 라이브러리 하나가 깔려서 도구 둘이 조용히 깨졌다 — `lxml` · openpyxl (2026-08-20 실사고)

- 아침 관문이 두 번 빨갛게 죽었는데 **둘 다 코드 변경 때문이 아니었다.** 원인은 하나다 —
  **`lxml 6.1.2` 가 이 기계에 02:06 에 깔렸다.** openpyxl 은 lxml 이 있으면 제 직렬화기
  대신 그것을 쓴다(`openpyxl.LXML == True`). 그 순간 서로 무관해 보이는 두 곳이
  같이 조용해졌다:
  · `findings_sheet` — 새 `<sheet>` 마다 `xmlns:r` 선언이 빠져 **워크북을 통째로 못 읽음**
  · `reorder_rows` — 머리글이 `&#51217;&#49688;ID` 같은 **숫자 문자참조**로 적혀 이름
    대조가 빗나감 → **정렬이 안 되는데 예외도 안 난다**(`[165]` 모양)
- ★ **되돌리는 것 자체가 원래 맞다.** 엑셀이 만든 정본에도 `&amp;` 는 들어온다 —
  그러니 예전 코드는 **캠프명에 `&` 가 있으면 그 행만 조용히 못 찾았다.** lxml 은 없던
  고장을 만든 것이 아니라 **드물게 나던 고장을 매번 나게** 했다.
- 고침은 `reorder_rows._unxml()` **한 곳**이고 머리글·셀·공유문자열 셋이 그것을 거친다(`[162]`).
- ⚠ **`pip install` 은 커밋이 아니지만 코드 동작을 바꾼다.** 아무도 아무것도 안 고친 날
  관문이 빨개지면 **`git log` 보다 먼저 설치 시각을 본다** — 그날 저장소는 아무 말도 안 한다.
- ⚠ **한 원인이 서로 무관해 보이는 실패 둘로 나타날 수 있다.** 처음엔 하나를 '옆 세션
  탓', 다른 하나를 '내 변경 탓'으로 각각 읽었다 — **둘 다 아니었다.**

## ★ 관문이 **서로 먼 세 자리**에서 동시에 죽으면 코드가 아니라 환경이다 (2026-08-20 실사고)

- 아침에 합성검증이 `t8_findings_sheet` 에서 죽었다. 고치자 `t21_reorder` 가 죽었고,
  그것도 고치자 `t206_finance_archive` 가 죽었다. 셋은 코드상 아무 관계가 없다 —
  시트 통합기 · 행 재배치기 · 보관본 생성기다. 그래서 세 세션이 각자 '가려져 있던
  별개 고장'으로 읽고 따로 파고들었다.
- **뿌리는 하나였다: 그날 02:06 에 깔린 `lxml 6.1.2`.** openpyxl 은 lxml 이 있으면
  제 직렬화기 대신 그것을 쓴다(`openpyxl.LXML == True`). 그러면 두 가지가 한꺼번에 바뀐다:
  · 인라인 문자열을 **숫자 문자참조**로 적는다(`&#51217;&#49688;ID` = `접수ID`)
    → 머리글 **이름 대조가 통째로 빗나가** 재배치가 한 줄도 안 옮기고 끝난다(`[165]`).
  · `xmlns:r` 을 **루트가 아니라 `<sheet>` 요소마다** 적는다
    → 문자열로 `<sheet … r:id="rIdN"/>` 를 덧붙이는 코드가 만든 워크북이 **통째로**
      안 읽힌다(`XMLSyntaxError: Namespace prefix r for id on sheet is not defined`).
- **코드는 한 줄도 안 바뀌었는데 관문이 세 곳에서 죽었다.** 그러므로 관문이 서로 먼
  자리에서 같은 날 여러 개 죽으면 **커밋 목록보다 `pip list` 의 설치 시각을 먼저 본다.**
- ★ **XML 오류의 위치 값(column)이 고치기 전과 똑같으면 그 코드는 안 탄 것이다.**
  이날 첫 고침이 선언을 **루트**에 넣는 자리 틀린 것이었는데, 그 한 가지가 짐작을 끊어
  줬다. 실제 XML 을 열어 보고서야 진짜 자리(요소마다)가 나왔다.
- 고친 자리 셋: `reorder_rows._unxml`(f308cef9) · `findings_sheet.upsert`(ea642c3d) ·
  `archive_worker`(1c2f6cd7). **같은 URI 재선언은 XML 상 유효**하므로 엑셀이 만든
  정본(루트에 선언이 있는 파일)에서도 안전하다.
- ⚠ **제일 나쁜 자리는 관문이 아니었다.** `archive_worker` 는 11:00·15:00 **보관본
  회차**라, 앱 저장은 성공하고 그 회차만 조용히 죽는다 — 적은 사람은 모른다.
  관문이 그것을 대신 잡아 준 것은 운이었다.
- ⚠ **어제 만든 `ALL GREEN` 을 오늘의 근거로 쓰지 않는다.** 이날 `/tmp` 에 `ALL GREEN`
  이라 적힌 파일이 있었지만 **어제 15:57**, 곧 lxml 이 깔리기 전 것이었다.
  초록은 **언제 잰 초록인지까지**가 근거다(`[169]`).
- ⚠ **문자열로 OOXML 을 깁는 자리를 새로 만들면 `r:` 접두사를 쓰는 요소에 선언을 같이
  넣는다.** 지금 그 자리는 둘이다(`findings_sheet` · `archive_worker`) —
  `grep -rn 'r:id="rId' --include=*.py` 로 센다.

## ★ 반만 내려온 화면은 **스스로 말한다** (2026-08-18 실사고 · 분담판 [117])

- 2026-08-18 13:09~13:10 류지영 업무센터가 `Uncaught SyntaxError: Unexpected end of
  input @ ryu-jiyeong?v=..:1` 을 **3회** 냈다. **코드 고장이 아니었다** — 인라인
  스크립트 4블록 811,725자를 `node --check` 로 재니 문법 오류 **0**. HTML 이
  **중간에서 끊겨 도착한 것**이다. 같은 시각 `server_guard` 가 '앱 서버 응답 지연'을
  적었고 09:50 회차(12:51 시작)가 Z: 를 훑고 있었다.
- ★ **위험한 자리는 여기다** — 스크립트가 끊기면 앱이 통째로 안 뜨는데 **말하는 코드가
  그 잘린 스크립트 안에 있다.** 사람에게는 반쪽 화면만 남고 아무 설명이 없다(`[169]`).
  담당자는 '앱이 고장 났다'고 읽고, 자료를 의심하러 간다(`[172]`).
- 고침은 **자리**가 전부다: 검사기는 `<head>` **맨 앞 블록**(열 줄짜리라 사실상 언제나
  도착한다) · 표식 `#csosDocEnd` 는 **마지막 `</script>` 뒤 · `</body>` 바로 앞**.
  뒤로 옮기면 정작 필요한 순간에 **같이 잘려 이 기능이 통째로 없어진다.**
  · ⚠ **표식보다 뒤에 아무것도 두지 말 것** — 뒤에 둔 것이 잘려도 '끝까지 왔다'가 된다.
    서버는 `.replace()` 만 하므로(`app_server.py:8827`) 지금은 안전하다.
- ★ **아무것도 안 빌린다** — `toast`·`errorHelp`·CSS 클래스·테마 토큰을 하나도 안 쓴다.
  그것들이 살아 있으면 이 배너가 필요 없고, **필요한 순간에는 그것들이 없다.**
  칠한 바탕 위 흰 글자라 밝은 화면·어두운 화면 어디서나 읽힌다(실측 명암비 6.54).
- ★ **스스로 새로고침하지 않는다 — 물어본다**(`offerUpdate` 와 같은 이유: 입력하던
  중이면 날아간다). 그리고 **자료가 틀린 것이 아니라는 말을 같이 적는다** — 안 적으면
  사람이 멀쩡한 자료를 의심하러 간다. 오류 코드는 `CSOS-LOAD-CUT`(`[346]` 모양).
- ★ **자국을 남긴다**(`[169]`) — `sendBeacon('/api/ux')` 로 한 줄. 몇 번 일어나는지
  모르면 뿌리를 영영 못 잡는다. 실패해도 배너는 그대로다(`try/catch` · await 없음).
- 실측(브라우저로 잼 · 60% 지점에서 자른 사본과 온전한 사본을 같이 띄웠다):
  잘린 문서 → 배너 뜸(맨 위 고정 1265×114 · 단추 94×45) · 온전한 문서 → **배너 없음** ·
  두 번 불려도 하나.
- ⚠ **남은 반쪽은 뿌리다** — 회차가 Z: 를 훑는 동안 앱 서버가 늘어지는 것.
  분담판 `[92]` 와 같은 뿌리일 수 있다. 여기서 한 것은 **보이게 만든 것**뿐이다.
- ⚠ 폰 사본(`docs/app.html`)에는 안 넣었다 — 이 사고는 PC 앱에서 났고, 없는 문제를
  만들지 않는다(`[172]`). 거기서도 나면 그때 같은 모양으로 넣는다.
- 검증 `[374]` — 자리(구조)와 **동작(node 실행)** 을 같이 잰다. 계기 자신도
  시험한다(`[272]`): 표식 검사를 빼면 온전한 문서에도 배너가 떠 그 자리를 짚는다.

## ★ 되돌리다 실패하면 **앱이 옛 글꼴로 남는다** — 그리고 자동 커밋이 원격까지 민다 (2026-08-25 실사고)

- 관문이 이날 두 번 죽었는데, 그 원인이 **담당자 화면과 폰 앱을 다치게 하고 있었다.**
- **사슬이 끝까지 이어졌다**:
  ① 글꼴 왕복 시험(`t126`)은 **진짜 파일 넷**을 바꿨다 되돌린다 — 그것이 요점이다([126]).
  ② 되돌리는 순간 담당자가 그 화면을 열고 있으면 **쓰기가 실패한다.** 실측: 앱이
     `index.html` 을 읽는 동안 쓰면 **25번 중 22번** `[Errno 22] Invalid argument`.
     [369] 뒤로 이 파일은 **요청마다** 디스크에서 읽히므로 부딪히는 창이 늘 열려 있다.
  ③ 그래서 관문이 t126 에서 죽고 네 파일 중 일부가 **나눔고딕으로 남았다.**
  ④ 3시간마다 도는 **폰 사본 갱신 회차가 그것을 쓸어 담아 커밋했다**(`527d5c8`) —
     그리고 **origin/master 까지 갔다.** 즉 **폰 앱이 나눔고딕으로 나가고 있었다.**
  ⑤ **오류가 한 줄도 안 났다.** [126] 이 적어 둔 그대로다 — *"네 곳 중 한 곳을 빠뜨리면
     그 화면만 다른 글꼴로 남고, 화면을 열어 보기 전에는 아무도 모른다."*
- 고침은 **`font_switch._write` 한 곳**이다([162] — `apply`·`sync_presets`·`sync_cards` 가 다 거친다):
  · **물러서며 여섯 번 다시 건다**([171] 과 같은 자리). 실측 **3/25 → 19/25**.
  · **반쪽 파일을 안 만든다** — 임시 파일에 다 쓰고 `os.replace` 로 갈아끼운다.
    `open(..., "w")` 는 **먼저 비우므로** 그 뒤에 실패하면 **빈 화면 파일**이 남는다.
  · 끝내 안 되면 **예외를 올린다**([171]) — 조용히 넘어가면 한쪽만 바뀐 채 남는다.
- ★ **베끼면 새는 자리** — `convert_dump.swap_in` 은 `PermissionError` 만 잡는다.
  실측된 것은 **`EINVAL`(22)** 이라 그 갈래가 아니다. 그대로 베꼈으면 **한 건도 안
  걸리면서 오류도 안 났을 것**이다([165]).
- ★ **`git checkout` 이 안 듣는 자리가 있다** — 깨진 상태가 **HEAD 에 이미 박혀 있으면**
  checkout 은 그 깨진 것을 되돌려 준다. 그때는 `python webapp/font_switch.py --modern`
  으로 네 파일을 **맞춘다.** 가르는 법: `git show HEAD:<파일> | grep -m1 -- "--font-ui:"`.
- ★ **서버는 안 내려도 된다** — `index.html` 은 `restart_server.LIVE_FILES` 라 요청마다
  읽힌다([369]). 브라우저 새로고침이면 반영된다. 여기서 서버를 내리면 담당자에게
  8초 502 다(**항상 류지영 업무가 우선이다**).
- ⚠ **뿌리는 아직 남아 있다** — 자동 커밋이 `git add -A` 로 쓸어 담는 것([104]).
  깨진 상태가 그 길로 **아무 확인 없이 원격까지 간다.** 여기서는 안 고쳤다(분담판 몫).
- ⚠ **실제 파일을 쓰는 검사를 새로 만들면 되돌리기에 물러서기를 같이 넣는다.**
  지금 그런 자리는 `t126` 하나이고 대상은 `font_switch.FILES` 네 개다.

### 그 증상의 **다른 뿌리** — 관문 둘이 겹치는 것 · 그리고 탈출구가 문을 열어 뒀다 (2026-08-26 실사고)

- 위는 **되돌리다 실패한** 자리이고, 같은 증상에 뿌리가 하나 더 있다: **관문 둘이 겹치는 것.**
  `t126` 은 진짜 네 파일을 legacy 로 썼다 되돌리는데([126]), 관문 A 가 legacy 로 쓴 **그 순간**
  관문 B 가 그 파일을 읽어 제 '원본'으로 삼으면 **A 가 되돌려도 B 가 옛 사본으로 도로 덮는다.**
  그러면 자동 커밋이 쓸어 담아 **원격까지 민다**([104]) — 2026-08-26 08:41 실측(커밋 `461f99b` ·
  `docs/app.html`·`docs/cal.html` 둘이 나눔고딕으로 밀려 **폰 앱이 그 글꼴로 나갔다**).
  ★ **물러서기로는 못 막는다** — 쓰기가 실패한 것이 아니라 **성공했는데 옛 원본으로 덮은** 것이다.
- 그래서 `tests/synthetic_check.py` 에 잠금을 뒀다(`reports/.gate.lock`) — 뒤에 온 관문이 물러난다.

#### ★ 그런데 **탈출구가 남을 위해 문을 열어 두고 있었다**
- 급할 때 쓰는 `COUPANG_GATE_FORCE=1` 이 **곧바로 돌아가 잠금을 한 글자도 안 남겼다.**
  그래서 11:00 에 force 로 돈 관문을 **11:18·11:20 관문 둘이 못 보고 올라타 셋이 같이 돌았다.**
  실측: `os.path.isdir("Z:")` **한 번에 155.9초**(평소 0.15초 · 1,000배). 그때 09:50 회차는
  **100분째** 돌던 중이었고, 회차가 예산(150분)을 넘기면 **류지영 매니저 대조가 통째로 빠진다.**
- ★ **force 는 '안 물러난다'는 뜻이지 '안 알린다'는 뜻이 아니다.** 물러나는 것과 알리는 것은
  **다른 두 가지**이고, 탈출구는 앞엣것만 건너뛰어야 한다.
- ★ **남의 살아 있는 잠금은 안 덮는다**(같이 잡았다). 회차·force 가 지나가며 잠금을 제 pid 로
  덮어썼는데, 그러면 내가 끝날 때 `_gate_lock_free()` 가 그것을 지워 **아직 도는 그 관문이
  무방비가 된다** — **지나가는 것**과 **자리를 빼앗는 것**은 다른 말이다.
- ★ **회차가 부른 관문은 여전히 절대 안 막는다**([412]) — 막으면 그날 대조가 통째로 안 돈다.
- ★ **못 읽으면 그냥 돈다**([169]) — 관문을 못 돌리면 실작업 관문이 통째로 없어진다.
  겹침의 값(관문 한 번 늦음)보다 크다. **모를 때 기우는 방향은 자리마다 다르다.**
- ⚠ **Z: 가 굶으면 그 값은 관문 하나가 아니다.** 09:35 원본정리·09:50 대조가 15분 간격인데
  거기에 사람 관문이 겹치면 회차가 예산을 넘긴다. **관문을 force 로 돌리기 전에 회차가
  도는지 본다**(`tasklist` · `reports/.daily_run.progress.json`).
- 검증 `[195]`(`t439`) — 계약 **열하나**를 실행으로 잰다([295]): 물러나나 · **알리나** 둘 다.
  계기 자기시험 둘([272]) — 물러나는 문을 없애면 (3)이 잡히나 · **옛 동작(force 면 곧바로
  돌아간다)을 넣으면 (6)이 잡히나**. 둘 다 잡는 것을 확인했다.
- ⚠ **앵커를 통글자로 적지 않는다**([301]-9 · 이 저장소가 **열 번째** 밟은 자리) — `t439` 가
  파일에서 `_gate_lock_or_yield` 보다 **먼저** 나오므로 `replace` 가 **제 글자를 바꾸고
  바꿨다고 여긴다**. 이어 붙여 만든다. 그리고 `_yield_to_running_round` 에도 같은 force 줄이
  있어 앵커가 **둘**이다 — 함수를 **줄 범위로** 갈아 끼운다.

## ★ 신호에는 담는데 **옮기는 자리가 없었다** — 브라우저 덤프 (2026-08-26 실사고)

형님이 낮에 붙여넣어 받은 덤프가 **몇 시간째 다운로드 폴더에 그대로** 있었다.

- `source_signals` 는 `~/Downloads` 의 `dump_*.json` 을 **밴드 신호에 담는다** —
  그래서 새 덤프가 오면 5분 파이프라인의 밴드 갈래가 **깨어난다**.  그런데 그것을
  Z: 로 옮기는 `download_intake` 단계는 **ERP 갈래에만** 있었다.
- 그러면 회차가 **'봤다'고 지문만 갱신하고** 덤프는 09:50 까지 그대로 남는다.
  실측 2026-08-26: 09:53·10:22 에 받은 덤프 둘이 12:02 회차를 그냥 지나갔고
  `밴드 덤프 흡수` 는 **'신규·변경 0개'** 라고 적었다 — **오류도 안 나고 회차도
  성공이다**(`[169]`).  형님이 물으시기 전까지 아무도 몰랐다.
- ★ **카톡 갈래는 제대로 하고 있었다**(`kakao_apply.py` 에 파일 경로를 직접 넘긴다).
  즉 같은 자리에서 한 갈래만 빠진 것이다 — `[300]`(한 곳에서 배운 것을 다른 곳이
  모른다)의 또 한 판이다.

### 지키는 것
- ★ **맨 앞이어야 한다** — 뒤에 두면 `밴드 덤프 흡수` 가 옮기기 **전의** Z: 를 읽어
  이번 회차도 0개로 끝난다(고친 뜻이 없다).
- ★ **덤프가 있을 때만 붙인다** — 이 단계는 실측 **93초**다(Z: 를 훑어 갈 자리를
  정한다).  Z: 쪽만 바뀐 회차에 매번 달면 그만큼 회차가 길어진다(`[168]`).
  **좁히는 것도 고장**이라 검증이 양쪽을 다 잰다(`[172]`).
- ★ **사람 폴더를 볼지 정하는 자리는 하나다**(`_user_drop_enabled` · `[162]`) —
  신호를 만드는 쪽과 단계를 고르는 쪽이 **서로 다르게 판단하면** 이 사고가 모양만
  바꿔 되풀이된다.  합성 모드에서는 둘 다 사람 폴더를 안 본다.
- ⚠ **새 원천을 신호에 담으면 그것을 실제로 처리하는 단계가 있는지 센다.**
  담기만 하면 회차는 깨어나서 아무것도 안 하고 지문만 갱신한다 — 그 조합이
  이 사고의 정체다.
- 검증 `[442]` — 맨 앞인가 · 없으면 안 붙는가 · 합성 모드를 존중하는가 · 판정이
  한 곳인가.  진짜 다운로드 폴더는 **한 글자도 안 건드린다**(`[247]` — 목으로만).
  계기 자신도 시험한다(`[272]`).

⚠ **같이 드러난 것 — 그리고 내가 처음에 틀리게 읽은 것**(`[67]`).
  상태 파일의 `attempts: 627` 과 `last_success_at: 2026-08-25T11:11` 을 보고
  *"밴드 갈래가 하루 넘게 한 번도 성공 못 했다"* 고 적었는데 **틀렸다** —
  `attempts` 는 **누적 카운터**이고, 이력 30회차에서 밴드 갈래는 **4번만** 돌았다.
  그 4번(11:46·11:51·11:56·12:01)이 전부 `밴드 대조`·`객관완료 판정` 에서
  *관리대장 v*.xlsx 를 찾을 수 없습니다* 로 죽었는데, **그 시각은 09:50 회차가
  Z: 를 물고 있던 때**다.  회차가 끝난 뒤 12:06 회차는 그 단계를 **통과했다**.
  곧 코드 고장이 아니라 **Z: 다툼**이고 스스로 낫는다(`[424]` 자원 실패).
  ★ **누적 카운터를 연속 실패로 읽지 않는다** — 그 한 줄로 멀쩡한 코드를
    지목할 뻔했다(`[172]`).  세는 것은 상태 파일이 아니라 **이력**이다.

## ⚠ 줄바꿈 하나가 호출을 통째로 삼킨다 — 화면 전환 보충 (2026-08-26 실사고)

- 커밋 `9be6f98`(`[432]` 메뉴 건수)에서 **줄바꿈 하나가 빠져** 이 한 줄이 됐다:
  `navCounts();   // 메뉴 건수 — 눌러 보기 전에 안다  queueLiveViewCatchup(v);   // …`
  뒤엣것이 통째로 **주석**이라 그때부터 화면을 옮겨도 **자료 보충이 안 돌았다**(`[377]` 계약).
- ★ **오류도 안 나고 화면도 멀쩡하고 `node --check` 도 통과한다** — 문법이 옳기 때문이다.
  그래서 [247] 을 고치러 그 자리를 읽다가 **우연히** 눈에 걸렸다(`[169]` 의 그 모양).
- 다행히 반쪽은 살아 있었다 — 5초 폴링 쪽 호출(`queueLiveViewCatchup(curView())`)은
  그대로라 아주 죽지는 않았다. 잃은 것은 **화면을 옮긴 그 순간의 보충**이다.
- ★ **교훈: 한 줄에 코드를 끼울 때는 그 줄이 주석으로 끝나는지 본다.** 주석 뒤에 붙이면
  그 코드는 사라지면서 **문법은 통과한다** — 이 저장소가 여러 번 밟은 '조용한' 자리의 새 판이다.
- ★ **재는 자리를 좁힌다**(`[172]`) — '주석 뒤에 호출처럼 생긴 글자' 를 세면 설명 주석이
  전부 걸려 거짓 경보가 된다. `applyView` 안에서 **꼭 불려야 하는 넷**만 본다
  (`renderDataHealth`·`navCounts`·`queueLiveViewCatchup`·`uxEvent`).
- 검증 `[451]` — 줄 주석을 걷어낸 뒤 넷이 남아 있는지 본다. 계기 자신도 시험한다
  (`[272]`): 옛 모양(줄바꿈이 빠진 것)을 만들어 넣어 정말 잡히는지.
- ⚠ `index.html` 을 고쳐도 **서버는 안 내린다**(`[369]` — 요청마다 디스크에서 읽힌다).
  담당자 화면을 끊지 않는다 — **항상 류지영 업무가 우선이다**.

## ★ '죽었다'와 **'PC 가 꺼졌다'** 는 다른 사실이다 (2026-08-28 실사고)

- 일일대조를 손으로 다시 돌렸는데 **exit 4** 로 끝났다. 진행 자국은
  `ERP PDF 사본 만들기` 단계 · **140.5분째 · 60단계 끝** 이고 `(회차 끝)` 표식이
  없다 — 곧 **끝을 못 봤다**. 그런데 배경 작업 출력은 **22바이트**
  (`[exited with code 4]`)뿐이라 물을 데가 없었다.
- ★ **재고 나서 갈랐다**([67]). 이벤트 로그가 그대로 답했다:
  **13:58:13** `StartMenuExperienceHost.exe … has initiated the 전원 끄기 …
  on behalf of user MULDER\hueng` (이벤트 **1074**) → **13:58:43** 이벤트 로그
  서비스 정지(6006) → **13:58:48** 커널 종료 전환(109) → **13:59:22 재부팅**
  (빠른 시작이라 40초 만에 돌아왔다) → 13:59:50 로그 서비스 시작.
  마지막 진행 자국이 **13:54:42** 이므로 **3.5분 뒤 PC 가 꺼진 것**이다.
- ★ **코드 고장이 아니었다.** 그런데 `[140]` 의 자국은 후보로 **워치독 30분 회차 ·
  이름으로 죽이는 자리** 둘만 적는다 — 그대로 따르면 **멀쩡한 워치독을 뒤진다**
  ([172] 틀린 지목이 못 잡는 것보다 나쁘다). 그리고 그 P0 가 인계 맨 위를
  차지해 **진짜 경보를 덮는다**([170]).
- ★ **`[385]`·`[468]` 이 절전에서 배운 것이 여기 안 와 있었다**([300] — 한 곳에서
  배운 것을 다른 곳이 모른다). 다만 **자는 것과 꺼지는 것은 다르다** — 부팅은
  **반드시** 모든 프로세스를 죽이므로, 저쪽은 '완화'이고 여기는 **지목**이다.
- **지어낼 것이 없다** — `Win32_OperatingSystem.LastBootUpTime` 하나다(실측 1.2초).
  재는 자리는 **`system_audit._boot_time` 한 곳**이고([162]) `daily_run` 은
  늦게 들여와 빌리기만 한다(순환 방지).

### 지키는 것
- ★★ **부팅했다는 것만으로 원인이라 단정하지 않는다**([172]). 그 사실이 말해 주는
  것은 '언젠가 PC 가 꺼졌다'까지다 — 코드 고장으로 먼저 죽고 **몇 시간 뒤**
  사람이 PC 를 끈 경우까지 재부팅 탓으로 돌리면 그것이 곧 **틀린 지목**이다.
  그래서 창을 둔다(`REBOOT_WINDOW_MIN` 45분): 가장 긴 단계가 **30분**(단계
  제한시간)이고 종료·부팅에 몇 분이 드니, 종료가 죽인 경우는 마지막 자국에서
  그 안에 들어온다(실측 그날 **3.5분**). **창 밖이면 아무 말도 안 한다**([169]).
- ★ **맨 앞에 세운다**([292]·[325]) — `schedule_watch.traces()` 는 `무엇` 을
  **120자에서 자른다**. 뒤에 붙이면 **정작 원인이 잘려 안 보인다**(옛 문구가
  이미 100자다). 실측 새 문구 **112자**로 안 잘린다.
- ★ **조치가 갈린다**([289]) — 재부팅이면 *"**코드를 뒤지지 않는다** — 다음 예정
  회차가 처음부터 다시 돈다"* 이고, 아니면 **예전 문구 그대로**다(좁히는 것도
  고장이다 · [172]).
- ★ **못 재면 아무 말도 안 한다**([169]) — `_boot_time` 이 `None` 이거나 자국 시각을
  못 읽으면 예전 갈래(`모름`)로 간다. 모름을 '재부팅 아님'으로도 '재부팅'으로도
  치지 않는다.
- ★ **갈래를 `재부팅` 으로 적는다** — 이 칸을 읽는 코드는 없지만(실측), 부팅이
  확인됐는데 `모름` 이라 적혀 있으면 **그 자체가 틀린 말**이다([169]).
- ⚠ **자국을 남길 때만 부른다** — 앞 회차가 완주했으면 이 함수에 오지도 않는다.
  평소 회차는 PowerShell 을 **한 번도 안 부른다**([168]).
- ⚠ **자국은 `+09:00` 이 붙은 값이고 부팅 시각은 로컬 naive 다.** 그냥 비교하면
  `TypeError` 로 죽는다 — [212] 가 시각 모양에서 겪은 그 자리이고, 그때는 인계
  문서가 통째로 안 나왔다.
- ⚠ **락은 저절로 회수된다** — `acquire_run_lock` 이 `pid_started_at` 까지 보므로
  죽은 pid 의 락은 다음 회차가 가져간다. 여기서 손댈 것이 없다.
- 검증 `[479]` — 배선([328]) · 재는 자리 한 곳([162]) · 네 갈래를 **실행으로**
  잰다([295]). 진짜 자국·진행 파일은 한 글자도 안 건드리고 부팅 시각은 목으로
  갈아 끼운다([247]·[211] · `finally` 로 되돌린다 · [371]).
  계기 자신도 시험한다([272]) — 창 문을 없애면 한참 뒤 부팅까지 지목하는지,
  뒤에 붙이면 120자에서 잘리는지.

### ⚠ 배경 작업이 죽으면 **출력이 통째로 사라진다**
- 그 회차의 배경 출력은 **22바이트**였다 — 140분치가 한 글자도 안 남았다.
  파이썬 stdout 은 파이프에 물리면 **블록 버퍼**라 죽을 때 버퍼째 사라진다
  ([427]·[381]·[406] 이 세 번 배운 그것).
- 그러므로 **오래 도는 것을 배경으로 돌릴 때는 `python -u` 를 주거나 파일로
  리다이렉트한다.** 이날 살린 것은 `note_progress` 의 자국 하나뿐이었다 —
  *"죽어도 남는다. 그게 요점이다"*([180]).

### 수집 창이 바뀌었다 — 이름이 아니라 **차선**으로 적는다 (2026-08-28 지시)

사용자 지시: **"옆창에서는 수집하지 말고 이 창에서만 수집해"**

- ★ **재고 나서 고쳤다**([67]). 실측 2026-08-28: 이 창(sid `c3ad3614`)이 이미
  `collect` 차선을 **358분째** 쥐고 있었고 옆 창(`817890ea`)은 `build` 였다 —
  곧 **구조는 이미 그렇게 서 있었다.** 안 쟀으면 멀쩡한 차선을 건드릴 뻔했다([172]).
- **고칠 것은 사람에게 나가는 안내 하나였다.** `COLLECT_SESSION_NAME` 이 아직
  `'CSOS 리서치 및 자료 수집_v1'` 이라 말하는데 형님이 창을 바꾸셨으니 그 이름은
  **낡았다** — 그대로 두면 막힌 사람이 **없는 세션을 찾아간다**([172]).
- ★★ **이름이 아니라 차선으로 적는다.** 세션 이름은 **기계가 못 본다**([313] — 그래서
  2026-08-07 의 이름 규칙이 한 줄도 안 지켜졌다) 그리고 창을 새로 열 때마다 바뀐다.
  상수에 박으면 **반드시 낡고**, 오늘이 그 증거다. 그래서 값은
  **`"collect 차선을 잡은 창"`** — 그것이 곧 `collect_gate` 가 실제로 보는 근거다.
- 창을 옮길 때 사람이 할 일은 두 줄이다 — 받는 창에서
  `python lanes.py --take collect --who claude` · 놓는 창에서 `python lanes.py --free collect`.
- ⚠ **역사 인용은 그대로 둔다**([387]) — 2026-08-19 의 `_v2`, 2026-08-22 의 `_v1` 은
  그때 형님이 하신 말이다. 고치면 **왜 이 문이 생겼는지**를 같이 잃는다([169]).
- ⚠ **`t387`-⑧ 이 `_v1` 을 글자로 못 박고 있었다** — 형님 지시를 따르는 순간 관문이
  빨개진다. 관문은 회차의 **0단계**라 그대로 두면 그날 대조가 통째로 안 돈다.
  **얼릴 것은 계약이지 그때 쓴 이름이 아니다**([39]·[219] — 이 저장소가 네 번째 밟은
  자리다: `[47]` 걸음 스택 · `[92]` 예시 키 · `[353]` 배치 한도 · 여기).
  지금 재는 것은 둘이다 — 안내가 **어느 창인지 말하나** · 그 값을 **실제로 찍나**.
- ⚠ **무인 회차는 그대로 통과한다**([387]) — 워치독·증분·09:50 은 세션 신분이 없어
  이 문에 안 걸린다. 창을 나눈다고 **자동 수집이 멈추면 안 된다**([169] — 그것은
  조용하다: 회차는 '성공'으로 적히고 수집만 0건이 된다).

## ★ 옛 실측 숫자를 **오늘 값처럼** 말하지 않는다 (2026-08-31 실사고)

- 옆 창이 원본 색인을 돌리며 *"실측 **24.8초**짜리라 짧습니다"* 라고 알렸다. 실제로는
  **6분 5초**였다. 그 값은 `[198]` 의 **2026-08-11** 실측이고 **그때 파일이
  147,223개**인데 오늘은 **168,246개**다(캐시도 2.4일 낡아 새로 stat 할 것이 많았다).
- ★ **값이 컸다.** 그 한마디로 *"짧으니 지금 돌리자"* 가 되어 **관문과 겹칠 뻔했다** —
  Z: 다툼이 관문을 **61분**까지 밀어낸 전례가 있다(`[465]`). 곧 숫자 하나가 두 창의
  일 순서를 바꿨다.
- ★ **`[288]`(시각으로 갈라라) · `[320]`(낡은 글에 새 시각) · `[449]`(몇 시간 된 진단)와
  같은 모양인데 자리가 '실측 숫자'다.** 이 저장소에 그 자리가 아직 없었다.
- **실측치에 붙는 조건은 셋이다** — ① **잰 날짜** ② **그때 규모** ③ **캐시 나이**.
  셋 중 하나만 달라도 그 숫자는 오늘 값이 아니다. 인용할 때 **셋을 같이 말하고**
  (*"2026-08-11 · 파일 147,223개 · 캐시 신선 기준 24.8초"*), 그 크기가
  **판단을 바꾸는 자리면 다시 잰다**(`[67]`).
  · ★ **셋째를 2026-09-02 실측이 보여 줬다** — 같은 원본 색인이 그날 **6분 5초**였는데
    그것은 **캐시가 2.4일 낡은** 상태의 값이다(새로 stat 할 것이 많았다). 캐시가
    신선하면 훨씬 짧다. 곧 **같은 날 같은 규모에서도 값이 몇 배로 갈린다** —
    날짜와 규모만 붙이면 아직 모자라다.
- ★ **낡았다고 지우지도 않는다**(`[169]`) — 그때는 참이었고, 무엇이 얼마나 변했는지는
  그 옛 값이 있어야 안다. 지우는 것이 아니라 **날짜를 붙이는 것**이다.
- ★ **그 숫자가 두 창의 순서를 정할 뻔했다 — 그때 무엇으로 갈랐나**(2026-09-02).
  옆 창이 Z: 를 훑는 색인을 시작하고 내 관문이 2~3분 먼저 돌던 자리에서, 어느
  쪽이 물러날지를 **근거 셋**으로 갈랐다:
  · **잃는 값이 다르다** — 색인은 `[406]` 뒤로 진도가 남아 물러나도 **잃는 것이 0**
    이고, 관문은 **이어하기가 없어** 처음부터 다시 25분이다
  · **컨텍스트 여유** — 관문 쪽 창이 87% 라 재시도할 자리가 없었다
  · **먼저 시작한 쪽**
  곧 물러나는 쪽은 **잃는 것이 적은 쪽**이지 늦게 온 쪽이 아니다.
  · ★ **되돌릴 수 있는 순서 문제라 형님께 안 여쭸다**(2026-09-01 지시). 대신
    **근거를 적어 알렸다** — 근거 없이 정하면 그것은 그냥 먼저 잡은 쪽이 이기는 것이다.
  · ⚠ 그날은 **색인이 이미 끝나 겹침이 없었다** — 그래도 근거는 그대로 선다.
    2026-08-28 *"관문 시작·끝을 서로 알린다"* 절의 실전 판이다.
- ⚠ 이 지시문은 실측 숫자로 가득하다. **여기 적힌 초·건수를 오늘 값으로 읽지 말 것** —
  대부분 그 절이 쓰인 날의 값이다.

