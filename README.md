# 이카운트(ECOUNT) ↔ 쿠팡 통합업무 관리대장 대조 시스템

관리대장(원장)에 기록한 **거래명세서·세금계산서·수금** 예상값을, 이카운트 ERP에 **실제로 등록된 전표**와 자동 대조해 누락·불일치를 잡아냅니다. 관리대장의 핵심 목적(접수→…→세금계산서→입금 누락검증)의 마지막 고리를 ERP 실데이터로 닫아줍니다.

---

## 0. 보안 (반드시 먼저 읽기)

- `config/ecount_config.json` 에 **API 인증키(비밀정보)** 가 들어 있습니다.
- 이 파일은 **관리대장 엑셀·카카오톡·메일·깃 저장소에 절대 넣지 마세요.** (`.gitignore`로 이미 제외)
- 인증키는 **관리대장(공유 파일)에 저장하지 않습니다.** 관리대장은 카톡 복붙·스냅샷으로 외부에 나가기 때문입니다.
- 공개 저장소에는 `config/ecount_config.example.json`(키 없는 템플릿)만 올라갑니다. 실제 키가 든 `config/ecount_config.json` 은 절대 커밋되지 않습니다.
- 인증키는 발급 후 1년 유효(만료 30일 전 자동 경고). 만료 시 이카운트에서 [유효기간 연장].

### 최초 설정
```bash
cp config/ecount_config.example.json config/ecount_config.json   # 복사 후 실제 값 입력
```

---

## 1. 최초 1회 설정 (3가지)

### (1) COM_CODE · USER_ID 채우기 — `config/ecount_config.json`
```json
"COM_CODE": "회사코드(이카운트 로그인 시 입력하는 숫자)",
"USER_ID":  "로그인 ID (예: 유현민 계정 ID)"
```
> 인증키만으로는 접속되지 않습니다. 로그인 시 쓰는 **회사코드**와 **아이디**가 필요합니다.

### (2) ★ 허용 IP 등록 (이카운트에서)
이카운트 → **Self-Customizing → 보안관리 → API인증키발급 → [IP등록]** 에서 **이 PC의 공인 IP**를 등록해야 API가 동작합니다. (발급 화면 안내문에 명시되어 있음)
- 내 공인 IP 확인: 브라우저에서 `내 아이피 검색` 또는 아래 셀프테스트 실패 메시지 참고.

### (3) ★ 중요 — 이카운트 OAPI의 조회 API 한계 (2026-07-24 실측 확인)
**이카운트 OAPI에는 판매현황·세금계산서·수금 "조회" API가 존재하지 않습니다.**
- 실서비스 키로 인증·세션·품목조회(7,281건 실데이터)까지 전부 성공했으나, 판매/세금계산서 조회 경로는 전수 탐침 결과 미존재(가짜 경로와 동일한 Not Found 응답).
- GitHub 공개 코드 전수 검색에서도 Save(입력)·품목·재고현황 API만 확인됨 — [API인증현황] 화면의 목록과 일치.
- 존재가 확인된 조회 API: `InventoryBasic/GetBasicProductsList`(품목), `InventoryBalance/GetListInventoryBalanceStatus(+ByLocation)`(재고), `Purchases/GetPurchasesOrderList`.

### 따라서 대조 데이터는 inbox 방식으로 공급합니다 (반자동)
이카운트 화면에서 목록을 엑셀로 내려받아 `inbox/` 폴더에 넣으면 자동 인식·대조합니다.
1. **판매**: 재고 I → 판매현황(조회) → 기간 설정(예: 7/1~7/31) → 엑셀 저장 → 파일명에 `판매` 포함해 `inbox/`에 저장
2. **세금계산서**: 세무 → 전자세금계산서 목록 → 엑셀 저장 → 파일명에 `세금계산서` 포함해 `inbox/`에 저장
3. `python ecount_reconcile.py` 실행 → 자동으로 inbox 파일을 파싱해 67건 대조

파서는 머리글 자동탐지(공급가액/합계금액/거래처/일자/승인번호)라 이카운트 기본 내보내기 형식 그대로 넣으면 됩니다. 합계행은 자동 제외.

---

## 2. 실행

```bash
# 설정/로그인만 점검 (COM_CODE·USER_ID·IP 확인)
python ecount_reconcile.py --selftest

# 이카운트 없이 원장측 준비표만 (지금 바로 사용 가능)
python ecount_reconcile.py --offline

# 정식 대조 (설정 완료 후): 이카운트 조회 → 3층 대조 → 리포트
python ecount_reconcile.py
```
> Python 3.9+ 필요. 외부 라이브러리는 리포트 xlsx 생성용 `openpyxl` 만 사용합니다.

결과물은 `reports/이카운트대조_날짜시각.{md,csv,xlsx}` 로 생성됩니다. **관리대장 원본은 절대 수정하지 않습니다.**

---

## 3. 대조 구조 (원장 ↔ 이카운트 필드 매핑)

| 문서층 | 원장(예상) 위치 | 이카운트(실제) | 판정 |
|---|---|---|---|
| ① 판매/거래명세서 | 06 O/Q(공급가액·합계), M(명세서번호) · 14 | 판매전표 금액·번호 | 일치 / 금액불일치 / 이카운트미등록 / 원장누락 |
| ② 전자세금계산서 | 15 L(발행일)·M(금액)·N(승인번호) · 06 U~X | 세금계산서 승인번호·금액 | 일치 / 승인번호불일치 / 원장발행-EC미확인 / 양측미발행 |
| ③ 수금/입금 | 16 N(입금일)·O(입금액)·P(미수금) · 06 AA/AB | 수금 전표 | (①②와 동일 규칙, receipt 엔드포인트 확정 시 활성) |

**매칭 키**: ① 프로젝트NO(UJ26…)가 이카운트 적요/품목/참조에 있으면 그것으로, 없으면 ② 공급가액+공급일자 근접(허용오차 config).

**현재 상태**: 원장 67개 정산 건 로드 정상. 이카운트 연결 시 각 건이 위 3층에서 자동 판정됩니다.

---

## 3.5 밴드(BAND) 연동 — 작업 게시글 자동 수집·대조

기사들이 밴드에 올리는 작업 게시글을 공식 Open API로 수집해, 관리대장 02(돌발AS)·04(정기점검)의 완료 건과 자동 대조합니다 (사진등록·밴드수정 검증 자동화).

**최초 1회 설정 (클릭만):**
1. https://developers.band.us 접속 → 밴드 계정 로그인 → 앱 등록(이름 자유)
2. Redirect URI에 `http://localhost:8976/callback` 등록
3. 발급된 `client_id`·`client_secret`을 config `band` 절에 입력
4. `python band/band_auth.py` 실행 → 브라우저에서 [동의] 클릭 → 끝

**이후 자동 실행:**
```bash
python band/band_sync.py        # 게시글 증분 수집 (0.7초/페이지, 예의 있는 트래픽)
python band/band_reconcile.py   # 원장 대조 → reports/밴드대조_*.md
```
매칭: 프로젝트NO 1순위 → 캠프명+날짜(±3일) 2순위, 담당기사 일치 시 신뢰도 표기.
토큰·게시글 캐시는 전부 로컬(.gitignore) — 공개 저장소에 올라가지 않습니다.

## 4. 파일 구성

```
ecount/
├─ config/ecount_config.json   # 인증키·회사코드·엔드포인트·대조옵션 (★비밀, git 제외)
├─ ecount_client.py            # Zone→로그인→조회 (표준 urllib, 외부 의존성 0)
├─ ecount_reconcile.py         # 원장 read-only 로드 → 이카운트 대조 → 리포트
├─ reports/                    # 생성 리포트 (git 제외)
├─ .gitignore
└─ README.md
```

---

## 5. 관리대장 반영 (STANDING ORDER 준수)

이 도구는 관리대장 xlsx를 **수정하지 않습니다**(openpyxl 저장이 차트·도형·검증을 손상시키므로). 인수인계 기록은 **v20**에 반영 완료:

- ✅ 2026-07-24: `..._v20.xlsx` 생성 — `19_AI작업인수인계` 62행에 본 시스템 인수인계 행 추가. **zip 단일파트 패치** 방식(sheet XML 1개만 교체)이라 차트·도형·x14 검증 등 나머지 파트는 v19와 바이트 단위 동일함을 검증함. v19 원본은 그대로 보존.
- 참고용 원문 (build 스크립트 재생성 시에도 사용):
  > 2026-07-24 #1 | 이카운트 OAPI 대조 시스템 신설 | 원장 06/15/16 read-only 로드 → 이카운트 판매·세금계산서·수금 실전표와 3층 대조(프로젝트NO 1순위·금액 2순위 매칭) → reports/ 에 md·csv·xlsx 리포트. 인증키는 워크북 밖 config에만 저장(카톡·스냅샷 유출 방지). 조회 method는 계정별 상이하여 config.endpoints로 파라미터화(API매뉴얼로 확정). 마스터 xlsx는 미수정(차트·도형·x14 보존). 후속: receipt 엔드포인트 확정, 결과를 build 파이프라인으로 23_이카운트대조 시트에 적재.
- `18_문서발행업무매뉴얼`·`20_쿠팡통합업무상세매뉴얼`·`12_사용방법`: "ERP 실등록 대조는 ecount 도구로 별도 수행" 절 추가 권장.
```
