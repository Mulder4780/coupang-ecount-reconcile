# 자금흐름 앱 → CSOS 업무센터 연동 (2026-08-14)

UNI Cash Flow 앱의 **자금 요약**을 CSOS 업무센터로 가져오기 위한 창구다.
자금흐름 쪽에서 창구·열쇠를 열어 두었고, **받아서 화면에 붙이는 것이 이쪽 몫**이다.

열쇠는 `config/cashflow.local.json` 에 이미 들어 있다(`config/*.local.json` 은 git 밖).
새로 만들 것도, 어디서 받아 올 것도 없다.

---

## 어디로 가져오나

| | |
|---|---|
| 주소 | `https://uni-cashflow.fly.dev/api/brief` |
| 방식 | `GET` |
| 열쇠 | 요청 헤더 `X-API-Key: <config/cashflow.local.json 의 key>` |

이 열쇠로는 **`/api/brief` 하나만** 열린다. 다른 길은 403 이다.
자금 앱 전체를 여는 열쇠가 아니다 — 그것이 의도다.

```bash
# 잘 되는지 한 줄로 보기 (키는 파일에서 읽는다 — 화면에 찍지 말 것)
python -c "import json,urllib.request; c=json.load(open('config/cashflow.local.json',encoding='utf-8')); r=urllib.request.Request(c['url']+c['path']); r.add_header(c['header'],c['key']); print(urllib.request.urlopen(r,timeout=20).read().decode()[:400])"
```

## 무엇이 오나

```jsonc
{
  "ok": true,
  "data": {
    "as_of": "2026-08-14T13:52:00",   // 자금 앱이 이 답을 만든 시각
    "today": "2026-08-14",            // 자금 앱 기준 오늘
    "last_anchor": "2026-08-13",      // 마지막 '실제 마감'일
    "anchor_age_days": 1,             // ★ 이 숫자가 크면 잔액은 실측이 아니라 추정이다

    "balance": 193277314,             // 지금 잔액 (원)
    "balance_date": "2026-08-14",
    "balance_is_actual": true,        // false 면 아직 마감 전 = 예상값

    "week": {                         // 오늘부터 7일
      "days": 7,
      "inflow": 143566104,
      "outflow": 54042333,
      "low": { "date": "2026-08-18", "expected_close": 166017030 }
    },
    "month_end": { "date": "2026-08-31", "expected_close": 206153125 },

    "shortage": { "date": "2026-09-10", "expected_close": -39357753 },  // 없으면 null
    "below_threshold": { "date": "2026-08-30", "expected_close": 19743364 },
    "risk_threshold": 100000000,      // 이 밑으로 내려가면 주의

    "receivable": { "count": 162, "sum": 13791452126, "overdue": 1804028860 },
    "approvals_waiting": { "count": 0, "sum": 0 },
    "alerts": { "danger": 2, "warn": 0, "info": 0 }   // 개수만 — 문구는 안 준다
  }
}
```

금액은 전부 **원 단위 정수**다. 반올림·천원 단위 변환을 하지 말 것 —
자금 화면과 숫자가 어긋나면 어느 쪽이 맞는지 아무도 판단할 수 없게 된다.

이 열 이름들은 **계약**이다. 자금 앱 화면이 바뀌어도 여기는 그대로 둔다.
(그래서 화면용 `/api/dashboard` 를 그대로 내주지 않고 이 창구를 따로 팠다.)

---

## 받는 쪽이 지킬 것 넷

### ① 밀지 말고 당겨온다
자금 앱(클라우드)은 항상 켜져 있고 **CSOS 앱은 이 PC 라 꺼진다.** 그래서 자금 쪽이
미는 구조로 만들면 PC 가 꺼져 있는 동안 전부 실패하고, 그걸 살리려면 재시도 큐가
필요해진다. 그 큐가 밀리면 **CSOS 화면은 옛 숫자를 멀쩡히 보여 준다.**
당겨오면 그 문제가 통째로 사라진다 — PC 가 깨어날 때 알아서 최신을 가져온다.

10분 간격이면 충분하다. 자금 원장은 그보다 자주 안 바뀐다.

### ② "언제 것인가"를 반드시 화면에 찍는다
`as_of` 를 사람이 읽는 자리에 넣는다 — 예: `자금 현황 (13:52 기준)`.
이게 없으면 사흘 전 숫자가 오늘 숫자 얼굴을 하고 앉아 있게 된다.
**빈 값은 눈에 띄지만 낡은 값은 안 띈다.**

`anchor_age_days` 가 2 이상이면 `(마감 전 추정)` 을 같이 적는다.
그 잔액은 실측이 아니라 계산된 값이다.

### ③ 못 가져왔으면 그렇게 말한다
옛 값을 지우지도, 최신인 척하지도 않는다 — `13:52 기준 (갱신 실패)`.
빈 화면보다 낫고, 조용히 낡는 것보다 훨씬 낫다.

### ④ ★ 기사(tech)에게 보이지 않는다
CSOS 의 기사 링크는 **카톡으로 돌아다닌다.** 이 응답에는 회사 잔액과 미수금이
들어 있다. 반드시 `_auth()` 를 통과한 뒤에만 내려보내고, `/api/tech/*` 계열에는
어떤 형태로도 섞지 않는다.

---

## 거절이 오면

| 코드 | 뜻 | 할 일 |
|---|---|---|
| `401` | 열쇠가 틀렸거나 끊겼다 | 자금흐름 쪽에 새 열쇠를 요청 (`python tools/apikey.py`) |
| `403` | 열쇠는 맞는데 **넘어섰다** | `/api/brief` 를 `GET` 으로만 부르는지 확인 |
| `502`·`timeout` | 클라우드가 자다 깨는 중 | 그냥 다음 회차에 다시. 첫 요청이 머신을 깨운다 |

401 과 403 은 **고칠 자리가 완전히 다르다.** 뭉뚱그려 "연동 실패"로 적지 말 것 —
그러면 없는 오타를 찾아 나서게 된다.

## 열쇠를 다루는 법

- `config/cashflow.local.json` 밖으로 꺼내지 않는다. 코드·로그·엑셀·카톡에 적지 않는다.
- 원문은 자금 앱 DB 에도 없다(해시만). 잃어버리면 새로 만들 뿐 복구는 없다.
- 새 열쇠·끊기는 자금흐름 쪽에서: `python tools/apikey.py --new "..." --scope "/api/brief"` / `--revoke <id>`

문의는 자금흐름 프로젝트(`UNI Cash Flow & Treasury Management`) 쪽 세션으로.
창구 코드는 `app/server.py` 의 `@app.get("/api/brief")` 한 곳이다.
