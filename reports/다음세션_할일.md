# 다음 세션이 이어받을 것 — 2026-08-07 22:xx 작성

**첫 명령은 `python ecount/session_handoff.py --adopt` 하나다.** 그다음 이 파일을 읽는다.

---

# ✅ 밴드 9건 — 사람이 붙여넣었고, **그 번호들엔 글이 없었다** (2026-08-08 21:20 결말)

결말부터: 붙여넣기는 정상 동작했다. 9건이 안 들어온 이유는 **그 번호에 글이 없어서**다.
증거는 `notime` 지문 9건이 **전부 동일**하다는 것 — 직전 화면(2026.08.06 완료 글)
본문이 껍데기에 묻어난 것이다. 시각이 없어 수집기가 버렸고, 그게 옳다(검증 [130]).

- 흡수까지 끝냈다: `download_intake --apply` → `convert_dump`.
  캐시 정상(90610953 5,253글 · 84789192 3,308글). 삭제 확정 4건 기록(84789192).
- **그런데 9건은 아직 목록에 남는다** — 삭제 확정은 **두 회차 연속** 같은 증거가
  있어야 한다(검증 [145]). 오늘이 1회차다. 한 번 더 붙여넣으면 확정돼 사라진다.
  안 해도 다음 수집 회차가 자연히 처리한다.
- ★ 걸린 것 하나: `convert_dump` 의 `os.replace` 가 **PermissionError(WinError 5)** 로
  한 번 죽었다(앱 서버가 캐시 파일을 읽는 중이었던 것으로 보인다). 5MB `.tmp` 가
  남았고 재시도에서 풀렸다. **자주 나면 흡수가 조용히 중단되는 자리다** — 재시도·
  잠금 대기를 넣을 값어치가 있다(아직 안 함).

## 그때 배운 것 — 브라우저 자동화로는 밴드를 못 긁는다

Claude 확장이 조종하는 탭은 **MCP 탭 그룹 안의 백그라운드 탭**이라 `document.hidden`
이 계속 true 였다. 밴드는 숨은 탭에 본문을 안 그리고 `grab_posts.js` 는 그때 시작을
거부한다(옳은 안전장치). `SetForegroundWindow` 로 창을 올리는 것은 Windows 가 막는다
(두 번 실패). **그래서 붙여넣기 파일 방식이 지금도 정답이다** — 사람이 보고 있는
탭에서는 `hidden` 이 아니다. 다음 세션은 이 길로 시간을 쓰지 말 것.

---

# (지난 기록) 밴드 9건 수집이 아직 안 끝났다 (2026-08-08 20:5x)

사용자가 로그인까지 다 해 두었는데도 못 돌렸다. **원인은 로그인이 아니라 탭이
보이지 않는 것이다.** 밴드는 백그라운드 탭에 본문을 안 그리고, 그 상태로 긁으면
멀쩡한 글이 전부 '없는 글'로 잡혀 캐시가 망가진다(2026-08-07 40건 사고). 그래서
`grab_posts.js` 가 스스로 `document.hidden` 이면 시작을 거부한다 — 옳은 안전장치다.

- Claude 확장이 조종하는 탭은 **MCP 탭 그룹 안의 탭**이라 계속 백그라운드였다.
  `SetForegroundWindow` 로 창을 앞으로 끌어오는 것은 Windows 가 막는다(두 번 실패).
- 대상 9건: `3525 3397 3378 3374 3373 2598 2597 2595 2573` (밴드 90610953)
- **가장 확실한 길은 원래대로 사람이 붙여넣는 것이다**:
  `band/수집_붙여넣기_90610953.js` 전체 복사 → 로그인된 밴드 탭 F12 콘솔 → 붙여넣기.
  그 탭은 사람이 보고 있으므로 `hidden` 이 아니다.
- 지금 그 MCP 탭에는 '보이는 순간 자동 시작' 리스너가 걸려 있다. 탭이 살아 있고
  누군가 그 탭을 앞으로 가져오면 그때 돈다(새로고침되면 사라진다 — 실제로 한 번 사라졌다).
- ★ 교훈: **브라우저 자동화로 밴드를 긁으려면 탭이 실제로 보여야 한다.** 이 제약은
  확장·CDP 어느 쪽으로도 못 넘는다. 그래서 붙여넣기 파일 방식이 지금도 정답이다.

---

# ✅ 끝남 — 밴드 댓글 본문 수집 (2026-08-08 지시, 같은 날 구현)

**코드는 다 됐다. 남은 것은 '긁는 것' 하나뿐이며 그건 'CSOS 리서치 및 자료 수집'
세션 몫이다.** 담는 쪽 둘이 같은 모양으로 담는다:
`band/grab_posts.js`(붙여넣기 JS 본문) → `band/convert_dump.py:conv_comments` ·
API `band/band_sync.py:api_comments`. 읽는 쪽은 한 줄도 안 고쳤다
(`band_extract.comment_text` → `cancel_hit` → `cancel_watch.py`). 검증 `[162]`.
- 시각 없는 댓글은 버린다 · 재병합은 댓글을 **합친다**(덮지 않는다) ·
  `comments_full` 로 '반쯤 읽음'을 사각지대로 계속 센다.
- **긁고 난 뒤 확인**: `python cancel_watch.py` — 댓글에서 잡힌 취소가 몇 건 늘었는지,
  `cancel_blind_count()` 가 줄었는지 본다(그게 사각지대의 크기다).

---

## (아래는 그때 남긴 설계 — 근거로 남겨 둔다)

## 왜 — 지금 반쪽이다
접수 취소는 대부분 **댓글**로 온다(실제: 김필우 "통화 완료 했습니다 / 작동 원활함.
**접수 취소 하세요**"). 접수 글은 이미 올라간 뒤라 고칠 자리가 댓글밖에 없다.
그런데 수집기가 댓글 **개수만** 가져온다:

    band/convert_dump.py:309   "comment_count": p.get("comment_count", 0),
    band/band_sync.py:88       "comment_count": it.get("comment_count", 0),

**읽는 쪽은 2026-08-08 에 이미 다 만들어 뒀다** — 캐시에 본문이 들어오는 순간
**한 줄도 안 고치고 동작한다**:
`band_extract.comment_text()` → `cancel_hit()` → `cancel_watch.py`(09:50 회차·대기열).

## 무엇을 고치나 — 담는 쪽 세 곳
1. **붙여넣기 스크립트**(`band/make_oneclick.py` 가 만드는 JS) — 글을 훑을 때
   댓글도 같이 걷어 덤프에 싣는다.
2. **`band/convert_dump.py`** (309행 부근) — 덤프의 댓글을 캐시 글에 담는다.
3. **`band/band_sync.py`** (88행 부근) — API 경로도 같은 모양으로.

## 캐시에 담을 모양 (읽는 쪽이 이미 이 모양을 기대한다)
```json
"comments": [{"author": "김필우", "created_at": 1786153200000,
              "content": "통화 완료 했습니다 작동 원활함. 접수 취소 하세요"}]
```
`comment_text()` 는 `content` 를 읽고 `body` 도 받아 준다. **시각 없는 댓글은 버린다**
(2026-08-07 유령 글 사고와 같은 지문 — 시각이 없으면 믿지 않는다).

## 반드시 지킬 것
- ★ **수집은 'CSOS 리서치 및 자료 수집' 세션 몫이다**(CLAUDE.md). 같은 밴드를 두
  세션이 동시에 긁으면 캐시가 서로 덮어써 오염된다(사고 #27: 유령 22건이 정기점검
  1건을 23건으로 부풀렸다). **코드만 고치고 실행은 그 세션에 넘기거나, 그 세션이
  안 돌고 있음을 확인하고 `ai_claim --take band` 를 잡은 뒤에 돌린다.**
- 다 되면 `python cancel_watch.py` 로 **댓글에서 잡힌 취소가 몇 건 늘었는지** 확인하고,
  `cancel_blind_count()` 가 0 에 가까워지는지 본다(그게 사각지대의 크기다).
- 검증 `[155]` 에 댓글 경로 항목을 더한다.

## ★★ 두 번째로 할 것 — 앱 전체 점검 (2026-08-08 지시)
사용자 지시: **"전체적으로 수정 보완해야할 부분이 있는지 검토해"** — 컨텍스트가 차서
착수 못 했다. **추측으로 하지 말 것.** 이 프로젝트에는 기록이 있다:

1. `python -c "import ledger_db,json;print(json.dumps(ledger_db.ux_summary(),ensure_ascii=False))"`
   — 실제 사용 기록(화면 이동·검색 무결과·오류·느린 화면). CLAUDE.md 가 UI 지시를
   받으면 **여기부터 보라**고 정해 뒀다.
2. `reports/UX개선_제안.md` — 3일마다 12:00 `ux_review.py` 가 ★신규 로 쌓아 둔다.
3. `reports/자동화_후보.md` — 매일 10:00 `auto_scout.py`.
4. 오늘 하루에 드러난 종류의 사고를 같이 훑을 것:
   · **함수 안 import 가 모듈 이름을 가리는 자리** — `parse_qs` 하나 때문에
     `/manifest.json` 이 이틀간 500 이었고 아무도 몰랐다. 다른 라우트에도 있는지 본다.
   · **화면은 200 인데 값이 낡은 자리** — 캐시·stale 경로 전반.
   · **사람 손 입력 칸을 근거로 쓰는 판정** — 계산서 발행율 0.9% 가 그 모양이었다.

## 지금 상태 (2026-08-08 측정)
- 밴드 글 8,561건 중 **댓글이 달렸다고 기록된 글이 1건뿐** — 이 채널이 통째로 어둡다.
- 본문 기준 취소 판정은 48 → 60건으로 이미 늘려 뒀다(띄어쓰기 `접수 취소`를 놓치고 있었다).
- 취소로 읽힌 49건 중 원장이 안 끝낸 8건은 **15:00 회차에 반영 완료**.

---

★ **이 세션의 역할**: 코딩 전용. **밴드·ERP 수집을 하지 않는다** — 'CSOS 리서치 및
자료 수집' 세션이 항상 하고 있다. 모인 자료를 **읽기만** 한다(CLAUDE.md 규칙).

---

## ★ 2026-08-08 끝낸 것 (다시 하지 말 것)
| 무엇 | 검증 |
|---|---|
| 현장 이력 창(달력 칸·목록·날짜 창·상세에서 프로젝트 누르면 밑에서 올라옴) | `[147]` |
| 플로우 담당기사 4인 명시 — `ledger_db.AS_TECHS` 한 곳에서 정한다 | `[142]` |
| 캘린더 캡처 목록 가운데에 사유·진행내용 | `[120]` |
| 입력란 DB 자동완성 — 전 화면 자동(MutationObserver) | `[148]` |
| ~~A5~~ 4:3 캡처 뱀 모양 재배치(마인드맵 삭제) | `[142]` |
| ~~A6~~ AS 기사 4명 전용 화면 + 크롬 강제 + PWA | `[149]` |
| 호칭 규칙 — 사용자를 항상 '형님'으로 (ecount/CLAUDE.md 맨 앞) | — |

★ **기사 링크를 사람에게 전할 때**: `python -c "import sys;sys.path.insert(0,'webapp');
import app_server as A;[print(c['name'], f\"{A.FIXED_LIVE_ENTRY}/t/{s}?k={A.tech_keys()[s]}\")
for s,c in A.AS_TECH_CENTERS.items()]"` — **이 링크는 비밀번호와 같다.** 채팅·커밋·
엑셀에 넣지 말고 본인에게만 보낸다. 새 링크가 필요하면 `config/tech_keys.local.json`
에서 그 사람 줄만 지우면 다음 실행 때 새로 만들어진다(옛 링크는 그 순간 죽는다).

## ★ 2026-08-08 오후에 더 끝낸 것
| 무엇 | 검증 |
|---|---|
| 금액 기준 셋 — ERP 비교 AS·점검 한정 · 발행 근거 세 갈래 · 부가세 ÷1.1 | `[154]` |
| 접수 취소 자동 판정 + 09:50 회차 + 대기열(8건 투입) | `[155]` |
| 사람 인계 사전(김혜진→류지영, 번호 근거) — 밴드·카톡 양쪽 | `[155]` |
| 갱신 속도 — 원장시각 2초 캐시 · stale-while-revalidate · 예열 강제 | `[156]` |
| 옛 코드로 도는 앱 서버 감지 + `restart_server.py` | `[156]` |
| 업무센터 카드에 AS 기사 4명 문 열기 | — |

## ★★★ 수집 세션('CSOS 리서치 및 자료 수집')에 넘길 것 — **밴드 댓글 본문**

**접수 취소는 대부분 댓글로 온다.** 실제 사례:
> 김필우: "통화 완료 했습니다 / 작동 원활함. **접수 취소 하세요**"

그런데 밴드 캐시(`band/cache/*.json`)는 **`comment_count` 숫자만** 담고 본문이 없다.
글 8,561건 중 댓글이 달린 것으로 기록된 글이 1건뿐이다 — 사실상 이 채널이 통째로 어둡다.

- 판정·대조·대기열 투입은 **이미 다 만들어져 있다**(`band_extract.comment_text` →
  `cancel_hit` → `cancel_watch.py`). 캐시에 `comments: [{content: ...}]` 가 들어오는
  순간 **아무것도 안 고쳐도 동작한다.**
- 못 읽어서 놓쳤을 수 있는 글 수는 `cancel_blind_count()` 가 세어 리포트에 적는다 —
  **0건이라고 말하지 않기 위해서다.**
- 수집기에 댓글 본문(작성자·시각·내용)을 캐시에 담는 단계를 넣어 달라고 요청할 것.

## ★★ 다음 세션이 **첫 번째로** 할 것 (2026-08-08, 사용자 "지금 다 진행해")
사용자가 남은 것을 전부 진행하라고 했다. 컨텍스트가 차서 여기서 끊겼을 뿐이다.
**아래를 순서대로, 다른 지시가 없으면 그대로 이어서 한다.**

0. **사람 몫으로 남은 것** — 2026-08-08 에 **근거를 끝까지 쫓아 결론을 냈다.**
   다시 뒤지지 말 것. 아래는 "아직 안 봤다"가 아니라 **"보니 없더라"** 이다.

   · **세금계산서 미발행 190건 · 126,594,000원** — ERP 도 아직 발행 전이다
     (3.오더처리 132 · 4.세금계산서발행대기 21). 표시 문제가 아니라 진짜 미발행이라
     완료로 바꿀 수 없다. 명세서번호가 **있는 54건**이 먼저고, 없는 99건은 명세서부터.
   · **`JS-2606-634`** 송파3Sub-FC · UJ2601032 · 완료 2026-06-12 — 금액 근거가
     **수집된 원본 어디에도 없다.** ERP 색인 supply=0 · 진행상태 `2.메일발송`(오더처리 전),
     원장 명세서합계 없음, 밴드 원본(글 5093, pdf·txt·jpg 73건)에는 '유료·공임'이라고만
     쓰여 있고 **숫자가 없다.** → 견적 확정 또는 ERP 오더처리가 되어야 금액이 생긴다.
   · **`JS-2607-708`** 인천2Sub-FC(고잔동) · UJ2601284 · 완료 2026-07-20 — 같다.
     ERP supply=0 · 진행상태 `확인`, 밴드 원본(글 5336)에 숫자 없음.
   · **`JS-2607-057`** 서초1Sub-Hub · UJ2601211 · 정기점검 · 완료 2026-07-14 —
     이건 성격이 다르다. **금액은 ERP 가 알고 있다(523,000원)**. 없는 것은 거래명세서다
     (번호·발행일 둘 다 빈칸). ERP `3.오더처리` 가 진행되면 저절로 풀린다 —
     '미청구(전표 없음)'가 **맞는 표시**다. 손댈 것이 없다.

   ★ 즉 이 세 건은 **AI 가 못 하는 게 아니라 아직 존재하지 않는 값**이다.
     억지로 채우면 틀린 금액이 화면에서 확정처럼 보인다(그래서 안 채웠다).

| 번호 | 할 일 | 비고 |
|---|---|---|
| ~~#20~~ | 끝남 — `datalake.note()` + `ledger_db.enqueue` 배선 | 커밋 d995e38 |
| ~~#21~~ | 끝남 — `put_record`/`record_rev` + `--band`(밴드 글 7,782건) | 커밋 d995e38 |
| ~~#22~~ | 끝남 — `link_records` + `--flow` mermaid | 커밋 d995e38 |
| #24 | ERP Excel → 앱 DB(datalake) 흡수 + 앱 화면 노출 | **이것만 남았다** |

### 형님 질문(2026-08-08): "밴드 글 수정되면 자동 감지 긁어오는 알고리즘 있어?"
**답: 반쯤 있다. 감지 장치는 있는데 자동으로 돌지 않는다.**
- 있는 것: `band/recheck_plan.py` — 캐시의 `captured_at` 유무로 재수집 대상을 기계가
  뽑는다. 실측 현황 "재수집 전 0" (가진 글은 한 번씩 다시 훑었다).
  그리고 `datalake.put_record` 가 **바뀐 것을 알아본다** — same/changed 판정 +
  어느 칸이 무엇에서 무엇으로 바뀌었는지 `record_rev` 에 남긴다(오늘 들어감).
- 없는 것: **주기적으로 다시 훑는 자동 회차.** 스케줄러에도 `daily_run` 단계에도 없다.
  못 만든 진짜 이유는 글당 5초+ 라 8,500건 한 바퀴가 12시간이고, 밴드는 사람
  로그인이 먼저라(절대규칙 3) 무인 스케줄러가 혼자 못 돈다.
- **제안(사용자 승인 전)**: 전량이 아니라 **최근 N일(예: 14일) 글만** 매일 09:50 에
  재수집 → `put_record` 가 바뀐 것만 `record_rev` 에 남기고 → 바뀐 것이 있으면
  인계 문서 맨 위에 올린다. 하루 수십 건이라 몇 분이면 끝난다.
  ★ 이 일은 **'CSOS 리서치 및 자료 수집' 세션 몫**이다(수집은 그 세션이 맡는다).
  사용자가 "만들어라" 하면 그때 시작한다 — 아직 승인 전이다.

★ **앱이 느리다는 말이 나오면 먼저 프로세스 나이를 본다** (2026-08-08).
`#17`(관리대장 1회 파싱)로 갱신은 콜드 6초·웜 0.4초가 됐지만, **돌고 있는
`app_server.py` 프로세스는 옛 코드를 메모리에 들고 있다.** 실측 그날 20:48 에
뜬 프로세스가 그대로 살아 있었다. 코드를 고쳐도 **다시 띄우기 전에는 아무것도
바뀌지 않는다.**
확인: `Get-CimInstance Win32_Process -Filter "Name like '%python%'"` 에서
`app_server` 의 `CreationDate` 가 마지막 커밋보다 이르면 그것이 원인이다.

- **#18·#19 는 옆 세션이 이미 끝냈다**(검증 `[150]` 전 자료 보관소, `[151]` 미수집 몰이).
  `datalake.py` 는 그 세션이 만든 파일이다 — **먼저 `python worksplit.py` 와
  `python ai_claim.py` 로 지금 누가 datalake 를 잡고 있는지 보고 시작한다.**
  같은 파일을 동시에 고치면 한쪽이 통째로 묻힌다.
- 시작 전에 `--take <번호> --who claude` 로 맡았다고 적을 것.

## A. 사용자가 시켰는데 아직 못 한 것 (우선순위 순)

### ~~A1. 4:3 비율 캡처~~ — **끝남**(2026-08-07, 커밋 49a5d37)
`flowToPng43()` · 머리 카드의 [4:3 캡처]·[복사]·[인쇄]. 꼴은 **마인드맵**이다
(뒤이은 지시: "나뭇가지처럼 뻗어가는 구조"). 넘치면 자르지 않고 줄인다.
실측 1~30단계 넘침 0. 검증 `[142]`.

### ~~A2. 개발자용 플로우차트~~ — **끝남**(2026-08-08, 커밋 8f28710)
막혀 있던 것은 그림이 아니라 자료구조였다. `flow_step` 에 `branch` 열을 넣어
**잇달아 같은 값이면 나란한 갈래**로 본다. 기본 흐름 12 → 15단계(접수 네 갈래).
화면·캡처가 갈래를 묶어 보이고, [개발 사양] 단추가 Mermaid 흐름도 + 단계 표 +
유의사항을 `.md` 로 낸다. 검증 `[142]`.

### A3. 5단계 이후 실제 절차 확인받기
- 지금 앱의 6~12단계(현장 조치 ~ 수금)는 **코드가 추정한 값**이며 메모에 `(확인 전)`
  이라 적혀 있다. 사용자가 "나중에 추가로 명령 내릴거야" 라고 했다.
- **확인 전 값을 사실처럼 다루지 말 것.** 개발자가 그대로 만든다.

### A4. 워크북 6~9회 재파싱 제거 (캡처 1회당 순수 중복 7~9초)
- `openpyxl.load_workbook(master_stream(master), ...)` 호출처 **9곳**:
  `app_server.py` 951·1192·1533·1936·2382·2765·3386·3517, `ecount_reconcile.py:196`
- `master_stream` 은 **바이트만** 캐시한다. 파싱(실측 1.14초)은 매번 다시 한다.
- ★ **워크북 객체를 공유하면 안 된다** — `read_only=True` 워크북은 이터레이터 상태를
  갖고 스레드 안전하지 않다. **시트에서 뽑아낸 데이터**를 (경로,mtime,크기) 키로
  캐시해야 한다. 호출처마다 읽는 시트가 달라 site 별 작업이 필요하다.

### A5. 4:3 캡처를 **뱀 모양**으로 재배치 (worksplit #25)
- 지시(2026-08-08): "중간에서 번호로 뿌려지는 구조 말고 순서대로 치고 치고 나가게 /
  왼쪽에서 오른쪽으로 가서 용지 공간이 없으면 다시 아래로 내려가서 왼쪽으로
  다시 꺾어서 순서대로 가는 구조로 깔끔하게"
- **지금 것(마인드맵)을 버리라는 뜻이다.** 뿌리에서 양쪽으로 뻗는 꼴은 보기엔
  좋지만 1→N 을 눈으로 좇기 어렵다("한눈에 알아보기 힘들여"). 사용자 판단이 맞다.
- 자리: `webapp/index.html` 의 `flowToPng43()`. 지금 구조에서 바꿀 곳은 셋뿐이다.
  ① `sides` 배치 → **행 단위 배치**(한 행에 들어갈 카드 수 = 폭 / 카드폭)
  ② 홀수 행은 왼→오, 짝수 행은 오→왼(그래야 줄 끝에서 바로 이어진다)
  ③ 이음선: 같은 행은 가로 화살표, 행 끝은 **아래로 꺾이는 ㄱ자**
- ★ 갈래(`FORK`)는 그대로 살려야 한다 — 나란한 갈래 넷은 한 칸에 세로로 쌓고
  다음 카드로 하나의 화살표가 나가는 모양이 맞다(합류를 그림이 말해야 한다).
- ★ 자르지 않는다. 행이 넘치면 지금처럼 통째로 줄인다(검증 `[142]`).

### A6. AS 담당자 전용 화면 (worksplit #26)
- 지시(2026-08-08): "업무 센터에 각 AS 담당자 4명도 넣어서 **별도의 비밀번호 없는**
  화면으로 딱 AS 담당자가 할 수 있는 업무만 / 링크 타고 열면 크롬으로 강제로 열어서
  앱을 모바일에 설치할 수 있는 구조"
- **먼저 확인할 것**: 담당자 4명이 누구인가. `STAFF_CENTERS` 와 `config/staff_contacts.local.json`
  에 이미 있는 사람과 대조하고, 없으면 **사용자에게 묻는다**(지어내지 말 것).
- ★ **'비밀번호 없음'은 '누구나'가 아니다.** 지금 `/api/*` 는 전부 PIN·세션을 본다.
  담당자 링크는 **추측 못 할 토큰이 든 URL**(예: `/s/<slug>?k=<랜덤>`)이어야 하고,
  그 토큰으로 열리는 범위는 **그 사람 것만**으로 서버가 좁혀야 한다.
  기존 `staff_slug` 배선(`X-Staff-Slug`·`_actor()`)이 이미 그 모양이니 거기 붙인다.
  민감 자료(통화 메모·정산 금액 전체)는 이 화면에 **올리지 않는다.**
- 크롬 강제·설치 유도: 카톡/밴드 인앱 브라우저는 PWA 설치가 안 된다.
  안드로이드는 `intent://…#Intent;scheme=https;package=com.android.chrome;end`,
  iOS 는 강제 못 하므로 **"사파리로 열기" 안내**만 띄운다(되지 않는 것을 된다고 하지 말 것).
  설치는 `beforeinstallprompt` 를 잡아 두었다가 단추로 띄운다.

---

## B. 사람이 해야 하는 것 (AI 가 못 함)

- **`agent_dispatch` 로그인** — 3일간 24회 연속 실패, 성공 0회.
  `"Not logged in · Please run /login"`, 그 앞 7회는 Codex `usage limit`.
  코드 문제가 아니라 외부 계정 인증이다. **AI 는 자격증명을 입력하지 않는다.**

---

## C. 저절로 확인될 것 (손대지 말 것)

- **`archive_keep` 증분 효과** — 코드는 들어갔다(하드링크 + `.index.json`).
  로컬 SSD 에선 효과가 안 보이고 **Z: SMB 에서만** 난다. 다음 09:50 회차에서 처음
  측정된다. 실측 근거: Z: 하드링크는 크기와 무관(1MB 215ms · 40MB 132ms),
  복사는 크기를 따라 증가(356ms · 679ms).
- **일일자동대조** — 2026-08-07 밤에 수동 실행 중이었다. 끝나면 앱의 에이전트 날짜가
  오늘로 바뀐다. 안 바뀌었으면 `reports/agent_status.json` 시각부터 볼 것.
- **밴드 `밴드_확인시각.json` 자기모순** — '없음확인 5438' 인데 캐시 최대는 5464.
  지금 그 구간은 전부 오염 표시라 실해 없음. **리서치 세션**이 다음 수집 때 확인.

---

## D. 이번 세션에서 끝낸 것 (다시 하지 말 것)

| 무엇 | 검증 |
|---|---|
| SWR 칩 거짓말 수정(나이 하나·실패 표시·시간 단위·중복 호출 차단) | `[140]` |
| 캡처 as-of 정직화(원장 저장 시각, 화면·PNG 동일 `_asOf`) | `[140]` |
| 보고 캡처 재설계 — 탭 열 때 미리 받고 누르면 0ms | `[140]` |
| 긴 글 접기(재서 정함·기능 숨김 금지·인쇄는 펼침) | `[141]` |
| 워크플로우 신설 + 마인드맵 + 길게 눌러 집기 | `[142]` |
| 4:3 캡처 + 머리 카드 + 나뭇가지 꼴(화면·캡처 둘 다) | `[142]` |
| 상세에서 원본 한 번에 열기(밴드·명세서·계산서·전표·PO) | `[143]` |
| 업무흐름 갈래(분기) + 개발 사양 내려받기 | `[142]` |
| 갱신 칩 3중 관문 + 빈 자리 자동 배치 + 사이드바 자체 스크롤 | `[140]` |
| 실제 절차 반영(접수 네 갈래·배정은 합의) | — |
| 사람 지시 즉시 엑셀 반영(`ledger_now`) | `[93]` |
| 한글 글꼴 실사용 교정(맑은고딕 → Noto Sans KR) + 작은 글자 상향 | `[126]` |
| `source_organizer` 자기 시간 상한(스케줄러는 손자 프로세스를 못 죽인다) | 사고 `#29` |
| 관리대장 새 버전 원자적 교체 | `[139]` |
| 15시 중복 스케줄러 삭제(매일 실패하던 원인) | — |
| 컨텍스트 감시 반복 경고 차단(세션 식별 실패) | `[119]` |
| `get_status` 가 캐시 읽기에 락을 잡던 것 제거 | — |

**정정한 것**: '404를 401로 가린다'는 결함이 아니다 — 인증된 요청은 정상 404 를 받고,
미인증에게 경로 존재를 안 알리는 것은 의도된 성질이다.


## ★ PO 대조 '금액 불일치 C' 44건은 오탐이다 (2026-08-08 확인)
- 앱 정산 카드에 붙는 주황 `PO C` 딱지는 po_reconcile 의 **유형C(금액 불일치)** 다.
  실측 51건 중 **44건이 C** — 거의 전부다. 그런데 근거를 보면 `원장공급가액합: 0` 이다.
- 원인은 대조기가 **원장 06시트 공급가액만** 보는 것. 그 칸은 사람 손 입력이라 비어 있고,
  앱 화면은 이미 ERP 금액으로 대신 채워 보여 준다(`금액출처: ERP`). **화면과 대조기가
  서로 다른 금액을 본다.**
- ERP 로 대면 **원 단위까지 맞는다**:
  · PO327948 쿠팡 7,551,500 = ERP 합 7,551,500 (전표 16건 · 전부 4.세금계산서발행대기)
  · PO326238 쿠팡 7,813,000 = ERP 합 7,813,000 (전표 15건)
- 고칠 자리: `po_reconcile.py` 의 원장 공급가액 합산이 비었을 때 **ERP 판매조회
  공급가액(`erp_sales_index`)** 을 같은 우선순위로 쓰게 한다 — `app_server` 가 이미
  쓰는 것과 같은 사다리다(실제작업 → ERP → 명세서). 그래야 진짜 불일치만 남는다.
- **경보가 44/51 이면 그 경보는 아무도 안 본다.** 조용한 사고의 반대편이지만 결과는 같다.

## ★ '작업 완료인데 계산서 미발행'의 정체 (2026-08-08 사용자 질문)
- ERP 진행상태 분포(판매조회 색인 전체): `6.세금계산서발행` 963 · `7.수금완료` 340 ·
  `3.오더처리` 293 · **`4.세금계산서발행대기` 122** · 나머지 소수.
- 화면의 '세금계산서 미발행' 대부분은 이 **4단계**다. PO 도 왔고(PO327948) 금액도 맞고
  명세서도 나갔다 — ERP 에서 발행으로 넘기는 **사람 손(류지영)만** 남았다.
  예: UJ2600012 403,000 · UJ2600021 433,000 · UJ2600022 201,500 (셋 다 PO327948).
- 예외가 섞여 있다: **UJ2600015 는 ERP 색인에 전표 자체가 없다.** 4단계 대기가 아니라
  아직 ERP 에 안 올라간 것 — 화면에서도 이 카드만 금액 뒤에 `ERP 기준` 이 안 붙는다.
  **같은 빨간 딱지 안에 종류가 다른 두 가지가 섞여 있다.**
- 할 값어치: 정산 화면의 '세금계산서 미발행' 을 **'발행 대기(ERP 4단계)'** 와
  **'ERP 전표 없음'** 으로 갈라 준다. 앞은 류지영 몫, 뒤는 전표 등록 몫이라
  **가야 할 사람이 다르다.** 지금은 한 덩어리라 누구에게 넘길지가 화면에 없다.


## ★ 근거로 완료 처리한 것 (2026-08-08 밤) — 11:00 회차에 들어간다
- 대기열 **340건 미반영** 상태로 넘긴다. 11:00 회차가 알아서 넣는다.
  · `청구상태` **286건** — 근거 `ERP 진행상태 7.수금완료` (`billing_status --queue`)
  · `PO번호` **27건** + `PO발행일` 27건 — 근거 `ERP 판매전표 PO 연결` (새 유형E)
- **다음 세션이 확인할 것**: 11:00 뒤 `python ledger_db.py --status` 로 340건이
  들어갔는지, `reports/청구상태_반영.md`·`PO대조_*.md` 와 어긋나지 않는지.
- 아직 사람 몫으로 남은 것:
  · 유형C **15건** — 쿠팡·ERP·원장 세 값이 다 다르다. 금액 확인 필요.
  · 유형A **6건** — 쿠팡 PO 는 받았는데 계산서가 안 끊겼다.
  · 유형B **1건** — 원장 PO번호가 쿠팡 목록에 없다(오기입 의심).
  · `발행 대기(ERP 4단계)` **22건** — 류지영이 ERP 에서 발행으로 넘기면 끝난다.
  · `ERP 전표 없음` **62건** — 전표 등록부터다.
- ★ **커밋 2개가 아직 안 밀렸다**(`79db466` 포함). 푸시가 이 세션에서 막혀
  `master...origin/master [ahead 2]` 다. 다음 세션에서 `git push` 를 먼저 한다.


## ★ 밤새 이어서 한 것 (2026-08-08 밤 → 09 새벽)
- **흡수가 조용히 죽던 자리를 막았다** — `convert_dump.swap_in()`. 윈도우는 읽는 쪽이
  물고 있으면 `os.replace` 가 `WinError 5` 로 죽는다. 물러서며 6번 재시도(실측 3번째
  성공), 끝내 안 되면 `.tmp` 를 **남기고 예외를 올린다**. 검증 `[171]`.
- **유형A 오탐 1건** — `PO336120` 은 ERP 가 `6.세금계산서발행` 이라고 말하는데 금액이
  422,089원 달라 금액 대조가 못 잡았다. 이제 번호로 묻는다. A 6→5 · 발행됨 47→48.
- **유형B 판정 분리** — '오기입 **또는** 기간 밖'을 날짜로 가른다. (PO372936 은 목록
  기간 안이라 여전히 오기입 의심으로 남는다 — 이건 사람이 볼 것.)
- **ERP 색인을 `erp_idx()` 한 곳으로** — 넷이 각자 `build()` 를 불러 Z: 를 네 번 훑다
  560초 제한에 죽었다(검증 `[168]` 과 같은 함정).

### 다음 세션이 먼저 볼 것
1. `git push` — 이 세션 커밋들이 밀렸는지 확인(`git status -sb`).
2. **11:00 회차 뒤** 대기열 340건이 실제로 반영됐는지 — `python ledger_db.py --status`.
   큐에 넣은 것과 반영된 것은 다른 이야기다.
3. 앱 전체 점검(아직 안 함) — UX 기록의 `/api/status` 가 남은 가장 느린 화면이다.
   `/api/exec_report` 는 오늘 고쳤다(락 밖 캐시 조회 + 예열).
4. 사람 몫: 유형C 15 · 유형A 5 · 유형B 1 · 발행대기 22(류지영) · ERP 전표없음 62.


## ★ 2026-08-09 오전 상태 — daily_run 은 살아 있다 (죽은 게 아니었다)
- 인계 문서가 "일일자동대조 22시간째 완주 안 함"을 올렸는데, **오늘 09:50 회차는
  정상 진행 중**이다. 10:39 실측 pid 104932 · 경과 0:49 · 그때 자식은 `band/ingest.py`
  (2분 전 시작). **느린 것이지 멈춘 것이 아니다.**
  · 마지막으로 **완주**한 종합리포트는 `종합리포트_20260808_0138.md` 다.
    즉 8/8 09:50 회차가 완주하지 못했고 그 경보가 남아 있었던 것.
  · **죽이지 말 것.** 진행 중인 회차를 끊으면 그날 대조가 통째로 빈다.
    판정은 항상 ① 잠금 pid 생존 ② **자식 프로세스가 최근에 바뀌었나** 둘로 한다.
    자식이 오래 그대로면 그때가 진짜 매달린 것이다(`_run_once` 주석의 13시간 30분 사고).
- **오늘 회차에 들어간 것 / 안 들어간 것**
  · `typo_watch.py` — 어제 밤에 배선했으므로 **오늘 09:50 회차에 들어 있다.**
  · `cross_signal.py` — 오늘 10:3x 에 배선했으므로 **내일 회차부터** 돈다.
    지금 결과가 필요하면 `python cross_signal.py` 를 손으로 한 번 돌린다(읽기 전용).

## 다음 세션이 이어받을 것 (2026-08-09 기준)
1. **`git push`** — `ahead 6`. 옆 세션 때문에 wrapup 이 푸시를 보류했다.
2. **11:00 회차 뒤** 대기열 340건(청구상태 286 · PO번호 27 · PO발행일 27) 반영 확인 —
   `python ledger_db.py --status`.
3. **밴드 댓글 수집** — 수집 세션이 진행 중. 8,107건이 들어오면
   `python cancel_watch.py --queue` 와 `python cross_signal.py` 를 다시 돌려
   **댓글에만 달린 취소**가 얼마나 나오는지 본다(지금은 본문만 보고 있어 반쪽이다).
4. **앱 전체 점검(아직 못 함)** — UX 기록의 `/api/status` 가 남은 가장 느린 화면.
   `/api/exec_report` 는 8/8 에 고쳤다(락 밖 캐시 조회 + 예열, 검증 [156]).
5. 사람 몫: PO 유형C 15 · A 5 · B 1 · 발행대기 22(류지영) · ERP 전표없음 62 ·
   오기입 의심 1건(`송파1MB(감일동)` → `송파5MB(감일동)`).


## ★ '32시간 미완주'의 정체 — 구조가 셋 잘못돼 있었다 (2026-08-09)
사용자 지시: "32시간째 미완주 왜그런거야 해결해 … 다시는 안생기게"

- **느린 게 아니었다.** 실측으로 잡은 것 셋:
  ① **어디서 멈췄는지 기록이 안 남았다** — 종합리포트는 `finish()` 가 맨 끝에 한 번
     쓴다. 완주 못 하면 **한 줄도 안 남는다.** 화면은 '32시간째'만 말하고 이유를 못 댔다.
  ② **한 단계가 회차를 통째로 먹었다** — `collect_all.py` 가 `timeout=3600`.
     09:50 회차가 2시간째일 때도 계속 그 단계였고 **뒤 단계는 시작조차 못 했다.**
  ③ **시간 초과를 재시도해 비용이 두 배** — 1시간 만에 초과로 죽고 **또 한 시간**을
     쓰는 중이었다. 회차 예산을 한 단계가 두 번 먹는다.
  그리고 안 끝나는 회차가 잠금을 쥐면 다음 회차는 '이미 실행 중'으로 조용히 건너뛰고
  **스케줄러는 '성공'이라 적는다.** 이틀 쌓이면 32시간.
- **넣은 것 넷** (검증 `[176]`)
  · `note_progress()` — 단계마다 `reports/.daily_run.progress.json`. 죽어도 남는다.
  · **회차 예산 150분**(`COUPANG_ROUND_BUDGET_MIN`) — 넘으면 남은 단계를 건너뛰고
    **완주시킨다**. 완주해야 리포트가 써지고 잠금이 풀리고 다음 회차가 돈다.
  · **한 단계 상한 = 예산의 1/5**(1800s). 3600s 2곳·2400s 4곳을 내렸다.
    검증이 이걸 **강제**하므로 앞으로 큰 상한을 넣으면 합성검증에서 막힌다.
  · **시간 초과는 재시도 안 함.** 재시도는 경합용이다(금방 실패하고 쉬면 지나간다).
  · `session_handoff.daily_step_now()` 가 자국을 읽어 **경보에 단계 이름을 붙인다.**
- **2026-08-09 12:26 현재 09:50 회차는 아직 옛 코드로 돌고 있다**(2시간 36분째).
  **죽이지 말 것** — 진행 중인 회차를 끊으면 그날 대조가 통째로 빈다.
  새 규칙은 **다음 회차부터** 적용된다.

## ★ 11:00 회차가 반영을 마쳤다 (2026-08-09)
- **923셀 · ok=1 · 미반영 0건.** 청구상태 286 · PO번호/발행일 81+81 · 사진등록 468 등.
- 그 전 10:42 강제 반영은 **실패**했다(`ok=0`) — 09:50 회차가 원장을 잡고 있었다.
  **돌고 있는 회차와 동시에 원장을 쓰면 한쪽이 통째로 묻힌다.** 강제라도 잠금은 못 뚫는다.

## 검증 번호 주의 — 옆 세션과 겹쳤다
- 옆 세션이 `[173]`(분류 캐시 지문)·`[174]`(ERP원장대조 열쇠)를 먼저 썼다.
- 내 것을 **`[175]`(댓글·카톡 교차) · `[176]`(회차 완주)** 으로 비켰다.
- 새 검증을 만들 때 **`grep -n "^def t1" tests/synthetic_check.py | tail`** 로 먼저 확인할 것.


## ★ 앱이 스스로 답한다 — `local_ai.py` (2026-08-09 지시)
사용자 지시: "클로드 코드 또는 코덱스 없이 앱 자체적으로 처리할 수 있는 AI 알고리즘
기능 넣어서 클로드코드 크래딧 사용 최대한 아낄 수 있게 설계하고 알고리즘에 반영해"

- **샌 자리는 계산이 아니라 왕복이었다.** 사람이 이상한 숫자를 보고 앱이 이유를 못
  말해서 클로드에게 묻고, 클로드는 **이미 디스크에 있는 사실**을 매번 다시 조립했다.
- `python local_ai.py "질문"` · 앱 대시보드 **[앱에게 물어보기]** 카드 · `/api/ask`.
- 실측: **8건 중 7건(87.5%)을 앱이 답함 · 평균 10.4ms.** 언어모델 안 부른다.
- 못 답하면 **지어내지 않고** 클로드에 붙여넣을 문구를 만든다(앱이 확인한 수치 동봉).
- ★ 만들면서 두 번 걸렸다 — 둘 다 '조용한' 종류였다:
  · **자가점검이 눈이 멀어 있었다.** "터지지 않으면 통과"라서 이 프로젝트에서 제일
    자주 온 질문("왜 계산서 발행이 안된거지")이 **모름으로 떨어졌는데도 '모두 통과'**.
    말투 정규식이 부정어 순서를 강요했다(안/못이 '발행' **뒤에** 온다).
    이제 `PROBES` 가 **기대 갈래**를 적고 거기로 안 가면 실패라고 말한다.
  · **ERP 색인 칸 이름이 영문이었다**(`state`/`supply`/`cust`/`po`, 알맹이는 `index`
    아래). 한글로만 물어서 **모든 번호가 '상태 없음'** 으로 나올 뻔했다 — 오류는 안 난다.
- 다음에 만들 규칙은 추측하지 말고 `python local_ai.py --stats` 의
  '아직 규칙이 없는 갈래'를 볼 것. 근거·갈래를 늘리면 `SOURCES`·`INTENTS`·**`PROBES` 셋 다** 고친다.
- 검증 `[181]`.


## ★★ 다음 세션이 이어받을 것 — 대시보드 명령 콘솔 (2026-08-09 지시, 미완)
사용자 지시: **"대시보드에 내가 앱에 텍스트로 명령하면 클로드 코드처럼 조사하고
정리하고 답변주고 구현하는 기능을 앱에 탑재해서 클로드 코드 안 거치고 대시보드에서
처리할 수 있는 기능 코딩해 (PC가 꺼져있어도 휴대폰으로 들고다니면서 업무처리할 수
있게 고도화 작업 진행해)"**

### 오늘까지 된 것 (커밋됨)
- `local_ai.py` — 규칙 기반 답변기. 9갈래 · 실측 87.5% 자답 · 평균 10.4ms. 검증 `[181]`.
- `/api/ask` + 대시보드 [앱에게 물어보기] 카드 (PC 켜져 있을 때).
- `local_ai.answer_pack()` — **PC 없이 쓸 답 꾸러미**. 미리 만든 답 7 + 번호 2,014건 +
  분류 말투. 압축 **53KB** · 만드는 데 **0.2초**. `python local_ai.py --pack`.

### 아직 안 된 것 — 여기서부터 시작한다
1. **`cloud_publish.payload()` 에 `d["ask"] = local_ai.answer_pack()` 한 줄.**
   이미 `docs/app.html` + 잠긴 `data.enc` 로 **PC 독립 앱**이 돌고 있다(GitHub Pages).
   꾸러미만 실으면 폰이 PC 없이 답한다. 53KB 라 사본 크기에 부담 없다.
2. **`docs/app.html` 에 같은 카드.** JS 는 `DATA.ask.규칙` 의 말투로 분류하고
   `DATA.ask.답`·`DATA.ask.번호` 를 **읽기만** 한다.
   ★ **규칙을 JS 로 옮겨 적지 말 것.** 같은 판단을 두 곳에서 하면 언젠가 갈리고
     갈린 뒤에는 어느 쪽이 맞는지 아무도 모른다. 그래서 답을 미리 만들어 실었다.
   ★ **`DATA.ask.만든때` 를 반드시 화면에 같이 띄운다.** 꾸러미는 PC 가 꺼진 동안
     낡는다. 낡은 답을 시각 없이 보여 주는 것이 여기서 제일 위험하다.
3. **'조사·정리' 를 넓힌다** — 지금은 미리 만든 9갈래 답뿐이다. 폰 사본에는 이미
   `codes`(프로젝트) · `unbilled` · 브리핑 목록들이 들어 있으니, **그 위를 훑는
   질의**를 붙인다: 캠프별·기간별·상태별 세기/합계/목록. 이것은 미리 만들 수 없으므로
   **JS 질의기**가 필요하다 — 다만 판정이 아니라 **집계**만 시킨다(판정은 파이썬 몫).
4. **'구현·처리' 는 예약이다.** 폰은 원장을 직접 못 쓴다(그래야 맞다). 이미 있는
   **입력 예약 → PC 켜지면 큐 전송** 길(`cloud_queue`)에 명령 결과를 얹는다.
   ★ 쓰는 길은 넓히지 않는다 — 근거 없는 값을 예약하면 **빈 칸보다 나쁘다**(`[170]`).
     예약은 사람이 확인창에서 승인한 것만.

### 정직하게 적어 두는 한계
- **"클로드 코드처럼 코드를 짜는" 부분은 모델 없이 안 된다.** 지금 설계가 대신하는 것은
  **조사·정리·답변·예약**이다 — 실제로 물어 온 것의 대부분이 그것이었다(실측 87.5%).
  코드 변경은 여전히 클로드/코덱스 몫이고, 그때 `--stats` 의 '아직 규칙이 없는 갈래'가
  무엇을 만들지 알려 준다.
- 이걸 "AI 가 다 한다"고 적으면 안 된다. **못 하는 것을 못 한다고 말하는 것**이
  이 답변기의 첫째 규칙이다.

### 시작 명령
    python local_ai.py --pack        # 꾸러미 확인
    grep -n "d\[.unbilled.\]" cloud_publish.py   # payload() 끼울 자리
