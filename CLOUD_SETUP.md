# 클라우드 상시 운영 — PC가 꺼져도 폰에서 보기

## 왜 이렇게 하나

관리대장 엑셀 원본은 **회사 Z: 드라이브**에 있고 매일 사람이 직접 입력한다.
그걸 클라우드로 옮기면 원본이 둘이 되어 어긋난다. 그래서:

```
사내 PC ──(하루 1회, 09:50 자동 PUSH)──▶ 클라우드 서버 ──▶ 폰 (24시간 조회)
   ▲                                          │
   └──────(폰에서 넣은 입력을 큐로 회수해 반영)──┘
```

* 클라우드에는 **화면에 쓰는 값만** 담긴 JSON 한 덩어리(약 720KB)가 올라간다.
* 엑셀 파일·이카운트 접속키·밴드 원문은 **올라가지 않는다**.
* 폰에서 조회는 전부 되고, **대조 실행·엑셀 수정은 사내 PC가 한다**(원본이 하나여야 하므로).
* 폰에서 넣은 값은 큐에 쌓였다가 PC가 다음 실행 때 관리대장에 반영한다.

---

## 1. 서버 하나 준비 (사용자가 직접)

계정 생성과 결제는 직접 하셔야 한다. 추천 순서:

| 후보 | 월 비용 | 장점 | 단점 |
|---|---|---|---|
| **Cloudtype** (국내) | 무료~1만원대 | 한국어 콘솔, GitHub 연결만 하면 배포, HTTPS 자동 | 무료 등급은 유휴 시 잠들 수 있음 |
| Oracle Cloud 프리티어 | 0원 | 상시 무료, 사양 넉넉 | 초기 설정이 번거롭고 가입이 까다로움 |
| AWS Lightsail | 약 7천원 | 안정적, 서울 리전 | 콘솔이 영어, 직접 관리 필요 |

어디를 쓰든 필요한 것은 **Python 3.10+ 와 포트 하나**뿐이다(외부 라이브러리 없음).

## 2. 서버에 올릴 것

```
webapp/cloud_server.py
webapp/index.html
```
두 파일만 있으면 된다. 저장소를 그대로 연결해도 되고, 두 파일만 복사해도 된다.

## 3. 환경변수 3개

| 이름 | 값 | 설명 |
|---|---|---|
| `CSOS_PIN` | 4자리 숫자 | 폰에서 접속할 때 입력. **사내 PIN과 다르게** 정할 것 |
| `CSOS_TOKEN` | 32자 이상 임의 문자열 | PC↔클라우드 전용. 사람이 입력할 일 없음 |
| `PORT` | 플랫폼이 주는 값 | 보통 자동 설정됨 |

토큰 만들기(사내 PC에서):
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

실행 명령:
```bash
python webapp/cloud_server.py
```

## 4. 사내 PC 설정

`config/cloud.json` 을 만든다(이 파일은 커밋되지 않는다 — .gitignore 처리됨):

```json
{"url": "https://내주소/api/push", "token": "3번에서 만든 토큰"}
```

수동 확인:
```bash
python cloud_export.py --upload
```

`daily_run.py` 마지막 단계에 자동으로 붙어 있으므로, 이후로는 매일 09:50 대조가
끝나면 알아서 올라간다.

## 5. 폰에서

배포 주소를 열고 `CSOS_PIN` 입력 → 홈 화면에 추가.
주소가 고정이라 기존 터널처럼 매번 바뀌지 않는다.

---

## 보안 메모

* 금액을 포함한 전체 자료가 올라간다(사용자 선택). **PIN을 반드시 사내와 다르게** 두고,
  6개월에 한 번 정도 바꾸는 것을 권한다.
* PIN 5회 실패 시 해당 IP는 10분 잠긴다.
* 검색엔진 수집을 막는 헤더(`X-Robots-Tag: noindex`)를 넣어 두었다.
* 이카운트 인증키(`config/ecount_config.json`)는 클라우드에 **올리지 않는다**.
* 토큰이 새면 남이 데이터를 덮어쓸 수 있다 — 채팅·메일로 보내지 말 것.
