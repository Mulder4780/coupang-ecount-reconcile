# 업무시작.bat — 부팅하면 업무 페이지가 한 번에 열린다

사용자 지시(2026-07-28): "지금 크롬에 열려 있는 페이지 전체가 시작 시 자동으로
열리게 하고, 로그인까지 그대로 해서 한 번에 업무 가능하게."

## 여는 것

| 순서 | 주소 | 왜 |
|---|---|---|
| 1 | `http://localhost:8899` | CSOS **PC 앱**(전체 기능·실행·전표) |
| 2 | `…github.io/…/app.html` | 고정 주소 앱(PC가 꺼져도 열리는 사본) |
| 3 | `login.ecount.com/Login/` | 이카운트 ERP |
| 4 | `band.us/band/90610953` | 쿠팡AS 밴드 |
| 5 | `band.us/band/84789192` | 매출처업무 밴드 |

## 왜 크롬 '시작 페이지' 설정을 안 썼나

그 설정을 프로그램으로 바꾸려면 **크롬 정책을 등록**해야 하고, 그러면 브라우저에
"조직에서 관리 대상" 배너가 붙고 사용자가 그 설정을 직접 못 바꾸게 된다.
배치는 그런 흔적을 남기지 않고, **시작프로그램 바로가기만 지우면 원래대로** 돌아간다.

## ★ 앱 서버를 먼저 기다린다

Windows 시작프로그램은 **실행 순서를 보장하지 않는다.** 앱 서버(`앱서버실행.bat`)가
아직 안 떴는데 크롬이 먼저 열리면 `localhost:8899` 가 "연결할 수 없음"으로 뜬다.
그래서 8899 포트가 응답할 때까지 최대 90초 기다린 뒤에 크롬을 연다.

## ★ .bat 은 ASCII 주석만 쓴다

한글 주석을 넣으면 **cmd 가 주석을 명령으로 읽고 엉뚱한 오류를 뱉는다.**
CP949 에서 한글의 두 번째 바이트에 `0x5C`(`\`)가 섞이는 경우가 있어서다.
UTF-8·CP949 둘 다 시도해 봤고 둘 다 깨졌다(2026-07-28). 설명은 이 파일에 둔다.

## 로그인은 어떻게 유지되나

- **PIN 은 주소(origin)별로** 브라우저에 저장된다.
  · PC 앱 `localhost:8899` → `cw_pin` — 주소가 고정이라 **한 번 넣으면 계속 유지**
  · 고정 주소 앱 → `csos_pin` — 이미 저장돼 있어 바로 열린다
  · **터널 주소는 띄울 때마다 바뀌므로 매번 다시 로그인해야 한다** → PC 에서는 쓰지 않는다
- 이카운트 비밀번호는 **이 배치가 다루지 않는다.** 크롬의 비밀번호 저장 기능을 쓰면
  로그인 화면에서 자동 입력된다. 비밀번호를 파일에 적지 말 것(절대규칙 1).
- 밴드는 크롬 세션 쿠키로 유지된다.

처음 한 번은 `localhost:8899` 에서 PIN 을 넣어야 한다(그동안 터널 주소로만 접속했다면
그 주소의 저장분은 localhost 에 적용되지 않는다). 이후로는 자동이다.

## 되돌리려면

`시작프로그램` 폴더의 `쿠팡업무_업무시작.lnk` 를 지우면 된다.
바탕화면 `Application\쿠팡 관리\업무 시작 (전체 열기).lnk` 는 수동 실행용이라 남겨도 된다.
