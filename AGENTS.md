# AGENTS.md — AI 작업자 승계 문서 (Claude · Codex · 기타 AI 공용)

> 이 저장소에서 작업하는 모든 AI는 **이 파일을 먼저 읽고 시작할 것.**
> 관리대장 엑셀의 `19_AI작업인수인계` 시트가 원본 인수인계 대장이며, 이 파일은 코드 저장소 측 요약이다.
> **사고 기록**: `INCIDENTS.md` 에 '무엇을 어떻게 틀렸나'가 증상→원인→조치→검증번호로 정리돼 있다.
> 비슷한 증상이 보이면 **거기부터 본다** — 오늘만 같은 함정에 여러 번 빠졌다.
> **교대 규칙**: Claude Code ↔ Codex는 크레딧 소진 시 서로 이어받는다. 추가 절차 없음 —
> 시작 체크리스트(이 파일 → 19시트 → 합성검증 ALL GREEN → workbench --status)가 곧 인수인계다.
> 종료 시: 이 파일 갱신 + `ledger_db.py --handoff --b "제목" --c "상세"`로 19시트 기록 예약
> + 비밀 스캔 후 commit·push. 19시트 실제 Excel 쓰기도 11:00·15:00 회차에서만 한다.
> 진실의 원천은 파일(이 문서·19시트·git)이다 — 특정 AI의 대화기록·메모리에 의존하는 정보를 남기지 말 것.

## Terra → Sol 인수인계 검토 관문 (2026-07-29)

- **Terra 종료 시**: 의미 있는 작업을 커밋·푸시한 뒤 `python handoff_review.py --mark-terra`를 실행한다. 이 표식은 `reports/`에만 남으며, Sol이 검토할 변경 범위를 기준 커밋부터 현재 HEAD까지 고정한다.
- **Sol 시작 시**: 쓰기 작업 전에 `python session_handoff.py --check --for-sol`과 `python handoff_review.py --review-sol`을 순서대로 실행한다. 검토는 커밋 변경분의 공백·비밀값 형태·변경 Python 문법·전체 합성검증을 확인한다.
- **강제 규칙**: Terra 표식이 있고 Sol 검토가 현재 HEAD와 정확히 일치하지 않으면 `ai_claim.py`가 Sol의 `ledger`, `code`, `band`, `publish` 점유를 차단한다. `read`와 `report` 점유는 검토를 위해 허용한다.
- Sol 검토 뒤 새 커밋이나 의미 있는 미커밋 변경이 생기면 검토 결과는 자동으로 오래된 것으로 간주한다. 다시 검토하여 PASS를 받아야 한다.

## 프로젝트 개요
(주)유니버셜리프트앤히타치코리아의 쿠팡 업무(돌발AS·정기점검·신규납품설치)를 관리하는
**관리대장 엑셀** ↔ **이카운트(ECOUNT) ERP** ↔ **네이버 밴드** 3자 자동 대조·자동화 시스템.
매일 08:30 유수비 대표 보고 체계의 데이터 무결성을 보장하는 것이 목적.

- 관리대장: `Z:\2. Cost\★★★쿠팡 업무 폴더★★★\♣ 1000. 쿠팡 통합업무관리 전산화 프로젝트\00. 대시보드 (프로젝트 일정, 담당자, 진행현황, 문제사항)\00. 쿠팡 통합업무 일일보고 관리대장\쿠팡_통합업무_일일보고_관리대장_v{N}.xlsx` (최신 v 자동탐지 지원, 구버전은 OLD/)
- 이 저장소: https://github.com/Mulder4780/coupang-ecount-reconcile (공개)
- Python: `C:\Users\hueng\AppData\Local\Programs\Python\Python312\python.exe` (PATH의 python은 Windows 스토어 스텁 — 사용 금지)

## ★ 동시 작업 규칙 (Claude ↔ Codex 가 **같이** 일할 때)
사용자 지시(2026-07-28): "동시 작업중일 때 둘이 대화해서 우선순위 정해서 협업해서 진행해."
AI끼리 직접 대화할 수단은 없다. 이 프로젝트의 진실의 원천은 파일이므로 **조율도 파일로** 한다.

1. **시작 전에 잡고, 끝나면 놓는다** — `python ai_claim.py --who <claude|codex> --take <lock> --why "이유"`
   · 잡을 것: `ledger`(관리대장 쓰기) `band`(밴드 수집·반영) `publish`(고정주소·폰사본) — **배타**
   · `report`·`read` 는 공유(동시 가능). 끝나면 `--free`, 세션 종료 시 `--free-all`.
   · 45분간 안 놓으면 자동 해제된다(크레딧 소진·중단 대비). 현황: `python ai_claim.py`
2. **관리대장 쓰기는 한 번에 하나만.** 둘이 동시에 vN+1을 만들면 **한쪽 작업이 통째로 묻힌다**
   (둘 다 v204를 읽고 각자 v205를 만들면 하나는 사라진다). 이건 되돌릴 수 없다.
3. **우선순위 — 충돌 시 이 순서로 양보한다**(사람이 매번 정하지 않아도 되게)
   ① 사용자가 **지금 대화 중인** AI가 우선 — 상대는 조회·분석으로 돌린다
   ② 원장 쓰기 > 밴드/게시 > 리포트·문서
   ③ 같은 순위면 **먼저 잡은 쪽**. 늦게 온 쪽은 다른 일을 한다.
4. **push 전에 `git pull --rebase`** — 상대가 먼저 올렸을 수 있다. 충돌하면 상대 것을 지우지 말고 합친다.
5. 서로의 작업을 **되돌리지 않는다.** 상대 결정이 틀려 보이면 지우지 말고 19시트·AGENTS.md에 이유를 적고 남긴다.

### 2026-07-28 갱신 (3) — 협업 규칙을 **강제**로 바꿈 (Claude)
- Codex가 `ai_claim.py` 를 원자적 잠금으로 강화한 위에, **규칙을 실제로 강제**하는
  `claim_guard.py` 를 얹었다. 문서에만 적어 두면 급할 때 그냥 도구를 돌리게 되고,
  그 순간 두 AI가 각자 vN+1을 만들어 **한쪽 작업이 통째로 묻힌다**(되돌릴 수 없다).
- 원장을 쓰는 도구 4개(`ledger_writer`·`workbook_patch`·`expand_rows`·`confirm_fill`)가
  실행 직전에 `require("ledger")` 를 거친다.
  · 남이 잡고 있으면 **멈추고 이유를 알려 준다**(exit 3)
  · 아무도 안 잡았으면 조용히 자동으로 잡는다(사람이 매번 명령할 필요 없음)
  · `CSOS_AI` 가 없으면(=사람이 직접 실행) 막지 않고 경고만 한다 — 자동화가 사람 손을 묶으면 안 된다
- AI는 실행 시 `CSOS_AI=claude` / `CSOS_AI=codex` 를 붙인다. 검증 [40]이 차단·허용·자동점유를 모두 본다.
- **PO 자료는 두 폴더를 항상 같이 본다**(사용자 지시): 관리대장 폴더(하위폴더+견적서 566개)와
  공유 폴더(평면 70개). 같은 파일이 양쪽에 있을 수 있어 파일명+종류로 중복을 제거한다.
  결과: 쿠팡 목록 96 vs 원본 통지문 100 · **금액 불일치 0** · 부가세 표기차 2(같은 건).

### 2026-07-28 갱신 (4) — 원본 자료 폴더 일원화 · 확인목록에 날짜·대표번호
- **원본 경로는 `source_dirs.py` 한 곳에서만 정한다.** 도구마다 하드코딩하면 자료가 옮겨질 때
  한쪽만 고쳐져 못 읽는다 — 실제로 PO 경로가 되돌아가 자료를 못 읽은 적이 있다(2026-07-28).
  · `ORIGIN_ROOT` = 관리대장 폴더 아래 **`0. 원본 자료`** (사용자 지시: 모든 원본을 여기 취합)
  · PO 원본 = `0. 원본 자료/26년도 PO 모음` + 공유 폴더(`Z:. Share\…`) **둘 다** 훑는다
  · 엑셀·카톡 원본도 `0. 원본 자료` 를 먼저 보고, 없으면 로컬 inbox 를 본다
    (급할 때 PC에 바로 떨어뜨리는 경우가 있어 둘 다 유지)
- 두 PO 폴더가 **같은 내용(574개)** 이라 파싱 **전에** 파일명으로 중복을 거른다.
  나중에 거르면 같은 PDF를 두 번 여는 셈이라 느린 네트워크 드라이브에서 두 배 걸린다.
- 확인필요·TOP5 목록 줄에 **날짜와 대표 프로젝트NO**를 붙였다(`idMeta`).
  사내 ID(AS-2512-005)만 있어서 "언제 건이냐 / 어느 프로젝트냐"에 바로 답을 못 했다.
  프로젝트NO는 `apply_rep_no` 가 채운 **대표번호**를 쓴다.

## ★ 절대 규칙 (위반 시 실사고)
1. **비밀정보**(이카운트 API키·밴드 토큰·비밀번호)는 `config/ecount_config.json`·`band/.band_token.json` 등 gitignore된 로컬 파일에만. 커밋·채팅·엑셀 입력 절대 금지. push 전 `git grep --cached`로 키 문자열 스캔.
2. **관리대장을 openpyxl로 열어 save() 금지** — 차트·도형버튼·x14 검증이 파괴된다. 읽기는 `read_only=True`만. 수정은 `workbook_patch.py`(zip 단일파트 패치, 3중 검증 내장)로 vN+1 새 파일 생성. 원본 덮어쓰기 금지.
3. **이카운트 트래픽**: 조회 1회/1초, 저장 1회/10초, 일 5,000건, 연속오류 30건/시(초과 시 ERP 자체가 제한될 수 있음 — 사용자 경고). 존재하지 않는 경로 무차별 탐침 금지. 판매·세금계산서·수금 "조회" API는 **존재하지 않음**(2026-07 삼중 확인) — 다시 찾으려 하지 말 것.
4. **실데이터·실서버 작업 전 반드시 `python tests/synthetic_check.py` 실행** — ALL GREEN이어야 진행 (사용자 상시 지시 2026-07-24).
5. **ERP 전표 실전송 금지 (2026-08-05 사용자 지시: "전표 실전송 행위는 하지마 이거 삭제해").** 앱·워크벤치·AI 어느 경로로도 ERP에 전표를 등록하지 않는다. 차단 지점 3곳: `ecount_upload.py`(`--post` 를 줘도 미리보기 후 중단) · `webapp/app_server.py start_task`(`upload_post` 키 거부) · 화면 버튼 제거(`webapp/index.html`, `coupang_workbench.py`). **버튼·`upload_post` 작업키·전송 코드를 되살리지 말 것** — Codex 워크트리를 병합할 때도 이 3곳이 살아 있는지 확인한다. 제공하는 것은 전송 대기 목록 미리보기까지이고, 실제 등록은 사람이 ERP 화면에서 한다. 이중계상 가드(명세서번호 보유 행 제외)도 끄지 말 것(--include-billed는 사용자 지시 시에만).
6. 관리대장 공통 상수 HDR_ROW=4, FIRST=5 변경 금지. 열 삽입·삭제 금지. STANDING ORDER(19시트 60행): 작업 시 18·20 매뉴얼, 19 인수인계, 21·22, A2 사용법 현행화. AS 기사 기준 4인: 김준형·권오철·김필우·차동호.
7. **날짜 정렬은 언제나 과거→최근**(오래된 것이 맨 위, 최근이 맨 아래). 앱·리포트·시트 전부 동일. 새로 추가되는 행도 자동으로 이 순서를 따라야 하므로, 목록을 만들 때는 `webapp/app_server.py`의 `sort_by_date(rows, kind, idkey)`를 거치게 한다(빈 날짜는 맨 뒤, 동률은 ID순). 사용자 상시 지시 2026-07-25. 검증: synthetic_check `[14]`.
8. **워크북에 셀을 만들 때 `t` 속성을 쓰면 캐시값(`<v>`)을 반드시 함께 넣되, 이미 있는지 판정은 `re.compile(r"<v[\s/>]")` 로 한다.** `"<v>" in cinner` 같은 문자열 비교는 `<v xml:space="preserve">…</v>` 를 놓쳐 `<v>`를 하나 더 넣게 되고, **한 셀에 `<v>`가 2개면 엑셀이 그 시트를 통째로 비운다**(2026-07-26 실사고: 313셀 → 02·03·04·06 시트 내용 전멸, v48→v50 복구). 파일을 만든 뒤에는 반드시 `python fix_workbook.py`로 확인한다 — daily_run에 포함됨. 검증: synthetic_check `[19]`. `t="str"`만 쓰고 `<v>`를 빠뜨리면 엑셀이 열 때마다 "내용에 문제가 있습니다" 복구 대화상자를 띄운다(2026-07-26 실사고: expand_rows가 만든 1,573개 셀). 관리대장을 만드는 모든 도구는 실행 후 `python fix_workbook.py`로 무결성을 확인한다 — daily_run에 단계로 포함됨. 검증: synthetic_check `[19]`.
9. **문서 이미지 OCR은 로컬 엔진만** 사용한다(Windows OCR 우선, 로컬 PaddleOCR 보조). 거래명세서·세금계산서는 금융 문서이므로 외부 OCR API·클라우드 업로드 금지. OCR은 반드시 오인식하므로(UJ→U], 콤마→|) 라벨:값 인접이 아니라 **줄 단위 패턴 + 금액정합성(공급가+세액=합계, 세액=공급가10%)** 으로만 값을 채택하고, 신뢰도 '높음'+금액 일치인 건만 대장 빈칸에 넣는다. 검증: synthetic_check `[15]`.
11. **쿠팡 담당자에게 어떤 메시지도 보내지 않는다** (사용자 상시 지시 2026-07-27). 메일·카톡·밴드 게시·댓글·API 어느 경로도 안 된다. 문제는 **유니버셜 내부에서 처리한다.**
    · 현재 코드에는 그런 경로가 없다 — 밴드·카톡 모듈은 **읽기 전용**(수집·파싱만, 글쓰기 API 없음), 이카운트 클라이언트는 사내 ERP 전표만 다루고, 메일 발송(GitHub Actions)은 2026-07-27에 제거했다.
    · 캡처·공유 기능(`captureMine`·`shareMine`·`captureMineDetail`·`shareMineDetail`·담당자 카드)은 **파일을 만들어 줄 뿐이고 받는 사람은 사람이 직접 고른다.** 자동 전송을 넣지 말 것.
    · 앞으로 알림·공유 기능을 붙일 때는 **수신자가 사내인지** 먼저 확인한다. 쿠팡이 보는 채널(쿠팡 담당자가 멤버인 밴드 포함)에 자동으로 글이 올라가게 만들지 말 것.
10. 확인 안 된 쿠팡 정책 단정 금지(특히 "PO 없으면 세금계산서 불가" 단정 금지). 원자료에 없는 값 임의 채움 금지.
12. **엑셀은 하루 두 번만 반영한다(11:00·15:00).** 앱·Claude·Codex·자동대조가 만든 확정 입력은
    `ledger_db.py`의 SQLite에 먼저 모은다. `ledger_writer.py --apply`를 일일 실행이나 에이전트가
    직접 호출하지 말고, 작업 스케줄러 `쿠팡업무_원장일괄반영`이 두 회차에만 일괄 실행한다.
    놓친 입력은 다음 허용 회차로 넘기며 임의 시각에 따라잡지 않는다. 긴급 `--force`는 사용자
    명시 지시 때만 사용하고 batch 감사기록을 남긴다.

## 시스템 구성 (전부 표준 라이브러리+openpyxl, 독립 실행)
| 파일 | 역할 | 실행 |
|---|---|---|
| `ecount_client.py` | 이카운트 OAPI 인증(Zone→로그인→세션캐시) | 라이브러리 |
| `ecount_reconcile.py` | 원장 로드(read-only) + inbox(엑셀 내보내기) 판매·세금계산서 대조 | `python ecount_reconcile.py` |
| `erp_ledger_check.py` | ERP 거래처별계정별원장 ↔ 원장 4유형 대조(A: ERP에만/B: 원장에만/C: 회계O·계산서X/D: 금액차) | `python erp_ledger_check.py` |
| `ecount_upload.py` | 원장→ERP 매출전표 자동등록(SaveInvoiceAuto). 기본 dry-run | `python ecount_upload.py [--post --limit N]` |
| `workbook_patch.py` | 관리대장 vN→vN+1 안전 버전업(인수인계 행 추가) | `python workbook_patch.py --b 제목 --c 내용` |
| `band/band_auth.py → band_sync.py → band_reconcile.py` | 밴드 게시글 수집·원장 대조 | 순서대로 |
| `kakao/kakao_reconcile.py` | 카톡 [대화 내보내기] .txt ↔ 원장 대조 (읽기 API 없음 — 내보내기가 공식 경로, PC 클라이언트 후킹은 약관위반이라 금지) | `kakao/inbox/*.txt` 투입 후 실행 |
| `ledger_writer.py` | **관리대장 자동입력 엔진**: 일괄 배치 큐를 zip 셀 패치로 반영. 정책: 빈 칸만·근거 필수·항상 vN+1 새 파일·19시트 자동 기록·calcChain 제거+fullCalcOnLoad. 덮어쓰기 기능은 의도적으로 미구현 | `ledger_db.py`만 호출 |
| `ledger_db.py` | 앱·Claude·Codex·대조 도구의 확정 입력을 SQLite에 안전하게 모아 11:00·15:00에 한 번씩만 관리대장으로 일괄 반영. JSON staging 재시도 중복 방지·회차 중복 방지·UX 사용기록 포함 | 작업 스케줄러 `쿠팡업무_원장일괄반영` |
| `pm_schedule_sync.py` | 류지영 정기점검 원본 폴더의 최신 xlsx를 현재 분기 기준으로 읽어 `27_정기점검원본일정`과 앱 예정 목록에 멱등 반영. 완료 실적·확정일 우선, 철거/점검불가/폐쇄/매각 제외, UJ번호 추측 금지 | `python pm_schedule_sync.py --apply` (`daily_run`이 자동 호출) |
| `daily_run.py` (+`daily_run.bat`) | 일일 에이전트 오케스트레이터: 합성검증→전체 대조→확정분 DB 적재→종합리포트. 엑셀 일괄반영·ERP 쓰기(--post)는 별도 | 작업 스케줄러 09:50 등록됨 |
| `coupang_workbench.py` (+`워크벤치실행.bat`, 바탕화면 바로가기) | PC 통합 앱(Tkinter): 상태판(최신버전·포크경고·에이전트 이력·대기건수)+클릭 실행(대조·자동입력·전표전송 승인·폴더 열기). 앱 없이도 에이전트는 독립 동작 | 더블클릭 / `--status` 콘솔 |
| `webapp/app_server.py` + `webapp/index.html` (+`앱서버실행.bat`) | **반응형 웹앱(ERP형)** — PC·휴대폰 브라우저 접속(포트 8899, 같은 LAN). 4탭: 대시보드(KPI·에이전트 이력·포크경고)/정산 그리드(검색·필터·상태칩)/실행(작업+라이브 로그, 전표 실전송은 이중확인)/리포트(md 렌더). PIN 4자리 인증(config/webapp.json, gitignore), 표준 라이브러리만, --demo=합성데이터 모드, 데이터는 PC 밖으로 안 나감. LAN 전용 — 외부 인터넷 개방 금지 | `python webapp/app_server.py` / t6 합성테스트 |
| `tests/synthetic_check.py` | 합성데이터 상시 검증(실서버 접촉 0) | 작업 전 필수 |

- 데이터 흐름: 이카운트 화면 엑셀 내보내기 → `inbox/` → 대조기 → `reports/` (md·csv·xlsx). 관리대장은 절대 직접 수정하지 않음.
- 회계 확정값: CUST=2548801036(쿠팡로지스틱스서비스 유한회사), CR_CODE=4049(제품매출, 변재선 과장 확인), TAX_GUBUN=11(가정—첫 실전송에서 검증).
- 전표 매칭 키: 원장 06시트 거래명세서번호("2026/07/01-4") = ERP 일자-No.("2026/07/01 -1", 공백 정규화).

## 현재 상태 (2026-08-06 기준)

### 2026-08-06 밤 — 밴드 전량 밤샘 수집 시작 · ★ERP 화면 접근 성공 (Claude)
사용자 지시: "밴드 데이터 및 ERP 데이터 전체 순차적으로 긁어와 어떻게든 다 긁어와서
정리해 / 컨텍스트 윈도우 다 차기전 컴팩팅 자동으로 / 밤을 새서라도 미처리된 건들 전부
대조해서 찾아서 해결하고 완료처리해".

**★ 막혔던 두 가지가 동시에 풀렸다 — 원인은 둘 다 '로그인된 크롬에 확장이 붙었나'였다**
`list_connected_browsers` → 크롬 하나(Browser 1) → `select_browser` 한 번으로
**밴드도 이카운트도 이미 로그인된 상태**로 잡혔다. 8/6 낮에 반나절을 잃었던 그 문제다.
- 밴드: 로그인 확인 → 수집기 주입 → 전량 주행 시작.
- **이카운트 ec5 도 `javascript_tool` 로 조종된다**(전에는 45초 타임아웃으로 전부 실패).
  · 메뉴는 `[data-prgid]` 로 열린다. 회계 I=C000001 → 출력물=C000017 안에
    계정별원장·계정별거래처별원장·거래처별계정별원장·분개장·주요재무제표·재무상태표·
    손익계산서·합계잔액시산표가 있다(2026-08-06 실측 목록).
  · 리프 메뉴는 텍스트 정확일치로 클릭하면 열린다(`계정별원장` 클릭 → 화면 전환 확인).
  · **여기까지 됐고 여기서 멈췄다**: 조회 결과 그리드가 최상위 문서에도 하위 프레임
    (s_page·s_coo·ifrmExcel — 전부 about:blank)에도 안 나타난다. 조회 조건(계정과목 등)이
    더 필요하거나 결과가 별도 창으로 열리는 구조로 보인다. **다음 세션의 첫 ERP 과제.**
  · 절대규칙 3 그대로 — API 무차별 탐침은 하지 않는다. 사람이 누르는 길만 따라간다.
  · 그 탭은 사람이 쓰던 탭이다. 조회 조건을 함부로 바꾸지 말 것.

**밴드 전량 수집 — 밤새 혼자 돈다**
`band/grab_all.js` 가 200건씩 수집→덤프 저장→메모리 비우기→다음을 스스로 잇는다.
`band/dump_watch.py --minutes 600` 이 떨어지는 덤프를 흡수한다.
- 90610953(쿠팡AS): 1~3585 · 18배치 · 실측 약 6.9초/건 → 약 6시간
- 84789192: 1~3333 (그다음 차례. **두 밴드 동시 실행 금지** — 실패율이 급증한다)
- 이어받는 법: 밴드 탭에서 `__allStatus()`. 멎었으면 `band/serve_scripts.py` 를 띄우고
  `fetch('http://localhost:8123/grab_posts.js').then(t=>eval(t))` 로 다시 주입한 뒤
  `recheck_plan.py --floor 1` 이 알려 주는 남은 범위로 `__allStart` 를 다시 건다.

### 2026-08-06 저녁 — 캘린더 개편: 날짜 창 · PC 텍스트 격자 · 고정 공유주소 (Claude)
사용자 지시: "캘린더 날짜 클릭시 밑에서 위로 올라가는 레이어창이나 모달창으로,
사용자 입장에서 편한 방식으로 / 공유시 크롬을 자동으로 열어서 설치하게 /
공유 캘린더 주소 고정(변동 주지마) / 날짜 크기 크게 / PC 웹은 텍스트로 자세히,
반응형으로 모바일은 지금처럼". 검증 `[120]`.

- **날짜를 누르면 그 자리에서 창이 뜬다.** 폰=아래에서 올라오는 레이어(아래로 쓸어
  닫기), PC=가운데 모달. 마크업은 하나고 CSS 미디어쿼리가 가른다. 예전에는 화면 저
  아래 목록만 바뀌어 폰에서는 "눌러도 아무 일이 없다"로 보였다.
  ★ 함정: 시트를 `#v-calendar` 안에 두면 **화면 밖에 그려진다.** `.view` 에 화면 전환
    transform·filter 가 걸려 있어 그 안에서는 `position:fixed` 가 화면이 아니라 그
    구역을 기준으로 잡힌다. `</main>` 밖에 두어야 한다(실측).
- **PC 는 칸 안에 캠프명을 글자로**(칸당 4건 + "+N건 더"), 폰은 지금처럼 색 점.
  글자를 폰에도 같이 실어 보내고 감추는 것은 CSS 다 — 창 크기를 재지 않는다.
- **날짜 글씨** 14→17px(좁은 폰 15.5 · PC 18). 공유 캘린더도 12.5→17px.
- **공유 주소 고정.** 달(`m=`)·필터(`off=`)를 주소에서 뺐다 — 누를 때마다 주소가 달라져
  이미 뿌린 링크와 새 링크가 다른 화면을 열었다. 옛 링크는 계속 받아 준다.
  `cal_share.py` 는 열쇠 파일이 없다고 **조용히 새 열쇠를 뽑지 않는다**(뽑히면 이미 뿌린
  링크가 전부 죽는다 — 다른 PC·계정에서 돌릴 때 실제로 그럴 뻔했다).
  고정 주소는 `reports/공유_캘린더_주소.txt`(git 밖).
- **카카오톡·밴드 안 브라우저는 설치가 아예 안 된다.** 감지해서 안드로이드는 크롬
  (`intent://` — `#Intent;` 를 맨 뒤에 두면 앞의 `#k=` 가 살아 넘어간다), 아이폰은
  **사파리**(`x-safari-`)로 넘긴다. 아이폰 크롬은 홈 화면 설치를 못 하므로 크롬으로
  보내면 한 걸음 헛돈다. 설치되는 것이 업무 앱이 아니라 캘린더가 되게
  `docs/cal-manifest.json` 을 따로 뒀고(안 그러면 아이콘을 눌러 PIN 화면이 떴다),
  설치 아이콘이 주소의 `#k=` 를 못 가져가는 문제는 **기기 기억**으로 풀었다.

### 2026-08-06 오후 — 브라우저 연결 함정 · 붙여넣기 오염 흡수 (Claude)
사용자 지시: "다른 계정에서 에이전트가 알아서 다 해줬는데 왜 이 계정으로는 안되는거야" /
"긁어오는건 다른 계정에서 시킬게, 긁기 말고 다른 업무 싹 다 처리해" /
"항상 남은 작업 보고하면서 작업하고".

**★ 도구가 '로그인 없는 크롬'에 붙어 있었다 — 능력 차이가 아니었다**
Claude 확장은 **확장이 연결된 크롬**만 조종한다. 사람이 로그인해 둔 주 크롬에 연결이
없어 도구가 임시 프로필 크롬(창도 안 보인다)에 붙었다. 다른 계정과의 유일한 차이는
그쪽 주 크롬의 확장이 **그 계정으로 로그인돼 있었다**는 것뿐. 이제 브라우저 작업 전에
`list_connected_browsers` 를 먼저 부른다(CLAUDE.md 규칙). 사고 `INCIDENTS.md 20`.
· 쿠키 복제·키보드 주입 우회는 **자동승인 정책이 막았고 하지 않는다.** 만들던 스크립트는 지웠다.

**대신 사람 손을 줄였다 — 붙여넣기가 밴드당 한 번**
250 은 탭이 얼지 않는 한 배치의 상한이지 붙여넣을 횟수가 아니다. 회차 사이에
`__grabSave()` 로 내보내고 `{keep:false}` 로 탭 메모리를 비우면 한 파일이 끝까지 돈다.
쿠팡AS 1,114건 5회→1회 · 매출 367건 2회→1회. 앞 배치가 돌고 있으면 그것부터 끝내고
이어 간다(안 그러면 첫 회차가 통째로 빠진다). 검증 `[112]`.
· 안내창 확인버튼은 `band/autoclick_dialog.ps1`(UI 자동화)이 누른다 — 실측 16회.
· 덤프는 `band/dump_watch.py` 가 흡수→대조→큐까지 자동으로 잇는다.

**★ 매칭 실패의 절반은 붙여넣기 흔적이었다** (김미영 대리 지적의 정체)
`&amp;`(웹 복사) · `<-메모` · `?` · 값 대신 `0`·`...`. 이름을 고치는 게 아니라 **비교할
때** 걷어 낸다(`customer_index.clean()`·`bare()`). 캠프 미매칭 18→14건, 코드 확정
200→202건. 값이 아닌 표시 6건은 '못 잇는 자료'가 아니라 **원장을 고칠 일**로 갈랐다.
`stmt_subset.norm_cust` 도 같은 규칙을 쓴다 — 규칙이 두 벌이면 반드시 갈린다.
검증 `[113]`. 사고 `INCIDENTS.md 21`.

**미분류 0건**: `10. 기준·참고 자료` 자리 신설(단가표·인수인계·관리 리스트) +
`upload_intake --apply` 회차가 **예전 미분류까지 다시 걸러 낸다**. 규칙은 계속 느는데
규칙이 없던 시절 미분류는 아무도 다시 안 봤다 — 5건 → 0건.

**남은 것**(긁기는 다른 계정): 밴드 쿠팡AS 1,114 · 매출 367 · ERP 2025 명세서 304 ·
ERP 미수집 화면 · 세금계산서 발행 153(사람 전용) · 명세서↔프로젝트 짝 없음 17건
(1억 980만원 — 정규화가 원인이 아님이 확인됐다. **ERP 판매조회 미등록 여부를 사람이 판단**).

### 2026-08-06 낮 — 8/5 누락 대조 · 리모컨 고도화 · 계정 인계 (Claude)
사용자 지시: "어제 날짜 돌발이 1건밖에 안잡혀 더 많이 있는데, 정기 점검도 그렇고
이거 대조해서 처리" / "이 세션은 완료되면 다른 계정으로 로그인해서 사용할거야,
그때 아무 문제 없이 처리될 수 있는 알고리즘 구성해".
상세 보고: `reports/작업보고_2026-08-06.md`

**★ 적용기가 수식 열 키를 못 찾아 조용히 버렸다 (실사고)**
`점검ID`·`접수ID`·`작업ID` 는 `=IF($B5="","","PM-"&…)` 수식이다. `ledger_writer` 가
수식 통(`data_only=False`)에서 키를 읽어 사전에 `'=IF(...)'` 를 넣었고, **실제로 있는
행을 "행 없음"으로 버렸다.** 8/5 정기점검 완료 2건이 그렇게 사라져 대표 보고에 0건으로
나갔다. → 값 통(`data_only=True`)에서 키를 읽는다. 같은 키가 여럿이면 **첫 줄**을 쓴다.
검증 `[110]`. **key_col 로 ID 열을 쓰는 모든 도구가 이 버그의 영향권이었다.**

**8/5 결과**: 돌발AS 1→2건 · 정기점검 완료 1→3건 (v480→v491).
사용자 절대명령으로 이 작업 동안만 `--apply --force` 사용, 종료와 함께 11:00·15:00 복귀.

**★ 수집 밀림은 조용한 사고다 — 이제 시작 화면이 잡는다**
쿠팡AS 밴드가 8/4 에 멈춰 있는데 화면은 멀쩡히 숫자를 보여 줬다. `session_handoff` 가
원본마다(**밴드는 밴드마다**) 최신일을 세어 '어제'가 비면 막힌 것 맨 앞에 올린다.
밴드를 합쳐 최댓값으로 보면 뒤처진 밴드가 가려진다 — 하필 그쪽이 돌발AS 원천이었다.
Z: 를 훑지 않고 원본 색인 캐시(`경로|크기|mtime`)만 읽어 즉시 끝난다.

**계정이 바뀔 때**: `python ecount/session_handoff.py --adopt` 하나. 죽었다는 증거(pid)가
있는 점유만 회수하고 살아 있는 옆 세션은 안 건드린다. 검증 `[111]`.
★ 밴드·이카운트 로그인은 **Claude 가 조종하는 탭 그룹 안에서** 해야 한다 —
사람이 쓰는 다른 탭에 로그인해도 AI 는 그 쿠키를 못 쓴다(2026-08-06 실측).

**리모컨 고도화(v6 세션 미완 5가지 전부)**: 버전 선택(불출·납품·재고 전부, `ver3`·`v4`
표기 통일) · 지사 불출자 이름란 · 줄마다 고치기 · 삭제(사유 필수, 되돌리기 가능) ·
강제 수정(이번 수정이 **새로 만든 문제만** 막는다). 검증 `[109]`.

### 2026-08-05 저녁 — 병렬 세션 구조 · 색인 정화 · 판매조회 회귀 (Claude)
사용자 지시: "지금 현재 열려있는 세션과 병렬 작업 가능한 구조로 정리하고 하던 작업 계속."

**병렬 작업은 두 층으로 나눈다** — 자원 잠금과 할 일 분담은 다른 문제다.
- `ai_claim.py`(Codex 개편) = **자원**. "지금 이 파일을 내가 쓴다" — 세션ID(sid) 단위, 짧게.
- `worksplit.py`(신설) = **할 일**. "이 일은 내가 맡았다" — 길게. `reports/작업분담.md` 로도 읽는다.
  자원 잠금만으로는 부족하다: 두 세션이 남은 일 목록을 각자 읽고 **같은 일을 몇 시간
  조사한 뒤에야** 중복이 드러난다. 그래서 일 단위로 먼저 나눈다.
  ★ 주인을 PID 로 적으면 안 된다 — CLI 는 명령 한 번에 끝나 **적자마자 죽은 주인**이 된다.
  ★ 동시 작업 중 `git add -A` 금지. 실제로 한쪽 변경이 남의 커밋 메시지 아래로 들어갔다.

**★ 판매조회 회귀 — 한 해를 받으면 다른 해가 사라졌다**
`erp_sales_index` 와 `stmt_link` 가 **가장 최근 판매조회 하나만** 읽었다(후자는 `break`).
2025 자료를 받으려고 2025 판매조회를 내려받는 순간 그것이 '가장 최근 파일'이 되어
색인이 통째로 2025 로 바뀌고 **2026 명세서 793건이 전부 짝 없음**이 됐다(확정 593→0).
→ 둘 다 **모든 내보내기를 읽어 합친다**. 같은 UJ 는 새 파일이 이긴다(합산하면 겹치는
기간이 두 배가 된다). 결과: 판매색인 **2,000건**(2025:882·2026:1,118) · 명세서 확정 **601**.

**색인이 스스로를 세고 있었다** — `_바로가기`(원본의 하드링크)와 `_보관`(일자별 백업)이
색인에 들어가 17,368건 중 8,400여 건이 파생물이었고 전부 '기타'로 뭉쳐 있었다.
앱 원본자료 화면에서 같은 자료가 여러 번 나온 원인. 제외 후 **6,745건**(진짜 원본만).

**★ 조용히 틀리는 것 셋을 더 잡았다 — 오류를 내지 않고 숫자만 어긋난다**
1. **캐시가 규칙 변경을 삼켰다.** FOLDER_KIND 순서를 고치고 색인을 다시 돌렸는데 결과가
   하나도 안 바뀌었다(밴드 5,209 그대로·게시글 0). 캐시 열쇠가 `경로|크기|수정시각`이라
   **파일이 안 바뀌면 옛 갈래를 그대로 돌려준다.** → 규칙 지문(FOLDER_KIND 해시)을 캐시에
   같이 적고 다르면 전부 다시 판별한다. `--rescan` 을 기억할 필요가 없어야 한다.
2. **폴더 이름을 두 곳에 적어 어긋났다.** 색인 규칙의 `2. 쿠팡 PO`·`5. 입금`·`6. 서류` 는
   **없는 폴더**였다(진짜는 `6. PO 원본`·`7. 입금내역`). 그래서 **쿠팡 PO 576건이 전부
   '기타'** 로 빠져 앱 PO 세그먼트에 안 나왔다. → 폴더 이름은 `source_dirs` 만 알고
   색인은 거기서 받아 쓴다. 갈래를 새로 만들면 **앱 세그먼트에도 넣을 것** —
   어느 세그먼트에도 없으면 '전체'에서만 보여 사실상 사라진다.
3. **바로가기를 만들기만 하고 거두지 않았다.** 오염된 색인으로 만든 링크가 그대로 남는다.
   → 이번 색인에 없는 링크는 거둔다. 단, 다른 이름이 없는 것(st_nlink==1)은
   마지막 사본일 수 있어 지우지 않고 보고한다(첫 실행 490개 — 재생성된 PDF 의 옛 판본).

**daily_run 실패 5건 중 4건은 결함이 아니라 경합**이었다(Z: 대량 보관·엑셀 점유와 겹친 순간).
손으로 돌리면 전부 정상. → 읽기 전용 단계는 실패 시 20초 뒤 **한 번 재시도**한다.
큐·반영·파일 이동 단계는 인자를 보고 자동으로 재시도에서 뺀다(호출부마다 적게 하면 잊는다).
경합 잡음에 묻혀 위 stmt_link 회귀를 못 볼 뻔했다 — 잡음을 줄이는 것이 진짜 목적이다.

**앱 API 오류의 정체** — 기록 2,777건은 실제로 **끊김 164번**이었다(한 번 끊길 때마다
열린 탭이 스무 곳을 한꺼번에 재호출. 12:33 한 분에 440건). '평균 3.2초'도 착시로,
숨은 탭 시간이 섞여 실제 기록은 평균 164초였다. 고친 것:
끊김은 30초에 한 번만 사유와 함께 기록 · 5초 이상 못 닿으면 **빨간 띠로 알린다**
(예전에는 `.catch(()=>{})` 로 삼켜 '갱신 중' 칩만 떠서 사람이 옛 숫자를 최신인 줄 알았다) ·
숨은 탭 시간은 '느림'에서 제외.

**통화 기록을 남기는 자리 신설** — `call_notes.py` + `0. 원본 자료/10. 통화·회의 기록`.
`## 할 일` 항목을 19시트 인수인계로 예약한다. 녹취 전문·욕설·인사 이야기는 넣지 않는다
(공유 폴더라 한 번 들어가면 지우기 어렵다). 김준형 통화(8/5) 할 일 5건 예약 완료.
그 통화에서 나온 사실 하나를 분류에 반영: 미실시 사유에 **'확인 필요(구매·조달 지연)'**
갈래 신설 — 기안·승인에 멈춘 건을 '자재 대기(정당)'로 묶으면 본사 절차가 병목이라는
사실이 숫자에서 사라진다. 발주가 이미 나간 정상 대기와는 구분한다.

### 2026-08-05 오후 — 전표 실전송 제거·비율 표기 규칙·밴드 수집기 복구 (Claude)
사용자 지시 두 건과, 그것을 하다 드러난 수집기 결함 하나.

- **ERP 전표 실전송 삭제**(지시: "전표 실전송 행위는 하지마 이거 삭제해").
  앱 버튼·워크벤치 버튼·서버 작업키(`upload_post`)를 없애고, `ecount_upload.py` 는
  `--post` 를 받아도 미리보기 뒤 멈춘다. 제공하는 것은 **전송 대기 목록까지**이고
  ERP 등록은 사람이 직접 한다. 절대규칙 5번을 이 내용으로 바꿨다 — **되살리지 말 것**.
- **비율 표기 규칙 단일화**(지시: "소수점 1자리까지 표기, 1건이 안되도 100%로 보이는
  문제 해결"). `pct_fmt.py`(서버)와 `index.html` 의 `pctNum/pctText/pctFix`(화면)가
  같은 규칙을 쓴다: 소수점 1자리 고정 · **미완료면 100% 금지**(999/1000→99.9%) ·
  1건이라도 했으면 0% 금지(1/10000→0.1%) · 모수 0이면 "대상 없음".
  적용처: 계산서 발행율·발행률·분기 진행률/경과율/격차·신규 처리율·원천 확인율·비중·리포트 이미지.
  **새로 비율을 만들면 반드시 이 함수를 거칠 것** — 각자 `round(x*100)` 을 쓰면 또 어긋난다.
- **밴드 수집기가 15분에 0건이던 원인 해결**(사고 #19). "원래 느리다"가 아니라 크롬이
  **숨은 탭의 setTimeout 을 1초~1분으로 늦춘 것**이었다. `band/grab_posts.js` 를
  워커 타이머 + MutationObserver 로 바꿔 같은 조건에서 **글당 7.4초**. `__grabStop()` 추가.
- **쿠팡 캘린더 iOS 개편 + 종류 필터**(지시: "돌발 AS, 정기 점검 등 선택한 항목만 볼 수
  있게 필터 추가하고 고도화 … 아이폰 형식으로"). 서버 `get_calendar` 가 02_돌발AS
  (방문예정일·작업완료일)·04_정기점검(실제점검일)을 일정으로 합쳐 **125건 → 910건**.
  분류 키 `pm_plan/pm_pred/pm_done/as_visit/as_done` 를 붙였고 **화면 필터는 이 키로 건다**
  (한글 라벨로 걸면 문구를 고칠 때 조용히 깨진다). 화면은 종류 칩 → 월 격자(날마다 색 점)
  → 그 날 일정 → 세부 순. 구글 iframe 은 접어서 맨 아래. 고른 필터·보던 달은 localStorage.
- **업무 현황 집계 기간 표시**(지시: "몇년도 몇월 몇일부터 몇월 몇일까지인지 표시").
  작업 완료일 기준 최초~최종일을 데이터에서 그대로 읽는다. 완료일 없는 건은 감추지 않고
  "완료일 미기입 N건 포함"으로 함께 적는다.
- **원본 자료 판매전표 갈래 신설**. 이카운트는 판매조회=판매전표이고 그 출력물이 거래명세서다.
  ERP 1,293건에 섞여 있던 청구 근거를 한 칸으로 뽑았다(kind `ERP:sales` + 이름 규칙).
- 검증 `[101]`·`[102]` 신설(비율 규칙·실전송 3중 차단·수집기 숨은탭 대응 /
  캘린더 필터·집계 기간·판매전표). ALL GREEN.
- **밴드 재확인 진행**: 90610953 은 821 → **579건** 남음(242건 재수집, 수정글 4건 감지).
  84789192 은 진행 중. 두 밴드를 동시에 돌리면 실패율이 오른다(A 8/250 vs B 63/119) —
  **한 번에 한 밴드씩** 돌릴 것. 실패분은 `captured_at` 이 안 남아 recheck_plan 이 다시 뽑는다.

### 2026-08-05 종일 — ERP 전수·건별PDF·거래처코드·앱 카드UI (Claude)
사용자 지시 다수(ERP 전량·건별 PDF·거래처코드·캠프 전수·원본 정리·카드 UI·UX 자동점검).

**수집·보관**
- ERP 화면 **40여 종**(판매·구매·수금·채권/채무·원장 3종·분개장·일월계표·재무제표 3종·
  계산서진행단계·집계표 4종·재고 4종·자금 3종 등) + **기초 데이터**(거래처등록 2,981·품목·
  창고·프로젝트·부서). 사원등록·계정과목은 **계정 권한 없음**.
- **건별 PDF 1,036건**: 세금계산서 246(`세금계산서_건별/연/월`), 거래명세서 790
  (`거래명세서_건별/연/월`). 파일명에 전표번호·거래처·프로젝트NO·금액이 들어가 열지 않아도 안다.
- **밴드 게시글 1,432건 전량 보관**(글별 PDF+본문txt+사진). PDF 실패 0.
- **원본 색인 11,124건** + **바로가기 1,724개**(종류별·월별 하드링크) + 길잡이 README.
  `source_index.py`·`source_tidy.py`, 둘 다 daily_run 연결.

**색인·마스터(신규 도구)**
`erp_sales_index.py`(935 프로젝트) · `customer_index.py`(캠프 200곳 코드 확정, 엑셀
`쿠팡_거래처코드_최신.xlsx` 생성) · `camp_master.py`(**캠프 297곳** — ERP+원장 158·ERP에만 42·
원장/밴드에만 58) · `stmt_link.py`(명세서 593건 UJ 확정) · `tax_archive.py`·`stmt_archive.py`.
전부 daily_run 에 연결했다.

**앱**
- 공용 액션 카드(`.ui-cards/.ui-card`) — 아이콘+제목+설명. 실행 화면 버튼 19개 카드화.
- **원본 자료** 화면 iOS 개편(갈래 세그먼트 9·월 선택·색 아이콘 리스트, 클릭=파일 열기).
- **업무센터 카드형 커스터마이징** — 5블록 순서·표시 저장(localStorage), 원본자료 8갈래
  바로가기, 담당자별 기본 갈래(류지영=밴드·오종현=세금계산서).
- 정산에 **거래처코드(CU###) 688건** 표시.

**★ 버그 5건 수정(모두 실측)**
1. `settlement_completion` 이 판매조회를 **파일명**(`판매조회*.xlsx`)으로 찾아 ERP 근거 완료가
   **한 번도 작동하지 않았다**(이름이 무작위라서). 색인 JSON 사용으로 완료 121→**231건**.
2. 앱 금액이 **부가세 포함액을 '부가세 별도'로 표시** — 명세서합계는 포함액이다.
   ERP 공급가액 우선으로 정정(619건). 0원 682→5건.
3. `customer_index`·`camp_master` 가 거래처**관리대장**(104행)을 거래처**등록**(2,981행) 대신
   집어 매칭이 **0**이 됐다 — 주소 열 유무+행 수로 정본을 고르게 수정.
4. `convert_dump` 가 캐시 저장 때 **images 를 버려 밴드 사진이 0장**이었다 — URL 1,132개 복원.
5. daily_run 분석 3종이 600초 타임아웃 FAIL → 1500초.

**자동화 신설**: `ux_review.py` + **쿠팡업무_업무센터UX점검(3일마다 12:00)** — 사용 기록에서
오류·느린화면·동선·미사용을 뽑아 `reports/UX개선_제안.md`(★신규 표시). 읽기 전용.

**★ 다음 세션이 이어서 할 일**
1. **UX 제안 16건 후속** — API 오류가 매우 많다(daily/exceptions 360회·exec_report 334·
   works 283·settlements 270·status 244·평균 3.2초). 화면엔 캐시된 옛 값이 보여 사람이
   모르고 있다. **원인부터 밝힐 것**(인증 만료·타임아웃·서버 재시작 중 요청 중 무엇인가).
2. **2025 거래명세서 304건** — 목록은 확인했으나 화면을 다시 여는 순간 기간이 2026 으로
   초기화된다. ★ **연도는 JS 로 못 바꾼다**(커스텀 셀렉트박스). 사람이 2025 로 바꿔 준 화면에서
   **다시 열지 말고 그 자리에서** 검색→인쇄 미리보기→Excel 25건 배치를 돌릴 것.
3. 밴드 사진 다운로드 이어가기(URL 은 복원됨) · 밴드 재수집 잔여 AS 821·매출처 178.
4. 세금계산서 미발행 153건(90일 초과 21) — **사람이 발행**해야 한다. 전수 조사 결과
   ERP 에서도 전부 발행 전이라 자동 완료 대상이 0건이다(추측 완료 금지).


### 2026-08-05 원본자료 iOS 카드·대시보드 사용자 편집 완료 (Codex)
- 원본자료 화면을 데스크톱 가용 폭과 아이폰 좌우 4px 여백에 맞춘 카드 그리드로 개편했다.
  검색·9개 자료 갈래·월 필터·새로고침을 44px 이상 터치 버튼으로 통일했고, 8천 건 이상
  색인은 서버 5분 캐시와 서버측 필터로 처리한다.
- 대시보드는 화면 카드 12개와 핵심지표 카드 8개를 체크로 넣고 빼며, 드래그 또는 화살표로
  순서를 옮기고 화면 카드 너비를 바꿀 수 있다. 배치는 기기별 localStorage v2에 저장되고
  v1 배치를 자동 이관한다.
- 검증: 합성검증 ALL GREEN, 실제 데모 앱 375px에서 가로 넘침 0px·카드 343px,
  카톡 필터 28건, KPI 숨김/복원 7→6→7, 좌우 이동 왕복, 브라우저 오류 0건.
  커밋 `22cec5e`, `cf738aa`, `6c0a6b4`; master push·8899 앱 서버 재기동 완료.

### 2026-08-04 (5차) ERP 전수 수집 완주 + 자동화후보 일일검색 신설 (Claude)
사용자 지시 ①"ERP에서 한건 한건 다 찍어서 거래명세표·세금계산서·기타 전표 등 모든 자료
다운받고 엑셀도 전부 다운받아 저장하고 시스템에 반영해" ②"깃허브나 오픈소스를 활용한
자동화 방안을 항상 검색해서 매일 오전 10시부터 실행" ③"완료 처리 확인된 건들은 리스트에
보여주지 마".
- **ERP 화면 엑셀 11종 확보(2026/01/01~08/04)** — 판매조회·구매조회·수금현황·거래처별채권·
  계정별원장·거래처별계정별원장·분개장·일월계표·재무상태표·손익계산서·합계잔액시산표.
  지난 세션까지 '남은 화면'으로 밀려 있던 9종을 이번에 전부 채웠다.
- **거래명세서 785건 전량**(1월130·2월99·3월115·4월110·5월92·6월112·7월114·8월13, 실패 0).
  `download_intake`가 내용판별로 stmt 분류 → PDF 사본 37개 → `erp_docs_check` 재대조 →
  `settlement_completion --sync` 객관완료 122건 DB 동기화 → 합성검증 ALL GREEN.
- ★ **ec5 자동화 요령(중요, 다음 세션이 그대로 쓸 것)**
  · **세션 키를 포함한 주소(`?w_flag=1&ec_req_sid=…`)로 새로고침하면 로그인이 유지된다.**
    세션 키 없는 진입 주소(`/ec5/view/erp?w_flag=1`)로 가면 **세션이 끊긴다** — 오늘 두 번 겪었다.
  · 메뉴 이동·검색·Excel 버튼은 **JS `.click()` 으로 다 된다**(사이트맵은 `.wrapper-sitemap`에
    `visible` 클래스를 직접 붙이면 열린다). 월 선택 드롭다운도 JS 클릭이 먹는다 —
    "실제 클릭만 먹힌다"는 기존 기록은 사이트맵 오버레이가 가로채던 것이 원인이었다.
  · 화면마다 Excel 버튼 id 가 `#outputExcel` 또는 `#excel` 두 가지다. 둘 다 찾아야 한다.
  · 계정 검색조건 미지정 경고 다이얼로그는 **'취소'** 를 눌러야 조회가 진행된다.
  · **거래명세서인쇄는 100건 선택 제한** — 25건 배치로 끊고, 인쇄 미리보기가 **다 그려질
    때까지 기다린 뒤** Excel 을 눌러야 파일이 생긴다(길이가 두 번 연속 같으면 완료).
    99건 미리보기를 10초만 기다리고 닫으면 파일이 안 만들어지고 렌더러가 오래 붙잡힌다.
- **버그 3건 수정**: ① `convert_dump` 가 내용 무변경 재수집의 `captured_at` 을 안 남겨 같은
  글을 무한 재수집(단조증가로 기록) ② `recheck_plan` 이 7월 옛 덤프의 capturedAt 을 재수집
  완료로 오판(시대 기준 ERA_MS=2026-08-04 도입) ③ `erp_pdf_export` 가 파일 37개를 한 번에
  PowerShell 로 넘겨 **WinError 206**(명령줄 32KB 한계) — 10개씩 분할.
- **정산 리스트에서 완료 확인건 숨김**: `renderSettle` 이 상태가 `완료(`로 시작하는 건을
  기본 목록에서 제외한다. 상태 필터로 그 상태를 직접 고르면 여전히 보인다.
- **자동화후보 일일검색 신설**: `auto_scout.py` + 작업 스케줄러 **쿠팡업무_자동화후보검색
  매일 10:00**. 프로젝트 반복업무 8종(밴드 수집·이카운트 연동·엑셀 서식보존·카톡 파싱·
  로그인 크롬 자동화·홈택스·폴더 감시·스케줄러)으로 GitHub 를 훑어 `reports/자동화_후보.md`
  에 ★신규 표시. 비인증 API(분당 10회 제한 회피용 7초 간격)라 비밀키 불필요, 실패해도 exit 0.
- **밴드**: 두 밴드 모두 **구멍 0** 달성(AS 56·매출처 10 소거, 캐시 1173→1229). 재수집 잔여는
  AS 821·매출처 178. 저녁 이후 밴드 상세 iframe 이 **전혀 렌더되지 않는다**(60건 8분에 0건,
  fetch 는 200 이나 SPA 셸만 옴) — 오늘 1,500건 넘게 훑은 뒤라 **밴드 쪽 스로틀로 판단**하고
  중단했다. 다음 세션에서 시간을 두고 재개할 것. 무리해서 계속 두드리지 말 것.
- ★ **ERP 와 밴드를 동시에 돌리지 말 것** — 같은 브라우저에서 병행하면 서로 굶어 둘 다 멈춘다
  (오늘 실측: 병행 시 밴드 25/100, ERP 0건). 하나씩 단독으로 돌린다.
- (같은 날 밤) **전체 갱신 완료** — 사용자 지시 "문제 전부 해결하고 엑셀·앱·DB 업데이트".
  · daily_run 전체 대조(약 70분) 후 **엑셀 강제 반영 1,541셀 → v436**, 반영 대기 **0건**,
    구조 시트 5종(23·24·25·27·28) 갱신, 합성검증 ALL GREEN.
  · daily_run FAIL 3단계(미발행 감시·불일치 진단·확인필요현황)는 **전부 타임아웃(600초)**이
    원인 — 원본이 늘어 10분을 넘긴 것. 단독 재실행 전부 성공 확인 후 timeout=1500 으로 상향.
  · **카톡 내보내기 경과 경고 신설**(1일 초과 시): `kakao/export_watch.py`(daily_run 단계)가
    reports/카톡_내보내기_경과.json 을 남기고, 대표보고 '3. 리스크'(app_server)와
    아침 브리핑 머리(daily_brief)에 "카톡 내보내기 오래됨 — 갱신 필요"가 뜬다.
  · 주의: `ledger_db --apply` 는 스스로 ledger 점유를 잡는다 — **미리 ai_claim 으로 잡아 두면
    반영이 실패한다**(오늘 실측). 반영 전에 점유를 잡지 말 것.
  · 앱 서버는 코드 반영을 위해 재시작함(카톡 경고 활성). 죽으면 워치독 30분 주기가 살린다.
  · git push 는 자동 승인이 막는다 — 사용자가 터미널에서 직접 실행해야 폰에서 보인다.
- (같은 날 밤 2차) **ERP 거래명세서 인쇄본이 아무도 안 읽히고 있었다 — `stmt_docs.py` 신설.**
  오늘 785건을 받았는데 `erp_docs_check` 는 "거래명세서 **0건**"으로 봤다. 원인 둘:
  ① 같은 'stmt' 분류지만 **레이아웃이 다르다** — 거래명세서 *현황*(표: 일자-번호|거래처|
     이메일|공급가액)과 *인쇄본*(ESD007E, 전표 블록이 세로로 반복)은 별개 파서가 필요하다.
  ② 인쇄본의 거래처는 쿠팡로지스틱스가 아니라 **캠프명**(송파5캠프·시흥2MB…)이라
     CUST 필터로 거르면 0건이 된다.
  → `stmt_docs.py` 가 블록을 뜯어 **790건·61억원**을 구조화(reports/거래명세서_상세.json)
    하고 원장 06시트와 대조한다(reports/거래명세서_대조.md). daily_run 상시 단계로 연결.
  ★ **전표번호 정규화 함정**: ERP 는 `20260126-7`, 원장은 `2026/07/01-4`, 게다가 원장에는
    `2026/07-01-2` 처럼 구분자가 밀린 손입력도 있다. **구분자를 믿지 말고 숫자만 뽑아**
    앞 8자리=날짜·마지막 덩어리=번호로 정규화한다. 이걸 안 하면 매칭이 0으로 나온다
    (첫 실행에서 실제로 0이 나왔다). 현재 원장 65개 중 **41개 일치**.
- ★ **GitHub Actions 는 넣지 말 것**(2026-08-04 시도·철회). PC 없이 돌리려고 워크플로를
  추가했다가 합성검증에 막혔다 — 절대규칙 11 때문에 `.github/workflows` **존재 자체**가
  금지다(2026-07-27 메일 발송 제거 후의 안전장치). 내용이 메일과 무관해도 예외 없다.

### 2026-08-05 대표보고 전수 교정·정기점검 예측·레고형 대시보드 (Codex)
- 대표보고 `1. 당일 업무 실적` 3개 카드가 엑셀의 오래된 수식 캐시(신규 8·완료 3·
  분기 93%)를 그대로 보여 주던 원인을 제거했다. 2026-08-04 실자료 재계산은 돌발AS
  **중복 제거 신규 3건·신규 중 처리완료 0건(0%)**, 정기점검 **완료 95/전체 302대
  = 31%**, 기간 경과 38%보다 7%p 뒤, 기준일까지 94/124대=76%다. 같은 캠프·같은 달의
  완료 한 건을 다른 날짜 일정에도 복제하던 7개 캠프 연결 오류까지 날짜·기사로 좁혔다.
- 류지영 정기점검 원본 355행에서 제외 53행을 빼고 125그룹·302대를 정본 분모로 삼았다.
  공식 날짜 42그룹 외 83그룹은 동일 장비 과거 점검일로 예측해 앱 캘린더에 `예측`·근거·
  신뢰도로 표시한다. 이력 없는 3그룹은 담당기사/전체 일자 중앙값을 **낮음**으로 표시하며,
  Google Calendar에는 자동 저장하지 않는다. 거래서류는 ERP·PO·명세서·세금계산서·밴드
  객관근거를 합친 정산 원천으로 재집계해 8월 4일 카드 5종은 0건, 명세서 대기는
  **1/675건**이다. 모든 요청 지표에 기준일 `(08-04)`가 붙는다.
- 대시보드에 외부 CDN 없이 12열 CSS Grid 기반 `내 대시보드` 편집기를 추가했다.
  12개 블록을 체크박스로 추가·숨김, 손잡이 드래그 또는 키보드/터치용 위·아래 버튼으로
  재배치, 전체·2/3·절반·1/3 너비 변경, 전체 표시·기본 복원이 가능하다. 구성은 버전 있는
  `localStorage`에 저장되고 다른 탭과 동기화되며 720px 이하는 자동 1열이다. GridStack의
  save/load·12열 모델, MDN CSS Grid/Web Storage, W3C·GitHub 접근성 권고를 반영했다.
  데스크톱·375px 모바일·숨김/재추가·순서·크기·새로고침 복원을 브라우저 실측했고 전체
  `synthetic_check.py`는 **ALL GREEN**이다.

### 2026-08-05 새벽 야간 자율작업 — 명세서 색인·0원 해소 (Claude)
사용자 지시 ①"밴드·ERP 아직 대조/조사 안 된 것, 이미지·PDF·엑셀 다 가져와 정리"(취침 전)
②"금액 0원인 건들 다른 방법으로 찾아서 반영해"(새벽, 정산 화면 캡처).
- **화면 잠금 중엔 브라우저 수집이 불가능하다**(실측): 탭이 hidden 이면 크롬이 타이머를
  분당 1회로 줄여 밴드 상세 iframe·ec5 SPA 렌더가 모두 멈춘다. 어제 저녁 "밴드 스로틀"
  판단의 실체도 대부분 이것이었다(창 최소화). **밤샘 수집을 시키려면 화면 잠금 해제 +
  크롬 창 표시가 전제**다 — 다음에 야간 수집 지시가 오면 이걸 먼저 안내할 것.
- **stmt_link.py 신설** — 명세서 인쇄본 790건에 판매조회 UJ코드를 붙인 색인.
  3단계(합계+일자 유일 → 합계+거래처(캠프) → 품목 단위 유일)로 **확정 593건·모호 0**.
  짝 없음 197건(쿠팡계 127·일반 70, 18.1억)은 묶음 발행·기간 밖으로 추정 — 추측 매칭 안 함.
  산출: reports/명세서_프로젝트_매칭.json/md. ※ stmt_docs 라벨은 '수신-거래처'였다
  (cp949 깨진 출력을 '부서-거래처'로 잘못 읽어 한 번 헛디딤) — 790/790 거래처 채움.
- **정산 0원 표시 해소**: 실제작업공급가액(03시트 수식 대기)이 비면 서버가
  **거래명세서합계로 대신 표시**(app_server.real_settlements, `금액출처=명세서` 표식,
  카드에 '명세서 기준' 뱃지). 0원 682→**5건**(근거 없는 5건만 정직하게 0). 엑셀 불변.
- 밴드 사진 `--all` 49장 신규 저장, doc_ocr 재실행(일치 139). PDF 텍스트 추출은
  라이브러리 없음 + ERP PDF 는 원본 xlsx 가 이미 텍스트라 불필요 판단.

### 2026-08-04 (4차) 대표 지시 3항목 — 보고 카테고리 카드·캡처 직접 반영 (Claude)
- 사용자 지적 "유수비가 이야기한 사항 반영 하나도 안되어있음": 8-3차에서 브리핑 블록·
  아래쪽 별도 섹션에만 넣었고 **대표가 보는 '1. 당일 업무 실적' 카드 3장에는 없었다.**
  `_augment_exec_daily`(app_server) 신설 — 서버가 보고 시점에 daily_brief·정산 집계로
  계산해 카드에 얹는다(DB-only, 엑셀에 안 씀). 화면·캡처 PNG·폰 data.enc 공통.
  · 돌발 AS·현장: `미실시 핫이슈(일정·사유) N건`(0이어도 표시 — '없다'를 보고할 수 있게)
  · 정기점검: `분기 진행률 93%` + `기간 경과 37% 대비 +56%p 앞섬`(뒤지면 빨강)
  · 거래서류·청구: `명세서 발행 대기(완료 기준) 1건/761건` — **모수 = 작업완료(유상
    정산) 기준, 사용자 확정 2026-08-04**(기존 '거래명세서 미발행' 판정 로직 재사용).
  항목 3원소째는 강조색 — execHtml(화면)·groupCols(캡처)가 같이 읽는다.
- **캡처 누락 버그 수정**: BRIEF 로딩 전에 캡처하면 핫이슈·경과율 블록이 말없이 통째로
  빠진 PNG가 저장됐다(대표가 본 게 이것일 가능성). captureReport/shareReport 에
  `waitBrief()`(최대 10초 폴링) 추가. 실서버 재시작·캡처 PNG 실측으로 반영 확인.
- **밴드 상세 수집 — 오늘은 글당 로딩이 매우 느릴 뿐 수집은 된다**: 상세 페이지가
  main thread 를 오래 점유해 CDP 45초 안에 안 끝나 "얼어 보였다". **비동기 배치
  (`__batch` fire-and-forget) + 나중 폴링**으로 우회하면 정상 수집된다(58건 12분 실측).
  → AS밴드 5360~5424 **65/65 전량 확보 완료**(구멍 11건 포함, 실패 0), 밴드대조
  미확인 11→9, 02 사진등록 443건 DB 큐(15:00 회차로 반영 완료). 이어서 구멍 구간
  batch2·3(4231~4828)로 **117건 추가 수집**(실패 0) — 캐시 1072→1173건,
  잔여: AS밴드 구멍 56·재수집 전 603 / 매출처 10·145. 수집기 정본 `band/grab_posts.js`,
  남은 번호는 `band/recheck_plan.py --band 90610953 --limit 60`이 그대로 뽑아 준다.
  ※ 구멍 번호도 대부분 실제 글이었다(삭제 아님) — 계속 훑을 가치가 있다.
  ※ 상세 수집분 일부(공지형 DOM)는 author/timeText 가 안 잡혀 created_at 이 없다 —
    본문·프로젝트NO는 온전해 대조에는 지장 없음. 다음 세션에서 셀렉터 보강 여지.

### 2026-08-04 업무센터 iOS 개편 + 저장소 정리 (Claude/Sol)
- 사용자 지시 "업무센터 디자인 애플 아이폰 타입으로 변경": `#v-center` 범위로만
  아이폰 설정·연락처 감각을 입혔다 — 큰 제목(31px), 담당자 아바타 연락처 카드
  (류지영 파랑·오종현 초록 그라데이션), 현황 줄을 회색 풋노트 필로, 버튼은
  틴트(옅은 파랑 배경·파란 글자)/채움 두 단계, 섹션 제목은 카드 밖 그룹 헤더로,
  드롭존·URL 입력도 iOS 필드 스타일. 구조·ID·JS(renderCenter 등)는 그대로라
  다른 화면과 공용 컴포넌트(renderReceiptUpload)는 영향 없다. 브라우저 실측으로
  적용 확인(콘솔 오류 0).
- 저장소 정리: `db/share_pull.json`(share_intake 런타임 상태)·`outputs/`(재생성
  가능한 분석 스크래치)를 .gitignore 에 추가 — Sol 검토 관문이 미커밋으로 막히던
  원인 제거. 시작 시 입력 큐 500건을 DB로 흡수(반영 대기 504건, 11:00 회차 예정).
- (같은 날 2차) **리모컨 공지(2026-08-04 사내 공지) 시스템 반영**: 불출 기록에
  **불출 일자(issued_on)·투입 예정 캠프명(camp)** 열 추가(remote_issue 마이그레이션,
  기존 DB ALTER 자동), remote_request/API/폼이 함께 받는다. 업무센터 리모컨 카드에
  '최근 불출' 표(불출일·지점·기사·투입 캠프·수량)와 공지 규칙 문구(실물 수량 기준
  실사 · 류지영 최종 취합) 추가. '오늘 확인할 업무' 체크리스트에 리모컨 항목 신설
  — 류지영(재고·불출·납품 공유분 취합 확인)·오종현(불출·납품 기록 확인). 검증 [98]
  확장(issued_on·camp 왕복) `ALL GREEN`. 담당자 입력 유휴 확인 후 서버 재시작 배포.
  참고: 공지의 '실시간 공유'는 이 앱 입력이 곧 공유다 — 지점 담당자가 불출 즉시
  기록하면 류지영 화면·대표보고에 같은 DB가 바로 보인다.
- (같은 날 3차) **밴드·ERP 전량 수집 → 빨간 상태 재판정** (사용자 지시 "빨간색 상태
  밴드·ERP 권한으로 전부 다운로드 받아 정리, ERP는 엑셀로 저장하고 전체 반영"):
  · **밴드** — 피드 무한스크롤이 10글에서 멈추는 레이아웃이라(점진 스크롤도 실패)
    **게시글 상세를 숨은 iframe으로 하나씩 여는 방식**으로 우회했다. 상세 페이지는
    '…더보기' 없이 전문이 나와 피드 긁기보다 품질이 좋다. AS밴드 5400~5409 신규 10글
    수집(캐시 1052→1062), 매출처업무밴드는 최신 3536까지 이미 보유해 신규 없음.
    문서사진 36장 추가 수집·OCR, 밴드대조 확인 846/미확인 11, 02 사진등록 큐 435건.
  · **ERP 엑셀 6종** — 판매조회(2026 전기간)·(세금)계산서진행단계(242행, 미발행 6)·
    홈택스 전자(세금)계산서(1/1~8/4 전체, 31쪽)·매출(세금)계산서요약·매출청구서현황·
    **거래처별계정별원장(쿠팡로지스틱스 2026/01~08, 전표 286건)**. 모두 Downloads →
    `download_intake --apply` 로 내용판별 흡수(sales/taxinv/hometax/tax/ledger).
  · **재판정 결과(실측)**: 정산 750건 중 **금액 재계산 대기 112 → 40건**(견적 원본확인·
    담당자확인 66 + ERP 전표 금액확인 6 = 72건이 객관근거로 완료 전환, resolution
    348→414건). **세금계산서 미발행은 153건 그대로** — 새 ERP·홈택스 자료에도 발행
    흔적이 없어 '진짜 미발행'임이 재확인됐다(발행 조치 대상, 경과 리포트가 우선순위).
    ERP원장대조 A94·B56·C5·D6, 확인필요 통합 547건.
  · ERP 조작 요령 추가: ec5 는 **F3 로 조회조건 패널**이 열리고, 월 선택은 커스텀
    셀렉트라 JS click 이 안 먹는다(실제 클릭 필요). 화면좌표는 CSS px 와 다르다
    (스크린샷 1568 : 뷰포트 1440) — **JS 좌표를 그대로 클릭에 쓰면 엉뚱한 곳을 누른다**.
- (같은 날 4차) **카톡 정리 + 리모컨 실재고 DB 이관** (사용자 지시): 카톡 정기점검방·
  돌발점검방 내보내기 2개(08:30 기준)를 투입함으로 흡수 → 대조 **확인 857·미확인 0**.
  리모컨은 사용자 제공 재고표·입출고 관리대장을 `remote_seed.py`(재실행 안전)로 DB에
  옮겼다 — 실측이 스키마와 두 곳에서 어긋나 함께 고쳤다.
  · **한도 초과가 실재한다**: 김필우·김준형 각 9개(한도 3). 개시 잔량을 거부하면
    시스템이 현실과 어긋나므로 `remote_open_balance()`(상태 `기초보유`)로 사실대로
    받고, **새 불출은 여전히 한도가 막는다**. `over_limit`으로 화면에 경고를 띄운다.
    기초보유는 지점 재고에서 빼지 않는다(기초 재고표가 이미 지급 후 net 값이라 이중차감).
  · **버전·처리유형 추가**: remote_stock/issue/delivery 에 `version`(미확인·기존형·
    VER.3·VER.4), delivery 에 `kind`(납품·사용·교체), stock 에 `moved_on`. 증평이
    기존형 20 + VER.4 49 로 갈려 있어 합계만으로는 어느 물건인지 알 수 없었다.
  · 반영 결과가 재고표 요약과 일치: 개인 보유 21 · 지점 재고 90(부산 21·시화 0·증평 69)
    · 전체 111. 담당자 손을 거친 건 납품(보유 차감), 공장→현장 택배·샘플은 재고 차감으로
    구분했다. 검증 [98] 확장(기초보유·버전별·한도) `ALL GREEN`.
- (같은 날 5차) **ERP 산출물 PDF 사본 체계** (사용자 지시 "ERP 모든 데이터 PDF/이미지로
  저장하고 원본 저장소에 전부 반영"): `erp_pdf_export.py` 신설 — 설치된 Excel 을
  **PowerShell COM**(pywin32 없이)으로 띄워 ExportAsFixedFormat 한다. 가로·폭 1장 맞춤
  (ERP 표는 열이 많아 세로로 뽑으면 잘린다). 저장은 원본과 같은 날짜 폴더 아래 `PDF/`,
  이름은 `<종류>_<원본명>.pdf`(ERP 다운로드 이름이 무작위라 inbox_scan 분류를 붙였다).
  **ERP 원본 24개 전량 PDF 사본 완료(18.9MB)** — 판매조회 19쪽 등 정상 생성 확인.
  · daily_run 6.87 단계로 연결 — 앞으로 받는 ERP 파일은 자동으로 PDF 사본이 생긴다
    (새 파일만 변환, 대상 0개면 Excel 을 띄우지도 않는다).
  · `download_intake` 보완: 내용 판별이 안 되는 ERP 화면(요약표 등)도 **파일명이 ERP
    다운로드 형태(무작위 대문자 12~20자·ETA002R 같은 프로그램코드)일 때만** 흡수한다
    — Downloads 의 개인 파일을 Z: 로 쓸어 담지 않기 위한 제한이다. 검증 [100] 신설.
  · 이번 추가 수집: 견적서조회 2026 전기간(31쪽·1MB), 매출계산서요약. **데이터센터
    일괄 내보내기는 이 계정 권한없음** — 화면별 수집이 유일한 경로다.
- (같은 날 8차) **대표 보고 통화 반영 — 진행 속도(%)·미실시 사유 성격 구분**:
  · ★ 대표 지시: "정기점검은 이번 분기에 **일수를 따졌을 때** 몇 % 진행됐고 이상이
    있는지". 진행률만으로는 빠른지 늦은지 모른다 → `분기경과율`(기간이 몇 % 지났나)과
    `분기진행격차`를 브리핑에 넣고, 화면·캡처에 "기간 경과 37% 대비 진행 93% — 56%p
    앞섬"처럼 나란히 보여 준다(늦으면 주황).
  · ★★ 대표 지시(핵심): "부품이 안 와서·합의된 일정이라 못 한 건 괜찮다. **기사가
    스케줄을 못 잡아 미루는 건 절대 안 된다.** 조만간 교체 예정 — 이런 게 있으면 안 돼."
    → `work_log_sync._reason_blocker()` 신설: 미실시 사유를 **책임 소재**로 가른다.
    판정 순서가 핵심 — "모터가 며칠 전에 **도착**, 방문 일정 조율 중"은 자재 대기가
    아니라 **스케줄 문제**다(물건은 이미 왔다). 기존 `_reason_group`은 '모터'만 보고
    자재 대기로 넣어 이런 건을 정당한 사유처럼 보이게 했다.
    · 확인 필요(핫이슈): 일정 미확정 · 사유 불명확 · 사유 미기재
    · 정당: 자재·부품 대기(미도착) · 합의·차기 점검 · 현장 사정 · 일정 확정(날짜 명시)
    실측 결과 **미실시 18건 중 12건이 핫이슈**(일정 미확정 8 · 사유 불명확 4)였다.
    화면은 붉은 블록으로 프로젝트NO·캠프·원문 사유까지 5건 미리보기, 캡처도 동일.
  · 대표가 지목한 UJ2601379(남양주1MB 02호기)는 유휴 해제로 이상 발견에서 제거 완료.
- (같은 날 7차) **통화(김미영)로 확정된 업무 규칙 + 밴드 수정글 감지**:
  · ★ **밴드 글은 수정된다** — 상태가 바뀌면 같은 글의 본문·사진을 고쳐 다시 올린다.
    기존 `convert_dump` 병합 규칙("본문이 긴 쪽을 남긴다")은 **짧아지는 수정과 같은
    길이의 내용 변경을 통째로 놓쳤다.** 수집시각(capturedAt)이 더 최신이고 내용이
    다르면 교체하되, '…더보기'로 잘린 피드 수집분이 상세 전문을 덮지 않게 막았다.
    바뀐 글은 `reports/밴드_수정글.json`에 남겨 대조·사진수집이 다시 훑는다.
  · **입금 주기(구두 확인)**: 계산서 발행일 기준 10일자 → 익월 11일, 20일자 → 20일,
    말일자 → 말일 입금. 실질은 **발행 후 약 60일(두 달) 말일**에 대부분 들어온다.
    2026년 1월 발행분이 3월 입금 — 현재 **1~5월 발행분까지 입금**, 6·7월분은 미도래.
    ※ 원장 '청구일·지급예정일' 칸은 **여전히 비운다**(대기 항목 7). 구두 규칙이고
      "거의 다"라는 단서가 있어 확정 규칙으로 날짜를 만들지 않는다 — 추정 표시까지만.
  · **계산서는 PO 단위 묶음 발행**(거래명세서는 건당). 그래서 건별 거래명세서를
    계산서와 1:1로 찾을 수 없고, **PO번호로 묶어야** 매칭된다. ERP 판매조회에서
    창고(쿠팡_돌발AS·쿠팡_정기점검 등) + 기간 전체로 뽑으면 PO번호별로 갈린다.
  · **입금내역 원본은 김미영이 은행에서 받아 `16.Share` 공유폴더에 넣어 준다** —
    `share_intake`가 매 회차 자동으로 가져오므로 손으로 옮기지 않는다.
  · 리모컨: 기사가 3~5개씩 들고 다니며 교체하면 **돌발방(밴드)에 글이 올라온다** —
    그 글이 곧 불출·사용의 1차 근거다(사람이 전화로 물어 모으는 일을 없애는 것이 목표).
- (같은 날 6차) **10:30 대표보고 대비 일괄 반영 + 리모컨 화면 통합**:
  · 엑셀 강제 반영 **1,403셀(v378→v399)** — 09:31·09:50 두 회차, 대기 0건. 입력
    보호시간(08:00~09:30) 가드에 한 번 막혔고 해제 직후 통과시켰다.
  · **대표보고 '4. 리모컨 현황'** 신설 — 01_대표보고 시트가 아니라 **서버가 DB에서**
    리스크 섹션 바로 뒤에 끼워 넣는다(`_append_remote_section`). 타일 값은 숫자만
    두고 부연은 lines 로 내렸다(긴 값이 타일 안에서 두세 줄로 접혀 지저분했다).
  · **리모컨 = 사이드바 '돌발 AS 바로 아래' 독립 화면(`v-remote`)**. 재고 등록·불출·
    납품 입력 3종과 보유/불출 기준, 최근 이력, **이미지 저장(캔버스 직접 그리기)·
    엑셀(CSV) 저장**을 한 화면에 모았다. 관리자 업무센터 탭은 요약 한 줄 + 바로가기만
    남기고, 류지영·오종현 업무센터에는 같은 카드(+저장 버튼)를 둔다.
  · 버그 2건: ① `renderRemoteCard` 에서 `bs` 를 선언 전에 써 ReferenceError → **카드가
    통째로 비어 보였다**(사용자가 화면으로 발견). 선언 순서를 고치고 렌더를 try/catch 로
    감싸 실패가 화면에 드러나게 했다. ② 함수 분리 때 `hostId` 가 끊겨 폼이 안 그려지던
    것도 인자로 넘겨 해결. ③ 이슈 상세 카드가 좁은 칸에서 잘리던 것(grid minmax 최소폭)
    수정 — minmax(0,…) + `min-width:0`.
  · **엑셀 수동 개방 제거**: `excel_recalc` 의 사람 해시 승인은 버전마다 만료돼 실제로는
    한 번도 안 돌고 있었다(승인이 v353용, 현재 v399). 승인이 보증하던 것을 **자동 점검**
    (ZIP 무결성·시트 XML 파싱·**직접 순환참조 0**)으로 대체해 통과 시 자동 발급한다.
    v399→v400 재계산으로 **앱 '엑셀을 열어 주세요' 대기 2→0건**. 실패하면 예전처럼 막는다.
  · 판매조회 최신본(E91RXX1FJ7KAKFP, 1,032행) 흡수·PDF 사본·ERP서류대조·완료동기화·
    미발행 감시까지 반영. resolution 후보 122건 불변, 미발행 153건 불변.
  · 남은 화면(다음 세션): 수금현황·거래처별채권 최신·구매조회·계정별원장·분개장·
    일월계표·재무상태표·손익계산서·합계잔액시산표. ec5 UI 는 화면마다 기본기간이 달라
    (전월+금월·최근30일) F3→월 선택을 **실제 클릭**으로 해야 하고, 사이트맵 오버레이가
    열려 있으면 좌표 클릭을 가로챈다 — 열었으면 먼저 닫을 것.

### 2026-08-03 ERP 전표 금액확인 완료·미발행 경과 감시·리모컨 관리 (Claude/Sol)
- 사용자 지시 "금액 재계산 대기 ERP 등 모든 자료 찾아 완료 처리"에 따라
  `settlement_completion`에 세 번째 근거 클래스 `완료(ERP 전표 금액확인)`을 추가했다.
  견적서가 없어도 같은 프로젝트 ERP 판매전표의 공급가액(또는 부가세포함 합계)이
  06 거래명세서합계와 **유일하게** 일치하고 전표 PO가 원장 PO와 모순되지 않는 건만
  완료한다. 실데이터 신규 완료 6건(후보 8건 중 PO 불일치 2건은 엄격 기준으로 제외).
  `resolution` 342→348건, 담당자 재동기화 결과 류지영 완료 1,250·오종현 96·유현민 45건.
- 사용자 지시 "오래된 세금계산서 미발행 잡아내"에 따라 `tax_invoice_watch.py`를 신설,
  daily_run 상시 단계로 연결했다. 미발행 153건을 작업완료일 경과 순으로 정렬해
  `reports/세금계산서_미발행_경과.md`(+json)로 쓰고 앱 '보고' 탭에 노출한다.
  현재 분포: 90일 초과 21 · 61~90일 14 · 31~60일 66 · 30일 이하 52. 발행일은 지어내지
  않으며 발행 사실은 ERP 원본이 들어와야 객관완료가 잡는다.
- 사용자 지시로 **리모컨 불출·납품 추적**을 DB 정본(`remote_issue`·`remote_delivery`)으로
  구현했다. 규칙: 분출 담당 부산=오종현·시화=안은숙·증평=류지영, AS 담당자당 보유
  최대 3개(승인대기 예약 포함), **분출 전 부사장(관리자) 승인 필수**, 납품은 보유 한도
  안에서 프로젝트NO/캠프 단위로 기록한다. 류지영·오종현 업무센터에 같은 카드가 들어가고
  관리자 업무센터 탭에서 승인한다. API `/api/remote/status|request|approve|deliver`.
- UX 기록 기반 개선: `/api/status`가 1,550회 호출 평균 5.2초(누적 8,067초)로 최다 병목 —
  TTL 만료 시 옛 값을 즉시 돌려주고 백그라운드 스레드 한 개만 재계산하는 서버측
  stale-while-revalidate 로 바꿨다. 남은 관찰: 프런트 promise 오류 236건·`/api/tasklog`
  평균 2.5초는 다음 개선 후보다.
- 합성검증 [97] 확장(ERP 전표 근거·미발행 감시)·[98] 신설(리모컨 규칙) 포함 `ALL GREEN`.
- (같은 날 2차) **통계 화면**: 기록 탭 상단에 통계 카드 — 정산 상태 분포·현장업무
  총/완료/잔여·담당자 객관완료·미발행 경과 버킷·리모컨 보유. `/api/tax-overdue` 신설.
  promise 오류 기록에 사유를 남기게 고쳤다(사유 없는 236건 재발 방지). 오늘 자료는
  투입함·다운로드 0건, daily_run 신규 채움 435건이 DB에 있고 15:00 회차에 엑셀로 들어간다.
- (같은 날 3차) **리모컨 개정**: 승인 단계 제거 — 불출 즉시 기록(3개 한도·지점 담당·
  납품 추적 유지), 카드 iOS 스타일, 대표보고 캡처에 리모컨 현황 섹션, 류지영(증평)·
  오종현(부산) 업무센터 체크리스트에 리모컨 항목 추가.
- (같은 날 5차) **ERP 회계 I·II 권한 열림 확인 + 회계 원본 첫 수집** (사용자 지시
  "ERP 회계2 포함 전부 긁어와 반영"): 크롬 로그인 세션으로 확인한 결과 ① **회계 II는
  전 화면 데이터 0건**(채권·수입비용 모듈 미사용 — 긁어올 것이 없음) ② 회계 I은 접근
  가능해져(과거 '권한없음' 기록과 다름) **매출(세금)계산서현황 2026 전기간 253행**과
  **거래처별계정별원장(쿠팡, 01-01~08-03)**을 처음으로 내려받아 Z:에 흡수했다.
  '회계2' 밴드는 없음(가입 밴드 전수 확인). inbox_scan 분류 버그 수정 — 쿠팡매출청구서
  현황 양식에 '내역보기' 열이 있어 tax 가 taxinv 로 오분류되던 것을 일자-No.+매출부가세
  지문 우선으로 고침(erp_docs_check 가 원본을 다시 찾는다). ERP서류대조 갱신
  (2026/04~07 월별), ERP원장대조 A93·B56·C5·D6. 남은 원천: 세무>홈택스 전자(세금)계산서
  내보내기(다음 수집 후보), 밴드 신규 수집은 band_auth 1회 인증 대기.
- (같은 날 5차) 대시보드 명확화: 업무센터 카드의 '확인·진행 N건'을 **'미완료 N건 ·
  눌러서 목록'**으로 바꾸고 설명 한 줄('미완료 = 진행 중 + 확인 필요')을 붙였다.
- (같은 날 7차) **16.Share 공유폴더 상시 수집 + 절전 복구**: `share_intake.py` 신설 —
  `Z:\16. Share\유현민\오종현`을 매 회차 복사 동기화(원본 보존, PO모음·입금내역
  하위폴더는 PO_DIRS/RECEIPT_DIRS 정본이라 제외). 루트 신규 5건 흡수. PO 원본 전체
  재스캔(견적서 449건·PO 66개). 절전으로 밀린 15:00 회차를 유예(15:45) 내에 잡아
  870셀 반영, v371~v378(인수인계 예약 8건 소진). 4원천 검증에 기간·대조시각 표시.
  검증 [99] 신설 ALL GREEN. 입금 파일은 7/28본 그대로라 묶음입금 유일매칭 여전히 0.
- (같은 날 6차) **세금계산서 미발행 153건 전수조사 + ERP 권한 항목 추가 수집**:
  ① 원본 3종 추가 수집·Z: 저장 — 홈택스 전자(세금)계산서 리스트(ETAX102M, 매출
  1/1~8/3, 51행·쿠팡 수신 12), 판매조회 최신본(1/1~8/3 전체), (세금)계산서진행단계
  실측 **전체 242 · 미발행 7건**(ERP 정본). ② 교차 분석 결과: 홈택스 개별 금액
  유일일치 1건은 공급가액/합계 기준 불일치로 근거 불채택. ERP는 여러 건을 한 장으로
  묶어 발행하므로 개별 금액 일치로는 입증 불가 — 잔여 153건은 ERP에 발행 흔적이
  없는 **진짜 미발행**(발행 조치 대상)이며, 경과 리포트가 우선순위를 제공한다.
  발행되면 기존 알고리즘(6.발행→입금대기/완료, 26시트 확정→완료)이 자동 처리한다.
  ③ findings_export·settlement/staff sync·미발행 감시 재실행 — resolution 348건 불변
  (7/31 이후 ERP 상태 변화 없음). 세무 메뉴는 여전히 권한없음(마스터 조찬희).
- (같은 날 4차) **엑셀 열림 감지 → 연기 → 닫힘 후 자동 재개** (사용자 지시, 상시):
  반영 회차에 관리대장이 사람에 의해 열려 있으면(`~$` 잠금) 기다리지 않고 즉시
  '연기'로 기록하고 다른 작업으로 전환한다. `defer_apply`가 닫힘 감시자
  (`ledger_db --resume-watch`, 3분 폴링·10시간 시한)를 하나만 띄우고, 저장(닫힘)이
  확인되면 미룬 회차를 **그 회차 이름으로** 반영한다(임의 시각 반영 아님). 감시자가
  죽어도 워치독 30분 주기의 `resume_check`(--resume-check)가 잇는다. 입력 보호시간에는
  재개하지 않는다. `--status`에 연기 회차가 표시된다. 검증 [94] 확장 `ALL GREEN`.

### 2026-08-02 금액·계산서 독립근거 완료 DB 상시 동기화 (Codex)
- 사용자 지시 "엑셀이나 DB에 근거자료가 있는 경우 금액재계산 대기, 세금계산서 미발행 건들
  모두 완료 처리"를 최신 v354와 PO 원본 캐시·25/26시트·완료 DB에 전수 적용했다.
- `금액 재계산 대기`는 거래명세서가 있다는 사실만으로 닫지 않고 **프로젝트NO·PO번호·부가세
  포함 총액**이 PO 원본 견적서와 거래명세서에서 모두 같으며 견적서가 유일한 49건만
  `완료(견적·명세서 금액확인)`으로 처리했다. 세금계산서는 25 ERP 원본과 26 계산서구성의
  `확정` 프로젝트가 직접 연결된 `JS-2601-068` 1건을 `완료(ERP 계산서 원본확인)`으로 처리했다.
- `resolution` DB는 292→342건이다. 앱 실측은 완료 336건(ERP 수금 286·금액 49·계산서 1),
  잔여 `금액 재계산 대기` **161→112건**, `세금계산서 미발행` **154→153건**이다. 잔여는
  독립 원자료가 유일 일치하지 않아 추정 완료하지 않는다.
- `settlement_completion.py --sync`를 `daily_run`의 담당자별 완료 동기화 직전에 연결했다. 이후
  새 원자료가 들어오면 같은 엄격 기준으로 자동 완료되며, 앱·확인필요 보고는 DB 완료 정본을
  우선한다. Excel 수식·작업금액·계산서 발행일은 수정하지 않았다.
- 담당자 DB 재동기화 결과는 류지영 완료 1,243건·오종현 완료 96건·유현민 완료 44건이다.
  합성검증 `[97]` 포함 `ALL GREEN`, Terra→Sol 검토 PASS.

### 2026-08-02 금액 재계산 대기·세금계산서 미발행 객관완료 전수조사 (Codex)
- 최신 v352의 `금액 재계산 대기` 301건과 `세금계산서 미발행` 154건, 합계 455건을
  ERP 판매조회(2026-01-01~07-31)·계산서진행단계·06/15/16시트와 전수 대조했다.
- 금액 재계산 대기 중 프로젝트번호가 유일하고 동일 프로젝트의 ERP 전표가 **전부
  `7.수금완료`**인 142건을 `resolution` DB에 `완료(ERP 수금확인)`으로 추가했다.
  Excel 금액 수식·세금계산서 발행일은 덮어쓰지 않는다.
- `UJ2600443`은 같은 프로젝트에 `확인`·`7.수금완료` 전표가 함께 있어 제외했다.
  같은 엄격 기준으로 과거 완료 중 `JS-2603-372` 1건도 `6.세금계산서발행`·`7.수금완료`
  혼재가 확인돼 정산·류지영 완료에서 정확한 ID로 철회했다.
- 최종 정산 객관완료 241건, 담당자 집계는 **류지영 완료 1,140건·오종현 완료 96건·
  유현민 완료 44건**이다. 잔여는 금액 재계산 대기 159·세금계산서 미발행 154·
  입금 대기 112·금액 미입력 2·미청구 1건이다.
- `erp_progress`는 같은 프로젝트의 복수 ERP 전표를 보수적으로 합치며, 전부 수금완료일 때만
  완료로 판정한다. `findings_export` 일일 자동실행이 새 근거를 계속 DB에 반영하고 현재 원본의
  완료·미완료 명시적 충돌만 정확한 정산ID로 철회한다. 합성검증 `[95]`을 강화했다.

### 2026-08-02 담당자별 객관완료 상시 판정 (Codex)
- 사용자 지시 "객관적인 자료가 확인 된 건들은 류지영 완료, 오종현 완료, 유현민 완료 처리해
  알고리즘 적용해서 계속 실행"을 DB 정본으로 구현했다. `staff_resolution`은
  `(담당자, 업무종류, 원천ID)` 단위로 상태·완료일·근거·최초 확인시각을 멱등 보관하며 상태 문구는
  `류지영 완료`·`오종현 완료`·`유현민 완료`로 고정한다. Excel 상태 셀은 건드리지 않는다.
- `staff_completion.py --sync`가 현장업무 `work_resolution`, 정산 `resolution`, PO 객관근거를
  합쳐 담당자별 완료를 동기화하고 `reports/담당자_객관완료.md`를 갱신한다. `daily_run`의 입력 DB
  적재·객관완료 동기화 직후 자동 실행하므로 이후 원천이 추가될 때마다 별도 지시 없이 계속 판정한다.
- PO 대조는 실제 수신 PO마다 오종현의 원본취합 완료를, PO·원장 금액 일치 또는 ERP/밴드 계산서
  발행 확인 건만 유현민의 시스템대조 완료와 류지영의 회계확인 완료로 남긴다. 미청구·목록부재·
  금액불일치·후보매칭은 완료하지 않는다.
- 2026-08-02 14:34 엄격 재동기화 최종값: **류지영 완료 999건**(현장업무 855 + 정산 100 +
  계산서 확인 44), **오종현 완료 96건**(수신 PO 원본), **유현민 완료 44건**(PO 시스템대조).
  최초 판정 중 PO343157·PO364923·PO367870은 ERP 계산서 금액만 같은 비유일 후보여서 류지영·
  유현민 완료 3건씩을 정확한 키로 철회했다. 다만 실제 수신 PO 원본이 있는 사실은 입증되므로
  오종현 원본취합 완료는 유지한다. 미청구 6·쿠팡목록 부재 1·금액불일치 43은 미완료 유지한다.
  재실행 결과 건수 불변, 전체 합성검증 `ALL GREEN`.
- 앱 읽기 API `/api/staff/completions`와 보고서 메뉴에서 세 담당자의 집계·근거를 확인할 수 있다.

### 2026-08-02 대기 신규행 객관완료 즉시 DB 반영 (Codex)
- 사용자 지시 "류지영, 오종현 업무 중 객관적으로 데이터가 입증될 경우 완료 처리"에 따라
  일일 에이전트 전 단계를 실행했다. 관리대장 v336·카톡·밴드·ERP·입금 원천을 대조한 결과,
  새로 완료 판정 가능한 건은 정기점검 `UJ2601362` 1건뿐이었다. 카톡 완료 원문과
  실제점검일 `2026-07-31`을 근거로 `work_resolution`에 완료 기록했다(PM 387→388,
  현장업무 객관완료 합계 854→855). 신규 `UJ2601361`(정기점검 접수)·`UJ2601363`(AS 접수)는
  완료 근거가 없어 그대로 두었다.
- `ledger_db.pending_work_completion_entries()`를 추가해 신규 업무가 아직 11:00·15:00 Excel
  반영 대기행이어도 프로젝트NO+실제 완료일+원천 evidence가 있으면 DB 완료 정본에 즉시
  기록한다. 취소·철회·AS전환·점검불가, 미래일, 근거 없는 날짜는 제외하고 중복 대기행은
  최신 열값으로 병합한다. `daily_run`은 입력 DB 적재 후 이 판정을 실행하도록 순서를 고쳤다.
- 오종현 PO/입금 대조는 객관적 신규 완료 0건이다. 거래명세서 미발행 6건, PO 목록 부재 1건,
  금액 불일치 43건은 미완료로 유지했고, 입금 70건은 묶음입금이라 정산행 유일매칭이 0건이므로
  추정 완료하지 않았다. 전체 합성검증 `ALL GREEN`.
- Excel은 강제 반영하지 않았다. 신규행과 보완 셀은 DB 대기 상태이며 정해진 15:00 회차에만
  반영한다.

### 2026-08-01 객관 근거 완료 전수처리 — 현장업무 DB 정본·앱 즉시 반영 (Codex)
- 사용자 지시 "객관적으로 확인된 자료가 있을 경우 전부 완료 처리"에 따라 밴드의
  **작업완료 게시+완료일**을 관리대장과 전수 대조했다. 날짜·상태가 모순 없는 완료 이력
  **854건(돌발AS 467·정기점검 387)**을 SQLite `work_resolution`에 근거·실제완료일·
  최초확인시각과 함께 멱등 저장했다. 2026년 앱 노출분은 AS 427·점검 341건이다.
- 완료 상태는 Excel 수식 셀을 덮지 않는다. 앱이 DB 완료 판정을 원장보다 우선해 읽어
  일괄반영 전에도 즉시 완료로 보여 준다. 새로 완료일이 확인된 건은 AS 4건
  (`UJ2601325/1352/1355/1360`)·점검 1건(`UJ2601330`)이며, 앱 집계는
  **AS 미실시 78→74·정기점검 대기 108→107**로 정정됐다. 완료보고서·사진·ERP 누락은
  완료 여부와 분리해 기존 검증 경고로 계속 남는다.
- 접수일(2026-03-03)보다 완료일(2026-02-11)이 앞선 `UJ2600307`은 원자료 충돌이라
  자동 완료에서 제외했다. 확인되지 않은 나머지 AS/점검도 숫자를 줄이려고 완료시키지 않았다.
- `confirm_fill`과 엄격 검증 `complete_verified`를 일일 자동화에 연결했다. 앞으로 새 완료
  근거가 들어오면 같은 DB 정본에 자동 반영된다. 앱은 완료일이 있는데 수식 캐시가
  `접수/미점검`으로 남은 경우도 완료일을 우선한다. 전체 합성검증 **ALL GREEN**.

### 2026-08-01 공급가액 부가세 기준 전 화면 공통 표기 (Codex)
- 사용자 지시에 따라 앱에서 공급가액을 보여 주는 대시보드·히어로·정산 KPI/목록/상세·
  월별 현황·대표보고·ERP 계산서 구성표·이미지 캡처·PC 독립 폰 사본을 모두
  **`공급가액(부가세 별도)`**로 통일했다. 상세의 `합계(VAT포함)` 표기는 유지해 두 금액의
  기준이 바로 구분된다. 내부 데이터 키·금액 계산은 건드리지 않았다.
- 고정주소 폰 사본이 옛 화면을 쥐지 않도록 서비스워커 캐시 판본을 올리되 기존 아이콘 해시
  식별 규칙은 보존했다. 합성검증 [78]에 PC 전체 공통 라벨·옛 `공급가액(원)` 잔존 금지·
  폰 사본 표기를 추가했고 전체 합성검증 **ALL GREEN**이다.

### 2026-07-31 작업 기준 폴더를 루트로 정비 — 지시문 정본 통일 (Claude)
- 작업 기준이 `ecount/` → 상위 루트(`COUPANG_INTEGRATED_WORK_AGENT/`)로 바뀌면서 루트
  `CLAUDE.md`가 옛 판(엑셀 두 번 규칙 이전, `ledger_writer --apply` 지시)으로 남아 있었다.
  → **지시문 정본은 `ecount/CLAUDE.md` 하나**로 통일하고, 루트 `CLAUDE.md`(Claude 자동 로드)·
  루트 `AGENTS.md`(Codex 자동 로드)는 그 복사본으로 만들었다. Terra→Sol 관문 절도 정본에 복원.
- `session_handoff.rule_copies()` 신설 — 세 파일이 다르면 `--check` "먼저 처리할 것"에 올린다
  (워치독 스냅샷에도 남는다). 루트 파일은 git 밖이라 이 감시가 없으면 또 조용히 낡는다.
- 작업 스케줄러 점검: 5개 모두 현 경로 정상. 단 `CSOS_유수비_대표보고_자동준비`와
  `쿠팡업무_일일자동대조`가 **같은 daily_run.bat을 09:50에 이중 실행**(락에 진 쪽이 매일
  실패코드 1을 남김) — AI 권한으로 스케줄러를 못 바꿔 사용자에게 비활성화 명령 안내함.
- `csos_cloud_queue_site/`는 자체 git 저장소인데 **원격이 없다**(이 PC 단본) — 백업 결정 대기.

### 2026-07-31 누적 지시 전수조사 — 거짓 경보 631건 제거·정산 분류기 공용화 (Claude)
사용자 지시로 그동안의 지시를 전수조사해 **실행 안 된 것**을 갈라내고 실행했다.

**실행 완료**
- **'금액 미입력 632건'은 거의 전부 오탐이었다.** 06시트 I열(실제작업공급가액)은 750행 중
  684행이 **수식**이고, 그 수식은 `IF(LEFT(원천업무ID,3)="AS-", SUMIF('03_현장작업실적'…), "")`
  이다. 즉 **정기점검(PM-) 305건은 설계상 영원히 빈칸**인데 '미입력'으로 잡고 있었다.
  더 나쁜 건 그 오탐이 if 사다리 **맨 위**에 있어서, 그 305건의 **진짜 문제(세금계산서
  미발행)를 통째로 가리고 있었다.** 남은 AS- 327건은 03시트 실적 행이 아직 없어 수식이
  0인 것으로, 재계산이 풀리면 저절로 채워진다 → '금액 재계산 대기'로 따로 부른다.
  · 결과: 금액 미입력 **632 → 3건**, 세금계산서 미발행 **54 → 359건**(가려져 있던 실제 문제)
  · ★ **I열에 값을 써넣지 말 것.** 수식을 덮으면 AE(작업대비거래명세서차액)가 항상 0이 되어
    이 시트의 존재 이유가 죽는다. `settle_status()` 주석에 못 박아 뒀다.
- **'금액 불일치 633건'도 같은 뿌리.** K열(실제작업합계)도 미재계산 수식이라 0인데,
  이걸 명세서금액과 비교해 **명세서 있는 전 건이 차액 -전액으로** 잡히고 있었다.
  → 작업금액이 계산되기 전에는 비교하지 않는다. **633 → 2건**
- **확인필요 합계 1500 → 869건** (거짓 경보 631건 제거).
- `ecount_reconcile.settle_status()`·`has_statement()` 신설 — 화면(app_server)과
  엑셀(findings_export)이 각자 들고 있던 같은 if 사다리를 한 곳으로 모았다.
  `has_statement` 는 번호만 보지 않는다: billing_fill 이 ERP 근거로 채운 **629건은
  발행일·금액은 있고 번호만 없어** 전부 '미청구(전표 없음)'로 잡히던 것을 바로잡았다.
- 앱 아이콘·버튼을 **가운데 정렬 + 좌우 꽉 차게**(업무센터 auto-fit 방식을 나머지로 확대).
  고정 칸(repeat 2·3·4·5)이 남기던 줄 끝 빈칸 제거. 규칙은 스타일 **맨 뒤**에 둬야
  기존 미디어쿼리를 덮는다 — 위로 올리지 말 것. `.rtabs` 는 overflow-x 라 가운데 정렬에서
  제외했다(넘칠 때 왼쪽이 잘려 손이 안 닿는다).
- 대시보드 **하단 '업무 바로가기'(정기점검·돌발AS)** 추가. 건수는 상단 타일과 같은 변수를 쓴다.
- **관리대장 복구 경고 원인 조사** — v328 은 XML 파싱·calcChain(55,914항목, 불일치 0)·
  definedNames(18개, 중복·#REF 0)·count 속성·행 순서·병합 **전부 정상**이다. 복구 경고를
  낼 구조적 결함이 **없다**. v303·v304 시절 경고는 이후 수정들로 이미 해소된 것으로 보인다.
  → 남은 것은 **사람이 엑셀로 한 번 열어 확인**하는 것뿐이다(아래 대기 항목).
- **밴드 게시글 1215건 성격 분류**(다음 할 일 3번). AGENTS.md 경고가 맞았다:
  **PO 통지 글 127건 + 밴드 시스템·멤버 알림 33건 = 160건은 등록 대상이 아니다.**
  본문 없음(사진만) 4건, 기타 47건. 나머지에는 **세금계산서 발행 요청·완료 공지 글**이
  다수 섞여 있다(금액·캠프·날짜 포함) — 이건 등록 대상이 아니라 **세금계산서 미발행
  359건을 푸는 근거 자료**다. 다음 세션에서 이 글들을 근거로 발행일을 채울 수 있다.

### 2026-07-31 (14) 업무센터 탭 승격·입금 업로드·헤더 종 아이콘 (Claude)
- **업무센터를 사이드바 탭으로 승격**(대시보드 바로 아래, `v-center`). 담당자 카드에
  실황 요약(류지영: 지연 AS/PM·기사 미배정 / 오종현: 미청구·미발행·입금 대기) +
  센터 열기·관련 탭 바로가기. 대시보드의 기존 업무센터 카드(`workcenter-hub`)는
  **모바일 전용**으로 남긴다(폰 입구). '업무 바로가기' 카드는 삭제 — 기능은
  사이드바·대시보드 타일이 대신한다.
- **입금·원천자료 업로드**(검증 [96] 확장): 업무센터 탭과 오종현 담당자 페이지에
  드래그앤드롭·파일선택·**URL 등록** 존. 서버 `/api/staff/receipt-upload`
  (관리자 또는 오종현만) → `_save_source_submission` 재사용: **Z: '7. 입금내역'**
  날짜 폴더 보관(URL 은 SSRF 검증 다운로드), xlsx 는 inbox 복사 후 **즉시
  `receipt_fill --queue`**(TASKS.receipt 신설) → DB 큐 → 11·15시 엑셀 반영.
  오프라인이면 outbox 가 보관(OUTBOX_PATHS 에 추가).
- **데스크톱 헤더 알림 종이 안 보이던 원인**: 모바일 남색 헤더용 흰 아이콘
  (`fill:#fff`)이 데스크톱 흰 헤더에도 적용 — ≥900px 에서 어두운 색으로 교정.
- app_server 에는 (13)의 계획대비 수정도 함께 들어 있다(이 커밋에서 같이 반영).

### 2026-07-31 (13) 계획대비 착시 수정·다운로드 흡수·탭 고도화·로고 정렬 (Claude)
- ★ **"정기점검 계획대비 +27건"은 착시였다** — 목표누계를 분기 균등 배분(대상×경과일/92일)
  으로 계산해 17이 나왔지만, 실제 일정은 7월에 몰려 있다. **점검예정일 기준**(오늘까지
  예정된 건수)으로 고치니 실상은 **-7건·적색**(예정 51 중 44 완료, 7건 지연).
  균등 배분 가정은 일정이 사람 손으로 이미 세워진 업무에는 쓰면 안 된다.
- `download_intake.py` 신설(09:50 단계): Downloads·바탕화면(OneDrive 변형 포함)의
  카톡 txt·밴드 dump_*.json·ERP xlsx(내용 판별)를 `0. 원본 자료`로 **이동**. Z: 공유라
  옮겨지는 순간 모든 PC·세션이 본다. 실적: 오늘 5건 흡수(덤프 3·ERP 2).
- 정밀 탭 고도화: **지연 N·오늘 N 빠른 필터**(예정일 기준) + 카드 **✓ 완료 처리**
  원클릭(상태=완료+완료일=오늘, confirm 후 DB 큐 2건). 실측: 지연/오늘 필터 정확,
  완료 버튼은 완료·취소 건에는 안 뜸.
- 일일보고 머리글: 가로형 유니버셜리프트 로고가 정렬을 깨던 것을 **정사각 마크**
  (universal-lift-mark.png, CSOS 아이콘과 같은 높이)로 교체.

### 2026-07-31 (12) ★ERP 브라우저 수집 성공 — 세금계산서 원천 뚫림 (Claude)
사용자가 크롬에서 이카운트 로그인(zone AB) → Claude 탭에 세션 부착, 사람 속도로 메뉴 조작.
- **이 계정 권한: 재고(판매·구매)만.** 회계 I·II / 세무 = 권한없음(마스터 조찬희).
  회계거래현황·계정별원장은 이 계정으로 영원히 불가 — 권한 요청 또는 다른 계정 필요.
- ★★ **(세금)계산서진행단계**(재고 소속, 대시보드 바로가기)가 뚫렸다:
  일자-No.·프로젝트명·**발행일자·공급가액·부가세·합계금액·종류** — 06시트가 요구하던
  바로 그 열들이다(그동안 "판매조회에 발행일 없음 → 재내보내기 필요"라던 것의 대체 원천).
  1/1~7/31 224행(쿠팡 111) 확보 → inbox_scan 이 taxinv 로 자동 분류 →
  **세금계산서 대조 0건 → 221건.**
- 함께 확보: 판매조회 1/1~7/31 1,001행(기존 0102-0726 대체) · 거래처별채권 119처
  (쿠팡로지스틱스 잔액 35.27억). 저장 위치: `1. ERP 내보내기/2026/07/2026-07-31/`.
- 수금현황(재고)은 **원천이 0건** — 수금은 회계 모듈 소관이라 이 화면에 아무도 입력 안 함.
  입금 원천은 계속 오종현 입금내역 파일이 정본(기존 진단 그대로).
- 구매조회는 전사 매입(115쪽)이라 쿠팡 업무와 무관 — 안 받음.
- ★ 브라우저 요령: ec5 는 SPA 라 메뉴가 새 탭으로 열리기도 한다. 그리드 본문은
  innerText 에 안 잡히는 화면이 많다 — **Excel 버튼이 서버 생성이라 화면과 무관하게
  전체 데이터를 준다.** 상단 메뉴 클릭 → 좌측 메뉴 → (기간) → 검색 → Excel 순서.

### 2026-07-31 (11) 밴드 브라우저 수집 재개 — 점진 스크롤은 된다 (Claude)
사용자 지시: "밴드 및 ERP 데이터 싹 다 긁어와."
- ★ **밴드 공식 API 발급 거절**(developers.band.us 확인, 07-24 신청분). 사유 비공개.
  재신청은 사용자 판단. 비공개 API 역공학은 계속 하지 않는다(기존 결정 유지).
- **브라우저 수집이 다시 됐다** — 단 방법이 다르다. `scrollTo(바닥)` 점프는 피드가 더
  안 불러오고(12개 고정), **`scrollBy(800px)` 점진 + 550ms 대기**는 계속 불러온다.
  collect_band.js 의 점프 방식은 이 레이아웃에서 죽는다 — 점진으로 바꿀 것.
  · 사진 base64 내려받기 단계는 **fetch 타임아웃이 없어 한 번 걸리면 영원히 멈춘다**
    (실제로 멈췄다). 수집은 URL만 담고, 사진은 별도 단계로 분리할 것.
- 수집 결과: AS밴드 34글(07-24~), 매출처업무밴드 8글(07-23~) → convert_dump 로
  캐시 1027→1052 · 188→193건. 대조: 확인 840·미확인 11, **02 사진등록 432건 큐 적재**.
  추출 4333건(미등록 14 — 신규 10건 눈 확인 대상, 리포트 참조).
- **ERP 는 로그인 화면에서 차단** — 자격증명 입력은 AI 가 하지 않는다. 사용자가
  크롬(Claude 탭 그룹의 이카운트 탭)에서 로그인해 주면 4종 내보내기를 이어서 한다.

### 2026-07-31 (10) 정기점검·돌발AS 정밀 관리 탭 신설 (Claude)
사용자 지시: "대시보드 아래 정기점검·돌발AS 탭 — 정밀 입력·출력 관리, 업무센터 반영,
오픈소스·유사 앱 벤치마킹 후 사용자 친화적으로."
- 벤치마킹: Grash/Atlas CMMS(오픈소스, 배정·추적)·Limble(카드·클릭 최소화)·
  MaintainX(모바일 우선) → 채택 패턴: **KPI 스트립=클릭 필터** · **저장되는 필터** ·
  **카드 인라인 편집**(기사/상태/일정) · **기사별 보드**(미배정 맨 앞) · CSV 출력.
- 새 화면 `v-pm`·`v-as`(검증 [96]): 사이드바 대시보드 바로 아래(≥900px 전용 —
  폰 하단바는 칸이 모자라 대시보드 '업무 바로가기'가 폰 입구). 류지영 업무센터에도
  두 탭 바로가기. 대시보드 타일·바로가기가 모두 새 탭으로 향한다.
- **입력은 전부 /api/input → DB 큐 → 11·15시 회차**(DB-only 원칙). 서버에
  OVERWRITE_COLS 신설: 담당기사·진행상태/점검상태·예정일·완료일만 덮어쓰기 허용,
  근거에 '앱 수정' 기록. 그 밖의 열은 예전대로 빈 칸만. — 류지영 매니저가 엑셀에서
  담당기사를 한 건씩 고치다 유실 사고(7절)가 난 **바로 그 작업의 안전한 대체 경로**다.
- 편집 즉시 화면 반영(낙관적) + '대기' 배지, 회차 반영 안내 토스트. 오프라인이면
  기존 outbox 가 받는다(/api/input 이 OUTBOX_PATHS 에 이미 있음).
- 실측(합성 데이터): KPI 집계·미배정 필터·기사별 보드(미배정 우선)·편집 필드 4종·
  내비 강조 모두 정상. 실데이터 인라인 편집은 PIN 로그인 화면에서 확인 필요.

### 2026-07-31 (9) 객관 입증 완료 처리 + DB-only 원칙 명문화 (Claude)
사용자 지시: "계산서·거래명세서 미발행 중 객관 입증되는 건 모두 완료 처리.
최종 목적은 엑셀에 저장 안 하고 DB로만 관리 — 알고리즘에 반영."
- `settle_status` 재분류(검증 [95]): ERP 판매조회 진행상태가
  · **7.수금완료 → 완료(ERP 수금확인)** — 발행·수금 모두 입증, 100건 완료 처리
  · **6.세금계산서발행 → 입금 대기** — 발행만 입증(105건). '미발행'이 아니지만
    수금이 안 끝나 완료도 아니다. 입금일까지 있으면 완료(ERP 발행확인).
  · 입증 없는 '세금계산서 미발행' 154건·'미청구(전표 없음)' 1건은 그대로 — 지어내지 않는다.
- **엑셀 백필 없음**: 06시트 발행일 칸은 채우지 않는다(판매조회에 발행일 열 자체가 없다,
  절대규칙 10). 대신 `ledger_db.resolution` 표(신설)가 정산ID·근거·최초/최종 입증 시각을
  기억한다. findings_export 가 매일 멱등 upsert — 확인필요 총 869 → **743건**.
- 앱: 완료 상태를 조치필요 필터·칩·색·처리안내에 반영("조치 불필요 — 완료입니다").
- **DB-only 원칙을 CLAUDE.md 상시 규칙으로 명문화**: 새 상태·판정·완료 기록은 엑셀 셀이
  아니라 DB에. 엑셀 두 회차 반영은 사람이 엑셀로 일하는 동안의 과도기이며, 이관 순서는
  상태·완료(끝) → 신규 입력 → 원장 자체. 엑셀을 끊는 결정은 사용자가 한다.

### 2026-07-31 (8) 앱 전역 격자 정렬 — 통계 타일·카드까지 좌우 빈틈 제거 (Claude)
사용자 지시: "가로 세로 배열 깔끔하게 중간정렬로 좌우 빈틈없이."
07-30 블록이 버튼·아이콘 줄을 끝냈고, 이번에 남은 **통계 타일·카드 격자 7종**
(kpis·pkpis·pmstats·check-kpis·rep-grid·egrid·ecells)을 마감했다.
- ★ auto-fit 격자로 안 바꿨다 — 격자는 마지막 줄도 칸폭 고정이라 빈칸이 남고, span
  트릭은 칸 수를 알아야 해서 auto-fit 과 못 섞인다. **flex-wrap + flex-grow** 는 마지막 줄
  항목들이 남는 폭을 나눠 가져 어떤 개수에서도 줄 끝까지 찬다. #skpis 의 인라인
  grid-template 지정도 display:flex 가 되면 자연히 무시된다(따로 지울 필요 없음).
- 2칸이 맞는 자리(calendar-form·issue 상세)는 격자 유지 + 홀수일 때 마지막만 전체 폭
  (업무센터와 같은 처리). 이 블록도 스타일 **맨 뒤**에 있어야 한다 — 순서를 올리지 말 것.
- 실측: 데스크톱 600px·모바일 375px 에서 7종 전부 줄끝여백 0px·넘침 없음(홀수 개수로 검사).

### 2026-07-31 (7) ★실사고: 사람 입력 유실 — 원인 2개 확정·재발 방지 (Claude)
류지영 매니저가 **다른 PC** 에서 관리대장 v331 을 열어 담당기사 미배정분을 입력하는 동안
(11:02~16:01, 엑셀 강제종료 반복), 두 자동화가 그 작업을 밟았다.

**원인 1 — autoprune 이 열린 파일을 치웠다.** v336 이 생긴 뒤, 매니저가 v331 을 저장할
때마다 autoprune 이 "구버전"이라며 OLD 로 옮겼다(`v331__from_작업폴더_1~8`, 8사본).
엑셀은 저장한 파일이 밑에서 사라지니 저장 충돌·응답없음·강제종료를 반복했다 —
"자꾸 엑셀 멈춰서 강제종료… 저장하는데도 다 날라가여"의 정체.
**원인 2 — 15:05 반영이 잠금을 무시하고 v336 을 만들었다.** 잠금파일이 있었지만
"이 PC 에 EXCEL 프로세스 없음 = 잔재"로 오판(로컬 프로세스로는 다른 PC 편집이 안 보인다).
그녀의 15:43 저장(v331)은 고아가 됐고, 새 정본 v336 에는 그 입력이 없었다.

**유실 수습(실측 셀 대조: 그녀 15:43 저장본 vs v336):** 그녀의 14:59 저장까지는 v336 에
이미 들어 있었고(15:05 반영의 밑판이 그 파일), 16:01 저장은 v336 자체에 했다. 순유실은
작았다 — ① `UJ2600094` 진행상태 '작업완료'(그녀가 새로 바꾼 것) → **사고복구 큐로 적재**
② `UJ2601070` 진행상태(그녀 '접수' vs v336 '취소') → **방향 불명, 사람 확인 필요**.
23·27시트 차이는 도구 재생성 잡음. 단, **저장 전에 크래시로 날아간 입력은 파일 어디에도
없다** — 매니저 PC 의 Excel 문서복구 사본(2:41 자동복구 등)을 Z: 에 저장해 주면 대조 가능.

**재발 방지(코드, 검증 [94]):**
- `ledger_versions._in_use()` — `~$` 잠금이 있거나 **30분 안에 저장된** 파일은 autoprune 이
  절대 옮기지 않는다. 판단 불가(네트워크 순단)여도 옮기지 않는다.
- `ledger_db.human_editing()` — 관리대장 폴더의 `~$` 잠금(24시간 이내)을 사람 편집으로
  본다. 소유자 이름까지 읽는다. **apply 는 쓰기 전(빈 회차의 구조 갱신 포함) 이 관문을
  지나며**, 잠금이 있으면 유예 내 3분 간격 대기 → 끝내 안 풀리면 회차를 **건너뛴다**
  (batch 미기록 → missed_slots 가 다음 실행에서 처리, 입력은 유실되지 않는다).
  `--force` 로도 못 뚫는다 — 강제의 용도는 시각 밖 반영이지 사람 밀어내기가 아니다.
- 운영 보완: 사용자가 매니저와 "작업 시작 전 알림" 프로토콜 합의(당분간 수동 병행).

### 2026-07-31 (6) 전 원천 수집·반영 전수 실행 — 막힌 곳 5개 확정 (Claude)
사용자 지시: "권한을 가지고 가져올 수 있는 모든 데이터 가져와서 반영해."
`collect_sources --apply` + `daily_run.py` 전 단계 실행(35단계 전부 OK, 스킵 3은 자료 없음).
**신규 424건 큐 적재 → 15:00 회차 반영.** 가져올 수 있는 것은 다 가져왔고, 남은 것은
전부 **자동으로는 넘을 수 없는 벽**이다. 다음에 또 세지 말고 여기서부터 볼 것.

1. **입금 70건·11.06억이 전부 보류** — 06시트 입금액이 701행 **전부 빈칸**인 진짜 이유다.
   원장 입금행 70건 대 입금일 빈 정산 698건인데 **유일매칭 0건**이다. 쿠팡로지스틱스 50건이
   10.9억으로 묶여 들어와 어느 정산에 얼마인지 금액으로 가를 수 없다.
   ★ 추정해서 넣지 말 것(절대규칙 10). 묶음 입금의 내역 분해는 사람만 할 수 있다.
2. **03시트 219건** — 원문에 작업내용이 있는데 03시트에 행이 없다. 엑셀을 한 번 열어
   B열 수식이 접수ID를 끌어와야 채워진다 → **복구 경고 원인 확인이 선행 차단**(대기 0번).
3. **ERP 내보내기 4개가 내용 0행** — 거래명세서현황·세금계산서현황·회계거래현황·
   무작위명(ixRsVB7c4XDo3vlz). 그래서 세금계산서 **0건**으로 잡힌다. 조회 API가 없어
   (절대규칙 3) 자동 재수집이 불가능하다 — 사람이 ERP에서 다시 내보내야 한다.
4. **구글 캘린더 읽기 실패(HTTPError)의 정체** — 서버 문제가 아니다. `config/gcal.json` 의
   `calendars[0].ics` 가 **빈 문자열**이라 공개 피드(`public/basic.ics`)로 떨어지는데,
   이 캘린더는 비공개라 404다(주석에 이미 적혀 있던 그대로다). 비밀 iCal 주소를 넣어야
   한다: 구글 캘린더 → 설정 → 해당 캘린더 → "비공개 주소(iCal 형식)".
5. **밴드 미등록 4건**(추출 4308 중) — UJ2600070(기타·유상·송파5캠프·작업완료),
   UJ2501092·UJ2600664·UJ2600825(신규납품설치, 필드 대부분 빈칸). 눈으로 보고 넣을 것.
   밴드 **신규 수집 자체는 여전히 불가**(API 토큰 없음, 앱 심사 대기) — 캐시로만 대조한다.

★★ **15:00 회차가 스케줄러에 아예 없었다(중대).** 규칙·코드·설치 스크립트는 모두
  11:00·15:00 두 회차인데(`WINDOWS=(11:00, 15:00)`, `install_ledger_schedule.ps1` 이
  트리거 2개 등록), **실제 등록된 작업에는 11:00 트리거 하나뿐**이었다
  (`Next Run Time: 2026-08-01 11:00`). 즉 **11시 이후에 큐에 들어간 것은 그날 반영되지 않고
  다음날 11시까지 방치**된다. 이번 424건도 그럴 뻔했다 — 스크립트가 개정된 뒤 재설치를
  안 해서 생긴 어긋남이다. `missed_slots` 덕에 유실은 아니고 지연이다.
  · 조치: 관리자 PowerShell 에서 `powershell -ExecutionPolicy Bypass -File ecount\install_ledger_schedule.ps1`
    재실행 → 두 트리거 확인. **사용자 실행 필요**(작업 스케줄러 변경은 AI 권한 밖).
  · 이번 424건은 15:00 슬롯이 유예 45분 안(15:05)이라 **강제 없이**(`forced=0`) 정상 반영했다.
    v331 → **v336** · 424셀. ★ 그때 잠금파일(`~$`)이 있었는데 "이 PC 에 EXCEL 프로세스가
    없다"며 잔재로 판정하고 진행했다 — **이 판정이 틀렸고 아래 (7) 사고의 한 원인이 됐다.**
    로컬 프로세스로는 다른 PC 의 편집을 볼 수 없다. 잠금파일이 진실이다. 지금은
    `ledger_db.human_editing()` 관문이 자동으로 막는다(검증 [94]).

★ Z: 는 네트워크 드라이브라 **순간적으로 끊긴다.** 이번에도 `data_status.py` 가 한 번
  "관리대장 v*.xlsx 를 찾을 수 없습니다" 로 죽었다가 재시도에서 정상이었다. 경로 설정이
  틀린 것으로 오해하지 말 것 — 같은 증상이면 그냥 다시 실행해 본다.

### 2026-07-31 (5) 폰에서 Claude Code 이어받기 — 준비 점검 자동화 (Claude)
사용자 지시: "휴대폰에서 클로드 코드 리모트로 할 수 있게 정리."
길이 둘이고 **하는 일이 다르다** — `ecount/REMOTE_PHONE.md` 가 정본 안내다.
- **A. 웹 claude.ai/code** — 폰 브라우저만. PC 꺼져도 된다. 단 **푸시된 것만** 보이고
  Z:·ERP·밴드에는 손이 안 닿는다 → 코드·문서·합성검증용. 저장소 루트가 곧 `ecount/` 라
  `CLAUDE.md`(정본)·`AGENTS.md` 가 그대로 로드된다((1)의 정본 통일이 여기서 이득).
- **B. SSH → 이 PC 의 `claude`** — Z: 까지 전부 된다. Windows 는 Tailscale 내장 SSH 를
  못 쓰므로 OpenSSH 서버가 필요하고, 접속은 Tailscale 사설망(100.119.175.113)으로만 —
  공유기 포트를 열지 않는다.
- `remote_ready.py` 신설 — 사설망·OpenSSH·절전·CLI 경로·저장소 동기를 한 번에 보고
  A/B 각각 가능/막힘을 판정한다. **아무것도 바꾸지 않는다**(전원·SSH 는 관리자 권한
  시스템 설정 → 명령만 안내). 09:50 daily_run 에 점검 단계로 넣어 조용히 막히는 걸 막는다.
  · 한국어 Windows 는 콘솔이 cp949 라 utf-8 고정 디코딩이면 powercfg 출력이 깨진다.
    문구 대신 **0x 값 순서(min·max·증가·AC·DC)** 로 읽어 언어에 기대지 않게 했다.
- ★ **지금 막혀 있는 것 2개(사용자 실행 필요, 둘 다 관리자/시스템 설정)**
  1. `powercfg /change standby-timeout-ac 0` — 전원 연결 상태인데도 **5시간 뒤 대기모드**다.
     07-30 에 "잠자기만 끄면 된다"고 진단해 놓고 값은 그대로였다(0x4650=18000초).
     이게 살아 있는 한 폰 원격도, 오프라인 큐 회수도 5시간 뒤 끊긴다.
  2. 관리자 PowerShell: `Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0`
     → `Set-Service sshd -StartupType Automatic` → `Start-Service sshd`
- A(웹)는 지금도 **바로 된다** — 이 커밋을 푸시한 시점부터 폰에서 최신으로 열린다.

### 2026-07-31 (4) PC 꺼짐 콜드 스타트 구멍 봉합 — 오프라인 입력 모드 (Claude)
(2)의 오프라인 큐 실측("서버 중지 → 쓰기 보관")은 **앱이 이미 열려 있던 기기**에서만
통했다. PC 꺼진 뒤 앱을 **새로 열면** `restoreRoleSession()`이 네트워크 실패를 인증
거절과 똑같이 취급해 PIN 게이트로 막고, `login()`도 오프라인이면 그대로 죽어 —
outbox까지 만들어 놓고 입력 화면에 못 들어가는 상태였다. 고친 것:
- **네트워크 실패와 인증 거절을 구분** — 연결 실패이고 이 기기에 로그인 이력
  (`cw_dev_auth`, 로그인/세션복원 성공 시 기록)이 있으면 **오프라인 입력 모드**로 연다.
  처음 보는 기기는 예전처럼 잠근다. 실제 반영 권한은 어차피 전송 시점에 서버가
  기기 인증 쿠키(HttpOnly)로 검증한다 — 플래그 위조로는 반영까지 못 간다.
- **outbox 401·403 은 지우지 않고 멈춘다** — 전에는 4xx 일괄 삭제라, 세션 만료 상태로
  재전송되면 오프라인에서 써 둔 입력이 통째로 유실됐다. 보관한 채 로그인을 기다린다.
- **오프라인 배너 + 복귀 감시** — "PC 꺼짐, 입력은 보관됨" 안내. 30초마다 ping,
  PC 복귀 시 **새로고침 없이**(입력 보호 지시) 보관분 전송 + 데이터만 재조회.
- login() 오프라인 실패에 안내 문구(이력 있는 기기는 닫았다 다시 열면 진입).
- 실증(8898 검증용 인스턴스): SW 껍데기 캐시 확인 → 서버 중지 → 콜드 스타트에서
  게이트 없이 열림·배너 표시 → /api/input POST 가 queued:true·보관 1건 → 더미 삭제.
- PC 꺼짐 대응 전체 구조(재검토 결과): ① 조회 = 고정 주소 암호화 사본(워치독이 수시
  게시) ② 예약 = 클라우드 큐(Sites+D1, 생존 확인함) → cloud_queue_sync 가 회수
  ③ 일반 입력·첨부 = 기기 outbox(이번에 콜드 스타트까지 완성) ④ 엑셀 반영은 PC 복귀
  후 11:00·15:00 회차 — 원본 단일성 원칙 그대로.

### 2026-07-31 (2) 나눔고딕 동봉·오프라인 큐·앱 속도 (Claude)
- **앱 속도** — 느린 원인은 파싱이 아니라 **네트워크 전송**이었다. 관리대장은 Z: 에 있고
  한 번 여는 데 ~1.0초인데 캐시가 없어 화면 한 번에 여러 번 다시 열었다(`real_works` 한
  호출에 `resolve_master` 7회, `load_workbook` 9군데). `master_stream()`(바이트 메모리 캐시,
  mtime+크기로 무효화)·`read_ledger` 결과 캐시·`resolve_master` 30초 메모이즈를 넣었다.
  **read_ledger 1,040ms → 98ms**, 정산 1,601→1,071ms, 업무 2,030→1,552ms.
  · 남은 1.5초는 openpyxl 파싱이다. 더 줄이려면 **시트별 파싱 결과를 캐시**해야 한다(다음 단계).
- **글자크기** — 13.5px 이하 232곳을 키웠다(8.5→11 … 13.5→14.5). 14px 이상은 그대로 둬
  위계를 지켰다. 본문 15→16px. 폭이 정해져 있던 **아이콘 버튼만** 같이 넓혔고(68→86px),
  **하단 탭바는 일부러 안 키웠다**(11.5px) — 탭 7~8개에 375px 폰이면 한 칸이 47px 뿐이라
  13px 로 키우면 '대시보드' 네 글자가 칸을 넘는다.
- **나눔고딕** — ★ 이력이 중요하다. 나눔고딕은 **2026-07-25 에 이미 지시**돼 구글 폰트 CDN
  링크로 들어와 있었는데, **07-30 iOS 개편이 본문 글꼴 스택을 -apple-system 으로 덮으면서
  조용히 되돌아갔다.** 링크만 남고 스택에서 빠지면 아무 일도 안 일어난다.
  이번엔 woff2 276조각(4.2MB, SIL OFL)을 `webapp/fonts/` 에 **동봉**하고 주소를 로컬로
  재작성했다 — 인터넷 없는 현장에서도 뜨고 외부로 요청이 안 나간다. 92조각 unicode-range
  구조를 지켜 첫 화면은 30조각만 받는다. 검증 [91] 이 이제 **외부 폰트 주소가 다시 들어오면
  실패**한다 — 같은 되돌림을 막는 장치다.
- **PC 가 꺼져도 폰에서 입력** — `폰 입력 → 기기 IndexedDB 보관 → PC 켜지면 자동 전송 →
  ledger_db 큐 → 11:00·15:00 엑셀 반영`. 뒤 두 단계는 기존 구조 그대로고 새로 만든 건 보관뿐.
  · `window.fetch` 를 감싸 **실패를 가로챈다.** 쓰기 호출이 화면 곳곳에 흩어져 있어 호출부를
    하나씩 고치면 새 화면에서 또 빠뜨린다. 큐 대상은 쓰기 9종뿐 — 로그인·PIN 변경·조회·
    엑셀 내보내기·`/api/ux` 는 넣지 않는다. **목록에 없는 주소는 예전처럼 실패한다**(삼키지 않는다).
  · 넣은 순서대로 보내고 막히면 멈춘다(순서가 뒤집히면 '수정'이 '등록'보다 먼저 도착한다).
    4xx 는 버리고 5xx 는 멈춘다. 첨부파일은 Blob 그대로 보관돼 오프라인에서도 대기한다.
  · **서비스 워커가 껍데기를 캐시**하게 했다 — 전에는 아무것도 캐시하지 않아 PC 가 꺼지면
    앱이 아예 안 열렸다. 화면은 네트워크 먼저(끊겼을 때만 캐시본)라 옛 화면이 남지 않고,
    `/api/` 는 캐시하지 않는다. 검증으로 둘 다 못박았다.
  · 실측: 서버 중지 → 앱이 캐시본으로 열림·나눔고딕 30조각·쓰기 보관(배지 1건) →
    서버 재기동 → 자동 전송, 대기 0건.
- **원본 자료** — `source_full_audit` 3,023개 전수조사(실오류 2=0바이트 로그, 경고 645=확장자
  불일치, 완전중복 8그룹). `collect_sources --apply` 로 신규 카톡 2건 수집. 카톡 미등록 0건 확인.
  ★ **ERP 자동 수집은 불가능하다** — 판매·세금계산서·수금 조회 API 가 존재하지 않는다(절대규칙 3).
    지금 있는 ERP 내보내기 4개도 **내용 0행**이라 사람이 다시 내보내야 한다
    (거래명세서현황·세금계산서현황·회계거래현황·무작위 파일명의 거래처별계정별원장).
  ★ **밴드 자동 수집도 불가** — API 토큰이 없다(밴드 앱 심사 대기).

### 2026-07-31 (3) 종전 세션 명령 대조·Excel 11시/15시 우회 경로 차단 (Codex)
- Claude Code 대화 3개, 프로젝트 Codex 작업 기록, AGENTS·19시트·Git을 대조해 약 230개
  사용자 발화를 완료·미완료·대기·후속 지시 대체로 분류했다.
  상세 원장: `reports/종전세션_명령대조_20260731.md`.
- 앱 고정 주소는 `https://mulder.tailf14aae.ts.net/`. 프로젝트 Codex 종전 작업 파일 10개는
  삭제하지 않고 `.codex/archived_sessions`로 옮겼으며, 4시간 감시는 현재 작업
  `019fb290-47ee-7493-b00d-d7dfc6ef1241`을 가리킨다. 다른 프로젝트 작업은 건드리지 않았다.
- **Excel 두 회차 규칙을 입력 셀 외 구조 갱신까지 확대했다.** 09:50 자동대조에 남아 있던
  23·24·25·27·28 시트 갱신, 복구, Excel 재계산을 읽기 전용 분석으로 바꾸고
  `ledger_db.scheduled_workbook_maintenance()`의 11:00·15:00 회차 안으로 옮겼다.
  앱 업무센터·기준일·폰 예약·일반 입력과 옛 `ledger_writer.py --apply` 호출도 DB 적재만 한다.
  종료 체크리스트의 19시트 인수인계도 `ledger_db.py --handoff`로 예약한 뒤 회차 마지막에
  `workbook_patch.py`가 기록한다.
- 작업 스케줄러 `쿠팡업무_원장일괄반영`은 정확한 한글 이름으로 11:00·15:00 두 트리거가
  등록돼 있다. 현재 DB 대기 424셀은 규칙대로 다음 11:00을 기다린다. 놓친 회차를 임의 시각에
  강제 따라잡지 않는다.
- ERP 원본 6개를 읽기 전용 재계수해 빈 파일은 **정확히 4개**임을 확인했다.
  기존 3개 외 `ixRsVB7c4XDo3vlz.xlsx`가 거래처별계정별원장(1행·1열)인데 목록에서 빠져 있었다.
- 나눔고딕 로컬 동봉본에 `webapp/fonts/OFL.txt`를 추가했고 검증 [91]로 라이선스 파일도
  빠지지 않게 했다. 375px 실제 렌더링에서 가로 넘침 없음·본문 16px·나눔고딕·서비스워커를 확인했다.
- 전체 합성검증은 **ALL GREEN**. 직접 `ledger_writer.py --apply`도 Excel을 열지 않고
  `DB 적재 0건 / 대기 424건 / 다음 11:00`으로 끝남을 실증했다.

### 2026-07-30 23:04 종전 세션 통합 — UI·안전 재계산·담당 이관
- 종전 Codex/Claude 작업의 예약 감시·코드·산출물을 현재 Codex 작업으로 인계했다.
  `쿠팡 실시간 문제 협업감시` 4시간 heartbeat의 대상 작업을 현재 작업으로 바꿨고,
  종전 Claude Code 실행 프로세스와 그 세션이 띄운 일일 실행·검증·데모 서버를 종료했다.
  상시 `app_server.py`와 `tunnel_run.py`는 서비스이므로 유지한다.
- 앱 전체를 iOS 시스템 색·SF Pro 스택·밝은 화면 고정·frosted 탭바·큰 모서리로 정리하고,
  87개 이미지 아이콘 참조를 `currentColor`를 상속하는 39개 인라인 SVG 스프라이트로
  교체했다. 모바일 탭 아이콘은 28px, 데스크톱 사이드바는 29px로 키웠다.
  대시보드 히어로는 장식 원을 없애고 제목·상태 배지를 같은 flex 행에 두어 겹침을
  제거했으며, 긴 금액이 잘리지 않도록 통계 폭을 내용에 맞춰 배분한다.
  흰 배경 하드코딩 77곳과 충돌하던 OS 다크모드는 밝은 화면으로 고정했다. 헤더의 검은
  회사 로고는 흰색으로 변환해 남색 헤더와 한 덩어리로 보이게 하고, 계정·알림 SVG도
  배경과 확실히 대비되게 고정했다.
- 종전 세션의 `excel_recalc.py` 초안은 그대로 실행하면 `DisplayAlerts=false`로 복구
  경고를 숨긴 채 정본을 열 수 있어 절대규칙과 충돌했다. 따라서
  `reports/진행체크포인트.json`의 복구 경고 금지가 먼저 없어지고,
  `reports/excel_recalc_clearance.json`에 **현재 파일명과 정확한 SHA-256** 승인이 있어야만
  Excel COM을 여는 이중 관문을 추가했다. 새 원장 버전·내용 변경 시 승인은 자동 만료된다.
  현재 v327의 재계산 대기는 0건이므로 `--run` 실호출은 정본을 열지 않고 안전하게
  `건너뜀`으로 끝났다. 복구 경고 원인 확인 전에는 승인 파일을 만들지 말 것.
- `fix_workbook.py`로 최신 v327의 수식 캐시 누락 0·행/셀/스타일/연결 구조 이상 0을
  재확인했다. 이 검사는 Excel 자체 복구 경고 해소 증거가 아니므로 아래 차단 항목은
  그대로 유지한다.
- 종전 Claude 세션의 마지막 미반영 지시였던 **변재선 업무 → 류지영 이관**도
  `responsibility.py`·앱·업무센터·합성검증에 반영했다. 류지영이 기존 운영 범위와 함께
  거래명세서·세금계산서·입금·금액 불일치 확인을 맡으며, 오종현의 원천자료 취합과
  유현민의 PO·ERP·원장 시스템 확인 범위는 유지한다. 기존 `/staff/byeon-jaeseon`
  바로가기/PWA는 끊지 않고 `/staff/ryu-jiyeong`으로 영구 리디렉션한다.

### 2026-07-30 23:30 종전 Claude 잔여 작업 회수 — DB 일괄반영·UX 근거
- 한 차례 중지 후 종전 Claude Desktop의 Claude Code 작업자가 다시 실행돼 UI 커밋 1개와
  미완성 `ledger_db.py`를 남긴 사실을 발견했다. Claude Code·Claude Desktop 전체와 그
  데모 서버를 종료했고, 상시 앱 서버·터널만 유지한다.
- 앱·Claude·Codex·자동대조의 확정 입력은 SQLite `db/ledger_queue.db`로 모은다.
  기존 JSON 큐는 잠근 채 staging으로 떼어 정확히 한 번 흡수하고, 엑셀은 작업 스케줄러가
  **11:00·15:00 각 회차 한 번만** 일괄 반영한다. 회차 밖·같은 회차 재실행은 대기하며,
  반영 도중 새로 들어온 행은 다음 회차에 남는다. DB는 Git에서 제외하고 `archive_keep.py`가
  SQLite backup API로 서버 보관소에 함께 저장한다.
- 앱은 DB 대기 건수·다음 반영 시각을 표시하고, 인증된 사용자의 화면 이동·버튼·느린 API·
  오류 유형만 90일간 기록한다. 검색어·업무 본문·PIN 같은 개인정보/업무내용은 기록하지 않는다.
  UI/UX 개선 지시를 받을 때는 `ledger_db.ux_summary()`의 실제 사용 근거부터 확인한다.

### 2026-07-30 21:43 전체 대조 후속 누락 완료 — 중복실행 잠금·ERP 원장 실대조
- `daily_run.py`에 프로세스 간 단발 잠금(`reports/.daily_run.lock`)을 추가했다. 두 실행이
  겹치면 두 번째 실행은 시작하지 않으며, 살아 있는 PID의 잠금은 건드리지 않고 비정상 종료로
  남은 잠금만 회수한다. 잠금 파일을 막 만든 순간의 빈 JSON을 죽은 잠금으로 오판하는 경합도
  60초 보호로 차단했다. 합성검증 `[86]`이 중복 차단·죽은 잠금 회수·내용 분류를 강제한다.
- ERP 원장 실행 조건과 실제 대조기를 모두 `inbox_scan.pick("ledger")`로 통일해 로컬
  `inbox/`뿐 아니라 `0. 원본 자료`의 무작위 파일명도 읽는다. 이카운트 파일의 잘못된
  `<dimension ref="A1:A1">` 때문에 `read_only=True`가 1셀만 읽던 문제를 재현 검사로
  막았고, 외상매출금 원장의 **매출=차변·입금=대변** 구조로 바로잡았다.
- 실원본 `ixRsVB7c4XDo3vlz.xlsx`를 전체 대조 재실행 없이 단독 대조했다. 조회기간
  **2026-01-10~2026-06-26**, 매출전표 **87건**을 읽었고 결과는
  **A(ERP에만) 87 / B(원장에만) 0 / C 0 / D 0 / 정상 0**이다
  (`reports/ERP원장대조_20260730_2143.md`). 7월 원장 61건은 ERP 원본 조회기간 밖이므로
  B 누락으로 잘못 세지 않게 기간 관문을 추가했다. A 87건은 1~6월 관리대장에 대응
  거래명세서번호가 없어 남은 확인 목록이며, 근거 없이 자동 반영하지 않는다.
- 폰 암호화 사본의 Base64 암호문에 우연히 `UJ25` 네 글자가 생겨 평문 유출로 오판하던
  확률성 검증도 수정했다. 암호문 문자열 검색 대신 허용된 봉투 메타데이터와 Base64 구조를
  확인한다. 최신 Sol 인수인계 검토는 `ef6f279` 기준 Python 문법·전체 합성검증
  **ALL GREEN**으로 통과했다.
- 서버 복구용 `git bundle`이 같은 날짜에 HEAD가 바뀌어도 기존 파일을 그대로 두어
  마지막 커밋이 빠지는 문제를 고쳤다. 오늘 marker가 현재 HEAD면 유지하고, 다르면 새 bundle을
  임시 경로에서 완성한 뒤 기존 파일과 원자 교체한다. 다른 날짜에 같은 HEAD가 이미 있으면
  중복 생성을 생략하며 `bundle_action()` 자체검증으로 네 경우를 고정했다.

### 2026-07-30 보고 날짜 즉시 전환 — 오늘/전날 기본값·캡처 기준 고정
- 보고 화면의 `날짜 변경` 창은 열 때마다 **보고일=오늘, 집계기준일=정확한 전날**
  (주말도 건너뛰지 않음)로 시작한다. 대시보드 날짜 입력 기본값도 같은 규칙을 쓴다.
- 보고일 또는 집계기준일을 달력에서 바꾸면 180ms 디바운스 뒤 해당 기준의
  `/api/exec_report`·`/api/brief`를 즉시 다시 불러와 화면과 캡처 공용 데이터를 갱신한다.
  별도 엑셀 저장 전에도 미리보기·캡처가 선택 날짜를 사용한다.
- 캡처 헤더에는 선택한 `보고일`과 `집계기준일`을 별도로 고정 표시한다.
  JavaScript 구문, 고정 주소 배포 코드, 날짜 회귀검사와 전체 합성검증은 **ALL GREEN**이다.

### 2026-07-30 공통 앱 헤더 재정렬 — 회사 로고 상하 배치
- 공통 헤더를 `시스템 식별 영역 / 회사 로고 영역 / 상태 영역`의 3구역으로 분리했다.
  앱 아이콘과 시스템명은 왼쪽에 묶고, 회사 로고 영역에서는 **유니버셜리프트 로고를 위,
  쿠팡 로고를 아래**에 중앙 정렬했다. 상태 점·시각은 별도 오른쪽 영역으로 고정했다.
- 기존 데스크톱 유니버셜리프트 CI 295px 규격은 유지하면서 제목이 두 줄로 밀리는 문제를
  해소했다. 모바일에서는 로고 묶음을 96px 안에 축소하고 데모/시각 문구를 숨겨 가로 넘침을
  방지한다. 공통 헤더이므로 관리자 화면과 류지영 업무센터 전체에 같은 배치가 적용된다.
- 합성검증 **ALL GREEN**, JavaScript 구문·Git 공백검사 및 실제 데스크톱 브라우저 렌더를
  확인했다.

### 2026-07-30 류지영 업무센터 개편 — 실행 제외 관리자형 업무 화면
- 기존 `류지영 업무방`을 **`류지영 업무센터`**로 개편하고 대시보드 바로가기는
  `업무센터 열기`로 정리했다. `/ryu`에는 관리자 화면과 같은 `대시보드·정산·보고·
  확인 필요·기록·쿠팡 캘린더` 메뉴를 제공하되, 요청대로 `실행` 메뉴는 노출하지 않는다.
- 돌발AS·정기점검·현장작업·거래서류/청구·확인 필요·자료 등록을 큰 카테고리로 나눴다.
  각 카테고리는 2026년 과거→최근 목록, 검색·상태·기간 필터, 선택 건 상세와 기존 값을
  함께 보여 준다. 목록은 화면 안에서 독립 스크롤하고 모바일에서는 카테고리와 하단 메뉴를
  가로 스크롤로 유지한다.
- 선택 건 입력은 카테고리별 사람 원천 필드만 허용하며 **기존 값은 읽기 전용, 빈칸만**
  `ledger_writer`의 `only_if_empty` 관문으로 반영한다. 수식·검증결과·상태 열은 직접
  수정하지 않으며, 근거파일은 원본 자료의 기존 카카오톡 일자 폴더에 보존한다.
- 카카오톡 2개 방 원본 업로드·조사 메모·근거 첨부와 현재 목록 이미지 저장·클립보드 복사·
  엑셀 내보내기를 한 화면에 묶었다. API는 `/api/ryu/records`, `/api/ryu/entry`를
  사용하며 기존 `/api/ryu/upload`와 호환된다.
- Python/JS 구문, 정적 회귀검사, 실제 2026년 읽기 전용 목록, 데모 저장 대기열,
  데스크톱·모바일 브라우저 렌더와 메뉴 전환을 확인했고 합성검증은 **ALL GREEN**이다.

### 2026-07-30 01:27 갱신 — 누적 지시 소급감사·잘못된 집계 차단
- 과거 지시를 앱 기능·관리대장·원본 대조 도구와 다시 비교했다. 증빙 기반 검증
  `verification_sync.py`는 확정 프로젝트 900개·변경 후보 0칸, 완료 근거 게이트
  `complete_verified.py`도 추가 완료 후보 0건이다. 이미 반영된 건을 다시 쓰지 않았다.
- 입금 정본과 날짜별 보관본이 동일 파일인데도 두 경로를 모두 읽어 **70건·
  1,106,167,980원을 140건·2,212,335,960원으로 두 번 세던 오류**를 고쳤다.
  파일명이 아니라 내용 SHA-256이 완전히 같은 원본만 한 번 읽으며, 내용이 다른
  갱신본은 보존한다. 실제 재집계는 70건·1,106,167,980원·파일 1개로 복구됐다.
- 밴드 사진이 `문서사진/YYYY/MM/날짜`로 이동됐는데 최상위 파일만 세어 앱 자료현황에
  0장으로 나오던 오류를 고쳤다. 2025년 20장은 통계에서 제외하고 **2026년 1,711장·
  153MB**만 재귀 집계한다. 느린 `glob("**")` 대신 `os.scandir` 스택을 써 밴드
  상태 집계를 0.61초, 자료현황 전체 갱신을 4.7초로 확인했다.
- 자동반영하면 안 되는 후보도 다시 격리했다: 입금 70건은 묶음입금/중복금액으로
  건별 유일매칭 0건, 정기점검 원본은 프로젝트 매칭 대기 81건, 현장 일지는 충돌확인
  3건·전체 미매칭 1건, 카카오톡 신규 후보는 날짜/유형 불명확분이 섞여 있다.
  임의 프로젝트·완료일·담당자·입금 귀속을 만들지 않았다.
- **중요: 최신 v303과 인수인계만 추가한 v304를 Microsoft Excel로 직접 열면 둘 다
  “통합 문서의 내용에 문제가 있습니다. 복구하시겠습니까?” 경고가 난다.**
  `fix_workbook.py`의 캐시/행·셀/스타일/연결 검사는 이상 0이지만 Excel 자체 경고가
  더 강한 신호이므로 두 번 모두 `아니요`로 중단했다. 따라서 수식 재계산 대기 636건과
  그 뒤 채울 수 있는 03시트 작업상세 219건은 손상 원인을 찾기 전까지 **미반영**이다.
  경고에서 `예`를 눌러 정본을 자동 복구·저장하지 말 것.
- 합성검증 전체 ALL GREEN, 로컬/고정주소
  `https://mulder.tailf14aae.ts.net/api/ping` HTTP 200을 확인했다.

### 2026-07-30 00:35 갱신 — 앱 아이콘·CI·공통 캡처도구·류지영 업무방
- 사용자가 제공한 새 CSOS 이미지를 `webapp/brand/csos-app-icon-source.png`에 원본 보존하고,
  실제 앱 타일 영역을 32·180·192·512px로 파생했다. 로그인·상단 헤더·좌측 메뉴·PWA
  파비콘/설치 아이콘·보고 HTML·모든 캔버스 캡처의 기존 체크/리프트 마크를 새 CSOS 아이콘으로
  통일했다. 고정주소 `https://mulder.tailf14aae.ts.net/`는 변경하지 않았다.
- 상단 배치는 `CSOS 아이콘 → Coupang CI → 시스템명 → Universal Lift CI` 순서다.
  데스크톱 Universal Lift CI는 기존 118px의 정확히 **2.5배인 295px**로 키웠고, 모바일은
  96px로 축소해 한 줄과 가로 넘침 0을 유지한다. 보고 머리글은 모바일에서 2단으로 재배치한다.
- 보고·대시보드·상세목록의 이미지 도구를 인쇄/이미지 저장/복사/엑셀/새로고침 아이콘형으로
  통일했다. 일지저장·공유/전달 버튼은 노출하지 않고 모바일에서도 `nowrap` 한 줄 가로배열이다.
- `/ryu` 전용 업무방을 추가했다. 류지영이 `★UNI★ 쿠팡정기점검`·`★UNI★ 쿠팡돌발점검`
  카카오톡 txt 두 개와 조사 결과/근거 파일을 항목별로 올리면 원본 자료의 연/월/일 폴더 보존,
  방 자동판별, 로컬 대조 입력, 카톡 대조 실행까지 이어진다. 다른 실행 중이면 사람이 다시
  누르지 않아도 종료 즉시 자동 대조가 예약된다.
- `쿠팡로고.ai`는 PDF 호환 원본에서 로고 영역만 추출해 `webapp/brand/coupang.png`로 보존했다.
  Universal Lift 가로/마크 CI도 승인 자산으로 버전관리한다.
- 문서 OCR은 외부 업로드 없이 로컬 PaddleOCR 작업자·영속 캐시를 추가했다. 기존 금액정합성과
  고신뢰도 입력 관문은 그대로 유지하며, 전수 재검사는 `%LOCALAPPDATA%\CSOS`에서 실행 중이다.
- Python/JS 구문, 데스크톱 1440×900·모바일 390×844 실제 브라우저 렌더, 콘솔 오류 0,
  합성검증 전체 **ALL GREEN**을 확인했다.

### 2026-07-29 23:12 갱신 — 밴드·ERP·거래명세서 증빙 기반 검증 자동완료
- `verification_sync.py`를 추가해 밴드·카톡 완료보고, ERP 판매조회, 06시트의
  거래명세서번호·발행일을 프로젝트NO로 정확히 대조한다. 2026년·완료건·확인일 있는
  근거만 사용하며, 03시트의 프로젝트NO 캐시가 비어도 02 완료행을 끌어오는 배열수식
  순서를 그대로 재현한다. 실제 작업내용 없는 예비행은 쓰지 않는다.
- 확정 근거가 생긴 02·03·04의 원인 열만 갱신하고 검증결과 수식은 보존했다.
  03 거래명세서 `반영완료` 189건, ERP `반영완료` 190건, 검증자 `유현민` 191건,
  검증일 190건이 최종 확인됐고, 04 점검보고서 `등록` 340건 등 총 **1,866셀**을
  v300→**v301**에 반영했다. 같은 작업 재실행 후보 0칸, ZIP·수식·구조 검사 정상이다.
- 앱의 돌발AS 카드·표·상세에 거래명세서반영, ERP반영, 검증자·검증일을 연결했다.
  Excel 재계산 전에도 모든 필수 원인 값이 충족된 건만 앱에서 `정상`으로 승격하며,
  방문일정·재방문 여부처럼 근거가 없는 값은 임의 보완하지 않고 `확인필요`로 남긴다.
- 대량 반영 때 `read_only Worksheet.cell()`로 1,866셀을 무작위 검증해 오래 걸리던
  병목을 시트별 1회 순차 읽기로 교체했다. ERP 판매조회는 파일 크기·수정시각 캐시를
  사용해 미리보기가 3초대로 줄었다. 합성검증 [84]가 증빙 게이트·빈 예비행 제외·
  수식열 보호·앱 노출·대량 순차검증을 회귀 확인한다.

### 2026-07-29 22:25 갱신 — 대표 PNG 4원천 표 화면과 동일화
- 대표 보고 PNG의 `4원천 검증 (밴드·카톡·ERP·쿠팡PO)`을 화면 표와 같은 구조로
  변경했다. 밴드·카톡의 대상/확인율/미확인 수, ERP 안내, 쿠팡 PO 검출 결과를
  각각 한 행으로 표시하고 미확인 프로젝트NO·PO번호는 파란 칩 3열로 배치한다.
- 미확인 번호 수에 따라 행과 전체 캔버스 높이를 자동 확장하므로 8개 이상도 잘리지
  않는다. 자료가 없는 ERP 행은 첨부 기준처럼 결과·번호 칸을 합쳐 회색 안내를 표시한다.
- 앱을 연 직후 4원천 상태 API가 늦게 도착해도 보고 화면을 다시 그리며, 이미지 저장·
  공유 버튼은 클릭 시점의 최신 집계를 다시 묶는다. 실제 운영 데이터로 PNG를 생성해
  첨부 기준과 같은 표·칩 배치를 확인했고 전체 합성검증 83개는 **ALL GREEN**, Sol
  인수인계 검토도 PASS했다. `19_AI작업인수인계` 217행에 기록해 최신본은 **v300**이다.

### 2026-07-29 22:05 갱신 — 대표 캡처 제목 문구 정리
- 대표 보고 PNG의 당일 업무 실적 제목에서 사용자가 지정한 `대표 보고 ·` 문구를
  제거했다. 제목은 이제 `YYYY-MM-DD 당일 업무 실적`로 바로 시작한다.
- 고정 전체기능 주소 `https://mulder.tailf14aae.ts.net/`의 실제 응답에서도 새 제목
  코드를 확인했으며, 구 문구가 다시 들어오지 않도록 합성검증 [79]에 회귀 조건을 추가했다.
- 전체 합성검증 83개는 **ALL GREEN**이고 Sol 인수인계 검토도 PASS했다.
  관리대장 `19_AI작업인수인계` 216행에 기록하여 최신본은 **v299**다.

### 2026-07-29 21:53 갱신 — daily 후속 검토·자동입력 행 탐색 누락 보완
- 21:34 `전체 대조 실행(daily)`은 로컬 스크립트 1회만 실행됐고 종료코드 0,
  합성검증 ALL GREEN, v295 구조·수식 무결성 정상이다. ERP 원장·매출서류,
  밴드 문서사진, 클라우드 업로드의 스킵은 원천/설정 부재에 따른 정상 스킵이다.
- 후속 감사에서 `ledger_writer.resolve_targets()`가 캐시를 시트명만으로 공유해,
  같은 02시트의 `접수ID`와 `프로젝트NO` 키가 섞이면 뒤 키를 첫 키 사전에서 찾는
  결함을 확인했다. 캐시를 `(시트, 키열)`로 분리했고 합성검증 [5]에 혼합 키 회귀를
  추가했다.
- 밴드 사진 큐 401건 중 307건은 접수ID가 수식 원문이라 행 탐색에서 제외됐다.
  밴드 대조는 이미 프로젝트NO로 정확히 연결하므로 앞으로 사진 큐도 프로젝트NO를
  우선한다. 수정된 대조기가 사진 후보 전체 401건을 프로젝트NO로 멱등 재적재했고,
  `UJ2601345` 완료확인 4필드와 `UJ2601354` 접수일 1필드를 합친 **406건**을 처리했다.
  v296→v297에서 실제 빈칸 6건을 반영하고 기존값 400건을 스킵했으며 큐는 0건이다.
  이어 확인필요 시트 갱신으로 최신본은 v298이 됐다. v298 폰 사본에서
  `UJ2600307` 사진등록, `UJ2601345` 완료보고서·관리자검증, `UJ2601354` 접수일을
  재확인했고 `fix_workbook.py`는 캐시 누락 0·구조 이상 0이다.
- 주의: 사용자는 후속 검토에서 daily 재실행 금지를 지시했고 이 Codex 검토 세션은
  daily를 호출하지 않았다. 다만 별도 자동 실행기가 21:57~21:59에 daily를 다시
  실행해 위 큐를 반영했다. 결과는 멱등 정책으로 정상이나, 같은 후속 티켓에서 로컬
  스크립트가 중복 기동되지 않도록 실행기 측 단발성 보장을 후속 확인한다.
  상세 근거: `reports/daily_followup_review_20260729_2153.md`,
  `reports/종합리포트_20260729_2159.md`.
- 이 세션의 Git 메타데이터는 읽기 전용이라 `.git/index.lock` 생성이 거부됐다.
  따라서 위 코드·검증·인수인계 변경은 커밋·푸시되지 않은 작업트리 상태다.
  다음 쓰기 가능한 세션은 `outputs/`를 제외한 위 4개 추적 파일만 검토·커밋하고,
  `git pull --rebase` 후 Sol 검토 관문과 비밀 스캔을 다시 통과시켜 push한다.

### 2026-07-29 밤 갱신 — 고정주소 복구·누적 지시 소급검증·Codex 인계
- **고정 전체기능 주소는 `https://mulder.tailf14aae.ts.net/`** 이다. Tailscale Funnel이
  `127.0.0.1:8899`로 연결하며 `/api/ping`·앱 본문 모두 HTTP 200을 재확인했다.
  **이 주소는 사용자 확정 정본이므로 Claude Code·Codex·자동 게시기 모두 변경 금지**다.
  장치 이름이 달라져도 새 주소를 따라가지 말고 Tailscale 이름/공개 경로를 원복한다.
  `trycloudflare.com` Quick Tunnel 주소는 세션마다 바뀌므로 정본으로 쓰지 않는다.
  앱 서버가 꺼져 있으면 `tunnel_run.py`·`phone_access.py`가 먼저 로컬 서버를 복구한다.
  21:33에는 PC 내부 응답은 정상인데 공개 Funnel 두 지점의 TLS가 모두 실패하는 상태를
  확인해 같은 주소로 Funnel을 재등록했다. 이제 `tailscale_serve.py`·`phone_access.py`와
  30분 워치독이 공용 DNS의 외부 relay를 직접 검사하고, 내부만 정상인 오판 상태면
  주소를 바꾸지 않은 채 공개 경로를 자동 재등록한다.
- 최신 카카오톡 내보내기 2개(07-29 17:03)를 원본 폴더와 `kakao/inbox`에 보존하고
  완료 원문 대조 후 신규 4건을 반영했다. 철거·납품은 05시트까지 기존코드로
  판정하여 다음 실행에서 중복 등록하지 않으며, 정상 등록 뒤 “대상 시트 없음”이라는
  잘못된 보류 문구도 제거했다. 전체 일일대조를 21:23까지 다시 실행한 업무본은
  **v295**이며(이후 19시트 인수인계 버전은 별도), 수식 복구 0·범위 오류 0·구조 이상 0이다.
- 원본 자료 **2,345개(332,655,809 bytes)**를 읽기 전용 전수감사했다.
  실제 손상·빈 파일은 0개다. 밴드 이미지 645개는 정상 JPEG 본문이 `.png` 이름으로
  저장된 **확장자 경고**여서 손상과 분리했고, 내용이 같은 충돌 보존본은 6그룹·추가
  사본 12개다. 자동 삭제·이름 변경은 하지 않았다(`reports/source_full_audit_latest.*`).
- 대표 보고 캡처 제목에서 특정인 이름을 제거하고 데이터 갱신시각, 돌발AS 대조
  시작~종료일, 미실시 사유, 취소·정상작동 상세, 완료일 확인 상세, 정기점검
  예정·실행·미실행 목록과 `GWJ1 M_순천1` 택배발송 실적을 함께 넣었다.
- 앱은 대표 보고를 해당 화면에서만 지연 로딩하고 관리대장 mtime 기준 캐시를 쓴다.
  정산 API는 첫 조회 약 7.1초·재조회 약 0.35초, 자료현황은 1분 이상 반복 스캔에서
  약 3초로 줄었다. 금액은 전 화면 정수 반올림·천단위 쉼표로 표시한다.
  실행 중에는 상단 아이콘과 실행 메뉴가 애니메이션으로 상태를 알린다.
- 앱·보고 통계는 **2026년만** 사용한다. 자료현황의 밴드 집계도 과거 원본은 보존하되
  2026-01-02~07-29의 2,325건만 숫자에 포함한다.
- 실행 버튼의 로컬 업무 스크립트는 한 번만 실행한 뒤 AI 후속 검토 티켓을 만든다.
  Claude 버전 확인 뒤 실제 호출에서 크레딧 소진이 발생해도 같은 티켓을 Codex가 즉시
  이어받는다. 오래 남아 있던 중복 대기 티켓 2개는 현재 대화에서 작업을 완료해
  `superseded`로 닫았다.
- BAND·ECOUNT 인앱 브라우저는 현재 둘 다 **로그인 화면**까지 안전하게 확인했다.
  세션 쿠키가 없어 원문 수집은 할 수 없으며 로그인정보 추측·무차별 API 탐침은 하지
  않는다. BAND는 로그인 세션 또는 기존 캐시, ECOUNT는 화면의 Excel 내보내기가 정식 경로다.
- 세션이 끊겨도 `reports/진행체크포인트.json`의 “다음 순서”에서 재개한다.
  새 Codex 세션은 루트 `AGENTS.md` 0번 단계의 `session_handoff.py --check --for-sol`로
  미완료·미커밋·미푸시 상태를 먼저 읽는다.

### 2026-07-29 갱신 — 원본자료 자동정리·현장 일지 대조·대표 캡처
- 집계마감시간은 `00_대시보드!B5`의 **당일 23:59**으로 고정한다. Excel에는 계산용 숫자 시간값과 표시형식 `hh:mm`으로 저장하며, `01_대표보고`·`08_일일보고`·`22_보고스냅샷`의 종료시점도 같은 값을 참조한다. 변경은 `workbook_patch.py --cutoff HH:MM`으로만 수행한다.
- 앱 좌측 하단 `쿠팡 캘린더`는 사용자가 제공한 **공개 임베드 URL만** 전체보기로 사용한다. 캐시 일정은 상세 확인용으로 표시하고, 새 일정은 Google Calendar의 사용자 권한 화면을 열어 최종 저장한다. 비공개 iCal 주소·설정 파일은 화면·로그·커밋에 절대 노출하지 않는다.
- 실행 화면의 AI 연계는 Claude Code를 먼저 확인하고, 사용 불가 시 Codex 요청 큐로 전환한다. 업무 스크립트는 로컬에서 **한 번만** 실행해 이중 원장 반영을 막으며, 상태 조회는 최근 60초의 안전한 가용성 결과를 재사용한다. CLI 권한/크레딧 문제로 AI를 직접 기동할 수 없을 때도 요청은 큐에 남기되 원장을 임의 변경하지 않는다.
- 2026-07-29 17시 기준 코드·합성검증은 완료했다. 카카오톡 내보내기 2개는 `3. 카카오톡 내보내기/2026/07/29`에 해시 검증 후 보관했다. Sol 검토 관문 통과 뒤 관리대장 **v286**에 `23:59`를 반영했고, `B5`는 계산용 시간값·`hh:mm` 형식, `fix_workbook.py` 구조검사까지 확인했다. 이어서 카카오톡 완료 원문과 대조해 v287에서 `UJ2601345`(천안1Sub-FC)의 담당기사·완료일만 빈칸 채움으로 반영했고, v288에서 신규 접수 `UJ2601353`(송파1MB)·`UJ2601354`(제주1Sub-hub)를 02시트에 접수 상태로 추가했다. 두 신규 건은 완료 근거가 없어 완료 처리하지 않았다.
- `0. 원본 자료`는 유형/연도/월/날짜(PO는 PO번호) 구조로 안전하게 정리한다. `원본자료자동정리.bat`와 작업 스케줄러 `쿠팡업무_원본자료자동정리`가 매일 09:35에 새 자료를 수집·정리하며, 원본 잠금/이름 충돌 시 삭제·덮어쓰기 없이 다음 실행에 재시도한다. 현재 잠긴 ERP 내보내기 1개만 보류되어 있다.
- `0. 원본 자료/100. 업로드용 자료`를 단일 투입함으로 추가했다(2026-08-03 지시).
  하위 폴더까지 전부 훑어 엑셀 머리글·텍스트/PDF 내용·파일유형으로 ERP·쿠팡 PO·카톡·
  밴드·정기점검 일정·PO/견적·입금·업무일지 정본에 분류하고, 판별 불가는 `9. 미분류`에
  보존한다. 30분 워치독은 새 원본을 발견하면 전체 대조를 깨우며 09:35/09:50 및 수동
  전체대조도 같은 경로를 먼저 실행한다. 동일 내용은 한 벌만 남기고 이력·결과 JSON을 기록한다.
- 원본 폴더의 중복된 `6. 26년도 PO 모음`은 실제 PO 없이 `Thumbs.db` 캐시 6개만 남아 있어 제거했다. PO 원본 573개는 `6. PO 원본`에 그대로 보존되며, 화면상 원본 분류는 `1~8` 순서(신규 업무 흐름도는 별도 `50.`)로 유지한다.
- `0. 원본 자료/50. 쿠팡 신규 프로젝트 업무 흐름도`의 최신 Excel은 `new_project_flow_sync.py`가 내부 DB `reports/new_project_flow_db.json`으로만 동기화한다. 현재 `쿠팡 신규 업무 흐름도_2026_07_29_15_05_19.xlsx`의 5개 시트·체크시트 10건을 보관했으며, 관리대장·앱 API·폰/고정주소 사본에는 노출하지 않는다. 원본자료 자동정리(09:35)와 일일대조(09:50)가 이후 파일도 자동 갱신하고, 이전본은 해당 폴더 `보관/연도/월/날짜`로 보존한다.
- `정기점검, 돌발AS 일지 (7.1~).xlsx`를 상시 원본으로 대조해 `28_정기점검돌발AS일지대조`에 기록한다. 2026-07 원본 기준 돌발AS 발생 86·처리완료 66·미처리 18·취소 1건이며, 미처리 사유는 자재·부품 대기 6 / 방문 일정 조율 5 / 기타 3 / 정기점검 이관 2 / 인력·협의 필요 2건이다. 완료일이 비어 있는 처리완료 1건은 임의 입력하지 않는다.
- 대표 보고와 이미지 캡처에는 위 돌발AS 일지 대조·사유와 정기점검 진행률을 함께 넣는다. 프로젝트 진행률은 기준일까지의 UJ 프로젝트만 집계하므로 7~9월 기준 예정 51·실행 44·미실행 7건(86%)이며, UJ 번호가 없는 원본 일정 7건은 진행률에 섞지 않고 원본 매칭 대상으로 보존한다.
- 검증: 합성검증 [79] 포함 ALL GREEN, `fix_workbook.py --check` 이상 없음. 앱 서버는 8899 포트에서 재기동했다.

- 이카운트 로그인 작동(COM_CODE 664540 / USER_ID 유현민 / ZONE AB / 허용IP 220.75.164.157). 품목조회 실데이터 7,281건 수신 확인.
- 전표 자동등록: 유상 62건 중 61건은 ERP 기등록 추정(명세서번호 보유)으로 제외, **JS-2607-041(울산2캠프, 1,472,500원) 1건 전송 대기** — 권한 게이트로 사용자 Run 승인 대기 중.
- 실사례 문제(동기): ERP 전표 2026/03/25-1 인천8MB 26,690,000원 — 회계반영O·세금계산서X·설치여부 미확인(변재선 과장 발견). `erp_ledger_check.py` 유형 A+C가 이런 건을 자동 검출.
- 밴드: 앱 "Coupang Report" 심사중(~2026-07-31 예상). 승인 후 client_id/secret → band_auth.
- 대상 밴드: band.us/band/90610953, band.us/band/84789192 (config band.대상밴드_URL).

### 2026-07-28 갱신 — Excel 통계 2026년 한정·완료 근거 게이트
- `stats_2026.py`: 원본 2025년 행은 보존하되 `00_대시보드`·`01_대표보고`의 누적/월별/문제행/TOP5
  수식을 업무 원천일 기준 **2026년만** 집계하도록 ZIP 패치한다. `07_불일치누락현황`의 숨김 Q열이
  원천 시트의 실제 업무연도를 계산한다. 현재 문제행 원천연도는 2025년 40건·2026년 260건이며,
  2025년 40건은 통계와 TOP5에서 제외됐다. Excel 전체 재계산 후 00·01 수식 오류 0건, TOP5 5건
  모두 AS-26/UJ26임을 확인했다.
- `complete_verified.py`: 완료일과 현재 검증결과만 보지 않고, 완료 상태를 넣은 뒤 요구되는
  사진·완료보고서·비용구분·ERP등록·재방문 여부·관리자 일치까지 모두 갖춘 행만 완료 후보로 잡는다.
  현재 완전한 근거를 갖춘 정기점검 3건은 이미 전부 `완료`; 새 완료 누락은 0건이다.
  `UJ2601328`은 최초 미리보기에서 후보였지만 `작업완료` 입력 후 사진·비용구분·ERP·재방문 근거 부족으로
  `확인필요`가 되어 즉시 원복했다. 근거가 채워지기 전에는 자동 완료하지 않는다.
- 앱·폰 사본도 2026년 전용으로 재검증했다. 업무/점검/문제/정산/ERP/기사 통계는 표시 경계에서
  2026년만 통과하며, 프로젝트 자동조회는 `UJ26` 코드만 보지 않고 내부 작업일·AS/PM/JS ID까지 확인한다.
  실데이터에서 UJ26에 AS-25 또는 2025 작업일이 연결된 4건을 차단했고, 게시 사본 전체의 2025 참조는
  0건이다. 서비스워커 `csos-v7-2026-only`와 암호화 사본을 고정 주소에 재게시했다.
- 합성검증 [48] 추가: 2026년 수식 제한, 숨김 연도 보조열, TOP5, 완료 후 필수근거 게이트를 검사한다.

### 2026-07-26 갱신 — 밴드 전수 반영 완료
- 밴드 **578건 전수 수집**(쿠팡AS 434 + 매출처업무 144, 2025-12 ~ 2026-07). 크롬 확장(Claude in Chrome)으로 수집.
  · 함정: 크롬은 **백그라운드 탭의 타이머를 얼려서** 스크롤 수집이 멈춘다 — 수집 중에는 그 탭을 앞에 둘 것.
- 24_밴드업무추출 421건. `band_extract.py --sheet`는 **--month 없이** 돌려야 전체가 들어간다(월 지정 시 그 달만 남는다).
- 02_돌발AS접수 249행 / 04_정기점검 234행 — 2025-12~2026-07 전 기간, 모두 날짜 오름차순.
- 행 재배치 대상에 **03_현장작업실적·06_거래서류청구수금 추가**. 다른 시트가 이 둘을 상대 행번호로 참조하지 않음을 확인함(ID는 새로 매기지 않고 행만 이동하므로 저장된 ID 참조는 그대로 따라간다).
- 날짜 빈칸: 밴드 게시일로 30건 자동 입력. 근거 없는 **13건은 23_확인필요현황 "날짜_미상"** 구분으로 표기(총 77건).
- 문서 사진 1,458장 `band/docs_inbox/` 확보(fetch_images.py — 브라우저는 CORS로 막히지만 서버 직접 요청은 된다).
  · 다수가 명세서가 아닌 **현장 사진**이라 OCR 인식률이 낮다. doc_ocr는 금액 정합(공급가+세액=합계)이 맞는 건만 채운다.

### 2026-07-27 갱신 — 폰 접속 주소 **영구 고정** 확정
- **고정 주소: `https://mulder4780.github.io/coupang-ecount-reconcile/`** — 사용자 지시로 **어떤 일이 있어도 변경 금지**.
  폰·카톡·문서 어디에 적어도 되는 유일한 주소다. 경유 터널 주소(trycloudflare)는 띄울 때마다 새로 받으므로 **밖으로 내보내지 않는다**.
- 앱 대시보드에 '고정 접속 주소' 카드를 **상시 표기**(복사 버튼 포함). 닫히는 안내 배너(tunnelNotice)는 폐기.
  터널 주소는 같은 카드 하단에 '현재 경유 주소(수시 변동)'로만 부기한다.
- 크롬 **[설치 및 바로가기 만들기]** 는 fetch 핸들러를 가진 서비스 워커가 있어야 뜬다. 설치 판정은 **출처 단위**라
  고정 주소 쪽(`docs/sw.js`)과 앱 쪽(`app_server.py` 의 `/sw.js`) **양쪽 모두** 필요하다 — 한쪽만 두면 그 출처에서만 설치된다.
  · 함정 1: 매니페스트 `start_url`을 **다른 도메인**으로 넣으면 크롬이 매니페스트를 통째로 무시해 설치가 아예 막힌다. 반드시 같은 출처의 `app.html`로 둔다.
  · 함정 2: `_send()` 는 str을 JSON으로 감싼다. `/sw.js` 는 **bytes로** 내보내야 하며, 감싸여 나가면 브라우저가 등록을 거부한다.
- 합성검증 추가: [6] `/sw.js` 실응답 검사(JSON 감싸짐·Content-Type·fetch 핸들러), [26] 고정 주소 상시 표기·양쪽 서비스 워커.

### 2026-07-27 갱신 (2) — 프로젝트 코드 자동채움 · PC 없이 쓰는 폰 앱
- **`project_resolve.py`**: 프로젝트NO 하나 → 시트·행·ID 체인(접수/점검/작업/정산/PO)·캠프·기사·일자·비용.
  증거 우선순위 = 관리대장 기존 행 > 24_밴드업무추출 > 밴드·카톡 원문. 필드마다 출처를 함께 돌려준다.
  · 채번은 **시트 수식과 완전히 동일**해야 한다(접두어-yymm-`ROW()-4`). 어긋나면 사람이 엑셀을 연 순간
    다시 계산돼 ID가 바뀌고, 03·06이 값으로 들고 있는 참조가 전부 끊긴다. 검증 [28]이 실제 시트 수식과 대조한다.
  · ID 열에는 **절대 쓰지 않는다** — 수식이 알아서 채운다. 쓰기는 좌표 지정 + '빈 칸만'.
  · 업무유형을 모르면(밴드 '기타') 시트를 찍지 않고 멈춘다. 02와 04에 같은 코드가 동시에 나온 적은 0건.
  · 현황(v172): 전체 코드 1,026 · 원장 등록 900 · 미등록 117(전부 자동 45 · 일부 수동 28 · 시트 미정 44).
- **원장 v172 — 06시트 채번 수식 정정**: 72행이 02시트에서 복사돼 `"AS-"` 접두어 + `$D`(프로젝트NO)를
  날짜로 읽고 있었다 → `"JS-"` + `$F`(작업완료일). 빈 행이라 드러나지 않았지만 **다음 정산 1건이면**
  정산ID가 접수ID와 이름이 겹칠 뻔했다. 도구: `fix_id_formula.py`(전 시트 일괄 점검).
- **PC가 꺼져도 폰·태블릿에서 쓴다**(고정 주소 그대로):
  `cloud_publish.py` → 사본을 잠가 `docs/data.enc`로 게시(628KB → 압축 46KB → 62KB).
  `csos_crypto.py` = 표준 라이브러리만으로 PBKDF2(60만회) + AES-256-CBC + HMAC(encrypt-then-MAC).
  AES는 **FIPS-197 공식 시험벡터**로 자체검증. 폰은 브라우저 내장 WebCrypto로 연다(PIN 입력 1회).
  `docs/app.html` = PC 독립 앱(코드 조회·자동채움·등록 예약). `docs/index.html`은 터널 상태를
  먼저 묻지 않고 항상 이 앱으로 넘긴다. `/api/enqueue`는 PC가 켜진 뒤 **폰에서 이 앱을 다시 열면**
  기기에 보관된 예약분을 원장 입력 큐로 전송한다.
  · **저장소가 공개**라 평문 게시 금지. 실 PIN을 소스에 리터럴로 적는 것도 금지 —
    한 번 새어 나가면 사본 잠금이 통째로 무의미해진다. 검증 [29]가 `git grep --cached`로 막는다.
- **사내 파일서버는 앱 서버가 못 된다**: Z: = `\172.30.1.250\data`. 445(SMB)만 열려 있고
  22·80·443·5000·5001·8080 전부 닫힘 → 프로그램을 올릴 수단이 없다. 게다가 172.30.x.x 사내망이라
  폰이 LTE로는 애초에 닿지 못한다. **파일을 놓는 곳이지 서비스를 띄우는 곳이 아니다.**
- 남은 용량 주의: 채번 수식이 깔린 빈 행 — 02 64행 · 04 95행 · 05 47행 · 06 56행 · **03 0행**.
  03_현장작업실적에 새 행을 넣으려면 `expand_rows.py`로 먼저 늘려야 작업ID가 매겨진다.

### 2026-07-27 갱신 (3) — 폰 접속 불가의 **진짜 원인** 확정
- **사내 DNS가 `*.trycloudflare.com` 을 풀어 주지 않는다.** 공개 DNS(8.8.8.8)로는 정상.
  그래서 사무실 PC에서 터널을 찔러 보면 언제나 `getaddrinfo failed` →
  `publish_endpoint` 는 "죽은 주소"라며 게시를 취소하고, `tunnel_run.watch()` 는
  "터널이 죽었다"며 주소를 새로 만들었다. **정작 폰(LTE)에서는 살아 있는 주소였다.**
  주소만 끝없이 바뀌니 폰 북마크·홈 아이콘이 매번 무용지물이 됐다 — 며칠간의 접속 불가가 이것이다.
  · `net_probe.py`(신규): 공개 DNS에 HTTPS(DoH)로 물어 IP를 얻고, **SNI·Host는 원래 이름**으로
    보내 바깥에서 보이는 진짜 상태를 확인한다. 표준 라이브러리만.
  · publish_endpoint·tunnel_run 이 이 판정을 쓴다. **앞으로 이 두 곳에서 직접 urlopen 하지 말 것.**
- **터널 대상은 반드시 `127.0.0.1`** — `localhost` 로 주면 윈도우에서 IPv6(`::1`)로 먼저 풀리는데
  앱은 IPv4(0.0.0.0)로만 들어 연결이 거부된다. 증상은 폰에서 **HTTP 530**(터널은 있는데 앱이 없음).
- **생성 한도가 복구까지 막고 있었다**: 4회/시간 제한이 *아무 주소도 없는 상태*에서도 대기를 걸어
  5분간 서비스가 통째로 없었다. 지금은 게시된 주소가 죽어 있으면 즉시 새로 만들고,
  `HARD_MAX`(10회/시간)로 폭주만 막는다.
- 증상 재발 시 첫 진단: `python net_probe.py` (인자 없이 → 게시된 주소를 바깥 기준으로 판정)

### 2026-07-27 갱신 (4) — 기사별 방문 · 버전 파일 정리
- **`tech_report.py`**: 누가 어디를 다녀왔는지. **완료된 것만** 방문으로 센다
  (02는 작업완료일, 04는 실제점검일 — 예정만 있는 건은 '미방문'). 동행 건('김준형, 김필우')은
  **양쪽 모두 1건**씩 세므로 연인원이 행 수보다 많다.
  · 현황(v173): 방문 733건 / 연인원 797 · 김필우 272 · 김준형 205 · 차동호 159 · 권오철 129.
  · **미방문 145건 · 다녀왔는데 기사 미기입 19건** — 후자는 채워 넣어야 할 실제 구멍이다.
  · `--camp 창원` / `--who 김필우` 로 바로 조회. 폰 오프라인 앱에도 [기사] 탭으로 들어간다.
- **`ledger_versions.py`**: 원장 수정 도구 11개가 전부 vN+1을 만들어 하루만 돌려도 대여섯 개가 쌓인다.
  작업 폴더에는 최신본 1개만 남기고 나머지는 지정된 `OLD/` 한 곳으로 **옮긴다(삭제하지 않는다)**.
  최신본은 어떤 경우에도 보호(`plan()`이 보장, 검증 [30]). `daily_run` 마지막 단계로 자동 실행.
- `daily_run` 에 **고정 주소 사본 올리기**(cloud_publish --push) 단계 추가 — 폰이 늘 최신을 본다.

### 2026-07-27 갱신 (5) — 무료 알림(GitHub Actions) 가동
- **왜**: 출장으로 사무실 PC를 며칠 못 켜도 미청구는 계속 늙는다. PC가 꺼져 있으면 아무도 세지 않는다.
  경과일은 **날짜만으로 계산**되므로 클라우드에서 매일 세는 게 맞다. 공개 저장소라 Actions는 무료이고
  **계정을 새로 만들 필요가 없다**.
- **어떻게 알리나**: 기준(90일/120일)을 넘으면 워크플로가 **일부러 실패(exit 1)** 한다 →
  GitHub가 저장소 주인에게 자동으로 메일을 보낸다. **SMTP 비밀번호·외부 서비스·유료 요금제 불필요.**
  메일이 안 오면 GitHub → Settings → Notifications 에서 Actions 실패 알림이 켜져 있는지 확인.
- **무엇을 보나**: `docs/aging.json` — **날짜만** 담는다. 금액·PO번호·현장명은 절대 넣지 않는다(저장소 공개).
  '90일 넘은 게 3건'은 날짜만으로 나오고 알림에는 그거면 충분하다. 상세는 잠긴 사본에 있고 폰에서 PIN이 필요하다.
  · 검증 [33]이 공개 파일 **항목**에 민감정보가 섞이는지, 89/90/120일 경계 판정, 깨진 날짜 내성,
    워크플로가 `${{ secrets.* }}` 를 요구하지 않는지까지 본다.
- 자료가 14일 넘게 안 올라와도 경고한다(= PC가 2주 넘게 안 켜졌다는 뜻).
- 실제 가동 확인(2026-07-27): 미청구 7건 중 90일 초과 3건(최장 154일) → 워크플로 실패 → 메일 발송.
- **한계(중요)**: 어떤 무료 서버도 **원장 엑셀은 못 고친다**. 엑셀이 사내망 파일서버에 있어 밖에서 닿지 못한다.
  쓰기는 앞으로도 사내 PC가 필요하다 — 폰에서 예약 → PC 켜지면 반영 구조가 그 제약 아래 최선이다.

### 2026-07-27 갱신 (6) — 입력 여유 확보 + expand_rows 채번 누락 수정
- **`expand_rows.ID_FORMULA` 에 02·04만 등록돼 있었다.** 03·05·06은 빠져 있어서 행을 늘려도
  **새 행에 채번 수식이 안 심긴다** — 값만 들어가고 ID는 영영 빈칸이 된다.
  실제로 03_현장작업실적을 120행 늘려 놓고도 '여유 0'인 채였다. 5개 시트 전부 등록했다.
  · 날짜열이 시트마다 다르다: 02·04는 **D**, 03·05는 **E**, 06은 **F**. 틀리면 ID의 연월이 어긋난다.
  · 검증 [28]이 `SHEET_ID` 의 모든 시트가 `ID_FORMULA` 에 있는지, 접두어·ID열 이름·**날짜열**까지
    실제 시트 수식과 같은지 대조한다(수식이 없는 13_PO발주관리는 자동 제외).
- **입력 여유 확보**(v194): 03 202→322행 · 02 594→744행 · 04 474→624행.
  현재 여유 — 02 198 · 03 120 · 04 213 · 05 47 · 06 56행. 월 50~60건 속도면 3개월 이상 여유.
  · 엑셀 COM으로 **복구 없이 열림 확인**(27시트·UsedRange 정상·순환참조 없음).
  · 잘못 늘린 v192~194는 `_이전버전/` 으로 되돌린 뒤 고친 코드로 다시 만들었다.
- 13_PO발주관리는 PO관리ID가 **수식이 아니라 수기**다(원래 그렇다). 늘릴 때 채번을 기대하지 말 것.

### 2026-07-28 갱신 — 기사배정 상태 고착·순환참조 경고 대응(v207)
- 첨부 화면의 실제 행은 `02_돌발AS접수` **523행**이었다. L523에 `김준형`이 들어 있어도
  M523이 수식이 아닌 정적 문자열 `미배정`으로 굳어 상태가 바뀌지 않았다.
  `fix_formulas.py`에 **수식 소유 열** 개념을 추가했다. 02시트의 F·G·M·AK·AL·AN은
  값이 보이더라도 수식이 진실의 원천이며, 프로젝트NO(B열)가 있는 행의 정적값을 정상 수식으로 복구한다.
  ID(A열)는 참조 보호를 위해 확정값일 수 있으므로 복구 대상에서 제외한다.
- v205→v206 ZIP 패치 결과: 정적 수식 285개 · 빠진 수식 25개 · 깨진 수식 1개 복구,
  참조 범위 23,242곳을 실제 끝행까지 확장. v206 재감사에서 추가 수정 0개.
  v207은 00 A2·18·20 매뉴얼과 19 인수인계를 함께 현행화한 최종본이다.
- Excel COM 실열기(read-only)로 27시트 정상 개방, `M523=배정완료`,
  수식 `=IF($B523="","",IF($L523="","미배정","배정완료"))`,
  `CircularReference=NONE` 확인. 첨부의 순환참조 경고는 열린 구버전/이전 계산 상태에서 발생한 것으로
  최신 v207에서는 재현되지 않았다. **열려 있는 구버전을 모두 닫고 v207만 다시 열 것.**
- 기사명 오탈자 `권오절`·`권오처르`는 `권오철`로 정규화한다.
  v205의 02 L542·24 E1248을 정정했고, `band_extract.normalize_tech()`가 이후 유입도 막는다.
- 자동화 안정성 보완:
  · `ledger_writer.py` — 동일 값은 버전 미생성, 큐 프로세스 잠금 + JSON 원자 교체.
  · `cloud_publish.py` — 존재하지 않는 `docs/resolve_index.json` 제거, git add/commit/push 어느 단계든
    실패하면 성공 문구 금지, 잠긴 사본도 원자 교체.
  · `fix_formulas.py`·`workbook_patch.py` — 임시 파일을 전량 검증한 뒤에만 정식 vN+1 이름으로 승격.
  · 합성검증 [5]·[10]·[23]·[29]에 각각 회귀 테스트 추가, 전체 38종 ALL GREEN.

### 2026-07-28 갱신 (2) — 입력시간 제외 실시간 문제감시
- **병렬 에이전트 3개로 스케줄·탐지·협업 충돌을 읽기 전용 감사**했다. 실제 워치독이
  08:27·08:57·09:27에도 실행되어 입력 중 서버/터널 복구·원장 OLD 이동·Git 게시를 할 수 있었고,
  `ai_claim.py`는 JSON load→save 경합으로 동시에 성공할 수 있었다.
- `operation_window.py`에 **08:00 이상 09:30 미만(KST) 완전 정지** 규칙을 단일화했다.
  `daily_run`·`watchdog`·`cloud_publish`·`publish_endpoint`와 웹앱 자동 게시/터널 재시작에 방어 게이트를
  넣었다. BAT도 로그 리다이렉션 전에 종료하므로 보호시간에는 프로젝트 파일을 갱신하지 않는다.
- `realtime_monitor.py`는 **PC 로그인·가동 중 4시간 주기 읽기 전용**으로 실행한다.
  Excel `~$` 잠금 없음 + 마지막 저장 120초 경과 + ZIP 검사 통과 뒤 임시 사본을 만들고,
  원본 stat이 그대로일 때만 구조·수식을 감사한다. 원장·ERP·외부 메시지는 자동 수정하지 않는다.
  결과는 `reports/realtime_issues.json|md`에 원자 교체하고 같은 이슈를 신규·지속·해결로 중복 제거한다.
  P2는 두 번 연속 확인돼야 신규로 승격한다.
- Windows 작업 `쿠팡업무_실시간문제감시`와 **Codex 협업감시 하트비트는 모두 4시간 간격**이다.
  이 로컬 감지는 읽기 전용이며, 사용자에게 알리는 하트비트는 위 JSON만
  읽고 신규 또는 P0/P1 지속 문제만 정리한다(2026-07-28 사용자 지정, 자동화 설정도 4시간 확인).
  한계: PC가 꺼졌거나 로그아웃 상태면 실행되지 않는다.
- 워치독의 원장 `OLD` 이동은 제거하고 버전 정리는 `ledger_versions.py`로 일원화했다.
  외부 터널 판정은 직접 `urlopen` 대신 `net_probe`만 사용한다.
  `ai_claim.py`는 원자적 디렉터리 가드 + 임시 JSON `os.replace`로 동시 점유 경합을 차단한다.
- v208 최종 실사: 구조·수식 이상 **0건**. 확인 이슈 1건은 최신 원장이 마지막 일일대조(09:52) 뒤에
  만들어졌다는 이력 차이이며, 다음 일일 교차대조 대상이다. 합성검증 [39] 추가, 전체 39종 ALL GREEN.
- 사용자가 지정한 10:30 카톡 내보내기 두 경로는 실제 PC에 존재하지 않아 내용 대조는 못 했다.
  모니터는 Desktop/OneDrive Desktop의 새 `KakaoTalk_*_group.txt`와 `kakao/inbox/*.txt`를 자동 탐지한다.

### 2026-07-28 갱신 (5) — 카톡 신규 접수 등록 · 입금 자동입력 · **빈 행 판정 실사고**
- **`kakao_extract.py`(신규)**: 카톡 '대화 내보내기' → 02·04 신규 행. 기존 `kakao_reconcile` 은
  **이미 원장에 있는 행**만 맞춰 보는 대조기라 미등록 건은 찾지 못했다.
  류지영 매니저의 `♣［돌발유료 A/S 안내]`·`♣［2026년 0분기 3개월 유료 A/S 안내]` 공지를 구조화한다.
  · 유형을 모르면 **찍지 않는다** — 철거·보관/납품설치/외근공유는 02·04 어느 쪽도 아니다.
  · 담당기사는 `band_extract.normalize_tech` 를 거친다(작업 메모가 기사 칸에 들어가던 문제).
  · `2026.00.00` 같은 템플릿은 빈칸으로 둔다. 두 자리 연도(`26.07.28`)도 읽는다 — 안 읽으면
    신청일자가 통째로 빈칸이 된다(실제 UJ2601345).
  · **카톡 10건 등록 완료(v225→v227)**: 돌발 3(UJ2601343·1345·1347) · 정기 6(UJ2601338·1339·1340·
    1341·1342·1349). UJ2601326 은 **철거·보관**이라 대상 시트가 없어 보류 — 사용자 확인 대기.
  · 카톡 원문엔 있는데 원장에 없는 건이 **82건** 더 있다(철거 21·납품 15·돌발 17·정기 6·유형미상 23).
    `python kakao_extract.py --new` 로 보고 **눈으로 확인한 뒤** 넣을 것. daily_run 은 `--days 7`
    로 최근분만 자동 등록한다 — 과거분을 자동에 태우면 파싱 품질을 아무도 안 본다.
- ★★ **빈 행 판정 실사고(되돌림 있음)**: 첫 빈 행을 **프로젝트NO 열만 보고** 정했다.
  02시트 547행은 사람이 번호 없이 내용만 적어 둔 행(M_순천1·김필우·리모컨)이었는데 '빈 행'으로
  잡혀 **전혀 다른 건(UJ2601347 중구1·경광등)의 번호가 그 행에 들어갔다.** 나머지 칸은 '빈 칸만'
  정책이 막아 줘서 겉으로는 조용했지만 한 행에 두 건이 섞였다. v225 를 `_이전버전/` 으로 되돌리고
  v224 부터 다시 했다.
  · 빈 행 판정은 **행 전체**로 한다(ID열은 수식이라 제외). `kakao_extract`·`backfill_rows` 둘 다 고쳤다.
  · 번호만 없는 행은 '새 행'이 아니라 **번호를 채워 줄 대상**이다 — 내용키 18자 일치 + 날짜 ±3일일 때만
    그 행에 채운다(UJ2601343 이 실제로 548행에 붙었다). 새 행을 만들면 같은 작업이 두 행이 된다.
  · 검증 `[42]`.
- **`receipt_fill.py`(신규)** — 폰 앱 '빈 항목 입력'의 **입금일·입금액을 자동으로** 채운다
  (사용자 지시 2026-07-28: "자료 확인되면 알아서 입력 자동화"). PO번호·PO발행일은 `po_reconcile` 이
  이미 채우고 있었고 입금 두 칸만 사람 손이었다.
  · 근거 = 거래처별계정별원장의 **대변**. 차변은 매출 발생이라 쓰지 않는다.
    '월 계'·'누 계'·'이월' 행을 세면 **수천만 원짜리 가짜 입금**이 하나 생긴다 — 걸러낸다.
  · po_reconcile 과 같은 **양방향 유일 일치**만 자동입력. 쿠팡은 여러 건을 묶어 한 번에 넣어
    금액이 겹치면 엉뚱한 정산행에 붙는다 → 겹치면 사람에게 넘긴다. 발행일보다 앞선 입금도 제외.
  · **청구일·지급예정일은 채우지 않는다** — 사내에서 무엇을 청구일로 보는지(세금계산서 발행일인지
    아리바 업로드일인지), 지급예정일 산정기준이 무엇인지 확정 규칙이 없다. 규칙 없이 날짜를 만들면
    미수 집계가 통째로 틀어진다(절대규칙 10). **사용자 확인 대기** — 확정되면 한 줄 추가하면 된다.
  · 현재 상태: 입금일 빈 정산 **66건**. 계정별원장이 아직 `개별거래처기준` 으로 안 들어와 대변 0건.
  · daily_run 2.7 단계로 상시 실행 — 자료가 들어오는 순간 사람이 시키지 않아도 채운다. 검증 `[43]`.
- `daily_run` 에 2.7 입금 대조·자동입력, 2.8 카톡 신규 접수 등록(`--days 7`) 추가.

### 2026-07-28 갱신 (6) — 쿠팡 입금 원천 확보 · 확인필요현황 엑셀이 하루 멈춰 있었다
- ★ **쿠팡 입금 정본 = `Z:\16. Share\유현민\오종현\26년도 쿠팡 입금내역`** (사용자 지시:
  "항상 여기서 확인해서 정리"). `source_dirs.RECEIPT_DIRS` 에 등록했고 `receipt_fill` 이 매일 읽는다.
  · **입금 70건 · 1,106,167,980원 · 2026-04-13~07-27** (쿠팡로지스틱스 50건 10.95억 +
    캠프 운영 협력사 7곳 20건 1,147만). 협력사·개인 이름은 **이 공개 저장소에 적지 않는다** —
    거래처별 내역은 `reports/입금현황.md`(gitignore된 로컬)에서 본다.
  · **대기항목 #2의 답**: 이카운트 계정별원장에서 입금 0건으로 보였던 건 조회 조건 문제였고,
    실제로는 11억이 들어와 있다. **미수 30.98억은 역시 확정 보고하면 안 된다.**
  · 같은 거래처가 표기만 다르게 적힌다: 끝 글자 누락(`쿠팡로지스틱스`/`쿠팡로지스틱`),
    전각 괄호, 법인격 표기(`(주)○○`/`주식회사 ○○`). `norm_cust` 로 합친다 —
    **법인격을 괄호보다 먼저** 떼야 한다(괄호부터 지우면 `(주)○○` 가 `주○○` 가 되어
    영영 다른 거래처가 되고 금액이 둘로 쪼개진다). 검증 [43]
  · 머리글 행을 박아 두지 않는다 — 사람이 만드는 표라 제목·빈 줄이 늘면 조용히 0건이 된다.
- ★★ **건별 입금 귀속은 지금 자료로는 불가**(억지로 붙이지 말 것):
  입금 중앙값 **6,951,670원** vs 06시트 정산 1건 최대 **3,049,310원** — 한 번 입금에 여러 건이
  묶여 있다. 금액이 정산 1건과 맞는 입금은 **0건**. 어느 계산서에 대한 입금인지는
  **쿠팡 지급명세(remittance)** 가 있어야 안다. `receipt_fill` 은 양방향 유일 일치만 자동입력하므로
  현재는 전량 보류하고 총액 대사만 리포트한다(`reports/입금현황.md`).
- ★ **06_거래서류청구수금이 실제 규모를 담고 있지 않다**: 데이터행 **67건 · 거래명세서합계 4,111만원**,
  **세금계산서합계·입금액 열은 전부 빈칸**(0건). 실제 입금 11.06억과 자릿수가 다르다.
  → 정산 원장 자체를 채우는 게 먼저다. 이 상태에서 나온 미수·수금률은 어떤 숫자든 의미가 없다.
- **`findings_export` 가 daily_run 에 연결돼 있지 않았다** — 그래서 관리대장 폴더의
  『쿠팡_확인필요현황_최신.xlsx』가 **2026-07-27 16:45 자로 하루 동안 멈춰** 있었다.
  읽는 사람은 멈춘 줄 모르고 어제 숫자를 오늘 숫자로 본다. daily_run 6.5 단계로 넣었다.
  · 23시트(평면 93행)와 **중복이 아니다** — 이 파일은 문제유형별 9개 탭에 유형별 상세열
    (명세서번호·PO번호·매칭근거·확인방법)을 담는다. 그 상세는 여기에만 있다. 지우지 말 것.
  · 갱신 결과: 어제 89건 → **오늘 92건**(정산 61·밴드 8·카톡 1·PO 10·날짜미상 10·문서 1·금액 1).
- **폰 앱 시트(상세창) 두 가지 수정**(사용자 지적) — `webapp/index.html`.
  앱의 모든 상세·목록 화면은 `#sheet` **하나**를 돌려 쓴다. 그래서 열고 닫는 길을
  `showSheet`/`closeSheet`/`closeSheetAll` 로 모았다. 새 화면을 띄우는 코드는 이 셋만 부를 것.
  · **뒤로가기가 앱을 통째로 되돌렸다** → `showSheet` 가 `history.pushState` 를 쌓고
    `popstate` 에서 한 겹씩 닫는다. 목록 → 상세로 들어갔으면 **목록으로 돌아간다**
    (이전 내용을 `_sheetStack` 에 보관). 기기 뒤로가기 = 한 겹, 배경 클릭 = 통째로 닫기.
  · **시트를 스크롤하면 뒤 화면이 같이 움직였다** → 열 때 `body` 를 `position:fixed` 로 잠그고
    스크롤 값을 기억했다가 닫을 때 그대로 돌려놓는다(`overflow:hidden` 만으로는 iOS 가 계속 움직인다).
    `.sheet{overscroll-behavior:contain}` 로 스크롤 체이닝을, `.sheet-bg{touch-action:none}` 로
    배경 쓸기를 막았다.
  · 실브라우저(375×812)로 확인: 배경 스크롤 300 → 시트 열림(body fixed·top -300px) →
    뒤로가기 → 시트만 닫히고 스크롤 300 복원 · 2단계(목록→상세) 복귀 · 전체닫기 시 history 원복.
  · `docs/app.html`(오프라인 앱)에는 시트가 없어 해당 없음.

### 2026-07-28 갱신 (7) — ★ 터널 주소는 고정할 수 없다 · 워치독이 깨어날 기회가 없었다
- 사용자가 특정 `*.trycloudflare.com` 주소를 고정해 달라고 했으나 **기술적으로 불가능**하다.
  무료 Quick Tunnel 은 `cloudflared tunnel --url http://127.0.0.1:8899` 로 띄우고 **주소는
  Cloudflare 가 켤 때마다 무작위로 발급**한다 — 지정 옵션 자체가 없다(`tunnel_run.py` 주석에도 기록).
  요청받은 주소는 확인 시점에 **이미 죽어 있었다**(어디서도 이름이 풀리지 않음).
  · 사용자 결정(2026-07-28): **기존 고정 주소 유지** = `https://mulder4780.github.io/coupang-ecount-reconcile/`
  · 진짜 고정 호스트명을 원하면 Cloudflare **명명 터널**이 필요하고 그건 **계정+도메인**이 있어야 한다.
    AI 는 계정을 만들 수 없다 — 사람이 준비하면 배선은 가능(`CLOUD_SETUP.md`).
  · **앞으로도 터널 주소를 밖(폰·카톡·문서)에 적지 말 것.** 고정 주소가 알아서 따라간다.
- ★★ **주소가 죽은 채 방치된 진짜 이유 = 워치독이 안 돌았다.** 로그 공백이 증거다:
  `16:27 정상(probably-article-…)` → **4시간 16분 공백** → `20:43 터널 재시작·옛 주소 게시 취소`.
  워치독 코드는 서버·터널을 제대로 되살린다(`heal_server`·`heal_tunnel`). **깨어날 기회가 없었다.**
  작업 스케줄러 `쿠팡업무_워치독` 설정 3가지를 고쳤다(사용자 승인 2026-07-28):
  | 설정 | 전 | 후 | 왜 |
  |---|---|---|---|
  | 로그온 트리거 | 없음 | **추가** | 재부팅 후 최대 30분 공백을 없앤다 |
  | `StartWhenAvailable` | False | **True** | PC 가 꺼져 놓친 실행을 따라잡는다 |
  | `DisallowStartIfOnBatteries` | True | **False** | 배터리로 돌면 아예 안 돌던 것을 푼다 |
  · 30분 반복(PT30M)은 그대로. 트리거를 교체하면 반복이 사라질 수 있어 적용 후 반드시 확인할 것.
- **쉰 것보다 쉰 걸 모르는 게 문제다** — `watchdog.gap_note()` 신설. 직전 기록과 지금이
  45분(30+여유15) 넘게 벌어지면 로그 맨 앞에 `★ 워치독이 N분 쉬었다` 를 남긴다.
  이게 없어서 '정상'만 줄줄이 쌓인 채 4시간을 아무도 몰랐다. 검증 `[9]` 확장.

### 2026-07-28 갱신 (7) — 첨부 수식 경고·대시보드 잔해 재검증(v229)
- 첨부 화면에서 지목된 “수식 누락 12칸”은 전부 자동복구 대상이 아니었다.
  `00_대시보드` 11칸은 제목·설명용 의도된 공백이고, `03_현장작업실적!H33`은
  정상 기사명 `엄진언`(inlineStr)이다. `<v>`가 없다는 이유만으로 빈칸으로 판단하면 사람 입력을
  덮어쓰므로, 실제 셀 내용과 수식 유무를 함께 보도록 고쳤고 대시보드는 일반 채움 대상에서 제외했다.
- 별개로 대시보드에는 채우기 내림 잔해 **38칸**이 실제로 있었다. v227에서 전용 ZIP 패치로 제거했고,
  `dashboard_clean`은 이제 openpyxl 캐시가 아니라 XML을 직접 읽어 캐시 없는 수식도 잡는다.
  `realtime_monitor`도 대시보드 잔해를 P1으로 감시한다.
- v225→v226 행 재배치 과정에서 누적번호 수식이 목적행 기준으로 정렬되지 않아
  **음수행 참조 44개**와 음수는 아니지만 과거 행까지만 세는 **조용한 누적범위 오류 652개**
  (`02` 275·`03` 120·`04` 257)가 생겼다. v229에서 모두 정상화했고 재검사 결과 0개다.
  `reorder_rows`는 앞으로 누적번호 수식을 목적행 바로 윗행으로 정규화하고, 임시본의 구조·깨진 수식·
  직접 순환참조 검사를 통과한 뒤에만 정식 버전으로 승격한다.
- `fix_formulas`·`dashboard_clean`·`reorder_rows` 실반영은 모두 `ledger` 점유를 요구한다.
  실시간 감시는 누적번호 오류까지 포함한 6개 지표를 검사한다.
- 최종 확인: v229 구조·수식·대시보드 이상 **0건**, Excel 실제 열기에서 복구·순환참조 경고 없음,
  전체 합성검증 **43종 ALL GREEN**. 실시간 P1은 해결됐고 남은 P2 1건은 09:52 일일 대조 이후
  원장이 변경됐다는 이력 차이로, 다음 일일 ERP·카톡·밴드 교차대조 대상이다.

### 2026-07-28 갱신 (8) — PC 종료 시 폰 앱 시작 경로 독립화
- 실제 고정 주소의 `app.html`과 잠긴 사본(`data.enc`)은 정상인데, `/`와 설치 매니페스트가 먼저
  임시 터널을 확인하고 실시간 PC 앱으로 넘겼다. PC 종료·느린 DNS·예전 터널 바로가기에서는
  고정 주소까지 죽은 것처럼 보였다.
- 고정 주소 `/`는 이제 터널을 전혀 확인하지 않고 **항상 `app.html`로 즉시 진입**한다.
  PWA `start_url`도 같은 출처의 `app.html`로 바꿨다. 실시간 PC 앱은 PC가 살아 있을 때만
  앱 안에 선택 버튼으로 표시하며 자동 이동하지 않는다.
- 서비스워커 캐시를 `csos-v3-pc-independent`로 올리고 `index.html`·`app.html`·`data.enc`를
  함께 보관한다. 로컬 서버를 완전히 종료한 뒤 새로고침해도 사본 기준 2026-07-28 16:40,
  프로젝트코드 1,038개가 다시 열리는 것을 실제 브라우저로 확인했다.
- GitHub Pages는 정적 호스팅이라 폰 예약을 서버에 받을 수 없다. 예약은 폰 기기에 보관되며
  **PC가 켜진 뒤 같은 폰 앱을 한 번 열어야** `/api/enqueue`로 전송된다. 안내 문구를 사실대로
  고쳤다. 폰 앱을 닫은 채 PC만 켜도 자동 전송되는 영구 큐가 필요하면 별도 서버리스 저장소가 필요하다.
- 예전 임시 터널 주소로 만든 홈 화면 아이콘은 그 주소가 죽으면 코드가 실행될 기회 자체가 없다.
  기존 아이콘을 한 번 삭제하고 고정 주소에서 다시 설치해야 한다.

### 2026-07-28 갱신 (8) — ★ "재부팅하면 앱이 안 열린다" 의 정체
증상: PC를 껐다 켜면 앱 창이 `probably-article-…trycloudflare.com` 에서
`ERR_NAME_NOT_RESOLVED`. 며칠간 반복됐다.

**원인은 서버가 아니라 창이 터널 주소에 눌러앉는 것이었다.**
1. 앱(`docs/app.html`)이 살아 있는 PC를 찾으면 **그 창을 터널 주소로 옮겼다.**
2. 크롬은 종료 시점의 창 주소를 그대로 복원한다 → 창이 터널 주소인 채로 저장된다.
3. 터널 주소는 **띄울 때마다 새로 발급**되므로 다음 부팅에는 그 이름이 죽어 있다.
4. 죽은 DNS 이름이라 **우리가 서버에서 가로챌 방법이 없다** — 페이지가 아예 안 뜬다.

조치
- `app.html` 의 '실시간 PC 앱 열기' 는 `location.href` 대신 **`window.open(..., '_blank')`**.
  고정 주소 창은 **언제나 고정 주소에 남는다.** 이게 재발을 막는 핵심이다.
  · 앞으로 어떤 화면을 붙이든 **고정 주소 창을 터널 origin 으로 옮기지 말 것.**
- `sw.js` 캐시명을 `csos-v4-no-tunnel-nav` 로 올렸다. **페이지를 고칠 때마다 올릴 것** —
  안 올리면 옛 사본을 쥔 기기가 언제 새 것을 받을지 알 수 없다. 실제로 이번에도
  옛 `app.html` 이 캐시에 남아 있어 고친 뒤에도 한동안 터널로 넘어갔다.
- 실브라우저(사용자 크롬)로 확인: `/` → `/app.html` 이동 후 **22.6초 관찰 · 주소 변화 없음**,
  서비스워커 activated, 캐시 `csos-v4-no-tunnel-nav` 단일.
- 워치독 쪽 보강은 갱신(7) 참조(로그온 트리거·StartWhenAvailable·배터리 제한 해제).

**남은 사람 몫**: 이미 죽은 주소를 띄우고 있는 **그 창을 한 번만 닫는다.**
크롬은 종료 시점에 열려 있던 창만 복원하므로, 한 번 닫으면 다시 살아나지 않는다.

### 2026-07-28 갱신 (9) — 부팅하면 업무 페이지가 한 번에 열린다
- `업무시작.bat` + 시작프로그램 `쿠팡업무_업무시작.lnk` (사용자 지시·승인).
  여는 것: **PC 앱 `localhost:8899`** · 고정 주소 앱 · 이카운트 로그인 · 밴드 2개.
  상세는 `업무시작_설명.md`.
- **크롬 '시작 페이지' 설정은 쓰지 않았다** — 프로그램으로 바꾸려면 크롬 정책 등록이
  필요하고, 그러면 "조직에서 관리 대상" 배너가 붙고 사용자가 직접 못 바꾸게 된다.
  배치는 바로가기만 지우면 원상복구된다.
- ★ **앱 서버를 먼저 기다린다**: Windows 시작프로그램은 실행 순서를 보장하지 않아
  서버가 뜨기 전에 크롬이 열리면 `localhost:8899` 가 연결 오류로 뜬다. 8899 포트가
  응답할 때까지 최대 90초 대기 후 크롬을 연다.
- ★ **`.bat` 에는 한글 주석을 넣지 말 것.** cmd 가 주석을 명령으로 읽고 엉뚱한 오류를 뱉는다
  (CP949 에서 한글 두 번째 바이트에 `0x5C`(`\`)가 섞이는 경우가 있다).
  UTF-8·CP949 둘 다 시도했고 둘 다 깨졌다. 설명은 옆에 `.md` 로 둔다.
- **로그인 유지 — PIN 은 주소(origin)별 저장이다.**
  · `localhost:8899` → `cw_pin` · 고정 주소 앱 → `csos_pin`(이미 저장돼 있음)
  · **터널 주소는 띄울 때마다 바뀌어 매번 다시 로그인해야 한다** → PC 는 localhost 를 쓴다.
    (갱신 8의 '창을 터널로 옮기지 말 것'과 같은 이유다)
  · 이카운트 비밀번호는 도구가 다루지 않는다 — 크롬 비밀번호 저장 기능을 쓴다. 절대규칙 1.

### 2026-07-28 갱신 (10) — PC 독립 영구 입력큐·완료근거 반영·앱 2026년 전용
- 사용자가 쓰는 주소는 계속 **`https://mulder4780.github.io/coupang-ecount-reconcile/` 하나**다.
  PC가 꺼져 있어도 이 주소의 암호화 사본을 열고 입력 예약을 할 수 있다. 예약은 무료 Sites+D1
  백엔드에 즉시 영구 보관되고, PC가 다시 켜지면 `watchdog.py`·`daily_run.py`의
  `cloud_queue_sync.py`가 `ledger` 점유 후 관리대장에 반영한다. 처리 성공 전에는 서버 큐를 지우지 않는다.
  휴대폰 토큰은 PIN 암호화 사본에만, 작업자 토큰은 gitignore된 `config/cloud_queue.local.json`에만 둔다.
- 완료보고·ERP 문서 근거를 자동 반영하는 경로를 일원화했다.
  `confirm_fill.py`는 완료 근거가 정확히 맞는 빈칸만 완료일·담당관리자·최종확인일·완료보고서등록에 넣고,
  `fill_erp_documents.py`는 완료 게시물에서 판매전표·거래명세서가 확인된 프로젝트만
  `ERP등록=완료` / `ERP판매전표=완료` / `거래명세서=발행완료`로 채운다. 상태 수식 셀과 기존값은 건드리지 않는다.
  v238→v239 거래명세서 144칸, v239→v240 완료근거 47칸(완료보고서등록 16건 포함)을 반영했다.
  재대조 결과 두 도구 모두 **잔여 0칸**이다.
- 앱은 원본 엑셀을 지우지 않고 **2026년 건만 표시**한다.
  `app_server.app_year_record(rec, kind)`가 AS=접수일, 정기점검=점검예정일, 정산=완료일,
  ERP=월/전표, 기사방문=방문일 기준으로 정확히 2026년만 통과시킨다. 연도 미상·2025/2026 혼합은 제외한다.
  라이브 앱·모바일 사본·프로젝트 코드·미청구·기사 방문·브리핑·리포트·캡처 경계에도 2차 필터를 두고,
  2026년 ERP 전표의 설명에 남은 `25년` 프로젝트 문구도 화면에서는 제거한다.
  실데이터 사본 확인: 확인필요 83 · AS 502 · 점검 367 · 정산 154 · ERP 96, 2025 표기 검출 0.
- 서비스워커 캐시는 `csos-v6-2026-only`. 합성검증 [45] 영구큐·ERP 문서완료,
  [46] 2026년 전용 표시를 추가했으며 전체 **46종 ALL GREEN**.

### 2026-07-28 갱신 (10) — ★★ 전체 기능 앱의 **영구 고정 주소** 확보 (Tailscale Funnel)
    https://mulder.tailf14aae.ts.net/       ← 전체 기능(실행·전표·엑셀 반영) · 재부팅해도 안 바뀜
    https://mulder4780.github.io/…/         ← 진입점(이 주소가 위를 가리킨다) · 오프라인 사본 겸용

- **왜 이걸로 끝났나**: 무료 trycloudflare 는 주소를 지정할 수 없어 띄울 때마다 새로 받는다.
  그 탓에 폰 북마크·창 복원이 재부팅마다 죽었다(갱신 7·8 참조). Tailscale 은 기기마다
  **MagicDNS 영구 이름**을 준다 — IP 가 바뀌어도, 회선을 갈아도, 재부팅해도 그대로다.
  이 PC 에 Tailscale 이 **이미 사용자 계정으로 로그인·실행 중**이었다(그래서 계정 생성이 필요 없었다).
- **사용자 결정(2026-07-28)**: 공개 범위는 **Funnel(인터넷 공개)**. 폰에 아무것도 안 깔아도 된다.
  · 기존 "LAN 전용 — 외부 인터넷 개방 금지" 문구는 이것으로 **갱신된다.** 다만 실제로는
    trycloudflare 터널이 이미 같은 수준으로 공개돼 있었고, 바뀐 것은 **주소가 고정됐다는 점**이다.
  · Funnel 은 tailnet 관리자 승인이 있어야 켜진다 — **계정 로그인이라 도구가 대신 못 한다.**
    `tailscale_serve.py --funnel` 이 승인 링크를 안내하고 멈춘다. 그 명령은 승인 전까지
    **끝나지 않고 기다리므로** 25초에서 끊는다(안 끊으면 도구가 통째로 매달린다).
  · 되돌리기: `python tailscale_serve.py --off` · 사설망 전용 전환: `--serve`
- **공개 전 확인한 것**: 비로그인 상태에서 `/api/settlements`·`/api/works`·`/api/status`
  **전부 401**. 메인은 PIN 게이트만 내준다. 데이터가 새지 않는다.
- `publish_endpoint.py` 가 **Funnel 이 켜져 있으면 그 주소를 먼저 게시**한다(`tailscale_url()`).
  터널 주소는 차선책으로만 남는다 — 워치독이 돌아도 진입점이 다시 터널 주소로 덮이지 않는다.
- 확인: 진입점 `endpoint.json` → `mulder.tailf14aae.ts.net` · `/api/ping` **200** ·
  전체 기능 앱(186KB, 실행·전표·탭 포함) 서빙 확인.
- 남은 정리: trycloudflare 터널(`tunnel_run`)은 이제 없어도 된다. 당장 떼지는 않았다
  (워치독·게시 경로가 얽혀 있어 Codex 작업과 겹친다). 뗄 때 `heal_tunnel` 도 같이 정리할 것.

### 2026-07-28 갱신 (11) — 용량 큰 **원본**은 서버로 (밴드 문서사진 1,458장)
- 사용자 지시: "용량 많이 차지하는데 옮겨도 문제 없는 것들을 `0. 원본 자료` 로."
- **옮긴 것**: `ecount/band/docs_inbox` 1,458장 · **130.6MB** →
  `0. 원본 자료/4. 밴드 원본/문서사진`. PC 폴더 1,257MB → **1,127MB**.
- ★ **순서가 중요하다**: 코드가 새 위치를 보게 **먼저** 고치고 그 다음에 옮겼다.
  반대로 하면 그 사이 OCR·대조가 자료를 못 찾는다.
  · `source_dirs.DOC_PHOTO_DIRS` 신설(서버 우선, PC inbox 도 계속 봄)
  · `doc_ocr.scan()` 이 **두 곳을 다 훑고 같은 파일명은 한 번만** 읽는다.
    PC inbox 를 남긴 이유: 새 사진을 급히 떨어뜨리는 자리이고, Z: 가 끊겨도 읽혀야 한다.
  · 확인: 이동 후 doc_ocr 가 서버에서 **1,458장 전부** 인식.
- ★ **복사 → 파일별 크기 대조 → 일치한 것만 삭제** 로 옮겼다(단순 move 아님).
  SMB 는 중간에 끊길 수 있어 move 로 하면 그 파일이 그냥 사라진다. 결과: 불일치 0장.
- **옮기지 않은 큰 것들과 그 이유** (원본 자료 폴더는 자료 보관소지 잡동사니 창고가 아니다)
  | 대상 | 크기 | 왜 안 옮겼나 |
  |---|---:|---|
  | `csos_cloud_queue_site/node_modules` | 717MB | npm 재설치로 복원되는 의존성. 옮길 게 아니라 지울 것 |
  | `ecount/outputs/20260728_stats_review` | 312MB | Codex 작업 폴더(관리대장 v242·v243 사본·감사 데이터) |
  | `ecount/webapp/cloudflared.exe` | 55MB | 실행 바이너리 — 옮기면 터널이 죽는다 |
  | `ecount/reports` | 14MB | 매일 도구가 읽고 쓰는 생성물 |
- **폴더 전체를 서버로 옮기는 것은 권하지 않는다**(사용자 질문에 대한 판단):
  코드에 `C:\` 절대경로는 0개라 코드 자체는 돌지만 —
  ① 작업 스케줄러 3개·시작프로그램·바탕화면 바로가기가 절대경로라 **조용히 멈춘다**
  ② Z: 는 **사용자별 매핑**이라 로그온 직후 매핑 전에 작업이 뜨면 실패한다(UNC 로 등록해야 안전)
  ③ **비밀키**(`config/ecount_config.json`·`webapp.json`)가 공유 폴더로 나간다 — 절대규칙 1 위반
  ④ 점유 잠금·입력 큐가 `os.replace` 원자 교체를 쓰는데 SMB 에서 공유위반이 난다
     (Claude↔Codex 동시작업 잠금이 이걸 쓴다 — 깨지면 한쪽 작업이 묻힌다)
  → 정본 자료는 Z:, 코드·캐시·큐·비밀키는 PC. **지금 구조가 이미 그 형태다.**

### 2026-07-29 갱신 — 밴드 사진 ★13만 장★ · 전량 수집은 지금 불가, 반영은 다운로드 없이
- **실제 규모(사진첩에서 직접 확인)**: 쿠팡AS **111,664장** + 매출처업무 **23,328장**
  = **134,992장**(≈12GB · OCR 37시간). 우리가 URL을 가진 건 1,803장(1.3%), 받은 건 1,458장.
  · 왜 적었나: `collect_band.js` 가 **피드 카드에서 최대 12장**만 긁는다(`imgs.slice(0,12)`).
    글 상세를 열지 않아 나머지를 통째로 놓쳤다.
- ★ **브라우저로 전량 수집은 막혀 있다** (시도해 보고 확인, 다시 시도하지 말 것)
  · 사진첩 '전체사진' 그리드가 **49장씩** 로드되고 스크롤로는 안 늘어난다 → `더보기`를
    **약 2,754회** 눌러야 하고, 그동안 탭을 앞에 둬야 한다(백그라운드 탭은 타이머가 얼음).
  · 브라우저 도구가 **쿼리스트링이 든 문자열 반환을 차단**한다. 밴드 이미지 URL 은
    `?type=` 이 있어야 원본 크기를 받으므로 URL 자체를 꺼낼 수 없다.
  · 밴드 **비공개 API 역공학은 하지 않는다** — 깨지기 쉽고 약관 회색지대다
    (카톡 PC 클라이언트 자동화를 안 하는 것과 같은 이유).
  → **정답은 공식 API.** `band_auth`·`band_sync`·`fetch_images` 는 이미 다 만들어져 있고
    `config band.client_id` 만 비어 있다. 앱 "Coupang Report" 심사(~7/31) 통과 시
    `python band/band_auth.py && python band/band_sync.py` 두 줄로 끝난다.
- ★★ **사진 열을 채우는 데는 사진 파일이 필요 없다** — `fill_photos.py`(신규).
  원장의 사진 열이 묻는 것은 "사진이 있느냐"이지 "파일을 갖고 있느냐"가 아니다.
  밴드 캐시의 `photo_count` 가 곧 그 답이다. **68칸을 다운로드 0장으로 채웠다**(v247→v248):
  02 사진등록 62 · 04 점검사진 6.
  · **동영상 열은 채우지 않았다** — 밴드 추출에 **동영상 항목이 아예 없다**(필드는 `사진` 뿐).
    사진이 있다고 동영상도 있다고 적으면 지어내는 것이다(절대규칙 10). 02 동영상등록 496칸·
    04 점검동영상 413칸이 빈 채로 남은 건 그 때문이다 — 자료가 생기기 전엔 그대로 둘 것.
  · 근거 없는 빈칸에 '누락' 을 찍지 않았다. 미완료 건은 사진이 없는 게 정상이고,
    '누락' 은 "있어야 하는데 없다" 는 뜻이라 거짓 지적이 된다(02 65칸·04 60칸 그대로 둠).
- `fill_report_photo --recheck` 재판정으로 완료보고서등록 **10칸** 추가 복구(v248→v249).
  근거는 밴드 완료 글. 처음 돌 때는 03시트 판정에 막혀 0건이었는데, 03시트가 갱신되며 풀렸다.

### 2026-07-29 갱신 (2) — 밴드 전수조사 시도 결과 ★수집 한계 확정★
사용자 지시: "2026-01-01~07-29 게시글 사진·내용 빼먹지 말고 전부."

**2026년 규모(캐시 기준)** — 글 1,114개 · 선언 사진 6,251장 · URL 확보 1,778장.
  월별 글: 01월 186 · 02월 146 · 03월 169 · 04월 164 · 05월 121 · 06월 196 · 07월 132.

★★ **`photo_count` 는 믿을 수 없다 — 실제보다 훨씬 적다.**
  `collect_band.js` 가 피드 카드의 `imgs.length` 를 그대로 photo_count 로 적는데,
  카드는 몇 장만 렌더링한다. 실제로 글 4196 은 캐시 `photo_count=10` 인데
  **상세 페이지에는 48장**이 있었다. 즉 6,251 은 **바닥값**이고 실제는 몇 배다.
  → 밴드 사진 장수를 근거로 쓰는 판단(사진등록 등)은 "있다/없다" 까지만 쓰고
    **장수 자체를 인용하지 말 것.**

★★ **브라우저로는 전수 수집이 불가능하다 — 네 가지를 다 시험했다. 다시 하지 말 것.**
  | 시도 | 결과 |
  |---|---|
  | 사진첩 '전체사진' 그리드 | 49장씩만 로드 · 스크롤로 안 늘어남 → `더보기` 약 2,754회 |
  | 글 상세 **이동** 후 `.uCollage img` | **된다.** 단 글 1개당 1회 이동 → 1,114회 필요 |
  | 글 상세 **fetch(HTML)** 파싱 | **안 된다.** SPA 껍데기 13KB · 이미지 URL 0개 |
  | 비공개 API(`api-kr.band.us`) | 역공학 필요 — **하지 않는다**(약관 회색지대·취약) |
  · 이미지 URL 자체는 브라우저 도구로 꺼낼 수 있다(쿼리 떼고 base 로). 막힌 건 열거 방법이다.
  → **공식 API 가 유일한 길.** `config band.client_id` 만 비어 있고 나머지는 준비 완료.
    심사 상태 확인: `https://developers.band.us/develop/myapps/list`
    (2026-07-24 등록 · "발급 심사중" · 안내문 "약 일주일 소요" → 7/31 전후)

### 2026-07-29 갱신 (3) — 대표보고 숫자 드릴다운·캡처·전역 목록 스크롤
- `webapp/app_server.py`의 `read_exec_details()`가 `01_대표보고` 3절(당일 금액·잔여)과
  4절(리스크)의 숫자를 만든 **동일 원천 행**을 06·07·15·17시트 수식 조건 그대로 읽는다.
  앱이 숫자를 다시 추정하지 않으므로 카드 숫자와 목록 건수가 갈리지 않는다. 현재 v249 실값은
  미청구 22,000원 1건, 금액불일치 1건, 문제업무 260건, 문서경고 67건,
  세금계산서 기한 2건, PO 49건, 명세서 5건, 아리바 4건과 일치한다.
- 3·4절 **모든 카드(0건 포함)**를 누르면 건별 목록이 열린다. 목록은
  `프로젝트NO · 프로젝트명 · 캠프 / 아래 날짜·업무ID·담당자` 순서이며,
  프로젝트를 누르면 프로젝트NO 재검색이 아니라 **종류+레코드ID 정확 일치**로 상세를 연다.
  같은 프로젝트NO가 정산·AS·점검에 겹치면 임의로 정산을 열지 않고 선택 목록을 보여 준다.
- 각 목록 하단에 `이미지로 전달`·`이미지 저장`을 추가했다. 받는 사람은 사용자가 공유창에서
  직접 고르며 자동 발송은 없다. 대량 목록은 이미지 60건 제한과 전체 건수를 함께 표기한다.
- 목록이 안 보이던 직접 원인은 데스크톱 미디어쿼리의 `.slist{display:none}`가 정산 본문뿐 아니라
  **모든 팝업 목록까지 숨긴 것**이다. 이를 `#slist`로 한정했다. 상세 시트는
  `#sheetbody` 독립 스크롤포트와 `#sheetactions` 고정 하단 영역으로 나눠 버튼이 행을 덮지 않는다.
  버튼 위 마우스 휠·PageUp/Down·Home/End를 지원하고 상세 복귀 시 스크롤 위치를 복원한다.
- 합성검증 `[49]` 추가. 인앱 브라우저 실제 검증도 완료: PC 75행(스크롤높이 8,015px),
  모바일 375×812에서 마지막 75행째 노출, 버튼 2개 상시 노출, 버튼 위 휠 620px 이동,
  `JS-2607-007` 정확 상세 이동과 목록 위치 복원 확인.
- 앱 서버 기동 직후의 3시간 자동 게시와 수동 `cloud_publish --push`가 겹쳐 동일 폰 사본 커밋이
  두 번 생기는 것을 실전에서 확인했다(데이터 손실은 없음). `cloud_publish`에 `publish` 점유를
  강제하고 자동 실행은 `CSOS_AI=server`로 주체를 밝혀, 사람·Claude·Codex 게시 중에는
  자동 게시가 즉시 양보하도록 고쳤다.

### 2026-07-29 갱신 (4) — 완료일 누락 상세·유수비 대표 접수 당일 처리 반영
- 대표보고의 `완료일이 안 적힌 오래된 건` 경고 카드 자체를 클릭형으로 바꿨다.
  누르면 전체 원천 목록(프로젝트NO·캠프·접수일·접수ID·담당기사)이 열리고, 각 행은
  `종류+접수ID`로 정확한 AS 상세를 연다. 하단 `이미지로 전달`·`이미지 저장`은 목록과
  분리된 고정 영역이며 이미지에는 최대 60건과 담당기사가 함께 들어간다. 합성검증 `[50]`.
- 유수비 대표가 2026-07-28 게시한 `GWJ1 M_순천1 / 리프트 리모콘 작동 안함` 건은
  류지영 매니저가 리모컨 택배 발송을 완료했다고 사용자가 확인했다. 이를 현장 AS 완료로
  오분류하지 않고 `reports/manual_daily_events.json` 근거의 **당일 업무 처리 1건**으로
  브리핑·앱에 표시한다(접수 07-27, 처리 07-28, 게시자·처리자·요청·처리내용 표기).
  프로젝트NO가 아직 없는 실제 접수도 날짜·캠프·내용이 있으면 브리핑 로더가 버리지 않는다.
  합성검증 `[51]`; 실제 `/api/brief?date=2026-07-28`와 브라우저 보고 화면에서 확인했다.
- **판정 주의**: `택배 발송 완료`는 부품/리모컨 발송 단계이지 AS 완료·수령 완료가 아니다.
  프로젝트NO가 확정되기 전까지 작업완료일을 채우거나 임의 UJ 번호를 만들지 않는다.
- 고정 주소의 `docs/app.html`에도 같은 두 결과를 표시한다. `cloud_publish.py`가 잠긴
  `data.enc` 안에 `당일처리목록`·`완료일미기입목록`을 넣고, 오래된 건 카드를 누르면
  전체 행을 끝까지 스크롤한다. 프로젝트 번호가 있는 행은 코드 조회로 이동하며,
  하단에서 담당자 전달용 PNG를 저장하거나 운영체제 공유창으로 전달할 수 있다.
  설치형 휴대폰도 즉시 새 화면을 받도록 서비스워커 캐시는 `csos-v8-brief-list-2026-only`.

- **지금 할 수 있는 만큼은 했다**: `fetch_images.py --all --year 2026` 신설
  (기존에는 본문에 명세서·계산서·견적·청구·세금이 든 글의 사진만 받았다).
  저장 위치도 서버('0. 원본 자료/4. 밴드 원본/문서사진')로 바뀐다(`out_dir()`).
- **본문(내용)은 이미 전수 확보돼 있다** — 24_밴드업무추출 4,279행 · 2025-12~2026-07.
  빠진 것은 사진 URL 이지 글 내용이 아니다.
- ★★ **결론: 밴드 사진에서 뽑아낼 데이터는 사실상 없다** (2026-07-29 실측, 1,731장 전량 OCR)
  · 신뢰도 **높음 0건** · 보통 10 · 낮음 1,721 · 판정 **일치 4건**(JS-2607-047·049·059·065)
  · **대장 빈칸 입력 0건** — 신규 273장을 받아 OCR 했지만 원장에 들어간 값이 하나도 없다.
  · 이유: 밴드 사진의 대부분이 **현장 사진**이지 서류가 아니다. 서류 사진도 기울기·반사로
    금액 정합(공급가+세액=합계)을 못 넘긴다.
  → **서류 데이터의 정본은 Z 폴더 PDF 3,194장**이다(파일명에 날짜+캠프명, 1:1 확정 1,156건).
    같은 목적에 밴드 사진을 더 받는 것은 실익이 없다 — 공식 API 가 나와도 이 판단은 같다.
    사진은 '있다/없다' 증빙으로만 쓰고, 금액·번호는 Z 폴더 PDF 와 ERP 내보내기에서 얻는다.

### 2026-07-29 갱신 (3) — ★ 자료현황 한 장 (`data_status.py`)
사용자 지시: "물어본 것들은 DB에 반영해서 다음에 빨리빨리 불러올 수 있게."

**같은 질문을 매번 다시 세지 말 것.** 아래는 이제 한 곳에서 나온다.
    reports/자료현황.md    ← 앱 **[기록] 탭 맨 앞**에 '자료현황' 으로 뜬다
    reports/자료현황.json  ← 다른 도구가 쓰기 좋게

담는 것: 밴드(레코드·기간·월별·유형·사진·OCR) · Z폴더 서류(PDF·1:1 확정·짝없음) ·
입금(건수·합계) · 지금 갖고 있는 원천 자료 · 관리대장 시트별 채움/빈칸 · 접속 주소 3종.

- ★ **느리면 아무도 안 본다.** Z: 2만 개 순회 같은 무거운 일은 **다시 하지 않고**
  `zscan`·`receipt_fill` 이 남긴 리포트에서 숫자만 읽는다(`from_report`).
  그래서 daily_run 에서 **대조·리포트가 끝난 뒤(6.7단계)** 에 와야 한다 —
  먼저 돌면 지난 숫자를 읽는다. 검증 [52] 가 이 순서까지 본다.
- 앱 노출은 파일명이 `app_server.latest_reports()` 목록과 정확히 맞아야 한다
  (`("자료현황.md", "자료현황")`). 이름을 바꾸면 앱에서 사라진다.
- 수동 갱신: `python data_status.py` (화면만 보려면 `--print`)

### 2026-07-29 갱신 (4) — ★ 세션이 갑자기 끊겨도 이어받는다 (`session_handoff.py`)
사용자 지시: "세션 컨텍스트가 다 차도 다음 세션에서 문제없이 구동되게."

**문제**: 종료 체크리스트는 **끝낼 시간이 있을 때만** 지켜진다. 컨텍스트가 차거나 크레딧이
끊기면 그럴 기회가 없다. 실제로 이 세션에서도 확인됐다 —
`ledger`(claude)·`publish`(codex) 점유가 **프로세스는 이미 죽었는데 잠금만 남아** 있었다.
그대로 두면 다음 세션이 45분간 원장을 못 고친다.

**해결 — 사람이 기억해서 넘기지 않는다. 파일이 기억한다.**
- 워치독(30분 주기)이 `session_handoff.py --snapshot` 을 부른다 →
  `reports/세션인계.md|json`. **세션이 언제 죽든 최대 30분 전 상태가 남는다.**
- 새 세션은 `python session_handoff.py --check` 를 **시작 체크리스트 0번**으로 읽는다
  (CLAUDE.md 갱신). "먼저 처리할 것" 이 빌 때만 새 작업을 시작한다.

**무엇을 잡아내나** — 큐 미반영 · `.tmp.xlsx` 잔재 · 죽은 점유 · 미커밋/미푸시 ·
최근 커밋(뭘 하던 중이었나) · AGENTS.md 의 '다음 할 일'(두 곳에 적지 않고 발췌).

- ★ **죽은 점유는 프로세스 생사(pid)로 판정한다.** 시간(45분)만 보면 그동안 원장이 잠긴다.
  · `ai_claim` 은 `at` 을 **에포크 초(float)** 로 적는다 — ISO 문자열이 아니다.
    이걸 틀리면 경과가 `?` 로 나와 죽은 잠금을 못 가려낸다(처음에 실제로 그랬다).
  · 살아 있는 점유는 **잔재가 아니다** — 상대가 일하는 중이므로 건드리지 않는다.
- ★ **이 도구는 아무것도 고치지 않는다.** 자동으로 점유를 풀거나 큐를 반영하면
  상대 AI 작업을 가로챈다. 무엇이 걸렸는지와 **풀 명령**만 알려 준다. 검증 [53] 이 이걸 강제한다.
- CLAUDE.md 에 **"컨텍스트가 차 가면"** 절 추가 — 끝까지 못 갈 것 같으면 순서대로 잘라낸다:
  ① 큐 비우기 ② 점유 놓기 ③ 커밋 ④ 19시트·AGENTS.md. 앞의 것일수록 안 하면 손해가 크다.

### 2026-07-29 갱신 (5) — 철거·신규납품은 **DB에만** 둔다 (앱 비표시)
사용자 지시: "철거 신규 납품 건은 DB만 저장해놓고 앱에 표기하지마."

- **저장 자리 = `05_신규납품설치`**. 이 시트에는 `철거·이전예정일` 열이 따로 있고
  기존 3행도 전부 업무구분 '철거' 라 원래 그 자리다. **앱이 읽지 않는 시트**이기도 해서
  지시와 정확히 맞는다(앱이 읽는 시트: 00·01·02·04·06·07·10·15·16·17·23·25·26).
  · 카톡 보류분 **36건 등록**(철거 21 · 납품 15) → 8~43행, v252→v253.
  · `kakao_extract.SIDE_KIND` 가 유형을 업무구분('철거'/'납품')으로 옮긴다.
    값은 반드시 `10_코드관리` M열 목록 안에서 쓸 것(신규발주·견적·PO·제작·출고·납품·설치·
    철거·이전·계단·안전바·경보장치·메자닌·이동식상하차리프트·기타).
- **앱은 화면에서만 뺀다 — 원장에서 지우지 않는다.** `isSideWork()` 가 데이터를 받는
  **관문 5곳**(정산·AS·점검·확인필요×2)에서 거른다. 되돌리려면 `SIDE_WORK` 를 비우면
  그 즉시 다시 보인다(자료는 그대로 있다).
- ★ **함정**: 05시트 업무구분은 `'납품'`·`'철거'` 처럼 **한 단어**다.
  처음에 `신규\s*납품` 으로 잡았더니 정작 원장에 적힌 `'납품'` 을 놓쳤다.
- ★ **돌발AS·정기점검은 절대 걸리면 안 된다** — 걸리면 본업이 화면에서 통째로 사라진다.
  검증 [54] 가 별도공사 5종은 걸러지고 본업 2종은 남는지, 관문 5곳에 다 걸렸는지 본다.
- 과거 카톡 02시트분 15건은 **함께 넣지 않았다** — "과거분은 파싱 품질을 사람이 한 번 보고
  넣는다" 는 규칙(갱신 5) 때문이다. 넣으려면 `python kakao_extract.py --new` 로 먼저 볼 것.

### 2026-07-29 갱신 (6) — 정기점검 예정·실행 상세와 담당자 캡처
- 대표 브리핑 정기점검은 기준일 `예정·실행`, 분기 `전체 예정·실행·미실행`을 따로 표시하고
  각 숫자를 누르면 그 숫자를 만든 전체 점검 목록을 연다. 목록은 과거→최근 정렬이며
  프로젝트NO·캠프·예정일·실행일·담당기사·점검ID를 보여 준다.
- 프로젝트 번호를 눌러도 중복 프로젝트의 다른 업무가 아니라 `점검ID` 원천 행으로 이동한다.
  상세 하단 `이미지로 전달`·`이미지 저장`은 기존 내부 공유창만 사용하며 자동 전송하지 않는다.
- `daily_brief.py`가 `점검예정목록`·`점검실행목록`·`분기점검목록`을 단일 원천으로 만들고,
  고정 주소 암호화 사본도 같은 목록을 쓴다. 설치형 앱 캐시 `csos-v9-pm-brief-2026-only`,
  합성검증 `[55]`.

### 2026-07-29 갱신 (6) — ★ 원문에 있으면 자동 기입한다 (모든 데이터 공통 원칙)
사용자 지시: "밴드나 카톡에서 확인되면 자동 기입해서 정리. **모든 데이터가 마찬가지야.**"

**이 프로젝트의 표준 경로다. 새 칸을 채울 때도 이 순서를 따른다.**
1. 원문에 있으면 **자동으로 채운다.** 사람이 옮겨 적게 하지 않는다.
2. 원문에 없으면 **비워 두고 확인 목록에 남긴다.** 지어내지 않는다(절대규칙 10).
3. 사람이 적어 둔 값은 **절대 덮지 않는다.** 단 자리표시자·빈 양식은 값이 아니다.
4. **무엇을 근거로** 채웠는지 함께 적는다.

- `fill_work_detail.py`(신규): 03시트 `실제작업상세` ← 카톡/밴드 **완료 글의 'A/S 내용'**.
  daily_run 2.9 단계. 이 칸이 비면 대표 브리핑에 "작업 내용이 안 적힌 완료건" 으로 뜬다.
  · 반영: 85건 + 7건 = **92건**(v253→v256). 남은 32건은 원문에 없어 비워 뒀다.
- ★★ **'신청내용'(요청)을 실적 칸에 쓰지 않는다.** 그건 "해 달라" 이지 "했다" 가 아니다.
  카톡 자료를 세어 보면 작업내용은 **26건**인데 신청내용만 있는 게 **542건**이다.
  구분 없이 채웠다면 대표 보고의 '무엇을 했나' 가 통째로 거짓이 됐다. 같은 이유로
  밴드도 **완료 글만** 쓴다(접수 글 본문도 요청이다).
- ★★ **공지의 빈 양식도 값이 아니다** — `?? 호기` · `What ? / 갯수 ?` 는 기사가 아직 안 채운 칸이다.
  실제로 그런 글이 있다(UJ2601350 등). 여러 호기 중 일부만 적힌 글은 **뒤에 빈 양식이 남으므로
  거기서 잘라야** 한다 — 처음에 안 자르고 넣었다가 6행에 섞였고, 잘라서 0행으로 정리했다.
  `※ 확인사항` 뒤(서류 발행·보고 지시)도 실적이 아니라 자른다.
- 검증 `[56]` 이 이 규칙 전부를 지킨다(신청내용 폴백 금지·빈 양식 제외·사내메모 제외·매일 실행).

#### 같은 날 이어서 — 왜 4건이 계속 '미기입' 이었나 (원인 둘, 실사고 하나)
① ★★ **`A/S 내용` 이 언제나 빈 값이었다.** 필드 정규식이 `▒` 에서 값을 끊었는데,
   그 값은 `▒▒ 01 호기 ▒▒ (유료)리모콘 SET 교체 완료` 처럼 **정확히 `▒▒` 로 시작한다.**
   고치니 작업내용이 **26건 → 840건**. 값의 끝은 `●`·`★`·`♠`·`※` 로 본다. 검증 [56].
② **같은 건이 '안내' 와 '완료' 로 두 번 온다.** 안내 글의 A/S 내용은 빈 양식이라
   그걸 값으로 치면 완료 글의 진짜 내용이 병합에서 밀려난다 → `work_content()` 로 거른다.
③ ★★★ **실사고(즉시 되돌림, v259)**: 화면의 4건은 03시트 칸이 빈 게 아니라 **행이 없었다.**
   그래서 빈 행에 실적을 써 넣었는데 — **03시트 B열(접수ID)은 값이 아니라 수식**이고
   `02시트에서 작업완료+완료일 있음+아직 03에 없는 접수ID` 를 **스스로 끌어온다.**
   내가 쓴 행의 접수ID는 **엑셀이 재계산할 때 정해지므로**, 엉뚱한 작업에 남의 실적이
   붙을 상태였다. v259 를 만들자마자 `_이전버전/` 으로 되돌렸다.
   → **03시트에 행을 직접 만들지 말 것.** 올바른 순서는
     ① `expand_rows` 로 행만 늘린다(322→502 완료) ② **엑셀을 한 번 연다**(B열이 채워진다)
     ③ 그 다음 `fill_work_detail` 이 프로젝트NO 기준으로 채운다.
   현재 대기: **219건**(원문에 작업내용 있음) · 빈 행 여유 300. 엑셀만 한 번 열면 풀린다.

### 2026-07-29 갱신 (7) — 보고 아래 `확인 필요` 전용 화면
- `webapp/index.html` 왼쪽 메뉴에서 **보고 바로 아래·기록 위**에 `확인 필요` 독립 메뉴를 추가했다.
  기존 정산 탭의 평면 표는 그대로 두고, 새 화면은 `23_확인필요현황`을
  **전체 건수·문제유형·담당자 표기 없음·30일 초과**로 요약한다.
- 문제유형별·담당자별 카드와 검색·유형·담당자 필터가 있는 전체 목록을 제공한다.
  목록 행에는 프로젝트NO·원천 ID·캠프·일자·담당자·금액·내용/근거를 표시하며,
  `AS/PM/JS` ID를 판별해 **같은 프로젝트의 다른 기록이 아닌 정확한 원장 행**을 연다.
- PO 확인행은 PO번호→정산 PO번호→근거의 UJ번호 순으로 연결하고, 원장에 아직 없는 7건은
  경고창으로 끝내지 않고 **확인 원문 상세**를 연다. 청구·입금·PO 확인은 내부 담당자,
  현장 확인은 원천 AS/PM의 담당기사를 보완해 빈 담당자 한 묶음으로 뭉치지 않게 했다.
- 유형·담당자·현재 필터 결과는 기존 전달용 이미지 엔진에 연결했다.
  상세창 안에서 목록을 끝까지 스크롤해도 `이미지로 전달`·`이미지 저장` 버튼은 하단에 고정된다.
  자동 외부 전송은 없고 사용자가 공유창에서 직접 보낸다.
- 2026년 전용·별도공사 비표시 규칙을 전용 화면에서도 다시 적용한다.
  실브라우저에서 원장 89건·유형 9개 표시, 유형 목록 54건 스크롤, `JS-2607-001` 정확 이동까지 확인.
  합성검증 `[58]`.

### 2026-07-29 갱신 (8) — 프로젝트번호 대표표시·유수비 예외보고·UI 동작 통일
- 화면 카드·KPI·상세·캡처에서 `AS/PM/JS/PO` 내부번호를 대표번호로 쓰지 않는다.
  원장 3종을 역참조해 `UJ26…` 또는 ERP 묶음번호를 찾고, 못 찾으면 **프로젝트 미확정**으로 표시한다.
  확인필요 PO 7건 실측: 근거에서 UJ번호 6건 복원, 1건은 미확정으로 남기며 PO번호를 프로젝트처럼 보이지 않는다.
- `webapp/app_server.py`에 원장을 고치지 않는 **읽기 전용 대표 예외보고 모델/API**를 추가했다.
  돌발AS 전산상 미완료·D+1/D+2/7/30·현장완료 서류미정리, 정기점검 실제 분기대상·목표누계·
  계획대비·잔여 필요처리량·기사당 필요량·예상미달·신호, 거래명세서 업무유형별 대상/발행/미발행을 계산한다.
  2026-07-29 v259 실측은 AS 미완료 76(D+2 72), 서류미정리 117, 점검 목표누계 16/완료 44,
  거래명세서 미발행 1건이다. 수치는 최신 원장을 다시 읽을 때마다 변한다.
- 대시보드 상단에 정상 상세은 접고 예외만 보이는 `유수비 대표 예외보고`를 추가했다.
  숫자를 누르면 프로젝트번호·캠프·일자·내부ID가 있는 정확한 목록과 기존 PNG 저장/수동 공유가 열린다.
  `확인 필요`에도 AS D+2·점검 계획대비·명세서 미발행·미확정 업무기준을 같은 원천으로 표시한다.
- 사용자가 제안 범위를 그대로 확정했다(2026-07-29). 내부 확인 담당은
  류지영=캠프/일정·카톡/밴드·현장자료·완료일, 변재선(회계)=거래명세서·세금계산서·입금·금액 불일치,
  오종현=PO 원본·견적서·구매/입금 원천자료 취합·누락, 유현민=PO 대조·ERP·원장 미등록·시스템 연결이다.
  현장 작업 담당과 별개인 **확인 책임**이며,
  `responsibility.py`를 앱·`23_확인필요현황`·별도 확인필요 엑셀이 함께 사용한다.
  v267 실측 97건은 류지영 24·변재선 62·유현민 11건, 미지정 0건이다.
- 버튼 눌림·키보드 초점·페이지 전환 애니메이션을 전 화면에 통일하고 `prefers-reduced-motion`을 지킨다.
  데모 브라우저에서 대표 KPI→5건 목록, UJ번호 대표표시, 시트 `overflow:auto`(681px/442px),
  하단 캡처 버튼 고정, 담당 범위 확정으로 미확정 정책은 7→5건이다. 합성검증 `[72]` 포함 전체 ALL GREEN.

### 2026-07-29 갱신 (9) — 개별 카톡 미확인 건 사용자 완료처리
- 사용자 지시로 `UJ2600017 / AS-2601-046`의 `카톡 보고 미확인` 1건을 완료 처리했다.
  원장 자체의 진행상태·완료일·완료보고·ERP·관리자검증은 이미 완료/일치였고, 카톡 원문 매칭만
  없던 건이므로 다른 업무 상태를 바꾸지 않았다. `02_돌발AS접수`의 빈 `조치내용`에
  `카톡 보고 미확인 완료처리(사용자 지시 2026-07-29)`를 근거로 기록했다(v259→v260).
- `kakao_reconcile.manual_kakao_done()`은 **이 명시적 문구가 있는 건만**
  `확인 / 사용자완료처리`로 유지한다. 완료 상태 전체를 카톡 확인으로 간주하지 않으므로
  실제 미보고 건을 한꺼번에 숨기지 않는다. 다음 일일 대조가 실행돼도 이 건이 다시 살아나지 않는다.
- 최신 카톡 대조는 해당 ID가 `확인 / 사용자완료처리`, 카톡 미확인 0건으로 재생성됐다.
  `23_확인필요현황`과 『쿠팡_확인필요현황_최신.xlsx』를 갱신해 이 건을 제거했다(v260→v261).
  실앱 확인 필요 화면에서도 `UJ2600017`·`AS-2601-046` 검색 결과 0건을 확인했다.
  합성검증 `[4]`에 사용자 개별 완료가 재대조 후에도 유지되는 회귀검사를 추가했고 전체 ALL GREEN.

### 2026-07-29 갱신 (10) — 관리대장 구버전 보관 위치 `OLD`로 단일화
- 작업 폴더에는 최신 관리대장 1개만 남기고 이전 v파일은 반드시 사용자가 지정한
  `...\00. 쿠팡 통합업무 일일보고 관리대장\OLD`로 옮긴다. `daily_run` 마지막 단계와
  `resolve_master`·`latest_master`의 자동 정리가 모두 같은 `ledger_versions.py`를 사용한다.
- 과거 `_이전버전/`에 남은 관리대장도 다음 정리 때 `OLD/`로 합친다.
  `OLD/`에 같은 파일명이 이미 있어도 기존 파일을 덮거나 어느 쪽도 삭제하지 않고
  `__from_이전버전` 꼬리표를 붙여 둘 다 보존한다.
- 자동 대상은 `쿠팡_통합업무_일일보고_관리대장_v*.xlsx`만이다. 다른 엑셀·잠금파일은 건드리지 않으며,
  최신본 존재를 이동 후 재확인한다. 합성검증은 `CSOS_SYNTHETIC=1`로 실폴더 이동을 차단한 상태에서
  최신본 보호·동명 충돌·예전 폴더 통합을 검증한다.

### 2026-07-29 갱신 (11) — 류지영 정기점검 원본 폴더 자동동기화
- 고정 입력 폴더는
  `0. 원본 자료/5. 정기점검 스케쥴 원본`이다. `pm_schedule_sync.py`가 폴더의 최신 xlsx와
  현재 연도·현재 분기 시트를 자동 선택하고, `daily_run`이 매일 관리대장 복구 직후 실행한다.
  앞으로 류지영이 같은 폴더의 원본만 갱신하면 별도 요청 없이 다음 일일 실행에 반영된다.
- 원본을 열어 둔 상태에서도 크기·수정시각이 안정된 사본만 읽는다. 현재 분기 밖의 월과
  `철거·점검불가·폐쇄·매각` 행은 제외하고, 캠프·일정·기사 단위로 묶어 안정적인 `SCH-2026Q3-…`
  일정ID를 만든다. 원본 월과 확정 날짜가 다르면 **확정 날짜의 월을 우선**한다.
- 관리대장에는 `27_정기점검원본일정`을 ZIP 패치로 생성한다. 기존 `04_정기점검`의 동일 캠프·월
  실적이 있으면 완료/실행 사실을 우선하며, 근거 없는 UJ 프로젝트번호는 만들지 않는다.
  앱에는 04와 겹치지 않는 원본 일정만 `예정월`로 추가하고 프로젝트가 없으면
  `프로젝트 매칭 대기`로 명시한다. 원본·행번호·기사·장비 상세를 읽기 전용 상세에서 확인한다.
- v272 실측: 원본 354행, 제외 52행, 일정 123그룹(날짜확정 32·월예정 91),
  완료 실적 우선 40, 04 동월 연결 2, 프로젝트 매칭 대기 81. 앱 정기점검은 전체 448건이며
  추가 원본 일정 81건을 포함한다. 같은 원본 재실행은 새 버전을 만들지 않는다.
- 정산 목록이 0건으로 보이던 원인은 전역 `periodRange()` 함수명 충돌이었다.
  업무 목록용을 `workPeriodRange()`로 분리해 정기점검 448건과 원본 상세/스크롤을 실브라우저에서 확인했다.
  합성검증 `[71]·[73]`이 함수 충돌 재발과 원본 자동동기화·멱등성을 막는다.

### 2026-07-29 갱신 (12) — 확인필요 담당 기준에 오종현 추가
- 사용자 지시로 `확인 필요 > 담당자별` 확정 기준 카드에 **오종현**을 추가했다.
  역할은 `PO 원본·견적서, 구매·입금 원천자료 취합·누락 확인`이며, 현재 확인할 건이 0건이어도
  확정 범위 카드와 상세 설명은 항상 표시한다.
- 역할 경계는 그대로 유지한다. `PO A` 같은 PO 대조·ERP·원장 미등록·시스템 연결은 유현민,
  입금·금액 판정은 변재선(회계), 캠프·일정·현장자료는 류지영이다.
  `PO 원본 누락·견적서/구매/입금 원천자료 누락`처럼 **자료 취합 문제만** 오종현에게 자동 분류한다.
- v274 읽기 전용 실측 88건은 변재선 62·류지영 18·유현민 8·오종현 0건이다.
  데모 브라우저에서 오종현 카드와 `원천자료 취합 담당` 상세 모달을 확인했고 합성검증 `[72]` 전체 ALL GREEN.

### 판단 기록 — 무료 클라우드의 역할
- 클라우드는 PC가 꺼진 동안 **조회 사본과 입력 예약을 보관**한다. 사내 Z: 관리대장 자체는 PC/사내망에
  있으므로 PC가 꺼진 상태에서 직접 수정할 수 없고, PC가 돌아오면 점유·합성검증 규칙을 거쳐 반영한다.
- '기타' 44건은 밴드 본문이 아예 비어 있어 업무유형을 **추측할 수밖에 없다** → 리졸버가 거부하는 게 맞다.
  억지로 분류하면 엉뚱한 시트에 등록된다. 사람이 확인할 목록으로 남긴다(`--audit` 에 나온다).

## 확정 사실·의사결정 (2026-07-24)
- **3월 인천8MB 전표(2026/03/25-1, 26,690,000원): 실제 설치 안 됨 확정** — 회계반영O·실물근거X. 전표 정정 여부는 변재선 과장/회계 판단(세금계산서 미발행이라 국세청 이슈 없음). 관리대장 v23 19시트 65행 기록.
- 비밀번호 변경 안 함(사용자 결정 — 재권고 불필요).
- 대표 보고: 매일 **10:30** 캡처+유선 구두보고(캡처·전송은 사용자 수동 — 자동화 제안 금지). 자동대조 에이전트는 **09:50** 작업 스케줄러 등록 완료("쿠팡업무_일일자동대조").
- 일일 운영 흐름: **08:00~09:30 류지영 매니저가 최신 관리대장에 수동 입력** → 09:50 에이전트가 그 입력 포함 전체 대조·확정분 자동입력(vN+1) → 10:30 보고. 류 매니저는 항상 **최신 v 파일**에 입력해야 함(워크벤치 [관리대장 열기] 사용 권장) — 구버전 편집 시 워크벤치가 포크 경고 표시.

## 확정 사실 추가 (2026-07-25 오후)
- **ERP 왕복 루프 실증 완료**: JS-2607-041 → SaveInvoiceAuto 전표 `20260720-2` 생성 → 원장 v27 M45·N45 자동기록.
- **이카운트 검증 체계**: 실서비스 키는 [API인증현황] '검증'된 API만 호출 가능("인증되지 않은 API" 오류). 미검증 API는 테스트키(IS_TEST=true, sboapi)로 — **sboapi도 실DB에 기록됨**. 테스트키 만료 **8/7** 전에 실키로 SaveInvoiceAuto 재시도해 검증 완료 확인할 것. SlipNos는 리스트 회신 → `2026/07/20-2` 규격 정규화(코드 반영).
- **ledger_writer 실사고 수정**: 자기닫힘 빈 셀(`<c s=.../>`)ary 정규식 오매칭 → 빈칸 채움 전면 실패했었음. 2단계 태그 파싱 + t5 회귀테스트.
- 밴드 백필: 4~7월 487건 캐시. 게시누락 잔존 **1건: AS-2607-021**(송파5MB 초이동·김필우·7/10).
- 앱: 나눔고딕·KPI 드릴다운·히어로 배너·보고탭(PNG 캡처·카톡 공유)·기준일 설정(00 B3·B4)·연간 월별표.
- IP 유동 회선: 현재 112.154.53.212 등록됨. 재부팅 후 변경 시 로그인 오류 메시지에 새 IP 표시됨 → 재등록.

## ★ 신규 행 추가 시 필수 주의 (2026-07-25 실사고)
관리대장 ID열(접수ID·점검ID·정산ID)은 **수식**이다. 새 행을 추가하면 엑셀을 한 번 열기 전까지
`openpyxl(data_only=True)`로는 **None**으로 읽힌다. ID로 행을 필터하는 코드는 새 행을 전부 건너뛴다.
→ 대조·조회 모듈은 반드시 `rid = ID or 프로젝트NO` 폴백을 쓸 것 (band_reconcile·kakao_reconcile·app_server 적용됨).
또한 백필 시 **ID열·자동계산열은 기입하지 말 것** — 수식이 접수일자(yymm) 기준으로 알아서 채번한다.

## 월별 백필 진행 상황 (2026-07-25 저녁 갱신)
- **5월 백필 완료(3차 목표 착수)**: 02시트 50건 + 04시트 36건 = **86건 / 672셀 등록(v43)**.
  선행 작업으로 `expand_rows.py` 신규 제작 → 04_정기점검 104→164행, 02_돌발AS접수 154→214행 확장(v41·v42).
- **`expand_rows.py`(신규)**: 시트 행 확장 도구. 마지막 행을 본떠 수식·서식만 복제하고 값은 비운다.
  dimension·dataValidation(x14 포함)·conditionalFormatting·표(xl/tables)까지 함께 늘린다.
  공유수식은 일반 수식으로 풀어서 삽입(공유 ref를 건드리면 파일 손상 위험). ID열은 채번 수식을 다시 심는다
  (fix_ids가 마지막 행 ID를 확정값으로 바꿔놔서 그대로 복제하면 채번이 안 된다).
  `backfill_rows.py`의 cap 하드코딩(154/104)은 **ws.max_row 자동감지**로 교체 — 확장하면 바로 반영된다.
- **상태열 수식 캐시 함정(2차 발현)**: 백필 행은 점검상태·진행상태(수식)가 None이라 완료된 점검 90건이
  전부 '미점검'으로 보였다 → `app_server.derive_status()`가 실제점검일·완료일로 직접 판정(검증 [16]).

## 월별 백필 진행 상황 (이전)
- **6월 백필 완료(2차 목표)**: 02시트 66~137행 72건 + 04시트 45~104행 60건 = **132건 등록(v32)**.
  밴드 대조 원장 완료건 88→**195건**, 게시 확인 **194건(99.5%)**. `backfill_rows.py --month YYYY-MM [--apply]`로 재현.
- **04_정기점검 용량 소진**: 104행이 마지막(수식 한계). 5월 백필 전 **행 확장 필요**
  (엑셀에서 마지막 행 복사 → 아래 붙여넣기, 또는 build 스크립트 상수 수정).
- 6월 **정산(06시트)은 미등록** — 금액이 밴드에 없어 ERP 계정별원장 6월분 투입 후 진행.

- **밴드 원천 확보 완료**: `band_extract.py`가 게시글을 구조화 파싱 → 관리대장 `24_밴드업무추출` 시트(403건, 4~7월).
  월별 원장 미등록(백필 후보): **5월 92건 · 6월 149건 · 7월 36건**. 7월은 98건이 원장에 이미 있어 추출 정확도 검증됨.
- **다음 백필 절차**: 24시트에서 해당 월 '미등록' 행 → 02/04/05 시트에 등록 → 06 정산 생성 → ERP 원장 대조.
  (신규 행 추가는 채번 규칙·류매니저 입력 흐름과 맞물리므로 사용자 확인 후 진행할 것)
- **ERP 월별 데이터는 API 불가 확정**(실키·테스트키 양쪽 404). 화면 [거래처별계정별원장] 엑셀 내보내기가 유일 경로.

## 시트 행 재배치 (2026-07-26)
- `reorder_rows.py`: 02·04 시트를 **날짜 오름차순(과거→최근)** 으로 재배치. v51→v52 적용
  (02 193행·04 139행 전부 이동, 내용 집합 동일·무결성 통과 확인)
- **ID는 다시 매기지 않는다**: 접수/점검ID는 연·월을 품고 있어 정렬만으로 오름차순이 되고,
  재번호하면 03_현장작업실적(AS 90건)·06_거래서류청구수금(AS 42·PM 25) 등이 값으로 들고 있는
  ID 참조가 전부 끊긴다. ID↔날짜 불일치는 리포트로만 알린다(현재 0건).
- 수식은 **상대참조 행번호를 이동량만큼 함께 이동**시킨다(자기 행 + 누적 카운터 `COUNT($AO$4:AO{직전행})`).
  절대참조($5)는 고정. 자기닫힘 `<f .../>` 는 반드시 2단계 파싱 — 게으른 정규식으로 잡으면
  다음 셀의 `r="B5"`까지 숫자를 바꿔 `B-3` 같은 잘못된 셀 참조가 생긴다. 검증: synthetic_check `[21]`.

## 2026년 전체 반영 현황 (2026-07-25 밤)
| 탭 | 1월 | 2월 | 3월 | 4월 | 5월 | 6월 | 7월 | 출처 |
|---|---|---|---|---|---|---|---|---|
| 정산 | 17 | 16 | 14 | 13 | 17 | 10 | 67 | 1~6월 ERP 계산서 / 7월 대장 |
| 돌발AS | 4 | 4 | 3 | 9 | 45 | 51 | 56 | 1~3월 ERP / 4~7월 밴드 백필 |
| 정기점검 | 3 | 3 | 3 | 3 | 36 | 60 | 40 | 〃 |

- **보완 규칙(중요)**: 관리대장에 자료가 **아예 없는 달만** ERP 계산서로 채운다.
  자료가 있는 달은 대장 우선 → 같은 달을 양쪽에서 세지 않으므로 이중 계상이 없다.
  ERP 행은 `출처="ERP"`, 상태 `ERP 계산서(묶음)`/`작업완료` 로 표시되고 조치필요·담당자
  배정 집계에서 제외된다(`needAction()`, buildMine).
- ERP 계산서 1장 = 작업 여러 건(최대 15건 확인). 건별 금액 배분은 **품목 단위 판매현황**
  엑셀이 있어야 가능하다 — 현재 자료로는 불가.
- 1~3월 밴드 원천 없음(캐시가 2026-04-03부터). `band/collect_band.js`로 수집 필요.

## 원본 자료는 한 곳에 모은다 (2026-07-28 지시)
정본 위치 = 관리대장 폴더 아래 **`0. 원본 자료`**. 경로는 `source_dirs.py` **한 곳에서만** 정한다.

    0. 수집안내.txt        폴더를 처음 여는 사람이 읽을 안내 (collect_sources 가 갱신)
    1. ERP 내보내기/       이카운트 xlsx        2. 쿠팡 목록/     쿠팡 PO 목록 등
    3. 카카오톡 내보내기/  대화방 txt           4. 밴드 원본/     밴드 API 원문 JSON
    26년도 PO 모음/        PO 통지문·견적서 574개 (오종현 수집)

- 취합: `python collect_sources.py` (미리보기) → `--apply` (실제 복사).
  **복사만 한다** — PC 원본을 지우거나 옮기지 않고, 내용이 다르면 덮어쓰지 않고 둘 다 남긴다.
- 사람은 해당 폴더에 그냥 넣으면 된다. 파일명이 무작위여도 **내용으로** 종류를 판별한다.

## 보고물에 '금일·당일'을 쓰지 않는다 (2026-07-28 지시)
읽는 시점에 따라 다른 날이 돼 대표 보고에서 어긋난다. 타일 라벨 `(금일)` 은 화면·이미지에서
`dateLabel()` 이 **실제 날짜**로 바꿔 찍고, 브리핑 문장은 모든 항목을
`날짜 · 프로젝트NO · 캠프` 로 시작한다. 완료건에는 `(접수 MM-DD · N일 만)` 을 붙인다. 검증 `[41]`

## 이카운트 화면은 **자동으로 못 긁는다** (2026-07-28 확인, 시도하지 말 것)
두 가지 자동화를 다 시험했고 둘 다 막힌다. 같은 시도를 반복하지 말 것.

| 경로 | 세션 | 결과 |
|---|---|---|
| Claude in Chrome(실제 크롬) | 있음 | 화면은 뜬다(제목 바뀜). 그런데 ERP SPA가 연결을 계속 물고 있어 **`document_idle` 이 영원히 안 온다** → 읽기·클릭·스크린샷 전부 45초 대기 후 실패 |
| 인앱 브라우저 | 없음 | `ec_req_sid` 를 URL에 넣어도 **로그인 화면으로 튕긴다**(세션은 쿠키 기반). 비밀번호 대입은 금지 |

→ **정답은 이카운트의 `Excel` 버튼이다.** 사람이 2번 누르면 되고, 받는 쪽은 자동이다.
   Downloads 에 떨어진 파일을 `collect_sources.py` 가 **최근 2주 · 아는 종류만** 집어
   `0. 원본 자료/1. ERP 내보내기` 로 옮긴다. 파일명이 무작위여도 내용으로 판별한다.

★ **거래처별계정별원장은 이 화면에서만 뽑는다** (사용자 지시 2026-07-28)
  회계 I > 출력물 > 장부 > **거래처별계정별원장**
  조건: 거래처 = 쿠팡로지스틱스서비스 유한회사 · 계정 = **1089(외상매출금)** · 기간 지정 → Excel
  2026-07-28 첫 수집분(2026/01/01~06/30)에서 **대변(입금)이 한 건도 없다**.
  30.98억 미수로 읽히는데, 입금이 다른 계정·거래처코드로 잡혀 있을 가능성이 있어
  **금액을 확정 보고하기 전에 반드시 확인할 것.**

★★ **재고 I > 영업관리 > 판매 > 판매조회** 가 가장 값어치 있다 (2026-07-28 확인)
  이 화면에만 **프로젝트코드(UJ번호)** 가 있다 — ERP 매출을 우리 프로젝트NO 와 **직접**
  이어 주는 유일한 자료다. 열: 일자 · 프로젝트코드 · 진행상태(1.미확인~8.무상납품완료) ·
  공급가액합계 · 부가세합계 · 금액합계 · 관리항목명 · 수금현황.
  이거 하나면 02시트 ERP등록·04시트 ERP판매전표·06시트 청구대조·17시트 문서대조가 한꺼번에 풀린다.
  (2026-07-28 기준 38페이지 — 화면 하단 `Excel` 한 번이면 통째로 받아진다.)

내보낼 화면 — ① 재고 I>영업관리>판매일괄회계반영>**매출(세금)계산서조회(재고)**
② 같은 곳 **매출(세금)계산서현황(재고)**(품목·내역 단위 — '내역보기'를 일일이 누를 필요 없음)
③ 회계 I>전자(세금)계산서>홈택스자료조회>**전자(세금)계산서**('미반영' 말고 **전체**)
④ 회계 I>전자(세금)계산서>**이카운트 vs 홈택스 자료비교**(차이가 바로 보이는 화면)

## 카카오톡은 API가 없다 (2026-07-28 확인)
개인 대화방을 프로그램으로 긁는 공개 방법은 없다. PC 클라이언트 자동화는 약관 위반·계정 정지
위험이라 **하지 않는다.** 카톡 앱의 '대화 내보내기' txt 가 유일한 길이며,
`0. 원본 자료/3. 카카오톡 내보내기` 에 넣으면 자동으로 잡힌다.

### 2026-08-05 원본 자료 필터 카드형 버튼
- 자료 갈래 9종을 색상 아이콘·선택 체크가 있는 카드 버튼으로, 기간 선택도 테두리 카드 버튼으로 개편했다.
  데스크톱은 9개 카드가 한 줄을 채우고 모바일은 2열이며 가로 넘침이 없다.
- 실브라우저에서 카톡 28건, 카톡+2026-08 8건 필터와 선택 상태를 확인했다. JavaScript 문법 및
  전체 합성검증은 ALL GREEN이고 앱 서버를 재시작했다(커밋 `aaa101a`).

## 다음 세션이 이어서 할 일 (2026-08-04 13:10 최신, 우선순위)

**상태 요약**: 관리대장 v420 · 반영 대기 **0건** · 미커밋 0 · 합성검증 ALL GREEN.
엑셀 수동 개방은 더 이상 필요 없다(`excel_recalc --run --force`가 캐시까지 갱신한다).

1. **거래명세서 발행 비율 — 기준 확정 대기(사용자)**. 대표가 "몇 건 발행해야 하는데
   발행 대기가 얼마인지"를 요구했는데 **모수를 무엇으로 잡을지**가 미정이다
   (작업완료 기준인가, PO 기준인가). 정해지면 당일 실적에 %로 붙인다 — 임의로 정하지 말 것.
2. **밴드 전량 재확인 미완**. 수정글 감지 알고리즘은 붙었고(`convert_dump`), 5360~5424
   구간 중 일부만 재수집했다(글당 5초+ 걸려 느리다). 나머지 구간은 이어서 훑을 것.
   방법: 밴드 탭에서 `__grab(밴드번호, 글번호)` iframe 방식(피드 무한스크롤은 10글에서 멈춘다).
3. **ERP 남은 화면**: 계정별원장·분개장·일월계표·재무상태표·손익계산서·합계잔액시산표·
   구매조회. 받은 것은 자동으로 PDF 사본이 생긴다(daily_run 6.87).
   ec5 요령 — F3로 조회조건, 월 선택은 **실제 클릭**만 먹힘, 사이트맵 오버레이는 먼저 닫을 것.
4. **입금내역 엑셀 대기(김미영)** — 은행에서 받아 `16.Share` 공유폴더에 넣어 주기로 했다.
   들어오면 `share_intake`가 자동 수집한다. 2026년 1~5월 발행분까지 입금된 상태.
5. **미실시 핫이슈 12건 후속** — 대표가 "있으면 안 된다"고 한 유형(일정 미확정 8·사유
   불명확 4). 담당자에게 사유 보완을 받아야 목록이 줄어든다. 화면 상단 붉은 블록에 뜬다.
6. 사용자/원천 대기(장기): UJ2601070 접수/취소 정본, 입금 송금명세·배분표, ERP 세무
   권한(마스터 조찬희), 비공개 iCal/ICS, 8/7 실키 검증, 청구일·지급예정일 확정 규칙.

## 과거 다음 세션 목록 (2026-07-29 기록 — 실행 판단은 위 최신 목록 우선)

0. ★ **엑셀 재계산보다 복구 경고 원인 확인이 먼저다.**
   v303·v304를 Excel로 직접 열면 둘 다 “내용에 문제가 있습니다” 복구 경고가 난다.
   `fix_workbook.py`는 이상 0이지만 정본에서 `예`를 눌러 복구 저장하면 무엇을
   버렸는지 모른다. 정상 개방이 확인되기 전에는 636건 재계산·219건 작업상세 입력을
   진행하지 않는다.

   경고가 해결된 정상본에서만 **엑셀을 한 번 열었다 닫는다.** 그러면
   03시트 B열(접수ID)은 **수식**이고 02시트에서 "작업완료+완료일 있음+아직 03에 없는" 건을
   스스로 끌어온다. 행은 이미 502행까지 늘려 뒀다(여유 300). **엑셀에서 한 번 열었다 닫으면**
   B열이 채워지고, 그 다음 `python fill_work_detail.py --apply` 가 원문(카톡·밴드 완료 글)에서
   작업 내용을 프로젝트NO 기준으로 채운다.
   · `excel_recalc.py`는 PowerShell Excel COM으로 열고 닫을 수 있지만 자동 승인하지 않는다.
     이 차단 항목을 해소한 뒤, 정상 개방을 확인한 정확한 파일명·SHA-256을
     `reports/excel_recalc_clearance.json`에 승인해야만 실행된다. 승인 파일은 새 버전에서 만료된다.
   · **빈 행에 미리 써 넣지 말 것** — 재계산 때 접수ID가 정해지므로 엉뚱한 건에 붙는다(v259 실사고).

1. ~~카톡 신규 10건 백필~~ **완료(2026-07-28, v225→v227)** — 9건 등록, `kakao_extract.py` 신설.
   ~~철거·납품 보류~~ **완료(2026-07-29)** — 05_신규납품설치에 36건 등록(철거 21·납품 15).
   앱에는 표기하지 않는다(`isSideWork`). 후속: 카톡 미등록 중 **돌발 17·정기 6·유형미상 23**.
   `python kakao_extract.py --new` 로 보고 **눈으로 확인한 뒤** 넣을 것.
2. ~~06_거래서류청구수금 정산 원장 채우기~~ **완료(2026-07-29, v273→v275)**
   `billing_fill.py` 신설. ERP 판매조회를 근거로 636건 적재 → **67행·4,111만원 → 703행·4.60억**.
   금액은 실제작업(I열)이 아니라 **거래명세서(N·O열)** 에 넣었다 — I는 03시트 실적 수식이고,
   둘의 차이는 AE(작업대비거래명세서차액)가 스스로 드러내는 것이 이 시트의 목적이다.
   · 최신 v327의 `recalc_pending.py` 결과는 현재 대기 0건이다. 새 입력으로 대기가 생기면
     앱에 표시하며, 복구 경고 원인 확인과 정확한 파일 해시 승인 전에는 자동으로 열지 않는다.
   · ★ 함정: 같은 내용의 판매조회가 여러 벌 있으면 금액이 **배수로 합산**된다(2026-07-30 3배 사고,
     36.2억→108.6억). `dedupe_files()` 가 SHA256으로 막는다 — 검증 [84]. 파일명(`__dup_`)으로 거르지 말 것.
   · 입금 건별 귀속은 여전히 **쿠팡 지급명세(remittance)** 가 있어야 가능 — 없으면 총액 대사까지.
   · 거래처별계정별원장을 다시 뽑을 때는 `대표거래처로 합산` 을 **개별거래처기준**으로.
     매출의 98%(747건)가 캠프명 거래처로 등록돼 있어 법인명만 잡으면 입금이 통째로 빠진다.

3. **밴드 85건 성격 분류** — 밴드 원문엔 있는데 관리대장에 없는 85건. 그대로 넣으면 안 된다:
   PO 통지 글(`Coupang이 새 구매 오더를 전송했습니다`)과 파싱 실패분이 섞여 있다.
4. **(세금)계산서진행단계 Excel** — 미발행 9건(쿠팡 6건 공급가 44,862,220원) 전수 확인.
   7/25자 5건의 법정 발행기한은 **8/10**.
5. ERP 미등록 잔여 92건(02시트 40·04시트 52) — 진짜 미등록인지, ERP 입력 시 프로젝트코드를
   안 넣어 연결이 끊긴 것인지 확인. 후자면 **입력 시 프로젝트코드 필수화**로 재발을 막는다.

### ★ 구글 캘린더 — 비공개 iCal 주소 한 줄 (2026-07-29 지시)
"COUPANG 설치+납품+AS" 캘린더를 상시 대조하도록 `gcal_sync.py` 를 붙였다(검증 [75]).
남은 건 **주소 하나**다. 공개로 돌릴 필요 없다 — 공개하면 전 세계에 일정이 열린다.

  구글 캘린더 → 해당 캘린더 설정 → **캘린더 통합** → 맨 아래 **"비공개 주소의 iCal 형식"**
  → 복사해서 `config/gcal.json` 의 `calendars[0].ics` 에 붙여넣기

· 그 주소는 **비밀키**다. 커밋·채팅·엑셀 금지(규칙 1). config/gcal.json 은 .gitignore 됨.
· 주소 대신 `.ics` 파일을 `0. 원본 자료/3. 캘린더` 나 `ecount/inbox/` 에 놓아도 동작한다.
· 캘린더 ID(...876b@group.calendar.google.com)만으로는 안 된다 — 공개 피드가 404다(확인함).
· 채우는 곳: 02 방문예정일·시간 / 04 점검예정일·시간 / 05 납품·철거예정일.
  **완료일은 캘린더로 채우지 않는다** — 예정과 실적을 섞으면 안 한 일이 한 일이 된다.

### 철거·신규납품을 앱에 다시 보이려면 (2026-07-29 지시: "추후에 추가할 수도 있으니 감안")
**두 줄만 바꾸면 된다. 반드시 같이 바꾼다** — 한쪽만 켜면 목록엔 없는데 보고 숫자엔 잡힌다.

  1. `webapp/index.html`  : `const SHOW_SIDE_WORK = false;`  → `true`
  2. `webapp/app_server.py`: `SHOW_SIDE_WORK = False`         → `True`
  3. 서버 재시작 (파이썬 파일은 재시작해야 반영된다. index.html 은 요청마다 읽는다)

· 검증 [77] 이 두 스위치가 어긋나면 합성검증을 실패시킨다 — 켤 때 한쪽을 빠뜨릴 수 없다.
· 자료는 계속 쌓이고 있다(05_신규납품설치 시트·캘린더·06시트). 켜는 즉시 과거분까지 다 나온다.
  다시 모으거나 되돌릴 작업이 없다.
· 숨기는 지점: 앱 6곳(정산·업무 as/pm·이슈 2곳·확인필요·일정) + 서버 4곳
  (/api/settlements·/api/works·/api/issues·대표보고). 전부 `isSideWork`/`is_side_work` 한 함수를 쓴다.

## 사용자 대기 항목 (2026-07-31 갱신 — 전수조사 결과 AI가 못 하는 것만 남김)

### ★ ERP 화면 자동수집 — 두 경로 다 막힘 확인 (2026-07-31, 다시 시도하지 말 것)
사용자 지시: "ERP 자료도 에이전트가 다 검색해서 할 수 있게 수단과 방법을 가리지 말고 조치해".
**실제로 두 가지를 다 해 봤고 둘 다 막혔다. 같은 것을 또 시도해 시간을 버리지 말 것.**

1. **사용자의 크롬(claude-in-chrome)** — 로그인은 되어 있다(회사명 표시 확인). 그런데
   스크린샷·스크립트실행·텍스트추출이 **전부 45초 타임아웃**한다. 원인: ECOUNT 는 화면이
   계속 폴링해서 브라우저의 `document_idle` 이 **영원히 안 온다.** 자동화 도구가 그 신호를
   기다리므로 도구 쪽에서 손을 쓸 수 없다. (같은 도구로 밴드는 정상 동작한다 — ECOUNT 특유 문제)
2. **인앱 브라우저** — `ec_req_sid` 가 든 URL 을 그대로 열어도 **로그인 화면으로 튕긴다.**
   세션이 원래 크롬의 쿠키에 묶여 있어 브라우저를 건너 옮겨지지 않는다.
3. **OAPI** — 판매·세금계산서·수금 조회 API 는 존재하지 않는다(절대규칙 3, 삼중 확인).
   ★ 없는 경로를 찾겠다고 무차별 탐침하면 **ERP 계정 자체가 막힌다.** 하지 말 것.

**남은 유일한 자동화 경로**: 로컬에 Playwright(또는 Selenium)를 깔고 **사용자 크롬 프로파일로**
ECOUNT 를 구동해 메뉴→조회조건→[엑셀 내보내기]까지 직접 누르는 것. `document_idle` 을
기다리지 않는 엔진이라 위 1번의 벽이 없다. 이게 다음 작업자가 만들 것이다.
· 뽑아야 하는 3개: 판매→**매출(세금)계산서현황** / 판매→**거래명세서현황** / 회계→**거래처별계정별원장**
· ★ 거래처별계정별원장은 **'대표거래처로 합산'을 끄고 '개별거래처기준'** 으로. 매출의 98%가
  법인명이 아니라 **캠프명**으로 등록돼 있어 법인명만 잡으면 입금이 통째로 빠진다.
  (2026-07-31 실수: 판매조회를 거래처명 '쿠팡'으로 걸렀더니 976행 중 11행만 남았다.)

### ★ 세금계산서 '미발행 359건'의 정체 — 발행 문제가 아니라 반영 문제 (2026-07-31)
ERP 판매조회(978행)의 `진행상태` 로 대조한 결과, 원장이 '세금계산서 미발행'이라 부르는 359건은:
| ERP 실제 상태 | 건수 | 뜻 |
|---|---:|---|
| 6.세금계산서발행 / 7.수금완료 | **206** | **이미 발행됨 — 원장에만 안 적힘(반영 누락)** |
| 3.오더처리 | 121 | 아직 계산서 단계 전 |
| 4.세금계산서발행대기 | 21 | **진짜 미발행 — 발행해야 함** |
| 1.미확인 | 11 | 확인 필요 |
· 화면의 **계산서 발행율 7%(51/738)** 도 같은 원인이다. 실제는 훨씬 높다.
· 대상 목록: `reports/세금계산서_반영누락.csv` (정산ID·프로젝트NO·ERP상태·캠프명·완료일)
· ★ **이 206건을 원장에 enqueue 하지 말 것 — 넣을 값이 없다**(2026-07-31 확인, 앞의 지시 취소).
  06시트의 세금계산서 열은 **발행일·공급가액·부가세·합계** 넷인데, 근거로 쓸 수 있는
  ERP 판매조회에는 **그 넷 중 아무것도 없다**(일자·진행상태·매출금액뿐).
  판매조회의 공급가액을 세금계산서 공급가액이라 가정해 넣는 것도 안 된다 — 매출 기록이지
  계산서가 아니다. 억지로 넣으면 원자료에 없는 값을 채우는 것이다(절대규칙 10).
  · 지금 할 수 있는 조치는 **이미 했다**: 분류를 '계산서 발행됨·원장 미기재'로 갈라
    이중발행을 막았다(커밋 `e221213`). 숫자가 틀렸던 것과 재발행 위험은 해소됐다.
  · **실제로 값을 채우려면 ERP [매출(세금)계산서현황] 내보내기가 있어야 한다.** 그것뿐이다.
· 거래명세서는 미청구 1건뿐이다. 다만 **명세서번호가 629건 비어 있다**(발행일·금액은 있음).
  번호는 `ERP_거래명세서현황` 에 있는데 그 파일이 0행이라 지금은 못 채운다.

### ★ 지정 파일 확인 결과 (2026-07-31)
- 판매조회 3벌(`__dup_` 2개 포함)은 **sha256 이 완전히 동일**(3c94ea24, 978행). 중복본에 숨은
  자료는 없다. 셋 중 아무거나 쓰면 된다 — 단 **합산은 한 벌만**(3배 사고 전례, 검증 [84]).
- 내용 0행 4개(다시 내보내야 함): `ERP_거래명세서현황`·`ERP_세금계산서현황`·`ERP_회계거래현황`·
  `ixRsVB7c4XDo3vlz`(거래처별계정별원장). 실데이터는 `ERP_회계거래조회` 359행뿐.

### ★ 밴드 브라우저 재수집 — 실익 없음 확인 (2026-07-31)
밴드 90610953 에 로그인해 `collect_band.js` 방식으로 수집을 붙였으나 **12건에서 더 이상
안 불러온다**(화면 맨 아래 도달, scrollHeight 안 자람). 밴드가 가상 스크롤을 쓰고 lazy-load 가
**프로그램 스크롤에 반응하지 않는다**(사람 손 스크롤과 다르게 본다).
★ 그런데 **2026년 밴드 자료는 이미 로컬에 다 있다** — 추출 2,337건, 2026-01-02~07-30,
  월별 1월156·2월222·3월403·4월421·5월288·6월452·7월395, 사진 1,711장(153MB).
  **다시 긁어도 새로 얻을 게 없다.** 밴드는 병목이 아니다 — 병목은 ERP 다.

### ★ 업무센터가 "불러오는 중"에서 멈춰 있던 진짜 이유 (2026-07-31 해결)
사용자 화면 캡처로 확인: 오종현·류지영 업무센터 **둘 다** "업무 현황을 불러오는 중입니다"
에서 멈추고 과거 목록이 0건이었다. **버그가 아니라 속도였다.**
- `/api/ryu/records` **첫 호출 111초**(실측). 2회차는 677ms. 사람은 111초를 안 기다린다.
- 무거운 것은 `get_works`·`get_settlements`·`get_issues` 의 최초 계산이다.
- 조치: 서버 기동 시 `warm_caches()` 스레드가 미리 계산한다. **한 번만 데우면 안 된다** —
  항목별 TTL 이 issues 300초·works/settle 600초이고, 11:00·15:00 반영으로 원장 mtime 이
  바뀌면 캐시가 통째로 비워진다. 그래서 **240초마다 다시** 데운다.
- ★ 교훈: 화면이 안 뜬다고 곧장 코드 버그를 찾지 말 것. **먼저 시간을 재라.**
  이 건은 API 를 한 번 재 보는 것으로 원인이 나왔다.

### ★ "PC 화면을 꺼도 앱이 돌게" — 범인은 화면이 아니라 잠자기 (2026-07-31 진단)
사용자 지시로 조사. **화면 끄기는 서버를 죽이지 않는다.** 죽이는 것은 **잠자기(standby)** 다.
현재 전원 계획(SAMSUNG MODE) 실측:
| 항목 | AC 설정 | 뜻 |
|---|---|---|
| 잠자기 | `0x4650` = 18,000초 | **5시간 뒤 잠자기 → 서버 죽음** ← 이것이 원인 |
| 최대 절전 | `0x278d00` | 사실상 없음 |
| 화면 끄기 | `0x4650` = 5시간 | 화면만 꺼짐 — **무해하다** |

조치(사람이 실행): `powercfg /change standby-timeout-ac 0` · `powercfg /change hibernate-timeout-ac 0`
· AI 는 시스템 전원 설정을 직접 바꾸지 않는다 — 명령만 제시하고 사람이 실행한다.
· 화면 끄기는 **그대로 둔다.** 전기를 아끼면서 서버는 살아 있다. 둘은 별개다.
· 노트북이면 뚜껑 닫기 동작(`lidclose`)도 '아무 것도 안 함'으로 둬야 한다.
· ★ PC 가 **완전히 꺼진** 경우는 이 설정으로 못 막는다. 그건 폰의 **오프라인 큐**가 받는다
  (2026-07-31 구현) — 폰에 쌓였다가 PC 가 켜지면 자동 전송된다.

### ★ 2026-07-31 지시 3건 — 1건 완료, 2건 설계만 (다음 세션이 만든다)

**(1) 앱 업데이트를 묻고 반영 — ✅ 완료**
전에는 새 버전을 감지하면 토스트 띄우고 **0.7초 뒤 스스로 새로고침**했다. 입력하던 중이면
그대로 날아간다. 이제 화면 아래 띠만 띄우고 **누르기 전에는 아무 일도 안 한다**
(`offerUpdate()`). '나중에'를 누르면 그 세션에서는 다시 묻지 않는다. 새로고침 버튼으로
직접 누르는 건 사용자가 원한 것이라 즉시 갱신한다.

**(2) AI가 사용패턴 보고 UI/UX 자동 반영 — 설계만**
★ **(1)과 정면으로 부딪히는 지시다.** "묻고 반영해라"와 "자동 반영해라"를 둘 다 지키려면
**만드는 것은 자동, 적용은 승인**으로 갈라야 한다. 이렇게 만들 것:
  · `ledger_db.ux_summary()` 가 쌓는 화면이동·무결과검색·오류·느린화면을 주기적으로 읽어
    "어디서 막히는가"를 뽑는다. **지금 이 기록이 비어 있다** — 앱을 며칠 써야 근거가 생긴다.
    근거 없이 UI 를 뜯으면 추측이고, 그건 지금까지 되돌림 사고의 원인이었다(나눔고딕 건).
  · AI 는 **제안서**(무엇을·왜·어디 근거)를 `reports/ux_제안_YYYYMMDD.md` 로 만들기만 한다.
  · 앱에 '개선 제안 N건' 배지를 띄우고 사용자가 **승인한 것만** 코드에 반영한다.
  · 자동으로 코드를 고쳐 배포하지 말 것 — 합성검증이 잡지 못하는 취향·업무흐름 문제가 있고,
    무엇보다 (1)에서 "말없이 바뀌는 게 싫다"고 한 지시와 어긋난다.

**(3) 시간대별 자동 근무 — 설계만**
지금 도는 것: 워치독 30분 · 원본자료정리 09:35 · 자동대조 09:50 · 원장반영 11:00/15:00 ·
문제감시 4시간. **업무 리듬에 비해 비어 있는 시간대**가 있다:
  · **08:30 대표보고 준비** — 매일 08:30 이 보고 시각인데 준비 자동화가 없다.
  · **10:30 대표보고 준비**(Codex 인계문서의 미실행 항목) — 위와 합칠지 정할 것.
  · **17:00~18:00 마감 점검** — 그날 미완료·미반영을 하루 끝에 한 번 훑는 자리가 없다.
  · 등록은 `install_ledger_schedule.ps1` 과 같은 방식으로 작업 스케줄러에 넣는다.
  ★ 스케줄러에 없으면 자동이 아니다. 코드만 만들고 등록을 빠뜨리지 말 것.

### ★ 2026-07-31 지시 — 아직 안 끝난 것 (다음 세션이 이어서 한다)
사용자가 "세션이 끝나면 다시 명령해야 하나?" 라고 물었다. **답은 아니오다.** 다만 조건이 있다:
대화에 남긴 지시는 사라지고, **파일 + 작업 스케줄러에 들어간 것만** AI 없이 돈다.
지금 등록돼 도는 것(확인함 2026-07-31): 워치독 30분·원본자료자동정리 09:35·일일자동대조 09:50·
원장일괄반영 11:00/15:00·실시간문제감시 4시간. 아래 것들은 **아직 그 목록에 없다.**

1. **Z: 관리대장 폴더 상시 공백 스캔** — "이 폴더에서 항상 데이터 가져와서 없는 자료 검토하고
   결과 반영" 지시. `daily_run.py` 에 단계로 넣고 스케줄러가 돌게 해야 그때부터 자동이 된다.
   지금은 사람이 시켜야만 도는 상태다.
2. **좌측 카테고리에 정기점검·돌발AS 버튼** — 대시보드의 '업무 바로가기' 카드는 **없애고**,
   `<nav class="tabbar">`(2057행 부근)에 `routeNav('pm')`·`routeNav('as')` 버튼을 넣는다.
   · ★ 주의: 지금 `pm`/`as` 는 **뷰가 아니라 settle 뷰의 모드**다(`goList('pm','')` 가
     `show('settle'); setMode('pm')` 을 한다). 버튼만 달면 정산 화면으로 튄다.
     **전용 뷰(`v-pm`·`v-as`)를 새로 만들어야 한다** — 목록 + **신규 입력 폼** + 관련 업무
     (사진·완료보고서·ERP 등록·거래명세서)까지 한 화면에서 되게. 이게 지시의 핵심이다.
   · 담당자 업무센터(`/staff/<slug>`)에도 같은 구성을 넣는다.
3. **밴드·ERP 화면 수집** — 2026-07-31 확인: **밴드는 로그인돼 있고(밴드 90610953 접근됨),
   ERP 는 로그아웃 상태**다. ERP 조회 API 는 존재하지 않으므로(절대규칙 3) **화면 수집이
   유일한 경로**다. 사람이 ERP 에 로그인해 두면 그때부터 진행할 수 있다.
   · ★ AI 는 비밀번호를 입력하지 않는다. 저장된 자동입력이 있어도 마찬가지다. 로그인은 사람이.
4. ~~**OLD 폴더 검수**~~ **완료 — 놓친 데이터 없음(2026-07-31 확정)**
   340개(401MB) 전수 대조. 옛 버전엔 있고 v328 엔 없는 ID 807건(02:291·04:336·03:180)이
   나왔지만, **전수 검증 결과 진짜 유실은 0건**이다. 근거 셋:
   · **행 수는 한 번도 줄지 않았다** — 02: 287→491→538→544→589 / 04: 237→369→407→413 /
     03: 173→198. 일괄 삭제가 있었다면 여기서 꺾인다. 안 꺾였다.
   · **사라진 ID 의 프로젝트NO 가 전부 v328 에 살아 있다**(02 291/291, 04 336/336, 03 180/180).
     즉 **번호만 바뀐 재번호**이지 일이 없어진 게 아니다. 연번 블록이 v104·v123·v148·v243
     네 지점에서 뭉텅이로 바뀐 모양도 재번호와 일치한다.
   · 03시트가 198행에 멈춘 것은 유실이 아니라 **재계산 대기**다(0번 항목). B열이 수식이라
     엑셀을 한 번 열어야 02시트에서 완료건을 끌어온다. 열면 180건이 저절로 생긴다.
   ★ 교훈: **ID 집합 차이만으로 '데이터가 묻혔다'고 판단하지 말 것.** 재번호와 유실은
     ID 로는 구분되지 않는다. 반드시 **내용(프로젝트NO)과 행 수 추이**로 확인한다.
     재현: `reports/OLD검수_누락후보.csv` · `OLD검수_판정.csv`

### ★ 지금 이것 하나면 가장 많이 풀린다 (2026-08-01 갱신)
**관리대장 최신 v336 을 엑셀로 열어 복구 경고 유무만 확인해 주세요.**
- 코드 진단으로는 **결함이 없습니다**(XML·calcChain 55,914항목·정의된이름·count·행순서 정상).
  v303·v304 때 경고는 이후 수정들로 해소된 것으로 보이나, **정본을 실제로 여는 것만은
  사람이 확인해야** 합니다(자동 승인하지 않는 규칙).
- 경고 없이 열리면 → 정확한 파일명·SHA-256을 `reports/excel_recalc_clearance.json`에 승인한
  뒤에만 예약 회차의 Excel 재계산을 허용합니다. 과거 v328 기준 숫자는 현재 v336에 재사용하지 않습니다.
- 경고가 뜨면 → **'예'(복구)를 누르지 말고** 화면 문구를 알려 주세요. 복구 저장하면
  무엇을 버렸는지 알 수 없습니다.


0. ★ ERP 내보내기 4개가 **내용 0행**(거래명세서현황·세금계산서현황·회계거래현황·
   `ixRsVB7c4XDo3vlz.xlsx`의 거래처별계정별원장) — 이카운트에서
   조회 조건을 넣고 **화면에 행이 보이는 상태**로 다시 내보내 `0. 원본 자료/1. ERP 내보내기` 에.
1. ★ 거래처별계정별원장(쿠팡로지스틱스·4049) **5·6·7월** 엑셀 → `inbox/`(파일명 '원장') — 3개월치 한 번에 넣으면 일괄 대조. / 쿠팡 PO 목록 → `inbox/` / 카톡 내보내기 → `kakao/inbox/`. 2026-07-28 10:30 첨부 2개는 지정 Desktop 경로에 없어 재첨부 필요.
2. 밴드 앱 심사 결과(client_id/secret, ~7/31)
3. 밴드 게시 누락 1건(AS-2607-021) — 김필우 기사 확인
4. ERP 전표 `20260720-2` 화면 확인 + 변재선 과장에게 API 자동등록 공유(이중 입력 방지)
5. 8/7 前 실키 SaveInvoiceAuto 검증 완료 확인
6. ~~**철거·보관 건은 어느 시트에 넣나**~~ **완료(2026-07-29)** —
   철거·납품·설치·이전은 `05_신규납품설치`에 업무구분을 보존해 저장하고 앱에는 숨긴다.
   다음 카톡 대조에서도 05시트를 기존코드 판정에 포함해 중복 등록하지 않는다.
7. ★ **청구일·지급예정일 산정 규칙** — `receipt_fill` 이 입금일·입금액은 자동으로 채우지만
   이 두 칸은 근거 규칙이 없어 비워 둔다. ① 청구일 = 세금계산서 발행일인가, 아리바 업로드일인가
   ② 지급예정일 = 어떤 지급조건(발행 후 N일 / 월말 마감 후 N일)인가. 정해지면 자동입력에 넣는다.
8. ~~**확인필요 내부 담당 범위 확정**~~ **완료(2026-07-30, 변재선 업무를 류지영에게 이관)** —
   류지영=캠프·일정/카톡·밴드/현장자료/완료일/거래명세서/세금계산서/입금/금액 불일치,
   오종현=PO 원본·견적서/구매·입금 원천자료 취합·누락, PO 대조·ERP·원장 미등록·시스템 연결=유현민.
9. ★ 비공개 Google iCal 주소 또는 `.ics` 원본 — 캘린더 ID만으로는 비공개 일정을 읽을 수 없다.
   주소는 `config/gcal.json`에만 두고 커밋·채팅·Excel에 넣지 않는다.
10. 2025·2024 자료 확장은 후속 **2026년 우선** 지시로 보류. 삭제된 요구가 아니라 우선순위 대기다.

## 크레딧 중단 시 승계 절차 (Codex 등)
1. 이 파일 + 관리대장 `19_AI작업인수인계` 시트(최신 행부터)를 읽는다.
2. `python tests/synthetic_check.py` 로 환경 검증(ALL GREEN 확인).
3. "사용자 대기 항목"부터 이어서 진행. 새 작업 완료 시:
   - `workbook_patch.py`로 관리대장에 인수인계 행 추가(vN+1)
   - 이 파일의 "현재 상태" 갱신 → commit·push (비밀 스캔 후)
4. 모든 판단 근거·시행착오는 이 문서와 19시트에 남긴다 — 다음 작업자가 같은 실수를 반복하지 않게.

## ★ 2026-08-01 Codex 정리 — 이 아래가 최신 우선목록

상세 근거: `reports/클로드_미완료_정리_20260801.md`. 위의 날짜가 오래된 대기 목록은
사고·판단 이력으로 보존하되, 실행 여부는 이 절을 우선한다.

- **완료**: 실패했던 확정 큐 43셀을 SQLite에 복구, 죽은 JSON 잠금 회수, 실패 예외 끝부분
  보존, 밴드 점진 스크롤/사진 fetch 분리, 중복 09:50 작업 비활성화, Z: 공백·서류 대조
  09:50 자동 연결. 합성검증 ALL GREEN.
- **현재 DB**: 셀 반영 대기 1,586건(이번 객관근거 보완 43셀 포함). 현장업무 객관 완료
  정본 854건(AS 467·점검 387). 2026-08-01 11:00·15:00은 PC 부재로 놓쳤으며 임의 시각
  강제 반영하지 않는다. 다음 허용 회차 2026-08-02 11:00.
- **사용자 확인**: v336 Excel 복구 경고, UJ2601070 접수/취소 정본, 입금 송금명세·배분표,
  ERP 회계/세무 권한, 비공개 iCal/ICS, `20260720-2` 및 8/7 실키, 청구/지급 규칙.
- **시스템 승인**: OpenSSH 설치·절전 해제. 무료 서버는 Oracle Always Free 하이브리드가
  1순위이며, Z:/Excel/로그인 세션은 DB 완전 이관 전까지 PC에 남는다.

## 2026-08-03 업로드 단일 투입함

- **완료**: `100. 업로드용 자료` → 유형별 원본 정본 자동분류, 미분류 원본 보존,
  SHA-256 동일자료 중복통합, 공유 잠금, 카톡·밴드·PO·문서사진 정본 직접 탐색,
  전체 대조 선행 실행, 30분 워치독 신규자료 감지 후 전체 대조 기동을 코드에 연결했다.
- **완료 판정 원칙**: 분류됐다는 이유만으로 완료하지 않는다. 정본 자료가 기존의
  프로젝트·PO·금액·발행/수금·현장완료 객관근거 관문을 통과할 때만 SQLite 완료 정본에 기록한다.

### ★ 압축 직후 이어서 할 일 (2026-08-07 08:1x 지시: "ERP 안가져온거 다 긁어오고, 밴드도 다")
1. **밴드 재수집 — 멈춰 두었다. 창을 앞으로 꺼내야 다시 돈다(2026-08-07).**
   남은 503건(90610953 273 + 84789192 230)을 걸었는데 20분에 성공 9 · 실패 61 이었다.
   밴드가 막은 게 아니라 **크롬 창이 뒤에 있어서**다 — 밴드 본문은 `rAF` 로 그려지고
   rAF 는 숨은 탭에서 안 불린다. 자세한 것은 `INCIDENTS.md` 22번.
   · 이제 `__grabStart` 가 숨은 탭에서는 **시작을 거절**하고, 돌던 중 창이 뒤로 가면
     실패로 적지 않고 **멈춰 기다린다**(`__grabStatus().paused`). 헛돌지 않는다.
   · 다시 걸 때: **크롬 창을 앞에 놓고** `band/serve_scripts.py` 띄워 grab_posts.js 주입 →
     `band/recheck_plan.py --floor 1` 로 남은 번호를 뽑아 200건씩. `dump_watch` 가 흡수한다.
   · 지금까지 모은 것 8,509글(90610953 5,201 · 84789192 3,308) — 전체의 94%다.
     남은 503건은 급하지 않다. 창이 앞에 있는 시간에 붙이면 된다.
   · ★ **더 중요한 것을 같이 찾았다 — '날짜 없는 글' 621건**(90610953 523 · 84789192 98).
     묘비인 줄 알고 세던 것의 대부분이 **본문은 멀쩡한데 날짜만 없는 글**이었다.
     밴드가 본문을 먼저 칠하고 작성시각을 조금 뒤에 채우는데 수집기가 본문을
     보자마자 가져가서 그렇다. 돌발유료 A/S 안내·쿠팡 PO 알림이 섞여 있다.
     **번호가 있으니 구멍도 아니고 오늘 받았으니 오래된 것도 아니라 어느 목록에도
     안 잡혀 있었다** — 캐시 숫자는 느는데 대조는 안 되는 종류다.
     이제 `band/recheck_plan.py` 가 `날짜없음` 으로 세어 재수집 대상에 넣는다.
     수집기도 작성시각을 최대 3초 더 기다린다. 검증 `[112]`.
     **창을 앞에 놓고 재수집하면 503건보다 이 621건이 먼저다** — 본문이 이미 있어
     한 번만 다시 열면 되고, 업무 대조에 바로 쓰인다.
2-완료. **ERP — 09:30 에 뚫렸다. 아래 진단은 기록으로만 남긴다.**
   막고 있던 것은 두 가지였고 둘 다 풀렸다:
   ① 크롬 창이 뒤에 있어 화면이 한 번도 안 그려졌다(격자 높이 0).
   ② **투명한 알림창이 화면 전체를 덮고 있었다.** `.ui-widget-overlay`(opacity 0,
      z-index 2001)가 클릭을 전부 먹었다. 그 뒤의 대화창은
      *"계정 검색조건을 지정하지 않고 검색 시 … 조회 대상계정을 재지정하겠습니까?"* —
      ERP 는 답을 기다리고 있었을 뿐이다. **눈에 안 보이는 모달을 의심할 것.**
      `document.elementFromPoint(버튼중심)` 이 버튼이 아닌 것을 돌려주면 그거다.
   받은 것: **계정별원장 전 계정 8,980행(2025/01~2026/08, 52계정)** ·
   **거래처별계정별원장 쿠팡 1,215행(전표 304건)**. 둘 다 Z: 흡수 완료.
   06시트 입금액의 원천인 **1089 외상매출금**에 쿠팡 입금(대변) **114건**이 있다.
   ★ **다만 `erp_ledger_check` 의 대조 전제가 틀렸다** — `06 거래명세서번호 =
   ERP 일자-No.` 가 성립하지 않는다(65건 중 9건만 겹치고 그 9건도 금액이 다르다).
   **그리고 이건 이미 2026-07-28 에 밝혀져 있었다.** `fill_erp_status.py` 머리말:
   *"판매조회 화면에만 프로젝트코드(UJ번호)가 있다. 이게 ERP 매출과 우리 프로젝트NO
   를 잇는 유일한 자료다. 그동안 06시트 거래명세서번호로 더듬느라 365건이
   '미등록'으로 남아 있었는데…"* — **맞는 열쇠는 판매조회의 UJ번호다.**
   `erp_ledger_check` 의 "A. ERP에만 1,856건"은 참고용으로만 볼 것.
   ★ **그 도구가 daily_run 에 없었다** — 2026-07-28 에 만들어졌는데 자동 회차 목록에
   빠져 있어 손으로 돌린 사람이 있을 때만 채워졌다. 넣었다(검증 `[112]`).
   그냥 돌려 보니 **채울 칸 52개**(46 완료·6 미등록)와
   **ERP 전표는 있는데 관리대장 상태가 미완료인 건 72건**이 나왔다
   (`reports/ERP전표있는데_상태미완료.md`). 72건은 상태 칸이 수식이라 **완료일이
   들어와야** 닫힌다 — 날짜를 지어내지 않는다. 밴드 글에서 날짜를 찾는 길이 남아
   있고 그건 `complete_verified.py` 의 영역이다.
   ★ 판매조회 원본이 **7/26 까지**다. 그 뒤 건은 판정 밖에 있다 — 최신본을 받을 것.
   앞으로 ERP 수집은 **사람 손이 로그인뿐**이다. 창만 앞에 있으면 메뉴 이동·기간
   설정·거래처 지정·검색·알림창 응답·Excel 내려받기까지 AI 가 다 한다.

   (예전 진단 — 참고용)
   **ERP — 막힌 자리가 바뀌었다(2026-08-07 오전 확인). 더 헤매지 말 것.**
   어제는 "검색은 됐는데 결과가 안 보인다"고 적었지만, 사실은 그게 아니었다.
   실제로는 **검색이 아예 시작되지 않는다.** 근거:
   · `fetch`·`XMLHttpRequest` 를 둘 다 가로채 놓고 `검색(F8)` 을 눌렀는데
     **네트워크 요청이 0건**이다. 결과가 숨은 게 아니라 요청 자체가 안 나간다.
   · 버튼(`#searchGroup`)에는 직접 붙은 이벤트가 없다. jQuery 1.10.2 가
     `document` 에 건 **mousedown·mouseup** 위임 처리기가 받는다.
     그래서 `.click()` 은 아무 일도 안 한다. mouseover→mousedown→mouseup→click
     을 순서대로 만들어 보내도 **여전히 0건** — 합성 이벤트로는 안 움직인다.
   · 그 탭은 `document.hidden === true` 인 **뒷면 탭**이고,
     격자(`#grid-EBZ057R`)의 높이가 0 이다. 화면이 한 번도 그려진 적이 없다.
   · `computer` 의 **screenshot 이 5초 시간초과로 계속 실패**한다(javascript_tool 은 잘 된다).
     그래서 좌표를 보고 진짜 클릭을 할 수가 없다. 눈을 못 뜬 채 손만 있는 상태다.
   **결론: 사람 손 한 번이 가장 싸다.** 이카운트 탭을 **앞으로 꺼내** 놓고
   `검색(F8)` → **Excel 버튼** 을 누르면 끝이다. 내려받은 파일은
   `download_intake.py --apply`(09:50 회차에 포함)가 알아서 Z: 로 옮기고
   `erp_ledger_check.py` 가 대조한다. **사람이 눌러야 할 보고서 목록은
   `reports/ERP_받아올목록.md`** 에 적어 두었다(순서·화면 경로까지).
   시도해도 되는 것 / 하면 안 되는 것:
   · 해도 됨 — 탭을 앞으로 꺼낸 뒤 다시 시도(그때는 screenshot 이 될 수 있다).
   · 하면 안 됨 — 절대규칙 3, API 무차별 탐침. 사람이 누르는 길만 따라간다.
   · 하면 안 됨 — 그 탭은 사람이 쓰던 탭이다. 조회 조건을 함부로 바꾸지 말 것.
   왜 급한가: **06시트 입금액 724건이 전부 빈칸**이고 그것을 채울 원본이 계정별원장이다.
3. **밤샘 대조가 왜 8번 다 실패했나 — 고쳤다(2026-08-07).** 코드 문제가 아니었다.
   전체 대조 한 회차는 지금 **약 2시간** 걸리는데(밴드 캐시가 2,078 → 8,499 글로
   커졌다) `overnight_run` 의 회차 제한이 **45분**이었다. 매번 거의 다 해 놓고 잘렸다.
   · 기본 제한 45분 → **240분**.
   · 출력을 `capture_output` 대신 **`reports/밤샘_대조.log` 파일로 흘린다** —
     시간초과가 나면 subprocess 가 모아 둔 출력을 버려서, 기록에 "시간 초과"
     네 글자만 남고 어느 단계에서 느렸는지 알 수가 없었다.

### ★ 다음 세션이 이어서 할 일 — 엑셀 한 파일로 통합 (2026-08-07 지시)
사용자 지시: **"쿠팡_거래처코드_최신.xlsx 와 쿠팡_확인필요현황_최신.xlsx 를 관리대장에
통합해서 관리하고, 앞으로도 별도의 엑셀 파일은 만들지 말고 관리대장으로만 관리해.
그리고 필요없는 엑셀은 지워."**

이번 세션에서는 **손대지 않았다**(컨텍스트 85%에서 파일 삭제를 시작하지 않는다).
아래는 조사해 둔 것이다 — 처음부터 다시 찾을 필요 없다.

**이미 절반은 만들어져 있다.**
- `findings_sheet.py` 가 관리대장 안에 **『23_확인필요현황』 시트**를 zip 수술로
  넣고 갱신한다. `ledger_db.py:1437` 에서 11:00·15:00 회차에 이미 돈다.
- 그런데 `daily_run.py` 의 "확인필요현황 엑셀 갱신" 단계(`findings_export.py`)가
  **별도 파일도 계속 만든다.** 주석에 이유가 적혀 있다: *"23시트는 평면 목록이라
  유형별 상세열(명세서번호·PO번호·매칭근거·확인방법)을 담지 못한다."*
  → **할 일: 23시트에 그 상세열을 넣고, daily_run 의 별도 엑셀 단계를 뺀다.**
- `customer_index.py:227` 이 `쿠팡_거래처코드_최신.xlsx` 를 openpyxl 로 만든다
  (10열: 캠프명·거래처코드·ERP거래처명·연결방식·원장건수·주소·담당자·연락처·Email·보유장비).
  → **할 일: `findings_sheet.py` 를 본떠 새 시트(예 『27_거래처코드』)로 옮긴다.**
  ※ 관리대장은 **openpyxl 로 열어 save 금지**(차트 파괴). zip 패치 도구만 쓴다.

**지우기 전에 확인할 것** — 삭제는 되돌릴 수 없다. 시트에 내용이 들어간 것을
**눈으로 확인한 뒤에** 지운다. 지금 관리대장 폴더에 있는 엑셀:
`쿠팡_거래처코드_최신.xlsx`(23KB) · `쿠팡_확인필요현황_최신.xlsx`(38KB) ·
관리대장 v524 및 v525~v533.

★ **덤으로 발견 — 11:00 회차가 5분 만에 v525→v533 아홉 개를 만들었다.**
  "하루 두 번만" 규칙은 지켰지만, 한 회차 안에서 **시트마다 새 버전**을 만든다.
  구조 손실은 없다(v524·v533 모두 77파트·29시트·차트5 로 동일 — 확인함).
  하지만 사용자가 "필요없는 엑셀은 지워"라고 한 것에는 이 중간본들도 들어간다.
  → **할 일: 한 회차는 vN+1 **하나만** 만들도록 묶고, 중간본 정리 규칙을 정한다.**
